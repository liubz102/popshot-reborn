# PROGRESS — V0.2 进度看板

> 只保留**当前状态**。流水账进 `sessions/`。
> **每完成一个可验证小步就立刻更新这里。**

最后更新：2026-08-11 · V0.2 会话 09

---

## 🎮 怎么跑起来

| 脚本 | 用途 |
|---|---|
| `start.bat` | **正常游玩。** 精简日志 |
| `start-debug.bat` | 逆协议 / 排查。逐包 dump |
| `stop.bat` | 关掉游戏 + 服务端（按端口精确停，不会误伤别的 Python） |
| `tools\build.bat` | **打发布包**（双击有菜单）。默认第 5 项 = 客户端包 + 服务端包（含 Linux 运行时） |

启动脚本会自检环境、**服务端已在跑就复用**、清掉残留的 `BigShot.exe`，再注入启动客户端。
Python 已内置在 `runtime\python`，目标电脑不需要安装 Python。
细节见 V0.1 的 PROGRESS「怎么跑起来」一节。

---

## 当前位置

**E~I 全部完成并经用户实机验收。J.1 / J.2 完成。J.3 **代码全部写完**：
传输层三条原版通道（`0x040e`/`0x040f` 回退、原版 TCP 中继、客户端自己那条 UDP）、
开局链多人化、战斗逻辑（死亡 / 重生 / 掉落 / 拾取仲裁 / 分数 /
换图等所有人 / 每座位一份结算 / 对战胜负 / 房间开局挡人与二次开局复位）。

**K（两个发布包）本会话做完** —— `tools\build.bat` 一键打客户端包 + 服务端包，
每次打包都会用**包里那份** Python 跑一遍**包里那份**服务端做自检。
`dist\` 里已经有一对批次一致的成品。

**现在只剩实机验证**：⏳ 第 7 / 7b（两台真机同一局）、第 8（服务端包丢到干净机器 /
云主机）、第 9（登录界面出不来的那位复测）。三条都要用**本会话打的新包**。

```text
整体：[■■■■■■■] E ✅ F ✅ G ✅ H ✅ I ✅ → J.1 ✅ J.2 ✅ J.3 ✅(待实机) → K ✅(待实机)
```

| 里程碑 | 状态 |
|---|---|
| **E** 跨会话进度管理文件 | ✅ 完成 |
| **F** 单进程服务端 + 多账号 + 密码 + 票据 | ✅ 完成（含实机：注册 → 登录 → 票据绑定账号）|
| **G** 注册网页 + 存档转移助手 | ✅ 完成（含浏览器实测）|
| **H** server.config + 单机/联机切换 + 登录框改造 | ✅ 完成（含实机：文案 / 切换 / 注册链接）|
| **I** 大厅联机（房间列表 / 加入 / 座位广播 / 聊天） | ✅ **完成**（用户两台电脑跑通 6 / 6b；离开时的模型残留已修，§147）|
| **J.1** 探查战斗内同步 | ✅ **完成 + 实机验证**（§149~§155）|
| **J.2** 决策点：传输层选哪条 | ✅ **用户已拍板 = 原版全部原样还原**（D078）|
| **J.3** 战斗内联机实现 | ✅ **代码完成**（传输层 + 开局链 + 战斗逻辑），双假客户端端到端跑通（§162）；**两台真机的实机验证待用户**（⏳ 第 7 条）|
| **K** 两个发布包 | ✅ **完成**（会话 09）。两个包都打出来了，Windows 侧启停脚本实测跑通；**Linux 侧和「丢到干净机器」待用户**（⏳ 第 8 条）|

★ 现在**「房里另一个人」这类验证不必再占用用户的第二台电脑** ——
`tools/fakeclient.py` 能造出真连接的第二个玩家（§148 / D076）。

## 已完成 ✅

### E — 进度管理文件

`CLAUDE.md` + `.claude/{PLAN,PROGRESS,FINDINGS,DECISIONS}.md` + `sessions/` 建好；
V0.1 关键结论速查表写进 `FINDINGS.md` 开头；
「需要用户实机操作就停下来问」写进 `CLAUDE.md` 和本文件的 ⏳ 区。
三个前置方案与用户确认，记为 D061 / D062 / D063。

### F — 服务端地基（多账号 / 密码 / 票据）

| 新增 / 改动 | 内容 |
|---|---|
| `server/config.py` **新增** | 解析 `server.config`（宽松容错：缺键给默认、脏行只警告）；端口常量；`register_url()` 会给 IPv6 补方括号 |
| `server/netlisten.py` **新增** | `create_listener()`：默认 `::` 双栈，机器没 IPv6 就退回 IPv4；**不开 `SO_REUSEADDR`** |
| `server/tickets.py` **新增** | `TicketStore`：签发 / 解析 / 过期（TTL 600 秒）；同账号再登录作废旧票；`short()` 给日志只露前 8 位 |
| `server/app.py` **新增** | **唯一入口**：一个进程起认证 47611 + 游戏 27799 + 注册页 + 控制通道（仅 loopback） |
| `server/account_store.py` | 加 `register` / `verify`（三态）/ `export_account` / `import_account` / `usernames`；**删掉 `active_account`**；schema 升到 2（老存档照读）；等级下限 `MINIMUM_PLAYER_LEVEL=2` |
| `server/authserver.py` | 真校验密码 + 签发票据；失败时回**客户端认识的 NMCO 错误码 20025 / 20026**，登录框自己就会弹「不存在的帐号」/「密码错误」（§128，不用改 bshook）；**不再往日志打密码**；监听改双栈 |
| `server/gameserver.py` | `0x0100` 改走 `on_game_login()` 凭票据认人，认不出回 `result=3` 断开；同账号重复登录顶掉旧连接；连接表加 `conns_for_user` / `pick_conn`；控制通道加 `who` 和 `--user` |
| `server/run_tests.py` **新增** | 一条命令跑全部测试（内置便携 Python 的 `._pth` 不认 cwd，见文件里的说明） |

★ **对战等级限制已默认解除**：客户端判的是 `[0x72e338] == 1`（恰好等于 1，V0.1 §83），
所以 `player_level()` 下发时把等级抬到 ≥2。**存档里仍是真实等级**，
`experience_bounds()` 照真实等级算，经验条不会变成负的。

### G — 注册网页 + 存档转移助手

`server/web/{__init__.py, server.py, index.html}`。路由：
`GET /`、`POST /api/register`、`POST /api/export`、`POST /api/import`。

- 页面顶部的服务器地址取自请求的 `Host` 头（服务端不知道自己的公网地址），已做 HTML 转义。
- 上传走 JSON 不走 multipart：文件在浏览器里 `FileReader` 读、`JSON.parse` 之后再发。
- 导入判定：没这个用户→直接建；有→必须用表单里的用户名密码校验通过才覆盖，
  且**上传 JSON 里缺的字段一律重置为默认值**。
- **已在真浏览器里走通**：注册成功 → 重名提示「该用户名已存在，请在登录界面直接登录」→
  打开「存档转移助手」→ 两个标签页切换正常，控制台 0 报错。

### H — server.config + 单机/联机切换 + 登录框改造

| 新增 / 改动 | 内容 |
|---|---|
| `server.config` **新增**（包根目录） | `server_address`（v4/v6/域名）+ `server_register_port` + `local_register_port`，带中文注释和三种写法的示例。缺失时由服务端按模板生成 |
| `server/relay.py` **新增** | 本机 TCP 中继：`127.0.0.1:47621 → 远端:47611`、`127.0.0.1:27809 → 远端:27799`。纯字节转发（两层都是有状态流密码，绝不能拆包），`getaddrinfo` 一次吃下 v4/v6/域名 |
| `hook/bshook.c` | ① 单选钮文案 → 「单机游玩」/「联机(服务器地址请改:server.config)」（后者加 `BS_MULTILINE` 放成两行 200×36，否则被裁）；② `det_connect` 按单选钮决定重定向到本机服务端还是中继（**靠端口区分，零状态**，D066）；③ 注册链接文案 →「在服务器 xxx 上注册用户」并随单选钮实时更新；④ **接管注册链接的点击**（原版那条 Static 根本点不动，§125 / D068）；⑤ 配置经环境变量从 `launch.ps1` 传进来 |
| `hook/bshook.c` **修 bug** | DR0 武装循环把毫秒当成了循环次数 → 启动**时有时无地失败**（§124）。改成按 `GetTickCount()` 算时间；握手超时 10 秒 → 15 秒 |
| `tools/launch.ps1` | 只起 `server/app.py` 一个进程；读 `server.config` 起中继（目标变了会重启中继）；把配置设进环境变量；新的中文提示 |
| `tools/shutdown.ps1` | 端口表加上注册页（从 config 读）和中继的 47621 / 27809 |
| `start.bat` / `start-debug.bat` / `stop.bat` | 中文提示改成「登录界面自己选单机 / 联机 + 地址配在 server.config + 先去注册」，**删掉「登录任意账号密码即可」那句** |
| `tools/gui_probe.py` | `enum` 现在连控件的 x/y/w/h 一起打（改文案要先知道装不装得下） |

**实机验收（本机单人部分全过）**：

- 登录框：`单机游玩` / `联机(服务器地址请改:server.config)` 两行显示完整不被裁；
  切到联机后注册链接立刻从「在服务器 localhost 上注册用户」变成「在服务器 127.0.0.1 上…」
- 点注册链接 → 浏览器打开 `http://localhost:27810/`，服务端记到 `[web] ::1 "GET / HTTP/1.1" 200`
  （顺带证明注册页的 `::` 双栈监听通了）
- 注册页建 `alice` → 游戏里用 alice/pw123 登录 →
  `认证通过 票据=d9a3c3e1…` → `gspRepLogin(result=0) 账号='alice' 下发等级=2`

### 测试

```powershell
runtime\python\python.exe server\run_tests.py
```

**294 项全过**（V0.1 结束时 228 项，本次新增 66 项：注册 / 重名 / 密码三态 / 账号隔离 /
**失败码必须是 20025/20026** / 导入要能改**每一个**字段 / 手改的等级要生效 /
「填错了」和「没填」要分开说 /
老存档兼容 / 等级下限不歪曲经验条 / 导出导入四条路径 / 配置解析 / 票据 /
认证服三态 / **票据必须放 s2** / 游戏服凭票认人 / 顶号 / 票据不进日志 /
控制通道不猜账号 / 双栈监听 / 中继字节透传 / 中继端口不撞车 /
注册页四条 HTTP 路径 / Host 头转义）。

### 用户实机反馈的 5 个问题 —— 全部已修（会话 02）

用户在 V0.2 会话 01 的成果上实际玩了一遍，报了 5 条。**5 条全部改完并实机验证过**：

| # | 现象 | 根因 | 改了哪 |
|---|---|---|---|
| 1 | 密码错 / 用户不存在都**毫无反应**，分区单选钮还变灰锁死 | ① 框其实弹了，被 V0.1 遗留的 `MessageBoxW` 抑制吞掉（§129）② 结果码 1/2 客户端不认，只会变成「认证服务器失败」（§128）③ 客户端点过「开始」就把单选钮永久禁用，失败时也不解禁（§130）| `bshook.c` 两个 detour 改成转发；`authserver.py` 回 20025/20026；`poll_login_dialog` 每轮解禁单选钮 |
| 2 | 存档上传填**错**用户名密码，提示却说「请填入用户名和密码」 | 「没填」和「填错」共用了一句话 | `account_store.import_account`：两条路分开，填错回「用户名或密码错误：这份存档属于账号「X」…」 |
| 3 | 改 `level` 再上传，提示成功但 `accounts.json` 里等级没变 | `level` 是**派生字段**，每次读盘都由经验重算（§131）| 新增 `experience_for_import()`：等级高于经验推出来的就把经验补到那一级（D070）。上传成功的提示现在会回显**服务器上真正存下来的**等级/经验/金币 |
| 4 | 点弹窗外面变暗的地方会把弹窗关掉 | `mask` 上挂了 click 关闭 | `index.html` 删掉那个监听器，只认右上角「关闭」 |
| 5 | 点注册链接**弹出两个网页**（死掉的 tiancity + 我们的注册页）| `SS_NOTIFY` 让 `WM_LBUTTONDOWN` 触发了 `STN_CLICKED`，客户端自己的处理器去开死链接（§130）| `link_wndproc` 把三个按下类消息一并吃掉 |

**实机验证**（会话 02，本机）：

- `alice` + 错密码 → 弹框 `登录失败 / 密码错误 (20026)`；
  `nosuchguy` → 弹框 `登录失败 / 不存在的帐号 (20025)`；两次之后单选钮都还能点
- 点注册链接 → 浏览器标签页 **13 → 14**（只多一个），新标签就是我们的注册页
- 注册页跑通「注册 → 导出 → 改 level=5/money/character → 上传」：
  提示 `（当前等级 5、经验 400、金币 1234）`，`accounts.json` 里落的就是这几个数；
  用户名填错 / 密码填错都回「用户名或密码错误」，都不填才回「请填入…」
- 弹窗：点变暗背景**不关**，点「关闭」才关

### 用户局域网实测通过 + 报的 2 个问题 —— 已修（会话 03）

**用户实机验证通过**（对应下面 ⏳ 区的第 3 / 4 / 5 条）：局域网另一台设备打开注册页、
第二台电脑选「联机」连过来、两个账号同时在线 —— **三条全过**。

用户在这基础上报了 2 条，**都已改完并实机验证**：

| # | 现象 | 根因 | 改了哪 |
|---|---|---|---|
| 1 | 被顶号时提示「在无法连接的地方尝试了连接」，很怪 | **文案是客户端自带的**（`Data\Chinese.ini`），我们没写错字。但走错了分支：顶号只关 socket，客户端**9 毫秒后自动重连并重放已作废的旧票据**，服务端回 `result=3` —— 那个码对应的就是这句话（§132）| `tickets.py` 记 `_revoked` 表；`gameserver.on_game_login` 认出「被顶掉的票据」就回 **`result=2`** →「现有连接已断开。请重新尝试连接。」（D071）|
| 2 | 要有连接日志（谁连、谁断、什么 IP）；云服务端包要两套启动脚本 | —— | 新增 `server/eventlog.py`（`[online]` 前缀 + 另存 `logs/online.log`，**和 `--verbose` 无关**，§133 / D072）；两套启动脚本写进 `PLAN.md` 里程碑 K |

**实机验证**（会话 03，本机）：

- 用 `tools/gs_ctl.py raw 0100 …` 把 result=2 / result=3 直接推给真客户端，
  截图对比 —— 两句话和 §132 的表完全对上
  （截图在 `.claude/sessions/2026-08-11-03/`）
- 真登录一次，`logs/online.log` 依次记下
  `认证服 ✓ 认证通过` → `游戏服 + 连上游戏服` → `游戏服 ✓ 登录`；
  再用假的「第二台电脑」顶号，记下 `⚠ 被顶号 …顶它的是 ip=…` 和 `- 断开 …在线 46.1 秒`

★ 这个「公告」框**不是 `MessageBoxW`**（客户端自绘的 D3D 弹窗，`0x40f304`），
bshook 的 MessageBox hook 抓不到，只能截图看。

### ★ hook 的 DR0 修复（别人电脑上登录界面不出现）—— 已合并（会话 03）

用户把包发给别人，那台电脑**启动脚本不报错但登录界面不出现**，弹
「Game guard文件不存在或已变更」。用户先用 GPT-codex 查过，改动放在
`hook_merge\`（三份快照 + 对方的日志 `logs_aaa\`）。

**核对结论：GPT 的诊断和修法都对**，日志能直接证明（§134）：
旧判据「主线程 EIP 不在 ntdll/kernel32/kernelbase/bshook 里」**把主模块自己漏了**，
而注入 APC 一返回主线程正落在**主模块尾部的 ASProtect 壳**（`EIP=0x007b27xx`），
于是注入后 5 毫秒就武装 DR0，随后壳恢复旧 CONTEXT 把它清掉 ——
日志写着「已武装」，`DR0 命中` 永远不出现。我这台机器不炸纯属采样时机的运气。

**冲突情况**：GPT 是在 **V0.1 的 hook** 上改的，而 V0.2 也动过同一个函数
（§124：循环上限按次数算 → 按 `GetTickCount()` 算）。两边改的是**同一个函数**，
但修的是**两个不同的坑**，不互相推翻。已合并，并在 GPT 版基础上又补了三处：

| 补的 | 为什么 |
|---|---|
| 保留 §124 的 **2 秒余量**（`TIMEOUT - 2000`）| GPT 版用满了整个超时，会和 bsloader 抢着报超时，DLL 的失败原因就写不出来 |
| `IsBadReadPtr` → **`VirtualQuery`** | 前者会真去踩一脚，可能把壳的 guard page 异常吃掉 |
| **兜底武装** | 等不到已知签名也照样武装 + 守护。不然遇到没预料到的 exe 变体会**比改之前更差** —— 旧代码好歹还武装了 |

**验证**（会话 03，本机）：

- `hook\test-watchdog.bat`（GPT 写的回归夹具，已收进 `hook/`）→ `[test] PASS`，
  且 DLL 日志里真有 `!! DR0 被清掉了…已补回（第 1 次）`
- 真游戏**冷启动 3/3 全过**：`目标指令已解壳（0 ms / 0 轮，原始 call）` →
  `★GameGuard 校验 @ 0054B0FC：DR0 命中` → 登录框改造完成、单选钮/注册链接都正常

⚠ **对方那台电脑还没复测**，见 ⏳ 区第 9 条。

### 启动脚本的重复文案 —— 已修（会话 04）

用户截图：控制台把「登录界面上可以自己选服务器…」**打了两遍**
（`tools/launch.ps1` 一遍，`start.bat` 里又硬编码了一遍静态版）。

修的时候踩出一个更大的坑（**§135**）：`chcp 65001` 下 cmd 按字符算偏移、按字节 seek，
`.bat` 里**任何**多字节字符（连 `rem` 注释里的）都在累积漂移，漂到某处就把后面的
命令行从中间劈开。**和 CRLF 无关，也不限于括号块**，先改 `goto` 仍然报错。

**定论（D074）**：`start.bat` / `start-debug.bat` / `stop.bat` **只允许 ASCII**，
中文提示全部由 `.ps1` 打印；`launch.ps1` 加脚本级 `trap` 负责说「[启动失败] …」。

**实机验证**（会话 04）：`start.bat` 跑通，那段说明**只出现一次**、无截断；
`stop.bat` 正常停掉 4 个进程。两个 bat 的 RC=0 / RC=1 分支也都用
`sed` 替身 + `cmd /c "... <nul"` 非交互跑过（手法记在 §135）。

### I — 大厅联机：协议已逆完，服务端已实现，**还没实机验证**（会话 04）

**逆向**（全部写进 `FINDINGS.md` §136~§142）：

| § | 查明了什么 |
|---|---|
| §136 | **流读写原语全表**（`0x5d59xx`/`0x5d5axx`）—— 认出这十几个 helper，任何包的线格式都能直接读出来 |
| §137 | `Session` 的线格式 = `0x0303` 的载荷（不含尾部 u16）；房间列表条目就是它 |
| §138 | **`0x0200` 房间列表每项 0x30 字节的字段全部查明**（PROGRESS 上一版留的待办）|
| §139 | `0x0200` **请求**（12 字节）的字段：最后那个 int32 是游戏类型 |
| §140 | **`0x0202` 加入房间**的请求 / 应答 / **6 个结果码**（文案全是客户端自带的）|
| §141 | **聊天 `0x0305`** 两个方向的线格式；显示名留空 = 系统提示 |
| §142 | 大厅包分发跳表 `@0x406332` 全表 |

**实现**：

| 新增 / 改动 | 内容 |
|---|---|
| `server/lobby.py` **新增** | 大厅房间表：`Room` / `Seat` / `Lobby`。**只管模型不碰协议**（不 import gameserver），所以能单独测。建房 / 加入 / 快速加入 / 离开 / 踢人 / 房主转移 / 按游戏类型过滤 |
| `server/gameserver.py` | `LOBBY` 单例；`build_session()` 从 `build_update_session()` 里拆出来复用；`0x0200` 回**真实房间列表**；`0x0202` 加入房间；`0x0205` 快速加入（**成功回 `0x0202`**）；`0x0305` 聊天广播；`0x030b` 客户端方向 = 踢人；`0x0201`/`0x0302` 同步进大厅；`0x0301` 换角色广播给全房间；退房 / 断线 / 被踢都摘座位并广播 |
| `server/test_lobby.py` **新增** | 25 项：模型本身（含 20 线程抢座位的并发用例）|
| `server/test_room.py` **新增** | 45 项：线格式 + 广播时序（进房四连发的**顺序**和**合并成一次 sendall**都钉死了）|

**进房四连发的顺序是硬约束**（§140）：`0x0303` → `0x0202` → `0x0300` → `0x030b`，
且必须合并成一次 `sendall` —— 和建房那一路同因（V0.1 §119 / §120 / D058）。

**实机验证（会话 04，单机一个客户端 + `fakeroom` 假房间，§143）** ——
截图在 `.claude/sessions/2026-08-11-04/`：

| 验了什么 | 结果 |
|---|---|
| 房间列表渲染 | 「组队战 / 무투전 / **待机中(2/6)** / **2号** 对战房」标题·状态·人数·房号全对 |
| §138 留的两个疑问 | **都确认了**：`Session+0x0c`=当前人数、列表项 u8=上限；房间号从 **0** 开始分配（显示 = 值+1） |
| 双击房间进房 | 三个座位的角色 / 昵称 / 等级 / 「房主」标记 / 人物选择头像**全部正确** |
| 聊天 | 客户端发的 `0x0305` 解析对；广播回去渲染成 `Bob : hello from bob`，座位角色头上还冒气泡 |
| 系统提示 | 显示名留空 → `someone joined the room.`，**没有「谁 :」前缀**（§141 确认）|
| 客户端自己建房 | 立刻出现在大厅列表；`0x0302` 的地图名同步进了大厅 |
| 退房 | 房间从列表消失 |

★ 实机顺手逼出一个真缺陷（§145）：`Lobby` 原本按「还剩几条**连接**」判房间是否
解散，应该按「**座位**还有没有人」。已改 + 补回归测试。

★ 新线索（§144，**里程碑 J 的头等大事**）：进房间之后客户端**每 10 秒发两发
`0x0310 gcpStartTcpRelay`（8 字节）**。战斗还没开始就在要中继通道 ——
§122 的推论第一次有了实测支撑。不回它不影响大厅任何功能。

### I — 用户两台电脑验收通过 + 报的 1 个问题已修（会话 05）

**用户实机验证通过**（⏳ 区第 6 / 6b 条）：A 建房 → B 在列表里看到并加入、
互相聊天、各自换角色、**房主退房后「房主」标记转到 B 头上**、B 退房后房间消失、
房主踢人 —— **全过**。（用户顺带发现**单击房间就能加入**，不是双击才行；
这是客户端自己的行为，不是缺陷，不改。）

**用户报的缺陷 —— 已修并实机复验（§147 / D075）**：

| 现象 | 根因 | 改了哪 |
|---|---|---|
| 非房主退房 / 被踢之后，「玩家列表」里的名字没了，但**房间上方蓝天白云那块的角色模型还在** | 离开广播发的是 `0x0301` **action 3**，而客户端里**只有 action 1/2** 会走 `0x405f8f` 销毁座位的 3D 模型；3 走的是 `0x406628`（按座位数据重建），对空座位干脆什么都不做。房主离开那条路歪打正着 —— 它会补发 `0x0300` 全量快照，而 `0x0300` 的处理器顺手把空座位的模型收掉了 | `gameserver.py`：`SEAT_ACTION_LEAVE` 3 → **1**；`build_session_member_update` 那条「1/2 绝不能发」的护栏（V0.1 单人时代的假设）改成只挡跳表不认的码 |

**实机复验**（截图在 `.claude/sessions/2026-08-11-05/`）：

- 对着用户留在屏幕上的那个鬼影直接推一发 `raw 0301 action=1` → **模型当场消失**
- 新工具 `tools/fakeclient.py` 当第二个玩家：bob 进房 → 退房 → 房主那边模型消失 +
  「bob 离开了房间。」；bob 再进来被踢 → 模型消失 +「bob 被房主请出了房间。」
- 反方向：bob 在房里、**房主**退房，假客户端收到的字节是
  `0x0301 action=1 座位=0` → `0x0300 房主座位=1` → 系统提示，顺序内容全对

### ★ 新工具：`tools/fakeclient.py`（§148 / D076）

一台电脑上就能造出**第二个真玩家**（真 TCP + 真认证 + 真票据，不渲染）：

```bash
runtime\python\python.exe tools\fakeclient.py bob pw456 join 0 sleep 3 ^
    chat "hi" shot <真客户端pid> two.png leave sleep 3 shot <pid> after.png
```

收到的每一帧都打印，`0x0301` 还会解出 action 和座位号 ——
「服务端到底广播了哪个 action」终于能在一台机器上逐字节看见。
**里程碑 J 要靠它验战斗同步**（目前只实现了大厅那几个包）。

### J.1 — 战斗内同步查清了，而且是好消息（会话 06）

§122 的推论**一半对一半错**。同步确实不在 gcp/gsp 里，但**不是只能靠 P2P UDP**：
客户端有**两条并行通道**，第二条本来就是「全部塞进已有的游戏服连接」。

| § | 查明了什么 |
|---|---|
| §149 | **双通道架构**：`GameSession::SendToAll`（`0x4077c2`）同一个包既走 UDP 直连、又走 `0x408619`（中继 or `0x040e`→游戏服）|
| §150 | **`0x0410` / `0x040e` / `0x040f` 三个包的线格式** —— V0.2 要实现的全部 |
| §151 | `UdpPacket` **12 字节头** + 每座位 `PktQueue` **按序列号去重**（所以两条通道重复投递是安全的）|
| §152 | 原版中继（`0x0310` / `0x0210` / rcp 三个 opcode）**全貌查清，决定不实现**（D077）|
| §153 | UDP 那一路的细节：bind 7788、`sendto`、地址来自座位上报的**内网 IP** |
| §154 | `GameSession`（`[0x72e29c]`）的字段速查表 |
| §155 | **实机验证**：三段实测全过，见下 |

**实机验证（会话 06，一台电脑，不占用用户的第二台）**：

- 对真客户端推一发 `0x0410`（int32 1）→ **24 毫秒后**它开始以 ~7.8 Hz 发 `0x040e`
- 两个假客户端（alice 建房 / bob 加入）→ 开关、转发、关闭全对，
  bob 发的 15 字节被 alice **逐字节**收到
- 真客户端的**真** `0x040e`（43 字节）转给假客户端，
  12 字节头逐字段和 §151 对上：`magic=0xff 座位=0 目标=广播 序列号=0 内层opcode=0x4001`

**已实现（`server/gameserver.py`）**：`0x0410` 开关（进房够两人就开、掉回一人就关、
必须排在进房四连发**之后**）、`0x040e` → `0x040f` **原样转发**、
`0x0310` 不回 `0x0210`。`tools/fakeclient.py` 加了 `create` 和 `peer` 两条命令。
测试 **389 项全过**（新增 14 项）。

### J.3 已完成的部分（会话 06）

| 块 | 状态 |
|---|---|
| 传输层：`0x0410` 开关 + `0x040e`→`0x040f` 转发 | ✅ 实现 + 实机验证（§155）|
| **开局链多人化** | ✅ 实现 + 单元测试（**还没实机**）|
| **原版 TCP 中继（D078）** | ✅ **实现 + 实机验证**（会话 07，§157~§160）|
| 战斗回显广播 / 结算 / 对战胜负 | ⏳ 未开始 |

**开局链多人化**（`RoomStartGame`，房间级状态机挂在 `lobby.Room.battle`）：

- 房主的 `0x0402` 驱动握手，应答（`0x0401` 倒计时 / `0x0400` 准备开局）
  **广播给全房间**，且**所有人拿同一个 seed** —— 只发房主的话别人连关卡都不加载；
- **所有人都报了 `0x0403`（加载完）才放行**那一发 `0x0402`（一起进 stage 7）；
- 非房主发的 `0x0402` 一律忽略（开局是房主的权力）；
- 等的人中途退房 → 重新算一次，剩下的立刻放行（否则永远卡加载界面）；
- 新人进房 → 上一轮握手作废；
- **单人房的包序列和 V0.1 逐包一致**（有专门的用例钉住）。

### J.3 —— 原版 TCP 中继已完成并实机验证（会话 07）

D078 用户要求的那条路做完了。**RE 先逐指令核对了一遍**（§157），
逼出两条 §152 没写、但会咬人的事实：

| § | 查明了什么 |
|---|---|
| §157 | 中继协议的**全部**线格式（`0x0210` 十八字节的字节序、rcp 六个方向的 opcode 和载荷长度）|
| §158 | ★★ **中继连接一断，客户端自己发 `0x0203` 退出房间**（`OnDisconnected` → `0x406191`），顺带把通道 A 的开关也清 0 —— **断了比没有更糟** |
| §159 | ★★ **`0x0210` 一条连接只能回一次**。客户端收到就无条件新建并覆盖全局指针，旧对象变孤儿，将来那次 `FD_CLOSE` 会把新连接一起带走；而 `0x0310` 是每座位 10 秒一发，**去重责任 100% 在服务端** |
| §160 | 实机验证（见下）|

| 新增 / 改动 | 内容 |
|---|---|
| `server/relayserver.py` **新增** | rcp 中继：帧和加密同 27799、无版本号开场白；票据签发/兑换/去重；opcode 0 注册 / 1 ping / 3 数据；投递「有中继走中继，没中继退回 `0x040f`」（D081）|
| `server/gameserver.py` | `PEER_RELAY` 单例 + `maybe_join_relay()` / `leave_relay()` / `on_start_tcp_relay()`；`0x040e` 也改走 `PEER_RELAY.deliver()`；断线时 `forget()`；`TCP_RELAY_ENABLED` 应急开关 |
| `server/app.py` | 第五个监听器 + `--no-tcp-relay` / `--relay-port` |
| `server/config.py` / `relay.py` | `PEER_RELAY_PORT=27798` / `RELAY_PEER_PORT=27808`；本机中继加第三条隧道 |
| `hook/bshook.c` | `popshot_map_port` 加一行 `27798 → 27808`（**已重编，`test-watchdog.bat` PASS**）|
| `tools/launch.ps1` / `shutdown.ps1` | 端口表和环境变量跟上 |
| `tools/fakeclient.py` | 收到 `0x0210` 会**像真客户端一样**去连中继；新命令 `waitrelay` / `rpeer` |
| `server/test_relayserver.py` **新增** | 23 项（帧编解码 / 票据去重 / 真 socket 端到端 / 回退 / ping / 不认识的包绝不断连）|

**实机验证（会话 07，一台电脑，§160）**：

- 真客户端 `0x0310` → 服务端 `0x0210` → **5 毫秒后它就连过来并 `rcpRegister`**；
- **它真的改走中继了**：整条连接上 `0x040e` 只出现过一次（中继建起来之前那一发），
  之后 235 份同步数据全从中继出去，一份都没走 `0x040f`；**真客户端会回我们的 ping**（发 5 回 5）；
- 重复的 `0x0310`（每 10 秒一发）**没有**换来第二发 `0x0210`（§159 的去重生效）；
- `gs_ctl.py raw 0211` 拆连接后，客户端**没退房、没弹框**，还在房里当房主
  —— 从反面印证了 §158「只有 `OnDisconnected` 才会踢人，析构不会」。

### J.3 战斗逻辑 —— 本会话完成（会话 08）

**动手前先做了 RE**（PROGRESS 上一版的硬要求），结论写进 **§161**：
六个包**全部可以广播，一个都不会崩**，因为客户端全部按跨机器一致的 id 找目标。
最关键的一条：**玩家角色的对象句柄 = 座位 × 100000 + 100001**
（`0x405f02` 写死的公式），六台机器上算出来一模一样。

| 新增 / 改动 | 内容 |
|---|---|
| `server/lobby.py` | `Room` 多一个空槽 `quest`（和 `battle` 分开：前者管「开起来之后」，回房间时整个丢掉）|
| `server/gameserver.py` **新增 `RoomQuest`** | 房间级的一局：掉落物句柄分配器、拾取仲裁表、死亡去重、每座位死亡数、通关标志、换图等谁、`settled`、`ranking()` |
| `server/gameserver.py` 新增三个取数口 | `quest_state()` / `battle_members()` / `battle_seats()` / `battle_broadcast()` —— **战斗处理器只有一条代码路径**（D084）|
| `0x0408 → 0x0406` | **广播全房间** + 按 `(句柄, 报的死亡次数)` 去重（怪是各机器各自模拟的，同一次死会被报两遍）|
| `0x0413 → 0x0419` | **广播全房间**（不广播的话别人屏幕上你一直躺着）|
| `0x0406 → 0x0404` | **广播** + 句柄改**房间级**分配（各连接各分配会撞号、后到的覆盖先到的）|
| `0x0407 → 0x0405` | **广播** + ★ **服务端仲裁**：一件东西只能被一个人捡到，晚到的那一发**一个包都不回** |
| `0x0410 → 0x0415` | **广播全房间**（不广播队友那一行分数永远是 0）|
| `0x0411 → 0x0417` | **广播全房间**（全房间一起换图）+ 同一张图的第二发请求不重复广播 |
| `0x0412 → 0x0418` | ★ **等所有人加载完才放行**（和开局链的 `0x0403` 同一个套路）；等的人退房了就重新算 |
| `0x0417 gcpMarkQuestSuccess` | 记在**房间**上，谁报到都算通关（合作模式关底是大家一起打的）|
| `0x040f → 结算` | ★ **房间级只结算一次**；每人收到「每个在座座位一份 `0x0309`」+「自己那份 `0x0411`」（D082）；每人按自己的分数入账 |
| **对战胜负** | 非闯关房按本局分数排名填 `0x0309` 尾部数组：最高分 `1`、其余 `-1`、全场 0 分不判（D083）|
| `current_quest()` | 先读**大厅那一份**描述符 —— 客人手上没有 `self.room`，不改的话「客人打通了关卡但难度没解锁」|
| 房间状态 | 所有人进 stage 7 → `游戏中`（大厅列表跟着变 + `Lobby.join` 挡人）；结算看完回房间 → `待机中` + `battle.reset()` + `quest=None`（**不复位的话第二局根本开不起来**）|
| `tools/fakeclient.py` | 加了整套战斗命令（`ready`/`loaded`/`die`/`respawn`/`drop`/`pickup`/`score`/`cleared`/`nextmap`/`maploaded`/`endquest`/`resultdone`），收到的战斗包也会解成人话 |
| `server/test_battle.py` **新增** | 41 项 |

**实机验证（会话 08，一台电脑，两个假客户端 + 真服务端，§162）** ——
掉落广播、死亡广播、**拾取仲裁**（`拾取被拒 … 已经被座位 1 捡走了`，零回包）、
分数双向广播、**换图等所有人**（`还在等 1 人，先不放行`）、
**每座位一份结算**（两份 `0x0309` + 各自的 `0x0411`）、
**一局只结算一次**、各按各的分入账、**客人也解锁了难度**、
房间「游戏中 ↔ 待机中」、**第二局能再开** —— 全过。

⚠ 这些验的都是**假客户端收到的字节**。「真客户端收到之后会不会照做」
必须两台真机在同一局里，见 ⏳ 区第 7 条。

### K — 两个发布包（会话 09 完成）

一个入口：**`tools\build.bat`**（双击有菜单，默认第 5 项 = 两个包都打 + 压缩 +
带 Linux 运行时）。命令行也行：`tools\build.bat -Client -Server -Zip -Force`。

| 新增 / 改动 | 内容 |
|---|---|
| `tools\build-common.ps1` **新增** | 两个打包脚本的共用底座：文件选择、编码转换、冒烟自检、ZIP / tar.gz、`BUILD.txt` |
| `tools\build-portable.ps1` **重写** | 客户端包。★ 上一版的手写白名单漏了 **V0.2 全部新增的 server 模块**，打出来的包在别人电脑上根本起不了服务端（§163.1）。改成**反向排除**（D086）|
| `tools\build-server-package.ps1` **新增** | 服务端包（K.2）。Windows / Linux 两套启停脚本、可选的 Linux 运行时、ZIP + tar.gz |
| `tools\build-menu.ps1` + `tools\build.bat` **新增** | 唯一入口。取代 `build-portable.bat` / `build-portable-menu.ps1`（**已删**）|
| `tools\server-package\` **新增** | 服务端包的模板：`start/start-debug/stop` 的 `.bat` + `.sh`、`serverctl.ps1` / `serverctl.sh`、`server.config`、`README.md` |

**三条值得记住的**：

1. **每次打包都会自检**（D087）：用**包里那份** Python 跑**包里那份** `server\app.py`，
   四个监听器全起来 + 注册页返回 200 才算成功。用的是另一套端口
   （47711 / 27899 / 27898 / 27910），开发服务端在跑也不打架。
   缺文件这类错在打包机上是看不出来的 —— 这一步就是为了把它拦在这里。
2. **两个包共用一个批次号**，`BUILD.txt` 里还有一行「共用服务端代码」哈希（D089）。
   D079 要求成对发，这两行让「有没有拿错包」变成可核对的事实。
3. **Linux 运行时以未解压的 `.tar.gz` 随包发**（D088），首次 `./start.sh` 时在
   Linux 上自解。它**存在项目的 `runtime-linux\` 里**（和 `runtime\` 同构，
   跟着 git 走，D090）—— 打包时直接 copy、**不联网**；只有目录里没有时
   `-LinuxRuntime download` 才去取一次。当前钉的是 **3.14.7 稳定版**
   （和 Windows 运行时同大版本），预发布版被匹配规则挡掉（§164）。
4. **新 clone 也能直接打包**：根目录的 `server.config` 不进仓库（是玩家自己填的
   地址），`build-portable.ps1` 发现它不在就照 `server\config.py` 的模板现生成。

**实机验证（会话 09，本机）**：

- 服务端包：`start.bat` → 四个端口全起、打印本机内网 IP 和防火墙命令；
  重复启动被拒并说清是谁占的端口；`stop.bat` 停干净；再 stop 一次说「无事可做」；
  `start-debug.bat` 走 `--verbose` 分支。中文提示无截断（`.bat` 保持纯 ASCII）
- 客户端包：包内 `tools\launch.ps1 -NoGame` 起服务端 + 三条中继全部就位；
  包内 `stop.bat` 两个进程都停掉
- 两个包的 `BUILD.txt` 批次号和代码哈希一致
- Linux 运行时下载路径：**实跑通过**，选中 3.14.7 稳定版（不是 3.15.0rc1），
  SHA-256 校验通过；`auto` 复用缓存；**把旁证 `.sha256` 藏起来后 `auto` 拒绝用
  那份缓存**（打印「忽略未校验的缓存文件」）
- `dist\`：客户端包 391.4 MiB（zip 365.4）、服务端包 56.5 MiB
  （zip 45.3 / tar.gz 45.4，含 Linux 运行时）

⚠ **没验的**：Linux 上跑（手上没有 Linux 环境）、把包丢到干净机器上跑 ——
⏳ 第 8 条。

## 正在做 🔨

**没有在做的事。** J.3 和 K 的代码都完成，等用户的实机验证（⏳ 第 7 / 7b / 8 / 9 条）。

## 下一步 ⏭

1. **等 ⏳ 第 7 条的实机结果**，再决定要不要动 D082 的那个代价
   （队友那一行的「分数 / 生命」显示 0；改成每座位一份 `0x0411` 即可，
   一个循环的事，但要先确认重复调 `0x4087f0` 是安全的）。
2. **把 `dist\` 里那对包发出去测**（⏳ 第 7 / 7b / 8 / 9 条）。
   包已经打好且批次一致；**客户端包和服务端包必须一起发**（D079）。
3. **`0x020d gcpReqUserList`**：实测发现它的服务端方向**不是用户列表** ——
   处理器 `0x553c5f` 把包里的两个字符串塞进一个 0x588 字节的弹窗对象（`0x40f87c`）。
   大厅右侧那个「玩家列表」面板的数据源是别的包，还没找到。
   现在回空表客户端照常工作，**优先级低**。
4. `0x0311` 那六项的业务语义（§161 查明是**六个座位**、纯表现层，但填什么数未知）。
   **优先级低**，回全 0 客户端照常工作。

★ 改战斗逻辑前先读 **§161**（每个包为什么能广播、句柄公式、三个档的胜负值），
以及本文件末尾「服务端现状」里的三条硬约束。

## 当前卡点 🚧

**没有阻塞性卡点。**

J 原本是整个 V0.2 最大的风险（PLAN 写着「唯一有真实失败风险的一段」），
**J.1 之后这个风险基本消失了** —— 传输层不需要 NAT 穿透，
复用玩家已经连着的那条 TCP 即可（§149）。传输层现在三条原版通道全在。

仍在的次级风险两条：

1. **§158：中继连接一断，客户端会自己退出房间。** 这是客户端写死的行为
   （`OnDisconnected` → `0x406191` 发 `0x0203`），我们只能绕不能改。
   实现上已经按「绝不主动关已注册的连接、要拆只用 `0x0211`」写死，
   并留了应急开关 `app.py --no-tcp-relay`（关掉就退回 `0x040e`，D078 的反悔条件）。
   ⚠ **客户端包和服务端包必须一起发**：新服务端会让客户端去连 27798，
   老客户端包联机时那里没人听 → 退房（D079）。
2. 带宽全压在服务端上（一个人 ~8 Hz × 43 字节，六人房 30 份/tick）。
   中继和 `0x040e` 两条路都是同一台机器转发，这一点没变。

---

## ⏳ 待用户验证

> agent 自己做不了的实机验证登记在这里。规则见 `CLAUDE.md` 的「需要用户实机操作时停下来问用户」。
> 每条写清「怎么做 / 期望结果 / 当前状态」。用户回话后**立刻**更新。

| # | 里程碑 | 怎么做 | 期望结果 | 状态 |
|---|---|---|---|---|
| 1 | F/H | **本机单人链路** | 见下 | ✅ **已由 agent 实机跑通**（会话 01）|
| 2 | F | 登录失败的提示 | 密码错 → 「登录失败 / 密码错误 (20026)」；未注册 → 「登录失败 / 不存在的帐号 (20025)」 | ✅ **已由 agent 实机验证**（会话 02，§128 + §129）—— 文案是客户端自带的，但 bshook 那个吞框的 detour 必须先拆掉 |
| 3 | G | 在**另一台设备**（手机 / 另一台电脑的浏览器）打开 `http://<本机内网IP>:27810/`，走一遍注册 → 存档导出 → 改一改再上传 | 页面能打开（说明 `::` 双栈对局域网生效），每一步都有中文结果提示 | ✅ **用户已验证通过**（2026-08-11）|
| 4 | H | 局域网第二台电脑上解压同一份客户端，把它的 `server.config` 的 `server_address` 改成第一台的内网 IP，登录界面选**「联机」**进游戏 | 能连上第一台的服务端；注册链接打开的是**第一台**的注册页 | ✅ **用户已验证通过**（2026-08-11）|
| 5 | F | 承接 4：两台机器**各用一个账号同时在线** | 两边各自看到自己的等级 / 金币 / 角色，互不干扰；同一个账号重复登录会把先登的顶掉 | ✅ **用户已验证通过**（2026-08-11）。顶号提示的文案问题已单独修掉，见 §132 |
| 6 | I | **★ 两台电脑各登一个账号**（第二台把 `server.config` 指向第一台，登录界面选「联机」）。依次做：① A 建房 → B 在房间列表里看到并双击加入 ② 两边互相打字聊天 ③ 各自点「人物选择」换个角色 ④ **A（房主）先退房**，看 B 那边 ⑤ B 也退房 | ① B 进得去；两边的「玩家列表」都能看到对方的昵称和等级，房间里也能看到对方的角色 ② 两边都看到 `对方昵称 : 内容`，还有「XX 进入了房间。」这类系统提示 ③ 对方的 3D 预览跟着换 ④ **B 那边的「房主」标记要转到 B 自己头上**，且看到「A 离开了房间。」 ⑤ 房间从列表里消失 | ✅ **用户已验证通过**（2026-08-11，五步全过）。顺带发现**单击房间就能加入**（客户端自己的行为，不是缺陷，不改）|
| 6b | I | 承接 6：B 在房间里时，A（房主）点 B 头像旁边的踢人按钮 | B 被弹回大厅，A 那边看到「XX 被房主请出了房间。」 | ✅ **用户已验证通过**（2026-08-11）|
| 6c | I | 承接 6 / 6b：**离开或被踢之后，房间上方那块天空里的角色模型有没有跟着消失** | 玩家列表里的名字和天空里的模型**同时**消失 | ✅ **已修 + agent 实机复验**（§147 / D075；会话 05 用 `tools/fakeclient.py` 造第二个玩家跑通退房和被踢两条路）。用户下次开两台电脑时顺手再看一眼即可 |
| 7 | **J.3** | **★ 两台电脑各登一个账号进同一个房间，房主选一个闯关地图按「开始」，两个人一起把这一关打完**（打不通就让时间耗光 / 生命耗光，照样会进结算）。然后：① 打的过程中互相看一眼**对方的角色有没有在动**、**打死怪掉出来的东西两边是不是都看得见**、**同一件东西是不是只有一个人捡得到**、**对方死了那边是不是真的倒下**；② 到了**结算界面**截一张图；③ 结算完回到房间，房主**再按一次「开始」**看第二局开不开得起来；④ 再开一间**对战房**打一局，看胜负和 BGM。最后把两边的 `logs\server.out` 发回来 | ① 都能看到；一件东西只有一个人捡到；② 结算界面上**两行都有人**（两个昵称、各自的「经验值 +N / 金币 +N」），左上角写「完成」或「未完成」；③ 第二局能开起来；④ 分高的那边写 CLEAR + 胜利曲，分低的写 FAILED + 失败曲 | **待用户**。★ 服务端侧已经用两个假客户端逐字节跑通（§162：广播 / 仲裁 / 换图等人 / 每座位一份结算 / 只结算一次 / 各按各的分入账 / 第二局能开）。**唯一还没证的是「真客户端收到之后会不会照做」** —— 那必须两个真客户端在同一局里。⚠ **已知会不对的一处**：结算界面上**队友那一行的「分数 / 生命」会是 `0 / 3`**（D082 的代价，别当成 bug 报，但请确认其余几行是对的） |
| 7b | J.3 | **★ 承接 6：两台电脑各登一个账号进同一个房间，什么都不用做，就待着**。然后把**两边**的 `logs\server.out` / 客户端那台的 `logs\bshook_*.log` 发回来 | 服务端日志里应当出现 `← 回 0x0210 gspJoinRelay 127.0.0.1:27798` 和 `[relay] ✓ 注册成功`，**每个账号各一次**；不应出现任何人莫名其妙退出房间。若第二台电脑那边**一进房就被弹回大厅**，说明它的中继连不上（§158），请立刻告诉我 —— 服务端加 `--no-tcp-relay` 就能绕开 | **待用户**。⚠ 前提：**两台电脑都要用 `dist\PopShot-portable-win64.zip`（批次 `20260811-224143`）**，服务端也要用同批次的那个（`bshook.dll` 的端口映射和服务端的中继端口是配套的，D079）。两边 `BUILD.txt` 的「打包批次」一致就说明拿对了 |
| 8 | **K** | **★ 包已经打好了**（`dist\PopShot-server.zip` / `.tar.gz`，批次 `20260811-224143`）。把它丢到**干净的 Windows 机器**或**Linux 云主机**上解压直接跑：<br>· Windows：双击 `start.bat`<br>· Linux：`tar -xzf PopShot-server.tar.gz && cd PopShot-server && chmod +x *.sh tools/*.sh && ./start.sh`<br>然后另一台电脑用**同批次**的客户端包（`dist\PopShot-portable-win64.zip`）改 `server.config` 指过去 | 不装任何东西就能开服；启动后打印四个端口和本机地址；玩家能打开注册页注册、游戏里选「联机」登录、进房间。**Linux 上会先自动解开 `runtime-linux/*.tar.gz`**（如果打包时带了；本次默认没带，会退回系统 `python3`，要求 3.10+） | **待用户**。⚠ Linux 那条路 agent 没法验（手上没有 Linux 环境），失败请把 `logs/server.err` 和 `./start.sh` 的完整输出发回来。云主机记得在**安全组**里放行 47611 / 27799 / 27798 / 27810 |
| 8b | K | 承接 8：确认 Linux 上**真的能自解并跑起来** —— 第一次 `./start.sh` 应当先打印「[运行时] 第一次启动，正在解开包内的 Linux Python」 | 解开后用的是包内的 `runtime-linux/python/bin/python3`（启动横幅里写「Python：包内自带 Python 3.14.7」），不再依赖系统 `python3` | **待用户**。★ 下载和打包这一侧 agent 已实跑通过（§164）：选中 **3.14.7 稳定版**、SHA-256 校验通过、tarball 已随包发出（`dist\PopShot-server.tar.gz` 45.4 MiB）。**没验的只是「Linux 上解开之后能跑」** —— 手上没有 Linux 环境 |
| 9 | — | **★ 把重新打的包再发给「登录界面出不来」的那位**（用 `dist\PopShot-portable-win64.zip`，批次 `20260811-224143`），让他重跑一次，并把 `logs\bshook_*.log` 发回来 | 日志里出现 `HWBP 目标指令已解壳（… ms / … 轮，原始 call）…DR0 已武装` **和** `★GameGuard 校验 @ 0054B0FC：DR0 命中`，登录界面正常出现。若日志里出现 `!! DR0 被清掉了…已补回（第 N 次）` 说明守护真的救了场（也算成功）；若出现 `兜底已武装` 说明那台机器解壳走了别的路，**请务必把日志发回来** | **待用户** |
| 10 | — | 顺带请那位确认：被顶号时弹的是不是「现有连接已断开。请重新尝试连接。」 | 不再是「在无法连接的地方尝试了连接。」 | **待用户** |
| 11 | **J.2** | **决策点：战斗内同步的传输层选哪条** | —— | ✅ **用户已拍板（2026-08-11）：「再实现一遍原版 tcp 中继，原版的所有连接方式以及回退方式都原样还原」**，记为 **D078** |

### ✅ J.2 已拍板：**原版的连接方式和回退方式全部原样还原**（D078）

用户原话：「再实现一遍原版 tcp 中继，原版的所有连接方式以及回退方式都原样还原」。
**保真优先** —— 这是把停运 15 年的游戏抢救回来，「和原版一样地跑」本身就是目标。

于是传输层要**三条都在**：

| 通道 | 状态 |
|---|---|
| UDP 直连（客户端自己开的，我们从没干预）| ✅ 天然原样 |
| **原版 TCP 中继**（`0x0310`→`0x0210` + rcp 协议）| ⏳ **待实现**，RE 已全做完（§152 + §156）|
| `0x040e`/`0x040f` 服务端转发（**原版的回退路径**）| ✅ 已实现并跑通（§155）|

★ **D077 只被推翻了「不实现中继」那一半**，`0x040e` 那条路要保留 ——
它不是权宜之计，就是原版 `0x408619` 在「没有中继连接」时走的分支。

**第 1 条（已跑通的部分，会话 01 实机）**：

- 干净冷启动 3/3 全部 `DR0 已武装 → DR0 命中 → 登录框已改造`
- 登录框两个单选钮文案正确、不被裁；切换时注册链接文字实时跟着变
- 点注册链接 → 浏览器打开本机注册页 → 注册 `alice` → 游戏里登录 →
  票据认出账号 → `gspRepLogin(result=0) 账号='alice' 下发等级=2`

---

## 服务端现状（V0.1 遗留，V0.2 要改造的起点）

**当前是单连接、单活动账号的假服务端**：

- `server/authserver.py` 收到登录包后把用户名写进 `accounts.json` 的 `active_account`，
  **密码完全不校验**，未知账号自动建号；
- `server/gameserver.py` 处理 `gcpReqLogin` 时读 `active_account` 定账号；
- 房间列表 `0x0200`、频道用户列表 `0x020d` 都是**硬编码的空表**；
- 全部战斗包只服务「我一个人」，`register_conn` / `latest_conn()` 是单例思路；
- 两个服务端是**两个独立进程**，由 `tools/launch.ps1` 分别拉起。

已实现并有测试覆盖的应答清单、账号 JSON 的字段含义、以及**一长串「绝对不要做」的禁忌**，
全部在 V0.1 的 `PROGRESS.md` 里，改造前**必读**：

```text
develop_history/V0.1_基础单机功能开发/.claude/PROGRESS.md
```

其中对 V0.2 影响最大的三条硬约束（改多人广播时极易违反）：

1. **建房四连发（`0x0303`+`0x0201`+`0x0300`+`0x030b`）和「回房间」三连发
   （`0x0403`+`0x0600`+`0x030b`）必须合并成一次 `sendall`**（V0.1 §120 / D058）。
   多人广播时**每个连接各自合并一次**，不要跨连接攒。
2. **`0x030b` 必须排在 `0x0300` 之后**，否则 11 个商城角色一个都出不来（V0.1 §119 / D057）。
3. **`0x0303` 的房间状态字段必须是 2**（`SESSION_STATUS_WAITING`），否则房间一片黑（V0.1 §102）。

## 测试

```powershell
runtime\python\python.exe server\run_tests.py
```

★ **不要**用 `..\runtime\python\python.exe -m unittest test_gameserver`：
内置便携 Python 的 `python314._pth` 里那个 `.` 指的是 **python.exe 所在目录**，
不是当前工作目录，会 `ModuleNotFoundError`。`run_tests.py` 就是为这件事存在的。
（系统 Python `C:\Python314\python.exe -m unittest ...` 在 `server/` 里照常能跑。）

V0.1 结束时 228 项，当前 **467 项全过**（会话 08 新增 41 项：
`server/test_battle.py` —— 死亡/重生广播、掉落物房间级句柄、**拾取仲裁**、
分数广播、换图等所有人、每座位一份结算、只结算一次、各按各的分入账、
对战胜负三档、房间「游戏中 ↔ 待机中」与第二局、`RoomQuest` 本身）。
V0.2 的每一步都不许让它变红。

hook 那边另有一条回归夹具（不进 `run_tests.py`，它要编 32 位程序 + 真注入）：

```bash
hook\test-watchdog.bat
```

DR0 被外部清掉后守护线程能不能补回来 —— 见 §134。
