# PROGRESS — V0.2 进度看板

> 只保留**当前状态**。流水账进 `sessions/`。
> **每完成一个可验证小步就立刻更新这里。**

最后更新：2026-08-10 · V0.2 会话 01

---

## 🎮 怎么跑起来

| 脚本 | 用途 |
|---|---|
| `start.bat` | **正常游玩。** 精简日志 |
| `start-debug.bat` | 逆协议 / 排查。逐包 dump |
| `stop.bat` | 关掉游戏 + 服务端（按端口精确停，不会误伤别的 Python） |

启动脚本会自检环境、**服务端已在跑就复用**、清掉残留的 `BigShot.exe`，再注入启动客户端。
Python 已内置在 `runtime\python`，目标电脑不需要安装 Python。
细节见 V0.1 的 PROGRESS「怎么跑起来」一节。

---

## 当前位置

**V0.1（基础单机）已完工归档。V0.2 联机开发刚开始。**

```text
整体：[■□□□□□□] E 进行中 → F → G → H → I → J → K
```

| 里程碑 | 状态 |
|---|---|
| **E** 跨会话进度管理文件 | 🔨 进行中 |
| **F** 单进程服务端 + 多账号 + 密码 + 票据 | ⏸ 未开始 |
| **G** 注册网页 + 存档转移助手 | ⏸ 未开始 |
| **H** server.config + 单机/联机切换 + 登录框改造 | ⏸ 未开始 |
| **I** 大厅联机（房间列表 / 加入 / 座位广播 / 聊天） | ⏸ 未开始 |
| **J** 战斗内联机（J.1 探查 → J.2 问用户 → J.3 实现） | ⏸ 未开始 |
| **K** 两个发布包 | ⏸ 未开始 |

## 已完成 ✅

- **E.1 / E.2 / E.3**：`CLAUDE.md` + `.claude/{PLAN,PROGRESS,FINDINGS,DECISIONS}.md`
  + `sessions/` 建好；V0.1 关键结论速查表写进 `FINDINGS.md` 开头；
  「需要用户实机操作就停下来问」写进 `CLAUDE.md` 和本文件的 ⏳ 区。
- 三个前置方案已与用户确认，记为 D061 / D062 / D063。

## 正在做 🔨

**里程碑 E 收尾 → 进入 F。**

F 的第一步是 `server/config.py` + `server/app.py`（单进程 + 双栈监听），
因为后面所有东西（注册页、中继、多账号）都挂在它上面。

## 下一步 ⏭

1. **F.1** `server/config.py`：解析 `server.config`。
2. **F.2** `server/app.py`：一个进程起认证 47611 / 游戏 27799 / 网页 27810 /
   控制通道 27800（仅 loopback），前三个 `::` 双栈。
   ★ 把 `authserver.main()` / `gameserver.main()` 里的监听逻辑抽成可被调用的函数，
   **`server/test_gameserver.py` 的 228 项测试不许断**。
3. **F.3** `account_store`：`register` / `verify` 三态 / `export_account` / `import_account`，
   删掉 `active_account`。
4. **F.4** 票据：认证服签发 → `CULoginReplyPacket` 的 `s1` → 客户端 `gcpReqLogin` 的首个 wstring。
   ⚠ **要实测确认客户端转发的是 `s1` 还是 `s2`**：先各填一个可区分的测试串看游戏服收到哪个。

## 当前卡点 🚧

**没有阻塞性卡点。**

已知的最大**未来**风险是里程碑 J：原版的战斗内位置同步很可能不走游戏服，
而是客户端之间的 UDP 直连（`UDPBinder` / `gspToggleUdpClientCommunication`）
加一条 TCP 中继兜底（`gcpStartTcpRelay` / `gspJoinRelay` / `RelayConnection`）。
云端服务器在 NAT 后大概率打不通 UDP 直连。**用户已定：先探查（J.1）再拍板（J.2）。**

---

## ⏳ 待用户验证

> agent 自己做不了的实机验证登记在这里。规则见 `CLAUDE.md` 的「需要用户实机操作时停下来问用户」。
> 每条写清「怎么做 / 期望结果 / 当前状态」。用户回话后**立刻**更新。

| # | 里程碑 | 怎么做 | 期望结果 | 状态 |
|---|---|---|---|---|
| 1 | F | 局域网第二台电脑上用另一个账号登录（第一台保持在线） | 两边各自看到自己的等级 / 金币 / 角色，互不干扰 | 未开始 |
| 2 | F | 用错误的密码登录一次；用没注册过的用户名登录一次 | 分别弹「密码错误」和「该用户尚未注册」类的提示 | 未开始 |
| 3 | G | 浏览器打开注册页 → 注册 → 用新账号登录游戏 → 回注册页导出存档 → 改一改再导入 | 每一步都有中文结果提示；导入后游戏里的数据跟着变 | 未开始 |
| 4 | H | 登录界面选「单机游玩」进一次；改 `server.config` 指向局域网另一台，选「联机」再进一次 | 两次都能进大厅；注册链接分别打开 localhost 和远端的注册页 | 未开始 |
| 5 | I | 两台电脑各登一个账号，A 建房，B 在房间列表里看到并加入 | 双方座位 / 昵称都对；互相能看到对方发的聊天 | 未开始 |
| 6 | J | 两台电脑同一个房间，任务模式合作打完一关；再打完一局对战 | 双方都看到对方在动、都能捡到东西、结算两行数值都对 | 未开始 |
| 7 | K | 服务端包丢到干净机器 / 云主机解压直接跑；另一台电脑连上去 | 不装任何东西就能开服；客户端能注册、登录、进房间 | 未开始 |

---

## 服务端现状（V0.1 遗留，V0.2 要改造的起点）

**当前是单连接、单活动账号的假服务端**：

- `server/authserver.py` 收到登录包后把用户名写进 `accounts.json` 的 `active_account`，
  **密码完全不校验**，未知账号自动建号；
- `server/gameserver.py` 处理 `gcpReqLogin` 时读 `active_account` 定账号；
- 房间列表 `0x0200`、频道用户列表 `0x020d` 都是**硬编码的空表**；
- 全部战斗包只服务「我一个人」，`register_conn` / `latest_conn()` 是单例思路；
- 两个服务端是**两个独立进程**，由 `tools/launch.ps1` 分别拉起。

已实现并有测试覆盖的应答清单、账号 JSON 的字段含义、以及**一长串「绝对不要做」的禁忌**，
全部在 V0.1 的 `PROGRESS.md` 里，改造前**必读**：

```text
develop_history/V0.1_基础单机功能开发/.claude/PROGRESS.md
```

其中对 V0.2 影响最大的三条硬约束（改多人广播时极易违反）：

1. **建房四连发（`0x0303`+`0x0201`+`0x0300`+`0x030b`）和「回房间」三连发
   （`0x0403`+`0x0600`+`0x030b`）必须合并成一次 `sendall`**（V0.1 §120 / D058）。
   多人广播时**每个连接各自合并一次**，不要跨连接攒。
2. **`0x030b` 必须排在 `0x0300` 之后**，否则 11 个商城角色一个都出不来（V0.1 §119 / D057）。
3. **`0x0303` 的房间状态字段必须是 2**（`SESSION_STATUS_WAITING`），否则房间一片黑（V0.1 §102）。

## 测试

```powershell
Set-Location server
..\runtime\python\python.exe -m unittest test_account_store test_gameserver
```

V0.1 结束时 **228 项全过**。V0.2 的每一步都不许让它变红。
