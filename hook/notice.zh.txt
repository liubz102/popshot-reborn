; ==========================================================================
;  登录界面公告框的文案原稿（反倒卖声明）
;
;  ★ 这个文件**不会进任何发布包** —— 两个打包脚本都只按名字拷 tools\ 下的
;    几个脚本，hook\ 只拷 hook\bin\。改完这里必须重跑 hook\build.bat：
;    tools\gen_notice_h.py 会把它编成 HTML、混淆后写进 hook\notice_blob.h，
;    再由 bshook.c 编译进 bshook.dll。**包里没有任何明文**，倒卖者改不掉。
;
;  编码：UTF-8 无 BOM，行尾 LF。
;
;  行首标记（标记后跟一个空格）：
;    ;        注释行，不输出
;    #        标题行（大号加粗深蓝）
;    !        警示行（红色加粗）
;    @ 左|右  两列表格行（左列灰色不换行，右列深蓝加粗）
;    空行     段间距
;    其它     普通正文行
;
;  另外两条自动规则：
;    · 值末尾挂着的「（补充说明）」会自动降成小字灰字；
;    · 文本里的 http(s) 网址会自动变成**可点链接**，点了开系统浏览器
;      （靠 bshook 接管 IDocHostUIHandler::TranslateUrl，见 FINDINGS §88）。
;
;  ⚠ 公告框只有 533×260 像素，而且原版把滚动条关死了（DOCHOSTUIFLAG_SCROLL_NO）
;    —— **写太长是直接裁掉，不是出滚动条**。改完必须实机看一眼。
;  ⚠ 内嵌 IE 跑在 quirks 模式：表格单元格**不继承字号**，CSS 里得显式写。
;  ⚠ 右键菜单原版就没有（ShowContextMenu 回 S_OK），玩家只能拖选 + Ctrl+C。
; ==========================================================================

# 《炮炮火枪手》复活工程已在 Github 开源 · 免费提供给所有人游玩
# 永久免费 · 禁止倒卖

! ⚠ 如果你这份游戏包是花钱买来的，说明你被骗了
! 　　—— 请立即要求退款，并向交易平台举报该商家。

@ 免费下载（唯一官方地址）|https://github.com/liubz102/popshot-reborn
@ 唯一玩家交流群 QQ|703732744　（群内有群主提供的云服务器，可免费多人联机）
