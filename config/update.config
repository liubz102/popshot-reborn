# ============================================================================
#  炮炮火枪手 —— 自动更新的「更新源」配置
#
#  两样东西写在这一个文件里：
#    manifest_url = ...     更新清单的地址（= 从哪个仓库下载游戏包）
#    http(s)://...          下载加速代理列表（一行一个）
#
#  ★★ 这个文件有两份，各有各的角色：
#    * 【服务器上那份】说了算。更新器每次跑升级，都先向自己要连的那台服务器
#      要一份（HTTP GET http://<服务器地址>:<注册页端口>/api/update-config）。
#      开服的人改完它，**不用重启服务端**，所有玩家下一次更新立刻生效。
#    * 【玩家本机这份】只是引导和兜底：取不到服务器那份时（服务器没开、端口
#      不通、服务端版本太老还没有这个接口）才用它。
#  两份文件格式一模一样，打包时也是同一份源文件。
#
# ---------------------------------------------------------------------------
#  manifest_url —— 更新清单 manifest.json 的地址
#
#  更新器先取这份清单，从里面拿到每个版本的下载地址、大小和 sha256，再去下
#  游戏包。所以换仓库只要改这一行（改服务器上那份），所有玩家立刻跟着换，
#  不用先更新一版客户端。
#  留空、或整行删掉 = 用更新器内置的默认地址（就是下面写的这个）。
# ---------------------------------------------------------------------------
manifest_url = https://github.com/liubz102/popshot-reborn/releases/latest/download/manifest.json

# ---------------------------------------------------------------------------
#  下载加速代理列表
#
#  从国内直连 GitHub 下载 400 MB 的客户端包往往很慢，所以更新器
#  （game_patched\BsPatcherChn.exe）真正下载之前会先「寻找最快的下载源」：
#    1. 先单独测直连 10 秒，速度超过 1 MB/s 就直连下载；
#    2. 否则把下面的代理每 5 个一组【并行】测速（每个 10 秒）。一组里只要有
#       超过 1 MB/s 的，就选组内最快的那个，后面的组不再测；
#    3. 全部测完都不够 1 MB/s，就用所有来源（含直连）里相对最快的那个。
#  「速度」按测速那 10 秒里最快的 1 秒算（峰值秒速），不是 10 秒的平均。
#  选中代理后，代理地址直接拼在原地址前面，例如：
#    https://gh-proxy.com/https://github.com/liubz102/popshot-reborn/releases/download/V0.2.8/PopShot-portable-win64_V0-2-8.zip
#  更新界面底部会显示「仓库：<仓库简写>」和「代理地址：直连Github」或代理网址。
#  上面那份 manifest.json 也用这个列表兜底，但不测速：直连 5 秒没取到就随机挑
#  一个代理试，5 秒不成再随机换一个，全都不成才提示手动下载。
#
#  格式：一行一个代理地址，必须以 http:// 或 https:// 开头，末尾的 / 可加可不加；
#  # 或 ; 开头的行是注释。列表顺序就是测速顺序，可以自己增删、调整。
#  一个地址都没有时，更新器只用直连。
#  ★ 玩家本机这份【会】被自动更新用新版本里的那份刷掉（代理站点常换）；
#    真正生效的是服务器上那份 —— 要加代理请找开服的人加。
# ============================================================================
https://cdn.gh-proxy.com
https://github-proxy.memory-echoes.cn
https://githubdog.com
https://jiashu.1win.eu.org
https://github.dpik.top
https://gh-proxy.com
https://ghfile.geekertao.top
https://gh.927223.xyz
https://github.mxw.qzz.io
