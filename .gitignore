# 炮炮火枪手复活工程
#
# main 分支保存：自写源码、测试、启动/打包脚本、逆向分析文本、两份 Python 运行时，
# 以及 CLAUDE.md / .claude 里的项目进度。
#
# 原版素材（game_org / 原版安装包 / Pack_decrypt）另存在别的分支，不并进 main。
# game_patched 要提交 —— 它是客户端实际跑的那份，打包脚本直接从它取；
# 但里面每次运行都会变的东西（崩溃转储、日志、本机设置）排除掉。

# ---------------------------------------------------------------------------
# 原版素材：体积大、且已在另一个分支保存，不进 main
#   game_org         368 MB  原始安装结果（只读母本）
#   原版安装包       352 MB  NSIS 安装包
#   Pack_decrypt     540 MB / 16266 个文件，是 game_patched/Pack/*.pkn 解出来的，可再生
# ---------------------------------------------------------------------------
/game_org/
/原版安装包/
/Pack_decrypt/

# ---------------------------------------------------------------------------
# game_patched：**目录本身要提交**，只排除每次运行都会重新生成/改写的东西
#
# ⚠ 这个文件里**不能写行尾注释** —— gitignore 只认整行注释，`#` 前面的内容
#   连同 `#` 后面的说明会被当成 pattern 的一部分，规则直接失效（实测踩过，
#   FINDINGS §165）。所以说明一律单独占行。
#
#   Dump/          崩溃转储，一个 32 MB，已攒到 249 MB
#   Debug/         客户端自己按日期分文件的调试日志
#   BigShot.rpt    崩溃报告，每次崩溃追加
#   GameGuard/*.erl  GameGuard 的运行日志（npgg.erl / npgl.erl）
# ---------------------------------------------------------------------------
/game_patched/Dump/
/game_patched/Debug/
/game_patched/BigShot.rpt
/game_patched/GameGuard/*.erl
# ★ GameGuard/GameGuard.ver 是原版安装带的，实测不是每次运行都变，保留。
#
# ★★★ UserConfig.ini **绝对不能忽略** —— 它看着像「本机设置」，其实是
#   **构建输入**（V0.1 §49 / D021）：
#     * 客户端是**登录成功那一刻**才写出这个文件的，全新环境第一次跑时它不存在；
#     * 文件不在就用内置默认值 = 全屏 + 16 位色，于是走进 D3D 模式枚举分支；
#     * 现代 N 卡不报告任何 16 位色模式 -> GetAdapterModeCount 返回 0 ->
#       `dec eax` 下溢成 0xFFFFFFFF -> 循环上界 42.9 亿，每轮还调一次 D3D API
#       -> 要跑 40 多分钟，**表现就是开局卡死**，而且看不出任何报错。
#   现在这份预置的 `FullScreen=1` + `ColorDepth=1` 就是 V0.1 的修复本身
#   （注意反直觉：这个老客户端的文件值语义和 D3D `Windowed` 相反，1 才是窗口模式）。
#   代价是它每次退出都会被改写（音量、LastLoginId），git 里会有噪音 ——
#   嫌烦就 `git update-index --skip-worktree game_patched/UserConfig.ini`，
#   **但不要把它加进本文件**。
# ★ GameGuard.des.bak 必须留着 —— 它就是被改名的 GameGuard（CLAUDE.md 铁律 2），
#   丢了就等于把 GameGuard 放回原位。

# ---------------------------------------------------------------------------
# 下载或解压的第三方工具（可重新下载）
# 保留 tools/ 根目录下我们自己写的脚本和源码。
# ---------------------------------------------------------------------------
/tools/_dl/
/tools/_pydeps/
/tools/ghidra/
/tools/jdk/
/tools/Sysinternals/
/tools/x64dbg/
/dist/

# ---------------------------------------------------------------------------
# 编译产物
# ---------------------------------------------------------------------------
*.obj
*.pdb
*.ilk
*.exp
*.lib
*.idb
*.iobj
*.ipdb

# ---------------------------------------------------------------------------
# 逆向工具生成的二进制镜像、内存片段和 Ghidra 本地工程
# 保留 re/*.txt、re/*.json、re/*.md 等可审阅的分析成果。
# ---------------------------------------------------------------------------
/re/*.exe
/re/*.img
/re/*_extra/
/re/*.gpr
/re/*.rep/
/re/*.lock
*.dmp
*.mdmp

# ---------------------------------------------------------------------------
# 运行时输出和本地存档
# logs 可能含明文登录口令、内网 IP、MAC、抓包及大量截图。
# accounts.json 是可变的本地账号/游戏存档，程序缺失时会自动创建。
# ---------------------------------------------------------------------------
/logs/
/server/data/accounts.json
/server/data/accounts.json.*.tmp
# tickets.json：会话 12 中途试过把票据落盘，用户拍板改回纯内存（D097），
# 现在**不会再生成**这个文件。留着这两行只为挡住那阵子留下的残留文件。
/server/data/tickets.json
/server/data/tickets.json.*.tmp

# 根目录的 server.config 是**本机玩家自己填的**联机服务器地址（可能是内网 IP 或
# 私人域名），不进仓库。模板在 server/config.py 的 DEFAULT_CONFIG_TEXT 里：
#   * 服务端启动时缺文件会自动生成一份；
#   * tools\build-portable.ps1 打客户端包时缺文件也会照模板生成，
#     所以新 clone 下来不用手工补也能打包。
/server.config

# ---------------------------------------------------------------------------
# Python 缓存、测试缓存和本地虚拟环境
# ---------------------------------------------------------------------------
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
.venv/
venv/
env/

# bug调查用的log
/bug调查/

# ---------------------------------------------------------------------------
# ★ 下面这些**必须**进仓库。写成显式的 ! 规则，是为了防止以后有人加一条
#   宽泛的忽略规则（*.exe / *.dll / *.tar.gz …）把它们悄悄带走 ——
#   少了任何一个，clone 下来就打不出能用的包。
# ---------------------------------------------------------------------------

# 内置 Python 的原生标准库（Windows 运行时）
!/runtime/python/*.pyd

# Win7 兼容运行时（CPython 3.8.10 win32）的原生标准库。
# ★ 和上面一条同理：`*.py[cod]` 会把 .pyd 一起吃掉，少了它 clone 下来的包里
#   Python 起不来（连 socket 都 import 不了）。见 §215。
!/runtime-win7/python/*.pyd

# Linux 服务端运行时：34 MB 的 .tar.gz，**故意不解压**（在 Windows 上解会丢
# 符号链接和可执行位，D088）。有它才能离线打出带 Linux 运行时的服务端包。
!/runtime-linux/*.tar.gz
!/runtime-linux/*.sha256

# GameGuard 绕过的两个二进制**必须成对**提交（CLAUDE.md 铁律 6）：
# 只换其中一个，握手就对不上，登录界面根本出不来。
!/hook/bin/*.dll
!/hook/bin/*.exe

# launch.ps1 靠它判断 D3D9 HAL 可不可用（串流会话会让画面出不来，V0.1 §61）
!/tools/d3d9_probe.exe
!/tools/d3d9_probe_x64.exe

# 服务端包的模板。注意上面那条 /server.config 是**根目录锚定**的，
# 不会波及这一份 —— 但这里写明白，免得以后有人把它改成不锚定的。
!/tools/server-package/server.config

# 版本管理的两个源文件（V0.2 版本门禁）：
#   * tools/build-ver.config      要打的版本号（发版前手动改；不进发布包）
#   * server-ClientFilter.config  服务器允许的最低客户端版本（打进两个包）
# 目前没有规则会忽略它们，这里显式写 ! 是防以后有人加 *.config 之类的
# 宽泛规则把它们悄悄带走 —— 少了它们，clone 下来就打不出带版本号的包。
!/tools/build-ver.config
!/server-ClientFilter.config

# 打包时的版本号，由打包脚本根据 /tools/build-ver.config 自动生成，不需要上传git
/BUILD.ver

# ---------------------------------------------------------------------------
# 本机配置、凭据、编辑器和操作系统杂项
# .claude 的计划、结论与会话记录要提交，只忽略 Claude 的本机权限配置。
# ---------------------------------------------------------------------------
/.claude/settings.local.json
/.claude/*.local.*
/.zcode/
.env
.env.*
!.env.example
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store
Thumbs.db
Desktop.ini
