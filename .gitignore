# 炮炮火枪手复活工程
#
# 仓库只保存自写源码、测试、启动脚本、逆向分析文本，以及 CLAUDE.md /
# .claude 中的项目进度。原版客户端、解包资源和本机工具只在本地保留。

# ---------------------------------------------------------------------------
# 下载或解压的第三方工具（可重新下载）
# 保留 tools/ 根目录下我们自己写的脚本和源码。
# ---------------------------------------------------------------------------
/tools/_dl/
/tools/_pydeps/
/tools/ghidra/
/tools/jdk/
/tools/Sysinternals/
/tools/x64dbg/
/dist/

# ---------------------------------------------------------------------------
# 编译产物
# ---------------------------------------------------------------------------
*.pdb
*.ilk
*.exp
*.lib
*.idb
*.iobj
*.ipdb

# ---------------------------------------------------------------------------
# 逆向工具生成的二进制镜像、内存片段和 Ghidra 本地工程
# 保留 re/*.txt、re/*.json、re/*.md 等可审阅的分析成果。
# ---------------------------------------------------------------------------
/re/*.exe
/re/*.img
/re/*_extra/
/re/*.gpr
/re/*.rep/
/re/*.lock
*.dmp
*.mdmp

# ---------------------------------------------------------------------------
# 运行时输出和本地存档
# logs 可能含明文登录口令、内网 IP、MAC、抓包及大量截图。
# accounts.json 是可变的本地账号/游戏存档，程序缺失时会自动创建。
# ---------------------------------------------------------------------------
/logs/
/server/data/accounts.json
/server/data/accounts.json.*.tmp

# ---------------------------------------------------------------------------
# Python 缓存、测试缓存和本地虚拟环境
# ---------------------------------------------------------------------------
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
.venv/
venv/
env/

# 内置 Python 的原生标准库必须随项目发布
!/runtime/python/*.pyd

# ---------------------------------------------------------------------------
# 本机配置、凭据、编辑器和操作系统杂项
# .claude 的计划、结论与会话记录要提交，只忽略 Claude 的本机权限配置。
# ---------------------------------------------------------------------------
/.claude/settings.local.json
/.claude/*.local.*
.env
.env.*
!.env.example
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store
Thumbs.db
Desktop.ini
