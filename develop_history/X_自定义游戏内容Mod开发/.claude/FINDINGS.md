# X_Mod FINDINGS —— 已查明的硬事实

**编号从 §1 起**，和 V0.1 / V0.2 / V0.3bot / V0.3商店 相互独立。
引用旧版一律写全前缀：「V0.1 §119」「V0.3商店 §50」。

**写法**：一条一小节，**先结论后证据**，**≤ 25 行**。不写调查过程、不贴大段反汇编 ——
贴地址 + 那一句指令就够。**失败的尝试也要记**。

可信度标记：✅实测 / 🔍静态（反汇编看出来的）/ 🤔推测 / ❓未知。**别把推测写成事实。**

---

## §1 ★★★★★ 爱琳（아이린 / ChrIndex=3）在 311 客户端里**只差模型**，别的全在（🔍静态 + ✅离线核对）

**结论**：她不是「原版没有的新角色」，是**原版做好了、中国区没开**的第 4 个基础角色。

已有：`Data/ChrProps.ini` 第 `[14]` 节 **131 个键**，和基础角色完全对等（Hp90 Sp110 Atk8
Def9 Speed7.5、Jab00、Dash00~05、三个碰撞圆）；`Data/Chinese.ini` 有 `아이린=爱琳` /
`아이린 전용=爱琳专用` / `您可以从4级开始使用爱琳`；`weapon.ini` 有 6 节 `ch03-*`
（트릭스터 / 씨드 폭탄 / 에이리얼 슈터，Id 1003000~1003520）；`NewMutu.ini` 格斗 10 节；
`Effects/CH03/` 102 个花瓣蝴蝶泡泡特效；`Data/default.amf` 有 `CH03_WP1 / WP1W / WP2a / WP2aW`；
`Images/Game/Ch003Weapons.png`；`Sounds/CharacterChanger/ch003.ogg`；
`weapon.ini` 全表 `IconIdx` 恰好 `0..11` = **4 角色 × 3 武器**，ch03 占 9/10/11。
客户端代码也全接好了：名字 switch `0x557128` 有 id 3；图标下标 `0x4f676e` 有 `3→(0x1a,0x1b)`；
服务端 `bot_chrprops.json` / `bot_weapons.json` 里 `"3"` 的数据本来就是全的。

**唯一缺口**：`Models/Characters/ch03/` **整个目录不存在**（ch00 746 / ch01 671 / ch02 669 / ch03 **0**）；
`Sounds/FX/**/ch03/` 也是 0。

## §2 ★★★★★ 挡着爱琳的是**六道 exe 闸 + 一道数据闸**（🔍静态，字节级）

| 闸 | VA | 原字节 → 新字节 | 说明 |
|---|---|---|---|
| 数据 | `ChrProps.ini [14]` | 删掉 `OpenLocale=1` | `0x471771` 读这个 key，缺省 `-1`；`map.ini:193`：`1=韩 2=日 4=中`，中国区是 `[[0x72e320]]==2` |
| P1 | `0x55853c` | `83 F8 03` → `83 F8 04` | 持有判定 `id<3 恒真`（白送）。唯一调用者 `0x4070f6` |
| P2 | `0x40724f` | `83 7D 10 03` → `04` | 枚举器 A（`0x407168`）的基础角色循环 |
| P3 | `0x407383` | `83 FE 03` → `04` | 枚举器 B（`0x4072c0`，房间 6 格面板）的同款循环 |
| P4 | `0x407160` | `8D 47 03` → `8D 47 04` | `0x40713a` 返回的按钮总数 = 持有数 `+3` |
| P5 | `0x4f58f4` | `0F 84 D1 00 00 00` → `90`×6 | 战斗 `CharacterChanger` **显式跳过 id 3**（同循环 `0x4f58e8` 跳过 98，保留） |
| P6 | `0x44caaf` | `83 7D 10 03` → `04` | **大厅面板自己的循环** —— 第三条独立代码路径，最容易漏 |

**三条枚举路径互相独立**：大厅 `0x44c8xx`、房间 6 格 `0x4072c0`、战斗换人条 `0x407168`。

★ **2026-09-17 起是九道，不是六道** —— 原版 4 级门那三处也**默认打掉**（D2 改口径，用户拍板），
三条特征串同样各自唯一、落点字节核过：

| 闸 | VA | 原字节 → 新字节 | 说明 |
|---|---|---|---|
| L1 | `0x44c954` | `0F 8C 52 01 00 00` → `90`×6 | 大厅面板：等级 <4 就不给爱琳建按钮（跳转抹掉 = 恒建）|
| L2 | `0x44cc07` | `0F 8D 85 00 00 00` → `E9 86 00 00 00 90` | 大厅面板点击：条件跳 `jge` 改成**无条件** `jmp`（多 1 字节用 `nop` 补）|
| L3 | `0x467eb1` | `0F 8D 08 01 00 00` → `E9 09 01 00 00 90` | 房间面板点击：同上 |

⚠ **L2 / L3 不能像 L1 那样 nop 掉** —— 那两处是「等级够就跳过弹窗」，抹掉跳转等于**永远弹窗**，
正好反了；要把 `jge rel32`（6 字节）换成 `jmp rel32`（5 字节）+ 1 个 `nop`，偏移相应 +1。

## §3 ★★★★★ 中文版大头像图集只声明 24 帧 ⇒ 爱琳和角色 110 的帧越界**必崩**，而图早就画好了（✅离线核对）

**结论**：`Images/Chinese/BigChrIcons_CN.smf` 只有 24 帧（0x00~0x17），
但 `0x4f676e` 给 110 的是 `(0x18,0x19)`、给爱琳的是 `(0x1a,0x1b)` ⇒ 越界 `C0000005 @0x430857`。
**PNG 里那 30 格全都画好了**（含爱琳的灰/彩头像），只是 `.smf` 把帧数截断了。

- `.smf` 格式：`u32 版本(=2) + u32 帧数 + 帧数 × 8×i32(x1,y1,x2,y2,-1,-1,-1,-1)`。
  3 列网格，大头像格 155×75、x 步长 157、y 步长 77、起点 (1,1)。
- `Data/Chinese.ini:37-38` **只重定向 `.smf`**，`.png` 仍走 `Images/NewUI2/`。
- `Chinese/BigChrIcons_CN.smf` 的**前 24 帧和 `NewUI2/BigChrIcons.smf`（28 帧）逐字节相同**
  ⇒ **修法 = 直接用后者整份覆盖前者**，不用碰任何图片。
- 小头像 `Chinese/ChrIcons_CN.smf` 已经有 28 帧，**不用改**。
- ⚠ **大厅面板也吃这份图集**（`0x44ca13` push 的就是 `"Images/NewUI2/BigChrIcons.smf"`）
  ⇒ 不补图集就开闸，**崩在大厅**，比战斗内更早。
- 补完之后 `hook/bshook.c` 的 `REGION_SITES[2]`（110 借用 106 图标）就多余了，应退役。

## §4 ★★★ 缺音频**不会崩**；爱琳开枪没声是 `weapon.ini` 的**两种路径写法**造成的（✅离线 + ✅实机）

「不会崩」：`ch01` 的 `.evn` 引用的 `FX/char/KR/voice/ch01_Casil/…` 目录**根本不存在**，
外加 6 个 `.wav` 也不在，共 14 条死引用；卡希尔照样跑了十几年。

**为什么爱琳开枪没声**（2026-09-15 用户实机反馈）：

```
[ch01-01]  Sound-Fire=ch01@Attack01.ogg              ← 裸文件名，相对 Sounds/ 根 ✓ 文件在
[CH03-01]  Sound-Fire=FX/wp/ch03/ch03@Attack01.ogg   ← 全路径          ✗ 目录从没发过
```

三个基础角色的武器音**平铺在 `Sounds/` 根目录**；爱琳用的是较新的全路径写法，
指向本该和 `Models/Characters/ch03/` 一起发的目录（§1）。

**修法**：`tools/ch03_sounds.py` 按**武器的结构角色**配 11 个音源，**不动 `weapon.ini`**。
挑得有依据的几条：`-hpup` ← `HpCharge.ogg`（原版的 HP 充能音，正对她的回血图腾）；
`-set` / `-hit` ← `Weapon-Hit-Bottle` / `Weapon-Hit2-Bottle`（她发射的是 **포션 药水**）；
`-bug` 蝴蝶碎片 ← `Weapon-Hit-ApplePiece`（原版 `ch00-02a` 用的就是它，**同一个「碎片」角色**）。
不满意改 `MAPPING` 一行重跑，幂等。

## §16 ★★★★ `weapon.ini` 的 `Image=` 分 **2D(`Anim`)** 和 **3D(`Model`)** 两种，决定弹体像不像自己（✅实机落锤）

用户实机：爱琳三把武器**只有 1 号的弹体是新的**，2/3 号还是卡希尔的 ——
和数据一一对上：

| 节 | `Image=` | 来源 | 实机 |
|---|---|---|---|
| `[CH03-01]` | `Anim,CH03_WP1` | 原版在 `Data/default.amf` 里发了 | ✓ 她自己的 |
| `[ch03-02]` | `Model,Characters/ch03/ch03D0002` | 3D 网格，克隆自 ch01 | ✗ 卡希尔的 |
| `[ch03-02a]` | `Anim,CH03_WP2a` | 原版发了 | ✓ |
| `[ch03-03]` / `[ch03-03a]` | `Model,…/ch03D0003A` | 3D 网格，克隆自 ch01 | ✗ |

⇒ **原版给爱琳发全了 2D 的那部分**（`default.amf` 的 4 个 `CH03_*` 条目 +
`Images/Game/CH03_Wp*`）**和全部特效**（`Effects/CH03/`），
缺的只有 3D 件 —— 因为 3D 件本来就都住在缺失的 `Models/Characters/ch03/` 里。
**手持武器网格** `chNNW%04d.msh`（由 `WAnimIdx`/`WMeshIdx` 选）同理，三把全是克隆的。

⚠ 别把目录里的 `D` 件当成「这个角色实机发射的东西」：`[ch01-03]` 캐논왈츠 用的是 2D 精灵 `Anim,CH01_WP3`（火箭弹），
`ch01D0003.msh` 那个宝箱在 `weapon.ini` 里**没有任何引用**，只是 `ch03D0003A` 抄头 / 骨骼 offset 用的模板。
全表里 `Model,` 引用的 `D` 件只有 `D0002`（各角色的投掷物）、ch00/ch01 的 `D0x12/D0x22/D0x32` 系列，以及爱琳的 `D0003A`（2026-09-16 核对）。

## §5 ★★★ 韩版 `ShopItem.ini` 里有爱琳整套 25 件衣柜，中文版没抄（🔍静态）

`Data/ShopItem.ini`（韩版原文件）有 `[Item-4010001]` … `[Item-4110037]` 共 25 件，
`Tag=Models/Characters/ch03/ch03B0001.msh` 这种全路径，
`Images/Shop/아이린_*.png` **25 张图标一张不缺**。
但客户端真正读的 `ShopItem-Chn.ini`（`Chinese.ini:26` 重定向）里**没有** ⇒
`server/shop_items.json` 里也没有 ⇒ **默认爱琳只穿 `ch03*0000` 那套默认装**。

⚠ 想上架得先解决一个真问题：**客户端只有 3 个装备槽位掩码**
（`0x5583d3 Equipment::Equip`：`cmp ax,3 / jge` 直接丢弃，`0x558501 IsSlotUsed` 同理）
⇒ **第 4 个基础角色不会有第 4 个掩码**，`shopdata.SLOT_MASK_COUNT` 必须保持 3。

## §6 ★★★★ 「一个角色」由什么构成（🔍静态 + ✅离线核对）

- **模型**：`Models/Characters/chNN/chNN<类><4位设计号>.msh` + 同名 `.dds`；
  `chNN@<动作>.mtn` + 可选 `.evn`。
  类字母（`0x505cee` 的跳转表）：`0`=头发/头 `H`=脸 `B`=上衣 `G`=手套 `L`=下装 `S`=鞋
  `^C`=头饰 `^N`=? `^T`=尾巴 `^W`=翅膀 `T`=套装 `W`=手持武器 `D`=弹体 `J`=?
  路径模板 `Models/Characters/Ch%02d/%s` + `ch%02d%s%04d.msh`（`%02d` 是最小宽度，100→`ch100`）。
  **最低必需动作集 = 15 个角色目录的交集 = 73 个**。
- **数据**：`ChrProps.ini`（`ChrIndex=` 才是真 id，节号只是下标；`WeaponIdxBase=1e6+id×1000`）、
  `weapon.ini` 的 `[chNN-dash/-01/-02/-03]`、`NewMutu.ini` 10 节、`Chinese.ini`（**韩文原串当 key**）、
  2D 弹体还要 `default.amf` 里的条目。
- **图集**：小头像 2 帧 + 大头像 2 帧（灰 / 彩），见 §3。
- **音效**：`Sounds/FX/char/KR/char/chNN/`、`…/voice/chNN/`、`FX/wp/chNN/`、
  `Sounds/CharacterChanger/ChNNN.ogg`。
- ★ **战斗内 HUD 立绘不是图集**：`Images/Ui/Portrait.png` 那 35 帧在 exe 里**没有任何引用**，
  HUD 立绘是把模型实时渲染到名为 `Game/Portrait` 的贴图上
  （`ChrProps` 的 `PortraitScale` / `PortraitXOffset` 就是给它用的）。**加角色不用管它。**

## §7 ★★★★ `.msh` / `.mtn` / `.evn` / `.dds` 格式（够用的部分）（🔍静态 + ✅离线核对）

- **`.msh`**：`u32 版本=3` + 4×4 矩阵 + `+0x6c` 顶点数 + `+0x70` 索引数 + 顶点 32 B +
  索引 u16 + 材质块（含 **`u32 码元数 + UTF-16LE 贴图名`**，如 `len=13 + "ch01B0000.dds"`，
  同一文件可能出现 2 次）+ `u32 骨骼数`，每根 `u32 长度 + UTF-16 骨骼名 + u32 n +
  n×索引 + n×权重 + 16 float`。
  ⚠ **偏移 `0x4c` 还有一个长度前缀串是挂接特效路径**（`ArmorSpakle\Efx\CH01_*.efx` 等，
  ch01 有 8 个文件带）—— **含 `/` 或 `\` 的串一律不能当贴图名改**。
- **`.mtn`**：`u32 版本=3 + u32 长度 + UTF-16 "AnimationSet0"` + 骨骼名表。
  ch01 的 86 个 `.mtn` 内部 **0 处** `chNN` 串 ⇒ 克隆时**一个字节都不用改**。
- **`.evn`**：明文 XML，CP949，声明 `encoding="ks_c_5601-1987"`，逐帧事件（`PlaySound` /
  `RenderEffect` + `ResourcePath`）。⚠ ch01 有 **2 个 `.evn` 引用别的角色**
  （`Mutu-DamagedFly`→ch00、`MutuStand-P00`→ch02），**无差别替换会改错**。
- **`.dds`**：128 字节头 + 像素区。ch01 的 268 张里
  **117 张 64×64 R5G6B5（8320 B）、122 张 64×64 A8R8G8B8（16512 B）、
  17 张 128×128 R5G6B5（32896 B）、只有 8 张 DXT1/DXT3** ⇒ **绝大多数是未压缩的**，
  改像素时**把 128 字节头原样保留**即可，别用 `PIL.Image.save(*.dds)`（它不保证吐出同样的头）。
- ★ **骨骼必须同源**：V0.3商店 §50 记过「骨骼按名字在角色骨架里找，找不到返 NULL，
  喂进矩阵乘直接崩」。ch02 有 `Bone_Spine_01`/`Bone_Pelvis_01` 而 ch00/ch01 没有。
  爱琳 ChrProps 点名的 `Bip01_R_Finger1` / `Bone_Wp00_02` / `Bone_Wp00_11` **在 ch01 骨架里全有**。

## §8 ★★★ 服务端**今天就已经接受 id 3**，几乎不用改（🔍代码）

`set_character()` 只 `max(0, …)` 无上界；`player_character()` 只对 `PREMIUM_CHARACTER_IDS`
（100~110）做「买了没」的退回，id 3 直接放行；`0x0301` 处理器不校验。
⇒ **打完 hook 补丁就能实机选中并存盘**，服务端那三个常量只是把口径对齐。

⚠ `bot.BOT_CHARACTER_PANEL_IDS` / `BOT_DEFAULT_CHARACTERS` 是从 `BASE_CHARACTER_IDS`
**派生**的 —— 把 BASE 改成 `(0,1,2,3)` 会让 bot 跟着用爱琳，而她的 2/3 号武器是
`SeedBomb` / `TotemLauncher`，服务端没有这两类的飞行模型（D54 的理由原样适用）
⇒ 这两个必须**解耦成字面量 `(0,1,2)`**。

## §9 ★★★★★ 加一个**真·全新** id（比如 111）的成本，比复活 id 3 高一个数量级（🔍静态）

- **商城角色那段的枚举上界有三处**，而且原版自带 off-by-one：
  `0x407153` 的 `cmp esi,0xb`（计数，100..110）、`0x407190` 的 `push 0xc`（列表，100..**111**）、
  `0x4072e8` 的 `push 0xb`（第二个枚举器）。
  ⇒ **现在就算给玩家发 id 111 的角色卡，它也永远拿不到按钮** —— 列表里有、计数里没有。
- `0x557128` 名字 switch（跳转表只覆盖 103..110）→ 新 id 显示 `???`。
- **图标下标 switch 有两处**（战斗 `0x4f676e` + 大厅 `0x44c964`）→ 新 id 落 default → **必崩**。
- `account_store.character_id_of_item()` 是唯一的角色卡白名单，要放开。
- 全套资源（ChrProps / weapon.ini / NewMutu.ini / Effects / Sounds / default.amf /
  `IconIdx`（0..11 已用满））**都要从零造**。

⇒ **铁律 12 的由来：能顶替或复活原版留好的坑位，就别去开新 id。**

## §11 ★★★★ `.msh` 的**完整**布局（✅ 257/257 round-trip 逐字节一致）

§7 只写了「够用的部分」。`tools/mshtool.py` 把整个格式吃透了，按顺序：

```
u32 版本=3 | 4x4 矩阵 | 0x44 处 4B 标志 | 0x48 处挂接特效数 + 每条(wstr + 4B)
| 32B 零块 | u32 顶点数 | u32 索引数 | 顶点(32B) | 索引(u16)
| 材质块（★ 45 个头发件有 **2 个**材质，不是一个）
| u32 子集数 + n×4×u32
| 骨骼块（u32 骨骼数，每根 wstr + u32 n + n×索引 + n×权重 + 16 float）
| ★ 末尾还有**模板阴影用的邻接表**：u32 面数 + 每面 40B
  （面号 / 邻居数 / 3×u16 邻居面 / 3×2×u16 共享边 / 14B 零）
```

- `ch0300011.msh` 的邻接表后面还挂着 **676 B 残片**，引擎只读前 n 面 —— 原样保留即可。
- ⇒ 改顶点坐标是安全的（只动 xyz，拓扑/骨骼/权重/邻接表一个字节不碰）；
  **追加三角形要同时改索引、子集面数、邻接表**，是完全不同的风险等级。

## §14 ★★★★ `.mtn` 格式全解 + 绑定姿态的三条坑（✅ 86 个文件复现，工具 `tools/mtntool.py`）

```
u32 3 | wstr "AnimationSet0" | f64 3600(tick/s) | f64 时长 | u32 229 | u32 0
| 骨架树 | u32 轨道数 | 轨道…
```

- **骨架树按 D3DXFRAME 序列化**：每节点 = 名字 + 16 f32；有子节点写 `1` 递归首子、
  有兄弟写 `2` 递归兄弟、收尾写 `3`。ch03 是 **229 个节点**（173 有子、55 有兄弟）。
- 轨道 = 名字 + 旋转 `(t, qx qy qz qw)×n` + 平移 `(t, xyz)×n` + 缩放×n，**帧距 120 tick**。
- **86 个文件的骨架树完全一致**；`Crouch-Start.mtn` 轨道后有 35602 B 垃圾残片（引擎不读）。
- **103 根骨没有轨道**，用树里的静态矩阵。

三条坑：

1. `.msh` 骨骼块尾部那 16 float 是**绑定姿态世界矩阵的逆**（网格单位）。
2. **默认件顶点是「场景单位 ÷ 2.8791」**（`0x04` 的世界矩阵就是这个缩放），
   另有 185 个文件用场景单位或轴置换。
   补缺骨的公式：`bind(子) = 局部(Stand00, t=0, 场景单位) · bind(父)`，
   ★ **平移不要再除 2.8791**。
3. **绑定姿态 ≠ 任何一帧动画**（Stand00 第 0 帧头在 y=69.2，绑定姿态 70.96）；
   而且**不同套装的手臂骨绑定姿态不同**（`B0014` 的 Clavicle 差 13）——
   从别的件借 offset 前必须逐骨核对共同骨。

## §15 ★★★ 邻接表和绕向的规则（✅ 256 个文件复现 213 个）

- 位置**逐位相等**焊接后找共边，每条共边**只记在面号小的那面**；
  条目 = `u32 面号, u32 邻居数, 3×u16 邻居(补 0xFFFF), 3×2×u16 本面该边顶点号, 14 B 零`。
- 面绕向 `cross(b-a, c-a)` = 外法线（98%+），**引擎开背面剔除** ——
  要做双面片（花瓣、蝴蝶结）就得像 `ch01B0006` 那样放 15 组反向重复面。
- **每顶点最多 3 根骨**；双材质只出现在头发类（39 个件）。

## §13 ★★★★★ 骨架有 **297 根骨**，自带完整的头发 / 辫子 / 裙摆**物理骨链**（✅离线核对）

> ★ 修正：229 是**骨架树里的节点数**（`mtntool.py` 结构化解析的结果，准）。
> 下面那个 297 是我用「找连续可打印段」粗扫出来的，**偏大**——
> 那个扫法会把相邻字符串粘在一起（比如把 `Bip01_L_Forearm` 读成 `Bip01_L_Forearme`）。
> **查骨骼名一律用 `mtntool.py` / `mshtool.py` 的结构化解析，别用扫字符串。**
> 骨链本身的结论不受影响。

**结论**：给角色加长发、辫子、裙摆、披风**不用做成死几何** —— 原版骨架里早就有对应的
多节骨链，而且**动画真的在驱动它们**。绑上去就会自己甩、自己飘。

从 `ch03@Stand00.mtn` 的骨骼名表数出来（任一动作文件都一样）：

```
Bone_Hairtail_L01..L07 / R01..R07    每侧 7 节「辫子骨」
Bone_Hair_L01..L03 / R01..R03        侧发 3 节
Bone_Hair_BL/BM/BR 01..05            后发 3 列 × 5 节
Bone_Skirt_{F,B,L,R,FL,FR,BL,BR}01..04   裙摆 8 向 × 4 节
Bone_Tail_01..06 / Bone_PTail01..06  尾巴
Bip01_Spine / Spine1 / Spine2 / Neck / Head / Pelvis
```

★ **现有网格远远没用满**：`ch0300000.msh`（头发件）只绑了 9 根
（`Bip01_HeadZ` + `Bone_Hair_{L,R}01/02` + `Bone_Hairtail_{L,R}01/02`）——
辫子骨 7 节里只用了 2 节；`ch03B0000.msh`（身体件）绑了 28 根。

⇒ 加几何时**按部位挂到对应骨链、权重沿链渐变**，别图省事全绑 `Bip01_Head`
（那样动起来必穿模）。骨骼块里可以追加条目，**只要名字在这 297 根里真实存在**
—— 引擎按名字查，查不到返回 NULL，喂进矩阵乘直接崩（V0.3商店 §50）。

## §12 ★★ 三条外观相关的实测事实（✅离线核对）

- **卡希尔（ch01）的头发贴图本来就是白的**，不是金发：材质色全 1.0、顶点无颜色。
  所谓「白红配色」实际是**白 + 棕**。（我最初交给子代理的「金发→银白」是错的判断。）
- **角色模型脸朝 `-z`。**
- 手套贴图里**皮肤和米白布的色相只差 5° 且抖动过**，按颜色分不开。
  解法是**按主骨骼把 UV 光栅化成分区掩码**（`mshtool.uv_zone_mask`）——
  以后要给任何角色分部位改色，都走这条路，别试图按颜色阈值切。

## §10 ★★★ 「穿着别人的衣服切到爱琳」不是**新**风险 —— 三个基础角色之间本来就不对齐（✅离线核对）

**结论**：网格路径是「**当前角色 id** + 部件类 + **物品带来的设计号**」现拼
（`ch%02d%s%04d.msh`，§6）⇒ 理论上换角色后可能去找一个本角色没有的设计号。
但**这个不对齐在原版三个基础角色之间早就存在**：

| | 网格件数 | 对方有而 ch01 没有的 |
|---|---|---|
| ch00 泰尔 | 284 | **36 个**（`L0001`~`L0008`、`L0056`~`L0063`、`0068`~`0070` 系列、`W0006`、`D1003` …）|
| ch01 卡希尔 | 256 | —— |
| ch02 布洛克 | 253 | **37 个**（`L0001`~`L0008`、`G0014`~`G0024`、`W0091`~`W0093` …）|

（`L` 缺得最多是因为卡希尔穿连衣裙，下装并进了 `B` 件。）

⇒ 原版带着这个不对齐跑了十几年 ⇒ **爱琳整套克隆 ch01 之后，和卡希尔同级安全**，
不需要额外兜底，也**不用为此做实机对照实验**。

⚠ 附带查明：`shopdata.usable_by(item, character)` 虽然定义了、也有单测，
但**没有任何生产调用点** —— 服务端不按角色过滤 `equipped`。
过滤发生在客户端 `0x5583d3 Equipment::Equip`（`ItemInfo+0x24` 角色限定）。
**这同样是原版行为，不是本版引入的。**

## §17 ★★★★★ 「裙子上有网格线」的根因：邻接表把双面薄片的**正面和反面配成了邻居**，不是贴图 / UV（✅离线复现，⏳等实机）

**结论**：`.msh` 末尾的邻接表是给**模板阴影**抽轮廓边用的（一对邻居面一朝光一背光 ⇒ 这条边挤出阴影体）。
`geometry.py` 的花瓣是双面薄片（正面 F/F2 + 反绕向副本 F'/F2'，每条内部边上 4 个面），
`mshtool.build_adjacency` 的旧规则「取第一个面号更大的面当邻居」写出了 `F2→F'`（正面配反面）——
正反面永远一朝光一背光 ⇒ 花瓣的**每条内部边都成了轮廓边**，阴影体沿三角形的边落回裙面，就是那些暗线。

- 贴图 / UV 排除：每片花瓣独占一块连续渐变区，缝只在花瓣外沿，画不出花瓣**内部**的格子（`tools/ch03_skin/compare_D_skirt.png` 左）。
- 原版 `ch01B0006`（蝴蝶结，15 组反绕向重复面）的表：内部边记 `(F,F2)`+`(F',F2')`，**只有边界边**才记 `(F,F')`
  ⇒ 导出工具的规则是「每条边上的面按面号排序后**两两配对**」。换成这条规则后 ch01 256 个文件复现 213 → **235** 个，无退步。
- 用 `mshtool.silhouette_edges` 按同一光向模拟：旧表在 `ch03B0000` 上抽出 508 条轮廓边（花瓣内部全是），新表 220 条（只剩外沿）；
  新表里法线相反的邻居对 128 个，**全部落在只有 2 个面的边界边上**，和 B0006 同构。
- 修法：只改 `build_adjacency` 规则重跑 `geometry.py`，顶点 / 索引 / 骨骼逐字节不动（`ch03B0000` 去掉 309 对、`ch0300000` 去掉 28 对）。
- ⚠ 🤔 exe 里没搜到 `stencil` 字样（只有 `Shadow=` 这个 ini 键和 `NpcBossShadow.png`），「引擎拿这张表做模板阴影」是从
  **表的结构**（每条边只记一次 + 记边的两个顶点号）推出来的；**实机看不到网格线才算落锤**。

## §18 ★★★★ 武器 / 弹体网格的坐标约定（✅离线核对；`tools/ch03_skin/weapons.py` 据此重做 5 件）

- **手持件挂在角色骨架的 `Bone_WpNN_*` 上**（`Dummy01`→`Bone_Rweapon01`→`Bone_Wp01_R01`→`R02`→… 都在 229 节点树里，全是静态骨，
  只有 `Wp01_L07/L08` 有 21 帧轨道 = 左枪击锤）。`inv(offset)` = 「骨骼局部（场景单位）→ 网格空间」；
  W0001 的顶点存成场景单位 ×5.849、头矩阵 0.171 抵消，W0002 / W0003 是 1:1。
- 在骨骼局部里量出来的原件布局：`Wp01_R02` 枪管从原点向 +x **抬 25°** 到 FirePoint (35.6, 0, 14.3)，击锤 R07..R09 在 (-21..-26, 0, 6..16)，
  握把到 (-3, 0, -12)；`Wp02_02` 瓶轴 = +x（FirePoint +22），球心在原点；`Wp03_01` 原点在手心（提梁沿 z 穿过原点），
  炮口朝 -z（FirePoint (12.4, 0, -36.2)），箱体在 x 2..30。
- **`Bone_Wp01_L02` 的 bind 是 R02 的镜像（det<0）**，原件左枪的局部坐标和右枪逐点相同、绕向已在网格空间里翻回 ⇒
  同一份几何放两次，左边按 det 反绕向即可。
- **弹体 `D*` 的骨 `Bone_C01W02_01` / `Bone_C01W03_01` 不在角色骨架里**：offset 是单位阵、原点在物体中心，是弹体对象自己的帧
  （`Models/Items/` 的 Coin / Heart / Candy 也都原点居中）。名字从卡希尔继承，原版 ch01 37 个武器网格全这样 —— **别改名**。
- 骨骼名只能用母本文件里已有的**子集**、offset 逐字节抄；少几根没关系（W0002 原件本来就只有 1 根）。
- ~~动作是卡希尔的：`Attack01` 双手举枪 ⇒ 트릭스터做成左右各一把~~ **2026-09-16 已推翻**：图标只有一把枪，
  持枪动作改从瓦尔基里移植（§20、D9），步枪只绑 `Bone_Wp01_R02`；`Attack02` 投掷、`Attack03` 发射仍是卡希尔的。

## §19 ★★★★ 武器动作名和枪口点都是**按名字**拼出来的，静态骨也可能被个别动作的轨道顶掉（🔍静态 + ✅离线）

- 动作名模板（`0x6858e0` 一带）：`Stand%02d` / `Attack%02d` / `Attack%02da` / `Reload%02d` / `Run-F%02d` /
  `Run-B%02d` / `Dash%02d` / `Dash-B%02d`，N = `weapon.ini` 的 `WAnimIdx`；Jump / Crouch / Damage 不分武器。
  ⇒ **持第 N 把武器的站姿是 `StandN`，不是 `Stand00`**（上一轮对照图全渲的 Stand00，看的不是实机姿势）。
- 枪口点 `0x506b74`：`sprintf("Bone_wp%02d_firepoint")` 在骨架树里找（不分大小写），找不到再找
  `Bone_wp%02d_R_firepoint` → `[obj+0x3d4]`；BarrelPoint 同款（`Bone_Wp%02d_BarrelPoint` → `_R_BarrelPoint`）→ `[obj+0x3d8]`。
  ⇒ **只用一个枪口点**；ch01/ch03 骨架里只有 `_R_` 版本，`Bone_Wp01_L_FirePoint` 没有任何代码引用。
- `Bone_Rweapon01` / `Bone_Lweapon01` / `Bip01_R_Clavicle` 也在 `0x4fbaa2..` 被按名字查出来存起来，树里必须有。
- 武器骨链是**手骨的静态子骨**：`Bip01_R_Hand > Dummy01 > Bone_Rweapon01 > Bone_Wp01_R01 > R02 > R_BarrelPoint > R03 > R_FirePoint`；
  `Bone_Wp02_*` 和 `Bone_Wp03_*`（经 `Dummy03`）也都挂在 `Bone_Rweapon01` 下。瓦尔基里（ch102）的步枪反而挂在
  **左前臂**：`Bip01_L_Forearm > Dummy01 > Bone_Wp01_BarrelPoint > 01 > 02 > FirePoint`。
- ⚠ 「静态骨」只是树里没轨道的默认；母本个别动作会给它们轨道：`Attack03` / `Idle103` 让 `Bone_Wp03_BarrelPoint` 后坐
  （宝箱炮的炮管），`Reload01` / `Idle101` 让 `Bone_Wp01_R02` 翻转（换弹甩枪），`Bone_Rweapon01` 在 Attack02 / 表情动作里有轨道。
  **改静态骨位置必须把这些轨道一起删**（`anim.py` 的 `patch_static` 会做），否则那个动作一播就被顶回去。
- 枪口点补丁做法：`rig.py` 在参考姿势的世界坐标里定枪口，算出 `*_BarrelPoint / *_03 / *_FirePoint` 三根的新局部矩阵
  （x 轴沿枪身），打进全部 86 个 `.mtn`（树保持一致）；自检：补丁后参考姿势下 FirePoint 世界坐标 = 设计枪口，误差 0.000。

## §20 ★★★★★ 跨角色移植动作 = **抄旋转、不抄文件**（✅离线复现，⏳等实机）

- ch102 骨架 169 节点 vs ch03 229。直接拷她的 `.mtn`：① 爱琳网格引用的头发 / 裙摆 / 胸 / 尾巴骨在她树里不存在
  ⇒ 按名字查骨返 NULL 喂进矩阵乘直接崩（V0.3商店 §50）；② 平移键带着她的骨长（DisplayHeight 90 vs 75），手臂会被拉长。
- 两边共同部分都是 3ds Max Biped 标准命名（`Bip01_*`），局部旋转约定一致 ⇒ **上半身 31 根骨只抄旋转**
  （按时长重采样到 120 tick 一帧、四元数邻键符号对齐后 nlerp），平移 / 缩放 / 下半身 / 头发裙摆全留爱琳自己的
  ⇒ 姿势过来了、身材不变。渲出来右手在胸前、左手托枪前段，和她的 Stand01 / Reload01 / Run-F01 一致。
- 时长：Stand01 / Reload01 两边本来相等；Run-F01 / Run-B01 取爱琳 0.667 s（跑步循环和 `ChrSpeed` 配套，ch102 的 0.8 s 重采样）；
  Attack01 取 ch102 的 **0.1 s**（步枪后坐，对应 `CoolingTime=90`；卡希尔 0.667 s 是双枪轮射）；Idle101 取 ch102 的 2.667 s，
  爱琳自己的轨道按原 1.667 s 循环铺满。
- **Attack0N 只含上半身轨道**（无 Bip01 / Pelvis / Spine / 腿，两个角色都这样）⇒ 引擎把它叠在当前下半身动作上。
- `mtntool.write()`：16 个角色目录 1343 个 `.mtn` 逐字节 round-trip；`mkchar.py --audit` 第 ④ 条改成
  「逐字节相同 **或** 骨架树逐节点一致」。
- 瓦尔基里自己的枪相对她双手：轴线在双手上方 ≈10、偏左 6~12（枪架在左前臂上，右手扶枪身右侧，指尖刚碰到枪身），
  按 75/90 缩放 ⇒ 爱琳的枪身轴线过 (9.5, 68.5, z)、枪口 z=-55.3（`rig.RIFLE`）。
## §21 ★★★★ 部件类还有个 **`J`**，只有卡希尔用：那把橙色大锤（✅实机 + ✅离线核对）

**结论**：用户实机看到「爱琳拿着和卡希尔一样的锤子」＝ `ch01J0000` 被整套克隆成了 `ch03J0000`。
**16 个角色目录里只有 ch01 有 `J` 件**（ch00 / ch02 / 11 个商城角色 / ch98 全是 0）⇒ 删掉是回到常态，不是「缺了个文件」。

- 网格：214 顶点前的那把锤 —— 27 面，头是橙黄带箍的方块、柄是深棕，只绑 `Bone_Wp05_01`，
  **站姿里就拎在右手上**（离线把它挂到 `Stand00` f0 渲出来，和用户的房间截图逐像素对得上）。
- `J` **不是装备槽位**：`0x505cee` 那张 12 项的部件类跳转表是 `0 H B G L S ^C ^N ^T ^W T W`，没有 J、也没有 D。
  `J` 有自己的路径模板 `Characters/Ch%02d/ch%02dJ%04d.msh`（VA `0x68518e`，加载点 `0x506467`，
  紧挨着 `…W%04d.msh` / `…D%04d.msh` 两条），加载失败就跳过 —— 这就是另外 14 个角色没有它也不崩的原因。
- `Data/` 和 exe 里**没有任何一处**提到 `J0000` 这个串 ⇒ 设计号是硬编码的 0。
- 修法：`mkchar.py` 的 `SKIP` 名单（体检 ① 会盯着它别再冒出来），删 `ch03J0000.msh` + `.dds`。

## §22 ★★★★★ 爱琳自己的冲刺攻击是**空手右拳**，原版连特效都发好了 —— 缺的只有动作（🔍静态 + ✅实机）

**结论**：`[ch03-dash]` 写的是 **`WMeshIdx=-1`（不拿东西）**，而 `[ch01-dash]` **没写这个键** ⇒
默认取 0 号网格 = `ch01W0000` = **那只泰迪熊**。卡希尔那一下是「抱着熊撞过去」，
爱琳照搬同一套 `Dash00` 动作就成了「抱空气」（用户 2026-09-16 实机）。ch00 / ch02 / ch101 / ch102 也都是 `-1`
⇒ **只有卡希尔有专属冲刺武器**，和 §21 的锤子一样属于「她自己的特例」。

- `ChrProps.ini` 里爱琳的 `Dash00-*` 是**她自己的一套**（和 Dash01~05 照抄卡希尔不同）：
  `DamagingObjBone=Bip01_R_Finger1`（右手打人，卡希尔是熊身上的 `Bone_Wp00_02`）、`TotalFrame=31`、
  `MoveGamma=0.4` / `Move=2.5` / `StartDegree=-90`。⇒ **移植后的动作时长要取 1.0 s（30 帧）**，
  用 ch102 的 0.833 s（25 帧）会在技能末尾定格 6 帧。
- `Effects/CH03/WP00/Efx/` 里**原版就发了** `CH03_DashAttack00.efx`（挂 `Bip01_R_Hand` / `Bip01_R_Finger0Nub`，
  花瓣贴图 `CH03_Wp00_Flower0*.dds`）和 `CH03_DashDust00.efx`；克隆来的 CH01 版挂在 **`Bone_Wp00_11`** ——
  熊身上的骨，爱琳没有熊，特效就从空气里冒出来。⇒ `ch03@Dash00.evn` 的两条 `RenderEffect` 改指 CH03 自己的。
- ⚠ **CH03 的 WP00 特效不止这两条**：`MutuStand-P00..P03` / `K00` / `K01` / `MutuJump-P00` /
  `MutuCrunch-P00` / `MutuCrunch-K00` 的 CH03 版也都在，而 ch03 的 `.evn` 现在还指着 CH01 的
  （其中 6 条同样挂在 `Bone_Wp00_04` 这根熊骨上）。Jab00 / Dash01~05 **没有** CH03 版，只能继续用卡希尔的。
## §23 ★★★★ 空手跑是 `Run-F00`，卡希尔那套是**单手托着熊跑**；换源要看「摆臂 vs 迈腿反相」（✅实机 + ✅离线）

**结论**：`Run-F%02d` 的 N = 当前武器的 `WAnimIdx` ⇒ 拿武器跑放 `Run-F01/02/03`（手锁在枪上，摆幅 0~8°，
本来就不该摆），**空手 / 格斗模式跑放 `Run-F00`**。卡希尔的 `Run-F00` 是给她那只泰迪熊编的：
右前臂几乎不动，手里没熊就成了「拎着个看不见的东西」（用户 2026-09-16 实机）。

**选源的判据不是「像不像」，是两个可量的数**（全部 16 个角色的 `Run-F00` 都量过）：

| | 上臂 L/R | 前臂 L/R | 手前后行程 L/R | 左手·左脚 |
|---|---|---|---|---|
| ch01 卡希尔（改前） | 58.5/49.0 | 34.3/**19.3** | 20.3/**16.2** | **−0.31** |
| ch102 瓦尔基里 | 78.3/**16.5** | 19.5/2.5 | —— | −0.80 |
| **ch00 泰尔（选中）** | 87.8/76.3 | 21.6/**21.6** | 33.9/34.0 | −0.81 |
| ch03 爱琳（改后） | 87.8/76.3 | 25.7/**25.7** | 41.2/**41.8** | **−0.97** |

- **「左手·左脚」= 同侧手和脚的前后位移（去均值归一化）的内积**，真人跑步是反相 ⇒ 越接近 −1 越协调。
  卡希尔只有 −0.31，这就是「不协调」的量化说法。
- ⚠ **瓦尔基里在这一条上是全表最差的**（左右上臂 78.3 vs 16.5，差 78.8°）—— 她把步枪架在左臂上跑。
  「持枪动作学她」不等于「空手跑也学她」。
- ★ ch00 的 `DisplayHeight` 和爱琳同为 **75**（ch02 是 90、ch100/ch103 是 92），前臂摆幅左右**恰好相等**。
- ★★ **光抄旋转不够，还要对相位**：两套跑步循环的起点差约 **1/8 个周期**，直接抄过来手比脚差半拍
  （−0.66）。`anim.leg_sync_phase()` 在动作自己的帧格上把一个周期试一遍、取「手·脚」最负的那个
  （实测 −3 帧 ⇒ −0.97）。**这个偏移是算出来的，不是写死的常数**（铁律 10），换源会自己重算。
## §24 ★★★★ 爱琳**自带的音效只有一个**：选人语音。武器音全是借的（✅逐目录数过）

**结论**（用户 2026-09-16 问「艾琳没有自带的音效吗」）：**没有**。
对着 `main/Pack_decrypt/`（原版解密树，没经过本项目任何修改）逐目录数：

| 目录 | 有哪些角色 | ch03 |
|---|---|---|
| `Sounds/CharacterChanger/` | Ch000~Ch002 / Ch100~Ch110 / **ch003** | ✅ **就这一个** |
| `Sounds/FX/wp/` | ch00 ch01 ch02 ch98 ch109 ch110 common | ✗ |
| `Sounds/FX/wplite/` | ch00~ch02 ch100~ch108 … | ✗ |
| `Sounds/FX/char/KR/char/` | ch00~ch02 ch98 ch100~ch110 | ✗ |
| `Sounds/FX/char/KR/voice/` | ch00~ch02 ch100~ch110 | ✗ |

⇒ `Sounds/CharacterChanger/ch003.ogg`（选人界面念她名字）是她在整个客户端里**唯一**的原生音。
没有台词、没有脚步 / 受击体感音、没有武器音。`tools/ch03_sounds.py` 配的 11 个**全是借的**。

★★ **选人语音是各角色念自己的名字，不是旁白** —— 10 个角色测出来 146~459 Hz 各不相同 ⇒
`ch003.ogg` 就是**爱琳本人的声线样本**，这是全工程唯一一条能给她定音高的证据：

| 角色 | 爱琳 | 卡希尔 | 泰尔 | 프로코 | 앨리어스(100) | 진(101) | 발키리(102) |
|---|---|---|---|---|---|---|---|
| 选人语音 F0 中位 | **324** | **459** | 329 | 286 | 269 | 242 | 216 |

⇒ **卡希尔是全表最高的一条**，比爱琳自己高 135 Hz —— 用户说「一听就是卡希尔」不只是「同一份文件」，
音域本来就不对。★ 判据用选人语音、不用实战吆喝：后者只有 8~24 个有声帧，自相关会锁到共振峰上，测不准。

★ **借的时候别借同屏角色的招牌音**（用户实机落锤）：3 号武器原先借卡希尔的 `ch01@Attack03.ogg`
（캐논왈츠 火箭炮，1.53 s、18% 低频的轰鸣），卡希尔就在旁边打，一听就是同一发。
换成 `FX/wp/common/weapon@watergun.ogg` —— **原版发了却一个地方都没引用的音**
（`Data/` 全部 ini + 61 个 `.evn` + 全部 `.efx` + exe 里都搜不到；`FX/wp/common/` 整个目录只有它）。
0.51 s / 低频 11% / 起音 0.021 s，对着 `[ch03-03]` 的 `LoadingTime=300` `ReloadTime=5300`
那种单发慢抛正好，而且「水枪」正对她发射的 **포션（药水）**。铁律 13 的又一例：**先搜，再动手造**。

同一条线上第二例：3 号武器的**换弹**（`ch03@Reload03.evn` 里的 `ch01@Reload03.ogg`，
0.58 s / 质心 4342 Hz / 49% 高频 = 宝箱炮的金属搭扣）换成 **`Water-Load.ogg`** ——
同样没有任何引用，0.82 s / 质心 2016 Hz / 高频只占 15%，一声低柔的「灌进去」。
⚠ 同目录的 `Water-Fire` 被 `weapon.ini` 用着、`Water-Spawn` 被 exe 用着，**只有 Load 和 Hit 是闲的**。

🔴 **1 / 2 号武器维持泰尔（ch00）的，别再动**（D17）。1 号 2026-09-17 换成过
`ch102@Attack01.ogg`（발키리 아머드 머신건），**用户实机听完说「新的不好听」，当天改回**。
⇒ 下面这两条是**已知代价，不是待办**：① `[CH03-01]` 的 `CoolingTime=90` 是全游戏最快的
连射枪（발키리 130 / 프로코 140 / 泰尔 **220**），而左轮有效 0.55 s，90 ms 一发叠六层；
② 泰尔是基础角色、天天同屏。**客观指标证不出「好不好听」** —— 3 号那次是用户先抱怨才改的，
这次是我自己推出来要改的，推错了。这类换法**先问，以耳朵为准**。

★ **两条路子别记混**：`weapon.ini` 点名的音（发射 / 命中 / 落地）走 `ch03_sounds.py` 的
`MAPPING`（拷文件到 `FX/wp/ch03/`）；**动作事件上的音**（换弹 / 脚步 / 受击 / 吆喝）
走 `anim.py` 的 `EVN_RES_REMAP`（改 `.evn` 里的 `<ResourcePath>`）。找错地方会白找一圈。

**2026-09-16 已整体换掉**（用户拍板「按结构角色逐条挑」，D15）：嗓音 / 脚步 / 拳脚 17 条
全取 **ch100 앨리어스（艾丽亚丝）** —— 有整套嗓音 + 体感音、**不是基础角色**、而且是三个候选里
音高离爱琳最近的女角。落到 31 个 `.evn` 上。**剩 12 条换不掉**（全游戏只有 ch01 有这些文件）：
`MutuStand-P01-2/3/4`、`MutuStand-P03`、`MutuJump-P01`、`Martial04`、`Dash03_01/02/03`，
外加三条**永远不会响**的（`dash05` / `Dash04_01` / `Dash-B04_01` 是 4/5 号武器的，她没有）。
守卫：`anim.audit_leftover_ch01()` 每次跑都和 `UNMATCHED` 对账，对不上直接断言失败。
★ 2026-09-17 这 12 条**逐条查明了放不放得出来**，见 §25。

## §25 ★★★★★ 「去卡希尔化」收口对账：残留 18 条里 **13 条根本放不出来**（✅逐条查明）

**结论**：把 `.evn` 里还指着卡希尔的东西全列出来是 **12 条音 + 7 条特效**，
但其中 **13 条挂在爱琳永远放不出来的动作上** —— 真正还听得见 / 看得见的只有
**5 条格斗打击叠加层**（`MutuStand-P01-2/3/4`、`MutuStand-P03`、`MutuJump-P01`，
都是打击层不是嗓音）。第 19 条 `Jab00` 的特效**已经改掉**（D16）。

**为什么放不出来**：爱琳在 `Data/ChrProps.ini` 的 `[3]` 段里**只有 `Dash00-*` 一套参数**
（16 个角色里 **13 个**都只有 Dash00；只有 0/1/2/14/15 有 Dash00~05），
而冲刺攻击 `[ch03-dash]` 写死 `WAnimIdx=0` ⇒ 她**永远只放 `Dash00`**。
于是 `Dash01/02/03/04/05` / `Dash-B04` 上挂的 6 条音 + 6 条特效一辈子不触发
（`ch01@Martial04.ogg` 也在里面 —— 它挂在 `Dash02` 上，注释里写的「Dash02 武打音」没错）。

**`Bone_Wp00_*` 是什么**：不是「熊的独立骨架」，是**挂在右手上的武器骨链** ——
`Bip01_R_Hand → Dummy01 → Bone_Rweapon01 → Bone_Wp00_01 … _11`，静态局部平移累计 **79.7**。
卡希尔的泰迪熊蒙在这条链上；爱琳 `WMeshIdx=-1` ⇒ 链还在、没有网格。
⇒ 挂在 `Bone_Wp00_11` 上的特效在她身上会在**离拳头 69**（她骨盆到头才 24）的空气里炸开。

**判据在代码里**：`anim.audit_leftover_ch01("sound"|"efx")` + `UNMATCHED` / `UNMATCHED_EFX`
两张名单对账，名单一漂就断言失败（特效那半边是 2026-09-17 补的，之前只守着音）。

★ 顺带结掉两条老悬案：**`Run-B00`（空手后退跑）16 个角色里 11 个都没有** ⇒ 爱琳缺它是常态，
不是缺口，别再想着补；`Dash01/02/03` 的动作文件虽然在她目录里，但同理是死文件。

## §26 ★★★★ CH03 那 8 个「一处引用都没有」的特效**不是缺口** —— exe 按名字构造（🔍静态）

**结论**（回答「原版给爱琳画了特效我们没用上？」）：**没有漏用**。
全盘搜 5246 个文件（`Data/*.ini` + 全部 `.efx` / `.evn` / `.msh`）后，
`Effects/` 下 53 个 `CH03_*` 特效里有 8 个无人引用 —— 但同名件对 **16 个角色全都无人引用**：

| 特效 | 谁在引用 |
|---|---|
| `CH03_MutuHit00/01` `CH03_MutuGuardDamage` `CH03_DashHit00` | exe 字符串表（`0x6840de` / `0x66d73e` / `0x68417e`），引擎**按 `CH%02d_名字` 拼路径** |
| `Wing/Efx/CH03_WingDown.efx` | 同上（`0x685680`）—— 翅膀滑翔，只有 CH00~CH03 有 |
| `CH03_Wp03_*Continue01` / `EnemyContinue00` | 原版美术多画的档，CH01 那边同样没人引用 |

⇒ **数据侧不用做任何事**，爱琳自动就有。★ 判「某个资源是不是漏用了」不能只搜数据文件，
**必须连 exe 的字符串表一起搜** —— 引擎按名字构造路径的东西在数据里永远搜不到。

## §27 ★★ 中文版还有 3 份 `.smf` 帧数比它替换掉的那份少（和爱琳无关，但和 §3 同族）

`Data/Chinese.ini` 的重定向表把 25 个资源换成 `Images/Chinese/` 下的中文版。
按 §3 的查法把**全部 675 个 `.smf`** 逐个比过（`u32 版本 + u32 帧数`，675 个大小全自洽）：

| 中文版 | 帧 | 原版 | 帧 | 前缀逐字节同 |
|---|---|---|---|---|
| `RoomUserSlot.smf` | 27 | `NewUI2/` | 28 | ✅ 是截断（多出来那帧 246×14，像条文字带） |
| `ClearResultSlotLadder.smf` | 22 | `NewUI2/` | 24 | ✅ 是截断（多出来两帧 39×17，像天梯标签） |
| `ShopSlotImg.smf` | 20 | `NewUI2/` | 22 | ✗ 中文版是**另画**的，不是截断 |

**没查**这三处的越界会不会真发生（要逐个找 exe 里的取帧点）。
和爱琳无关 —— 她吃的是 `BigChrIcons_CN`（28 帧，§3 已补）和 `ChrIcons_CN`（32 帧，本来就够）。
记在这儿是因为**这类崩不会提前报警**，将来谁碰到 `C0000005` 可以先想到这条。

## §28 ★★★ 「蝴蝶炸完不消失」的排查过程：排除了三条（✅ 已结案，结论见 §29）

★ **已定案，本节只留「排除了什么」**。`HomingAngle=300` 当时被当成误排除 —— 它**确实是共犯**，
只是共犯的方式和当时猜的不一样（不是算出零速，是算出一个小到不蹭地的盘旋圈）。见 **§29**。

用户 2026-09-17 实机：爱琳 2 号武器 `씨드 폭탄` 分裂出的蝴蝶（`[ch03-02a]`，`Id=1003520`）
**有概率**在「最后撞到人的位置」定住、**继续扇翅膀、永不消失**；正常的撞人就没。

**`[ch03-02a]` 的三个数据异常**（她这套数据原版写好但从没跑过）：

| 异常 | 全表对照 | 结论 |
|---|---|---|
| `HomingAngle=300` | 另外两把追踪武器（`ch01-03` / `ch102-02`）都是 **30** | ⚠ 像多打一个 0，但**排除**（见下）|
| `Attribute=4` / `AttributeTime=2100` | **全游戏只有她这一个武器有** | 🤔 未排除 |
| `EffTrace` | 80 多个 `EffTrace` 用户里她是**唯一 `Type=Splinter`**（其余全 `Shot`）| 🤔 未排除 |

**已排除的三条**：
① **追踪算不出 NaN / 零速**：`0x47e53a` 起那段是 `fild HomingAngle → 限幅 → sin/cos 旋转速度向量`
   （`0x47e587`~`0x47e609`），**没有除法**，旋转还保模 ⇒ `300` 只是「每 tick 都能瞬间转向」，冻不住弹体。
② **目标中途消失有兜底**：`0x47e40d` 查不到目标就把 `[弹体+0x328]` 置 `-2` 并继续，不是早退。
③ **拖尾特效漏了也会自己停**：`ButterflyTail00.efx` 的 `SpawningTime=5` 秒、粒子寿命 0.6 秒。
   另外 `ch00-02a` 苹果碎片同样**没有** `ExplodedTime` 却不出问题 ⇒ 不是缺这个字段。

★★ **为什么现有日志答不了**：弹体诊断的追踪表原来只有 **64 格**，那一局登记了 **180 颗**弹体，
早被挤了三轮 ⇒ 「某颗弹体的 `PROJ.` 行断了」既可能是它被销毁、也可能只是被挤出表，
**两种情况在日志里长得一模一样**。已把表加到 512 并在挤掉还在 tick 的弹体时打 `PROJ~` 警告，
逐帧行也补上了 `寿命(+31c)` / `碰撞型(+32c)` / `追踪目标(+328)`。
⇒ 下次复现时**看日志就能分流**：还在打行 = 弹体活着卡住（查它的状态 / 寿命）；
不再打行却仍在扇翅膀 = 弹体没了、**留下来的是视觉对象**。两条路要查的地方完全不同。

## §29 ★★★★★ 蝴蝶绕尸体转：**追踪锁定后不再判死活**，而命中那侧判（✅实测 + ✅逐指令）

`Projectile::Homing`（0x47e347）两条路的判据**不对等**：

| 路 | 做的检查 |
|---|---|
| 还没锁定（`[弹体+0x328] == -1`）| 方框搜索 + 回调 0x47e29d 逐条筛：`0x417c72`（状态 `+0x54` 是 3/4 = 死）跳过、自己跳过、同队（`+0x15c`）跳过、`vft+0xac`（HP）≤0 跳过 |
| 已经锁定（0x47e3e0）| **只有两条**：句柄还解不解析得出对象（解不出 → `[+0x328] = -2`）、距离还在不在 `HomingRange` 内 |

⇒ **咬住之后才死的目标，追踪不撒手**；而命中侧的候选回调 0x47e037（`0x47e05b` 那一发）
**会**用 `0x417c72` 把死人跳过 ⇒ 永远追得上、永远打不着，直到尸体对象被回收。

**为什么全游戏只有蝴蝶看得出来**：每帧最大转角 = `HomingAngle / 7` 度
（0x47e53a：`fild HomingAngle` × `0.142857` × `π/180`），稳态盘旋半径 `R = v / (2·sin(θ/2))`：

| 武器 | HomingAngle | °/帧 | R | 结局 |
|---|---|---|---|---|
| `ch03-02a` 蝴蝶 | 300 | 42.9 | ≈ 1.37·v ≈ **8~11** | 圈小到不蹭地、又远小于 `HomingRange=200` 不脱靶 ⇒ **卡住** |
| `ch01-03` | 30 | 4.3 | ≈ 13.4·v ≈ 267 | 甩出 200 外，原版自己置 -2 |
| `ch102-02` | 30 | 4.3 | 13.4 | 比自己 `Size=15` 还小 ⇒ 必撞上 |

**实测**（2026-09-17 21:34，`logs/bshook_20260917_213135_pid20424.log`）：弹体 `3A439A00`
咬住怪物句柄 `1100029`，服务端 21:34:42.136 收到 `0x0408` 报它 HP 归零（`0x0010c8fd` @ (2052,633)），
此后该弹体连续 17 帧绕 (2052.5, 578) 画半径 **8.4** 的圆（=公式在 v≈6.2 时的值），
`+0x328` 一直是 1100029 没松口。**修法见 D18。**

⚠ 同一份日志里 42 颗蝴蝶有 21 颗**一行轨迹都没有** —— 不是没跑，是诊断表在骗人（弹体对象池化，
同地址一局能发 6 次；`proj_tick_log` 撞上第一个同地址的旧格就按它的句柄判并 `return`）。
已修：`proj_track_add` 先作废同地址旧格，tick 侧 `return` 改 `continue`。
★ `寿命(+31c)` / `碰撞型(+32c)` 这两个偏移**猜错了**，全程恒定 33554431 / 0，别再拿它们判事。
