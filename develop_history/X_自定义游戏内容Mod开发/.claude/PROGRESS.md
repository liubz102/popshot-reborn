# X_Mod PROGRESS —— 现在在哪

**只保留当前状态，全文 ≤ 150 行。** 做完的事从「正在做」挪走，不留历史；流水账进 `sessions/`。

最后更新：2026-09-19（会话 14）

---

## 现在在哪

**X5「开放中文版漏掉的 18 把 3 级武器」全部做完（§45 / D35）：代码 / 数据 / 资源卷 / 服务端都已落地，
两套运行时 4015 项全绿。现在等实机验 V39~V41。**
X4 的 V36~V38、X3 的 V27~V30 / V33~V35、X1 / X2 的 V15~V25 仍在等，见下面那张表。

### 会话 14 做的：开放 18 把 3 级武器（§45 / D35）

用户报「重机枪 爆裂3 游戏里没有、物品库也看不见，可它的模型明明在」。根因不是资源缺失：
中文版 `ShopItem-Chn.ini` 比韩版少 622 条，武器差 18 件（**1 号槽和 3 号槽的 `D3/R3/F3` × 3 角色**）
—— `weapon.ini` 不在 `Chinese.ini` 的重定向表里，小节和**全部美术**一件不缺，缺的只是目录条目。
新工具 `tools/openweapons.py` 把韩版那 18 组 `[Item-]` / `[Stock-]` **原样抄回**（插在 goldwp 块之前，幂等）。
顺带修掉 `tools/shopdata.py` 一个静默 bug（同 id 只认先遇到的那一节 ⇒ `[Stock-]` 在前会丢
`Tag`/`PartFlag`）。产物重跑：物品表 1879 → 1897 件、图集 673 → 691 图；`server/data/` 已
`backfill`（`items.json` +27、`shop.json` +18，留了 `.bak`）。细节见 `sessions/2026-09-19-06.md`。

★ **还有两批「只有模型」的没开**（§45，要造资源，成本同 X3 那 9 把，等用户拍板）：
爱琳 `ch03W0111..0333` **27 件** D/R/F 全套、布洛克 `ch02W0091..0093` **3 件**。
效果图：`logs/ch03-weapons-all27.png`（爱琳 27 件全图，用户 2026-09-19 点名要的）/
`logs/ch02-unused-weapons.png`。**用户说先不做，看完效果图再决定。**

### 再往前的三轮（都做完了，细节在 sessions/）

| 会话 | 做了什么 | 细节 |
|---|---|---|
| 13 | 闯关里数值被冲回 ini 原值：改写武器表有**两条**路，`0x48ADB0` 那条不经过 `Load`，已补钩子 `WTAB_MERGE_VA`；`test_patchsites_wtab` 改成**穷举全镜像**核调用者（§44）| `sessions/2026-09-19-05.md` |
| 12 | 准星弹格补画（`tools/aimring.py` 补 `2..20` 共 13 个容量，图集 19 → 32 帧；`0x48ca0d` 改写成跳我们的表，`> 20` 仍走圆环）+ 管理页参数范围收紧 + 弹窗定宽 5 列 grid + 三处统一 PVE 在前（§43 / D33 / D34）| `sessions/2026-09-19-04.md` |
| 11 | PVE/PVP **由服务端在开局握手前说**（`0x0F01` 多一个模式字节，`0xFF` = 只换表）⇒ 属性改动一律下一局生效（§42 / D32，推翻 D27）| `sessions/2026-09-19-03.md` |

**一直没改的**：版本号（仍 **0.4.1**）、原版 `weapon.ini` 的数值、原版那 6 张准星盘。

## 下一步

1. **实机复验 V39~V41（X5）+ V36~V38（X4）+ V31~V35**，顺带把一直欠着的 V27~V30 一起做了。
2. 发版：客户端包 + 服务端包都要重打（资源卷、服务端数据、**`bshook.dll` 都变了**）。
3. 等用户看完 `logs/ch03-weapons-all27.png` 再决定做不做爱琳那 27 件（§45）。

## 当前卡点

**没有卡点。** 资源卷已重打（用户 2026-09-19 22:2x 同意后跑的 `stop.bat` -> `build-pack -Verify`
-> `launch.ps1`：只重写 `Data~.pkn` 一卷，`verify` 17131 文件 0 差异），服务端已重启
（`gs_ctl shop-backfill` 回「没有要补的」= 它看到的就是新表）。等实机验 V39~V41。

★ 老客户端里**买过的自定义武器照旧能用**：物品 id 没变，客户端按 `Tag` 重新查 `weapon.ini` 拿新 Id。
★ 服务端数据变了 ⇒ 必须 `stop.bat` 再 `start.bat` 一次（铁律 7）。

## ⏳ 待用户验证

**X5 · 会话 14 的 18 把 3 级武器（资源卷和服务端都已经换好、客户端也重开过了，直接验）**：

| # | 怎么做 | 期望看到 | 状态 |
|---|---|---|---|
| V39 | 管理页物品库搜「爆裂3」 | 多出 **6 条**（左轮手枪 / 狙击枪T1 / 复古短枪 / 华尔兹加农炮 / **重机枪** / 火箭炮），图标是真图不是问号；搜「极速3」「复合3」各 6 条 | ⏳ |
| V40 | 游戏里进商店 → 武器 | 三个角色各多 **6 把** 3 级枪，**18 级、18000 金**；买一把进仓库、装上、进图开枪 | ⏳ |
| V41 | 拿 3 级和同槽 2 级对比 | 伤害更高、图标和模型是**各自那一把**（不是借 2 级的）；`BigShot.rpt` 没有新崩溃 | ⏳ |

★ 判据：18 件的 id 是 `X12001/3/4/6/7/9 3`（`1120013` … `3120093`）。
★ 没重打资源卷的症状：商店 / 仓库里那一格**是空的**（客户端在自己的 `ShopItem-Chn.ini` 里查不到图标）。

**X4 · 会话 12 的准星弹格（★ `bshook.dll` **和资源卷**都换了 —— 两个必须一起换，
只换 dll 会让帧 19..31 在老图集里查不到。先 `stop.bat` 再 `start.bat`，客户端重开）**：

| # | 怎么做 | 期望看到 | 状态 |
|---|---|---|---|
| V36 | 管理页把某把自定义武器的弹匣改成 **15**，保存，**重开一局** | 准星外圈是 **15 个格子**，打一枪掉一格（以前是一个连起来的光滑圆环）| ⏳ |
| V37 | 同上改成 **20** / **4** / **12** 各试一次 | 都是「一格一发」，格子数 = 弹匣数；**改成 21 或更大**则回到光滑圆环（这是说好的）| ⏳ |
| V38 | 管理页弹窗里试着填 **101** 发弹匣、伤害 **501**、初速 **1001** | 每格是「项目名 · 限 a ~ b · 输入框 · 单位 · 参考 N」，**输入框上下对齐成一条线**；超范围那格输入框变红、范围变红、**输入框正下方一行**红字「要在 1 ~ 100 之间」（不折行），顶上「⚠ 有 N 处填得不对」，**保存键按不动**；改回范围内按钮立刻恢复 | ⏳ |

★ V38 顺带看：物品浮窗和弹窗底下的预览，两套数值都该是 **PVE 在上、PVP 在下**
（和上面两栏「PVE 左 / PVP 右」一个方向）。
★ 顺带看一眼**原版武器**（泰尔左轮 6 发、爱丽 14 发、瓦尔基里 18 发）：外圈应该**和以前一模一样**。
★ 日志判据：`logs/bshook_*.log` 该有一行「★准星弹格 @ 0048CA0D: 弹匣 2..20 全部有刻度盘…图集 19 → 32 帧」。

**X3 · 会话 11 的修复（★ `bshook.dll` 和服务端都换了：先 `stop.bat` 再 `start.bat`，客户端也要重开）**：

| # | 怎么做 | 期望看到 | 状态 |
|---|---|---|---|
| V33 | 管理页把某把枪的 **PVE** 身体伤害改成一个很好认的数（比如 1），保存；**然后**开一局闯关 | **第一局就按 PVE 那套打**（以前要先打一局废的、或者再存一次才生效） | ✅ **内存核对通过** |
| V34 | 打完闯关回房间，改开**对战**房再打一局 | **按 PVP 那套打**（以前会继续用 PVE 的数值） | ⏳ |

★ **V33 已用活内存核过（2026-09-19 21:22，用户挂在 `Quest03_1` 难度 1 里）**：9 把武器 × 12 格
和 `weaponcfg.effective(…, 'pve')` 逐格比对，**0 处对不上**；同一张图同一难度，21:09 那次（旧 DLL）
是 ini 原值。另核过「`weapon.ini` 写了但服务端没取到」= **0 格**；标「服务端没发这一格」的
是 ini 本来就没写这个键（客户端用解析器缺省值），而且**这些格在管理页照样能设、也照样生效**
（机枪的溅射伤害 / 溅射范围 / 腿部伤害 / 最大初速 ini 里都没有，设了就进内存）。

★★★ **会话 13 在这儿抓到真 bug（§44）**：闯关进图时 `0x48adb0` 会加载一份额外的 ini、
把小节逐条解析进同一张武器表，**不经过 `WeaponTable::Load`** ⇒ 我们写的 PVE 值被冲回 ini 原值。
已补钩子（`WTAB_MERGE_VA`），`bshook.dll` 重编 → 清单 V0.4.1 = `4a7a1267…`。**验 V33 要用新 DLL。**

★★ 验的时候**别只填弹匣**：PVE 栏没填的格一律用参考值，看着就像「没生效」。填**身体伤害**
一眼能看出来；弹匣填 **≤20** 才有刻度（>20 是光滑圆环，会话 12 定的）。
| V35 | **局内**改弹匣 / 伤害并保存 | 这一局**什么都不变**（准星外圈也不变）；退回房间再开一局才是新值。管理页弹窗上有「⏱ 属性改动下一局生效」那行字，保存回执也这么说 | ⏳ |

★ 日志判据：`logs/server.out` 每次开局都该有一行「发 0x0F01 … 模式=任务/对战；开局」，
且它排在「广播开局握手 0x0400」**之前**；`logs/bshook_*.log` 对应一行
「收到自定义武器表 vN：9 条，模式 任务/对战」。管理页保存那一发的模式该是「不施加」。

**X3 · 会话 09 欠着的**（这次一并做）：

| # | 怎么做 | 期望看到 | 状态 |
|---|---|---|---|
| V27 | 管理页「金币 / 经验」页给自己发一把 `1920001`；进仓库看、装备、进图开枪 | 仓库有金色图标、名字「左轮手枪 自定义」，能装备；手里是金枪，子弹 / 拖尾 / 命中特效银白 | ⏳ |
| V28 | 对战房里，管理页物品库 → 该武器「自定义属性」→ 右栏 PVP 身体伤害改 1 → 保存 → **重开一局**打 bot | 不用重登，**下一局**掉血明显变少；改回参考值就恢复 | ⏳ |
| V29 | 看提示框：商店（若上架）/ 仓库 | 第一行「仅显示PVP属性，PVE的请看管理页」**不折行**，数值是 PVP 那套；改说明文后**重新登录**仓库提示框才更新 | ⏳ |
| V30 | 一台仍是 V0.4.0 的客户端连本地服务端 | 被版本门禁拒（预期）；服务端日志里对它**没有**「发 0x0F01」 | ⏳ |

**已确认修好**：V31（弹匣按容量换弹、准星外圈出现）、V32（结算不卡死）—— 用户 2026-09-19 实机确认。

**X1 / X2 遗留（2026-09-18 起等）**：V15 声线 / V16 `Jab00` 特效 / V18 低等级号能选爱琳 / V20 蝴蝶不绕尸体 /
V21 蝴蝶打中 bot 减速 / V22 商店没有爱琳 / V23 bot 蹭图腾 / V24 「♥HEAL」不崩 / V25 Alt+Tab 不崩 ——
做法和期望见 `sessions/2026-09-18-03.md`。

**出问题时一次给全这 4 样**：① 卡在哪一条；② `game_patched/BigShot.rpt` 的**最后一段**；
③ `logs/bshook_*.log` 的**最后 50 行**；④ 崩溃地址（我拿 `tools/re_bs.py dis <地址>` 定位）。

---

## 给接手会话的十句话

1. ★★★ **武器 Id 不能随便编**（§41 / D31）：客户端拆数位当弹药数组下标，必须满足
   「系列 ∈ [3,5] 且 档*10+槽 < 50」。现行 `1 0 0C 3 S 5`。`test_weaponini` 守着，别绕过去。
2. **原版武器一个字节不许改**（用户 2026-09-19）：`test_weaponini` 钉着 `weapon.ini` 前 221519 字节的 sha256。
   想调数值去管理页物品库的「自定义属性」弹窗，只有 9 把自定义武器能调。
3. ★★ **自定义武器的数值链**：`server/data/weapons.json` → `weaponcfg.effective()` → `0x0F01` → bshook 写内存。
   `weapon.ini` **每次进图重读**（`Load` 会把我们写的值冲掉），所以 hook 在 `Load` 返回点上还要再写一遍（§36 / D26）。
4. ★★★ **PVE / PVP 由服务端在开局握手里说，不是 hook 猜的**（§42 / D32，推翻了 D27）：
   `0x0F01` 头上那个模式字节，`0xFF` = 只换表不写内存 ⇒ **属性改动一律下一局生效**（用户拍板）。
   开局那一发必须排在 `0x0400` **之前**（弹匣容量进图时就快照走了）。`test_room` 守着这两条。
5. 自定义资源全部由 `tools/goldwp/build.py` 从爆裂 3 母本生成，**幂等**；改色规则在 `palette.py`，清单在 `spec.py`。
   手改生成物会在下次重跑时被盖掉。
6. `default.amf` 只许经 `tools/amftool.py` 改（定长记录，写错一格整张表读歪，§37）。
7. `shop_items.json` **FORMAT 2**、`bot_weapons.json` 13：改字段必须和重生成的产物同一次落地。
8. 部位码 `92` = 自定义武器（`tools/shopdata.PART_KIND`）；**物品 id `X92000S` 没变过**，改的只是武器 Id。
9. **版本号只由用户定**：`build-ver.config` 里是尚未发布的开发版本（现在 0.4.1，线上 V0.4.0）。别自己抬号。
10. ★ 跑测试：`runtime\python\python.exe test\run_tests.py`（3.14）和 `runtime-win7\python\python.exe test\run_tests.py`（3.8），
    全量名单现扫 `test/test_*.py`。**别加 `-j1`、别 `| tail`**。
