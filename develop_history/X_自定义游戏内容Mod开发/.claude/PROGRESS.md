# X_Mod PROGRESS —— 现在在哪

**只保留当前状态，全文 ≤ 150 行。** 做完的事从「正在做」挪走，不留历史；流水账进 `sessions/`。

最后更新：2026-09-20（会话 17）

---

## 现在在哪

**X7「管理页来源筛选 + 批量多选添加」代码做完（§48 / §49 / D39 / D40 / D41）：
两套运行时 4046 项全绿，而且已经在真页面上逐条点过。只动前端三个文件，
服务端一行没改 ⇒ 不用重启服务端、不用重打包。**
X6 的 V42~V47、X5 的 V39~V41、X4 的 V36~V38、X3 的 V27~V30 / V33~V35、X1 / X2 的 V15~V25 仍在等。

### 会话 17 做的：X7（用户 2026-09-20 第二批需求）

两条：① 管理页加「全部物品 / 原版物品 / 自定义物品」筛选下拉；② 「添加」改成能一次勾多件。

- **来源筛选**（D39）：判据 `sourceMatches()` 折进七处共用的 `dropdownsMatch()`，
  四个配置页 + 「选择物品」/「修改仓库」/「发送奖励」弹窗 + 「装备卖出」页各挂一个 `<select>`
  （用户点名「凡是有筛选条的地方都加」）。判据就是物品身上的 `custom`，catalog 一直在发（§48）
  ⇒ **服务端零改动**。实测物品库 **844 = 826 原版 + 18 自定义**，不重不漏。
- **批量多选**（D40）：`openPicker` 加 `max` / `chosen` / `ownedLabel` 三个选项；
  `addEntry` 抽出 `makeEntry()` 后改多选（已上架 /已有配方的画灰点不动，掉落页不灰）；
  **配方材料做成整组编辑器** —— 4 格点哪个都开、当前几种自动勾上、取消勾选 = 移除（用户第二轮追加）。
  单值字段那三处（换商品 / 换产物 / 换掉落材料）**仍是单选**，用例用括号配平扫着这条不变式。
- **顺带**（D41）：掉落页加第四个下拉之后实测会折成两行，把「筛出 x / y」搬进面板标题那一格
  现成的 `#cfgCount`（这仓库为同一理由搬过一次）。`#cfgShown` 整个删掉。
- 两套运行时 **4046** 项全绿（新增 `test/test_adminpicker.py` 24 条 + 单选守卫 1 条）。

★ **还有两批「只有模型」的没开**（§45）：爱琳 27 件、布洛克 3 件。效果图已交
（`logs/ch03-weapons-all27.png` / `logs/ch02-unused-weapons.png`），**用户说先不做**。

### 再往前的两轮（都做完了，更早的翻 `sessions/`）

| 会话 | 做了什么 | 细节 |
|---|---|---|
| 16 | `tools/goldwp` → `tools/custom-weapon`；18 把自定义武器在「新商品」里置顶（`shop.is_pinned`，D38）| `sessions/2026-09-20-01.md` |
| 15 | 第二批 9 把自定义武器：批次化 `spec.BATCHES`、部位码 93 / 武器 Id `100C4S5`、P7 樱花粉 + 抹茶绿、默认名拆「自定义1 / 2」（§46 / §47 / D36 / D37）| `sessions/2026-09-19-07.md` |

**一直没改的**：版本号（仍 **0.4.1**）、原版 `weapon.ini` 的数值、原版那 6 张准星盘、`bshook.dll`。

## 下一步

1. **实机复验 V48~V49（X7）+ V42~V47（X6）+ V39~V41（X5）+ V36~V38（X4）+ V31~V35**，
   顺带把欠着的 V27~V30 做了。
2. 发版：客户端包 + 服务端包都要重打（资源卷、服务端数据都变了；**`bshook.dll` 这次没变**）。
3. 等用户看完 `logs/ch03-weapons-all27.png` 再决定做不做爱琳那 27 件（§45）。

## 当前卡点

**没有卡点。** 会话 17 结束时：

- ★ 服务端是**会话 17 自己起的**（`tools/launch.ps1 -NoGame`，只有服务端没有客户端，§49），
  管理页在 http://127.0.0.1:27810/admin 挂着，可以直接点。不要了就 `stop.bat`。
- X7 **只动前端**（`admin.js` / `admin.html` / 两个测试文件）⇒ **不用重启服务端**，浏览器强刷即可（§49）。
- 会话 16 那条「重启服务端，新商品置顶才生效」随这次重起自然满足了。
- **发版前仍欠一次资源卷**：会话 16 换 `INI_MARKER` 时重写了 `weapon.ini`，当时 `Data~.pkn`
  被运行中的客户端占着、`build-pack` 自己拦住了。影响面为零（改的是 `;` 注释行，客户端跳过），
  但 `tools/gamedata-stamp.json` 因此和明文树对不上，下次 `build-pack` 会顺带重跑 `update-gamedata`。

★ 老客户端里**买过的第一批自定义武器照旧能用**：物品 id 和武器 Id 一个字节没变，只有中文显示名变了。

## ⏳ 待用户验证

**X7 · 会话 17 的管理页改动（只动前端 ⇒ 浏览器 `Ctrl+F5` 强刷就是新的，服务端不用重启）**：

**agent 已在真页面上验过的**（服务端还挂着，见「当前卡点」；改动都放弃修改复原了）：
物品库 844 = 826 + 18 · 四页 + 两个弹窗的新下拉 · 货架批量加 3 条（已上架的灰着点不动）·
配方材料整组编辑（预勾选 / 上限红条 / 换材料后数量不变）· 单值字段三处仍 `multi=false`。

| # | 怎么做 | 期望看到 | 状态 |
|---|---|---|---|
| V48 | **用你自己的游戏账号**登管理页 → 「装备卖出」页 | 筛选条上有第三个下拉「全部物品」，选「自定义物品」只剩自定义武器 | ⏳ `admin` 没有同名游戏账号 ⇒ 那一页整块是锁的（原有行为），agent 验不了 |
| V49 | 「材料掉落」页，**在你平时用的窗口宽度下**筛一下 | 筛选条**是一行**（标题那格写「筛出 42 / 52 条规则」）| ⏳ 1320 px 下实测一行；更窄会折（和加这个下拉之前一样）|

**X6 · 会话 15 的第二批 9 把自定义武器（资源卷和服务端数据都已换好，起来就能验）**：

| # | 怎么做 | 期望看到 | 状态 |
|---|---|---|---|
| V42 | 管理页物品库搜「自定义」 | **18 条**：9 条「xxx **自定义1**」（金 + 银白图标）+ 9 条「xxx **自定义2**」（樱花粉 + 抹茶绿图标），图标都是真图不是问号 | ⏳ |
| V43 | 管理页「金币 / 经验」页发一把 `1930001`，进仓库 → 装备 → 进图开枪 | 手里是**粉 + 绿**的枪；子弹 / 拖尾 / 命中特效**也是粉绿**，没有残留的紫 | ⏳ |
| V44 | 三个角色 × 3 个槽各试一把（`X930001..3`）| 9 把的模型 / HUD 图标 / 商店图标都是**各自那一把**，不是借第一批的 | ⏳ |
| V45 | 对新武器开「自定义属性」弹窗，改 PVP 身体伤害 → 保存 → **重开一局** | 下一局生效（同第一批）；`logs/bshook_*.log` 有「按【…】写入 **18/18** 条」 | ⏳ |
| V46 | 看老的 9 把 | 名字变成「xxx **自定义1**」，**配色仍是金 + 银白**，数值和说明文一个字没丢 | ⏳ |
| V47 | 游戏里进商店（不用点任何标签，进去默认就是「新商品」）| **第 1 页 8 格全是自定义武器**，第 2 页也是，第 3 页前两格收尾；点「上市顺序」仍是这 18 把在前（只是内部倒过来，自定义2 在前）；点「武器」标签则**恢复原来的排法**（会话 16）| ⏳ |

★ 判据：新武器物品 id `X93000S`（`1930001` … `3930003`）、武器 Id `1 00C 4 S 5`（`1000415` …）。
★ 没重打资源卷的症状：商店 / 仓库里那一格**是空的**（客户端在自己的 `ShopItem-Chn.ini` 里查不到图标）。
★ **出厂不上架**（同第一批）—— 要在商店买得到得自己去管理页上架。

**X5 · 会话 14 的 18 把 3 级武器（资源卷和服务端都已经换好、客户端也重开过了，直接验）**：

| # | 怎么做 | 期望看到 | 状态 |
|---|---|---|---|
| V39 | 管理页物品库搜「爆裂3」 | 多出 **6 条**（左轮手枪 / 狙击枪T1 / 复古短枪 / 华尔兹加农炮 / **重机枪** / 火箭炮），图标是真图不是问号；搜「极速3」「复合3」各 6 条 | ⏳ |
| V40 | 游戏里进商店 → 武器 | 三个角色各多 **6 把** 3 级枪，**18 级、18000 金**；买一把进仓库、装上、进图开枪 | ⏳ |
| V41 | 拿 3 级和同槽 2 级对比 | 伤害更高、图标和模型是**各自那一把**（不是借 2 级的）；`BigShot.rpt` 没有新崩溃 | ⏳ |

★ 判据：18 件的 id 是 `X12001/3/4/6/7/9 3`（`1120013` … `3120093`）。没重打资源卷的症状 = 那一格**是空的**。

**X4 · 会话 12 的准星弹格（★ `bshook.dll` **和资源卷**都换了 —— 两个必须一起换，
只换 dll 会让帧 19..31 在老图集里查不到。先 `stop.bat` 再 `start.bat`，客户端重开）**：

| # | 怎么做 | 期望看到 | 状态 |
|---|---|---|---|
| V36 | 管理页把某把自定义武器的弹匣改成 **15**，保存，**重开一局** | 准星外圈是 **15 个格子**，打一枪掉一格（以前是一个连起来的光滑圆环）| ⏳ |
| V37 | 同上改成 **20** / **4** / **12** 各试一次 | 都是「一格一发」，格子数 = 弹匣数；**改成 21 或更大**则回到光滑圆环（这是说好的）| ⏳ |
| V38 | 管理页弹窗里试着填 **101** 发弹匣、伤害 **501**、初速 **1001** | 每格「项目名 · 限 a~b · 输入框 · 单位 · 参考 N」且输入框**对齐成一条线**；超范围那格标红 + 正下方一行红字「要在 1 ~ 100 之间」（不折行）+ 顶上「⚠ 有 N 处填得不对」+ **保存键按不动**；改回去立刻恢复 | ⏳ |

★ 顺带：浮窗 / 预览两套数值都该 **PVE 在上 PVP 在下**；**原版武器**（泰尔 6 / 爱丽 14 / 瓦尔基里 18 发）
外圈和以前一模一样；`logs/bshook_*.log` 有一行「★准星弹格 @ 0048CA0D … 图集 19 → 32 帧」。

**X3 · 会话 11 的修复（★ `bshook.dll` 和服务端都换了：先 `stop.bat` 再 `start.bat`，客户端也要重开）**：

| # | 怎么做 | 期望看到 | 状态 |
|---|---|---|---|
| V33 | 管理页把某把枪的 **PVE** 身体伤害改成很好认的数，保存；**然后**开一局闯关 | 第一局就按 PVE 那套打 | ✅ **活内存核对通过**（2026-09-19，过程见 `sessions/2026-09-19-05.md`）|
| V34 | 打完闯关回房间，改开**对战**房再打一局 | **按 PVP 那套打**（以前会继续用 PVE 的数值） | ⏳ |
| V35 | **局内**改弹匣 / 伤害并保存 | 这一局**什么都不变**（准星外圈也不变）；退回房间再开一局才是新值。弹窗上有「⏱ 属性改动下一局生效」，保存回执也这么说 | ⏳ |

★★ **别只填弹匣**（PVE 没填的格用参考值，看着像「没生效」）—— 填**身体伤害**一眼能看出来。
★ 日志判据：`logs/server.out` 每次开局一行「发 0x0F01 … 模式=任务/对战；开局」且排在 `0x0400` **之前**；
`logs/bshook_*.log` 对应「收到自定义武器表 vN：**18** 条，模式 …」；管理页保存那一发该是「不施加」。

**X3 · 会话 09 欠着的**（这次一并做）：

| # | 怎么做 | 期望看到 | 状态 |
|---|---|---|---|
| V27 | 管理页「金币 / 经验」页给自己发一把 `1920001`；进仓库看、装备、进图开枪 | 仓库有金色图标、名字「左轮手枪 自定义」，能装备；手里是金枪，子弹 / 拖尾 / 命中特效银白 | ⏳ |
| V28 | 对战房里，管理页物品库 → 该武器「自定义属性」→ 右栏 PVP 身体伤害改 1 → 保存 → **重开一局**打 bot | 不用重登，**下一局**掉血明显变少；改回参考值就恢复 | ⏳ |
| V29 | 看提示框：商店（若上架）/ 仓库 | 第一行「仅显示PVP属性，PVE的请看管理页」**不折行**，数值是 PVP 那套；改说明文后**重新登录**仓库提示框才更新 | ⏳ |
| V30 | 一台仍是 V0.4.0 的客户端连本地服务端 | 被版本门禁拒（预期）；服务端日志里对它**没有**「发 0x0F01」 | ⏳ |

**已确认修好**：V31（弹匣按容量换弹、准星外圈出现）、V32（结算不卡死）—— 用户 2026-09-19 实机确认。

**X1 / X2 遗留（2026-09-18 起等）**：V15 声线 / V16 `Jab00` 特效 / V18 低等级号能选爱琳 / V20 蝴蝶不绕尸体 /
V21 蝴蝶打中 bot 减速 / V22 商店没有爱琳 / V23 bot 蹭图腾 / V24 「♥HEAL」不崩 / V25 Alt+Tab 不崩 ——
做法和期望见 `sessions/2026-09-18-03.md`。

**出问题时一次给全这 4 样**：① 卡在哪一条；② `game_patched/BigShot.rpt` 的**最后一段**；
③ `logs/bshook_*.log` 的**最后 50 行**；④ 崩溃地址（我拿 `tools/re_bs.py dis <地址>` 定位）。

---

## 给接手会话的十句话

1. ★★★ **武器 Id 不能随便编**（§41 / §47 / D31）：客户端拆数位当弹药数组下标，必须满足
   「系列 ∈ [3,5] 且 档*10+槽 < 50」。C 批 `1 0 0C 3 S 5`、P 批 `1 0 0C 4 S 5`，**只剩 4 个合法组合**，
   第三批必须改系列位。`test_weaponini` 守着，别绕过去。
2. **原版武器一个字节不许改**（用户 2026-09-19）：`test_weaponini` 钉着 `weapon.ini` 前 221519 字节的 sha256。
   想调数值去管理页物品库的「自定义属性」弹窗，只有那 18 把自定义武器能调。
3. ★★ **自定义武器的数值链**：`server/data/weapons.json` → `weaponcfg.effective()` → `0x0F01` → bshook 写内存。
   `weapon.ini` **每次进图重读**（`Load` 会把我们写的值冲掉），所以 hook 在 `Load` 返回点上还要再写一遍（§36 / D26）。
4. ★★★ **PVE / PVP 由服务端在开局握手里说，不是 hook 猜的**（§42 / D32，推翻了 D27）：
   `0x0F01` 头上那个模式字节，`0xFF` = 只换表不写内存 ⇒ **属性改动一律下一局生效**（用户拍板）。
   开局那一发必须排在 `0x0400` **之前**（弹匣容量进图时就快照走了）。`test_room` 守着这两条。
5. 自定义资源全部由 `tools/custom-weapon/build.py` 生成，**幂等**、**一次跑全部批次**（两个 ini 都是
   「砍掉标记再整块重写」，只跑一批会把另一批带走）。批次表 / 编号 / 选定的配色变体都在 `spec.BATCHES`，
   改色规则在 `palette.SCHEMES`。**不带参数跑就是重现盘上那一套**；手改生成物会被下次重跑盖掉。
6. `default.amf` 只许经 `tools/amftool.py` 改（定长记录，写错一格整张表读歪，§37）。
7. `shop_items.json` **FORMAT 2**、`bot_weapons.json` 13：改字段必须和重生成的产物同一次落地。
8. 部位码**一批一个**：`92` 第一批 / `93` 第二批（`tools/shopdata.CUSTOM_WEAPON_PART`），
   物品 id `X<部位码>000S`。⚠ 别为了省一个部位码去共用 92 换尾号 —— `weapon_variant()` 会误判（§47）。
   两批的默认中文名靠**韩文名后缀字母**分（`shopcfg.CUSTOM_WEAPON_ZH_BY_SUFFIX`：`C`→自定义1 / `P`→自定义2）。
9. **版本号只由用户定**：`build-ver.config` 里是尚未发布的开发版本（现在 0.4.1，线上 V0.4.0）。别自己抬号。
10. ★ 跑测试：`runtime\python\python.exe test\run_tests.py`（3.14）和 `runtime-win7\python\python.exe test\run_tests.py`（3.8），
    全量名单现扫 `test/test_*.py`。**别加 `-j1`、别 `| tail`**。
