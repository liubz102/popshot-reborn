# X_Mod PROGRESS —— 现在在哪

**只保留当前状态，全文 ≤ 150 行。** 做完的事从「正在做」挪走，不留历史；流水账进 `sessions/`。

最后更新：2026-09-20（会话 22）

---

## 现在在哪

**★ 会话 22（X11）：老服务器升级时，新武器自动补进运营配置。**
云上 V0.4.0 的 `items.json` 里 808 件，V0.4.1 新加的 36 把武器（18 自定义 + 18 原版隐藏）
一件都进不去（`ensure_files` 不覆盖已存在的配置，D7；手动出口 `shop-backfill apply`
在云上走控制通道，那个口默认关着）。⇒ 新增 `shopcfg.backfill_new_weapons()`，
开服在 `app._report_shop_config()` 里跑一发：**缺哪把武器补哪把，隐藏武器顺带上架，
自定义武器只进库不上架**（D48 / §56）。**每次开服都跑、幂等、不设标记**。

**拿用户下载回来的云端 data 副本实跑过**：808 → 844 件、货架 495 → 513 行，
上架的正好是那 18 把 3 级枪，自定义武器一把没上，第二次跑一行日志都不打。
两套运行时全量测试 4111 条全绿（新增 11 条用例）。**只动服务端 Python，
`bshook.dll` / 资源卷一个字节没碰** ⇒ 客户端包不用重打。

X10（会话 20）代码仍是最新：`0x0F02` + 绘制点补丁 `0x4554e7`，`bshook.dll` 为
V0.4.1 → `4ff5eacc…`。X9 的 V57~V60、X8 的 V52~V56、X7 的 V48~V49、X6 的 V42~V47、
X5 的 V39~V41、X4 的 V36~V38、X1 / X2 的 V15~V25 仍在等（多数是**观感 / UI**，
自动化核不了）。

★ **还有两批「只有模型」的没开**（§45）：爱琳 27 件、布洛克 3 件。效果图已交
（`logs/ch03-weapons-all27.png` / `logs/ch02-unused-weapons.png`），**用户说先不做**。

### 再往前的几轮（都做完了，更早的翻 `sessions/`）

| 会话 | 做了什么 | 细节 |
|---|---|---|
| 21 | 18 把自定义武器的参数链路跑通**无人值守端到端验证**，全绿（§53~§55 / D46 / D47）| `sessions/2026-09-20-06.md` |
| 20 | X10 说明文按场景切 PVE / PVP：新 opcode `0x0F02` + 绘制点补丁 `0x4554e7`（§52 / D45）| `sessions/2026-09-20-05.md` |
| 19 | X9 第一批 9 把的金色重做：`ramp` 渐变映射 + G4 原版金铠，**枪身换色 / 特效不动**（§51 / D43 / D44）| `sessions/2026-09-20-04.md` |
| 18 | X8 追踪两格：`0x0F01` 格式 2→3，所有弹体类通吃（§50 / D42）| `sessions/2026-09-20-03.md` |
| 17 | X7 管理页「来源」筛选（七处）+ 批量多选添加 + 配方材料整组编辑（§48 / §49 / D39~D41）| `sessions/2026-09-20-02.md` |
| 16 | `tools/goldwp` → `tools/custom-weapon`；18 把自定义武器在「新商品」里置顶（D38）| `sessions/2026-09-20-01.md` |

**一直没改的**：版本号（仍 **0.4.1**）、原版 `weapon.ini` 的数值、原版那 6 张准星盘。

## 下一步

1. **实机复验 V61~V68（X10）+ V57~V60（X9）+ V50~V56（X8）+ V48~V49（X7）+ V42~V47（X6）
   + V39~V41（X5）+ V36~V38（X4）+ V31~V35**，顺带把欠着的 V27~V30 做了。
2. 发版：客户端包 + 服务端包都要重打。★ **`bshook.dll` 变了两处** —— X8 的
   `WTAB_FORMAT` 3（装旧 dll 会把 `0x0F01` 整份丢掉、数值全退回资源包原值）和
   X10 的 `0x0F02` + 绘制点补丁（装旧 dll 只是房间里不跟着模式换，不崩）。
   所以**客户端包必须一起发**，靠 `server-ClientFilter.config` 那道门逼更新。
   ★ X11 之后**服务端包要重打一次**（`dist/PopShot-server_V0-4-1` 里是 X11 之前的
   `app.py` / `shopcfg.py`），否则上云还是补不了那 36 把。
3. 等用户看完 `logs/ch03-weapons-all27.png` 再决定做不做爱琳那 27 件（§45）。

## 当前卡点

**没有卡点，但要先重启服务端 + 换 dll 才验得了 X8 / X10**：

1. ★ **`stop.bat` 再 `start.bat`** —— 改了 `server/weaponcfg.py` / `shopcfg.py` /
   `gameserver.py`（铁律 7），而且客户端要装上新编的 `hook/bin/bshook.dll`
   （X8 那半边两个必须一起换，只换一个就整份丢包）。
2. 管理页那半边（`admin.js` / `.css`）是现读磁盘的，浏览器 `Ctrl+F5` 就够（§49）。
3. **发版前仍欠一次资源卷**：会话 16 换 `INI_MARKER` 时重写了 `weapon.ini`，当时 `Data~.pkn`
   被运行中的客户端占着、`build-pack` 自己拦住了。影响面为零（改的是 `;` 注释行，客户端跳过），
   但 `tools/gamedata-stamp.json` 因此和明文树对不上，下次 `build-pack` 会顺带重跑 `update-gamedata`。

★ 老客户端里**买过的自定义武器照旧能用**：物品 id 和武器 Id 一个字节没变。

## ✅ 自动化已核过的（会话 21，`test/custom-weapon-e2e/`）

判据 = **客户端内存里那条武器记录的每一格字节**。跑一次 `run.py` 就能全部重验。

| 覆盖到的 | 用例 | 原来挂在哪 |
|---|---|---|
| 18 把 × 14 格 × **PVE** 全部等于设定值 | T1 | —— |
| 18 把 × 14 格 × **PVP** 全部等于设定值 | T2 | V28 / V45 的数值那半边 |
| **PVE→PVP→PVE 来回切不串** | T2 / T3 | **V34** |
| **局内改参数这一局不变、下一局才变** | T4a / T4b | **V35** / V45 |
| 留空的格回退到参考值（mask=0 那条分支）| T5 | —— |
| 边界值（下界 / 上界、负重力、`homing_angle=0`）| T6 | —— |
| **原版武器一个字节没被蹭到** | T7 | —— |
| 弹匣容量进图时快照进持枪器 | T8 | V36 的数值那半边 |
| 追踪两格 `+0x78` i32 / `+0x7c` **f32** 写对了 | T1 / T2 | V52 / V53 的数值那半边 |

⚠ **它只证明「参数进到了游戏的武器记录里」**，不证明手感和观感 ——
「子弹真的会拐」「溅射真的炸到人」「准星外圈画成几格」「金色好不好看」这些**仍要人眼**。
下面那些条目里凡是看效果的，自动化都替不了。

## ⏳ 待用户验证

**X11 · 会话 22 的新武器自动补齐（★ 在**云上**验：服务端包要**重打一次**再上传，
`server/data/` 原样留着别删）**：

| # | 怎么做 | 期望看到 | 状态 |
|---|---|---|---|
| V69 | 云上换成重打过的 V0.4.1 服务端包，**照常重启**（什么都不用手动做），然后看 `logs/server.out` | 一行「**新武器: 物品库补上了 36 件（…）；其中 18 件照默认摆上了货架**」；`data/` 里多出 `items.json.bak-*` / `shop.json.bak-*`；**再重启一次这一行不该再出现** | ⏳ |
| V69b | 管理页「物品库」搜「自定义」和「爆裂3」；游戏里进商店 → 武器 | 物品库 **844** 条（18 条自定义 + 18 条 3 级枪都在）；商店里三个角色各多 **6 把** 3 级枪（18 级 / 18000 金），**自定义武器在商店里买不到**（要卖得自己去「商店货架」上架）| ⏳ |

★ 判据是日志那一行 + 文件里的条数（808 → 844 / 495 → 513），不是「看着像有了」。
★ 离线那半边已经拿**用户下载回来的云端 data 副本**跑通了（会话 22），这里验的是
真机上那一次重启。

**X10 · 会话 20 的说明文按场景切（★ 服务端和 `bshook.dll` 都换了 —— 先 `stop.bat` 再
`start.bat`，客户端也要重开；管理页强刷即可。资源卷这次没动）**：

| # | 怎么做 | 期望看到 | 状态 |
|---|---|---|---|
| V61 | 大厅进仓库，悬停任一自定义武器 | 首行「**仅显示PVE属性**，PVP的请看管理页」**不折行**，数值是 PVE 那套 | ⏳ |
| V62 | 大厅进商店：货架格 + 商店页里的仓库格各看一个 | 两处都是 PVE 那套 | ⏳ |
| V63 | 建一个**对战房**，在待机房间里打开快速换装的仓库 | 首行变成「**仅显示PVP属性**，PVE的请看管理页」，数值换成 PVP 那套；`logs/bshook_*.log` 有「WDESC 收到仓库说明文表 …按【对战】」+「仓库提示框 id=1920001 换成【对战】那一套」 | ⏳ |
| V64 | 房主在房里把模式从**对战改成任务**（不开局），再看 | 当场变回 PVE 那套，日志再来一行；只换地图 / 难度则**一行都不该多** | ⏳ |
| V65 | 退出房间回大厅，再看仓库 | 变回 PVE | ⏳ |
| V66 | 管理页改某把自定义武器的说明文 → 保存，**不重登**，看仓库提示框 | **立刻**是新文案（顺带解掉的老限制）；原版武器照旧要重登 | ⏳ |
| V66b | 管理页开「自定义属性」弹窗，看两栏的栏头 | PVE 那栏「大厅和任务房画这一栏」、PVP 那栏「对战房画这一栏」，**各一行、不折行不顶出去**；右栏那圈琥珀高亮**没有了**（两栏现在平等）| ⏳ 用户 2026-09-20 报的漏改 |
| V67 | 看**非**自定义武器（原版枪 / 卡片 / 材料）的提示框 | 一个字没变（hook 只认那 18 个物品 id）| ⏳ |
| V68 | 合成页 / 礼物盒的提示框 | 画 PVE 那套，不崩（合成那处**故意没打**补丁）| ⏳ |

★ 装了**旧 dll** 的症状：`0x0F02` 被当未知包忽略 ⇒ 处处画 PVE（含房间里），**不崩不错乱**。
★ 想做对照组：`BSHOOK_KEEP_CABINET_DESC=1` 起客户端 = 完全回到打补丁前的行为。
★ 判据在日志里：客户端 `bshook_*.log` 找 `WDESC`，服务端 `server.out` 找「← 发 0x0F02」。

**X9 · 会话 19 的金色（★ 资源卷**已经重打好了**，`stop.bat` → `start.bat` 客户端重开就能看）**：

用户选定：**枪身 `G4` 原版金铠、子弹 / 飞行 / 爆炸特效保持老的 `C`**（分两桶，D44）。

| # | 怎么做 | 期望看到 | 状态 |
|---|---|---|---|
| V57 | 商店 / 仓库里看那 9 把「自定义1」的图标 | 比以前**更浓更亮**、偏柠檬金；暗部偏红铜不是灰黑 | ⏳ |
| V58 | 装备一把进图，看手里那把枪 | 枪身有明显的明暗层次 + 暖白高光，**不再是一整片平黄** | ⏳ |
| V59 | 开枪：弹道 / 枪口火光 / 命中爆炸 | **和以前一模一样**（这几样刻意没动）—— 枪变了、特效没变 | ⏳ |
| V60 | 看 9 把「自定义2」（粉绿） | **一点没变** | ⏳ |

★ 判据很硬：特效桶维持 `C` 而 `C` 就是原来盘上那版 ⇒ 重跑后**特效文件逐字节不变**。
  实测 369 个计划文件里只有 **21** 个变（9 手持件 + 3 HUD 图集 + 9 商店图标），
  两个 ini 和 `default.amf` 没动 ⇒ **老客户端买过的照旧能用，物品 id / 名字一个字节没变**。
★ 重打了 6 个卷：`Models~Characters~ch00/01/02`、`Images~Shop`、`Images~Game`、`Data~`
  （`Data~` 顺带把「当前卡点」第 3 条欠的那次 stamp 对齐补上了）。**`bshook.dll` 没碰。**
★ 选定之后才走实装：改 `spec.BATCHES["C"].variant` → `build.py` → `build-pack.bat` → 两套运行时测试。

**X8 · 会话 18 的追踪参数（★ 服务端和 `bshook.dll` 都换了 —— 先 `stop.bat` 再 `start.bat`，
客户端也要重开；管理页强刷即可）**。**V50 / V51 agent 已在真页面上点过**（`tools/launch.ps1 -NoGame`
起的服务端，验完已停），剩下的全要进游戏：

| # | 怎么做 | 期望看到 | 状态 |
|---|---|---|---|
| V50 | 管理页开任一把自定义武器的「自定义属性」弹窗 | 分组多出一组「**追踪**」两格（追踪转向 / 追踪距离）；转向那格输入框**正下方**一行灰字「0 = 不追踪」；溅射两格 / 重力 / 最大初速也各有一句「0 = …」（最大初速那句是「0 = 子弹不动（不是「不限速」）」）| ✅ **agent 在真页面上验过**（`openWeaponModal(1920001)`）|
| V51 | 只填「追踪转向 300」、距离留空，点保存 | **保存键按不动**，距离那格标红 + 顶上「⚠ 有 1 处填得不对」，红字「追踪距离要 > 0，否则锁不上目标」**不折行、不跑出卡片** | ✅ **agent 在真页面上验过**（1280 宽，墨宽 165 ≤ 格宽 192）|
| V52 | 泰尔 1 号槽「自定义1」（`1920001`）PVP 填 转向 **1300** / 距离 **600**，保存，**重开一局**打 bot | 子弹明显朝人拐过去；打死的目标不会被绕着转（D18 的补丁）。`logs/bshook_*.log` 有「按【对战】写入 **18/18** 条」| ⏳ |
| V53 | 同样设一把 **2 号槽**（手雷，`AppleGrenade`）和一把 **3 号槽** | 三种弹体类**都会追** —— 这条是本次结论里最该实机落锤的一条（静态查的是「手雷的 Tick 头几条就转调基类」）| ⏳ |
| V54 | 把转向改回 **0** 保存，重开一局 | 完全不追踪，走回原来的弹道 | ⏳ |
| V55 | 看商店 / 仓库的物品提示框 | 开了追踪的枪多一项「追踪 N」；**只有排不下时**「飞行速度」才让位（出厂值下 18 把都排得下，两个都该在）；没开追踪的枪**一个字没变** | ⏳ |
| V56 | 卡希尔 3 号槽的「自定义1 / 自定义2」（`2920003` / `2930003`）| 弹窗里「参考」列就是 **30 / 220** —— 它俩**本来就带追踪**（母本抄来的）；不填照旧追踪，填 0 就关掉 | ⏳ |

★ 装了**旧 dll** 的症状：自定义武器全部退回资源包原值（伤害 / 弹匣都变回爆裂 3 的数），
客户端日志里一行「0x0F01 格式对不上（format=3 …），丢弃」。两边必须一起换。

**X7 · 会话 17 的管理页改动（只动前端 ⇒ 浏览器 `Ctrl+F5` 强刷就是新的，服务端不用重启）**：

**agent 已在真页面上验过的**（服务端还挂着，见「当前卡点」；改动都放弃修改复原了）：
物品库 844 = 826 + 18 · 四页 + 两个弹窗的新下拉 · 货架批量加 3 条（已上架的灰着点不动）·
配方材料整组编辑（预勾选 / 上限红条 / 换材料后数量不变）· 单值字段三处仍 `multi=false`。

| # | 怎么做 | 期望看到 | 状态 |
|---|---|---|---|
| V48 | **用你自己的游戏账号**登管理页 → 「装备卖出」页 | 筛选条上有第三个下拉「全部物品」，选「自定义物品」只剩自定义武器 | ⏳ `admin` 没有同名游戏账号 ⇒ 那一页整块是锁的（原有行为），agent 验不了 |
| V49 | 「材料掉落」页，**在你平时用的窗口宽度下**筛一下 | 筛选条**是一行**（标题那格写「筛出 42 / 52 条规则」）| ⏳ 1320 px 下实测一行；更窄会折（和加这个下拉之前一样）|

**X6 · 会话 15 的第二批 9 把自定义武器（资源卷和服务端数据都已换好，起来就能验）**：

| # | 怎么做 | 期望看到 | 状态 |
|---|---|---|---|
| V42 | 管理页物品库搜「自定义」 | **18 条**：9 条「xxx **自定义1**」（金 + 银白图标）+ 9 条「xxx **自定义2**」（樱花粉 + 抹茶绿图标），图标都是真图不是问号 | ⏳ |
| V43 | 管理页「金币 / 经验」页发一把 `1930001`，进仓库 → 装备 → 进图开枪 | 手里是**粉 + 绿**的枪；子弹 / 拖尾 / 命中特效**也是粉绿**，没有残留的紫 | ⏳ |
| V44 | 三个角色 × 3 个槽各试一把（`X930001..3`）| 9 把的模型 / HUD 图标 / 商店图标都是**各自那一把**，不是借第一批的 | ⏳ |
| V45 | 对新武器开「自定义属性」弹窗，改 PVP 身体伤害 → 保存 → **重开一局** | 下一局生效（同第一批）；`logs/bshook_*.log` 有「按【…】写入 **18/18** 条」 | ⏳ |
| V46 | 看老的 9 把 | 名字变成「xxx **自定义1**」，**配色仍是金 + 银白**，数值和说明文一个字没丢 | ⏳ |
| V47 | 游戏里进商店（不用点任何标签，进去默认就是「新商品」）| **第 1 页 8 格全是自定义武器**，第 2 页也是，第 3 页前两格收尾；点「上市顺序」仍是这 18 把在前（只是内部倒过来，自定义2 在前）；点「武器」标签则**恢复原来的排法**（会话 16）| ⏳ |

★ 判据：新武器物品 id `X93000S`（`1930001` … `3930003`）、武器 Id `1 00C 4 S 5`（`1000415` …）。
★ 没重打资源卷的症状：商店 / 仓库里那一格**是空的**（客户端在自己的 `ShopItem-Chn.ini` 里查不到图标）。
★ **出厂不上架**（同第一批）—— 要在商店买得到得自己去管理页上架。

**X5 · 会话 14 的 18 把 3 级武器（资源卷和服务端都已经换好、客户端也重开过了，直接验）**：

| # | 怎么做 | 期望看到 | 状态 |
|---|---|---|---|
| V39 | 管理页物品库搜「爆裂3」 | 多出 **6 条**（左轮手枪 / 狙击枪T1 / 复古短枪 / 华尔兹加农炮 / **重机枪** / 火箭炮），图标是真图不是问号；搜「极速3」「复合3」各 6 条 | ⏳ |
| V40 | 游戏里进商店 → 武器 | 三个角色各多 **6 把** 3 级枪，**18 级、18000 金**；买一把进仓库、装上、进图开枪 | ⏳ |
| V41 | 拿 3 级和同槽 2 级对比 | 伤害更高、图标和模型是**各自那一把**（不是借 2 级的）；`BigShot.rpt` 没有新崩溃 | ⏳ |

★ 判据：18 件的 id 是 `X12001/3/4/6/7/9 3`（`1120013` … `3120093`）。没重打资源卷的症状 = 那一格**是空的**。

**X4 · 会话 12 的准星弹格（★ `bshook.dll` **和资源卷**都换了 —— 两个必须一起换，
只换 dll 会让帧 19..31 在老图集里查不到。先 `stop.bat` 再 `start.bat`，客户端重开）**：

| # | 怎么做 | 期望看到 | 状态 |
|---|---|---|---|
| V36 | 管理页把某把自定义武器的弹匣改成 **15**，保存，**重开一局** | 准星外圈是 **15 个格子**，打一枪掉一格（以前是一个连起来的光滑圆环）| ⏳ |
| V37 | 同上改成 **20** / **4** / **12** 各试一次 | 都是「一格一发」，格子数 = 弹匣数；**改成 21 或更大**则回到光滑圆环（这是说好的）| ⏳ |
| V38 | 管理页弹窗里试着填 **101** 发弹匣、伤害 **501**、初速 **1001** | 每格「项目名 · 限 a~b · 输入框 · 单位 · 参考 N」且输入框**对齐成一条线**；超范围那格标红 + 正下方一行红字「要在 1 ~ 100 之间」（不折行）+ 顶上「⚠ 有 N 处填得不对」+ **保存键按不动**；改回去立刻恢复 | ⏳ |

★ 顺带：浮窗 / 预览两套数值都该 **PVE 在上 PVP 在下**；**原版武器**（泰尔 6 / 爱丽 14 / 瓦尔基里 18 发）
外圈和以前一模一样；`logs/bshook_*.log` 有一行「★准星弹格 @ 0048CA0D … 图集 19 → 32 帧」。

**X3 · 会话 11 的修复（★ `bshook.dll` 和服务端都换了：先 `stop.bat` 再 `start.bat`，客户端也要重开）**：

| # | 怎么做 | 期望看到 | 状态 |
|---|---|---|---|
| V33 | 管理页把某把枪的 **PVE** 身体伤害改成很好认的数，保存；**然后**开一局闯关 | 第一局就按 PVE 那套打 | ✅ **活内存核对通过**（2026-09-19，过程见 `sessions/2026-09-19-05.md`）|
| V34 | 打完闯关回房间，改开**对战**房再打一局 | **按 PVP 那套打**（以前会继续用 PVE 的数值） | ⏳ |
| V35 | **局内**改弹匣 / 伤害并保存 | 这一局**什么都不变**（准星外圈也不变）；退回房间再开一局才是新值。弹窗上有「⏱ 属性改动下一局生效」，保存回执也这么说 | ⏳ |

★★ **别只填弹匣**（PVE 没填的格用参考值，看着像「没生效」）—— 填**身体伤害**一眼能看出来。
★ 日志判据：`logs/server.out` 每次开局一行「发 0x0F01 … 模式=任务/对战；开局」且排在 `0x0400` **之前**；
`logs/bshook_*.log` 对应「收到自定义武器表 vN：**18** 条，模式 …」；管理页保存那一发该是「不施加」。

**X3 · 会话 09 欠着的**（这次一并做）：

| # | 怎么做 | 期望看到 | 状态 |
|---|---|---|---|
| V27 | 管理页「金币 / 经验」页给自己发一把 `1920001`；进仓库看、装备、进图开枪 | 仓库有金色图标、名字「左轮手枪 自定义」，能装备；手里是金枪，子弹 / 拖尾 / 命中特效银白 | ⏳ |
| V28 | 对战房里，管理页物品库 → 该武器「自定义属性」→ 右栏 PVP 身体伤害改 1 → 保存 → **重开一局**打 bot | 不用重登，**下一局**掉血明显变少；改回参考值就恢复 | ⏳ |
| V29 | 看提示框：商店（若上架）/ 仓库 | 第一行「仅显示PVP属性，PVE的请看管理页」**不折行**，数值是 PVP 那套；改说明文后**重新登录**仓库提示框才更新 | ⏳ |
| V30 | 一台仍是 V0.4.0 的客户端连本地服务端 | 被版本门禁拒（预期）；服务端日志里对它**没有**「发 0x0F01」 | ⏳ |

**已确认修好**：V31（弹匣按容量换弹、准星外圈出现）、V32（结算不卡死）—— 用户 2026-09-19 实机确认。

**X1 / X2 遗留（2026-09-18 起等）**：V15 声线 / V16 `Jab00` 特效 / V18 低等级号能选爱琳 / V20 蝴蝶不绕尸体 /
V21 蝴蝶打中 bot 减速 / V22 商店没有爱琳 / V23 bot 蹭图腾 / V24 「♥HEAL」不崩 / V25 Alt+Tab 不崩 ——
做法和期望见 `sessions/2026-09-18-03.md`。

**出问题时一次给全这 4 样**：① 卡在哪一条；② `game_patched/BigShot.rpt` 的**最后一段**；
③ `logs/bshook_*.log` 的**最后 50 行**；④ 崩溃地址（我拿 `tools/re_bs.py dis <地址>` 定位）。

---

## 给接手会话的十句话

1. ★★★ **武器 Id 不能随便编**（§41 / §47 / D31）：客户端拆数位当弹药数组下标，必须满足
   「系列 ∈ [3,5] 且 档*10+槽 < 50」。C 批 `1 0 0C 3 S 5`、P 批 `1 0 0C 4 S 5`，**只剩 4 个合法组合**，
   第三批必须改系列位。`test_weaponini` 守着，别绕过去。
2. **原版武器一个字节不许改**（用户 2026-09-19）：`test_weaponini` 钉着 `weapon.ini` 前 221519 字节的 sha256。
   想调数值去管理页物品库的「自定义属性」弹窗，只有那 18 把自定义武器能调。
3. ★★ **自定义武器的数值链**：`server/data/weapons.json` → `weaponcfg.effective()` → `0x0F01` → bshook 写内存。
   `weapon.ini` **每次进图重读**（`Load` 会把我们写的值冲掉），所以 hook 在 `Load` 返回点上还要再写一遍（§36 / D26）。
   ★ 现在是 **14 格**（末尾两格是追踪，§50 / D42）。加格 = 改五处：`FIELDS` / `LEGACY_LIMITS` /
   `FIELD_GROUPS` / `WIRE_FORMAT` / `bshook.c` 的 `WTAB_FIELD[]`+`WTAB_FORMAT`，**漏一处就静默出错**
   （漏 `LEGACY_LIMITS` 是 `KeyError` 打穿读盘、漏 `FIELD_GROUPS` 是页面上不画、漏格式号是整份丢包）。
3b. ★★ **说明文是另一条链，别和数值链搞混**（§52 / D45）：`weapons.json` 的 `desc` →
   `shopcfg.item_desc_zh(mode=…)`（**缺省 PVE = 大厅那一档**）→ 两条出口：
   `0x0500`/`0x0501`/`0x0508` 照旧发给客户端，外加 **`0x0F02`** 把整段文案推给 bshook，
   由它在绘制点 `0x4554e7` 按**物品 id**（不是武器 Id！）换掉仓库提示框那一段。
   下发挂了七处，靠 `Conn.hook_desc_key`（模式 + 载荷 crc32）去重 ⇒ **调用点宁多勿少**。
4. ★★★ **PVE / PVP 由服务端在开局握手里说，不是 hook 猜的**（§42 / D32，推翻了 D27）：
   `0x0F01` 头上那个模式字节，`0xFF` = 只换表不写内存 ⇒ **属性改动一律下一局生效**（用户拍板）。
   开局那一发必须排在 `0x0400` **之前**（弹匣容量进图时就快照走了）。`test_room` 守着这两条。
5. 自定义资源全部由 `tools/custom-weapon/build.py` 生成，**幂等**、**一次跑全部批次**（两个 ini 都是
   「砍掉标记再整块重写」，只跑一批会把另一批带走）。批次表 / 编号 / 选定的配色变体都在 `spec.BATCHES`，
   改色规则在 `palette.SCHEMES`。**不带参数跑就是重现盘上那一套**；手改生成物会被下次重跑盖掉。
6. `default.amf` 只许经 `tools/amftool.py` 改（定长记录，写错一格整张表读歪，§37）。
7. `shop_items.json` **FORMAT 2**、`bot_weapons.json` 13：改字段必须和重生成的产物同一次落地。
8. 部位码**一批一个**：`92` 第一批 / `93` 第二批（`tools/shopdata.CUSTOM_WEAPON_PART`），
   物品 id `X<部位码>000S`。⚠ 别为了省一个部位码去共用 92 换尾号 —— `weapon_variant()` 会误判（§47）。
   两批的默认中文名靠**韩文名后缀字母**分（`shopcfg.CUSTOM_WEAPON_ZH_BY_SUFFIX`：`C`→自定义1 / `P`→自定义2）。
9. **版本号只由用户定**：`build-ver.config` 里是尚未发布的开发版本（现在 0.4.1，线上 V0.4.0）。别自己抬号。
10. ★ 跑测试：`runtime\python\python.exe test\run_tests.py`（3.14）和 `runtime-win7\python\python.exe test\run_tests.py`（3.8），
    全量名单现扫 `test/test_*.py`。**别加 `-j1`、别 `| tail`**。
11. ★★ **动了武器数值链（`weaponcfg.py` 的 `FIELDS` / `bshook.c` 的 `WTAB_*`）就去跑
    `test/custom-weapon-e2e/`**：`C:\Python314\python.exe test\custom-weapon-e2e\run.py`
    （要真客户端 + 独占鼠标键盘约 10 分钟；`--selftest` 是不碰客户端的离线那半边）。
    它**不在全量单测里**（D47），漏跑不会报错 —— 这正是它唯一的风险。
    先读那个目录的 `README.md`，尤其是「本会话踩过的坑」那一节。
