# X_Mod PROGRESS —— 现在在哪

**只保留当前状态，全文 ≤ 150 行。** 做完的事从「正在做」挪走，不留历史；流水账进 `sessions/`。

最后更新：2026-09-26（会话 40）

---

## 现在在哪

**★★★ 会话 40（2026-09-26，`sessions/2026-09-26-01.md`，和会话 39 并行）：用户「屏蔽游戏内的选词框，只用原版输入法的选词」—— X14 改完，只动 hook。**
- 查因（§106）：游戏自带框只在 `IMN_CLOSECANDIDATE` 时清词表，搜狗在游戏里从不发 ⇒ 旧词残留；搜狗的选中序号越过它给的 9 条 ⇒ 翻到第三行框消失。
  搜狗 / 微软拼音本来就画着自己的候选。「聊天发不出去」是另一回事：回包晚了 7 秒 ~ 3 分钟（§107，含从 bshook 日志解收包的方法）。
- 改了（D70）：`bshook.c` 新增一节「输入法一律用它自己的候选界面」—— `0x40edcb` 的 call 改指分流 thunk；`test_patchsites` 加 7 条钉站点。
- 第一版 dll 实机：自带框没了 ✅，位置不对 —— 位置询问只在激活时来一次（alt+tab 后搜狗才对）、微软拼音只认 ImmSet*（§106）。
  第二版改成焦点 / 光标一变就主动 `ImmSetCompositionWindow` + `ImmSetCandidateWindow` → 位置对了 ✅，但微软拼音叠两层。
  第三版（现在的）：组字三条原函数照跑后吞掉、带上屏结果的只留结果位照交（D70）。
  已重编（用户把版本抬到 0.4.4，清单里 V0.4.4 条目原位刷新），开发树 `start.bat` 已起 —— **第三版等实测**（V122~V124）。

**★★★ 会话 39 第二段（2026-09-25/26，`sessions/2026-09-25-04.md`）：用户「不一样的有 bug 的地方都改掉，别再一个个问，一次改完」—— 改完，只动服务端，代码在工作区没提交。**
- **物理整个换成客户端逐帧那一套**（§102~§105 / D68）：bot 自己走、外推真人、寻路规划三处共用 `botmove.frame()`
  （走路按列爬坡 20 / 下坡 10、起跳 vy −20.78/−24 + ¼S、空中操控 + 锁、上升计时器、撞墙投票反射、弹跳台延一帧、
  掉出地图底算出界）；心跳 vx = trunc(底速 + 操控)。离线重放 19:51 >10 px 222→139、00:08 673→361。
- **战斗**（§101 / D69）：三连射 ×3 弹 + 每轮 ±π/64、`SpreadRandom` 缺省随机、射速 max(C,R) 缺省 200、捡来的枪只算冷却、
  状态键看手上那把（临时枪 / 图腾枪不挂）；缩小 ×0.6 八秒、闯关统一尺寸、出拳第 4 个圆（⌊TF×0.625⌋ 帧）、出拳 / 冲刺火墙不烧。
- **状态**（§97~§99 / D67，第一段）：蹲照别人眼里记、状态走速、冰冻房间级结算、武器状态只认直接命中、碎片吃强力倍率。
- 两套运行时全量 **4544 绿**。**没进游戏打过**；要 `stop.bat` → `start.bat`；云上要重打服务端包。待验 V116~V121。
- 没做的：fallback 过坑率（Quest02_1 约 41%，0 掉坑）、离开移动平台的启发式、出拳朝向用的是 `0x0008` 包里的方向。

**★★★ 会话 39（2026-09-25，`sessions/2026-09-25-03.md`）：用户「前一个会话说的两点（D65 ⑥）帮我修复」—— 改完（§95 / §96 / D66），只动服务端，代码在工作区没提交。**
- **真人格挡对 bot 生效**：服务端记真人的 `rpGuard`（开关 + 3 格过渡；蹲着不算；死了 / 出格斗招式 `0x0016` / 新一局清，闯关换图不清），
  bot 的直接命中 / 近身打到正在挡的人就在包里置 `0x80`（伤害数不变，收方扣 `int(0.25×伤害+1)`），台账同口径；溅射 / 火墙照原版挡不住。
- **bot 的强力 / 三连 / 毒弹按打空几匣结束**（原版 `0x509feb`）：左轮满匣起 18 发、没写弹匣的枪仍是 3 发；捡来的枪打的不算。
- 两套运行时全量 4510 绿（+19 条新测试）。云上要重打**服务端包**。待验 V113~V115。

**上一轮 · 会话 38（`sessions/2026-09-25-01.md` / `-02.md`，已提交 `53275a94` / `a96e2e0b`）**：[幸运幸存者] 查清改完、bot 打真人补受害者一侧
（§90 / §91 / D64）；血量台账逐条照收方 `OnHit` 记（截断、格挡、免伤、中毒、回复剂、心 / 红心、换图不清，§92~§94 / D65）。待验 V111 / V112。

更早的几轮（会话 16~37，一轮一行）挪到 `sessions/PROGRESS-待验归档.md` 第 1 节。

**一直没改的**：原版 `weapon.ini` 的数值、原版那 6 张准星盘。
**版本号**：工作区里 `build-ver.config` / `server-ClientFilter.config` 都已是 **0.4.2**
（用户改的），`manifest-hook.json` 里 V0.4.2 的 dll 哈希每次重编都会自动刷新。

## 下一步

1. 实机打一局验 **V116~V121**（本轮），顺带 V113~V115 / V111 / V112 / V110；服务端要先 `stop.bat` → `start.bat`。
   会话 40 的 **V122~V124**（选字）同一次开发树 `start.bat` 就能验。
2. 发版：两个包都要重打（旧的发版注意事项原文在归档第 2 节）。本轮和会话 36~39 都只动了 `server/` ⇒ 服务端包必须重打，
   云上跑的还是旧代码。★ 会话 40 换了 `bshook.dll` ⇒ **客户端包也必须重打**；`manifest-hook.json` 里 V0.4.3 的哈希是原位刷新的
   —— V0.4.3 若已发出去，按 `tools/gen_hook_manifest.py` 头注释整包重传 + 覆盖服务端（版本号用户定）。
3. 等用户看完 `logs/ch03-weapons-all27.png` 再决定做不做爱琳那 27 件（§45）。

## 当前卡点

**没有卡点。** 改了 `server/` ⇒ 先 `stop.bat` 再 `start.bat`（铁律 7）才验得了本轮。
旧的卡点原文（X8 / X10 换 dll、资源卷 stamp）挪进了归档第 2 节。

## ⏳ 待用户验证

**会话 40 · 选字交给输入法（X14 / §106 / D70）—— 换了 `bshook.dll`：开发树 `start.bat` 起就是新 dll（云服清单没有这个哈希，别连云服测）**：

| # | 怎么做 | 期望看到 | 状态 |
|---|---|---|---|
| V122 | 搜狗：房间聊天框打 `nihao` 空格、`a` 退格、`a` Esc，翻页到第三行，再点走点回聊天框 | 游戏的竖排框**一次都不出现**；搜狗的选字条贴在聊天框旁边（不再在右下角）；打字 / 发送 / bot 命令照常 | 第一版：框没了 ✅、位置要 alt+tab 才对；第二版：位置对了 ✅ → ⏳ 第三版复核 |
| V123 | 微软拼音：同上 | 选字条贴在聊天框旁边（不再在左上角）；聊天框里的拼音只剩一份、**不再叠两层**；上屏的字照常进聊天框 | 第二版：位置对、字叠两层 → ⏳ 第三版 |
| V124 | 战斗里的聊天框、房间名等别的输入框各打一次；设定里能切 800×600 的话切过去再看 | 都贴着那个输入框；800×600 下位置也对 | ⏳ |

| # | 怎么做 | 期望看到 | 状态 |
|---|---|---|---|
| V116 | 任一图加 bot 看它走、跳、二段跳、下平台（↓）、踩弹跳台、走下崖 | 你屏幕上 bot **不再瞬移 / 抖回去**，落点就是它自己跳到的地方；不卡墙、不在半空停住 | ⏳ 等你来打 |
| V117 | 让 bot 打你：站着 / 走着 / 跳起来 / 刚踩弹跳台 / 从平台边走下去 | 你屏幕上被打中 = 服务端也判中；「雷在身上消失、别处炸」比 V109 时更少 | ⏳ |
| V118 | 闯关图（Quest02_1 那种有坑的）带 bot 打一局 | bot 能过坑、不往坑里掉；过不去的会绕 / 停，不会反复跳进去 | ⏳ |
| V119 | bot 捡到三连射后开火 | 一发变三组（中间一组 + 上下各偏一点），伤害按实际打中的算 | ⏳ 捡到才看得到 |
| V120 | 你吃缩小道具 8 秒内让 bot 打；闯关房里让 bot 打 | 缩小时不再「打空气也判中」；闯关里各角色一样大 | ⏳ |
| V121 | 对着 bot 的火墙出拳 / 冲刺穿过去 | 出拳 / 冲刺那一下火不烧；拳头前面那一圈挡 bot 子弹（算身体） | ⏳ |

**会话 39 · 格挡对 bot 生效 + 按打空几匣数（§95 / §96 / D66）—— 只改了服务端，开发树 `start.bat` 起就是新代码**：

| # | 怎么做 | 期望看到 | 状态 |
|---|---|---|---|
| V113 | 对战房加 bot，站在地上**按住格挡**让 bot 的子弹直接打中你；左右两边都让它打几发 | 挡着时掉血明显少（约 1/4）；`server.out` 有「受害者在格挡（直接命中）：座位N 带 0x80」。⚠ 可能只有一边管用：收方还有一道「正面挨打」门，朝向是进图时随机的（§92，原版就这样）| ⏳ 等你来打 |
| V114 | 挡着让 bot 贴脸冲刺（近身）打；再让它的手雷炸在你旁边 / 火墙烧你 | 近身同样少掉血；溅射 / 火墙照常掉血（原版就挡不住）；蹲着挡不算 | ⏳ |
| V115 | bot 捡到强力射击 / 三连 / 毒弹后，看它用多发弹匣的枪（泰尔 1 号左轮 6 发一匣）打多久 | 加强一直持续到打空 3 匣（满匣起 18 发）；`server.out`「挂上了，打空 3 匣为止」…「打空了最后一匣，撤掉」；苹果雷那种一发一装的仍是 3 发 | ⏳ bot 捡什么是随机的，碰上再看 |

**会话 38 · bot 打真人的受害者一侧（§91 / D64）—— 只改了服务端，开发树 `start.bat` 起就是新代码**：

| # | 怎么做 | 期望看到 | 状态 |
|---|---|---|---|
| V111 | 穿 [幸运幸存者] 进有 bot 的对战房，残血（15 以下）时让 bot 直接打中 | 大约一半头上冒「LUCKY!」、不掉血；血多时挨打照常掉；`server.out` 有 `受害者一侧（直接命中）：座位N [幸运幸存者] 免伤`。被溅射 / 火烧 / 近身打中时不冒字（包里没那一格，原版如此），只是不掉血 | ⏳ 等你来打 |
| V112 | 穿带防御的铠甲（或宠物「防护装置」）让 bot 打 | 偶尔（约 15%）冒「DEFENSE!」，那一发伤害少一点 | ⏳ |

**会话 37 · 自定义燃烧弹的火墙（§89）—— 只换了资源卷 `Data~Quest.pkn`：用开发树 `start.bat` 起（或重打客户端包）**：

| # | 怎么做 | 期望看到 | 状态 |
|---|---|---|---|
| V110 | 闯关里拿卡希尔「自定义1」「自定义2」的 2 号往自己脚下扔，再往队友（真人 / bot）脚下扔 | 火照铺、照烧怪，自己和队友站在火里**不掉血**（和初始 2 号一样）；对战房里照旧谁都烧（原版如此，没动） | ⏳ 等你来打 |

**更早的待验（会话 36 及以前）整表原样挪到 [`sessions/PROGRESS-待验归档.md`](sessions/PROGRESS-待验归档.md) 第 4 节，状态照旧有效**。
还开着的：V109 · V105 / V106 · V99 · V89 / V90 · V85 / V86（等你点头重打卷）· V92~V95（等发版）· V70b / V71 ·
V73~V80（挂机）· V69 / V69b（云上）· V61~V68 / V66b（X10）· V57~V60（X9）· V52~V56（X8）· V48 / V49 · V42~V47 ·
V39~V41 · V36~V38 · V34 / V35 · V27~V30 · X1 / X2 遗留。会话 21 那张「自动化已核过的」表在第 3 节。

**出问题时一次给全这 4 样**：① 卡在哪一条；② `game_patched/BigShot.rpt` 的**最后一段**；
③ `logs/bshook_*.log` 的**最后 50 行**；④ 崩溃地址（我拿 `tools/re_bs.py dis <地址>` 定位）。

---

## 给接手会话的十句话

1. ★★★ **武器 Id 不能随便编**（§41 / §47 / D31）：客户端拆数位当弹药数组下标，必须满足
   「系列 ∈ [3,5] 且 档*10+槽 < 50」。C 批 `1 0 0C 3 S 5`、P 批 `1 0 0C 4 S 5`，**只剩 4 个合法组合**，
   第三批必须改系列位。`test_weaponini` 守着，别绕过去。
2. **原版武器一个字节不许改**（用户 2026-09-19）：`test_weaponini` 钉着 `weapon.ini` 前 221519 字节的 sha256。
   想调数值去管理页物品库的「自定义属性」弹窗，只有那 18 把自定义武器能调。
3. ★★ **自定义武器的数值链**：`server/data/weapons.json` → `weaponcfg.effective()` → `0x0F01` → bshook 写内存。
   `weapon.ini` **每次进图重读**（`Load` 会把我们写的值冲掉），所以 hook 在 `Load` 返回点上还要再写一遍（§36 / D26）。
   ★ 现在是 **14 格**（末尾两格是追踪，§50 / D42）。加格 = 改五处：`FIELDS` / `LEGACY_LIMITS` /
   `FIELD_GROUPS` / `WIRE_FORMAT` / `bshook.c` 的 `WTAB_FIELD[]`+`WTAB_FORMAT`，**漏一处就静默出错**
   （漏 `LEGACY_LIMITS` 是 `KeyError` 打穿读盘、漏 `FIELD_GROUPS` 是页面上不画、漏格式号是整份丢包）。
3b. ★★ **说明文是另一条链，别和数值链搞混**（§52 / D45）：`weapons.json` 的 `desc` →
   `shopcfg.item_desc_zh(mode=…)`（**缺省 PVE = 大厅那一档**）→ 两条出口：
   `0x0500`/`0x0501`/`0x0508` 照旧发给客户端，外加 **`0x0F02`** 把整段文案推给 bshook，
   由它在绘制点 `0x4554e7` 按**物品 id**（不是武器 Id！）换掉仓库提示框那一段。
   下发挂了七处，靠 `Conn.hook_desc_key`（模式 + 载荷 crc32）去重 ⇒ **调用点宁多勿少**。
4. ★★★ **PVE / PVP 由服务端在开局握手里说，不是 hook 猜的**（§42 / D32，推翻了 D27）：
   `0x0F01` 头上那个模式字节，`0xFF` = 只换表不写内存 ⇒ **属性改动一律下一局生效**（用户拍板）。
   开局那一发必须排在 `0x0400` **之前**（弹匣容量进图时就快照走了）。`test_room` 守着这两条。
5. 自定义资源全部由 `tools/custom-weapon/build.py` 生成，**幂等**、**一次跑全部批次**（两个 ini 都是
   「砍掉标记再整块重写」，只跑一批会把另一批带走）。批次表 / 编号 / 选定的配色变体都在 `spec.BATCHES`，
   改色规则在 `palette.SCHEMES`。**不带参数跑就是重现盘上那一套**；手改生成物会被下次重跑盖掉。
6. `default.amf` 只许经 `tools/amftool.py` 改（定长记录，写错一格整张表读歪，§37）。
7. `shop_items.json` **FORMAT 2**、`bot_weapons.json` 14（会话 36 加 `pass_coll_flags`）：改字段必须和重生成的产物同一次落地。
8. 部位码**一批一个**：`92` 第一批 / `93` 第二批（`tools/shopdata.CUSTOM_WEAPON_PART`），
   物品 id `X<部位码>000S`。⚠ 别为了省一个部位码去共用 92 换尾号 —— `weapon_variant()` 会误判（§47）。
   两批的默认中文名靠**韩文名后缀字母**分（`shopcfg.CUSTOM_WEAPON_ZH_BY_SUFFIX`：`C`→自定义1 / `P`→自定义2）。
9. **版本号只由用户定**：`build-ver.config` 里是尚未发布的开发版本（现在 0.4.1，线上 V0.4.0）。别自己抬号。
10. ★ 跑测试：`runtime\python\python.exe test\run_tests.py`（3.14）和 `runtime-win7\python\python.exe test\run_tests.py`（3.8），
    全量名单现扫 `test/test_*.py`。**别加 `-j1`、别 `| tail`**。
11. ★★ **动了武器数值链（`weaponcfg.py` 的 `FIELDS` / `bshook.c` 的 `WTAB_*`）就去跑
    `test/custom-weapon-e2e/`**：`C:\Python314\python.exe test\custom-weapon-e2e\run.py`
    （要真客户端 + 独占鼠标键盘约 10 分钟；`--selftest` 是不碰客户端的离线那半边）。
    它**不在全量单测里**（D47），漏跑不会报错 —— 这正是它唯一的风险。
    先读那个目录的 `README.md`，尤其是「本会话踩过的坑」那一节。
12. ★★ **角色运动只有一套**：`botmove.frame()` 就是本人那台客户端的一帧（D68）—— bot 自己走、外推真人、可达图三处共用，
    改物理只改这一处，改完两份心跳重放（§105 的数）不许变差；`walk_speed` / `surface_near` / `CLIMB_SLOPE` 只是规划用的估计。
