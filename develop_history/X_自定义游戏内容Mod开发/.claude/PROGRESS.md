# X_Mod PROGRESS —— 现在在哪

**只保留当前状态，全文 ≤ 150 行。** 做完的事从「正在做」挪走，不留历史；流水账进 `sessions/`。

最后更新：2026-09-18（会话 08）

---

## 现在在哪

**X1「第 4 个基础角色爱琳」已经打包上云、有真人在玩。本轮（会话 08）处理的是
**V0.4.0 上线当晚收到的 5 份客户端崩溃包**（`bug调查/24`）—— 两个根因全部查明并修掉，
两套运行时 3907 项全绿，等实机验收。**

结论已落定，**不用重新调研**：图里那个角色 = `아이린` / 爱琳 / `ChrIndex=3`，
311 客户端里她**只差模型**（§1）；挡着她的闸表见 §2（★ **2026-09-18 起是五道 + 三道 4 级门**，
商店那道故意撤了）。

| 里程碑 | 状态 |
|---|---|
| M0 ~ M6 本套进度文件 / 克隆 / 图集 / 字节补丁 / 服务端常量 / 首次实机 | ✅ |
| M7 外观改造：头发 / 身体 / 裙摆 / 三把武器 / 持枪动作 | ✅ **用户已验收** |
| M8 去卡希尔化：删锤子 / 冲刺攻击 / 跑步摆臂 / 格斗特效 / 声线 / 3 号武器音 | ✅ **用户已验收**（V10~V14）|
| M9 去卡希尔化**收口**：全量对账 + `Jab00` 特效 + 特效侧守卫 | ⏳ 等实机（V16）|
| M10 蝴蝶绕尸体转：追踪侧补死活判据（§29 / D18）| ⏳ 等实机（V20）|
| M11 测试搬家 + 三个从没跑过的模块归队 + 审计日志竞态（§30 / D19 / D20）| ✅ 两套全绿 |
| M12 线上三条反馈：蝴蝶减速 / 商店画错人 / bot 蹭回血图腾 | ⏳ 会话 07 做完，等实机（V21~V23）|
| M13 **V0.4.0 线上 5 份崩溃包**：结算界面帧越界 + D3D `Lock` 不检查 | ⏳ **本轮做完，等实机（V24 / V25）** |

**本轮（会话 08）做了什么** —— 细节在 `sessions/2026-09-18-03.md`

5 份崩溃分成两类，根因都查明了：

| 类 | 份数 | 根因 | 修法 |
|---|---|---|---|
| A | 4 | 爱琳的回血图腾治到队友 → 结算界面取 `frames[22]` 画「♥HEAL」→ 中文版图集只有 22 帧（**§34**）| 补资源到 24 帧（**D24**）|
| B | 1 | `IDirect3DIndexBuffer9::Lock()` 失败不检查，往 `NULL` 里 `memcpy`（**§35**）| 跳板补丁 `try_patch_d3d_ib_lock`（**D25**）|

A 类为什么现在才崩：全游戏只有爱琳 3 号是图腾（§32），中文版从没开过她 ⇒
那条取帧的路 15 年没被走到过。同房间两人**同一秒双崩**正是因为两台各自都要画这个标签。

顺手修的两条：`tools/update-gamedata.ps1` 还在找 `server\test_*.py`（D20 搬家漏的，
**`build-pack.bat` 的「明文树变了」那条路一直是断的**）；`test/run_tests.py` 打失败报告时
遇到 GBK 装不下的字（`♥`）会把运行器自己搞崩。

产物 / 构建：`bshook.dll` 重编、`manifest-hook.json` 的 V0.4.1 刷到 `9b26e1f5bfa60786…`；
`Images~Chinese.pkn` 重打（回读 16948 个文件差异 0）。

---

## 下一步

0. ★★ **客户端包要重新打并让玩家更新** —— 资源和 `bshook.dll` 都改了，
   不重打的话线上那两个崩法一个都没消失。打包是发布动作，等用户拍版本号。
1. **等用户走一遍 V15 / V16 / V18 / V20 / V21 / V22 / V23 / V24 / V25。**
2. 本轮认下、没做的（想做要先问）：
   - 减速认武器只能问 `conn.peer_weapon` ⇒ **母弹直接命中也会减速**（原版只有碎片会）、
     **蝴蝶在空中时换枪会漏**。想收紧得先抓一局真包，看 `rpExplode +0` 的弹体句柄够不够。
   - 商店里那套 detour 方案（真把爱琳画出来）**已被否**，理由在 D22，别再推翻。
3. 更早就记着、仍然没做的：
   - 🔴 **1 / 2 号武器的发射音维持泰尔（ch00）的，别再动**（D17）。
   - 还剩 5 条卡希尔的音真会响（`MutuStand-P01-2/3/4`、`MutuStand-P03`、`MutuJump-P01`）
     —— 全是**格斗打击叠加层，不是嗓音**，全游戏只有 ch01 有这些文件。
   - 剩 6 条 CH01 特效 + 7 条 ch01 音**挂在她放不出来的动作上**（§25）⇒ **不用管**。
   - `Jump` / `Crouch` / `Damage` 不分武器，仍是卡希尔那套（D9 认下的代价）。

## 当前卡点

**没有卡点。** 两个崩因都改完并跑绿了，只差实机 + 重打包。

★ 本轮（会话 08）改的文件：`game_patched/Pack_develop/Images/Chinese/ClearResultSlotLadder.{smf,png,xml}`
（+ 重打的 `Pack_publish/Images~Chinese.pkn`、`pack-index.json`、`tools/gamedata-stamp.json`）、
`hook/bshook.c`（+ `hook/bin/bshook.dll`、`server/manifest-hook.json`）、
`tools/update-gamedata.ps1`、`test/run_tests.py`、新增 `test/{test_smfframes,test_patchsites}.py`、
本套进度文件。**按铁律 14 停在工作区，没有提交。**

★ 会话 07 那一批（`server/*.py` / `bot_weapons.json` / `re/packet_api.md` / 上一版 `bshook.dll`）
**用户已经自己提交了**，就是 `b568077e` ⇒ 工作区里现在只有本轮这一批。

## ⏳ 待用户验证

**已实机确认正常的**：第 4 个角色出现且名字是爱琳、房间面板有她、能进图能打、
三把武器的弹道 / 爆炸 / 回血特效、开枪有声（2026-09-15）；三把武器的模型和持枪动作、
冲刺攻击的动作和特效（V11）；锤子没了、跑步摆臂、格斗 / 表情特效（V10 / V12 / V13）；
3 号武器发射音（V14 的发射那半，2026-09-16）；**V19 已交付日志并定案**（§29）。

**待验**（★ V21~V23 **必须先重启客户端**：`bshook.dll` 换了）：

| # | 怎么做 | 期望看到 | 状态 |
|---|---|---|---|
| V15 | 挨几下打、格斗打一套 P/K 连招、跑一跑、蹲着走 | 受击闷哼 / 吆喝 / 脚步 / 蹲行 / 拳脚命中全换成 **ch100 艾丽亚丝**的声线 | ⏳ |
| V16 | **空手（格斗模式）近身打一拳**（`Jab00`）| 命中特效在**她拳头上**炸开，不再是从身侧一米外的空气里冒出来 | ⏳ |
| V18 | 拿一个**等级 < 4** 的号（新注册的就是 1 级）建房 | 房间 6 格面板里爱琳照样出现、点得动、能进图 —— 不再弹「4 级才能用」。★ **商店里她不出现是对的**（D22）| ⏳ |
| V20 | 拿 2 号武器把怪打死，盯着分裂出来的蝴蝶 | 怪一死，还在空中的蝴蝶**立刻不再绕圈**，改成按重力斜着飞出去、碰到地形炸掉 | ⏳ |
| V21 | 用爱琳 2 号武器的蝴蝶打中一个 bot | bot 当场**走得明显变慢**约 2 秒，然后自己恢复 —— 不再是照常速跑 | ⏳ |
| V22 | 进商店，看顶部那排角色按钮 | **只有 3 个原始角色，没有爱琳** ⇒ 左边再也不会画成艾丽亚丝。★ 房间里选爱琳照旧 | ⏳ |
| V23 | 组队 / 闯关房，把 bot 队友打到 90% 以下，在它视野内放回血图腾 | bot **主动朝图腾挪**，进圈后照旧对敌人开枪、照旧躲子弹，血条往上走；图腾 5 秒没了之后回去正常打 | ⏳ |
| V24 | ★ **两个真人**一房（一个用爱琳），爱琳拿 3 号武器把图腾扔到**队友**身边给他回血，把这一局打完看结算界面 | 结算那一行末尾多出一个闪烁的「♥HEAL」小标签，**两台都不崩**（原来是全房间同一秒 `C0000005 @0048E786`，§34）| ⏳ |
| V25 | 全屏进游戏，反复 Alt+Tab / Win 键切出去再切回来（或者按一次 Ctrl+Alt+Del 再回来），多切几次 | 切回来照常能玩，**不再崩在 `005F5714`**。★ 先看 `logs/bshook_*.log` 里有没有「★索引缓冲空指针判据 @ 005BF6D0」那一行，没有就说明补丁没打上 | ⏳ |

★ V25 怎么读结果：跳板的**成功那半（指针有效 → 回填充循环）每次启动都会跑**
（启动建渲染器就是一次「设备重建」）⇒ **游戏正常起来、画面正常，就已经证明跳板本身没接错**；
要专门去试的只是「指针是 NULL」那一支，它只有真丢设备才走得到。

★ V23 的预期管理：`TotemLifeTime=5000` + `TotemRange=200` 是**原版数据** ——
图腾只活 5 秒，bot 只有在它落在自己附近（约 1 秒跑得到）时才吃得到几跳。这不是 AI 的问题。

★ V21 / V23 的离线证据都在 `logs/server.out` 里：搜「被带属性的武器打中」
「号位放下回血图腾」「站在回血图腾里」三条。

**出问题时一次给全这 4 样**：① 卡在哪一条；② `game_patched/BigShot.rpt` 的**最后一段**；
③ `logs/bshook_*.log` 的**最后 50 行**；④ 崩溃地址（我拿 `tools/re_bs.py dis <地址>` 定位）。

---

## 给接手会话的九句话

1. **图里那个角色叫爱琳，`ChrIndex=3`，原版埋好没开，只差 `Models/Characters/ch03/`。**
   别再重新调研，结论和证据全在 `FINDINGS.md` §1。
2. ★★ **中文版图集少帧 = 必崩，而且不会提前报警**：`BigChrIcons_CN.smf`（→28 帧，§3，
   不补就 `C0000005 @0x430857`）、`ClearResultSlotLadder.smf`（→24 帧，§34，线上崩过）。
   exe 有两处**绕过边界检查**直接按常量下标取帧。守卫在 `test/test_smfframes.py`，
   改中文图集前先看 D24。
3. ★ **`0x44c8xx` / `0x44c9xx` 那一片不是「大厅」，是商店**（`UiShopChrSelPanel`，§2）。
   而且商店那道闸 **2026-09-18 起故意不打**（§33 / D22）—— 别当成漏了给补回去。
4. **`bot.BOT_CHARACTER_PANEL_IDS` / `BOT_DEFAULT_CHARACTERS` 已经钉成字面量 `(0,1,2)`**，
   故意不跟 `BASE_CHARACTER_IDS`。别「顺手」改回派生写法（§8 / `test_bot` 有守卫）。
5. **`character_id_of_item(4400001)` 必须继续返回 `None`** —— 爱琳不是角色卡
   （`test_gameserver` 有守卫）。
6. **爱琳的 86 个 `.mtn` 不再是 ch01 的逐字节克隆**（anim.py 改写过）。
   改动作走 `anim.py`，别手拷别的角色的 `.mtn`（骨架树不同，查不到骨就崩，§20）。
7. ★ **「克隆自卡希尔」≠「卡希尔有的她都该有」**：`J0000` 锤子（§21）、`W0000` 泰迪熊（§22）、
   围着熊编的 `Dash00` / `Run-F00`（§23）都是卡希尔的**个人特例**。
   ★★ 但反过来也别乱补：**先查这东西她放不放得出来**（§25），再查别的 15 个角色有没有。
8. ★ **`tools/weapondata.py` 的 `FORMAT` 和 `server/weapondata.py` 的必须一起改，
   而且要和重新生成的 `server/bot_weapons.json` 同一次落地** —— 对不上时 `_read()` 返回
   **空表**，症状是**全房间 bot 当场不开枪，一句报错都没有**。现在是 **13**。
9. ★ **跑测试**：`runtime\python\python.exe test\run_tests.py`（不是 `server\run_tests.py`），
   测试代码和夹具都在根目录 `test/` 下，全量名单现扫 `test/test_*.py`（D19 / D20）。

★★ **重建链的顺序**（全部幂等，母本都是只读的 ch01 / ch102）：
`mkchar.py → recolor.py → ears.py → geometry.py → anim.py → weapons.py`。
`mkchar.py` 会**拒绝覆盖**被改过的文件（`--force` 才覆盖）。**weapons.py 必须在 anim.py 之后跑**。
改武器位置 / 枪口点只改 `rig.py`，然后 anim.py + weapons.py 各跑一遍。
⚠ `mkchar.py` 不加 `--no-prune` 会顺手删掉 `ch03D0003A.dds`（weapons.py 产出的）——
会打印「删除 1」，重跑 weapons.py 就回来了。
