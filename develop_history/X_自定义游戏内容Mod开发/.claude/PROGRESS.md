# X_Mod PROGRESS —— 现在在哪

**只保留当前状态，全文 ≤ 150 行。** 做完的事从「正在做」挪走，不留历史；流水账进 `sessions/`。

最后更新：2026-09-17（会话 05）

---

## 现在在哪

**X1「第 4 个基础角色爱琳」—— 能选到、能进图、能打，外观 / 动作 / 特效 / 声线用户都验收了。
本轮把「去卡希尔化」这件事**收口**：不再等用户一条条指摘，全量对账查了一遍还剩什么。**

结论已落定，**不用重新调研**：图里那个角色 = `아이린` / 爱琳 / `ChrIndex=3`，
311 客户端里她**只差模型**（§1）；挡着她的是**六道 exe 闸 + 一道数据闸**（§2）；
中文版大头像图集只声明 24 帧那道坑已补（§3）。

| 里程碑 | 状态 |
|---|---|
| M0 ~ M6 本套进度文件 / 克隆 / 图集 / 九处字节补丁 / 服务端常量 / 首次实机 | ✅ |
| M7 外观改造：头发 / 身体 / 裙摆 / 三把武器 / 持枪动作 | ✅ **用户已验收** |
| M8 去卡希尔化：删锤子 / 冲刺攻击 / 跑步摆臂 / 格斗特效 / 声线 / 3 号武器音 | ✅ **用户已验收**（V10~V14）|
| M9 去卡希尔化**收口**：全量对账 + `Jab00` 特效 + 特效侧守卫 | ⏳ 上轮做完，等实机（V16）|
| M10 蝴蝶绕尸体转：追踪侧补死活判据（§29 / D18）| ⏳ **本轮做完，等实机（V20）** |

**本轮（会话 05）做了什么**

**「蝴蝶炸完不消失」定案并修好**（**§29** / **D18**）。用户 2026-09-17 21:34 那一局的
`bshook` 日志 + 服务端日志对上了：弹体 `3A439A00` 咬住怪物句柄 `1100029`，服务端
21:34:42.136 收到它 HP 归零，此后该弹体连续 17 帧绕 (2052.5, 578) 画半径 8.4 的圆。
根因是 `Projectile::Homing` **锁定之后只查「句柄还解不解析得出对象」和「还在不在
HomingRange 里」，一个死活判据都没有**，而命中那一侧（0x47e037 里的 `0x417c72`）**判**
⇒ 永远追得上、永远打不着。`HomingAngle=300` 是共犯：它把稳态盘旋半径压到 `1.37·v ≈ 8~11`，
小到不蹭地形、又远小于 `HomingRange=200` 不会脱靶 —— 全游戏只有蝴蝶凑齐这两条。
补丁挂在 0x47e40a，目标 `+0x54` 进 3/4 就把 `eax` 清零，**复用原版那条 `[+0x328] = -2`**。

顺手修掉**弹体诊断自己的失真**：弹体对象池化复用，`proj_tick_log` 撞上第一个同地址的
旧格就按它的句柄判并 `return` ⇒ 那一局 42 颗蝴蝶有 21 颗**一行轨迹都没有**，和「被销毁了」
长得一模一样。`proj_track_add` 改成先作废同地址旧格，tick 侧 `return` 改 `continue`。

验收：`hookuild.bat` 重编过（manifest 刷到 `87d0ba23a6e216d8…`）；**反汇编核过编出来的
detour 逐字节就是写的那几条**；实机起客户端，钩子日志里「★追踪弹撒手死目标 @ 0047E40A」
在位；**两套运行时 3744 项全绿**。细节在 `sessions/2026-09-17-05.md`。

上两轮（2026-09-17 会话 04）：去卡希尔化收口、全量对账、`Jab00` 特效、4 级门固定去掉。
细节在 `sessions/2026-09-17-01.md`。
上一轮（2026-09-16，会话 03）：删锤子 / 冲刺攻击改瓦尔基里 / 格斗表情特效 / 空手跑摆臂 /
3 号武器发射音与换弹音 / 声线整体换给 ch100 艾丽亚丝。细节在 `sessions/2026-09-16-03.md`。

---

## 下一步

1. **等用户走一遍下面的 V15 / V16 / V18 / V20。**
2. 还没做、想到了记在这儿的：
   - 🔴 **1 / 2 号武器的发射音维持泰尔（ch00）的，别再动**（D17）：1 号换成 발키리的机枪
     试过，用户听完否掉、当天改回。「泰尔是基础角色」「左轮有效 0.55 s 对 90 ms 射速」
     是**已知代价，不是待办**。
   - **还剩 5 条卡希尔的音真会响**（`MutuStand-P01-2/3/4`、`MutuStand-P03`、`MutuJump-P01`）——
     全是**格斗打击叠加层，不是嗓音**，全游戏只有 ch01 有这些文件。要换只能拿别人的打击音顶。
   - 剩下 6 条 CH01 特效 + 7 条 ch01 音**挂在她放不出来的动作上**（§25）⇒ **不用管**，
     留着零代价，`UNMATCHED` / `UNMATCHED_EFX` 钉着。
   - `Jump` / `Crouch` / `Damage` 不分武器，仍是卡希尔那套（D9 认下的代价）。
   - §27 的「中文版 3 份 `.smf` 少帧」和爱琳无关，没查越界会不会真发生。

---

## 当前卡点

**没有卡点。** 上一轮的「蝴蝶炸完不消失」**已定案并修好**（**§29** / **D18**）——
根因是 `Projectile::Homing` **锁定之后不再判目标死活**，而命中那一侧判，于是弹体
永远追得上、永远打不着；`HomingAngle=300` 把盘旋圈压到半径 8~11，小到不蹭地形，
所以全游戏只有蝴蝶看得出来。补丁在 0x47e40a，目标进「死」状态就走原版的 `-2` 放弃路径。

⚠ 上一轮遗留：`tools/build-ver.config` 从 `0.3.3` 变成了 `0.3.5`，来源不明，没动它，
等用户确认是不是自己改的。

★ 本轮改的是 **`hook/bshook.c`**（已 `hookuild.bat` 重编，`server/manifest-hook.json`
刷到 `87d0ba23a6e216d8…`）。**服务端和客户端都已由 agent 停掉重起过**，
现在跑的就是新 DLL（钩子日志 `logs/bshook_20260917_220346_pid33964.log` 第 189 行
有「★追踪弹撒手死目标 @ 0047E40A」）。

## ⏳ 待用户验证

**已实机确认正常的**：第 4 个角色出现且名字是爱琳、房间面板有她、能进图能打、
三把武器的弹道 / 爆炸 / 回血特效、开枪有声（2026-09-15）；三把武器的模型和持枪动作、
冲刺攻击的动作和特效（V11）；锤子没了、跑步摆臂、格斗 / 表情特效（V10 / V12 / V13）；
3 号武器发射音（V14 的发射那半，2026-09-16）；
**V19 已交付日志并定案** —— 蝴蝶确实是「弹体活着卡住」，绕着尸体转（§29）。

**待验**（**要先重启客户端**）：

| # | 怎么做 | 期望看到 | 状态 |
|---|---|---|---|
| V15 | 挨几下打、格斗打一套 P/K 连招、跑一跑、蹲着走 | 受击闷哼 / 吆喝 / 脚步 / 蹲行 / 拳脚命中全换成 **ch100 艾丽亚丝**的声线（比卡希尔低一档）| ⏳ |
| V16 | **空手（格斗模式）近身打一拳**（`Jab00`）| 命中特效在**她拳头上**炸开，不再是从身侧一米外的空气里冒出来；花瓣色，和冲刺那记大招不是同一个 | ⏳ |
| V18 | 拿一个**等级 < 4** 的号（新注册的就是 1 级）进大厅 / 建房 | 爱琳照样出现、点得动、能进图 —— 不再弹「4 级才能用」| ⏳ |
| V20 | 拿 2 号武器把怪打死，盯着分裂出来的蝴蝶 | 怪一死，还在空中的蝴蝶**立刻不再绕圈**，改成按重力斜着飞出去、碰到地形炸掉 —— 不再有「在原处小幅度来回飞十几秒」 | ⏳ |

V17（1 号武器换 발키리机枪）**已验并否掉** —— 用户听完说「新的不好听」，当天改回（D17）。

★ V18 **已有离线证据**：`logs/bshook_*.log` 里九处补丁全打上（含三条「★爱琳 4 级门解除」，
**没设任何环境变量**），三条特征串在镜像上核过各自唯一。差的只是「拿低级号真的点一下」。

★ V16 不满意有两条退路，都是改 `anim.EVN_RES_REMAP` 一行再跑一遍：
换 `CH03/WP00/Efx/CH03_DashAttack00.efx`（更华丽，但和冲刺撞车）／删掉这条回到卡希尔那份。

**出问题时一次给全这 4 样**：① 卡在哪一条；② `game_patched/BigShot.rpt` 的**最后一段**；
③ `logs/bshook_*.log` 的**最后 50 行**；④ 崩溃地址（我拿 `tools/re_bs.py dis <地址>` 定位）。

---

## 给接手会话的八句话

1. **图里那个角色叫爱琳，`ChrIndex=3`，原版埋好没开，只差 `Models/Characters/ch03/`。**
   别再重新调研，结论和证据全在 `FINDINGS.md` §1。
2. **M2 必须排在 M4 之前。** 不补 `BigChrIcons_CN.smf`（24→28 帧）就打开闸，
   **一进大厅就崩** `C0000005 @0x430857`（§3）。守卫在 `mkchar.py --audit` 第 ⑦ 条。
3. **六道闸，不是五道。** 最容易漏的是 `0x44caaf`（大厅面板）——
   大厅 / 房间 6 格 / 战斗换人条是**三条独立代码路径**（§2）。
4. **`bot.BOT_CHARACTER_PANEL_IDS` / `BOT_DEFAULT_CHARACTERS` 已经钉成字面量 `(0,1,2)`**，
   故意不跟 `BASE_CHARACTER_IDS`。别「顺手」改回派生写法（§8 / `test_bot` 有守卫）。
5. **`character_id_of_item(4400001)` 必须继续返回 `None`** —— 爱琳不是角色卡，
   给她补一张卡会把 `0x55853c` 那处补丁架空（`test_gameserver` 有守卫）。
6. **爱琳的 86 个 `.mtn` 不再是 ch01 的逐字节克隆**（anim.py 改写过）。
   改动作走 `anim.py`，别手拷别的角色的 `.mtn`（骨架树不同，查不到骨就崩，§20）。
7. ★ **「克隆自卡希尔」≠「卡希尔有的她都该有」**：`J0000` 锤子（§21）、`W0000` 泰迪熊（§22）、
   围着熊编的 `Dash00` / `Run-F00`（§23）都是卡希尔的**个人特例**。
   ★★ 但反过来也别乱补：**先查这东西她放不放得出来**（§25 那张「13 条根本不触发」的账），
   再查**别的 15 个角色有没有**，最后才动手。
8. ★ **「哪个动作在什么时候放」看 `WAnimIdx`**：`Stand/Attack/Reload/Run-F/Run-B/Dash` 后面那两位
   就是当前武器的 `WAnimIdx`（§19）。**冲刺攻击是个例外** —— `[ch03-dash]` 自己写死
   `WAnimIdx=0`，所以不管手里拿哪把枪，冲刺永远放 `Dash00`（§25）。

★★ **重建链的顺序**（全部幂等，母本都是只读的 ch01 / ch102）：
`mkchar.py → recolor.py → ears.py → geometry.py → anim.py → weapons.py`。
`mkchar.py` 会**拒绝覆盖**被改过的文件（`--force` 才覆盖）。**weapons.py 必须在 anim.py 之后跑**。
改武器位置 / 枪口点只改 `rig.py`，然后 anim.py + weapons.py 各跑一遍。
⚠ `mkchar.py` 不加 `--no-prune` 会顺手删掉 `ch03D0003A.dds`（weapons.py 产出的）——
会打印「删除 1」，重跑 weapons.py 就回来了。
