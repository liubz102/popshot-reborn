# X_Mod DECISIONS —— 为什么这么选

**编号从 D1 起**，和 V0.1 / V0.2 / V0.3bot / V0.3商店 相互独立。
引用旧版一律写全前缀：「V0.2 D103」「V0.3商店 D51」。

**只记「为什么」，不记「是什么」**（「是什么」在 FINDINGS 和代码注释里）。
一条选择一节，**≤ 20 行**，写清**被否掉的方案**和**否掉的理由** —— 那才是防止后人白推翻的东西。

---

## D1 · 爱琳的模型 = **整套克隆 ch01 卡希尔 + 改皮**（用户 2026-09-15 拍板）

**选**：把 `Models/Characters/ch01` 的 671 个文件整套复制成 `ch03`，
`.msh` 里的贴图名同步改名（`ch01`→`ch03` 等长，原地替换），再照原画改贴图/网格。

**否掉的方案**：
① **照原画从零建模 + 绑骨** —— 本仓库没有 `.msh` 写出器，也没有任何 3D 建模软件，
工作量以周计，而且绑骨一错就是「骨骼查不到返 NULL 喂进矩阵乘直接崩」（§7）。
② **克隆 ch106 시리아**（白发、只有 175 文件 / 5.7 MB，体积小四倍）——
她是深肤 + 盔甲风，和花精灵的气质差太远，重绘量反而更大。
③ **只克隆最小零件集**（默认套 + 武器 + 动作，约 175 文件）——
省 12 MB，但会放大「身上穿着别人的衣服切过来，去找 ch01 根本没有的设计号」的风险。

**理由**：克隆同一个角色的 `.msh` + `.mtn` 保证**骨架同源**，这是不崩的前提；
ch01 是纤细女性体型，和原画最接近；爱琳 ChrProps 点名的三根骨在 ch01 骨架里全有（§7）。

**代价（要认）**：仓库 **+32 MB**（明文 20 + 卷 12）。爱琳第一版长得像卡希尔。

## D2 · 爱琳走「第 4 个基础角色，白送」，**但保留原版的 4 级门**（用户 2026-09-15 拍板 + 建议）

**选**：patch `0x55853c` 的 `cmp eax,3` → `4`，让持有判定对 id 3 恒真；
服务端 `BASE_CHARACTER_IDS` 加 3。**不发任何物品。**

**否掉的方案**：**商城角色卡**（上架一张 `4400001`，金币买）——
要改 `account_store.character_id_of_item()` 的白名单、改 `shopcfg` / `shopdefaults` 的默认货架，
`test_shopcfg` 那条「货架恰好 11 张角色卡」会红，牵动面比白送大得多。

**理由**：原版中国区的文案写的就是「**您可以从4级开始使用爱琳**」——
她本来就是**免费基础角色**，不是商城角色。照原版做（铁律 12）。

**★★ 4 级门：2026-09-17 改口径 —— 固定去掉，没有开关**（用户原话：
「去掉就是固定去掉，不要搞选项，没让你搞选项」）。
三处判据（`0x44c954` / `0x44cc07` / `0x467eb1`）现在和那六处**一起无条件打**，
爱琳 **1 级起就能选**；环境变量 `BSHOOK_IRENE_LEVEL_FREE` 已**删除**，别再加回来。

- **原先**（2026-09-15）是「白送 ≠ 取消等级解锁，照原版保留 4 级门，要试就设环境变量」。
  否掉它的不是技术理由，是**用户要的产品形态**：这是 Mod 分支，爱琳是送的，
  不该让新号先练到 4 级才能用。
- ⚠ **别把它和 `BSHOOK_KEEP_IRENE_LOCK` 搞混**：后者是「整个角色回到原版」的总开关
  （九处一起不打，爱琳在三个面板里都不出现），不是 4 级门的选项，保留。
- ⚠ 改了 `bshook.c` 就**必须 `hook\build.bat` 重编**，它会顺带刷
  `server/manifest-hook.json` 的哈希 —— 不刷的话服务端会判客户端「被篡改」强制更新。

★ 推论：`character_id_of_item(4400001)` **必须继续返回 `None`** ——
以后谁好心加一张爱琳的角色卡，会把 `0x55853c` 那处补丁架空。

## D3 · 建模改造和贴图改造**外包给 Fable 5.1 子代理**（用户 2026-09-15 拍板）

**选**：M1 克隆 + M3 证伪实验通过之后，起一个 `model: "fable"` 的子代理做外观，
和主会话的 hook / 服务端工作**并行**。给它白名单（只许碰 `ch03/**` 和新建的几个 tools 脚本），
禁止碰 ch01、`server/`、`hook/`，禁止跑 build-pack 和任何 git 命令。

**否掉的方案**：① 主会话自己顺手改 —— 串行，而且美术活和协议活抢同一份上下文；
② 开 `isolation: "worktree"` 隔离 —— `game_patched` 有 540 MB 在 git 里，开 worktree 代价太大。
`ch03/` 整个目录是本次新增的未跟踪文件，靠白名单就足以避免撞车。

**理由**：外观改造是一块**边界清晰、可离线验收**的活（改完渲对照图就能看），
天然适合外包；而它又需要写 `.msh` / `.dds` 的解析器，是硬活。

**准入门槛（硬性）**：改网格之前，`tools/mshtool.py` 的 parse→write 必须在**全部 256 个
`.msh` 上 round-trip 逐字节一致**。做不到就停在「只改贴图」，**不许硬改**。

## D4 · 补大头像图集用「**整份覆盖韩版 smf**」，不给 exe 再打一处借位补丁

**选**：`Images/Chinese/BigChrIcons_CN.smf` ← `Images/NewUI2/BigChrIcons.smf`（24→28 帧），
并**退役** `hook/bshook.c` 的 `REGION_SITES[2]`（角色 110 借用 106 图标那条）。

**否掉的方案**：照 `REGION_SITES[2]` 的老办法，再加一处 patch 把爱琳的 `0x1a/0x1b`
改成借用别人的图标。

**理由**：帧其实**早就画在 PNG 里了**（§3），只是中文版的 `.smf` 把帧数截断
—— 这是个**数据 bug，不是设计**。补数据才是真修好；借位是把两个角色都顶着别人的脸。
而且前 24 帧逐字节相同，覆盖等于只追加 4 帧，风险极低。

**额外收益**：顺手修掉「角色 110 进关卡必崩」那个老 bug，110 拿回自己的头像。
**这一步可以完全不碰爱琳就独立验收**（让有 110 的号进一次关卡看头像）。

## D5 · 这一版**不做**衣柜 / 武器音效 / 打包

**选**：只做到「实机能选到并打完一局」。

**理由**：
- **衣柜**：韩版 `ShopItem.ini` 里那 25 件是现成的（§5），但要上架得先解决
  「第 4 个基础角色没有第 4 个装备掩码」这个**真问题**（客户端 `0x5583d3` 只认 3 个）。
  这是一个独立课题，不该塞进第一版。
- **武器音效**：11 个 `FX/wp/ch03/*.ogg` 缺了**只是没声，不会崩**（§4 已证伪「会崩」）。
- **打包**：`dist/` 和更新器不动，等实机跑通再说。

## D6 · X_Mod 的进度文件用「**需求池 + 体量红线**」，不沿用线性路线图（用户 2026-09-15 拍板）

**选**：`PLAN.md` 改成需求池（每个 Mod 一条，`X1` 起，四种状态）；
`CLAUDE.md` 里把每个文件的体量预算写成硬规矩；编号全部从 1 重新起。

**否掉的方案**：照抄 V0.3 的 `M0→M9` 线性路线图 + 接着 `D118` 往下编号。

**理由**：V0.1~V0.3 是**有终点的版本**，X_Mod 是**长期会不断加需求的分支**，
线性路线图表达不了「随时插一条新需求」。
编号重起是为了让引用不含糊 —— 本版写 `§1`，旧版一律写全前缀。

**体量红线的由来**：V0.3 那套四个文件加起来 **826 KB**，后期每个会话光读索引就吃掉大量上下文。
判据定成「**下一个会话需不需要它**」：改了哪一行 git 里有，**为什么这么改**才要写下来。

## D7 · 双面薄片的修法：**只改邻接表规则**，顶点照旧复制一份反着放（Fable 子代理，2026-09-15）

**选**：`mshtool.build_adjacency` 改成原版导出工具的「每条边上的面按面号**两两配对**」；`geometry.py` 的 `mirror_faces` 不动
（正面 / 反面各一份顶点、法线相反）。几何一个字节不变，只有邻接表重算（§17）。

**否掉的方案**：
① **把 UV 岛向内缩半个纹素 / 加 padding** —— 贴图和 UV 本来就干净（每片花瓣独占一块连续渐变），
   缝只在花瓣外沿，怎么缩都画不出花瓣**内部**的格子；
② 照 `ch01B0006` **共用顶点、只反绕向**（不复制顶点）—— 反面会拿正面的法线，永远背光；
   而且它不解决问题：只要邻接规则错，共用顶点的 4 面边一样会配错；
③ 干脆做成**单面** —— 裙摆从下往上看、披风从侧面看会消失（引擎开背面剔除）。

**理由**：根因在表不在网格；两两配对在 ch01 256 个文件上的复现从 213 涨到 235、无退步，
说明它才是导出工具真正的规则，不是为这一个 bug 打的补丁。

## D8 · 武器网格**在骨骼局部坐标里从零造**，不在卡希尔的件上改（Fable 子代理，2026-09-15）

**选**：`tools/ch03_skin/weapons.py`：每件在「骨骼局部（场景单位）」的设计坐标里用管子 / 盒子 / 花瓣拼，
再乘母本文件里那根骨的 `inv(offset)` 落进网格空间；骨骼名取母本文件的子集、offset 逐字节抄；贴图 128×128 R5G6B5 按件独立。

**否掉的方案**：
① **给卡希尔的件换贴图** —— 手枪 / 酒瓶 / 宝箱的造型摆在那儿，怎么涂都不是叶子枪 / 种球 / 喇叭花；
② **在网格空间里直接摆坐标** —— W0001 的顶点带 5.849 缩放、枪管还抬 25°，手算必错；
   骨骼局部里「原点 = 手、+x = 枪管」，尺寸直接对着原件的包围盒抄（§18）；
③ 트릭스터只做**一把**（参考图只画了一把）—— 动作是卡希尔的双枪，左手会空着；
④ 弹体换成别的骨名 —— 弹体骨是对象自己的帧，名字一改引擎查不到（§18）。

**理由**：用户要的是造型（「能不能换成她自己的」）；骨骼局部坐标让「在手里的位置 / 朝向」直接继承原件，
风险只剩造型本身。`D0003A` 顺手脱离 `D0003` 的别名关系，变成独立的药水 / 图腾（有自己的 `ch03D0003A.dds`）。

## D9 · 步枪的持枪动作：**从瓦尔基里移植上半身旋转**，不拷文件、不手摆（用户 2026-09-16 要求「类似瓦尔基里」）

**选**：`tools/ch03_skin/anim.py`：Stand01 / Attack01 / Reload01 / Run-F01 / Run-B01 / Idle101 六个动作，
上半身 31 根骨抄 ch102 的旋转键，其余全留爱琳自己的（§20）。母本 ch01 → 爱琳，幂等。

**否掉的方案**：
① **直接拷 ch102 的 `.mtn`** —— 她的树 169 节点，爱琳网格引用的头发 / 裙摆骨不在里面，查不到就崩（§20）；
② **手工摆姿势** —— 没有任何 3D 软件，一帧一帧写四元数不现实，而且做不出换弹 / 跑动那种连贯动作；
③ **保留卡希尔的双枪动作只换模型** —— 图标只有一把枪，双手各举一把手枪的姿势拿着一把步枪必穿模。
④ 全身移植（含腿）—— 腿长不同，脚会浮起或插进地里；下半身留自己的最稳。

**代价（要认）**：Jump / Crouch / Dash01 / Damage 仍是卡希尔的双枪姿势，步枪跟着右手走，那几帧里只是单手拎着。

## D10 · 步枪绑在右手 `Bone_Wp01_R02`，枪口点用**静态骨补丁**挪到枪口，不反过来迁就原 FirePoint

**选**：`rig.py` 在移植后 Stand01 第 0 帧的世界坐标里定枪身轴线和枪口，换算成 R02 局部坐标摆网格；
`Bone_Wp01_R_BarrelPoint / R03 / R_FirePoint` 三根静态骨的局部矩阵改成沿枪身、落在枪口，打进全部 86 个 `.mtn`。手杖同理。

**否掉的方案**：
① **让枪身穿过原来的 FirePoint** —— 那个点是卡希尔手枪的枪口（R02 前方 38、抬 25°），移植后的右手朝向一变，
   它跑到头顶左上方去了（世界 y≈100），枪身要迁就它就得斜指天；
② **新增一个 `Bone_Wp01_FirePoint` 节点**（引擎优先找它）—— 要改树的节点数，86 个文件 + 所有网格的骨骼查找都跟着变，风险高于改三根已有骨的矩阵；
③ **绑左手链 `Bone_Wp01_L02`**（瓦尔基里的枪挂左前臂）—— 引擎只读 `_R_FirePoint`（§19），左链上没有枪口点。

**理由**：改矩阵不改拓扑，`mkchar --audit` 的骨架树一致性照样成立；`rig.py` 一份数字两边共用，网格和枪口点不会各说各话。
## D11 · 卡希尔的锤子走 `mkchar.py` 的 **`SKIP` 名单**，不是手动删文件（用户 2026-09-16 要求删）

**选**：`mkchar.py` 加一份 `SKIP = {("ch01","ch03"): {"ch01J0000.msh", "ch01J0000.dds"}}`，
克隆时就不产出；体检 ① 反过来盯着「SKIP 里的文件又冒出来」当**硬伤**报。

**否掉的方案**：
① **只 `rm` 掉那两个文件** —— 下次谁重跑一遍 `mkchar.py` 就原样长回来，而且长回来是静默的；
② **把网格改成空网格 / 把贴图涂成全透明** —— 文件还在、还会被加载，只是看不见；
   而另外 14 个角色**根本没有 `J` 件**，照它们来才是对的（§21）；
③ **把它改造成爱琳自己的近战武器** —— 她的 `[ch03-dash]` 写的是 `WMeshIdx=-1`（空手），
   `Dash00-DamagingObjBone=Bip01_R_Finger1`（右拳）—— 原版就没打算让她拿东西（§22）。

**理由**：克隆脚本是**唯一的产出口**，白名单 / 黑名单都该长在那里；
「删了又长回来」这种回归只能靠体检卡住。

## D12 · 冲刺攻击移植取「**全部 45 根 Biped 骨的旋转 + 爱琳自己的时长和平移**」

**选**：`anim.CLIPS` 的每条动作自带一个「抄哪些骨」的选择器；`Dash00` 用 `take_biped`
（全部 `Bip01_*`），时长取爱琳自己的 **1.0 s**，平移 / 缩放照旧全留爱琳的。
`ch03@Dash00.evn` 的两条特效改指 `Effects/CH03/` 里原版发好的那两个。

**否掉的方案**：
① **照 D9 只抄上半身** —— 冲刺是整个身体扑出去，只换上半身就成了「瓦尔基里的上身 + 卡希尔的抱熊步」，
   离线对照条上一眼能看出来没像源动作；
② **取 ch102 的 0.833 s 时长** —— 爱琳 `Dash00-TotalFrame=31`（≈1.0 s），动作短 6 帧会在技能末尾定格；
③ **连根骨平移一起抄（按 75/90 缩放）** —— 渲出来和留爱琳自己的看不出区别，
   却要多一套「重锚定 + 缩放」的机制，没有收益就别加（D9 的「平移全留自己的」继续成立）；
④ **留着 CH01 的冲刺特效** —— 它挂在 `Bone_Wp00_11`（熊身上的骨），爱琳没有熊，
   特效就从空气里冒出来；CH03 版挂 `Bip01_R_Hand`，是原版给她画好的花瓣（铁律 12）。

**代价（要认）**：D9 ④「腿长不同、脚会浮起」在这一条上**不成立** —— Dash00 全程腾空，脚不落地。
别把这条当成「以后全身移植都行」的先例。

## D13 · `.evn` 里的特效改指做成**一张全局路径映射表**，放在 `anim.py`，不是逐个动作写

**选**：`anim.EVN_EFX_REMAP` = `{母本 ch01 的特效路径: CH03 版}`，对全部 61 个 `.evn` 生效；
名单由「母本引用的 24 个特效逐个去 `Effects/` 找 CH03 版，**有就换、没有就留**」得出，共 16 条。
main() 开头逐条断言目标文件真实存在，跑完断言每条都在母本里命中过（名单不许留死条目）。

**否掉的方案**：
① **按动作名分组写**（`{"Dash00": {...}, "MutuStand-P00": {...}}`）—— 同一个特效被 7~8 个动作引用
   （`CH01_MutuDust` 7 个、`CH01_DashDust00` 8 个），分组写就是把同一条抄 8 遍，改一次要改 8 处；
② **搬进 `mkchar.py` 当克隆的一部分** —— 概念上更对（这是「克隆得更像」），但 `mkchar` 的
   `.evn` 现在是**一个字节都不改**的纯字节复制，破了这条就得同时改它的幂等 / 下游保护那套判据；
   而 `anim.py` 本来就在写 `.evn`（`EVN_FROM_SRC` 把 Reload01 换成瓦尔基里的），放这儿不新增职责；
③ **机械串替换 `CH01`→`CH03`** —— `CH01_MutuDust.efx` 的 CH03 版叫 `CH03_MutuDust00.efx`（多个 `00`），
   而 `Jab00` / `DashAttack01..05` / `Dash04` / `Dash-B04` **根本没有 CH03 版**，无脑替换会造出死引用。

**理由**：判据是「磁盘上有没有」，不是「名字像不像」；一张显式表 + 两条断言，
比任何猜名字的规则都好查。`mkchar --audit` 第 ⑤ 条（死引用不许变多）再兜一道。

## D14 · 空手跑 `Run-F00` 的源取 **ch00 泰尔**，而且摆臂相位**算出来**，不写死

**选**：`CLIPS["Run-F00"] = dict(src="ch00", ..., sync_legs=True)`；
`leg_sync_phase()` 在动作自己的帧格上把一个周期试一遍，取「同侧手·同侧脚前后位移内积」最负的偏移。

**否掉的方案**：
① **继续用瓦尔基里**（前两轮的动作都来自她）—— 她的 `Run-F00` 是把步枪架在左臂上跑，
   左右上臂摆幅 78.3° vs 16.5°，是全表**最不对称**的一个，抄过来比不改还糟（§23）；
② **取 ch02**（左右差最小，2.8°）—— 他 `DisplayHeight` 90、摆臂幅度小、而且腿相位和卡希尔差 14/24 个周期，
   要对的相位更多；ch00 和爱琳同为 75、前臂摆幅左右恰好相等；
③ **把源的左臂镜像给右臂**（不换源，只修对称）—— 实测 Biped 左右骨的局部旋转**没有**干净的镜像关系
   （恒等 / 翻 yz / 翻 xw 三种猜法误差都在 0.28~1.24，没有一种接近 0），做不出来；
④ **连腿一起抄**（`take_biped`）—— 相位问题自然消失，但迈步幅度会跟着变，和 `ChrSpeed 7.5` 不配套会脚底打滑；
   腿是现在唯一没人抱怨的部分，不动它；
⑤ **相位偏移写成常数 `-3 帧`** —— 那是拿这一对动作的观测值当真理（铁律 10）。换一份源、
   或者母本动作改了，常数就错了，而且错得没有任何提示。

**理由**：「协调」是可量的 —— 手和同侧脚反相。既然判据能算，就让脚本每次自己算。

## D15 · 爱琳的嗓音 / 体感音整体换给 **ch100 앨리어스**，判据是**选人语音的基频**（用户 2026-09-16 拍板）

**选**：`EVN_RES_REMAP` 里 17 条（受击闷哼 / 三条吆喝 / 格斗受击三件 / 跑步左右脚 / 蹲行左右脚 /
jab / 格斗命中 / 强踢 / 冲刺两条 / 武打音）**全取同一个人** —— ch100 앨리어스（艾丽亚丝）。
剩下 12 条全游戏只有 ch01 有对等件，**留着卡希尔的**，用 `audit_leftover_ch01()` 和
`UNMATCHED` 对账，名单一漂就断言失败。

**怎么挑的**：`Sounds/CharacterChanger/ChNNN.ogg` 是各角色**念自己的名字**（10 个角色
146~459 Hz 各不相同 ⇒ 不是旁白）⇒ `ch003.ogg` 就是爱琳本人的声线，F0 中位 **324 Hz**。
在「有整套嗓音 + 体感音」的 5 个角色里按「离 324 最近 + 女角 + 不是基础角色」筛：
앨리어스 269 ✓ / 진 242（男）✗ / 발키리 216 ✗。

**否掉的方案**：
① **泰尔 ch00（329 Hz，只差 5 Hz，全表最近）** —— 但他是**基础角色，天天同屏**，
   换过去等于把「像卡希尔」换成「像泰尔」，用户下一句就会再提一遍；프로코 286 同理；
② **发키리 ch102**（她的动作已经全是这套，最「连贯」）—— 216 Hz 比爱琳低 108，
   花精灵配一把成熟女战士的嗓子，比现在还别扭。**动作可以学她，嗓子不行**；
③ **按每条音单独挑最像的** —— 会拼出一个「受击是 A、吆喝是 B、脚步是 C」的怪物。
   一个人一套声线才是「结构角色」该有的口径；
④ **拿实战吆喝的基频当判据** —— 那些文件只有 8~24 个有声帧，自相关锁共振峰，
   同一个角色不同文件能差 200 Hz，测不准（选人语音有 75~134 帧，稳）。

**代价（要认）**：她现在听着像艾丽亚丝。真要「像她自己」只能自己配音 —— 原版没给她录（§24）。

---

## D16 · `Jab00` 的特效取「**结构同级**」的 `CH03_MutuStand-P00`，不是同名件也不是 `DashAttack00`

**背景**：D13 定的口径是「母本引用的特效逐个去 `Effects/` 找 CH03 版，**有就换、没有就留**」。
`CH01_Jab00.efx`（格斗普通拳）没有同名的 CH03 版 ⇒ 按 D13 留着了。
但它挂的是 `Bone_Wp00_11` —— **右手上那条武器骨链的末端**，静态偏移累计 79.7（§25）。
卡希尔的泰迪熊蒙在那条链上，爱琳 `WMeshIdx=-1` ⇒ 特效在她**离拳头 69 的空气里**炸开。
而这一条**是她真放得出来的**（另外 6 条 CH01 特效全挂在她放不出来的 `Dash01~05` 上）。

**选**：改指 `CH03/WP00/Efx/CH03_MutuStand-P00.efx`。
判据是**结构同级**，不是「名字像」：`CH01_Jab00.efx` 是 **9306 字节 / 2 个发射器**，
`CH03_MutuStand-P00.efx` 是 10115 / 2，挂 `Bip01_R_Hand`，用她自己的 `CH03_MutuAttack00.dds`。

**否掉的方案**：
① **继续留着卡希尔那份**（D13 的字面口径）—— 等于明知道它飘在身外还不管，
   和用户「剩下的格斗特效也改成艾琳的」那句原话相违；
② **改指 `CH03_DashAttack00.efx`**（按**美术血缘**它才是正解 —— `CH01_Jab00.efx` 9306 和
   `CH01_DashAttack00.efx` 9314 只差 8 字节、同一对贴图，卡希尔的「普通拳」本来就是拿冲刺特效改的）
   —— 但爱琳那份被原版重画过头了：**27984 字节 / 6 个发射器**的花瓣大招，
   一记轻拳放这个太重，而且和她 `Dash00` 撞成同一个；
③ **自己造一个 `CH03_Jab00.efx`** —— 没有 `.efx` 的写出工具，收益也撑不起。

**⇒ 口径从此扩一档**：没有同名 CH03 版时，**先在她自己的特效里找结构同级的顶上**，
实在没有才留源角色的；留下来的必须进 `UNMATCHED_EFX` 对账（§25）。

---

## D17 · 1 号武器的发射音**维持泰尔的左轮** —— 换 발키리的机枪试过，用户听完否掉（2026-09-17）

**★ 结论先说**：`ch00@Attack01.ogg` 不动，**别再换**。下面那套「按射速挑」的推理**推错了**，
留着是为了让下一个会话别再推一遍。

**过程**：先按射速把它换成 `ch102@Attack01.ogg`（발키리 아머드 머신건），当天用户实机听完
说**「新的不好听」**，当场改回。数值上 발키리那条确实更短更亮
（0.41 s / 有效 0.30 / 质心 1376，对左轮 0.77 / 0.55 / 1076），**但人耳投票已经投过了**。

**⇒ 教训**：客观指标（时长、质心、低频占比）能证明「结构上对不上」，
**证不出「好不好听」**。§24 那条「别借同屏角色的招牌音」是**风险提示，不是判据** ——
3 号武器那次是用户先说「和卡希尔一样」才改的，这次没人抱怨，我是自己推出来要改的，
推错了。**这类换法一律先问、以耳朵为准**，别拿数值当授权。

**现在的已知代价（是代价，不是待办）**：① 泰尔是基础角色、天天同屏；
② 左轮有效 0.55 s 而她 `CoolingTime=90`，连射会叠层。**两条都认下来。**

---

## D17 附 · 当时那套推理（存档，别照着再改一遍）

**背景**：D15 挑声线 donor 用的是「选人语音基频」，那管的是**嗓子**。
武器音不是嗓子，得换一条判据。1 号 `트릭스터` 原先借泰尔的 리볼버，两条毛病：
`CoolingTime=90` 是**全游戏最快的连射枪**（泰尔左轮 220，差 2.4 倍），而左轮那条有效 0.55 s，
90 ms 一发叠六层；泰尔还是**天天同屏的基础角色**（§24 那条「别借同屏角色的招牌音」）。

**选**：`ch102@Attack01.ogg` —— 발키리 아머드 머신건，`CoolingTime=130`，
是快枪档（90~140）里离她最近、且**不是基础角色**的一把。
0.41 s / 有效 0.30 s / 质心 1376 Hz / 低频 37%，lite 那份 0.38 s / 2721 Hz 一起换。
★ 附带好处：爱琳 1 号的**持枪动作本来就是从瓦尔基里移植的**（D9）—— 动作和声音同源。

**当时考虑过的其他候选**（都没上；**现在也不要上**，除非用户点名）：
① **`ch101@Attack01`（진）** 0.41 / 有效 0.39 / 1579 Hz —— 更亮更脆，角色更罕见；
② **프로코 ch02 的机枪**（140 ms，结构最像）—— **基础角色**，和泰尔同类问题；
③ **「没人引用的孤儿音」**（3 号武器那条路子，铁律 13）—— 全盘扫过 1367 个音频，
   武器类孤儿只有 `chNN@Attack04/05` 之类（0.60 s / 起音 0.050 s），是**蓄力炮**不是连射枪；
④ **2 号武器一起换** —— 三个角色的投掷音几乎是同一个声（0.17/843、0.16/809、0.15/777），
   换了也听不出，**白改**。这一条到现在仍然成立，2 号维持泰尔的。

★ 那次得出的方法论「嗓子看基频、枪看射速」**只对了一半**：它能挑出「结构上合适」的，
挑不出「好听」的。见 D17 正文的教训。

## D18 · 蝴蝶卡住走**exe 字节补丁**（追踪侧补一条死活判据），不去调 `HomingAngle`

**为什么不调数据**（第一反应就是把 `HomingAngle=300` 改成别人那样的 30）：
那只是把盘旋半径从 ≈8 改成 ≈83 —— **仍然小于 `HomingRange=200`，照样绕，只是圈大了**，
能不能解脱全看那个大圈有没有蹭到地形。要让它像 `ch01-03` 那样自己甩出 200 外，
得把 `HomingAngle` 压到 **12 以下**（R = v/(2·sin(θ/2))，v 只有 6~8），追踪基本就废了。
★ 而且这个「够不够大」取决于弹体**当帧的速度**（重力一直在改它）——
拿一个常量去躲一个逻辑 bug，正是全局规矩里禁的那种阈值。

**选定**：在 0x47e40a（`add esp,0x14` / `test eax,eax`，正好 5 字节、两条完整指令、
函数内无分支落在其中、13 字节特征串全镜像唯一）挂 detour，目标 `+0x54` 进 3/4 就把 `eax` 清零，
**复用原版 0x47e411 那条 `[+0x328] = -2`**。之后弹体走的是「一开始就没找到目标」那条
**已经在日志里验过**的路：按重力飞、落地爆炸。

**判据为什么取「状态 3/4」而不是 HP ≤ 0**：要和**命中侧**（0x47e037 里 `0x417c72` 那一发）
**逐字一致**。HP 先归零、`Character::Die()` 要等服务端 `0x0406` 死亡广播才调，
中间那几帧命中侧**仍然打得着** —— 那时候就不该撒手。跟着同一个谓词走，两侧永远同进同退。

**副作用**：对所有追踪弹生效（`ch01-03` / `ch102-02` 也吃到），方向一致，属于修好不是改味。
`BSHOOK_KEEP_HOMING_DEAD=1` 可退回原版行为做对照。

## D19 · 全量测试的名单改成**现扫目录**，不再手写 `MODULES` 表

**被否掉的方案**：继续维护手写表，另加一条守护用例（「表里必须覆盖所有 `test_*.py`」）。
这是本仓库对付「一件事两个源」的惯用手法（`test_ports` / `test_packdirs` 就是这么干的）。
这次不用它 —— 那两处是**没法只留一个源**（C 头文件必须生成出来），而这里**能**：
目录本身就是名单。加守护用例的话，新增模块仍要手动登记一次，只是漏了会红，
不如根本不需要登记。

**为什么非改不可**：这张表漏登记过**至少三次**（`test_pkn` / `test_packdirs` /
`test_questrecord`，合计 64 条用例从没跑过），而症状是**沉默的** —— 全量照样报绿，
只是少跑几十条。下游后果已经发生：2026-09-17 pkn 金样被当成「无用的测试数据」删掉
（`ad43f621`），正因为守着它的 `test_pkn` 不在名单里，看起来真没人用。

**代价**：`test/` 里随手放一个 `test_xxx.py` 就会自动进全量。认了 —— 这正是要的效果。
★ 扫不到任何 `test_*.py` 时**红着退出**，绝不许报一句 `Ran 0 tests / OK`
（那正是要消灭的那种「沉默的绿」）。

## D20 · 测试代码和夹具整体搬到根目录 `test/`（用户 2026-09-18）

**用户的话**：夹具「不要放到 testdata 目录里，跟 data 排在一起看起来很别扭」；
「所有测试相关代码也挪到 test 文件夹里，数据在 test 文件夹里单开一个文件夹」。

布局：`test/run_tests.py` + `test/test_*.py` + `test/testsupport.py`，
夹具在 `test/data/<用途>/`（目前只有 `pkn/`）。被测代码仍在 `server/`。

**为什么这样搬不会出事**：

- `test/` 和 `server/` **平级** ⇒ 各测试里的 `ROOT = os.path.dirname(HERE)` 一个字不用改；
- 每个测试模块把 `server/` 和 `test/` **一起**挂上 `sys.path`（原来只挂自己那一个目录）；
- 打包器按**白名单**从根目录挑东西，`test/` 不在名单里 ⇒ 发布包里不会多出测试；
- `run_tests` 的 worker `cwd` **仍然指 `server/`**，故意没跟着搬 —— 用例本来就不该依赖
  cwd，但借搬家顺手换掉它，将来真出问题，查的人会先去怀疑搬家。

★ `test/README.md` 里写清了「这里的东西看着没人用也别删」，就是为了不再出 `ad43f621`。

## D21 · 武器自带的减速**只压服务端走速，不广播 `0x040a`**

**被否掉的方案**：照 `_step_on_slow_mine()` 的成例，服务端压走速 **+** 广播一发
`0x040a gspItemEffect(bot 座位, 10603)`。那是 §109 定下来的「bot 身上挂状态的唯一正确姿势」，
所以第一反应就是照抄。

**为什么这次不能抄**：两者的施加侧**方向相反**（§31）。胶水那一摊只对「踩上去那台机器的
本地玩家」生效，bot 没有本机 ⇒ **没有一台会算**，必须补广播；而武器属性走
`Projectile` 的碰撞处理（`0x47e0f7` / `0x47ef51`），那两个函数里**一个 `IsMine` 门都没有**
⇒ **每台机器都已经给 bot 挂上了**。再广播就是双份，而且 `Item.ini [Slowed]` 走的是
`Status.ini[14] Time=4.0`，和武器写死的 `AttributeTime=2100` 对不上，bot 会比真人多慢 1.9 秒。

**代价**：服务端和客户端的到期时刻各算各的，差一个网络单程。认了 —— 真人之间本来就是这样。

★ 判据别记反：**问「这个效果是谁算的」**，不是「bot 有没有本机」。

## D22 · 商店里**干脆不出现第 4 个角色**，不给 bshook 加 detour（用户 2026-09-18 拍板）

**背景**：商店左侧模型画成艾丽亚丝，根因是数组只有 3 格、绘制没边界检查（§33）。

**被否掉的三个方案**：

1. **给 bshook 加 detour**（`VirtualAlloc` 可执行内存 + 5 字节 `jmp`），把商城槽 0 那个
   `Character` 的 `ChrIndex` 在 3 和 100 之间按需切。效果最好，但那是**注入 DLL 里的新机制**，
   而且爱琳在商店里仍然是**不带装备的裸模型**（穿衣服那三条路写死丢弃 id 3）。
   ⇒ 风险和收益不成比例，落在已经有真人在玩的客户端上不值得。
2. **id ≥ 3 就不画**（左边空着）。23 字节窗口里塞不下到函数出口的远跳，实际上**也得走 detour**，
   而结果只是「空着」。
3. **把第 4 个基础模型挤进商城数组第 0 格**（改 `0x44dcb4` 的 `cmp …,3`）——
   会把 **ch100 艾丽亚丝的商城预览**顶成爱琳，而那 11 张商城角色卡是真上架物。

**选的**：撤掉 P6（`0x44caaf`，商店角色行的循环上界），商店只剩 3 个原始角色。
**零新机制、零风险**，而且撤掉的是一个本来就没用的入口：爱琳在商店里穿不了装备、
货架按 `usable_by()` 过滤后也只剩「不限角色」那几件。选角色打游戏走的是房间 6 格面板
和战斗换人条，**一点都没动**。

★ L1 / L2（`0x44c954` / `0x44cc07`）跟着变成**死补丁**，故意留着不删：零风险，
而且哪天要把爱琳放回商店时它们还得在。

## D23 · bot 去蹭回血图腾：**血 < 90% 且看得见就去，一进圈就撒手**（用户 2026-09-18）

**用户的话**：「如果自己血量低于 90%，并且视野范围内有回血图腾，则主动尝试靠近图腾给自己
回血（回血期间也不要傻站着，在范围内都可以回血，回血期间不要阻断躲子弹以及向敌方开枪的
动作）」；敌我判定选「**只蹭队友放的**」。

**被否掉的方案**：和「主动捡道具」同一档 —— 只在 `target is None`（这一枪打不出去）时才绕过去。
更保守，但**血很低又正好有射界时反而不去回血**，和用户的原话不符。

**排在哪**：`_move_intent()` 里「破障」之后、「闯关 / 框外走位 / 拉开距离」之前。这个位置让
三件事变成**结构上保证**的，不靠约定：① 躲子弹 / 脱困 / 牵引绳都在它上面；② 走位和开火
在 `_tick_bot` 里是分开的两段，开火只吃 `machine.aim`；③ 闯关和「视野里没敌人」那两支都在
它下面，用不着各挂一份（原计划要挂三处，实际一处就够）。

**一进圈就不再干预走位**：`_walk_to()` 到了目标点返回的恰恰是「不动」，那正是用户说的
「傻站着」。所以只在半径外改 `spot`。代价是可能走出去再回来 —— 但每次进圈都真回了血。

★ **不会变成 §175 / §176 那种停机状态**：图腾自己 5 秒就没了，目标必然消失；血涨过 90%
也退出。两个出口都是事实，不是定时器。

## D24 · 「♥HEAL」那两帧**补资源**，不打字节补丁；§27 另外两份**不动**

**为什么不补丁 exe**：`0x48e77e` 取 `frames[22]` 是**对的** —— 原版图集本来就有 24 帧，
标签也本来就该显示。缺的是中文版翻译时截掉的那两帧（§34）。
补丁会把一个「资源少了」的问题伪装成「代码不该这么写」，而且会**永远看不到这个标签**。
`.smf` 前 22 帧和原版逐字节相同 ⇒ 整份抄回来是零风险的，和 §3 补 `BigChrIcons_CN` 同一招。

**为什么 §27 另外两份不跟着补**：

- `RoomUserSlot.smf`（27 / 28）、`ShopSlotImg.smf`（20 / 22）**没有这种取帧点**。
  全镜像扫过「`mov r,[x+0x24]` 紧跟 `mov r,[x+r*4+K]`」这种绕过边界检查的写法，
  **带高位常量下标的只出现在 `0x48e3xx`~`0x48e7xx`**（全是结算界面这一个函数），
  别处都走 `ImageSet::Draw`（`0x5ccefc`）—— 那条路查 `([this+0x28]-[this+0x24])/4`，
  越界只是不画，不会崩。
- `ShopSlotImg` 中文版还是**另画**的（不是截断，§27 已记）⇒ 照抄原版会错位。

⇒ 这张「帧数下限」表只收**确实会崩**的两份（`test/test_smfframes.py` 的 `GUARDED`）。

## D25 · D3D 索引缓冲那条走**跳板补丁**，而且**离线把站点钉死**，不指望实机复现

**为什么非跳板不可**：站点 `0x5bf6d0` 只有 9 字节，三条指令一条都省不掉
（`[ebp+0x54]` 是函数返回值，`[ebp+0x5c]`/`[ebp+0x60]` 是循环计数），
加 `cmp`+`jcc` 还差 6 字节；而那一带**没有 `int3` 填充洞**（§33 查过，exe 加过壳）。
⇒ `VirtualAlloc` 一块 RWX 当洞。这是本仓库第一个跳板式补丁，`install_inline_hook`
那套用不上（它是给**函数头 + C detour** 用的，这里要的是函数中段、`ebp` 相对的局部变量）。

**判指针不判 `HRESULT`**：要防的就是「往这个指针写」，那就直接判它。
而且 `0x5bf6c7` 先把它清了零，`Lock` 成功必然写非零 ⇒ 判指针严格包含判 `HRESULT`。

**跳到 `0x5bf73f` 而不是 `0x5bf733`**：压根没锁上，就不该 `Unlock`。
代价是这一次索引缓冲没填好（下一次重建成功前画面可能有乱三角）—— 但原版在这条路上是
**直接崩**，没得比。真要「重建失败就整体重试」是另一个量级的改动，不在这一轮。

**被否掉的方案**：① 包 `Lock` 的 vtable，失败时发一块 3 KB 草稿纸给它写 —— vtable 是
全进程共享的，surface 比 9 字节站点大得多；② 干脆改窗口化（`UserConfig.ini FullScreen=0`）
—— 那是**玩家的选择**，不该由我们替他定，留作规避手段写进 §35。

★ **「没法实机复现」不等于没法验**：站点特征串、两个跳转目标落在哪条指令上、
代码洞每一个字节、以及「循环的回跳不会落进被覆盖的 9 字节」，全部拿
`re/BigShot_22524.img` 离线判定，见 `test/test_patchsites.py`（9 种改坏法都试过会红）。

## D26 · 自定义武器的数值走「服务端下发 `0x0F01` + bshook 写内存」，不走重打包 / 改 ini（X3，2026-09-19）

**要的是**：管理页改一格，在线玩家不重登、下一枪就生效；原版武器一个字节不动。

**被否掉的方案**：① **改 `weapon.ini` 重打 `Data~.pkn`** —— 每改一次数值都要重发客户端整包，
而且只能「下次启动生效」；② **hook 拦文件读取喂一份现算的 ini** —— 卷内文件走引擎自己的 VFS
（`0x55e7f0` / `0x55e890`），不经过 Win32 API，要 hook 到引擎内部，收益却和 ① 一样只在启动时生效；
③ **收到包时写一次内存就完** —— `weapon.ini` 每次进图都重读（§36），写进去的数第二局就被 ini 原值盖掉。

**选的**：服务端登录后发一次、管理页保存后广播；hook 吞掉包、按 Id 查表写 12 格，并挂在 `WeaponTable::Load`
的**返回点**上每次重读后再写一遍（返回地址改成桩，桩里施加再跳回）。没在 mask 里的格写回 hook 自己存的原值，
所以「清掉某一格」= 回到资源包参考值，不用服务端知道 ini 里写了什么。老客户端收到 `0x0F01` 无害（§38）。

## D27 · ~~PVE / PVP 两套表由 hook 按「谁在重读 ini」挑，不由服务端按房间模式推~~

> ⚠⚠ **已被 D32 推翻（2026-09-19）**。下面那句「这两个函数本身就是事实」站不住：
> `Load` 不在开局时跑（§42 有实测时间线）。**现在模式由服务端在开局握手里说。**
> 保留本条只为记住「当初为什么会那样选」——「零时序」这个好处是真的，
> 可惜前提是假的。返回地址那套仍然留着当兜底（训练场 / 教程不走服务端房间）。

**被否掉的**：服务端在进房 / 改模式 / 开局时按房间描述符判 PVE / PVP 再发对应的表。多一条时序依赖
（表必须在场景构造前到），训练 / 教程等入口也要一个个覆盖，而且服务端对「这一局是什么」的认知
和客户端可能不同步。

**选的**：`weapon.ini` 全镜像只有两个场景构造函数会重读它 —— 闯关 `0x4a3b7c` 和对战 `0x497cb6`，
这两个函数**本身就是**「这局是闯关还是对战」的事实。hook 在 Load 返回时看返回地址：`0x4a3b81` → PVE，
`0x497cbb / 0x497c63 / 0x435684` → PVP，别的地址沿用上一次并打一行警告。零时序、零猜测（铁律 10）。
服务端一发同时带两套，客户端自己挑。`BSHOOK_WEAPON_MODE=pve|pvp` 只是实机核对用的强制开关。

## D28 · 编号与资源口径：部位码 `92`、~~武器 Id `100C9S0`~~、`_` 键同指黄金资源、原版 ini 逐字节 + 追加块

> ⚠ **武器 Id 那一条已被 D31 推翻**（`100C9S0` 会让客户端的弹药数组越界，§41）。
> 现行编号是 `1 0 0C 3 S 5` / 碎片 `1 0 0C 5 3 5`。本条其余各项仍然有效。

- 物品 id `X92000S` 是用户点名的（「泰尔 112 → 192」）；`92` 在 `PART_KIND` 里登记成 weapon + `custom`，
  首位仍是角色 ⇒ 客户端 `EquipmentEx::Equip` 按 `id/1e6-1` 分桶照旧对。**否掉**沿用 `12` 部位码 + 新序号：
  和 D/R/F 的 `SS` 编码撞车，`weapon_variant()` 会把它误判成某个系列。
- 武器 Id `1 0 0C 9 S 0`（碎片 / 火墙 `…S5`）：保持「`(Id-1e6)/1000` = 角色」，`10009xx` 全空。
- `_`（全年龄）键全部指向和普通键同一批黄金资源，不另做气泡版：没查到 `[0x72e358]` 由谁置（§36），
  两种模式都看到黄金是唯一不赌的做法。**否掉**「照 D3 也做一套气泡色」—— 多 40 个 efx、几十张贴图，收益是零。
- `weapon.ini` 恢复成**原版 CP949 文件逐字节**再追加（标记行起，`build.py` 幂等重写那一块）。
  **否掉**保持 PR 的 UTF-16 整份：原版部分逐字节等于原版才是最硬的守卫（`test_weaponini`），
  而追加块里没有任何中文（说明文早就不在 ini 里了）。
- 手持网格等长改名 `013S→014S`（`mkchar` 的手法，偏移一个不动）；特效副本 `D3→C`、没 `D3` 的加 `_C`。

## D29 · 说明文进第七份运营配置 `weapons.json`，删掉 ini 里的 `Desc=`；仓库提示框重登后生效（用户拍板）

**选的**：`server/weaponcfg.py` 照 `sellprice.py`（无标签页的运营配置：`_SPECS` 登记、热重载、坏文件保留上一份、
备份回滚同组、写锁同序），`{custom: {id: {pve, pvp}}, desc: {id: 文}}`，`item_desc_zh()` 优先用它的说明文；
自定义武器的数值行画 PVP 那套 + 首行「仅显示PVP属性，PVE的请看管理页」（第一版更长的那句在 234 px 里折行，
用户当天缩短；占掉 5 行配额里的 1 行，
最多 8 项按 2 项/行正好装下）。原版武器只开放说明文（服务端拒收数值，前台不画两栏）。

**否掉的**：① 把数值 / 说明文塞进 `items.json`（物品库那条配置页链）—— 弹窗要有自己的「保存」并立刻推送，
和配置页的脏标记 / 三方合并 / 页面级保存搅在一起只会更糊涂；② PR 那种 `weapon.ini` 的 `Desc=` —— 改文案要重打包，
而且 CP949 装不下中文；③ hook 强制刷新仓库提示框 —— ItemDB 插入忽略重复 key（§39），要清表 + 清「已请求」集合，
多一处逆向换来的只是「不用重登」，用户 2026-09-19 选了重登。

## D30 · 配色先出三版效果图给用户挑，实装先用占位配色把整条链跑通

`tools/custom-weapon/preview.py` 用 `mshtool.rasterize` 软光栅渲 9 把手持件 + 精灵 / 图标 / 116 张特效贴图，
黄金 + {黑曜 A / 宝蓝 B / 银白 C} 三版并排；`build.py --variant` 从只读的爆裂 3 母本重做，换变体重跑即可。
**否掉**「等用户选完再动手」：资源 / 服务端 / 管理页 / hook 四条线互不依赖颜色，先用 A 占位把打包、
测试、下发全跑通，用户选完只需重跑一次 `build.py` + `build-pack.bat`。

## D31 · 自定义武器 Id 改成 `1 0 0C 3 S 5`（D 系第 4 档），**不去补丁客户端的解码器**（X3，2026-09-19）

§41 查明武器 Id 会被客户端拆数位当**数组下标**。D28 定的 `1 0 0C 9 S 0` 解出索引 91/92/93，
而弹药数组只有 4 格且调用方不查上界 ⇒ 实机表现「自定义武器无限开火 + 结算界面卡死」。三个方案：

- **改编号**（选中）：`1 0 0C 3 S 5` = 档 3 + 系列 5（D 系），碎片 `1 0 0C 5 3 5`。11 个号全空闲，
  解出来的索引正好是槽位 1/2/3。**只动数据不动代码**；而且从此所有「按系列分支」的客户端代码
  都把它当一把正常的 D 系武器 —— 和铁律 12「顺着原版埋好的语法走」一个路子。
  档位 T 在解码器里**只参与组成索引、归一化后就被丢掉**（另外三个输出是 类别=1 / 角色 / 系列），
  所以 T=3 没有别的副作用。
- ~~hook 补丁改 `0x40a138`~~：**否**。为 9 把武器去改一个 118 把原版武器都在走的基础函数，
  风险和收益完全不成比例；而且真正缺的是**调用方的上界检查**，改解码器是治标。
- ~~hook 给弹药数组扩容~~：**否**。那是 `std::vector`，长度由别处构造，钩不到一个干净的点。

**物品 id 没变**（仍是 `X92000S`）⇒ 玩家仓库和 `server/data/weapons.json`（按物品 id 存）
一个字节都不用迁移，铁律 11 满足。`tools/custom-weapon/spec.py` 是唯一源头，`decode_weapon_id()`
也放在那儿（只依赖标准库，便携运行时能跑），`test_weaponini` 拿它守两条：
「每把自定义武器的索引 == 槽位」+「118 把原版武器回归」。

## D32 · PVE / PVP 由**服务端开局时说**；所有属性改动统一「下一局生效」（X3，2026-09-19，推翻 D27）

D27 的前提「谁在重读 ini 谁就是事实」被 §42 证伪 —— `Load` 根本不在开局时跑。

- **选的**：`0x0F01` 载荷格式 1 → 2，头上多一个模式字节（`0` 任务 / `1` 对战 / `0xFF` 不施加）。
  服务端在 `broadcast_start_game` 里、**`0x0400` 之前**按 `room.session_type` 发真模式
  （房是它自己建的，它最清楚 —— 铁律 10）；登录后和管理页保存后那两发都是 `0xFF`。
  hook 收到 `0xFF` **只换表、不写内存**；收到真模式才写并记进 `g_wtab_mode_server`。
  模式取值三级：环境变量强制 > 服务端 > `Load` 返回地址（兜底，给训练场 / 教程用）。
- **为什么 `0xFF` 不写内存**：弹匣容量在进图时就快照进持枪器了（§42），局内改记录只会让
  准星和实际弹匣对不上。与其做个半吊子的即时生效，不如把承诺说清楚：**所有属性改动下一局生效**
  （用户 2026-09-19 原话：「游戏局内不需要及时反应属性变化」）。管理页弹窗常驻一行提示、
  保存回执也写明；说明文那半边写明「要重新登录客户端，仓库提示框才更新」。
- ~~在换弹函数 `0x48bc2a` 上加 detour 实时刷那份快照~~：**否**。为一个边缘场景去改 118 把
  原版武器都在走的战斗逻辑，风险和收益不成比例（同 D31）。~~让 hook 读某个「当前场景类型」
  全局~~：**否**。闯关那条路不是 GameContext 构造，没找到两边都成立的全局；房间类型服务端本来就知道。

★ `Load` 返回点的重施加**保留**：`Load` 会把整张表按 ini 重解析一遍，不再写就全回默认值。
★ 两边的常量（格式号 / 头长度 / 模式码）由 `test_weaponcfg.HookSourceStaysInSyncTests` 拿
  `bshook.c` 源码核死 —— 漂了不会报错，只会被客户端静默丢包，实机才看得出来。

## D33 · 准星弹格：**补画缺的盘**，不是限制容量、也不是凑合复用（X4，2026-09-19）

§43 查明客户端只为 6 个弹匣容量画了刻度盘，其余退成光滑圆环。三条路摆给用户，他选了补画。

- **选的**：`tools/aimring.py` 拿**原版帧 4（光滑环）当底切 N 条等分黑缝**，把 `2..20` 里缺的
  13 个容量补进图集空白区（帧 19..31），hook 把 `0x48ca0d` 整个改写成跳到我们的表。
  **不画从零画的新盘** —— 实测原版 14 格盘在格子中心的径向剖面和光滑环**逐像素一致**，
  原版美术本来就是这么切的；照这个模子生成，`w=2.5` 出来的 14 格盘和原版帧 7 几乎重合。
- **`> 20` 仍走光滑圆环**：用户拍板，格子太密反而糊成一片。不是技术限制（图集还空着 ~32 个格位）。
- **原版那 6 个容量仍返回原版帧号**：原版武器观感一个像素不变（`test_aimring` 钉着）。
  `cap=2` 用 6 格盘是原版就这样，不顺手"修正"。
- ~~把管理页弹匣限制成那 6 个值~~：**否**。5 分钟能改完，但弹匣容量的自由度是这个 Mod 的卖点。
- ~~只 detour、让任意容量挑一张"格数能整除它"的盘~~：**否**。零资源改动，但一格代表多发，
  而且 17 / 19 这种质数无解，仍然退回圆环 —— 是凑合，不是修好。

★ **帧号不许在 `bshook.c` 里写字面量**：`tools/aimring.py` 一边画帧一边生成 `hook/aimring.h`
  （`build.bat` 每次编译前跑一遍 `--header-only`，和 `ports.h` 同一范式）。
  图集和表分叉 = 客户端拿越界帧号去画野指针（§3 那类崩法），`test_aimring` 三边对账守着。

## D34 · 武器参数范围：管理页收紧，**读盘照旧放行**（X4，2026-09-19）

用户要「加合理的范围限制，不能过于大的离谱」，弹匣指定 1~100。上限照**原版 `weapon.ini` 的
实际分布**定（伤害 0..80 → 开到 500，初速 0..230 → 开到 1000，等等），不是拍脑袋。

- **两套范围**：`weaponcfg.FIELDS` 管**管理页输入**（新的、紧的），`LEGACY_LIMITS` 管**读盘**
  （= 收紧之前那套，宽的）。`save_item()` 对本次提交的那一件用 `strict=True` 再校一遍。
- **为什么不能一刀切**：`shopcfg._load()` 对校验不过的文件是「**整份退回出厂值**」——
  要是读盘也按新范围拦，一格存量的离谱数值就会让 GM 配过的**全部**数值一起消失，
  而且只在日志里留一行 warning（铁律 11）。⇒ 存量值照旧能读能看，GM 改一次就自然换掉，
  **不需要迁移脚本**。`test_weaponcfg.RangeTests` 把这两条都钉住了。
- ~~读盘时把越界值 clamp 到边界~~：**否**。`shopcfg._load()` 的 `validate()` 没有回传警告的
  口子（第三个元素硬编码空列表），clamp 只能静默改数据 —— 比放行更坏。
- **下限没动**：`cooling_ms` 下限一直是 1，而原版有 13 节 `CoolingTime=0`（种子炸弹那类
  + 8 个怪物碰撞伤害）。那是防「射速间隔 0 = 每帧一发」的有意保护，不在这次范围内，别顺手放开。

★ **前台也拦一道**（用户 2026-09-19 追加）：范围写在每一格旁边（「限 a ~ b」），
  超了当场标红那一格 + 整行写清「要在 a ~ b 之间」+ **锁住保存键**。判据在
  `admin.js` 的 `weaponFieldError()` / `weaponDescError()` / `weaponErrors()` 三个**纯函数**里
  （所以 `test_weaponrange.py` 能在 node 里直接问它们）。范围的数只从 `spec.min/max` 来，
  前端一个数都不写死 —— 写死就会和服务端分叉，而症状是「页面放行、服务端拒收」。
  ⚠ 这不是安全边界，服务端 `save_item()` 那道严格校验必须留着。
  ★ 存量的超范围值**打开弹窗就标红并锁保存**：读盘放行只为不丢数据，不为让它能原样存回去。

★ **一格的排版**（用户 2026-09-19 第二轮）：`项目名 · 限 a ~ b · 输入框 · 单位 · 参考 N`，
  出错时在**输入框正下方**加一行红字、**不折行**。做法是**定宽的 5 列 grid**，不是 flex ——
  flex 下标签一长一短输入框就参差不齐，而那是看得见却没有测试会红的回归。
  代价：一栏从 2 列变 **1 列**（五样东西两列放不下，183 px vs 需要的 ~290 px），
  12 行比 6 行高一截 ⇒ 输入框压到 31 px、行距收一收，弹窗照旧靠 `.weapon-scroll` 滚。
  ~~保留两列、把范围塞进 `title` 或只在出错时显示~~：**否**，用户要的就是「写在管理页里」常驻可见。
  ★ 定宽网格靠「每格孩子数一样」对齐 ⇒ **没有单位的字段也要塞一个空 `span`**，
  写成 `if (spec.unit)` 会让后面的东西整体左移一列。`test_weaponrange.SourceGuardTests` 钉着。

★ **PVE / PVP 的先后**（用户 2026-09-19 第三轮）：三处统一成 **PVE 在前** ——
  设置那两栏「PVE 左 / PVP 右」本来就是，浮窗 `admin_desc()` 和弹窗预览区原先反着来
  （PVP 在上），一处左右一处上下，看着别扭。`admin_desc()` 改成 `for mode in MODES`、
  预览区改成照 `view.modes` 画 ⇒ **顺序只在 `weaponcfg.MODES` 定一次，三处跟着走**，
  不在两边各写死一份。两栏仍是硬编码（中间夹着「复制到另一侧」那一列，不好循环），
  由测试钉住它出现的先后。

## D35 · 中文版漏掉的 18 把 3 级武器：**原样抄韩版条目**，别新编 id（X5，2026-09-19）

铁律 12「打开原版埋好的优先于自己造一个」的又一例：数据、网格、特效、图标在中文版客户端里
一件不缺，缺的只有 `ShopItem-Chn.ini` 里那 18 组 `[Item-]` / `[Stock-]`（§45）。
⇒ `tools/openweapons.py` 把**韩版原文逐字节**抄回去，`weapon.ini` 一个字节不动、不编新 id、
不造任何资源。名字 / 价格 / 等级全是既有设计表算出来的（`shopdefaults.weapon_name` +
`WEAPON_PRICE[3]` / `WEAPON_LEVEL[3]`），和 2 号槽那 9 把 3 级武器待遇完全一致。

- ~~给它们编自定义 id（像 X3 那样走部位码 92）~~：**否**。新 id 要补三处枚举上界、两处图标
  switch、名字 switch，还得从零造资源；而原版 id 客户端本来就认，老存档里买过也不会串。
- ~~连 54 条期限售卖形态（`162xxxx` 等）一起抄~~：**否**。它们只有 `[Stock-]`、`ownable` 为假，
  物品库（只收 `ownable`）和我们的商店都收不了 —— 抄过来是 54 条谁也用不上的死条目。
- **插在自定义武器块之前**：`custom-weapon/build.py` 的 `build_shop_ini()` 是「砍掉
  `[Item-1920001]` 之后的一切再追加」，我们的块在那一刀上游 ⇒ 两个工具**任意顺序重跑都稳**。
  `test_openweapons` 钉着这条位置关系，不然哪天重跑生成器 会把 18 件悄悄带走。
- **老服务器的 `items.json` / `shop.json` 不自动写**（D7 / 铁律 11）：`ensure_files` 对已存在的
  配置一律不覆盖 ⇒ 靠 `shopcfg.backfill_defaults()`（只增不改、幂等、写前留 `.bak`），
  入口是控制通道 `shop-backfill apply`。**上不上架是运营的决定**，和 D51 的角色卡同一条口径。

## D36 · 第二批自定义武器做成「**批次**」而不是复制一份脚本（X6，2026-09-19）

用户要在 9 把黄金武器之外再加 9 把（母本换复合 3、配色换粉绿）。三条路：

- **把「批次」提成一等公民**（选中）：`spec.BATCHES` 一批一条（字母 / 母本 / 配色方案 / 变体 /
  部位码 / Id 档位 / Id 系列位），`spec.WEAPONS` 变成**两批拼起来的 18 条**。
  好处是 `build.py` 和 `test_weaponini` 里所有 `for w in WEAPONS` 自动覆盖新批次，加第三批不用改循环；
  而且 `weapon.ini` / `ShopItem-Chn.ini` 都是「砍掉标记再整块重写」，**天然只能一次写全部批次** ——
  这正好也是 `openweapons.CUSTOM_WEAPON_ANCHOR = [Item-1920001]` 那条不变式要求的（`test_openweapons` 钉着）。
- ~~复制一份 `custom-weapon2/`~~：**否**。八处 `D3` 字面量散在四个文件里（§46 列了），复制等于把坑复制一遍；
  而且两个脚本会互相砍掉对方在两个 ini 里的块。
- ~~共用部位码 92、物品 id 换尾号~~：**否**，尾四位会被 `weapon_variant()` 误判（§47）。

**顺带拆掉的雷**：`--variant` 原来默认 `A`，而线上用的是 `C` —— 不带参数重跑会把第一批刷成黑曜。
现在**变体记在 `spec.BATCHES` 里**，不带参数跑就是重现盘上那一套（实测重跑 369 个文件 0 待写）。
没定配色的批次直接拒绝跑，并提示去跑 `preview.py`。

## D37 · 配色迭代了三轮，最后定「樱花粉 + 抹茶绿」（用户 2026-09-19）

- 第一轮 P1~P3（照 Splatoon 2 官方 logo 色 `#ff3f7a` / `#8de926` 算）：**用户否掉「太艳」**，
  要「更少女、更粉嫩、粉得明显、和爆裂系的红有明显差别」。
- 第二轮 P4~P6：查明粉嫩的关键是**压饱和度**（母本紫 s≈0.84，P1 只压到 0.75，亮部仍是「艳」），
  靠 `val_pow` 抬明度。用户选 **P5 樱花粉**（主色 `#FE9EBB` s0.38，对照「少女粉」参考色 `#FFB6C1` s0.29）。
- 第三轮 P7~P9（粉固定、只换绿）：用户选 **P7 抹茶绿**（`#C9FF8C` s0.45）。
- ★ **不用 `sat_by_val`**（按明暗给饱和度）：特效贴图 95% 的像素 v≈1.0，按明度分档等于全取
  「最亮那档」，整体发白。`sat_mul` 保住各自的相对层次，枪身和特效同时成立。
- ★ 粉色目标必须 **≥335°**：紫掩码上界 320 + `h_soft` 8 = 328，落在 328 以下会被自己的规则再吃一次。
- 代码里只留 `P1`（Splatoon 官方基准，已否）和 `P7`（选定）两个变体，中间那些在 git 履历里。

## D38 · `tools/goldwp` → `tools/custom-weapon`；「新商品」置顶用**派生判据**（X6 收尾，2026-09-20）

用户 2026-09-20：自定义武器已经不止金色那批了，目录该改名；另外那 18 把要排到「新商品」最前面。

- **改名的唯一硬约束**：`INI_MARKER` 那一行**写死在 `weapon.ini` 里**，是 `build.py` 判断
  「原版后面那段是不是我生成的」的唯一依据。改文案 ⇒ 盘上那份认不出来、当场 `SystemExit`。
  **处理办法就是拿旧字符串把尾巴砍掉（或 `git checkout` 回去）再重跑一次**，换成新标记。
- ~~加一张 `LEGACY_INI_MARKERS`「历史标记」兼容表~~：**否**（用户 2026-09-20）。
  这套自定义资源**一个包都没发过**，唯一有过旧标记的就是本工作区，重跑一次就没了 ——
  留那张表是在**假装有历史包袱**。★ 这条推广开来：兼容层要有**真实存在的旧数据**才配存在，
  「万一别人手上有」不算理由，那是自己给自己造包袱。
- **置顶判据 = `shop_items.json` 的 `custom`**（选中），不在 `shop.json` 里加开关：
  以后再加一批自定义武器会自动跟着置顶，不用逐条去点；调用方只有 `shelf_entries` 一处，
  哪天要让别的东西置顶，把 `shop.is_pinned()` 改成查配置即可。
  ~~在 `shop.json` 加 `pinned` 字段~~：**否**。要动校验器 + 管理页 UI + 531 条存量补字段，
  换来的只是「现在就能点」，而用户要置顶的正好就是「自定义武器」这一整类。
- **只在「新商品」那一格置顶**：那一格本来就**收全部**（`CATEGORY_NEW`，D 早先拍板
  「对复活项目的玩家来说所有物品都是新的」），是「有什么新东西」该看的地方；
  别的标签照旧，否则「武器」里玩家熟悉的「同系列相邻、由低到高」就乱了。
- **置顶那一撮内部仍按玩家选的顺序排** —— 只把它们整体抬到前面，不替玩家把排序也改了。
  ★ 实测进商店第一发就是分类 2 ⇒ 玩家一进商店第一页就是这 18 把。

---

## D39 · 管理页「来源」筛选折进 `dropdownsMatch`，挂满七处（X7，用户 2026-09-20）

用户：物品多了之后找不到自己加的那批，要一个「全部物品 / 原版物品 / 自定义物品」下拉。
问到范围时明确「**凡是有筛选条的地方都加**」—— 以后会有自定义材料 / 防具，
卖出页也不会空（自定义武器可以卖）。

- **判据折进 `dropdownsMatch()`**（它已经是七个调用点共用的那一条）：加一个下拉 = 加一条判据 +
  七处各挂一个 `<select>`。~~每页各写一份筛选~~：否 —— 同一个筛选抄七份，选项名、顺序、
  判据各歪一点，D42a 当年就是为这个把「上架状态」收成一份的。
- **判据读原版数据 `BYID[].custom`**，不读物品库的页面模型（§48）。和隔壁「角色」那一栏**反着**，
  所以注释和用例都点名写了「别顺手统一」。
- **服务端一行不改**：`custom` 早就随 catalog 发了。~~在 `items.json` 加一个「是不是自定义」的开关~~：
  否，那是让运营去维护一件**代码已经知道**的事，还会和产物打架。
- **「金币 / 经验」「称号卡片」两页不挂**：它们压根没有筛选条（一张定死的二维表 / 17 行一屏看完）。

## D40 · 「添加」改批量；配方材料做成**整组编辑器**，单值字段仍单选（X7，用户 2026-09-20）

用户要「一次上架多个物品」，同时点名「点已有条目弹出的换物品 popup 必须单选」；
第二轮又追加「点已有**材料**时也要开多选，并自动勾上已选的那几种」。

- **分界是「单值字段 vs 一组」**，不是「新增 vs 修改」：一条货架卖一件、一条配方一个产物、
  一条掉落一种材料 —— 多选对它们没有意义（勾两件，哪一件该落进这一格说不清）。
  而配方的材料本来就是一**组**（最多 4 种）。
  ⇒ 代码上的判别特征很干净：**带 `selected:` 的就是单选**，`test_web_admin` 用括号配平扫着这条。
- **材料整组编辑器**：4 个格子点哪个都开同一个多选窗、当前几种自动勾上，
  **取消勾选 = 移除**（用户拍板）⇒ 它**包含**了「换一种材料」，而且满 4 格时也打得开
  （原来的单选版满格就连一颗「＋」都没有）。卡片上那颗「移除」照旧留着，两条路都能删。
  ~~做成「4 − 已有」的补充器~~：否 —— 那样满格还是打不开，而且用户要的是「差几个补几个」。
- **`mergeMaterials` 必须保序保数量**：`Object.keys(chosen)` 对数字型键返回**升序**不是点击序，
  照它重排的话，GM 只想加一种材料，另外三种的位置和填好的数量全被洗一遍（`test_adminpicker` 钉着）。
- **已在货架 / 已有配方的画成灰角标点不动**（服务端本来就拒收重复）；**掉落页不灰**（规则允许重复）。
- **「0 元 + 已上架」不加保存前护栏**（用户 2026-09-20 拍板「什么都不加」）：新条目默认
  `price: 0` + `listed: true`，批量 18 条直接保存 = 18 件白送商品上架。现有 531 条货架里
  **一件 0 元都没有**，所以这是新增的事故面 —— 用户认下了，理由是保存前 18 张卡片都摊在眼前、
  价格那一格明摆着写着 0。**别后人自作主张加回来。** 配方的「空材料存不进去」同口径，只在 toast 里提一句。

## D41 · 掉落页的「筛出 x / y」搬进面板标题（X7 收尾，2026-09-20）

加了「来源」下拉之后**实测**：掉落页筛选条一出现「筛出 42 / 52」就折成两行
（`.filters` 36 → 69 px）—— 正是 D68a 记下的那个位置，1320 里的余量只差 25 px。

- **搬进 `#cfgCount`**（面板标题那一格，写成「筛出 42 / 52 条规则」）：这个仓库**为同一个理由
  搬过一次** ——「删掉 N 条还没保存」当初就是因为「工具条只剩 70 px」才写在标题里的（`touched()`）。
  跟着它走，信息一个字没丢，工具条回到一行。`#cfgShown` 整个删掉。
- ~~再把 `main` 的 `max-width` 从 1320 放宽~~：否。D68a 已经 1180→1320 过一次，再放只是把同一个
  问题推到下一块屏幕上，而且 1366 的笔记本反而更早折行。
- ~~压搜索框~~：否，D68a 明确否过（「控件会在不同页上宽窄不一」）。
- ~~掉落页干脆不挂来源下拉~~：否，用户点名要「凡是有筛选条的地方都加」。

## D42 · 追踪走「`0x0F01` 加两格」，不改 ini、不加开关键（X8，用户 2026-09-20）

§50 查明客户端的追踪开关**就是 `HomingAngle != 0`**，而且所有弹体类都走同一条 tick。
⇒ 这件事的形状是「12 格变 14 格」，不是新机制。

- **被否掉的 ①：往 `weapon.ini` 的自定义小节里写 `HomingAngle=`**。ini 只是**参考值**，
  每次进图重读（§36 / §44），真正说了算的是 `0x0F01`；而且改 ini 要重打资源卷、
  数值还只能「下次发版才变」。走内存那条，GM 改一格、下一局就生效。
- **被否掉的 ②：在管理页另加一个「开启追踪」勾选**。客户端没有这个概念（那个 `Homing=` 键是死键），
  多一个勾就多一处「勾了但转向是 0」的不一致。**填 0 = 不追踪**本来就是引擎的语义，
  照实说出来（输入框下面那句灰字）比再造一个开关诚实。
- **被否掉的 ③：把子弹药（碎片 / 火墙）也开出来**（用户拍板不做）。它们没有独立物品 id，
  要另想挂载方式，工作量翻倍；沿用 X3「子弹药数值不单独开放」的口径。
- **范围 0~1300 / 0~1000 是用户定的**。转向留到原版最大值（300）的四倍多是有由头的：
  每 tick 只转 `值 / 7` 度，而自定义武器初速 100~211，配原版那个 30 几乎看不出弯。
- ★ **代价写在明处**：`WIRE_FORMAT` 2→3 ⇒ 装旧 DLL 的客户端整份丢包、自定义武器数值全退回
  资源包原值。靠 `server-ClientFilter.config` 那道版本门禁逼更新，所以**必须连客户端包一起发**。

## D43 · 金色重做走「给引擎加 `ramp` 算子」，不是继续调 `GOLD` 那三个参数（X9，用户 2026-09-20）

用户嫌第一批的金像**工地黄**。三条路：

- **继续调 `hue` / `sat_mul` / `val_mul`** —— **否掉**。§51 量出来问题不在色值在**曲线**：
  色相全程钉死、62% 的像素 S≥0.9。这三个旋钮是全局乘加，改不出「按明暗走的色相 + 饱和度」。
- **按 `v` 区间把掩码拆成三条规则**（不改引擎就能做的分层）—— **否掉**。软边界在三条规则之间
  会互相叠加，交界处要反复对参数；而且 `v` 在特效贴图上是死的（§51），分层分不开。
- **加 `ramp`（多档渐变映射）**（选中）：一条规则表达整条色阶，**纯增量**（没这个键时行为逐字节
  不变，已用 git 里的旧 `recolor.py` 对拍证实 ch03 那条链 16 同 / 2 异，两异改前就有）。
  `ramp_range` **故意不给「逐图自适应」的缺省**，缺了直接 `raise` —— 把 §51 那条坑钉死在 API 上，
  比写在注释里管用。

★ **撞色银白一个字不改**：用户只说要改黄色。顺手把那个字面量抽成 `SILVER` 常量给 C / G1~G3 共用，
变体 `C` 仍逐字节复现盘上那 9 张（回归对拍过）。
★ **老变体 A / B / C 全留着**：`C` 是「改之前」的对照列，同 P 批留 `P1` 的做法。
★ 三档 G1 / G2 / G3 **渲出来给用户挑，不在代码里替他定**（同 D30）；差别是一条真实的取舍轴
—— 高光越白枪身越闪，但弹道 / 火光越掉金味（G1 保金 / G3 最闪）。
★ **G4 是用户中途提的**（「`3010015` 原版黄金铠甲能不能参考」）—— 照铁律 12「打开原版埋好的」
应该照办，而且量出来原版那套 profile 和 G1~G3 收敛到的是一回事（§51），等于给判据做了旁证。
G4 照它的分位色做，**暗端自己往下补了三档**（那件铠甲最暗只到 `#9C6100`，直接用枪身没有暗部）。

## D44 · 改色拆成「本体 / 特效」两桶，`spec.Batch` 两格变体（X9，用户 2026-09-20）

用户选完色后加的一条：「**枪身做成 G4，子弹特效飞行特效爆炸特效等保持之前的 C**」。三条路：

- **另开一个 scheme / 另开一批** —— **否掉**。两桶用的是同一套掩码、同一个母本、同一批文件名，
  只是取的变体不同；开新批要占部位码和武器 Id 组合（§47 说只剩 4 个），代价离谱。
- **`build.py` 里按文件名 / 路径猜哪些算「特效」** —— **否掉**。判据会散在正则里，
  以后加一类资源就漏一处，而且漏了是**静默变色**，不报错。
- **`spec.Batch` 加一格 `fx_variant`，`Plan` 出 `body_variant` / `fx_variant` 两个属性**（选中）：
  分类写在**调用点**上，`build.py` 每个改色出口都显式说自己取哪一桶。

★ **故意不给「`fx_variant` 缺省跟 `variant` 走」的兜底**：P 批也把两格写成一样的。
  有兜底的话，以后新增一类资源忘了分类就会悄悄跟着本体变色；没有兜底，漏了当场看得见。
★ CLI 同一个道理：`--variant C=G2` 设**两格**（「整批刷成这个色」），`--fx-variant C=C` 再收窄。
  宽的先生效、窄的后覆盖，没有 `orig` 之类的特例分支。
★ 分桶判据落在**「这是枪本身、还是打出去的东西」**：手持件贴图 / HUD 图集 / 商店图标 = 本体；
  3D + 2D 弹体 / 碎片 / 拖尾 / 特效贴图 / `.efx` 的 `<ColorValue>` = 特效。
  ⚠ **3D 弹体归特效**（它是子弹的模型，不是枪身），这一条最容易归错。
★ 验收判据很硬：特效桶维持 `C` 而 `C` 就是盘上那版 ⇒ **重跑后特效文件必须逐字节不变**。
  实测 369 个计划文件里**只有 21 个变**（9 手持件 + 3 HUD + 9 商店图标），两个 ini 和 `.amf` 没动。

## D45 · 仓库说明文按场景切：在**绘制点换指针**，不改 ItemDB、不清缓存（X10，用户 2026-09-20）

需求：大厅商店页 / 仓库页画 PVE，待机房间的快速换装仓库画**该房间的模式**（首行提示跟着变）。
难点是客户端把 `ItemInfo` 缓存死了（插入忽略重复 key，§39）—— 服务端单独做不到。

- **选的**：服务端把算好的整段文案经**新 opcode `0x0F02`** 推给 bshook，bshook 在
  `0x4554e7`（`UiCabinetToolTip` 取说明文那一处，§52）按物品 id 换掉字符串指针。
  文案 100% 在 Python 那边拼（和 `0x0501` 同一个 `item_desc_zh` + `desc_wire`），**C 侧一个字不拼**
  —— 字段名 / 宽度预算 / 「排不下让飞行速度让位」那套逻辑复制到 C 里迟早分叉。
  场景判据 `Conn.weapon_desc_mode()` 复用 `room.session_type == SESSION_TYPE_QUEST`，
  和开局那一发同源（D32），**不新造第三处**。
- **否掉 ① 改写 ItemDB 里那个 `wchar_t*`**：省一个代码补丁，但缓冲的所有权就归了游戏的 CRT
  —— 它在 ItemDB 析构 / 重登时会 `free` 我们的指针，而注入 DLL 和它不是同一个堆，直接炸。
- **否掉 ② 清表 + 清「已请求」集合让客户端重问**（D29 ③ 就否过一次）：要动三处
  （插入的重复 key 分支、`[ItemDB+0x14]`、让仓库面板重建），风险和收益完全不成比例。
- **否掉 ③ 合成提示框（`0x45ff1b`）一起打**：同样的 9 字节、同样的 `this` 布局，但合成只在
  大厅，跟着房间模式走语义是错的。⇒ 补丁必须按**固定 VA**装，按签名全图搜会一次打两处。
- **顺带解掉**（用户同一轮点的）：管理页保存后 `broadcast_hook_item_desc()` 再推一发 ⇒
  自定义武器的仓库提示框**不用重登**就更新了（D29 那条「要重登」只剩原版武器还适用）。
- ★ 下发**全部事件驱动**且带「按内容翻转」的闩（`Conn.hook_desc_key` = 模式 + 载荷 crc32）：
  闩在手，调用点就可以宁多勿少；换图 / 换难度这种不动模式的 `0x0302` 一个包都不发。
  ⚠ 闩的是**载荷**不是 `weapons.json` 的 serial —— 文案还依赖 `shop_items.json` 的武器基值，
  拿 serial 当闩会漏掉「运营改了物品库」那一路，症状是「就是不更新」。
- ⚠ 踢人走 `LOBBY.kick`，**不经过 `leave_room()`** ⇒ 那一路单独挂了一发（漏了就是「被踢回大厅，
  提示框还停在 PVP」）。建房走 `0x0201` 自己那段、**不经过 `finish_join()`**，同理单独挂。

## D46 · 参数端到端验证走「**外部读内存逐格核对**」，不给 hook 加 dump（2026-09-20）

用户要「确保每一把武器的每一个属性都能正确反应到游戏里」，而溅射范围 / 追踪距离这类
参数在画面上根本看不出来。判据定成：**客户端进程里那条 596 字节记录的每一格字节**。

- **为什么不加 hook 的全字段 dump**：那要改产品代码、重编 `bshook.dll`、还要重新发版；
  更要命的是它验的是「hook **自己**认为写了什么」，**是自证**。外部读的是内存**此刻**的样子，
  连「有人在 hook 之后把值冲回去了」（§44 那一类）都能抓到 —— 那正是最需要抓的一类。
- **为什么不能只看日志**：`bshook` 的「回读」只有**第一条记录的 2 格**（`Damage`/`Magazine`），
  另外 12 格一个字没说，而且日志带去重闩，数行数会误判（§5.3 / §53）。
- **为什么不用 `fakeclient`**：它没有那张武器表 —— 要验的最后一跳只存在于真客户端里。
- **期望值分三层**，且填了的那一层**不经过 `effective()`**（否则成了自己验自己）：
  填了 = 设定值本身；留空 = 参考值；参考值也没有 = T0 基线（mask=0 时 hook 写回缓存的原值）。
  T0 基线自己再过一道独立校验：凡是 `weapon.ini` 写了的键，内存值必须等于离线抽出来的参考值。
- 取值**每格互不相同**，而且**同范围的字段之间也错开**（`damage`/`head`/`legs`/`splash` 都是
  0..500）—— 否则「爆头伤害被写进了身体伤害那个偏移」这类**字段之间写串**根本查不出来。

## D47 · e2e 放 `test/custom-weapon-e2e/`，**不进全量单测**（用户 2026-09-20 拍板）

用户要求「放 test 下，但别和单测混在一起，跑单测时不要跑它，只在需要时手动单独跑」。

- `run_tests.all_modules()` 是**现扫 `test/` 顶层的 `test_*.py`**（D19）—— 子目录既不以
  `test_` 开头也不是 `.py`，**天然扫不到**。⇒ 不用改 `run_tests.py`，也不用加排除名单。
  ⚠ 代价是这里的文件**永远别起 `test_` 开头的名字**，否则会被吸进全量、然后在没有游戏
  进程的机器上红成一片。
- **否掉放 `tools/`**：那里是给人手动跑的探针，和「测试」这件事的语义对不上；用户点名要 test 下。
- **否掉塞进全量**：它要真客户端、要独占鼠标键盘、一跑 20 分钟 —— 放进全量等于把
  「1.5 分钟跑完的单测」变成没法在 CI 上跑的东西。

## D48 · 新版本加的**武器**开服自动进物品库，隐藏武器顺带上架（X11，用户 2026-09-20）

**推翻 D35 的最后一条**（「老服务器的 `items.json` / `shop.json` 不自动写，入口是控制通道
`shop-backfill apply`」）。理由是那条路在云上根本走不通：**控制通道只绑 `127.0.0.1`
且服务端包里默认关着**，运营要补齐得先改配置重启一次；而不补的后果是 V0.4.1 新加的
36 把武器（18 自定义 + 18 原版隐藏）在老服务器上**永远不出现**——玩家看不见的东西不存在。

- **每次开服都跑，不设「跑过没有」的标记**：判据是 §56 那条 ——「物品库里没有 = 这台
  服务器从来没见过它」，本身就是状态，补完即消失 ⇒ 天然幂等，不需要标记文件也不需要
  事件（对比 `apply_first_run_upgrades` 那条靠 `cards.json` 从无到有的一次性升级）。
- **范围收到武器**（`shopdata` 的 `kind == "weapon"`），不是整份物品库照默认表补：
  D35「别顺手替用户改他别的配置」那一半仍然算数，用户这次点名要的也只是这 36 把。
- **货架只给「刚进物品库的那一批」补行**：货架有删除键（§56），进过库之后货架上是什么样
  全是运营的事 ⇒ **下架 / 删行不会自己回来**。这是整件事敢每次开服都跑的全部理由。
- **上不上架照 `default_shop()`，不在这里抄名单**：隐藏武器有系列 + 档次 + 角色限定 ⇒
  默认货架收它（`listed: true`）；自定义武器没有系列号 ⇒ 默认货架根本不收 ⇒ 自动不上架。
  正好等于用户要的「隐藏的自动上架、自定义的不上架」，而「该不该上架」只有一个出处。
- ⚠ **已知边角**：先装了没有这一版的 0.4.1、又在管理页「物品库」保存过一次（`fillItems`
  会把 36 件写进库）⇒ 之后再升级，货架那 18 行不会自动补。手动出口仍是 `shop-backfill apply`
  或管理页「商店货架 → 添加」。不为这个边角再养一份迁移记录。

## D49 · 精灵批的 `Lock()` 失败：**Release + 清槽 + 走 Begin 自己的失败出口**，不是「不填就走」（§57，2026-09-20）

§35 那处（`0x5bf6cd`）的收尾是「指针为空就跳过填充」，这里**不能照抄**：索引缓冲是
**懒创建**的，`[精灵+0x14]` 一旦非空，以后每次 `Begin` 都整段跳过 ⇒ 没填好就是
一整局拿没初始化的索引画三角形。两处的生命周期不一样，收尾就不能一样。

- **选定**：判到指针为空 → `Release` 刚建的缓冲 → `[精灵+0x14]=0` → 置
  `D3DERR_INVALIDCALL` → 跳 `0x611f3b`。理由不是「这样更保险」，而是这个收尾状态和
  **「CreateIndexBuffer 失败」那条路**（`0x611858 jl 0x611f3b`，槽同样是 0、begun
  标志同样没置）**逐字节相同** —— 是 D3DX 自己就会产生、调用方（`0x5bdff8 test eax,eax
  / jge`，画不了就返回 0）本来就在处理的状态。代价只是这一帧不画，下一帧自己重来。
- **否掉「只跳过填充」**：省 5 条指令，换一整局的花屏，还查不出来。
- **否掉「包一层 Lock 的 vtable，失败时返回一块 scratch」**：一次补齐所有站点很诱人，
  但 scratch 得猜大小、猜够不够（调用方写多少字节没有上界），猜错就是堆越界 ——
  拿一个确定的崩换一个不确定的崩。
- **否掉「改 `[esi+0x44]` 的池」**：那是 D3DX 自己定的，改了牵连 OnLostDevice 整套。
- 日志按**状态翻转**去重（`g_sprite_ib_failing` 0↔1），不按次数 —— 锁不上会每次重建
  都再来一遍，按次数记就是刷屏，按状态记才看得出「什么时候坏的、什么时候好的」（铁律 10）。

## D50 · ~~「析构里摸 `[0x72e29c]`」一律补调用方，不补取值器~~ —— **当天就被 D51 推翻**

原话是：取值器 `0x4045f9` 只有 20 个字节、塞不下判据，加判据得整函数搬进代码洞，
「风险面从 1 涨到 97」，所以只补调用方。

**推翻理由见 D51：「塞不下」这个前提是错的。** 保留这条是为了记住那个错误的前提 ——
数字节之前先想想**两条检查能不能合成一条**。

## D51 · 座位取值器**原地改 3 个字节**，一次盖住 97 个调用点（§58 / §60-C，2026-09-20）

用户当天追问「以后服务端停机就不会再崩了吗」。照 D50 只补一个调用方，
诚实答案是**不保证**：97 个调用点只有 3 处判空，这一族随时冒第四处。

重看那 20 个字节才发现 D50 的前提错了：原版用 **`jl`(负数) + `jge`(≥6) 两条**
查下标，而**无符号 `jae` 一条就够**（负数当无符号看必然 ≥ 6）——
省下的两字节正好是 `test ecx,ecx`。⇒ **等长原地改 3 个字节**，不用代码洞、
不用跳板；`0x404d42` 同一招再来一遍。

- **等价性不是眼看的**：`test/test_seatgetter_exec.py` 把改前改后的机器码都
  `VirtualAlloc` 出来**真跑**，全输入矩阵逐个比（只能在 32 位那套运行时上跑）。
- 三个字节**分三次写、顺序挑过**（test → jae → jz）：不是原子写，每个中间态
  都不能比原版弱（用户态指针恒 < 0x80000000 ⇒ 先改成 `test ecx` 时那条 `jl`
  只是永不成立，上界仍由 `jge` 守着）。
- 三个二级取值器（`0x40460e` / `0x40462c` / `0x404d9e`）都是「先调它、`al` 为假
  走空分支」⇒ **自动一起被保护**。
- §58 那处调用方补丁（`0x46fe01`）**留着**：取值器那条是静音的，而它会打一行
  「加载画面析构时 LobbyStage 已经没了」—— 出事时要看得见。

## D52 · 日志时间戳一律带时区后缀，**现算不写死**（§59，用户 2026-09-20 要求）

bug调查/25 里拿开发机（UTC+9）的 `buildId` 去比玩家机器（UTC+8）的崩溃时刻，
把因果关系比反了。⇒ 凡是**给人看的**时刻，后面跟 `UTC+8` 这样的后缀。

- **一处算、处处用**：`server/tzstamp.py`（Python）/ `bslog_zone()`（注入 DLL）/
  `utc_offset_text()`（更新器）/ `Get-UtcOffsetText`（打包脚本），四份同一套写法。
- **现算，不缓存、不写死**：玩家可能在任何时区；夏令时会让同一台机器的偏移变，
  而客户端一开十几个小时是常事（那台崩的跑了 17.7 小时）。C 侧拿
  `GetLocalTime` 减 `GetSystemTime` 现减，连注册表都不读。
- **半小时时区要写得出来**（印度 `UTC+5:30`、尼泊尔 `UTC+5:45`）—— 只印小时会把
  它们抹平。`test_tzstamp.py` 拿假时区把这几档全钉死，因为开发机（UTC+9、整点、
  无夏令时）一辈子跑不到。
- **不加的两处**：① 文件名里的时间戳（加了会弄脏文件名、打乱既有排序）；
  ② `BUILD.ver` 的 `buildId`（它是客户端包 / 服务端包**逐字比对**的键，D079）——
  改成在同一份 `BUILD.ver` 里多写一个 `timeZone` 字段说明它是哪个时区的。
- **存档里的时刻不动**（`accounts.json` 的礼物 `sent`）：那是玩家数据不是日志，
  而且 `shop.build_systemtime()` 按固定格式在解析它（铁律 11）。

## D53 · 挂机判定补**客户端证据**，不去削弱服务端那条（§61 / §62，用户 2026-09-20）

单人局服务端看不见键盘，唯一剩下的「得分」被连点器刷满（§61）。两条路：

1. ~~把「加分」从单人局的证据里降级~~ —— 真日志重放能抓到（他 15% / 真人 1%），
   但那是**削弱**证据，抓的是「这个号恰好不捡东西」，换个脚本就绕过去了；
2. ★ **选定**：`bshook` 把服务端**结构上看不见**的四件事报过来（§62）——
   非方向键的键盘 / 鼠标键 / `GetLastInputInfo()` / 游戏在不在前台。

选 2 的理由：**判据同源**（键盘那条和调好六轮的 `INPUT_PEER_OPCODES` 同一哲学
⇒ 那条 20 秒的线原样复用，不用重新标定）；**前台是行为证据不是数值证据**，
脚本绕不过去，而且原版自己就认它；**`GetLastInputInfo` 是服务端永远拿不到的
一维** —— 它把「人在但游戏丢后台」和「人根本不在」分开。

实现上的三条取舍：

- **走已有的位置数据 UDP 旁路**（多一个 `kind`），不碰那条加密 TCP ——
  有状态的流密码，插包会把密钥流冲掉。老服务端不认识新 kind 会安静丢掉
  ⇒ 线格式版本号不用动。
- **客户端只报事实，判定全在服务端**（中继也原样转发、一个字节不解）：
  阈值是用户拿真日志调的，改它不该要求重发客户端或中继。
- ★★ **这一版只记不判**：`conn_is_afk()` 一个字没改，先拿线上一天的真数据
  看分布，再照 §111 的老规矩定线。`test_presence.py` 守着这一条。

