# V0.3 后半 FINDINGS —— 已查明的硬事实

**编号从 §1 起**，和 V0.1 / V0.2 / V0.3bot 相互独立。引用旧版一律写全「V0.1 §99」。

**写法**：一条一小节，**先结论后证据**。不写调查过程、不贴大段反汇编 ——
贴地址 + 那一句指令就够。**失败的尝试也要记**（省下别人重踩的时间）。

可信度标记：✅实测 / 🔍静态（反汇编看出来的）/ 🤔推测 / ❓未知。**别把推测写成事实。**

---

## §1 ★★★★★ 装备加成是**纯客户端算的**，服务端只需下发 itemId 列表

**结论**：服务端**不需要也没法**下发加成数值或公式。它只要通过**已经在跑的
`0x030b gspSlotEquippedList`** 告诉客户端「这个座位装备了哪些 itemId」，
客户端自己查本地 `Data/EquipBonus.ini` 就生效。
★ **2026-09-05 实机落锤，这一节已经是 ✅实测**（原为 🔍静态）—— 见 §16。

```
启动    0x40d68d push L"Data/EquipBonus.ini"(0x65f604) → 0x413ec9 从 pak 读
        → 0x413f60 解析成 INI 对象 [0x72e454] → 每个 section 建 EquipBonusEntry → map [0x72e440]
运行时  0x030b(座位, 3×掩码, itemId[]) → 处理器 0x406ea1 → EquipmentEx ctor 0x414d95
        → [GameSession + 0x250 + seat*4]
战斗    0x4070aa 读 [edi + esi*4 + 0x250] → 0x407014 GetEquipBonus(seat, index, key)
        → 0x4806bf 改伤害 / 0x50a09e 改 MaxHp / 0x5074f4 改移速
```

**关键佐证**：`0x406ea1`（`0x030b` 处理器）写的正是 `[GameSession + 0x250 + seat*4]`，
和 `0x4070aa`（所有战斗加成的读取点）读的是同一格。

**分桶规则**（`EquipmentEx::Equip` `0x414e43`）：`key = itemId/1000000 - 1`，
`itemId < 1e6` → `key = -1`（通用）。取值时 `0x41543e` 把**桶 -1 + 桶 key** 相加。
和 V0.1 §119 的角色持有判定是同一套规则。

**⚠ 三条限制**：

1. `EquipBonus.ini` 在客户端 pak 里（`0x413ef5` `PackHasFile` → `0x413f51` `PackReadFile`），
   **改数值要重打包 + 走 updater**，不能热下发。
2. `SetEquipBonusFunction` 在整个 exe 里**只有一个 C++ 调用点**（`0x41324d`），
   来自 INI 解析器，**没有任何网络包路径** ⇒ 服务端永远不能在运行时下发新公式。
3. 数值路径用 `_wtoi`（`0x413268`）⇒ **`Defense=5.0` 解析成 5，`Hp=0.5` 解析成 0**。
   服务端侧提取时也要用 `int()`，别用 `float()`。

**❓ 未验**：散文件能否覆盖 pak 里的 `Data/EquipBonus.ini`（`0x55e7f0`/`0x55e890` 没跟进去）。
真要改数值时先实测这条，能覆盖就省掉重打包。

---

## §2 ★★★★ `EquipBonus` 的 index 映射与各属性算法（**攻防只有 15% 概率触发**）

★ 「Attack 的 15% 概率门」和「`dmg = dmg*(100+x)/100` 朝零截断」**已实机落锤**，见 §16 ③。

**index 映射定死**（静态初始化器 `0x413096` 建的名字表 `0x732bfc..0x732c30`，
解析循环 `edi` 从 1 起）：🔍静态，与 14 个 `GetEquipBonus` 调用点的常数完全对上

| idx | 属性 | idx | 属性 | idx | 属性 |
|---|---|---|---|---|---|
| 1 | Attack | 6 | Sp | 11 | IncSplashRange |
| 2 | Defense | 7 | TeamDmg | 12 | DashAttack |
| 3 | Critical | 8 | SelfDmg | 13 | TeamReflection |
| 4 | MoveSpd | 9 | AntiGhostCnt | 0 | *(空串，保留未用)* |
| 5 | Hp | 10 | HeartBoost | | |

**算法各不相同**（常数：`0x693720=1.0f`、`0x693724=0.01f`、`0x6938cc=0.15f`）：

| 属性 | 读取点 | 算法 |
|---|---|---|
| **Attack** | `0x4807a5` | `dmg = dmg*(100+x)/100`，★ **前面有 `fcomp [0x6938cc]` ⇒ 只有 15% 概率生效** |
| **Defense** | `0x480906` | `dmg = dmg*(100−x)/100`，★ **同一个 15% 概率门** |
| **Hp** | `0x50a09e` | `MaxHp = 基础 + x`，**加法，100% 生效** |
| **Sp** | `0x50a0de` | `MaxSp = 基础 + x`，加法 |
| **MoveSpd** | `0x5074f4` | `speed *= (100+x)/100`，百分比，100% 生效 |
| **DashAttack** | `0x481e40` | `dmg *= 1 + x/100`，**没有 15% 门** |

**⚠ 「换弹速度」根本不在这 14 格里。** `ReloadTime` 只从武器 INI 读
（`0x489381`，默认 `0xc8`=200），**不经过任何装备加成** ⇒ 想让换弹变快
**只能换武器**（极速 R 系列 reload 700/680/660 vs 基础 720）。🔍静态

UI 提示串（`0x65fdac`~`0x65fee8`）自证这套算法：
`Attack %+d%%` / `Defense %+d%%` / `Speed %+d%%` / `HP %+d` / `SP %+d`。

**❓ 未查**：index 3 (Critical) 和 13 (TeamReflection) 在 `0x407014` 的调用点里
一次都不出现；`0x4070aa` 还有 7 个直接调用点没逐个查。

---

## §3 ★★★★★ 结算界面「合成材料」栏 = **`0x041c gspRewardReceived`**（纠正 V0.1 §99）

**结论**：那一栏有**专用包**，不是从 `0x0309` 里出来的。
**V0.1 §99 猜的 `0x48e5a8` 是错的** —— 那段在画角色立绘（`[ebp-0x20]` 比的
`0x66/0x68/0x69/0x6b/0x6c/0x6e` 是商城角色 id 102/104/105/107/108/110）。🔍静态

```
标签    0x48e7cd push L"ITEM_클리어리절트"(0x670f14) → 0x558916 翻译 → 画在 (161,465)
        （Chinese.ini：ITEM_클리어리절트=合成材料）
数据    0x48c9be(seat) → [GameContext + 0x74  + seat*20]   ← ★ 合成材料栏
        0x48c9dd(seat) → [GameContext + 0xec  + seat*20]   ← ★ 称号卡片栏
绘制    0x48b7a1 遍历（元素是指针数组，[+4]=itemId，[+8]=数量）
        itemId → [0x72e1dc] ItemDB(0x415a94) → +0x14 图标路径 → 0x5c5e14 载入贴图
写入    0x493ed1 ← 0x493808 的 opcode 链
```

**opcode 链**（`0x493808`，逐条算过）：`sub 0x406 / dec / sub 0xb / dec / dec / sub 8`
⇒ `0x0406`、`0x0407`、`0x0412`、`0x0414`、**`0x041c` → `call 0x493ed1`**。

**线格式**（Deserialize `0x54c5d0`，四发 `0x5d59ff`「读 4 字节」，无 bool
⇒ 线偏移 = 结构偏移 − 4）—— **body 16 字节**：

| 线偏移 | 类型 | 含义 |
|---|---|---|
| 0 | int32 | 座位号 |
| 4 | int32 | **槽类型**：`0` = 合成材料栏，`1` = 称号卡片栏 |
| 8 | int32 | 物品 id |
| 12 | int32 | 数量 |

**两条硬约束**：

1. `0x493f24` 是 `add [entry], count` ⇒ **同 itemId 发多次会累加**。
   服务端每种材料发一发即可，不用自己合并。
2. ★ **必须在 `0x0411 gspEndGame` 之前发** —— 它写的是 `GameContext`，
   关卡一结束就变 0（和 `0x0309` 同一个约束，V0.1 §99）。
   ⇒ 顺序：**`0x041c` × N → `0x0309` → `0x0411`**。
3. ★ **itemId 必须在 `ShopItem-Chn.ini` 里真有 `[Item-*]` 节**，否则 ItemDB
   查不到、图标画不出来。**已核对：全部材料都有**（见 §8）。

---

## §4 ★★★★ 商店 / 合成 / 仓库的协议全表

> ⚠ **本节已被 §20 ~ §23 取代**（2026-09-05）。下面这张表里
> 「`0x0605` 是 `u8+u16+i32+i32`」和「货架目录走哪个 opcode 还没落锤」
> **两条都作废了**，别再引用。留着是因为分发器地址和子结构那几行仍然对。

分发器 **`ShopStage` vft `0x666164` 槽 `+0xc4`**，跳表 `@0x44434a`，
覆盖 `0x0500..0x050c` + `0x0604`。收发都在主连接 `[0x72e30c]` 上。

**★ 进商店时客户端连发四个请求**（ShopStage ctor 里 `0x444009`~`0x44402c` 顺序调用）：

| 顺序 | 发送函数 | opcode | 期望应答 |
|---|---|---|---|
| 1 | `0x44722b` | **`0x0704`（无正文）** | `0x0604 gspRepEquippedList`（处理器 `0x447278`） |
| 2 | `0x447467` | **`0x0605` gcpReqCompositionList**（`u8+i32+u16+i32`，§20） | `0x0505`（处理器 `0x4474e4`） |
| 3 | `0x44792d` | **`0x0607` gcpReqGiftList** | ❓（本版回空清单） |
| 4 | `0x5541c1` | **`0x0700`（无正文）** | ❓（V0.1 §50 记过大厅也在轮询它） |

**服务端 → 客户端**：

| opcode | 类 / vft | 线格式 | 处理器 |
|---|---|---|---|
| `0x0500` | `gspRepShopItemList` `0x666290` | `i32, i32, i32 n, n×Item@ShopStock{i32,i32,i32}`（步长 0x10） | `0x445ed9` |
| `0x0502` | `gspRepItemBuy` `0x666158` | `i32 ok, i32, i32` | `0x44615b` |
| `0x0505` | `gspRepCompositionList` `0x6662a8` | `i32, i32, i32 n, n×CompositionRule` | `0x4474e4` |
| `0x0506` | `gspRepComposeItem` `0x66623c` | 单 `i32` | `0x4476c4` |
| `0x0604` | `gspRepEquippedList` `0x666248` | 见下 | `0x447278` |
| `0x0503` / `0x0504` | ❓ | ❓ | `0x446700` / `0x446b62` |
| `0x0508`~`0x050c` | ❓（礼物 / 修理 / 丢弃一类） | ❓ | `0x44798d` `0x447c08` `0x448072` `0x4468bc` `0x448849` |
| `0x0501` / `0x0507` | 跳表指向 default ⇒ **客户端不处理** | — | — |

**客户端 → 服务端**（都是「无 RTTI 类」，用裸序列化函数发）：

| opcode | 线格式 | 发送点 | 语义 |
|---|---|---|---|
| `0x0602` | `i32 n, n×i32` | `0x446115` | **购买**（回应 `0x0502`）🔍 |
| `0x0603` | `i32` | `0x4466d2` | ❓（大概率回 `0x0503`）🤔 |
| `0x0604` | `i32, i32` | `0x446f5b`（`ret 8`） | **装备 / 卸下** 🤔 |
| `0x0606` | `i32` | `0x44765c` | 合成（回应 `0x0506`）🔍 |

**子结构**：`CompositionMaterial`（`0x666104`，Des `0x4438dc`）= `i32 itemId + u16 count`；
`CompositionRule`（`0x666254`，Des `0x44394d`）= `i32 + i32 + i32 n + n×Material + i32 + i32`。

~~**❓ 最大的残余不确定：货架目录到底走哪个 opcode。**~~
**已解决**：货架目录就是 `0x0500`，请求是 `0x0600`（§20 / §21）。
上表那句「`0x0500` 的元素只有 3 个 int」是把中间层 `ShopStockGroup` 当成叶子了。

**`0x0604` 处理器的一个细节**：它先建空 `EquipmentEx`（`0x414d27`），
对每个 itemId 查物品库 `[0x72e1dc]`（`0x415a94`），**只有 `flags & 8`（可装备）
的才 `Equip`**（`0x44736a: test byte ptr [eax+0x10], 8`）。

**★ `0x0604` 和 `0x030b` 是两条独立的路**：`0x0604` 落到 `[ShopStage+0x134/0x138]`
（只影响商店界面），`0x030b` 落到 `[GameSession+0x250+seat*4]`（战斗加成的唯一来源）。
⇒ **让加成生效必须发 `0x030b`**，发 `0x0604` 不够。

---

## §5 原版数据表：位置、编码、字段

全在 `D:\git\popshot-reborn\main\Pack_decrypt\Data\`（**只读**）。

| 文件 | 编码 | 内容 |
|---|---|---|
| `ShopItem.ini` / `-Chn` / `-Jpn` | **UTF-16LE** | 只有 3 个键：`Image` / `Tag` / `PartFlag`。★ **没有价格 / 名字 / 等级 / 期限** |
| `EquipBonus.ini` / `-Chn` / `-Jpn` | **UTF-16LE** | `[itemId] 属性=值`。KR 633 / CN 567 条 |
| `Enchant.ini` | UTF-16LE | 只有 8 行 4 条（附魔石）。★ **客户端 exe 里两种编码都搜不到这个文件名 ⇒ 纯服务端概念** |
| `weapon.ini` | **CP949** | 229 个小节。已提取成 `server/bot_weapons.json` |
| `Promotion.ini` / `-chn` | CP949 / UTF-16LE | 107 条任务定义 + 奖励 |
| `Item.ini` | UTF-16LE | **局内拾取道具**（id `10300~10604`），和商城 id 空间不重叠 |
| `Chinese.ini` | UTF-16LE | 单节 `[Chinese]`，1391 行「韩文原句=中文」。★ **没有物品名** |
| `Ui/*.ui` | UTF-16LE 的 XML | 界面定义 |

★ 中文版的切表是靠 `Chinese.ini` 里的路径替换做的：
`Data/ShopItem.ini=Data/ShopItem-Chn.ini`、`Data/EquipBonus.ini=Data/EquipBonus-Chn.ini`。

★ **两种货币的中文译名是反直觉的**：`픽셀`(Pixel，打游戏赚的) → **「金币」**；
`캐시`(Nexon Cash，充值的) → **「游戏币」**。别搞反。

★ **`NewMutu.ini` 跟合成无关** —— 「무투」是近身格斗模式，160 个小节全是格斗招式。

---

## §6 ★★★ 商店 id 编码 与「爆裂 / 极速 / 复合」三系列

**7 位 id = 角色(1) + 部位(2) + 序号(4)**。角色首位 `1`=泰尔 `2`=卡希尔 `3`=布洛克。

**部位 ↔ `PartFlag`（位掩码，共 16 位）**：

| 部位 | Flag | 部位 | Flag | 部位 | Flag |
|---|---|---|---|---|---|
| `01` 上衣 | 1 | `06` 突击技 | 32 | `12` 武器槽1 | 1024 |
| `02` 下装 | 2 | `07` 喷漆 | 64 | `12` 武器槽2 | 2048 |
| `05` 头/脸 | 4 | `11` 头饰 | 128 | `12` 武器槽3 | 4096 |
| `04` 鞋 | 8 | `08` 尾饰 | 256 | 称号 | 8192 |
| `03` 手套 | 16 | `09` 翅膀 | 512 | `13` 戒指 | 16384 |
| | | | | 宠物 | 32768 |

套装用**组合值**（实测出现过 9 / 11 / 19 / 23 / 31 / 539）。
`5x` 段（部位码 + 50）是**同一件外观的期限制售卖形态**，只有 `[Stock-]` 没有 `[Item-]`。
9 位 id（`101400001` 等）是**角色本体商品**，`account_store.character_item_id()` 已实现。

**★ 武器 id 规律**：`C 12 0 SS T` —— `SS`=`01~03`→**D 爆裂**（槽 1/2/3），
`04~06`→**R 极速**，`07~09`→**F 复合**；`T`=1/2/3 级。
`[Item-1120011] Tag=1000015.0` 里的 **`Tag` 就是 `weapon.ini` 的弹药 `Id`**。

**三系列的数值特征**（左轮 `Damage/ReloadTime`，基础 = `4/720`）：

| 系列 | 特征 | 1 级 | 2 级 | 3 级 |
|---|---|---|---|---|
| **D 爆裂** | 伤害↑、装填↑（更慢） | 4/720 | 5/740 | 6/760 |
| **R 极速** | 装填↓（更快），伤害不动 | 4/700 | 4/680 | 4/660 |
| **F 复合** | 两边各给一点，居中 | 4/710 | 4/710(头伤+1) | 5/710 |

D/R/F 只存在于 **ch00/ch01/ch02** 三个初期角色，每人 3 槽 × 3 系 × 3 级 = 27，共 81 条。

**⚠ 中文版砍掉了 18 件**：手枪 / 狙击的全部 3 级（D3/R3/F3），
只有榴弹系列（사과탄 / 화염탄 / 스크류런처）的 3 级留着。⇒ **上架前要按
`ShopItem-Chn.ini` 过滤**，中文版没有的 id 发下去客户端查不到图标。

**⚠ `weapon.ini` 里没有 SP 消耗字段** —— SP 消耗在 `GameProps.ini`
（`AssultSpCost=40` 等）和 `ChrProps.ini`（`Dash00-SpCost`）里。

**⚠ 部位 12（武器）在 `EquipBonus.ini` 里一条都没有** —— 武器不走装备加成，
强弱直接写在 `weapon.ini`。

---

## §7 ★★★ 合成配方在客户端里**彻底不存在**（找过的地方全列在这）

**结论**：原版配方在已停服的服务端 DB 里，随游戏一起没了。**必须自己设计。**

**找过而没有的**（别再找第二遍）：

| 找过 | 结果 |
|---|---|
| `NewMutu.ini` / `NewMutuConfig.ini` | 是**近身格斗招式**，不是合成 |
| `Enchant.ini` | 是**附魔**（4 条月亮石），不是合成 |
| `Data/` 全目录内容级 grep（`30018` / `Composition` / `Compose`，含 UTF-16 形式） | 只命中 `EquipBonus`（子串误命中）/ `Promotion` / `ShopItem` / 文件名 `CompositionToolTipNewUI.ui` |
| `Quest/` 全部文件 | 只有关卡脚本和台词，**没有任何奖励/掉落定义** |
| `Chinese.ini` 全量 | 只有 UI 词条，**一个物品名都没有** |
| 新浪 2007 攻略页 | 只有材料名，**没有配方** |

**能反推出的配方形状**（`Ui/ComposeItemNewUI.ui` + `CompositionToolTipNewUI.ui`）：

- 表头：`조합재료`(合成材料) / `요구량`(需求量) / `소지량`(持有量) / `상태`(状态)
- ★ **`ImgBar0~3` + `Rectangle0~3` = 恰好 4 个材料槽 ⇒ 一条配方最多 4 种材料**
- tooltip 有 `조합비용 : 픽셀`(合成费用：金币) / `ReqLevelTxt` / `ReqCharTxt` / `UseTimeTxt`
- ⇒ `CompositionRule` ≈ `{产物 itemId, 等级要求, 角色限制, 花费, 使用期限, ≤4×{materialId, count}}`

**★ 没有成功率**：UI 上没有任何概率控件；`Chinese.ini` 的失败文案全是前置校验
（`재료가 부족합니다`材料不足 / `픽셀이 부족합니다`金币不足 / `이미 소지하고 있습니다`已拥有）。
**不要自己发明成功率**（铁律 12）。

**产物候选**：`Images/Shop/` 里有 **77 张图没被任何 ShopItem.ini 引用** ——
`태양의 반지`(太阳戒指，对应材料 `태양의 파편`太阳碎片)、
`풍요의 황금반지`(丰饶黄金戒指)、`타이/카실/프로코_마스터리아머 셋트`(大师铠甲套装)、
`다크엔젤` / `블랙샤크` 等。配合已上架的 `1010066 타이_메이져피닉스아머`
（`Attack=11 Sp=2`）和材料 `불사조의 깃털/눈물`(不死鸟之羽/泪)，
**「不死鸟系材料 → 凤凰铠甲」「太阳碎片 → 太阳戒指」就是原版的合成线**。🤔推测

**★ 称号也是合成出来的且带期限**：`Chinese.ini` 有
`조합에 성공하셨습니다.\n(사용기간 : %d일)\n내 창고에서 타이틀을 장착하세요.`。
素材 `60002 파이터` ↔ 称号 `560002`、`110001 리볼버고수` ↔ 称号 `610001`，一一对得上。
（本版不做称号。）

---

## §8 材料 id 清单 —— **全部都有 `[Item-*]` 节，图标查得到**（✅已核对）

`ShopItem-Chn.ini` 里材料的形态：`PartFlag=0`、`Tag` 空、只有 `Image`，
且**只有 `[Item-]` 没有 `[Stock-]`**（从不出售）。✅实测

```
[Item-10001] Image=Images/Shop/검은구슬.png     黑珠      [Item-10002] 붉은구슬 红珠
[Item-10003] Image=Images/Shop/초록구슬.png     绿珠      [Item-10004] 푸른구슬 蓝珠
[Item-20007] 철광석 铁矿石                       [Item-30005] 파이프 水管
[Item-30006] 화염조각 火焰碎片                    [Item-30013] 제작서17 制作书17
[Item-30016] 불사조의 깃털 不死鸟之羽              [Item-30017] 불사조의 눈물 不死鸟之泪
[Item-30018] 청동파이프 青铜管                    [Item-30019] 부유석 浮游石
[Item-40008] Z칩 Z芯片                          [Item-40009] 용의 눈물 龙之泪
[Item-50015] 태양의 파편 太阳碎片                 [Item-60002] 파이터 (称号素材)
```

完整段位：`1000x` 珠子 4 种、`20007` 铁矿石、`30005~30019` 杂项、
`40008~50015`（`4xxxx` 和 `5xxxx` **两套完全同名同图**）、
`6000x` + `11-13000x` 称号素材。

★ `Chinese.ini` 里唯一一条能佐证中文名的整句：
`<초록구슬> 아이템을 2개 획득하셨습니다...= 获得2颗<绿色小珠>...`
⇒ 珠子的官方中文名是「**绿色小珠**」这一类。

★ **中文版砍掉了 `30007 수상한가루`（可疑粉末）和 `60009`**，其余材料 CN 全在。

★ `210001~210005` 是**商城消耗品**（라이프命 / 캐릭체인저换角色 / 스패너扳手 /
확성기喇叭 / 전적초기화战绩重置），不在 `Item.ini` 里。和 V0.2 §193 的
「`gspUsableItemList` 只认 `210001`/`210002`」吻合 —— 那个包发的是**局内可用**的商城消耗品。

---

## §9 任务奖励基线在 `Promotion.ini`，**不在 `Quest/`**

字段：`Reward0/1/2 = <类型>,<参数…>`

| 类型 | 含义 |
|---|---|
| `0` | 金币（픽셀）：`0,100` |
| `1` | 物品：`1,<itemId>,<天数>`（`0` = 永久） |
| `2` | 经验值：`2,20` |
| `3` | 物品（韩版形态，第三格恒 100） |

**★ 原版只有 4 关给材料**（三个角色线各一份，共 12 条）：

```ini
[타이_퀘스트_레벨15]  UserLevelLimit=12  Reward0=0,200   Reward1=1,0030018,0   ← 青铜管
[타이_퀘스트_레벨19]  lv=13              Reward0=2,120   Reward1=1,0030018,0
[타이_퀘스트_레벨24]  lv=15              Reward0=0,500   Reward1=1,0030019,0   ← 浮游石
[타이_퀘스트_레벨26]  lv=15              Reward0=0,500   Reward1=1,0030018,0
```

**★ 中文版把 17 条装备奖励从「永久」改成「30 天期限」**（`3,<id>,100` → `1,<id>,30`），
但**材料奖励和戒指原样保留**。

**★ 珠子类材料原版主要来自对战模式**（新浪 2007 攻略页：「可在对战模式的某模式中获得」；
`Chinese.ini` 也有「队友完成任务你可以获得合成素材」的提示）。
`Promotion.ini` 里 18 条 PVP 任务带 `PromotionItemID` + `PromotionItemProb=100`。

---

## §10 服务端现有接入点（照抄这些，别另起炉灶）

| 要做的事 | 照抄谁 | 位置 |
|---|---|---|
| 存档加字段 + 老号自动补齐 | `NEW_ACCOUNT_DEFAULTS` + `_merged_account()` | `account_store.py:27-53, 350-359` |
| 启动时一次性**幂等**修正 | `realign_levels()` + `app.py` 的调用 | `account_store.py:304-348`、`app.py:288` |
| 原子写 + 锁 | `_write_unlocked()`（tmp + fsync + os.replace） | `account_store.py:279-294` |
| **json 改了不重启即生效** | `load_client_filter()` 的 mtime+size 缓存（带 `_reload=True` 测试参数） | `versioning.py:56-58, 173-206` |
| 组包纯函数 | `build_slot_equipped_list()` | `gameserver.py:1408-1458` |
| 下发 `0x030b` | `send_slot_equipped_list()` | `gameserver.py:6225-6247`；触发点 `7916`/`9159`/`9015` |
| 结算发奖 | `add_quest_reward()` 调用处 | `gameserver.py:7758-7787` |
| 掉落规则纯函数的形状 | `quest_reward()` / `pvp_reward()` | `gameserver.py:1991-2032` |
| 买完刷新金币 | `send_rep_money()`（`0x0600`） | `gameserver.py:6555-6567` |
| opcode 分发 | `Conn.on_game_packet` 的 if 链（**结尾在 9356，没有 else**） | `gameserver.py:9075-9356` |
| Web POST 路由 | `do_POST` 的路由 dict（★ `_read_body()` 必须在路由之前） | `web/server.py:324-339` |
| Web 失败提示 | `raise AccountError(...)` → 自动变友好提示 | `web/server.py:350-352` |
| 登录限速 | `RegisterRateLimiter` | `web/server.py:149-198` |
| 调试命令 | `handle_control_command` 的 if 链 + `CONTROL_HELP` | `gameserver.py:9639-9914, 9532-9605` |
| 离线提取器 ↔ 运行时读取器成对 | `tools/weapondata.py` ↔ `server/weapondata.py` | — |

**★ 登录后主动下发的顺序**（`gameserver.py:6503-6514`）：
`0x0100 gspRepLogin` → `0x0102 SetPlayerName` → `0x0600 gspRepMoney` → `0x020c QuestReachedDifficulty`。
`0x030b` **不在登录时发**，它是房间级的（要 `0x0300` 先把座位标成已占用）。

**★ 打包**：`.py` 自动进包（`build-common.ps1:248-278`），
**新增 `.json` 不会自动进包**（要照 `Copy-WeaponData` 写一个），
web 文件是硬编码白名单（`build-common.ps1:304`），
`server/data/` **打包只建空目录**（`build-common.ps1:315-317`）。

**★ `.gitignore` 是逐文件的**（`/server/data/accounts.json` 这样写的），
不是整个目录 ⇒ 新增的 `server/data/*.json` 不补规则就会被 git 追踪。

**★ `server/data/tickets.json` 不存在** —— 票据只在内存里（V0.2 D097）。

---

## §11 有两批物品「只有货架、进不了背包」—— 判据是 `ownable`，不是 `timed`

`ShopItem-Chn.ini` 里 `[Stock-x]` 和 `[Item-x]` 是**两种节**：前者 = 出现在货架上，
后者 = 能进背包的持有物条目。**只有 `[Item-]` 节才有 `Tag` 和 `PartFlag`。**

只有 `[Stock-]` 的两批（共 226 条，实测）：

| 形态 | id 规律 | 例 |
|---|---|---|
| 部位码 +50 的期限版 | `51..63` 段 | `1510001` = `1010001` 的期限售卖形态 |
| 榴弹类的「售卖变体」 | 尾四位 `SSTV`（基础 3 位 + 变体位） | `1120220/221/222` = `1120022` 사과탄 D2 的三个变体 |

⇒ **要发给玩家的 id 一律先过 `shopdata.ownable()`**。发一个只有货架条目的 id
下去，客户端在自己的表里查不到持有物定义 —— 界面上是空格子。

★ 这也是 `weapon_variant()` 必须用**尾四位**判 D/R/F 的原因：只看尾两位的话，
SE（`…0001~0004`）、Platinum（`…0101`）、和这批 `SSTV` 变体都会被误判成 D 系。

**中文版实际能进背包的 D/R/F 武器 = 63 件**（3 角色 × 3 系列 × 7 件；
韩版 81，砍掉的 18 件是手枪 / 狙击的 3 级）。M5 的商店就卖这一批。

---

## §12 `Promotion.ini` 的 `UserLevelLimit` 里 **1000 是标记位，不是等级**

取值有 `1`~`11` 和 `1011`~`1016` 两段。实测（106 条全查过）：

- `>= 1000` 的 **51 条全部**是 `PromotionType=4` 且**全部**带 `PromotionQuestStageID`
  （= 闯关模式的成就）；`< 1000` 的 55 条里只有 12 条带 StageID。
- `UserLevelLimit % 1000` 和文案 `TextMissionInfo` 里的「等级N」
  **105 条全部吻合，0 例外**。

⇒ 真等级是 `% 1000`。`tools/shopdata.py` 已归一化成 `level`，原值留在 `level_raw`。

★ **给材料的 12 条全在这一段**（`PromotionType=4`），带的
`quest_stage` + `difficulty` **正好就是服务端 `Conn.current_quest()` 返回的那两个数**
—— M6 的 `drops.json` 可以直接按 `(quest_stage, difficulty)` 建表：

```
stage=7 diff=1 -> 30018 청동파이프    stage=1 diff=2 -> 30018
stage=4 diff=3 -> 30019 부유석        stage=1 diff=3 -> 30018
```
（三个角色线各一份，共 12 条；`CharacterLimit=0` 说明不限角色。）

---

## §13 ⚠ `tools/` 和 `server/` 有四对**同名不同物**的模块，`import` 会串

`mapdata` / `weapondata` / `chrprops` / `shopdata` 四个名字**两边各有一份**
（`tools/` 的是离线提取器，`server/` 的是运行时读取器）。

**踩过的坑**：`tools/shopdata.py` 里写 `sys.path.insert(0, HERE)` +
`import weapondata`，单独跑没问题，**跑全量测试就炸** ——
`server/` 先被导入过，`weapondata` 已经在 `sys.modules` 里，
后插的 `sys.path` 根本不起作用，拿到的是服务端那份，
当场 `ImportError: cannot import name 'read_ini'`。

⇒ **要用同伴模块就按路径加载、挂独立模块名**（`tools/shopdata.py` 的
`_load_sibling()` / `server/test_shopdata.py` 的 `load_tool()` 是样板）。
**永远不要把 `tools/` 塞进 `sys.path`。**

---

## §14 ⚠ **id 里的部位码不能当槽位判据**，只有 `PartFlag` 算数（补充 §6）

§6 那张「部位 ↔ Flag」表容易让人以为 `1_08_0039` 的 `08` 就意味着尾饰(256)。
**不成立。** 全表 1870 条逐条对过（`id[1:3]` → 实际 `PartFlag` 的分布）：✅实测

| 部位码 | 实际 `PartFlag` 分布 |
|---|---|
| `01` | 1×159，**套装组合值 539×3 / 31×8 / 23×4 / 19×1 / 11×1**，4×3 |
| `04` | 8×71，**2×12**（下装！），16×1 |
| `08` | 256×6，**8×12**（鞋！） |
| `03` | 16×77，8×1 |
| `12` | 4096×25 / 2048×30 / 1024×21，**0×54**（只有货架条目的那批，§11） |

⇒ 一律走 `shopdata.part_flag(id)`（它读的是 `ShopItem-Chn.ini` 里真的
`PartFlag=`）。**装备冲突只用 `conflicts()`**，别自己按 id 切片。
部位码只能当「人看的分类线索」。

★ 同一类错误的第三次了（前两次：§11 的 D/R/F 要看**尾四位**、
§12 的 `UserLevelLimit` 的 1000 是标记位）。**通则：原版 id 里的每一段
都是「大多数情况下成立」的约定，不是规则；能直接读到字段就别从 id 推。**

---

## §15 关卡只有 **7 个**，id ↔ 中文名（三个来源互相印证）✅静态

`drops.json` 的 `stage` 比的就是这个 id（`quest_materials` 拿它和房间描述符的
第一个参数比，`current_quest()`）。写进 `shopcfg.QUEST_ZH`，管理页的关卡下拉框用它。

| id | boss / 主题（`map.ini` → `Chinese.ini`） | 成就名（`Promotion-chn.ini`） | 地图 |
|---|---|---|---|
| 1 | 불프로그 → **机械青蛙** | [任务] 起火的村庄! | `Boss00` / `Boss01` |
| 2 | 드라카 → **岩浆巨龙** | [任务] 埃斯佩拉的大怪兽! | `Quest02_*` |
| 3 | 비밀의 섬 → **神秘岛** | [任务] 神秘岛攻略! | `Quest03_*` |
| 4 | 자미로건쉽 → **鲸鱼战舰** | [任务] 鲸鱼战舰击破! | `Quest04` |
| 5 | 다크나이트 → **黑骑士** | [任务] 加密尔地下废墟的秘密! | `Quest05_*` |
| 6 | 브레그마 → **太阳齿轮** | [任务] 突破扎米洛基地! | `Quest06_*` |
| 7 | 자미로 비밀기지 → **没有中文译名** | [任务] 寻找扎米洛的痕迹! | `Quest07_*` |

**「只有 7 个」的依据**：客户端建房时那个关卡下拉框只认静态表 `0x6dc52c`
里的 `(3, 2, 1, 4, 5, 6, 7)` —— V0.3bot 的 `tools/probe_quest_list.py`
早就逆出来了（记录 `+0x40` 是关卡 id，要等于表里的值才进下拉框）。
`Quest08` 的图在包里有，但**进不了那张表**，所以选不到。

**id ↔ 主题的两条独立佐证**（所以不用再去逆一遍）：
① `map.ini` 里几张 boss 图的 `RequiredQuestClear`（Draka=2 / Island=3 /
Airship=4 / Darknight=5 / Bregma=6）；② 同一份文件里那行注释
`1:개굴 2:용 3:섬 4:비행기 5:말 6:해`。

★ 第 7 关的「扎米洛秘密基地」是**我们自己拼的**（原版没给中文）：
「扎米洛」照第 6 关成就名里的官方译法，「秘密基地」照韩文主题名。

★ `Promotion-chn.ini` 那一列**已经在 `server/shop_items.json` 里**
（`promotions[].name` + `quest_stage`），不用再去解 ini。

---

## §16 ★★★★★ **装备真的在战斗里生效了** —— 两条互相独立的实证（✅实测）

**结论**：`0x030b` 那条路整条打通了。§1 / §2 从 🔍静态升级成 ✅实测。

### ① 内存：客户端算 `MaxHp` 要读的那张表里，就是我们发下去的加成

`tools/probe_equip_bonus.py`（本次新增）读跑着的客户端，
**在大厅里就读得到**（`0x030b` 一到桶就建好，不用真开一局）：

```text
座位0 角色id=0(泰尔) EquipmentEx=0x1fdfefa0
  桶 0 (泰尔): Attack=-1, Defense=3, Hp=29, Sp=2
  桶 100..110: 全 0            ← 商城角色 id（9 位）分到的桶，本来就没有加成
```

穿的是 `1010037/1020062/1030058/1040037/1050037` 五件铠甲 + 武器 `1120012`。
`EquipBonus-Chn.ini` 里这五件的 `Hp` 是 8+6+3+7+5 = **29**，一个不差。
⇒ `MaxHp = 100 + 29 = **129**`（`0x50a06b` 就是一次加法，没有概率门）。

★ 桶号 = `itemId/1000000 − 1`（`0x414e43` 的 `idiv 0xf4240`），取值时
`0x41543e` 把**桶 −1 + 桶 key** 相加，`key` = 座位的角色 id
（`0x404d9e` 读 `[LobbyStage + seat*0x3c + 0x4c]`）。
⇒ 商城角色 id 落在桶 100~110，**永远不会被求和**，不用管它。

### ② 线上：`rpFire` / `rpExplode` 直接看得见换枪和加成

2026-09-04 22:18 那一局（`logs/game_*.txt` 里 69 发）：

- **69 发 `rpFire` 的 `ammo` 全是 `1000115`** = `[ch00-01D2]` 리볼버 D2
  （基础左轮是 `1000010`）⇒ 换枪 100% 生效；
- `rpExplode` 的伤害：**60 发 5.0、9 发 4.0**（D2 的 `Damage=5`）。

### ③ ★ 「有时候 5 有时候 4」= `리핑체인 바지(1020062)` 的 `Attack=-1`

`0x4806bf` 里那一段（地址逐条对过）：

```text
004807f0  fcomp [0x6938cc]          ; 随机数 vs 0.15  ← 15% 概率门
004807fe  jp 跳过整段
00480800  test edi, edi ; jle       ; edi = GetEquipBonus(座位, 1=Attack, -1)
0048080d  mov [ebp-4], 0x10         ; ★ 正加成 -> flags |= 0x0010
00480825  jge 结束
00480827  or byte [ebp-3], 8        ; ★★ 负加成 -> flags |= 0x0800
0048082b  add edi, 0x64 ; fimul 伤害 ; fmul 0.01 ; ftol   ; dmg = dmg*(100+x)/100，朝零截断
```

`int(5 × 99 / 100) = 4`。而实测**那 9 发 4.0 的 `rpExplode`，flags 全带
`0x0800`**（`2048` 或 `2112`），60 发 5.0 的一个都不带。9/69 = **13%**，
和 0.15 那道门对得上。

⇒ **不是 bug，是原版机制**（铁律 12 / D8 早写过「攻防加成只有 15% 概率触发」）：
玩家穿的裤子攻击是 **−1%**，每一发有 15% 的概率把伤害压成 4。
把那条裤子脱了就永远是 5。

★ `rpExplode +20` 的 flags 现在认得四位（前两位是 V0.3bot §89 记的）：

| 位 | 含义 | 出处 |
|---|---|---|
| `0x0001` | 受害者 **Defense** 加成触发（同一道 15% 门）| `0x480938` |
| `0x0004` | 夺分模式「目标踩在地上」×0.75 | `0x47e700` |
| `0x0008` | 夺分模式「距离 > 1024」×0.75 | `0x47e6df` |
| `0x0010` | 射手 **Attack 正加成**触发 | `0x48080d` |
| `0x0800` | 射手 **Attack 负加成**触发 | `0x480827` |
| `0x0100` | 伤害被**清零**（`0x4809b0`，前面 `cmp eax, 0x88b84` = 称号 `560004`）| `0x4809b0` |

❓ `0x0040` 还没查（实测里它总跟着 `hit == 5` 一起出现，伤害不变）。

★ **用户报的「虚弱攻击」那个提示，在原版资源里找不到**：`Data/*.ini` 全量搜
（UTF-16 / GBK / UTF-8 三种编码）没有「虚弱」二字，`Images/` 里也没有对应的
文字贴图（只有 `BlockTextImg.png` = **免疫!**）。⇒ 名字大概率记岔了，
**现象本身就是上面这条 `0x0800`**。

---

## §17 ★★ 伤害只按**部位**分档，没有随机数（`weapon.ini`）

`Damage` / `HeadDamage` / `LegsDamage` 三档，`SplashDamage` + `SplashRange`
另算。**没有任何 `DamageRandom` 之类的字段** —— 同一部位同一把枪，伤害是定数。

| 武器 | Id | Damage | HeadDamage | ReloadTime |
|---|---|---|---|---|
| `[ch00-01]` 리볼버（基础）| 1000010 | 4 | 6 | 720 |
| `[ch00-01D2]` 리볼버 D2 | **1000115** | **5** | **7** | 740 |

⇒ 伤害会变的原因只有三个：**打中哪个部位**、**模式倍率**（夺分 / 模式 5 ×2）、
**装备加成的那两道 15% 门**（§16）。看见非预期的数先去查 `rpExplode` 的 flags。

★ `GameProps.ini` 的 `GuardDamageRate=0.25` 是**举盾**那条路，和上面无关。

---

## §18 难度号 ↔ 中文名（管理页的下拉框用它）

`1=简单 2=普通 3=困难 4=极限`。号码 → 难度是**客户端拼地图文件名**那一段定死的
（`mapdata.DIFFICULTY_SUFFIX`，出处 `0x405742` 的四发 `dec edi` →
`#Easy/#Normal/#Hard/#Extreme`）。

中文名取 `Chinese.ini` 的官方译法：`이지 모드=简单` / `노멀 모드=普通` /
`하드 모드=困难`（另一处 `EASY=简单` / `NORMAL=普通` / `HARD=困难` 互相印证）。

★ **难度 4 的中文名是我们自己拼的**：中文版把 `익스트림 모드` 原样留成了
`Extreme Mode`（没翻）。和第 7 关的名字（§15）是同一种情况。

写进 `shopcfg.DIFFICULTY_ZH`；`test_shopcfg` 卡死它和 `mapdata.DIFFICULTY_SUFFIX`
以及校验器上限（1..4）三处一致。

---

## §19 ★ 进商店那几发**实测对上了**（✅实测）

> ⚠ **本节第 2 条曾经写错，2026-09-05 已改正**（见 §20）。原文说实测把
> `0x0605` 的线格式「纠正」成 `u8 + u16 + i32 + i32` —— **那是错的**，
> 静态结论 `u8 + i32 + u16 + i32` 才对。留着这行是为了让引用过旧结论的人
> 知道要回头改；判据见 §20。

1. **顺序是 `0x0600 → 0x0704 → 0x0605 → 0x0607 → 0x0700`**，同一毫秒发完；
   **服务端一发都不回，客户端不崩也不断线**（M4 的止损线没踩到）。
   ★ 打头那发 `0x0600` 是 2026-09-05 从同一份日志里补认出来的（§20）。
2. ~~`0x0605` 的线格式是 `u8 + u16 + i32 + i32`~~ → **`u8 + i32 + u16 + i32`**，见 §20。
3. **点界面里的小标签会重发 `0x0605` / `0x0600`**（13 发里 11 发是点标签点出来的）
   ⇒ 它是**分类 + 分页请求**。哪个数对应哪个标签**已经对上了**，见 §22。

★ 顺带观测到 `0x0505 gcpAccumulatedWeaponDamage`（客户端→服务端，一局结束时）：
16 字节一条、末尾补零，键是 **`weapon.ini` 的 `ROH`**（`110001` 这种），
不是 itemId。本版用不上，记着免得下次重查。

---

## §20 ★★★★★ **货架目录请求是 `0x0600`**（同号反向！）—— M4 最大的未知落锤了

**结论**：客户端**要**货架目录，走的是 **`0x0600`（客户端 → 服务端）**，
服务端回 `0x0500 gspRepShopItemList`。⇒ §4 那句「货架目录到底走哪个 opcode
仍然没落锤」**作废**，也不用再去逆 `0x0503` / `0x0504` 了。

⚠⚠ **`0x0600` 是同号反向**：服务端方向的 `0x0600` 是 `gspRepMoney`
（右上角数据栏，`send_rep_money`）。**收到 `0x0600` 千万别当 money 处理。**

**为什么以前没看见**：会话 03 的 `SHOP_PROBE_OPCODES` 里没有 `0x0600`，
监听器不认它 —— 但**通用包日志早就记下来了**。
`logs/server-20260904-223901.out:186`（就是那次实机）：

```text
[22:13:12.181] #2 ★ 游戏包 opcode=0x0600 (?) 载荷 11 字节
    0000  00 02 00 00 00 00 00 00 00 00 00
```

**发送点**：`0x445e40 ReqShopItemList(this=分类id, u8 角色, u16 页号, u8 标志)`
→ 组包 `0x5592da`（`push 0x600`）。ShopStage ctor `0x444009` 调一次，
换分类 / 翻页各调一次（`0x44869f` / `0x44871d` / `0x45b5b2`）。

**线格式 = `u8 + i32 + u16 + i32`（11 字节）**，和 `0x0605` 一模一样：

| 线偏移 | 类型 | 含义 |
|---|---|---|
| 0 | u8 | **角色过滤**：当前预览角色 id（0/1/2）；分类高半字 == 3（용병 雇佣兵）时客户端强制填 `0xff` |
| 1 | i32 | **分类 id**（`(组<<16)\|序号`，见 §22）|
| 5 | u16 | **页号**，从 0 起 |
| 7 | i32 | ❓ 标志，取自 `[面板+0x160]`，实测恒 0（客户端按 u8 零扩展成 4 字节写）|

★ **10 秒节流**：`[ShopStage+0x144]` 是个 `0x2710`ms 定时器，**收到 `0x0500`
才清掉**（处理器第一句 `0x445ef1`）。⇒ 不回包的话客户端每 10 秒重发一次
—— 那三发 `0x0600`（22:13:12 / 23.354 / 33.744）就是这么来的。

### ★ 顺带把 `0x0605` 的线格式改回来：**`u8 + i32 + u16 + i32`**

§19 那条「实测纠正」是**错的**。三条独立证据：

1. `Serialize 0x443a38` 逐句写 1 / 4 / 2 / 4 字节（`0x5d5901` / `0x5d591f` /
   `0x5d5910` / `0x5d591f`），`Deserialize 0x443a74` 按同样顺序读 —— 两边对上；
2. 合成面板 ctor `0x45e0a3` 把「当前分类」初始化成 **2**，而进界面那一发的
   `bytes[1:5]` 正好是 `02 00 00 00`；按错误格式解，这个 2 会跑到 u16 那一格；
3. 同形状的 `0x0600` 实测到 `00 | 00 00 06 00 | ...` = 分类 `0x00060000`
   = **组 6（무기 武器）的父标签**，和 §22 的标签树严丝合缝；
   按错误格式解则是「u16=0, i32=6」，和标签树对不上。

⇒ 原来那条推理链的漏洞是：`0x50003` 这种数**看着不像 id 所以判成解错了**，
其实它就是 id（§22 的 `(组<<16)|序号` 编码）。**「这个数不好看」不是判据。**

---

## §21 ★★★★★ `0x0500 gspRepShopItemList` 的完整线格式（✅ **实机货架画出来了**）

**2026-09-05 实机落锤**：商店界面的 8 个格子里出现了图标 + 中文名 + 价格 +
「金币」+「购买」按钮，翻页、角色切换、武器 1/2/3 子分类过滤**全部生效**。
⇒ 下面这份线格式和「itemId / 名字 / 价格 / 货币」四个字段的语义**从 🔍静态
升成 ✅实测**（其余三格仍是 ❓）。

**三条顺带被实机证实的**：

1. **总页数 / 当前页那两格是真的**：泰尔的武器 20 件 → 界面显示 `1 / 3`，
   点下一页服务端收到 `页=1` 并回第 2 页；
2. **角色过滤是客户端说了算的**：点界面上方的角色箭头，`0x0600` 的第一个字节
   跟着变 `0 → 1 → 2`，货架跟着换成 `1120xxx / 2120xxx / 3120xxx`；
3. **一页 8 件**这个上限对（多了画不出来）。


`0x0500` **不是**「3 个 int 的元素」—— 它是**三层嵌套**。§4 说它「装不下名字和
价格」是把中间那层 `ShopStockGroup`（只有一个 vector）当成了叶子。

```text
gspRepShopItemList (Des 0x4439cc)
    i32   总页数        -> 面板 +0x14c（0x45b300：当前页 >= 它就跳回第 0 页）
    i32   当前页        -> 面板 +0x148（0x45b2be：夹到 [0, 总页数-1]）
    i32   n                                     ★ 一页最多 8 项（面板只有 8 个格子）
    n × ShopStockGroup (Des 0x4437fe，步长 0x10)     = 货架上的一个格子
          i32 m
          m × ShopStockGroupItem (Des 0x4436c7，步长 0x34)  = 这个格子的一档买法
                wstr  ❓（档位名？「7일」这类）
                ShopStock (Des 0x44360a)
                    i32   物品 id      ← 图标就是拿它查 ItemDB 的
                    wstr  **商品名**（画在格子上）
                    i32   **价格**     ← 旁边的标签是 `가격`
                    i32   ❓
                    i32   **货币**：0 = 픽셀（中文版叫「金币」）/ 非 0 = 캐시（「游戏币」）
                    wstr  ❓
                    i32   ❓
                    i32   k
                    k × Item@ShopStock（步长 0x10）：i32 物品 id + i32 数量 + i32 期限天数（后两格 §75 才钉死）
```

**字段语义的出处**（`0x45bc93` 刷新一个格子）：`+0x14`→`가격` 标签
（`0x45bdde`）、`+0x1c`→在 `픽셀`/`캐시` 之间二选一（`0x45be0b`）、
`+0x10`→设进文字控件、`+0x0c`→`0x4169e9` 查图标。
★ **角色限定 / 修理次数是客户端自己从 ItemDB 查的**（`타이 전용` 等串在
`0x45c025`~`0x45c0d3`），**不在包里** ⇒ 服务端不用管。

⚠ **未知 itemId 会走告警路径**：处理器 `0x445f8c` 对每个 `Item@ShopStock`
查 ItemDB，查不到就收集起来，循环完弹提示（`0x445fe5`）。
⇒ **发下去的 id 必须过 `shopdata.ownable()`**（§11）。

---

## §22 ★★★ 分类 id = **`(组 << 16) | 序号`**，标签树全表（🔍静态）

三个面板各有一棵标签树，id 编码相同。**低半字就是 §6 的部位码**。

| 组 | 含义 | 成员（id → 韩文名）|
|---|---|---|
| 1 | 服装 | `0x10001` 상의 上衣 · `0x10002` 하의 下装 · `0x10003` 장갑 手套 · `0x10004` 신발 鞋 · `0x10005` 머리 头 · `0x10006` 세트 套装 |
| 3 | 雇佣兵 | `0x30001` 용병 |
| 4 | 杂项 | `0x40001` 치장 装饰 · `0x40002` 기타 其他 |
| 5 | 非装备 | `0x50001` 재료 材料 · `0x50002` 카드 卡片 · `0x50003` 이벤트 活动 |
| 6 | 武器 | `0x60001` / `0x60002` / `0x60003` = **武器槽 1 / 2 / 3** |

**`(组 << 16) | 0` = 那一组的父标签**。

★ 出处：三段建树代码 `0x452ef6~0x45341b`（仓库）、`0x45a5ae~0x45a9bf`（商店）、
`0x45e42f~0x45e6bb`（合成）。每个标签是 8 字节 `{wstr 名字, i32 id}`。

⇒ 服务端按这张表把商品分到分类里：`part_flag` 1/2/16/8/4 → `0x10001..0x10005`、
武器的 1024/2048/4096 → `0x60001..0x60003`、材料 → `0x50001`。
**父标签要回子标签的并集**。

### ★ 商店界面的 6 个顶级标签 ↔ 组号（2026-09-05 实机逐个点出来的）

| 中文版标签 | 分类 id | 子标签 | 可信度 |
|---|---|---|---|
| 新商品 | `2` | — | ✅实测 |
| 武器 | `0x60000` | 武器1/2/3 = `0x60001` / `0x60002` / `0x60003` | ✅实测（子标签过滤也实机验过）|
| 道具 | `0x40000` | ❓ | ✅实测 |
| 装备 | `0x10000` | 上衣 / 下装 / 手套 / 鞋(`0x10004`) / 头 / 套装 | 🤔（`0x10004` 实测，父标签按组推）|
| 人物 | `0x30000` | **英雄 = `0`**、雇佣兵 = `0x30001` | ✅实测 |
| 技能 | `0x20000` | ❓ | 🤔（消去法，`0x20000` 实测到但没对上是哪个标签）|

⇒ **组号和顶级标签一一对应**：1=装备 2=技能 3=人物 4=道具 6=武器；
组 5（材料 / 卡片 / 活动）只在**仓库**面板里有，商店没有。

### ⚠⚠ **`0` 不是「全部」，它是「人物 → 英雄」标签的真 id**

2026-09-05 实机报的 bug：点「人物 → 英雄」列出了一堆武器。
根因是服务端把 `分类 = 0` 当成了通配。按上表，`0` 就是 `(组 0)`，
而**一件商品都不归在组 0** ⇒ 正确行为是空货架。

★ 上一版扫标签树时把 `영웅 = 6` / `세트 = 3` 记进了 §22，当时判成误命中
（扫描器取的是「最近一次 push 的字符串」后面那个立即数，撞上了同一段里别的
字段）。实机数据说英雄是 `0`；**套装 = `3` 其实是对的**（2026-09-06 实机，§39），
错的是这里按仓库那棵树推的 `0x10006`。
**⇒ 通则又一次应验：机械扫出来的映射要有第二个来源才能当事实。**

★ 组 7（`0x70000`，2026-09-04 从 `0x0605` 抓到过）在商店面板上没出现 ——
它多半是**合成面板**那棵树上的标签，等 M7 再对。

---

## §23 ★★ 剩下四个下行包的线格式（🔍静态）

**`0x0604 gspRepEquippedList`**（Des `0x443a30` → `Equipment::Des 0x404c3f`）
—— 和 `0x030b` 的包体**只差一个座位号**：

```text
12 字节  槽位掩码 ×3    （0x5d59c1 原样读 12 字节，处理器不读它 ⇒ 填 0 就行）
i32      物品数
物品数 × i32  物品 id
```

处理器 `0x447278` 拿它重建 `[ShopStage+0x134/0x138]` 两个 `EquipmentEx`，
**只有 `flags & 8`（可装备）的才 `Equip`**；**未知 id 会走告警路径**
（`0x447406`，和 §21 同款）。★ 它只影响商店界面，战斗加成还得靠 `0x030b`。

**`0x0508 gspRepGiftList`**（Des `0x443b33`）= 单个 `vector<Gift>`：
`i32 n + n × Gift`。⇒ **空清单 = 4 个 0 字节**。
★ `0x0607 gcpReqGiftList` 的应答就是它（`Ser`/`Des` 都是 `ret 4` ⇒ 请求无正文）。

**`0x0505 gspRepCompositionList`**（Des `0x443ae1`）：

```text
i32   总页数      -> 合成面板 +0x144
i32   当前页      -> 合成面板 +0x140
i32   n
n × CompositionRule (Des 0x44394d)
      i32   ❓（产物 id？）
      i32   ❓
      i32   m + m × CompositionMaterial{ i32 物品id, u16 数量 }   ← 最多 4 种（§7）
      i32   ❓
      i32   ❓
```

**ShopStage 跳表全表**（`@0x44434a`，索引 = `opcode - 0x500`）：

| opcode | 处理器 | 类 |
|---|---|---|
| `0x0500` | `0x445ed9` | `gspRepShopItemList` |
| `0x0502` | `0x44615b` | `gspRepItemBuy` |
| `0x0503` | `0x446700` | ❓**读一个 bool 然后弹提示框** —— 不是货架 |
| `0x0504` | `0x446b62` | ❓ |
| `0x0505` | `0x4474e4` | `gspRepCompositionList` |
| `0x0506` | `0x4476c4` | `gspRepComposeItem` |
| `0x0508` | `0x44798d` | **`gspRepGiftList`** |
| `0x0509` | `0x447c08` | `gspRepSendGift` |
| `0x050a` | `0x448072` | `gspRepGiftAction` |
| `0x050b` | `0x4468bc` | `gspRepItemDispose` |
| `0x050c` | `0x448849` | `gspRepItemUse` |
| `0x0604` | `0x447278` | `gspRepEquippedList` |
| `0x0501` / `0x0507` | default | 客户端不处理 |

---

## §24 ★★★★★ 穿 / 脱装备是 **`0x0702` / `0x0703`**，`0x0604` 上行其实是「修理」

**结论**（🔍静态，逐个按钮跟出来的）：

| 上行 opcode | 线格式 | 语义 | 组包点 |
|---|---|---|---|
| **`0x0702`** | 单 `i32` itemId | **穿上** | `0x559464` ← `0x554393` |
| **`0x0703`** | 单 `i32` itemId | **脱下** | `0x55949e` ← `0x55441c` |

两发**都回 `0x0604 gspRepEquippedList`**（没有专用应答）。

### ⚠⚠ **每一发都必须回，一发不落** —— 客户端拿应答当计数器

仓库里点「착용 穿上」时，客户端先把**冲突的旧装备逐件发 `0x0703`**，
每发一件就 `inc [ShopStage+0x13c]`（`0x4459da`），最后发一发 `0x0702`。
`0x0604` 的处理器结尾（`0x4473e0`）是：

```text
if ([ShopStage+0x13c] > 0) { 减一; 不刷新界面 }
else                       { 调 0x44e103 刷新仓库界面 }
```

⇒ 换一件抢两个槽的装备 = 3 发请求，服务端**少回一发**，计数器就停在 1，
**界面永远不刷新**（玩家看到「点了没反应」）；**多回一发**则会提前刷新一次。

### ★ `0x0604` 上行不是装备 —— 是「用扳手修理」

`0x446f2e` 唯一的调用点 `0x45087d` 写死第二个参数 `0x33453` = **210003**
（스패너 扳手，§8 那批商城消耗品之一），调用点是「수리하시겠습니까 要修理吗」
确认框的确定键。⇒ §4 / packet_api 里那句「`0x0604` = 装备 / 卸下 🤔」**是错的**。

### ★ 顺带把商店段剩下的上行全对上了（每个都只有一个调用点）

| opcode | 语义 | 判据 |
|---|---|---|
| `0x0601` | **「这些 itemId 我不认识，给我定义」** | 调用点全在各处理器的「查不到 id」分支（`0x445fe5` / `0x447426` / `0x493f51`…）|
| `0x0602` | **买**（购物车里每个 `ShopStock+4` 的 itemId 列表）| 空购物车时弹 `구입하려는 물품이 없습니다` |
| `0x0603` | **卖**（单 itemId）| 调用点是 `되팔기 转卖` 对话框的确定键 |
| `0x0604` | **修**（itemId + 210003 扳手）| 见上 |
| `0x060b` | **用**（`gcpReqItemUse`，单 itemId）| 调用点是 `전적 초기화 战绩重置` 确认框 |

★ **`SessionSlot` 里那串 `item_ids` 不是装备上报**：客户端序列化器
`0x556ccc` 原样写 `[slot+0x1c]`，那一格是服务端 `0x0300`/`0x0301` 发下来的
快照。**客户端改一份副本发回来，不是新信息** ⇒ 别指望从 `0x0301` 读装备。

### ★ 教训：**机械扫 `SetType` 会漏**

`tools` 里那套「扫 `push imm` + `call 0x5bba0a`」的办法**没扫到 `0x0702`/`0x0703`**
（线性反汇编在 `.text` 里的数据处失步了）。这两发是**顺着 UI 按钮一路跟**
才找出来的：`착용` 串 `0x668094` → 按钮存 `[obj+0xac]` → 分发 `0x45270a`
→ `0x445703` → 检查全过之后 `0x554393`。
⇒ **要确认「某个功能到底发不发包」，跟按钮比扫 opcode 可靠。**

---

## §25 ★★★★★ `0x0600` 最后那个标志位 = **右下角的排序开关**（✅实测触发）

**用户 2026-09-05 报的现象**：商店右下角有「基本顺序 / 上市顺序」两个按钮，
**点了没反应**。根因是服务端把请求里那个标志位读出来就扔了。

```text
0x45b5be  点按钮：== [面板+0x154] -> 标志 = 0
                  == [面板+0x15c] -> 标志 = 1
                  其余             -> 取反
          标志存在 [面板+0x160]（1 字节），然后触发一次货架重发
0x45b59b  组包时 `mov al, byte [esi+0x160]` push 进 0x0600 的第 4 格
```

* **`0` = 基本顺序**（进商店的默认值 —— 实机日志里进商店那一串全是 `标志=0`），
  `1` = 上市顺序。
* 线上那一格是 **int32**，内存里才是 1 字节（和 `0x0502` 的 bool 同款）。
* ★ **排序只能在服务端做** —— 一页只发 8 件，客户端手里没有全表。

**「上市顺序」按什么排**：原版的上市日期随 2009 年停服的服务端 DB 一起没了。
唯一不用自己编的近似是 **`ShopItem-Chn.ini` 的行序**（`shop_items.json` 的
`items` 保序 ⇒ `shopdata.catalog_index()`），**倒过来**用 = 新的在前。
实测在真实货架上两种顺序差别明显（武器 1 页：`1120011…` ↔ `1120083…`）。

---

## §26 ★★★★ 探针轮**负结果**：`ShopStock` 剩下三格，商店面板一个都不读

2026-09-05 开着 `shelf-probe on` 让用户把能点的标签全点了一遍，
**界面上 777 / 888 / ※探针※ 一个都没出现**。日志确认探针**确实在包里**
（`0x0500` 的 hex 里有 `09 03 00 00` / `78 03 00 00` / `04 00 3b 20 a2 63 88 94 3b 20`）。

静态复核对上了：格子刷新 `0x45bc93` 从头到尾**只读 4 格** ——
`+0x0c` 图标 id、`+0x10` 名字、`+0x14` 价格、`+0x1c` 货币
（这些偏移是**相对 `ShopStockGroupItem`**，比 `ShopStock` 自己的偏移大 8）；
购物车 `0x456923` 只取 `ShopStock+4` 的 itemId。
⇒ **`ShopStock` 的 `+0x10` / `+0x18`(wstr) / `+0x1c` 三格在商店面板上是死字段**，
填 0 / 空串即可，别再花时间找。（礼物 / 合成面板会不会读，没查。）

**内存布局**（Des `0x44360a`，大小 `0x2c`，`+0` 是 vftable 不上线）：

```text
+0x04 i32 物品id   +0x08 wstr 名字   +0x0c i32 价格   +0x10 i32 ❌死
+0x14 i32 货币     +0x18 wstr ❌死   +0x1c i32 ❌死   +0x20 vector<Item>
```

★ 教训：**探针法的负结果也是结果**，但它只证明「这个面板不画」，
不证明「谁都不读」—— 结论要写清楚是在哪个界面上试的。

---

## §27 ★★★★ 合成段的线格式全逆出来了（🔍静态，M7 直接照做）

**`CompositionRule`**（Des `0x44394d`，内存步长 `0x20`，`+0` 是 vftable）：

```text
i32   产物 itemId    ← 图标/名字都用它查 ItemDB；★ 0x0606 发回来的就是它
i32   合成费用       ← 「조합비용」标签（0x45fa4c 读 rule+8）；货币写死 픽셀 金币
i32 m + m×材料       ← 材料 = { i32 itemId, **i16** 数量 }，内存步长 0xc
i32   产物有效天数   ← >0 显示「기간:%d일」；<=0 走 ItemDB 的修理次数显示
i32   ❓             ← 五个消费点一个都没读它（同 §26），填 0
```

**判据出处**：`0x4474e4`(处理器，步长 0x20 + 拿 `rule+4` 查 ItemDB)、
`0x45f99f`(格子刷新，费用/天数)、`0x45d361`~`0x45d708`(确认框：
`cmp [余额 0x72e330], [rule+8]` 判买不买得起、遍历材料 `[rule+0xc]..[rule+0x10]`)。

★ 客户端**自己算得起买不起**（拿 `[0x72e330]` 比 `rule+8`）—— 费用要如实发，
乱填会让「明明够钱却点不动」。等级 / 角色限定 / 修理次数**全是 ItemDB 查的**，
不在包里（和货架同款，§21）。

### ⚠⚠ `0x0606` 认的是**产物 itemId**，不是配方号

`0x45d738: push [rule+4]` → `0x44765c` 组 `0x0606`（单 i32）。
⇒ **一个产物只能有一条配方**，否则服务端分不清玩家点的是哪条。
`recipe.json` 里 `result` 撞车时必须拒收（M7 的校验要加这条）。

**`0x0505 gspRepCompositionList`**（Des `0x443ae1`）= `i32 总页数 + i32 当前页
+ i32 n + n×CompositionRule`，和 `0x0500` 一个骨架；请求 `0x0605` 的线格式
和 `0x0600` **完全相同**（含 §25 那个排序位）。

---

## §28 ★★★★★ **客户端的物品定义表是空的，全靠服务端喂** —— `0x0601` ⇄ `0x0501`

**这一条解释了 2026-09-05 实机报的四个 bug 里的三个**（购买提示「无法从服务器
读取道具信息」、仓库空的、左侧人物模型不显示装备）。

客户端有**两张**物品表，以前一直把它们当成一张：

| 全局 | 内容 | 谁填的 |
|---|---|---|
| `[0x72e1e0]` | 图标路径 + `PartFlag` | **本地** `ShopItem-Chn.ini`（`[Item-*]`/`[Stock-*]` 节）|
| `[0x72e1dc]` | **`ItemInfo`**：名字 / 部位 / 形态标志 / 等级 / 角色限定 / 耐久 | ★ **只有服务端能填** |

⇒ 货架能画出图标和名字（名字在 `0x0500` 包里、图标查本地表），但**凡是要问
「这件东西是什么」的地方全废**：穿装备、买东西、仓库列表、左侧人物模型。

### 协议：同号反向的一对（又一次）

| 方向 | opcode | 载荷 |
|---|---|---|
| 客户端 → 服务端 | **`0x0601`** | `i32 n + n×i32 itemId + **u8 用途标志**`（Ser `0x559318`，发送函数 `0x55403a`）|
| 服务端 → 客户端 | **`0x0501`** | `i32 n + n×ItemInfo + **u8 用途标志**`（Des `0x559357`，处理器 `0x554136`）|

★ 服务端方向的 `0x0601` 是**持有物清单**（见 §29），**不是**物品定义。

**处理器 `0x554136`**：逐条 `0x415aab` 插进 `[0x72e1dc]`（key = `ItemInfo+4`），
然后调 `当前Stage->vft[0xb4](清单, 用途标志)`。

### ⚠⚠ 用途标志必须**原样回**

`ShopStage::vft[0xb4] = 0x44602a` 第一句就是 `cmp byte [ebp+0xc], 2` ——
**只有标志 == 2 才重建左侧人物模型**（`[ShopStage+0x134]` 那个 `EquipmentEx`），
其余值直接 return。客户端发请求时按用途填：

| 标志 | 谁发的 | 发送点 |
|---|---|---|
| `0` | 货架 `0x0500` 里有不认识的 id | `0x445fd3` |
| `1` | 持有物清单 `0x0601` 里有不认识的 id | `0x5542ac` |
| **`2`** | 装备清单 `0x0604` 里有不认识的 id ⇒ **回来要重建人物模型** | `0x44740c` |
| `5` | 结算界面 | `0x493f3e` |

★ 实机日志对得上：进商店后第一发 `0x0601` 尾字节是 `02`（6 件穿着的），
第二发是 `00`（8 件货架的）。

### `ItemInfo` 线格式（Des `0x5586a6`，内存大小 `0x38`，`+0` 是 vftable）

| 线序 | 结构偏移 | 类型 | 含义 | 出处 |
|---|---|---|---|---|
| 1 | `+0x04` | i32 | **itemId**（就是 map 的 key）| `0x554168` |
| 2 | `+0x08` | i32 | ❓ 全镜像找不到消费点 ⇒ 填 0 | |
| 3 | `+0x0c` | i32 | **`PartFlag` 部位掩码** | `0x4152ed: test [槽掩码], eax` |
| 4 | `+0x10` | i32 | **形态标志**（见下）| `0x443444` / `0x44736a` |
| 5 | `+0x14` | wstr | **物品名** | `0x552602`「%s의 내구도…」、`0x45238d` 提示框标题 |
| 6 | `+0x18` | wstr | ❓ 无消费点 ⇒ 填空串 | |
| 7 | `+0x1c` | i32 | ★ **等级要求** | `0x445817: cmp eax, [0x72e338]` |
| 8 | `+0x20` | i32 | ❓ 无消费点 ⇒ 填 0 | |
| 9 | `+0x24` | **i16** | ★ **角色限定**：`-1` = 不限，`0/1/2` = 泰尔/卡希尔/布洛克 | `0x44e1b8: cmp ax, 0xffff` |
| 10 | `+0x28` | i32 | ❓ 无消费点 ⇒ 填 0 | |
| 11 | `+0x2c` | i32 | ❓ 无消费点 ⇒ 填 0 | |
| 12 | `+0x30` | i32 | **最大耐久**（`0` 时客户端跳过百分比计算）| `0x5525dd` |
| 13 | `+0x34` | i32 | **修理次数上限**，`< 0` = 不限 | `0x412934` |

**形态标志 `+0x10` 的位**（`0x443444` 按位挑提示文案）：

| 位 | 含义 | 界面表现 |
|---|---|---|
| `0x01` | 计数持有 | 提示框写「소지개수 : %d개」，数字取**持有物条目 +0x08** |
| `0x02` | 期限持有 | 写「%d일」，天数取**持有物条目 +0x0c** |
| `0x08` | ★ **可装备** | `0x0604` 的处理器**只有这一位为 1 才 `Equip`** |
| `0x10` | 和 `0x02` 一起 = 「100%」 | |
| `0x20` | ❓ 仓库格子上多画一个标记 | |
| `0x40` | 按局数 | 写「%d게임」 |

⇒ 本版：**装备发 `0x08`，材料发 `0x01`，一件都不发 `0x02`**（不做期限售卖），
标志全 0 的话提示框写「기간제한없음」。

★ **两条硬约束**（发错了玩家点了没反应，而且客户端不说为什么）：
1. **等级要求必须 ≤ 玩家等级**，否则「착용 穿上」按钮直接弹框拒绝（`0x445817`）；
2. **角色限定要和 id 对上** —— `-1` 才是「不限」，`0` 是「泰尔专用」。

---

## §29 ★★★★★ **仓库列表 = 服务端方向的 `0x0601`**，请求是 `0x0700`

**`0x0700`（上行，无正文）不是「进商店了」—— 它是「给我持有物清单」。**
⇒ §3.8 里那句「❓语义未查明，不是商店专用」作废；大厅 / 房间也发它正是
因为哪儿都要用持有物。发送函数 `0x5541c1`，6 个调用点（ShopStage ctor
`0x44402c`、大厅 `0x43b5c4` / `0x4692c2`、`0x0609` 和 `0x0701` 的处理器尾巴）。

**应答 = 服务端方向的 `0x0601`**（处理器 `0x55420e`，Des `0x559413`，
条目 Des `0x412621`，内存步长 `0x20`）：

```text
i32 n
n × 持有物条目：
      i32   物品 id
      i32   数量 / 剩余局数        -> 条目 +0x08
      f64   剩余分钟数（0 = 永久） -> 条目 +0x10 ★ 8 字节 double
      i32   已修理次数             -> 条目 +0x18
```

处理器把它整份换进全局背包 `[0x72e41c]`（`0x4126cb`），再调
`当前Stage->vft[0xb8]`；`ShopStage` 的是 `0x446f8a` = **刷新仓库面板
`[ShopStage+0x108]`** ⇒ 这一发不发，仓库界面就是空的。

⚠ **顺序**：`0x0501`（定义）必须排在 `0x0601`（持有物）**前面** ——
仓库面板是在收到 `0x0601` 的那一刻建的，那时 ItemDB 还空着就建成空的，
而补发的 `0x0501` 标志位不是 2，`ShopStage` 不会再刷一次。

### ★ `0x0604` 发的是**穿着中**，不是「我拥有的全部」

处理器 `0x447278` 把清单里每个 `flags & 8` 的条目 `Equip` 进
`[ShopStage+0x134]`（左侧人物模型的数据源）—— 发全部持有物的话，
玩家会看到自己**同时穿着所有装备**。⇒ 只发 `equipped_items()`。

### ★ 客户端自己会在两个时机重新要一次持有物

`0x0701 gspRepMoveInto`（换频道结果，我们本来就在发）的处理器 `0x552b47`
处理完发一发 `0x0700`（`0x552d31`）；`0x0609`（耐久更新）的处理器
`0x552593` 尾巴上也发一发（`0x552794`）。**买东西之后它不发**
（`0x44615b` 里没有 `0x5541c1` 的调用点）⇒ 买完那一发只能服务端主动推。

---

## §30 ★★★★★ `0x0502` 第二格是**失败原因码**；买完那四发的**顺序**是硬要求

**§24 里那句「失败时客户端什么都不显示」是错的**（2026-09-05 实机推翻）：
失败会弹一个「购买失败」框，正文就是拿包里第二格查表。

### ① 失败原因码（`0x0502` 的 `+8`）—— `switch` 在 `0x45915f`

弹窗构造函数 `0x4586e1(this, ok, **原因码**, ?, 差额)`；组包点 `0x4461f8`
把包里的第二个 i32 当第二个参数递进去。分支表：

| 码 | 韩文串 | 中文版文案 |
|---|---|---|
| 1 | `%d 픽셀이 모자랍니다.` | 还缺少 %d 金币 |
| 2 | `%d 캐시가 모자랍니다.` | 还差 %d 游戏币 |
| 3 | `레벨이 낮습니다.` | 等级太低。|
| 4 | `레벨이 높습니다.` | 等级太高。|
| 5 | `인벤토리가 꽉 찼습니다.` | 购物车已满。|
| 6 | `내부 오류입니다.` | 内部错误。|
| 7 | `이미 소지한 아이템입니다.` | 已拥有的道具。|
| 其余（**含 0**）| `정의되지 않은 오류입니다` | 未定义的错误。|

★ 1 / 2 里那个 `%d` **不在包里** —— 客户端自己拿购物车总价减余额算
（`0x457dae`，`0x4461da` 处取）。⇒ 服务端只发码，不发金额。
★ **兜底不能填 0**：0 = 「未定义的错误」，等于我们知道原因却不说。

### ② ⚠⚠ 买完那四发：**仓库必须排在购买结果前面**

买成功之后客户端会弹「구입하신 아이템을 착용하시겠습니까?（要不要直接穿上
刚买的这件）」，条件是**购物车正好一件**且它 `PartFlag != 0`
（`0x44625d: cmp eax,4` / `0x446268: cmp [eax+0xc],0`）。
点「确定」（`0x4463e3: cmp edi,5`）之后：

```text
0x4463e8  push [ItemInfo+4]        ; 刚买的 itemId
0x4463eb  mov eax, 0x72e41c        ; ★ 全局背包（0x0601 灌进去的那份）
0x4463f0  call 0x412742            ; 在背包里找它
0x4463f7  je  0x446400             ; ★★ 找不到 -> 什么都不做，一声不吭
0x4463fb  call 0x445703            ; 找到了才走「穿上」（发 0x0703×N + 0x0702）
```

⇒ **`0x0502` 到达时背包里必须已经有这件东西**，否则「选了装备却没装上」
（2026-09-05 实机现象；日志里买完到手动装备之间一发上行包都没有）。
`0x424b13` 那个确认框是**阻塞**的，排在 `0x0502` 后面的包要等玩家点完才处理。

**⇒ 服务端买完的发包顺序钉死为**：
`0x0501` 定义 → `0x0601` 持有物 → `0x0604` 穿着 → `0x0600` 数据栏 →
**`0x0502` 购买结果（最后）**。

### ③ ★ 不许按「玩家当前是哪个角色」拦购买

实机：泰尔（`player_character = 0`）买布洛克的 `3120031 火箭筒 爆裂1` 被
服务端拒了。商店上方那排角色箭头本来就是给「替别的角色买」用的 ——
货架已经按**预览角色**过滤了（`0x0600` 的第一个字节，§20），
再按当前角色拦一道是我们自己加的规矩。**原版没有。**

---

## §31 ★★★★★ 三处「被判成死字段」的格子其实都有人读

2026-09-05 第三轮实机推翻了 §23 和 §26 各一条结论。**通则**：
「某个面板不读」≠「谁都不读」—— §26 结尾自己写过这句，这次就栽在上面。

### ① ⚠⚠ `Equipment` 头上那 12 字节 = **每个角色的槽位掩码**（§23 那句作废）

**实机现象**：房间（待机房间）下面的仓库格子里点「卸下」，弹「已卸下。」
（韩文 `이미 아이템을 착용하고 있지 않습니다` = 「你根本没穿这件」），
**而且一个包都不发**，界面上那件还标着「使用中」。

**判定函数 `0x5584ab(ecx=ItemInfo, eax=EquipmentEx)`**：

```text
si = [ItemInfo+0x24]                        ; 角色限定
if (0 <= si < 3):
    if ((EquipmentEx[0xc + si*4] & [ItemInfo+0xc]) != [ItemInfo+0xc])
        return false                        ; ★ 掩码里没有这个部位 -> 「没穿」
return  itemId 在 [EquipmentEx+0x18..0x1c) 的 vector 里
```

⇒ 掩码全 0 ⇒ 凡是**有角色限定**的装备一律判成「没穿」。

**客户端自己怎么维护它**（`Equipment::Equip 0x5583d3`）：

```text
ax = [ItemInfo+0x24]
0 <= ax < 3  ->  [Equipment + 0xc + ax*4] |= [ItemInfo+0xc]      ; 只点亮那一个
ax == -1     ->  三个掩码全部 |= [ItemInfo+0xc]                   ; 不限角色
然后 itemId push 进 [Equipment+0x18]
```

**为什么只有房间里出问题**：`0x0604` 的处理器是**先建空的再逐件 `Equip`**
（`0x4472d6` 默认构造 + `0x447379`）⇒ 掩码是客户端自己算的，我们发什么都没用；
`0x030b` 的处理器走的是**拷贝构造** `0x414d95` → `0x414d03` 的
`memcpy(EquipmentEx+0xc, Equipment+0xc, 12)` ⇒ **原样吃我们发的零**。

★ 战斗加成不受影响（`0x55853c` 只遍历 id vector，不看掩码）——
所以 §16 那轮实测在掩码全 0 的情况下也是对的。

⇒ **`0x0604` 和 `0x030b` 都按 `shop.equipment_slot_masks()` 算着发。**

### ② `ShopStock+0x10` = **划线原价**（§26 判它「死」，只对货架格子成立）

**实机现象**：商店提示框写「价格： 0 → 3000 金币」。

`0x45c351`：`if ([ShopStockGroupItem+0x18] != [+0x14]) 先用灰色画原价 + 箭头`，
相等才只画一个数（`0x45c449`）。（那两个偏移相对 `ShopStock` 是 `+0x10` / `+0x0c`。）
⇒ **不打折就必须把原价填成和售价一样**。

### ③ `ShopStock+0x18`（wstr）和 `ItemInfo+0x18`（wstr）= **物品说明**

两个提示框各读各的：

| 面板 | 说明从哪来 | 切分点 |
|---|---|---|
| 商店提示框 | `ShopStock+0x18` | `0x45c485` |
| 仓库提示框 | **`ItemInfo+0x18`** | `0x4554e7` |

两边都是 `0x5d5789(dst, src, "\|")` 按 **`|`** 切，⚠ **只画前 2 段**
（`0x45c4c9` / `0x455534` 的 `cmp .., 2` 在循环**顶部**，`i` 只取 0/1
—— 2026-09-09 订正，原文写「最多 3 段」是错的，见 §53），填进
`ItemInfo0Txt` / `ItemInfo1Txt`
（`ShopToolTipNewUI.ui`：240×88 / 238×51，位置 y=165 / 274）。
`ItemInfo2Txt`（240×14，y=256）**不参与切分**，画的是 grant 列表那行绿字。

⇒ 现在**两段都用**：第 1 段数值、第 2 段特殊效果（§53），段内用 `\n` 换行。
★ 原版的说明文字随服务端 DB 一起没了 —— 现在发的是
`shopcfg.item_desc_zh()` **从本地数据现算**的（`weapon.ini` 的伤害/弹匣/换弹/初速、
`EquipBonus-Chn.ini` 的加成）。这不算发明玩法：每个数客户端自己也查得到。

### ★ 还剩两个 ❓

`ShopStock+0x1c` 和 `0x0502` 的第三格（`+0x0c`）。`shelf-probe` 现在只填这两格
（值 `888`）。

---

## §32 ★★★★★ 全客户端**只有一个**等级全局 `[0x72e338]`，等级门槛一共 7 处

**为什么要有这一节**：显示等级、原版等级门、**商店物品的「穿上」判定**
读的是同一个 dword。所以「抬高等级来解锁对战」和「商店按等级卖东西」
在客户端这一侧是同一个数，不可能各要各的（D22）。

写它的只有三处：`0x54f329`（`gspRepLogin` 第 4 个字段）、`0x5538 9a`
（`0x0600 gspRepMoney` 的 `+0x18`，u16）、`0x552444`（升级时 `inc`）。
⇒ **改完等级不用重登，补一发 `0x0600` 就行**。

### 读它的地方（脱壳镜像上按 `38 E3 72 00` 全扫，49 处，逐条解码过）

| 分类 | 地址 | 判据 | 处置 |
|---|---|---|---|
| ★ **穿装备** | `0x445817` 商店/仓库、`0x46aff1` 房间 | `ItemInfo+0x1c` ≤ 等级 | **保留**（这就是商店等级限制在客户端的落点）|
| 门 1 | `0x440b08` | **== 1** ⇒ 拦「对战」频道 | patch |
| 门 2 | `0x440cd9` | 同上（§83 说的第 2 个引用点） | patch |
| 门 3 | `0x465338` | 中国区 + 生存 + 房主 + **< 4** ⇒ 强制夺分 | patch |
| 门 4 | `0x465a2c` | 同上（V0.2 §203 逆到的那处） | patch |
| 门 5 | `0x4374d5` | 建房(对战)「人数」表 `0x72e570` 按要求等级过滤 | patch |
| 门 6 | `0x54f8f2` | **== 1** ⇒ 建房时把地图换成 `Newbe2-1` | patch |
| 门 7 | `0x43b676` | **< 6** ⇒ 拦天梯标签 | patch（用户拍板）|
| 原版行为 | `0x44c94d` / `0x44cc00` / `0x467eaa` | **< 4** ⇒ 角色 3 아이린 锁着 | 不动（她本来就放不出来）|
| 原版行为 | `0x43b47e` | **< 4** ⇒ 不画 `FirstUserIcon.png` | 不动（中国区本来就跳过）|
| 显示 | `0x41a467` 数据栏、`0x465948` 房间信息栏、`0x530fda`（钳到 60）| —— | —— |
| 传参 | `0x436833` / `0x437470` 下拉框、`0x463f5f` 地图过滤 `0x40b5d0` | —— | 地图那条已由 D142 patch |

### ★ 两条**只有实机/静态全扫才会发现**的

1. **建房「人数」下拉框有等级门**：表 `0x72e570` 五条 `{2,3,4,5,6}` 人里，
   **5 人和 6 人的要求等级是 4**（`0x72e594` / `0x72e5a0`）。
   ⇒ 把 `0x72e400..0x72e660` 整段按 12 字节 `{名字, 值, 要求等级}` 扫了一遍，
   **全客户端的下拉框静态表里非 0 门槛就只有这两条**（其余全是 0）。
2. **1 级号建房会被塞进新手图**：`0x0201` 的处理器里 `等级 == 1` 时把地图
   改成 `Newbe2-1`（지하유적 입구，`map.ini` 里 **MaxUser=2**）/
   `Newbe2-1:NewPvp`。★ D120 时代 1 级被顶成 4，**这条从没触发过**
   —— 换成真实等级才第一次进入射程。

### 工具

`re/BigShot_22524.img` 上找某个全局的**全部**引用，别用 `re_bs.py xref`
（它从 imm 的位置往回猜指令边界，`ff 35 …` 会被解成 `xor eax, imm32`）：
按 `img.find(b'\x38\xe3\x72\x00')` 找地址字节，再往前 1..8 字节试解码，
**要求指令刚好覆盖那 4 个字节且 op_str 里含 `[0x72e338]`**（内存操作数）。
49 处一次全对。

### 实机

2026-09-05 起服务端 + 客户端，`logs/bshook_*.log` 里七行
`★玩家等级门槛 @ …` 全部出现 ⇒ 七处特征串在**解壳后的真进程**里都命中。

★★ **2026-09-05 晚用户实机全部确认通过**（✅实测）：真实等级显示对、
七道门（对战频道 / 5-6 人房 / 生存不被顶回夺分 / 不被塞进新手图 / 天梯 /
关卡 / 全地图）都放行、商店按真实等级卖。⇒ **D22 那条路走通了** ——
「下发真实等级 + hook 打补丁解门」比 V0.2 D120 的「把等级顶成 4」干净。
同一轮里 M5 的四个修复（重复卸下、划线原价、提示框说明换行、跨角色购买）
和管理页玩家仓库页也都确认了。

---

## §33 ★★★★★ 合成段剩下的三件事：`0x0506` 结果码 / 标签树只有 8 个 / 一页 8 条

M7 动手前补的一轮（🔍静态，判据全在脱壳镜像上）。§27 只逆出了**列表**那一半，
这一节补齐**结果**那一半，外加两个「不看就会白做」的边界。

### ① `0x0506 gspRepComposeItem` 的那一个 i32 = **结果码**（处理器 `0x4476c4`）

`0x44771b: cmp [ebp-0x2c], 0` 挑标题（`0` = `조합하기 성공`，非 0 = `실패`），
`0x44777a` 起的 `sub eax,0 / dec eax` 链挑正文：

| 码 | 界面文案 |
|---|---|
| **0** | 合成成功 ——「请在我的仓库里装备道具」；**选中那条配方的 `rule+0x18` 天数非 0** 时换成带「(사용기간 : %d일)」的那句（`0x44785f` 读 `[面板+0x594]->[+0x18]`）|
| 1 | `픽셀이 부족합니다` 金币不足 |
| 2 | `재료가 부족합니다` 材料不足 |
| 4 | `이미 소지하고 있습니다` 已拥有 |
| 其余（**含 3**）| `알 수 없는 오류` 未知的错误 |

⚠⚠ **和 `0x0502` 极性相反** —— 那一发填 0 是「未定义的错误」，
**这一发填 0 是「合成成功」**。拒绝时兜底填 0 = 玩家看到「合成成功」，
然后发现仓库里什么都没有。

⚠⚠ **没有「等级太低」这一档** ⇒ 等级不能拿来在合成上拒绝，只能当列表过滤（D27）。

★ 处理器**只弹个框，什么都不刷**（函数体里既没有 `0x5541c1` 要持有物、
也没有 `0x447467` 要配方列表的调用点）⇒ 仓库 / 金币 / 配方页全靠服务端主动推。

### ② 合成界面的标签树：**5 个顶级 + 7 个子标签**（✅ 2026-09-06 实机确认）

**顶级标签**在 `0x45e2f0` 那张表里，一行一个「标签名索引」：

```text
[ebp-0x40]=0 신상품 新商品   [ebp-0x3c]=1 아이템 道具   [ebp-0x38]=2 의상 装备
[ebp-0x34]=8 타이틀 称号     [ebp-0x30]=9 이벤트 活动
```

索引查的是**全局名字表 `0x72e6a0`**（10 个 `std::wstring`，`0x635389` 一次性
填好）：`0 신상품 / 1 아이템 / 2 의상 / 3 스킬 / 4 캐릭터 / 5 무기 /
6 받은선물 / 7 수집품 / 8 타이틀 / 9 이벤트`。
建标签的公共函数是 `0x44f451`（`lea eax,[eax*4+0x72e6a0]`），
仓库 / 商店 / 合成各调一次（`0x452e4e` / `0x45a4e6` / `0x45e367`）。

**子标签**（`0x45e42f` 起，一路 `push 标签名` + `mov 槽位, id`）：
装备下面 `0x10005` 머리 / `0x10001` 상의 / `0x10002` 하의 / `0x10003` 장갑 /
`0x10004` 신발；道具下面 `0x40001` 치장 / `0x40002` 기타；
新商品自己一格 `2`。**称号和活动没有子标签**。

⚠⚠ **没有武器、没有套装**（商店那棵树有 `0x60000` 武器和 `0x10006` 套装，
这棵没有）⇒ 产物归在那两类的配方，在「装备 / 道具」下面**永远点不到**
—— 现在靠「新商品」兜住（`shop.CATEGORY_NEW` 收全部，D32）。

★ 教训：**只读到叶子就以为读全了**。我第一版只扫到 `0x45e42f` 起的那 8 个
子标签，就写成「合成界面只有 8 个标签」；用户实机看到的是 5 个顶级标签，
名字一个都对不上。顶级那一层在**另一个函数、另一张表**里，
而且是「索引 → 全局名字表」的间接寻址，按字符串搜搜不到。
⇒ 逆界面结构时，**先找到「建一个标签」的那个公共函数**，再看谁调它、传了什么。

★ 和货架同样的坑：**分类 `0` 不是通配**，它是「人物 → 英雄」标签的真 id（§22）。

### ③ 一页 **8 条**，和货架一样

`0x45f011`（配方列表 setter）的循环走 `[ebp-8] = 0 .. 0x100` 步长 `0x20`
（一条 `CompositionRule` 的内存大小）⇒ 面板只有 8 个格子。
总页数写 `[面板+0x144]`、当前页写 `[面板+0x140]`，客户端自己会把当前页
夹到 `[0, 总页数-1]`（`0x45efb9` 的 `dec ecx`）⇒ **一条配方都没有也要报 1 页**。

### ④ `0x0505` 的处理器会把**产物和每一种材料**都拿去查 ItemDB

`0x447575` 查 `rule+4`（产物）、`0x4475ad`~`0x4475bf` 遍历
`[rule+0xc]..[rule+0x10]` 步长 `0xc` 查 `mat+4`（材料），查不到的攒起来
发一发 `0x0601`，**用途标志是 `3`**（`0x447603: push 3` —— 用途标志表里
原来缺的那一档）。⇒ 服务端要在 `0x0505` **之前**把两批 id 的定义都喂下去，
否则四个材料槽是没名字的空格子（和 §28 同一条道理）。

### ⑤ 「持有量」是**现场读背包**的，不是包里带的

按 §32 那套「全扫地址字节」的办法扫背包全局 `[0x72e41c]`，22 处引用里
落在合成面板范围（`0x45c000`~`0x45f000`）的有四处：`0x45d271` / `0x45d57b` /
`0x45db08` / `0x45dcb5`，全在**确认框和材料槽刷新**里 ⇒ 合成完推一发 `0x0601`
就能让「持有量」变对。⚠ 但**面板不会自己重画** —— 所以我们额外把客户端
最后一次发过的那发 `0x0605` 原样重放（D28）。

★★ **2026-09-05 晚实机全部通过**（✅实测）：合成成功、材料和金币照扣、
**「持有量」当场就变**（⇒ D28 那一发重放是有效的，不是白发）、
「材料不足」/「金币不足」的失败框文案也都对上了。

### ⑥ 合成面板**没有购物车**，这是原版设计

用户 2026-09-05 报「商店里点一下会自动加购物车，合成里点了只预览」。
判据是协议的形状，不是界面：`0x0606` 的载荷是**单个 i32**，而按下
「조합」的那一句是 `0x45d738: mov eax,[esi+0x594] / push [eax+4]`
—— push 的是**当前选中的那一条规则**的产物 id，不是某个清单。
⇒ 合成一次只处理一件，服务端也没有任何包能让格子「攒起来」。
（商店那边才有购物车：`0x0602` 是 `i32 n + n×i32`，`0x456923` 遍历它。）

---

## §34 ★★★★ 套装部位后缀原版**自己就不统一**：`바지` / `신발`

**症状**（用户 2026-09-05 实机）：合成界面里「泰尔只能看到上衣和手套，
另外两个角色有上衣 / 下装 / 手套」。

**根因**：`shopcfg.PART_SUFFIX_ZH` 只收了 `몸 / 다리 / 손 / 발 / 머리`，
而 `ShopItem-Chn.ini` 里：

| 部位 | 카실 / 프로코 | 타이 |
|---|---|---|
| 下装 | `다리`（腿）| **`바지`**（裤子）|
| 鞋 | `신발` | `신발` —— **`발` 从来没单独出现过** |

⇒ 泰尔的四条下装（`1020064/65/66/67`）和全部的鞋（`1040064` `1040067`
`2040064` `2040067`）**既进不了默认配方，中文名也翻不出来**（退回韩文）。
프로코 干脆没有鞋，那是原版就缺件，不是我们漏了。

⚠⚠ **`신발` 也以 `발` 结尾** ⇒ 后缀必须**从长到短**试。按 dict 的插入顺序
先撞上 `발`，`타이_마스터리아머 신발` 会被切成 `…아머 신`，套装名对不上，
那一件照样消失 —— 换个死法而已，而且更难看出来。
现在统一走 `shopcfg._split_part_suffix()`，它自己 `sorted(key=len, reverse=True)`。

★ **教训**：从原版资源里按名字分组时，「后缀表」是**观察到的**词汇，不是
规范。写完要拿全表反查一遍「有多少件一个后缀都没匹配上」，别等界面上少了
东西才发现 —— 这一条漏了 8 件，从 M1 一直躺到 M7 实机。

---

## §35 ★★★ 管理页「服务器内部错误」十有八九是**编辑器把 json 独占打开了**

**结论**：`POST /admin/api/config/*` 回 500、日志里写
`PermissionError(13, '拒绝访问。')`，原因不是代码，是 **`server/data/*.json`
正开在 サクラエディタ 里**（它默认独占打开），`write_json` 的
`os.replace` 撞上 Windows 的共享冲突。关掉编辑器里那个文件就好了。

**怎么确认是哪个进程占着**（不用装 Sysinternals）：PowerShell 里调
Restart Manager（`rstrtmgr.dll` 的 `RmStartSession` /
`RmRegisterResources` / `RmGetList`），它直接回进程号 + 程序名。
.NET 的 `[System.IO.File]::Open($p,'Open','ReadWrite','None')` 只能告诉你
「被别的进程占着」，说不出是谁。

**改了什么**：`_admin_config_post` 现在自己接住 `OSError`，回一句
「写不进 xxx.json（…），这个文件多半正被别的程序占着（编辑器打开了它？）」。
兜底那层 `except Exception` 只会打一行日志 + 回「请看服务端日志」——
用户根本猜不到要去关编辑器。

★ **教训**：凡是「写文件」的接口，`OSError` 都该有一句自己的话。
把它交给通用兜底 = 把一个**用户能自己解决**的问题变成一个要看日志的问题。

---

## §36 ★★★ 合成面板「称号」标签 = **`0x70000`**、「活动」= `0x50003`（✅实测，补 §33 ②）

**结论**：合成面板五个顶级标签发的分类 id 是 `2` 新商品 / `0x40000` 道具 /
`0x10000` 装备 / **`0x70000` 称号** / `0x50003` 活动。§22 末尾那句
「组 7 多半是合成面板的标签」就是它 —— 称号（`part_flag=8192`）归
`0x70001`（`shop.CATEGORY_TITLE`），父标签按组收。

**证据**：`logs/server-20260904-223901.out` 22:13:44.050 ~ 22:13:45.745，
`0x0605` 半秒一发依次是 `0x40000` / `0x10000` / `0x70000` / `0x50003`
（载荷 `00|00 00 07 00|…` 和 `00|03 00 05 00|…`），和界面上从左到右的
标签顺序一一对上。09-04 就抓到了，一直没人回头对。

⇒ 商店面板**没有**称号标签（§22 那六个），称号只能靠合成拿 —— 和 §7 说的
原版设计（称号是合成出来的）一致。

---

## §37 ★★ 2007 新浪官方资料页：物品的**官方中文名**和材料的来路（🔍资料）

用户给的 `games.sina.com.cn/o/z/pphqs/2007-07-04/1530263890.shtml`（合成系统）
同一栏目还有 `1533263893`（称号系统）/ `1505263876`（角色介绍）/
`1551263901`（道具介绍 = 局内拾取道具，和商城无关）。
★ 页面是 **GB18030**，`WebFetch` 拿到的全是乱码；`urllib` 抓原文
`decode("gb18030")` 才读得出来。

**合成系统页**：黑 / 红 / 绿 / 蓝色小珠、橡皮管、熔岩碎片、铁矿石
「可在对战和任务模式中获得」；Z芯片（机械青蛙·普通）/ Z晶片（困难）、
龙之血 / 龙之精华（岩浆巨龙）、破碎的衣服 / 破碎的军服（神秘岛）、
螺旋桨 / 合金螺旋桨（鲸鱼战舰）。
⇒ §8 里「`4xxxx` 和 `5xxxx` 两套完全同名同图」的材料 = **普通 / 困难两档**。
🤔 哪套是普通只能按「id 小的 = 普通」猜，没有更硬的证据；黑骑士 / 太阳齿轮
两关的困难档（`50012` / `50015`）页面上没有，名字是我们拼的。

**称号系统页**：称号素材叫「**卡片**」—— 红心 / 乌龙 / 信心 / 完美 / 射手 /
幸运 / 厄运 + 九张武器卡片。获取口径：武器卡片「仅限在对战模式中获得」，
射手「仅可在对战」，完美「对战、任务（普通,困难）」，其余「对战、任务」。
★ 武器卡片的 id（`110001` 等）**= `weapon.ini` 的 ROH**，也就是 §19 里
`0x0505 gcpAccumulatedWeaponDamage` 上报的键 ⇒ 原版多半是「一局里用某武器
打出最多伤害的人拿那张卡」🤔。本版先按对战随机掉。

**武器的官方中文名**（从卡片名反推，和「角色介绍」页互证）：
左轮手枪 / 苹果弹 / 狙击枪 / **复古短枪**（카멜나이트）/ 火焰弹 /
**华尔兹加农炮**（캐논왈츠）/ **重机枪**（머신건）/ **榴弹发射器**（스크류런처）/
**火箭炮**（바주카）。已按 D45 改进 `items.json`。

**角色介绍页**：「加密尔公国著名的武器制造大师**京**」= 진（角色卡
`102400001`）；T1 ~ T4 都是他造的，T2 被海贼希莉亚偷走 —— 用户要的
「真 → 京」出处在这。

---

## §38 ★★★★ 闯关**只有三档难度**：第 4 档「极限」被中国区版本跳过了（🔍静态 + ✅用户实机）

**结论**：引擎里第 4 档是完整存在的 —— 四份地图（`Quest02_1#Extreme` 等，
V0.3bot §140）、四份怪物表（`Quest0N/mob-1..4.ini`）、名字串
`익스트림 모드`（`0x692c98`）、拼文件名的四发 `dec edi`（`mapdata.DIFFICULTY_SUFFIX`）
一样不少。但**中国区版本选难度时一到 4 就绕回 1**，玩家永远选不到它。
用户 2026-09-06 实机报「游戏里没有极限」就是这个。

**证据**（`SelectQuestMap.ui` 的 `difficultyRBtn`「难度 ▶」按钮处理器）：

```text
00466487  inc  [ebp+8]                 ; 难度 + 1
0046648a  cmp  [ebp+8], esi ; jle      ; 超过表里的最大值就回 1
00466496  cmp  [ebp+8], 4 ; jl 跳过     ; ★ 到了 4
0046649c  mov  eax, [0x72e320] ; cmp [eax], 2 ; jne 跳过   ; ★ 区域号 == 2（中国区，V0.1 §5141）
004664a6  mov  [ebp+8], 1              ; -> 绕回「简单」
```

同一函数里 `difficultyLBtn`「◀」那一支（`0x466468`）减到 0 就回最大值，
没有区域判断 —— 但最大值那条路进去之后 ▶ 一按又被上面这段拉回 1，
界面上从来不会停在 4。`Chinese.ini` 把 `익스트림 모드` 原样留成 `Extreme Mode`
没翻，大概也是因为中国区根本看不到它。

**难度名字 → 编号**（`0x5572f7`，`sub eax,0 / dec eax` 链）：
`1` 이지 / `2` 노멀 / `3` 하드 / `4` 익스트림，和拼文件名那一段一致。

⇒ `shopcfg.DIFFICULTY_ZH` 只留 1 / 2 / 3，`validate_drops` 的上限跟着它
（D17a）。`mapdata.DIFFICULTY_SUFFIX` **照旧四档** —— 那是拼文件名用的，
bot 读地形还要它。`account_store.QUEST_DIFFICULTY_MAX = 4` 没动：
它只是存档里「已达成难度」的封顶，客户端的准入校验（V0.1 §118）
`min(已达成 + 1, 4)` 本来就到不了 4 的选项。

---

## §39 ★★★★ 商店「装备 → 套装」= **`3`**；合成面板「装备」下第一格是「新商品」= `2`（✅实测，纠正 §22）

**两条都是从 2026-09-06 23:43 ~ 23:51 的 `logs/server.out` 里对出来的**
（用户逐个点标签，`0x0600` / `0x0605` 的分类 id 按点击顺序排）：

```text
商店   0x10000(装备) → 3 → 0x10005 → 0x10001 → 0x10002 → 0x10003 → 0x10004
合成   0x10000(装备) → 2 → 0x10005 → 0x10001 → 0x10002 → 0x10003 → 0x10004
```

1. **套装 = `3`**。和「新商品 = 2」「英雄 = 0」一样是不按 `(组 << 16) | 序号`
   编码的小号。§22 里那句「上一版扫标签树把 세트 = 3 记进去是误命中」**错了**
   —— 3 是对的，错的是后来按组推的 `0x10006`。症状就是用户报的
   「套装标签空着、装备父标签下却列着羽翼套装」：父标签 `0x10000` 按组收
   `0x1000x`，`0x10006` 归得进去；子标签 `3` 精确匹配，什么都对不上。
   ⇒ `shop.CATEGORY_SET = 3`，父标签「装备」靠 `PARENT_EXTRA` 把它一起收下。
   ⚠ 顺带堵了一个洞：`3 >> 16 == 0`，按组算会被「英雄」标签（`0`）收走
   —— 小号 id 一律不走组匹配（`category_matches`）。
2. **合成面板的「新商品」挂在「装备」下面**，是那一排子标签的第一格，发 `2`
   （商店同一位置是「套装」）。§33 ② 写的「新商品自己一格 2」说的就是它。
   ⇒ 合成面板的 `2` 只列装备（`composition_category_matches`），商店的
   `2` 仍是顶级标签、收全部（D32）。两边同号不同义，靠**面板**区分。

**顺带看图确认的**（`Images/Shop/`）：`chNN?0015` 是**金色**骑士甲、
`chNN?0016` 是**银色**骑士甲（同款两色，防御 8 / 5）；`0018` / `0019` 是另一款
黄 / 白太空服（生命 8 / 5）。每个角色的金 / 银套各三件：上衣 + 头盔 +
鞋（布洛克是手套）。id 尾号 27/28、38/39（以及 29/30、36/37、40~43）
是**同图同属性的复制品** —— 全表按（类别 · 角色 · 槽位 · 图标 · 加成 · 武器数值）
去重共 108 件，只留 id 最小的那一件上架。

---

## §40 ★★★ 数据备份要知道的五件硬事（2026-09-07，D46）

1. **`server.config` 原本只有读没有写回**：`config.parse_text` / `load`，
   `ensure_exists` 只在缺文件时整份写模板。现在的 `save_keys` 逐行替换、缺键追加
   带说明、每行沿用原有行尾；`ensure_keys` 幂等（不缺就不开写句柄）。
   ⚠ `parse_text` 同键取**最后一次** ⇒ 写回时同键出现几次就全换。
2. **`web/admin._config_locks` 只护管理页保存那条路**；`ensure_files` /
   `backfill_defaults` / 外部脚本都不持锁；存档另有 `AccountStore._lock`（RLock）。
   现有代码没有任何一条路同时持两套 ⇒ 备份按固定顺序（`shopcfg._SPECS` 的顺序 →
   存档锁）拿两套不会死锁。
3. **Windows 上读文件会挡住别人的 `os.replace`**（Python `open()` 无 FILE_SHARE_DELETE）
   —— 备份拷贝期间必须持锁，否则游戏线程写存档 `PermissionError`、结算奖励丢；
   和 §35 编辑器独占是同一个物理现象。
4. **回滚一份等大小的旧文件会骗过 `shopcfg` 的热重载**（缓存键 `(st_mtime_ns, st_size)`）
   ⇒ 回滚后必须 `shopcfg.invalidate()`（`test_backup.RestoreTests` 钉住）。
   `accounts.json` 存储层零缓存（每次操作现读盘），但 `Conn.account` 是登录时抓的
   **视图**，写盘不会把它盖回去，只是玩家看到的数过期 ⇒ 回滚后 `reload_account`
   + `_push_account`。
5. **管理员账号在 `accounts.json` 的 `admin_accounts` 段里** ⇒ 回滚老存档能把自己锁在
   管理页外面（更老的连这一段都没有）；`databackup.admin_survives` 在写盘前拦，
   回滚后再跑一遍 `ensure_item_fields` + `realign_levels`（启动那两步，幂等）。

**打包**：自检真跑一次包内 `app.py`，`build-common.ps1` 的 `$argList` 加了 `--no-backup`
（照 `--no-log-cleanup`），否则备份会被打进发布包。`.gitignore` 加了
`/server/data/backups/`（备份含明文口令）。

---

## §41 ★★★★ 仓库界面的标签树：7 大 15 小，**套装 = `0x10006`**（🔍静态，中文名✅查表）

以前只记了「仓库那棵树的代码在 `0x452ef6~0x45341b`」（§22），没人读出来。
2026-09-07 为管理页玩家仓库弹窗逆的。`Pack_decrypt\Data\Ui\` 里**没有**仓库的 `.ui`
（只有 `BuyItemNewUI` / `ComposeItemNewUI` 那一组），标签树全在代码里建；
中文名来自 `Data\Chinese.ini`（UTF-16LE，`韩文键=中文值`），**照原版用词**：
「下衣」「其它」「鞋子」。

顶级索引表 `0x452dcf`（`5,1,2,4,3,7,8` 七项）→ 名字 → 分类 id 的 switch `0x44f578`
（跳表 `0x44f5c2`，10 项）：

| 大分类 | id | 小分类（屏幕顺序）| 小分类 id |
|---|---|---|---|
| 武器 무기 | `0x60000` | 武器1 / 武器2 / 武器3 | `0x60001` `0x60002` `0x60003` |
| 道具 아이템 | `0x40000` | 装饰 치장 / 其它 기타 | `0x40001` `0x40002` |
| 装备 의상 | `0x10000` | **套装 세트** / 头部 / 上衣 / 下衣 / 手套 / 鞋子 | **`0x10006`** `0x10005` `0x10001` `0x10002` `0x10003` `0x10004` |
| 人物 캐릭터 | `0x30000` | 佣兵 용병 / 英雄 영웅 | `0x30001` / `0`（永远空）|
| 技能 스킬 | `0x20000` | — | |
| 收集品 수집품 | `0x50000` | 材料 / 卡片 / 活动 | `0x50001` `0x50002` `0x50003` |
| 称号 타이틀 | `0x70000` | — | |

**⚠⚠ 仓库的「套装」是 `0x10006`，商店的是 `3`**（`0x45309e` vs `0x45a74c`，合成同一格
是「新商品 = 2」`0x45e4d9`）—— §22 那句「按仓库那棵树推的 `0x10006` 是错的」要改成
「`0x10006` 对仓库是对的，对商店是错的」。房间面板（`0x46e420`，第四棵树：
武器 / 道具 / 装备 / 技能 / 称号）也用 `0x10006`，和仓库共用过滤函数。
其它两棵树顺带补齐：商店 6 顶级 = 索引 `0,5,1,2,4,3`（新商品 / 武器 / 道具 / 装备 /
人物 / 技能，「技能 = `0x20000`」由此从 🤔 变成静态确证）；人物下面
**佣兵 `0x30001` 在前、英雄 `0` 在后**。

**仓库标签是纯客户端过滤，没有包**：`0x4127d7` 直接遍历本地背包 `[0x72e41c]`，
`requested == 0` 直接返回空；组匹配 `(requested>>16) == (cat>>16)`，低半字非 0 时
还要低半字相等。物品落在哪一格由客户端自己的两个函数算：

- `part_flag != 0` → `0x55bb57`：1→`0x10001` 2→`0x10002` 4→`0x10005` 8→`0x10004`
  16→`0x10003` **32→`0x20000`** 64/128/256/512/32768→`0x40001` 1024/2048/4096→`0x6000x`
  **8192→`0x70000`** 16384→`0x40002` 其余组合值→**`0x10006`**；
- `part_flag == 0` → `0x55ba64` 按 **id 的部位码** `(id/10000)%50`：材料 / 卡片
  （6~8、11~13 是卡片）/ 活动，9 位角色商品→佣兵 `0x30001`。

套到 808 件 `ownable` 上：装备 159/87/78/84/130/18、技能 15、佣兵 11、装饰 61、
其它 11、材料 26、卡片 17、活动 12、武器 21/30/25、称号 20，**钥匙 3 件归 `0`，
哪个标签都看不到**。⇒ `shop.warehouse_category_of()` 照这两条翻过来，
`server/test_shop.WarehouseCategoryTests` 钉住；商店 / 合成的 `category_of` 一点不动。

**材料在仓库里有专门的一格「收集品 → 材料」**（✅ 2026-09-05 用户实机也看到过），
所以管理页的玩家仓库弹窗把 `materials` / `inventory` 两桶合成一张表按这棵树分
—— 两桶是存档结构的划分，不是游戏界面的划分。

---

## §42 ★★ 第 7 关的 boss 还是机械青蛙；原版对战任务给的是羽翼散件，不是珠子（🔍资料）

- `Data\Quest\Quest07\Map03Intro.txt`（UTF-16LE 剧情脚本，末列是中文）：
  「机械青蛙堵住出口了~！」「又大又恶心的家伙！」，`boss07a-*.ini` 是它的四档攻击表；
  第 6 关开场：「这里是扎米洛军用机器人的生产基地」。⇒ 不死鸟之羽 / 之泪**没有对应的
  boss**，D49 把它放第 7 关是设计选择，不是原版事实。
- `Promotion.ini` 里带 `PromotionItemID` 的 18 条对战任务（`type=2`）给的全是
  `1520022` / `1530022` / `2540022` / `3510022` = **羽翼套装散件**（`prob=100`），
  珠子不在里面。「珠子可在对战获得」只有新浪页那一句（§37）。
- `Mob.ini` 只有 24 个小节、数字 id 带韩文注释（`8001` 비행기 飞机、`8002` Carrier、
  `9001~9004` 各种士兵），boss 不在里面，各关 `boss0Na-*.ini` 单独一份。
- 套装图标：`Images/Shop/<韩文名> 셋트.png` 是整套穿在身上的全身图（61 / 62 / 63 九套
  和大师铠甲有），其余只有部位图。D49 的风格配对就是看这批图定的。

---

## §43 ★★★ 「角色全解锁」在服务端存档层，不在 hook；hook 里角色 110 的图标修补要留着（🔍代码）

- 全开逻辑 = `account_store.NEW_ACCOUNT_DEFAULTS["character_unlock_all"]`（V0.1 §119 起，
  D51 删）。`hook/bshook.c` 里**没有**任何「角色持有判定」patch —— 客户端的判定
  `0x55853c` 只查 `0x030b` 填进座位的物品 vector，服务端发几张角色卡就有几个角色。
- hook 唯一和商城角色有关的是 `REGION_SITES[2]`（`0x4f67d1`）：角色 110 시리아 마스的
  战斗内头像下标 0x18/0x19 超出中国版 24 帧图集，进关卡必崩，改借 106 的 0x10/0x11。
  **角色可买之后这条必须留**，不然买了 110 的人进关卡就崩。
- 角色卡 `part_flag == 0`、`kind == "character"` ⇒ 商店分类只能靠 kind 分到佣兵 `0x30001`
  （§22 的表）；`shop.category_of` 原来按 `part_flag == 0` 一律兜进「道具 → 其他」。
- 11 张卡的图标各不相同（`엘리어스_셋트1` / `진` / `발키리` … / `시리아 마스`），
  `shopdefaults.dedupe` 不会把它们合并；同角色的 `1019000xx` 三件只有货架条目，不 ownable。

## §44 ★★★★★ 拿**真的云服存档**（71 个号）跑完迁移：一个字段都没丢（✅实测）

2026-09-08 用 `server/data-云服/accounts.json`（线上取下来的真件，`schema_version 2`、
71 个号）复刻 `app.py` 的三步开服跑了一遍。**结论：干净，可以上线。**

- **丢失 0 / 值被改 0**；只新增 `inventory` / `equipped` / `materials` 三个空字段，
  旧键 `character_unlock_all` / `owned_characters` 71 个号全部删干净。
- ★ **等级重算 0 个** —— 云上已经在跑当前的等级曲线了，
  「上线那一刻等级数字会跳」这条担心（PROGRESS 里那句）在**这份数据上不成立**。
- **36 个号选着付费角色**（100~110，`character` 分布 109×9、110×5、101×6…），
  迁移后 `player_character()` 全部退回 0 泰尔，存档里的 `character` 字段**原样留着**
  （D51 的设计：不改存档，只在读的时候退回）。`owned_characters` 云上 71 个号全是空表
  ⇒ 迁移后**没有任何人**手里有角色卡，都得去商店买。
- 四份运营配置：云上一份都没有 ⇒ `ensure_files()` 按模板生成，
  和开发机 `server/data/` 的四份**逐字节相同**（808 物品 / 495 上架含 11 张角色卡 /
  122 配方 / 52 掉落，零警告）⇒ D50「模板 = 现在的数据」是真的。
  `_report_character_shelf()` 不会喊。
- **连开三次服**：第 2、3 次 `realign=0` / `item_fields=0` / `ensure_files=[]`，
  `accounts.json` 的 sha256 **和 mtime 都没变**（真的没写盘，不只是内容一样）。
- 改一件商品价格再开服，`shop.json` 没被盖（D7）。建出 `admin` / `Admin123`（系统管理员）。
- 演练脚本在 scratchpad，不进仓库；要复跑就照这三步：
  `AccountStore.realign_levels()` → `.ensure_item_fields()` → `shopcfg.ensure_files(dir)`。

## §45 ★★ 「凌晨 4 点自动备份」**不用等到 4 点**也能验（🔍代码）

`databackup.BackupService._run()` 每一圈都重读设置再算下一次到点时刻，
而 `update_settings()` 写完 `server.config` 就 `self._wake.set()` ⇒
**管理页「数据备份」里把时刻改成两分钟后、保存**，线程当场被叫醒重排，
到点就会多出一份「自动 · 每日自动备份」。验完改回 `04:00` 即可。
（这是铁律 10 说的那个例外：等的是时钟，物理上没有别的事件可等 ——
但「设置变了」是有事件的，所以那一头是事件驱动的。）

## §46 ★★★★★ 装备槽是**每个角色一套**；服务端曾按整个账号一套算 —— 一人穿铠甲另两人被脱光（✅实测 + 🔍静态）

**现象**（2026-09-09 用户在 `dist/…V0-3-0 - 副本` 实机，`logs/server.out` 01:00:41）：
泰尔穿上 `1010052`，服务端日志「顶掉 [2020023, 2010047]」—— 那是卡希尔的两件；
`3120011`（布洛克武器槽 1）同样顶掉了 `1120042`（泰尔武器槽 1）。
整局客户端**一发 `0x0703` 都没发** —— 它根本不认为这些是冲突。

**根因**：`shopdata.resolve_equipped()` 用**一个**总掩码收敛整份 `equipped`，
「同 `part_flag` 即冲突」不看角色。客户端是三个掩码、一个角色一个（§31 ①）：

| 客户端 | 地址 | 规则 |
|---|---|---|
| 穿上前的预检 | `0x55832c(ItemInfo, Equipment, 0)` | 角色限定 0/1/2 → 只查 `掩码[角色] & PartFlag`；`-1` → 三个掩码都查 |
| 收集要先脱的旧件 | `0x415304(Equipment, &out, 角色, PartFlag)` | 穿着中的每件：`(旧.角色 == 新.角色 ‖ 旧.角色 == -1) && (旧.PartFlag & 新.PartFlag)` 才进清单（逐件发 `0x0703`）|

⇒ 同一个部位、不同角色，客户端**从不认为冲突**；服务端一「顶掉」，回的 `0x0604`
少了那几件，客户端整份重建（先建空再逐件 `Equip`，§31），另两个角色就光了。

**受影响的**：凡是角色限定的装备全中 —— 铠甲各部位 / 套装、武器三个槽、
突击技（`part_flag` 32）、喷漆（64）、戒指（16384）。不限角色的只有称号（8192）
和宠物（32768），它们和角色装备没有共用的 `part_flag`，所以没被殃及。

**修法**（D54）：`shopdata.slot_owners(item)` 一处定「这件点亮哪几个掩码」
（照 `Equipment::Equip 0x5583d3`），`resolve_equipped` / `conflicts` /
`shop.equipment_slot_masks` 共用；收敛时三个掩码各算各的。
存档不用迁移：被错顶的装备只是从 `equipped` 掉出来，`inventory` 还在，重穿即可。

⚠ 客户端那条收集规则是**不对称**的：新件不限角色（-1）、旧件角色限定时，
`0x415304` 不收旧件（只匹配 `旧 == -1`），预检 `0x55832c` 却会判「占着」。
服务端按掩码模型（对称）收敛，这种情况会替它顶掉旧件；现在的物品表里没有
这种同槽组合，`test_shopdata` 里那件合成的「不限角色上衣」只是为以后钉规则。

---

## §47 ★★★★★ 穿着「火焰蝙蝠」用溅射武器打空 → 客户端闪退：原版 `0x47e776` 的 `flags & 0x200` 中国区分支没判空（✅实测 + 🔍静态）

**现象**（2026-09-09 01:36:42，`game_patched/Dump/LastCrashReport.txt` +
`BigShotV0311N001.mdmp`）：和 1 个 bot 打对战，扔苹果弹（`1120053`）打空的一发
之后 45 ms 闪退。`C0000005` @ `0x47ea6c: mov eax,[edi]`，`EDI = 0`；栈：
`0x492a4e`（`OnExplode`）← `0x49194e`（`0x040e` 内层分发）← `0x407dbc` ←
`0x405815`（`GameSession::Tick` 的收包 flush）。

**触发的那一发是自己发的**（hook 日志 01:36:42.057，`Encrypt#597`）：
`rpExplode` 句柄 100058、目标 0、命中类型 0、**flags = 0x200**、伤害 38。
本局 11 发里只有这一发带 0x200；bot 的 flags 恒 0（`botsync.explode_body` 不填）。
自己发的包本地回环处理（V0.2 §151 的 `0x405a5d`），所以**射手自己先崩**。

**`0x200` 是什么**：`Projectile` 虚表槽 `+0x128` = `0x47e618` 的尾巴
（`0x47e716..0x47e76b`）：`GetEquipBonus(射手, 11 = IncSplashRange, -1) > 0`
且随机数 < 0.15（`0x6938cc`）且武器 `SplashDamage > 0`、`SplashRange > 0`
⇒ `or byte [ebp+0xd], 2`。**不看有没有目标。**
全 `EquipBonus-Chn.ini` 只有一件带 `IncSplashRange`：**`220003` 火焰蝙蝠（+10）**，
用户 01:35 刚穿上它（`online.log`）。

**崩在哪**：`OnExplode`（`0x492715`）查目标句柄（`0x492841 → 0x474225`）查不到
就走 `0x4929be` 那条路，最后照样调虚表槽 `+0x12c` = **`0x47e776(target=NULL,
flags, pos)`**（画「加成提示」的函数）。两支不对称：

| 支 | 判空 | 中国区（`[[0x72e320]] == 2`）分支 |
|---|---|---|
| `flags & 0x20`「HEAD SHOT!」 | `0x47e79d cmp edi,esi ; je` ✅ | 取图 `Images/Chinese/Img…`，挂在目标位置 |
| `flags & 0x200`「ESPERAN SPLASH!」 | ❌ **没有** | `0x47ea21` 取图 → `0x47ea6c mov eax,[edi] ; call [eax+8]` 取目标位置 → **读 NULL** |

韩国区分支（`0x47e9d3`）把文字画在爆炸点、不碰目标，所以韩服没这个问题；
中国区分支是原版 bug，大概原版商店从没卖过这件戒指。

**为什么只能在客户端修（D55）**：① 回环让射手先崩，服务端改包救不了他；
② 收方拿 `flags & 0x200` 决定溅射范围倍率（`0x492785 test byte [ebp+0x1d],2`
→ `GetEquipBonus(射手, 11)` → `1 + x/100` 乘进 `[esi+0x4c]`），抹掉就变玩法。

**修法**：`hook/bshook.c` `try_patch_splash_visual_guard()`：`0x47ea21` 头 5 字节
（`push 7 ; lea eax,[ebp+0xc]`）改 `E9` 跳 detour，`edi == 0` 就走函数尾
`0x47eb0e`（和 `0x47e9c2` 那条 `je` 同一个出口），否则补回两条原指令跳回 `0x47ea26`。
实机：hook 日志 01:59:12 打出「★溅射加成提示判空 @ 0047EA21」；
**「打空那一发不再闪退」还没在实机复现过**（待验证 N-53）。
`BSHOOK_KEEP_SPLASH_VISUAL_CRASH=1` 保留原版对照。

★ 顺带：`rpExplode +20` 又认得一位 —— `0x200` = 射手 IncSplashRange 加成触发
（15% 门），已补进 `re/packet_api.md` §5.3（连 §16 那四位一起）。

---

## §48 ★★ `BigShot.rpt` 08-11 那次：教程弹窗点「确认」时大厅对象为空（🔍静态，触发条件没复现）

`C0000005 @ 0x40f4ef`，`EAX = 0`；栈 `0x43b46a`（大厅初始化 `0x43b11b` 的教程弹窗
分支，V0.1 §54）← `0x40decb`（主循环）。`0x40f4df StartTutorial(App)` 第一句就写
`[[0x72e29c]+0x40]`（LobbyStage），整个函数没有判空。弹窗是模态的，弹着的时候
大厅被拆掉（掉线 / 服务端重启）再点确认就是这条路 —— 08-11 正是 V0.2 反复起停
服务端做自动化的日子。注册页默认勾「跳过新手教程」（V0.2 §168），真人极少走到。
**修**：hook 在 `0x40f4ea` 补判空，LobbyStage 为空直接走函数尾 `0x40f57b`（D56）。
`BigShot.rpt` 里 08-06 ~ 08-08 另外五次（`0x40197f` ×2 / `0x50a368` ×2 / `0x5ccf29` /
`0x47252d` / `0x430857`）都是 V0.1 时期假服务端的包不对，V0.1 §64 / §82 / §C 已查过并修掉。

## §49 ★★★ `BigShot.rpt` 09-05 两次：退出拆到一半又收到 WM_CLOSE，字体管理器已经没了（🔍静态 + 日志）

两次寄存器一模一样（`ECX = ESI = 0x100`，`EAX = 0x19ed58`）。栈：`0x5cedc9`（哈希表
查找）← `0x5bd018`（`0x5bcffe` 字体按名取，`this = [0x6e9400]`）← `0x424b53`
（`0x424b13` 弹消息框）← `0x40f0cc`（主窗口 WndProc `0x40ed98` 的 **WM_CLOSE** 分支
`0x40ef90`，弹「종료 확인」）← user32 ← mshtml / urlmon（内嵌 IE 控件泵消息）←
`0x40d9c7`（**App 析构** `0x40d9be`）← WinMain。
App 析构里 `0x40dbef` 先把 `[0x6e9400]` 置 NULL，之后拆 IE 控件时泵到一条排队的
WM_CLOSE → `this = 0` → `+0xfc + 4 = 0x100` → 读 `[0x108]` 崩，ECX = 0x100 就是这么来的。
`stop.bat` 只 `Stop-Process`、不发 WM_CLOSE ⇒ 两次都是人手多点了一下 X。
**修**：hook 在 `0x40ef90` 补判空，字体管理器为空就当「确认框已在弹」走 `0x40f119`
返回 0（D56）。实机：登录界面 `CloseMainWindow()` 照旧直接退出，没有崩溃报告。

## §50 ★★★★ `BigShot.rpt` 09-06 两次 = D31a 那个「角色限定改宽会闪退」的机制：骨骼名只在自己角色的骨架里（✅日志对上 + 🔍静态）

时间线（develop 工作树 `logs/server-20260906-220652.out` / `online-20260906.log`）：
21:46:22 合成 `3010065`（布洛克 初阶凤凰铠甲·上衣）→ 21:47:06.818 穿上 → **同一秒崩**；
重登 21:47:44 一进商店（穿戴清单里还有它）→ 又崩；21:50:40 管理员保存 items.json；
21:52:52 脱下再穿上 → **不崩**。⇒ 崩的是 `0x0501` 里那份定义（角色限定），不是网格。

崩点 `0x58cbff`（静态链入的 D3DX 矩阵乘，`ECX = 0x58`）← `0x5d280d`（`0x5d2702`
CPU 蒙皮循环）。那张表 `[+4+i*4]` 是蒙皮记录、`[+0x25c+i*4]` 是对应骨骼指针（各 150
格，`0x5c2556` 构造时清零）；骨骼按名字在角色骨架里找，找不到就是 NULL，`+0x58`
喂进矩阵乘就崩。

`.msh` 格式（够用的部分）：`u32 版本=3`、4×4 矩阵、`+0x6c` 顶点数、`+0x70` 索引数、
顶点 32 字节、索引 u16、材质块（含 `u32 长度 + UTF-16 贴图名`）、`u32 骨骼数`，每根
`u32 长度 + UTF-16 骨骼名 + u32 n + n×索引 + n×权重 + 16 float`。骨骼名表在 `.mtn`
里：**`Bone_Spine_01` / `Bone_Pelvis_01` 只有 ch02（布洛克）有**，ch00 / ch01 是别的
名字。ch02 网格挂到泰尔 / 卡希尔身上 = 两根全 NULL。

**数据层已堵**（D31a，同一天）：`validate_items` 只认原版角色限定，管理页只读。全表
586 件带角色网格的物品，id 前缀角色和 Tag 的 `chNN` **全部一致**；今天 478 件上架角色
网格逐件对过骨架，0 问题。**再兜一层**（D56）：hook 在 `0x5d27f1` 跳过骨骼为 NULL 的
记录（顶点留在原点，缺一块模型，不崩），退出时打一行跳过计数。

## §51 ★★ 上架物品资源审计（2026-09-09，脚本在会话 20 的记录里）

643 件上架（商店 / 合成产物 / 掉落材料）：铠甲 478、武器 76、材料 26、染色剂 24、
突击技 15、角色卡 11、宠物 7、戒指 6。

- 617 件有 `Tag`；478 件是角色网格（`Models/Characters/chNN/…`）：`.msh` / `.dds` 全在，
  骨骼全在对应骨架里，角色限定全对。其余 Tag 是武器 id / 颜色 / `None`，不是文件路径。
- **图标缺 3 件**：`1010022` / `2010022` / `3010022` 羽翼套装，ini 指
  `Images/Shop/chNNT0022.png`，包里没有（只有 `chNN00022.png` / `chNN^W0022.png`）。
  09-06 实机穿着 `1010022` 没崩，只是没图。
- 330 件是中国版没起过中文名的（`name_kr`）：铠甲 191 / 武器 76 / 染色剂 24 /
  突击技 15 / 角色卡 11 / 宠物 7 / 戒指 6 —— 资源检查过关，但**实机没穿过**。
  有 §50 的守护兜底后，万一穿坏是缺一块模型 + 退出日志一行计数，不再是闪退。

## §52 ★★★ 装备加成路径逐条排查：只有火焰蝙蝠那一条漏判空（🔍静态，2026-09-09）

用户问「还有没有类似火焰蝙蝠的加成会闪退」。`0x407014 GetEquipBonus` 全镜像 14 个
调用点，按下标对上 12 种属性（**Critical 没有任何调用点，也没有任何物品带它**）；
`0x4070aa`（取座位加成表指针）另外 7 个直接调用点全是按固定槽位读，
没有按物品属性动态索引的地方。

**上架物品里真的能触发的属性**（`EquipBonus-Chn.ini` × 上架表）：

| 属性 | 谁带 | 客户端路径 | 判空 / 风险 |
|---|---|---|---|
| Attack / Defense / Hp / Sp / MoveSpd | 数百件铠甲，原版中国区在售 | `0x4806bf` / `0x50a09e` / `0x50a0de` / `0x5074f4` / `0x4fca62` | 原版跑过，目标指针 `0x480846` / `0x4808d7` 都先判空 |
| IncSplashRange (11) | 220003 火焰蝙蝠 | `0x47e716` 置 flags 0x200 → 收方 `0x47e776` 画图 | ★ 中国区分支漏判空，**已修**（§47 / D55）|
| HeartBoost (10) | 220001 青鸟 | `0x522ae3`：捡心时对自己多发一次道具效果 10316（`0x493af5`）| 只碰自己；10315 / 10316 在服务端 `ITEM_INI_ITEM_IDS` 里 |
| AntiGhostCnt (9) | 220002 / 220006 防护装置 | `0x508635`：按次数挡「幽灵」，自己身上放 `Effects/Pet/Pet01/Efx/Pet01_GhostBarrier00.efx` | 角色对象 `0x5084f9` / `0x50855d` 判空，特效建不出 `0x508689` 判空，文件在包里 |
| DashAttack (12) | 220004 迷你机械青蛙 | `0x481e40`：伤害 × (1+x/100)，flags 置 0x400 | 纯算术；0x400 收方没有任何消费（`OnHit 0x4ff27d` 只看 0x80）|
| TeamReflection (13) | 220005 萤火虫 | `0x50b64e`：六个座位逐个取角色，同队且 `+0x2b4 == 0` 才给反射 | `0x50b668` 判空；`0x50b4ef` 只在「那个队友是我」时放特效，和 10303 反射同一条路（V0.2 实机跑过）|
| TeamDmg / SelfDmg (7 / 8) | 只有称号 560007 / 560008，**没上架** | `0x4807b8` / `0x4807cc` | 提示框有专门格式串 `Self/Team %d%% %s`（`0x413ddf`）|

四枚戒指的路径里**一处 `[[0x72e320]]` 区域判断都没有** —— 火焰蝙蝠崩的是中国区
独有分支，别的属性没有这种分支。提示框（`0x4136af`）按固定槽位
`[表+0x10 / 0x14 / 0x18 / 0x20…]` 逐属性写死，稀有属性只是不显示。

⇒ **同类漏判空只有火焰蝙蝠一条。** 其余四枚戒指静态上干净，但和 §51 一样
**实机没戴过**；戴上玩一局各触发一次（青鸟捡心 / 防护装置挨打 / 机械青蛙冲刺 /
萤火虫和队友一起）就算落锤（O-54 顺带）。

★ **两处用词订正**（2026-09-09）：① `22000x` 是**宠物**（`PartFlag=32768`），
不是戒指 —— 真戒指是 `x130059` / `x130060`，只有普通数值；② `0x47e776` 应为
**`0x47e778`**（`0x47e775` 那条 `ret 8` 占到 `0x47e777`；虚表 `0x66ca58` 实测）。
③ **DashAttack 的 `0x400` 是有消费点的**，见 §53 末尾 —— 上表那句「没有任何消费」错了。

---

## §53 ★★★★★ 13 种装备加成的**真实语义**逐条查明；提示框只有 2 段（🔍静态，2026-09-09）

**起因**：用户发现「火焰蝙蝠有溅射加成，但说明文是空的 —— 要不是它引发闪退，
我根本不知道」。根因不在数据，在展示：`shopcfg.BONUS_ZH` 只翻了 **5 种**，
客户端认 **13 种**，剩下 8 种被一句注释「客户端界面上没有格子，说明里也就不提」跳过了。

### ① 13 种属性的语义（`GetEquipBonus` `0x407014` 的 **14 个调用点全部逐条对过**）

| idx | 属性 | 调用点 | 算法 / 效果 | 概率门 |
|---|---|---|---|---|
| 1 | Attack | `0x4807a5` | `dmg × (100+x)/100`；正加成置 flags `0x10`、负 `0x800` | **15%** |
| 2 | Defense | `0x480901` | `dmg × (100−x)/100`；置 flags `0x01` | **15%** |
| 3 | Critical | **无** | ★ 见 ② | — |
| 4 | MoveSpd | `0x5074f4` / `0x4fca62` | `speed × (100+x)/100` | 无 |
| 5 | Hp | `0x50a09e` | `MaxHp = 基础 + x`（加法） | 无 |
| 6 | Sp | `0x50a0de` | `MaxSp = 基础 + x`（加法） | 无 |
| 7 | TeamDmg | `0x4807b8` → `0x48084a` | 同队（`[+0x15c]` 相等）且非自己（`0x409f7d`）时 `dmg × (100+x)/100` | 无 |
| 8 | SelfDmg | `0x4807cc` → `0x480884` | 命中自己（`[+0xd0]` 相等）时 `dmg × (100+x)/100` | 无 |
| 9 | AntiGhostCnt | `0x508635` | `cmp 已挡次数, x ; jge` 不够才生效 → `inc` 计数 + `Pet01_GhostBarrier00.efx` ⇒ **一局挡 x 次「幽灵」** | 无 |
| 10 | HeartBoost | `0x522ac0` 掷 → `0x522ae3` | `0x493af5(self, 10316, x)`，`10316 = HeartBoostHpUp`（`Item.ini`）⇒ **捡「心」时额外回 x 点血**；`0x522af1` 某条件下 `add eax,eax` 翻倍 | **15%** |
| 11 | IncSplashRange | `0x47e71d`（发）/ `0x4927c0`（收） | 15% 门 + 武器 `SplashDamage/Range > 0` ⇒ flags `0x200`；收方把溅射范围 `× (1+x/100)` | **15%** |
| 12 | DashAttack | `0x481e40` | `dmg × (1+x/100)`，flags `0x400` | 无 |
| 13 | TeamReflection | `0x50b64e` → `0x50b4ef` | `imul x, 0x3e8` + 道具 `10303`（`반사`）⇒ **给同队队友挂 x 秒反射** | 无 |

常量：`0x693720=1.0f`、`0x693724=0.01f`、`0x69371c=0.5f`、`0x6938cc=0.15f`。

### ② `Critical` 是**彻底的死属性**

名字表里有（`0x65ff68`，idx 3）、显示分支也在（`0x480bd0 test byte [ebp+0xc],2`
→ `0x66d4e0 "CRITICAL!"`），但：三份 `EquipBonus*.ini` **零条**、`GetEquipBonus`
**零调用点**、flags `0x02` 的**产生点全镜像找不到**。⇒ 原版永远不会触发，说明文不提。

### ③ ★ 这 7 种「特效加成」原版**任何界面都不显示**

装备加成面板 `0x4136af` 只画 7 个格式串（`0x65fdac~0x65fee8`：`Attack/Defense/
Speed %+d%%`、`HP/SP %+d`、`Team/Self %d%% %s`），稀有属性一个格子都没有。
⇒ 这就是「火焰蝙蝠有加成却没人知道」的**制度性原因**，不是我们漏了什么。
（❓ `[0x72e440]` 那张表谁填的没查，所以「`+0x14/+0x18` 到底是 Team/Self 还是
Hp/Sp」没定论；属性↔index 的映射是名字表 + 14 个调用点 + INI 三重印证的，不受影响。）

### ④ `EquipBonus` 的值可以是 **Lua 源码**（CN 表里 12 条）

解析器 `0x413208` 拿值的第一个宽字符调 `iswdigit`，不是数字（也不是 `-`）就整格当
Lua，走 `SetEquipBonusFunction`（`0x41324d`）；取值时 `0x4133f2` 把**静态数值和
脚本返回值相加**。胶水代码在 `0x66005d`。CN 表 12 条全是称号：

```
560001 Hp = -GetDefaultMaxHp(...)*0.1        （另有静态 Attack=5）
560002 Defense: GameType==1 and PvpMode==2   → 2
560003 Defense: GameType==1 and PvpMode!=2   → 2
610001~630003  Attack: GetLastBulletROHIdx() == <族号> → 15/5/3
```

族号 = `weapon.ini` 的 `ROH`：`11/12/13xxxx` = 泰尔 / 卡希尔 / 布洛克，
末位 1/2/3 = 武器槽 1/2/3。⚠ `PvpMode==2` 是不是「格斗」**没有实证**，
是按称号原名（격투의 달인 / 슈팅의 달인）反推的。

### ⑤ 按 itemId **硬编码在 exe 里**的效果：只有称号，三条全部查实

代码段里出现过的物品 id 一共 26 个（全表 1870 件逐个搜 4 字节立即数），
其中**上架的只有 `220004`**，而那一处（`0x48e6c1 push 0x35b64` → `0x50a144`
= `Equipment::HasItem`）是**画图标**，不是加成。真正的硬编码效果全在称号槽
（三处都走 `0x50a167` → `0x41528b(-1, 0x2000)`，`0x2000` = 称号的 `PartFlag`；
取「我自己的称号」再包一层 `0x40a129` = `0x409f39()` 取自己角色 → `0x50a167`）。

**★ 2026-09-09 第二轮：三条全部查穿**（用户说称号将来要上架，要求查到底）。

#### `560004` [幸运幸存者] —— 受击时 50% 概率完全免疫

`0x480995 fcomp [0x69371c]`（**0.5f**，不是 0.15）→ `0x4809a4` 取**受害者**
（`edi = [ebp+8]`）的称号 → `0x4809a9 cmp 0x88b84` → `0x4809b4 and [esi],0`
**把伤害清零**，同时置 flags `0x100`（收方画「LUCKY!」+ `Effects/ROH/Efx/LuckyGuy.efx`）。

#### `560005` [手下留情] —— 伤害翻倍，累计造成 **45 点伤害**后失效

★ **45 是伤害点数，不是开枪次数** —— `0x50b404 sub eax, ecx` 扣的是
**翻倍后的伤害值**。苹果雷 D2（伤害 23）翻倍 46 > 45 ⇒ **一发就用完**；
机关枪（伤害 3）每发扣 6，能撑 7 发多。

| 环节 | 地址 | 干什么 |
|---|---|---|
| 发放 | `0x49136e` | 在 `GameContext` 虚表槽 **`+0xc`**（`0x491244`；`GameContext` / `GameContextWaitingRoom` / `GameContextQuest`(`0x4a3ccf`) / `GameContextTutorial` 都挂着它）里逐座位查称号，命中就 `0x50b31c(角色, 45)` |
| 存 | `0x50b339` | `[角色+0x728] = 45`（额度），并挂 `Effects/ROH/Efx/FireTime00.efx`（`0x685568`）|
| 生效 | `0x4808cd → 0x50b3bd` | 在伤害函数 `0x4806bf` 的尾巴上，**要求射手就是本地玩家自己**（`0x409f39()` 取自己的角色，`[+0x2ac]` 座位 == 射手座位 `[ebp-8]`）：<br>`ecx = [伤害] << 1`；`if (额度 < ecx) ecx = 额度`；`额度 -= ecx`；**`[伤害] = ecx`**<br>⇒ **`新伤害 = min(原伤害 × 2, 剩余额度)`，额度按翻倍后的值扣** |
| 结束 | `0x50b3c8` | 额度 ≤ 0 ⇒ 清零 + 摘特效；**一次性**（`[角色+0x71c]` 去重）调 `0x493b5d(座位)` |

⚠ **原版怪癖**：额度见底那一发走的是 `ecx = 剩余额度`，然后 `[伤害] = ecx`
—— 剩余额度**比原伤害还小**时，那一发反而**打得比平时低**（机关枪伤害 3、
只剩 2 点额度 ⇒ 那发只打 2）。以后看到「偶尔一发伤害偏低」别当 bug 查。
| 复活 | `0x502fde` | 额度 > 0 才把特效重新挂上 —— **不重新发放** |
| 取消 | `0x506eb3` / `0x551df3` | `0x50b31c(角色, 0)`，清额度 + 摘特效 |

★ `0x50b31c` 的 `this` 走 **`esi`**（不是 `ecx`），调用方要先把角色放进 `esi`。

#### `560006` [红心达人] —— 捡「心」时 15% 概率全队各回 5 点

在捡心函数 `0x522987`（`__thiscall`，`this` = 心）里：

```
0x5229d6  [捡心者]->vft[+0x12c]([心+0x2bc])   ; 先给捡的人回血（本体效果）
0x5229e1  cmp [捡心者+0x2ac], 我的座位          ; ★ 不是我捡的就整段跳过
0x522a1a  fcomp [0x6938cc]   -> [ebp-2]        ; 15% 掷点（★ 和宠物 heartboost 那道门是**两次独立掷点**）
0x522a2f  call 0x40a129                        ; 我戴的称号
0x522a3a  cmp eax, 0x88b86                     ; 560006 ?
0x522a45  [ebp-0xc] = 5      （0x522a4e: = 10）; ★ 游戏类型==3 或 [session+0x1c]==5 时翻倍
0x522a55  for seat in 0..5:
0x522a66      if [角色+0x2b4] != 0: continue    ; ★ 死了的跳过
0x522a86      if 队伍 != 我的队伍: continue      ; vft[+0x144] 取队伍
0x522a96      0x493af5(座位, 10315 하트, 量, 我的座位)
```

⇒ **含自己在内、同队活着的每个人各回一次心。**

★ 顺带查实：**`[角色+0x2b4] != 0` = 已死亡**（它和当前 HP `[角色+0x150]` 成对写：
`0x4ffbce` 写 `HP=0` 配 `0x4ffbd4` 写 `1`；`0x51f695` 写 `HP=1` 配 `0x51f69f` 写 `0`；
复活函数 `0x503086` 写 `0`）。TeamReflection（§53 ①）那道 `0x50b684` 判的是同一件事。
★ 另：`[角色+0x2b4]` 的 **`+4` 起**才是角色状态哈希表（`0x401c0c` / `0x401b0f` 查/加，
键就是 `Item.ini` 的 `CharAttr`），`+0` 这个字节是独立的「死了没」标志。

#### ⚠ 两条**客户端上行**包，服务端目前没有处理器

| 谁发 | opcode | 载荷 | 组包处 |
|---|---|---|---|
| `heartboost`（青鸟 `220001`，**已上架**）/ `560006` | **`0x040b`** | 4 × int32：`目标座位, itemId(10315/10316), 量, 我的座位` | `0x493af5` → `0x558ec2` |
| `560005` 额度耗尽 | **`0x0415`** | 1 × int32：座位 | `0x493b5d` → `0x558ef6` |

两个 opcode 服务端**只往下发过**（`build_grant_item` 是 1 个 int32、
`0x0415 gspUpdateQuestScore` 是 2 个 int32）—— **上行方向形状不同、也没有处理器**，
落到 `on_game_packet` 只会记一行日志，不会崩。青鸟已经在货架上，实机上大概率
已经发生过。要不要回点什么（比如广播给全房间让别人也看到回血）**没验过**。

### ⑥ 提示框：**只有 2 段**，不是 3 段（订正 §31③）

`0x45c4c9`（商店）/ `0x455534`（仓库）/ `0x45ff65`（合成）都是循环**顶部**的
`cmp i,2 / je 出口`，`i` 从 0 起、尾部 `inc`（`0x45c575`）⇒ **`i` 只取 0 和 1**。

| 标签 | 内容 | 内缩后可用区 |
|---|---|---|
| `ItemInfo0Txt` | 说明第 1 段 | 234 × 82 px ≈ 6 行 |
| `ItemInfo1Txt` | 说明第 2 段 | 232 × 45 px ≈ 3 行 |
| `ItemInfo2Txt` | ★ **不参与切分** —— grant 列表（`0x45c59a` 读 `ShopStock+0x20`，绿色 `0xff22c701`） | 240 × 14 px |

三个提示框类共用 `Data/Ui/ShopToolTipNewUI.ui`（`UiShopToolTip 0x45b84c` /
`UiCabinetToolTip 0x4545fb`），合成用 `CompositionToolTipNewUI.ui`，布局同构。
字体 `SetFont(高 10, weight 400)`（`0x45c516`），行高 = 「가」的高 + 1 px
（`0x5be359` 现量，静态算不出精确值）。

### ⑦ 长度硬闸四道（服务端一个字都不截）

| 闸 | 上限 | 超了会怎样 |
|---|---|---|
| wstr 读取 `0x5d5b3a` 的 `jg`（**有符号**） | 32767 字符 | ★ **整段变空串**，无提示 |
| split 栈缓冲 `0x5d57c8` `wcsncpy 0x3e7` | 整串 999 字符 | 静默截断 |
| 每段 `_vsnwprintf` `0x5be33b` `0x1ff` | 单段 511 字符 | 静默截断 |
| 竖直 bottom `0x5be537` | 画到出框为止 | 多余行不画（但见 ⑧） |

⚠ **说明正文里不能有裸 `|`**（`wcstok 0x5fa904` 吃掉它，`||` 还会被合并）。
`%` 是安全的（文本是 `L"%s"` 的可变参数，`0x45c544`）。**没有任何颜色码 /
富文本标记** —— 颜色是渲染函数写死的一次 `SetColor`。

### ⑧ ⚠ 换行符：引擎认的是**字面 `\` + `n`**，不是 U+000A

`0x5be3e7` 只比对 `0x5c`（`\`）后跟 `n`/`N`，命中就把「量出来的宽」硬设成
「可用宽+1」强制断行。**旁证**：`Chinese.ini` 官方译文用的就是字面 `\n`。

我们现在发的是真 U+000A，画面上看着对，是因为 `ID3DXFont::DrawTextW` 自己会在
LF 处断行（Format `0x100 = DT_NOCLIP`，没有 `DT_SINGLELINE`）。**代价**：外层
`0x5be2eb` 的行计数和 bottom 截断双双失效 ⇒ 段一长就**直接溢出**到别的标签上。

⇒ **决定保持 U+000A**（M5 已实机验过，换掉是拿已验证的东西冒险），
改为在**生成端硬截行数**（`ITEM_DESC_MAX_LINES` 5 / `_2` 3）让它永远走不到溢出。
真要改成字面 `\n` 的话先实机验一次 —— 这条是静态判据，没跑过。

### ⑨ 顺带扫出的同型闪退隐患：`DashDamage` 的绘制支（**已按 D55 补上守护**）

`DashDamage` 有自己的一对虚表槽（`0x66d5dc`：`+0x128 = 0x481dfd` 置 flags `0x400`、
`+0x12c = 0x481e73` 绘制）。`0x481e73` 和火焰蝙蝠那条 `0x47e778` 结构完全一样，
而且**连头射那道判空都没有**：`0x481e92 test byte [ebp+0xd],4` → 中国区分支
`0x481ef3` → `0x481f3d mov eax,[edi]`（`edi` = 目标，可能是 NULL）。
带 DashAttack 的只有 `220004` 迷你机械青蛙，**它已经上架、可合成**。

⚠ **可达性没查实**（`DashDamage` 会不会真以「目标 = 0」进 `0x492715 OnExplode`、
在句柄表 `0x474225` 里登不登记，没查证）⇒ 补丁是**兜底**，不是复现过的 bug。

---

## §54 ★★★★★ `0x0103` **本身就是崩溃报告包**，而且客户端上报完会**把文件删掉**（✅实测，2026-09-09）

原来记的是「客户端先把 `LastCrashReport.txt` 发上来，再发 `0x0103` 收尾」
（V0.1 §44）。**不对。** `0x0103` 自己就是那一发：

```text
int32  有没有报告：0 = 没有，1 = 有
int32  正文字节数
byte[] Dump\LastCrashReport.txt 的正文，原样字节
```

证据（`logs/server-20260909-122222.out`）：12:09 那次登录的 `0x0103` 载荷 **1890 字节**
= `int32(1) + int32(0x756=1878) + 正文`，而 1878 正是当时那个文件的大小；
其余每次登录都是 **8 字节** 的 `int32(0)+int32(0)`。

★★ **上报完客户端就把 `Dump\LastCrashReport.txt` 删了** —— 12:09 之后文件消失，
`BigShot.rpt` 和 `.mdmp` 都不受影响。⇒ 那个文本是**一次性**的。
`crashwatch` 因此在**客户端进程退出的那一刻**就把它读进内存（`read_crash_report`），
打包时用内存里那份，不再读文件（`test_report_text_comes_from_memory_not_from_disk`）。

★ 正文里换行是 `0d 0d 0a`：文件本身 CRLF，客户端又按文本模式读，`\n` 被二次转换。

⚠ 澄清 V0.2 §208 的矛盾：那 14 份云端崩溃遥测来自**这一发**，不是 `0x0b01`
（后者至今未观测）。`re/packet_api.md` 的 `0x0103` 一节已按本条重写。

## §55 ★★★★ 崩溃现场三份文件的关系，以及「哪一份属于这一次」怎么定

| 文件 | 性质 | 这一次的那份怎么取 |
|---|---|---|
| `Dump/LastCrashReport.txt` | **只有最后一次**，下次登录被上报并删除 | 就是它本身 |
| `Dump/BigShot<版本>N<序号>.mdmp` | 累积，`.gitignore` 注释说攒到过 249 MB | ★ 读 `LastCrashReport.txt` 里的 `Dump File Name:` 那一行 |
| `BigShot.rpt` | **追加式**，一份文件里躺着历次崩溃（本机现有 13 段，跨 4 个不同目录） | 按 `==================   logged at ` 切块**取最后一块** |

★ `Dump File Name:` 写的是**绝对路径**，而 `BigShot.rpt` 里躺着别的机器 / 别的目录
留下的历史路径（`D:\work\popshot\`、`D:\git\popshot-reborn\develop\3_Shop_Craft\`）。
⇒ **只取 basename，在本机的 `Dump\` 里找**，不要直接用那个路径。

`logged at` 的格式是 `MM/DD/YY, HH:MM:SS`（美式）。认不出时退回文件 mtime ——
**绝不拿一个畸形字符串去拼目录名**。

★ 读这两份文本必须 `encoding="latin-1", newline=""`：原版是按 **ANSI（CP936）** 写的
（UTF-8 解码会抛异常），而通用换行会把 `\r\n` 悄悄变成 `\n`，包里那份就不再和玩家
机器上那份逐字节相同了。

## §56 ★★★ 认「哪个进程是本次客户端」和「它死了没有」，事件都是现成的

* **PID**：中继 `accept` 到 `127.0.0.1:<peer>` 的连接后，`GetExtendedTcpTable`
  (`AF_INET=2`, `TCP_TABLE_OWNER_PID_ALL=5`) 按 (本地口=peer, 远端口=中继监听口)
  查 owning PID。表里端口是**网络字节序塞在 DWORD 低 16 位**。
* **死了没有**：`OpenProcess(SYNCHRONIZE|PROCESS_QUERY_LIMITED_INFORMATION)` +
  `WaitForSingleObject(INFINITE)`。ctypes 调用期间放开 GIL，干等着不挡别的线程。
* **会话起点**：同一个句柄 `GetProcessTimes`，FILETIME 减 `116444736000000000`
  再除 `1e7` = Unix 时刻。**挑「本次运行的日志」的那条边界就是它**，不是拍的时间窗。
* **退出码**：同一个句柄 `GetExitCodeProcess`（崩溃时是 `0xC0000005` 之类）。

★★ **进程对象被置位 ⇒ 进程已完全终止 ⇒ 它所有文件句柄都被 OS 关闭**
⇒ `.mdmp` / `.rpt` / `LastCrashReport.txt` 必定写完且没被占着。
**这条排除了「读到半截文件」的竞态，所以整条链上一个 `sleep` 都不需要。**

`hook/bsloader.c:383` 早就在做同一件事（`WaitForSingleObject` + `GetExitCodeProcess`），
但它把结果只打进 `logs/bsloader.out`，没人读。

## §57 ★★★ `logs/bshook_*_pid<PID>.log` 的文件名里带游戏进程 PID

`hook/bshook.c:4956` 的格式串是 `bshook_%Y%m%d_%H%M%S_pid%u.log`。
⇒ 「挑出本次会话的客户端日志」是**精确匹配**，不用按时间窗猜。

同理 `game_patched/UserConfig.ini` 里有 `LastLoginId=<上次登录框里输的内容>`：
⚠ **它不是注册账号名** —— 原版**登录失败也会写**，内容可以是中文、空格、`../`、`*:|`，
而且是 ANSI 编码。当客户端 ID 用之前必须过白名单清洗（见 D59）。

## §58 ★★ minidump 的压缩比实测（2026-09-09，本机那份 35.2 MB 的 `BigShotV0311N001.mdmp`）

| 方式 | 结果 | 用时 |
|---|---|---|
| `zlib` level 6 | 8.6 MB | 0.4 s |
| `zlib` level 9 | 8.4 MB | 1.1 s |
| `lzma` preset 6 | 5.7 MB | 7.7 s |

⇒ 取 `ZIP_DEFLATED` + `compresslevel=6`：lzma 只多省 2.9 MB 却要多花 7 秒 CPU。

整包实测（mdmp 35.2 MB + 崩溃那局的 bshook 日志 11 MB + 其余）
→ **11.6 MB / 0.5 秒**；本机环回上传 **0.36 秒（32 MB/s）**。

---

## §59 ★★★★ 提示框里那行**绿色加成文字是客户端自己画的**，和服务端无关（✅实机 + 🔍静态，2026-09-09）

用户实机看完 §53 那版说明文后报了 5 个现象。**5 个里有 4 个不是我们改出来的** ——
那行绿字是原版自己的东西。

### ① 绿字从哪来：客户端按 itemId 查**本地** `EquipBonus-Chn.ini` 现生成

```
0x455605  mov  eax, [ebx+0xd0]      ; ItemInfo*
0x45560b  push [eax+4]              ; itemId
0x455628  call 0x414128             ; ★ 生成加成文字列表
0x45563e  ...逐条拼成一串...
0x455687  mov  ebx, [ebx+0x10c]     ; ItemInfo2Txt
0x4556ce  push 0xff22c701           ; 亮绿
```

`0x414128` 内部：`0x41414d mov eax, 0x72e440`（EquipBonus 显示表）→
`0x417a76` map.find → 查得到才 `0x4136af` 格式化。⇒ **服务端一个字都改不了。**

### ② ★★ **有两个生成器**，五个消费点（2026-09-09 实机订正）

第一版只找到 `0x414128`，堵完用户报「商店浮窗里还有绿字」—— 商店走的是**另一个**。

| 生成器 | 参数 | 消费点 | 界面 | 绿色怎么设 |
|---|---|---|---|---|
| **`0x414128`** | 单件 `itemId` | `0x455628` | `UiCabinetToolTip` ★ **一个类被大厅仓库 / 房间内背包 / 商店页里的仓库格共用**（§31 那批调用点）| `0x4556ce push` |
| | | `0x460059` | `UiCompositionToolTip`（合成）| `0x4600ff push` |
| | | `0x450614` | `Data/Ui/RepairItemUI.ui` 的 `ItemEffectText` | `0x45067d mov [eax+0x4c],` ★ 不是 push |
| | | `0x45078d` | 同上，`DlgRepairItem` 虚表槽 `+0x4` | — |
| **`0x4141AC`** | **id 列表**（把多件的加成**累加**起来）| `0x45c5fa` | ★ **`UiShopToolTip`（商店）** | `0x45c69b push` |

**商店那条**：`0x45c59a` 先把 `ShopStock+0x20` 的「买了会得到什么」列表（每元素 16 字节、
取 `[+4]` = itemId）收集出来，再交给 `0x4141AC` 求合计加成。⇒ 礼包 / 套装那种
「一件商品给你好几样」的合计属性。**我们没填 `+0x20`，但商品自己那一件在列表里**，
所以照样有绿字。

★ 另有 `0x45d1BF` 直接调格式化器 `0x4136AF`，输出进 `[UiComposition+0x57c]` 一个标签
（合成**面板**自己的，**不是绿字**）—— 没动它。

⇒ 全查法：绿色常量 `0xFF22C701` 在代码段出现 **7 处** —— 3 处是三个提示框构造函数里的
默认色（`0x4549C9` / `0x45BBB9` / `0x45F828`，`mov eax,` 形式），3 处是提示框绘制
（`push` 形式），1 处是修理界面（`mov [eax+0x4c],` 形式）。**按 `push` 扫会漏掉后者。**

### ③ 绿字自带三个原版缺陷，**全在 pak 里，服务端改不了**

| 现象 | 根因 |
|---|---|
| 「速度+d%」 | `Chinese.ini` 错字：`Spd%+d%%=速度+d%%`，**漏了 d 前面的 `%`**（长格式 `Speed %+d%%=速度 %+d%%` 是对的，短格式坏了）|
| 称号上出现「Team 5% Down」 | `Chinese.ini` **漏翻 3 条**：`Team %d%% %s` / `Self %d%% %s` / `Down`（只翻了短格式 `Team%d%% %s=团队…` 和 `Dn=降低`）|
| 换行溢出到下面的框 | `ItemInfo2Txt` 在 `.ui` 里只有 **240×14 = 一行**，属性一多就换行顶出去 |

### ④ 「绿字错位到下面」不是错位 —— 三个框的位置本来就是这样

`ShopToolTipNewUI.ui` 里写死：第 1 段 `ItemInfo0Txt` y=165(h88) →
**绿字 `ItemInfo2Txt` y=256(h14)** → 第 2 段 `ItemInfo1Txt` y=274(h51)。
绿字永远夹在两段中间；第 2 段空着时看上去就像「掉到下面去了」。

### ⑤ ★★ `.ini` 加载器**只读 pak，散文件不生效**（把 §1 那个 ❓ 结掉）

```
0x413ef5  call 0x55e7f0      ; 这个文件在 pak 里吗
0x413efa  ...neg/sbb/inc...  ; bl = 不在 pak 里 ? 1 : 0
0x413f13  jne 0x4140d2       ; ★ 不在 pak 里 -> 0x4140d2 = **函数尾，直接 return**
```

⇒ 往 `game_patched/Data/` 放一份改过的 `Chinese.ini` / `EquipBonus.ini` **没有任何效果**，
要改只能重打 `.pkn`（项目里没有打包工具）。**§1 结尾那条「真要改数值时先实测散文件能否覆盖」
到此有答案：不能。**

### ⑥ 处理：**两处各一个字节**关掉绿字（D65）

打在**两个生成器**上，不是五个绘制点上。两处的循环体一模一样
（`map.find(itemId)` → `test` → `je 跳过`），把 `je` 改成 `jmp` 即可：

| 补丁点 | 原字节 | 改成 | 效果 |
|---|---|---|---|
| `0x41415E`（`0x414128` 里）| `74 20` | `EB 20` | 永远返回**空表** |
| `0x414208`（`0x4141AC` 里）| `74 07` | `EB 07` | 永远**不累加** ⇒ 累加器保持全 0 |

后者要成立还得有一条：`0x4136AF` 对**每一项都判零**才输出
（短格式 `0x41379D` 那一串、长格式 `0x413A7F` 那一串，两条分支都判）
⇒ 累加器全 0 时一条都不输出，同样是空表。✅ 已逐条核对。

★ 这两刀几乎零风险：走的就是**原版每天都在走**的那条路 —— 武器 / 材料 / 纯外观装备
在 `EquipBonus-Chn.ini` 里都没有条目，本来就走 `je` 这一支；SEH 状态 `[ebp-4]`
两条路完全一样，vector 的构造 / 拷贝 / 析构一步不少。
`hook/bshook.c` 的 `try_patch_hide_client_bonus_text()`（一张两条的表，
**两处都打中才算成功**），`BSHOOK_KEEP_CLIENT_BONUS_TEXT=1` 可回到原版。

### ⑦ 教训：**「按 push 扫常量」和「找到一个生成器就收工」都会漏**

第一版只扫 `push 0xFF22C701` ⇒ 漏了修理界面那处 `mov [eax+0x4c],`；
只跟到 `0x414128` ⇒ 漏了商店走的 `0x4141AC`，用户实机一眼就看见了。
⇒ 以后堵这类「同一份信息有多个入口」的东西，判据要落在**数据源**
（这里是 `[0x72e440]` 那张表：`xref` 出 11 个引用，逐个分类），别落在「我找到的那个入口」。

---

## §60 ★★★ 打包**自检**是「真的开一次服」—— 启动路径上任何「缺文件就地生成」都会落进包里（2026-09-09）

**现象**：用户发现 V0.3.0 的两个包里 `server\data\` 不再是空目录，躺着
`items.json` / `shop.json` / `recipe.json` / `drops.json` 四份。V0.2.9 的包
（9/7 22:01）还是空的。

**不是打包脚本被改坏**：`build-common.ps1` 全文只有一句
`New-Item -ItemType Directory ... 'data'`，没有任何地方拷 `server\data`。
弄脏它的是打包的最后一步 —— `Invoke-ServerSmokeTest` 用包里的 Python 跑包里的
`server\app.py`，**工作目录就是包根**。而 V0.3 商店给启动路径挂上了
`shopcfg.ensure_files()`（`app.py._report_shop_config`），`shopcfg.DATA_DIR` 又是
**钉死在 `shopcfg.py` 同级**的 `data\` —— 于是四份默认配置就地生成在包里。

**为什么以前没事**：V0.2.9 及以前的包里连 `shopcfg.py` 都没有。商店那条线
2026-09-08 13:40 才合进 `main`，V0.3.0 是**第一个带商店代码的包**，一打就带出来了。
要指一次改动的话是把 `ensure_files()` 挂上启动路径的那次（`06907f04`，9/4 在分支上），
但真正显现是合进 main 之后的第一次打包 —— **写代码那次和出事那次隔了 5 天、
中间还夹着十几次无关提交**，所以别拿「最近改了啥」去猜。

**自检其实已经躲开了别的坑，唯独漏了这个**：`accounts.json` 有 `--accounts` 指去
临时目录、备份线程有 `--no-backup`（那行注释明写着「免得往包里的 `server\data\`
写备份」）—— 但 `ensure_files` 那时没有出口，`app.py` 压根没有 `--data-dir`。

**判据（怎么分清「拷进去的」还是「跑出来的」）**：把包里的 `server\` 拷到临时目录、
清空 `data\`、照自检那条命令行跑一遍。四个文件原样长回来，日志自己招：

```
[app] 商店配置: 新建了 items.json、shop.json、recipe.json、drops.json（在 …\server\data；…）
```

另外这四份和 `shopdefaults` 现生成的**默认模板逐字节一致** —— 说明是现生成的，
不是从开发机的 `server\data` 拷进去的。

**危害**：`server\data\` 只装用户数据。随包发出去，开服的人「解压覆盖老目录」升级时，
管理页改过的定价 / 配方 / 掉落会被默认模板盖掉 —— 正是 `ensure_files` 的
D7「已存在的一律不覆盖」要防的那件事（铁律 11）。全新部署倒是没差（内容就是默认模板）。

**教训**：凡是挂在服务端**启动路径**上的「缺文件就生成一份」「到点写一份」，
都要先问一句「它写到哪儿去、那个位置在不在包里」。这类漏子在打包机上**一点症状都没有**
—— 包打出来一切正常，装到别人机器上覆盖升级才发作。处理见 D66。

## §61 ★★★ `server.config` 随包发出去的模板**有两份**，服务端包那份是手写的（2026-09-09）

**结论**：加了服务端侧的配置项只改 `server/config.py` 的 `DEFAULT_CONFIG_TEXT`
是**不够**的 —— 那只喂客户端包。服务端包发的是
`tools/server-package/server.config`，一个**独立手写**的文件，没有任何东西
从前者生成它、也没有任何东西核对两者。

**证据**：

- `build-portable.ps1`（客户端包）拷 `config\server.config`，那个文件
  `.gitignore` 掉了，没有时照 `config.py` 的 `DEFAULT_CONFIG_TEXT` 现生成
  —— 所以客户端包那份**永远是新的**。
- `build-server-package.ps1` 拷的是 `$Template\server.config`，
  `$Template = tools\server-package\` —— 和 `config.py` 毫无关系。
- 实测：`config/server.config` 与 `DEFAULT_CONFIG_TEXT` 逐字节相同；
  同一时刻服务端包模板里 `crash_max_upload_mb` / `crash_upload_cooldown_seconds` /
  `crash_keep_days` 三个键**一个都没有**（服务端因此一直吃默认值，
  开服的人看不到也改不了）。

**这不是第一次**：`log_retention_days`、`backup_*` 当初也是隔了一轮才补上，
头部注释到 2026-09-09 都还写着「只有两个需要改的东西」。**分开维护 + 没人核对
= 必然漏**，靠记性堵不住。

**⚠ 反过来也不对**：不是「把 `DEFAULTS` 里的键全抄进服务端包模板」就完事。
`server_address` / `proxy_*` / `crash_upload` 是**客户端侧**读的，写进去只会误导
开服的人；`udp_sync` / `udp_sync_redundancy` 服务端确实读，但**故意不写**
—— 它是对照测试用的开关，README 的端口表对开服的人只说「UDP 27799 必须放行」。
所以判据得**逐键说明**，不能靠猜。处理见 D67（铁律 13）。

## §62 ★★★★ `tools/server-package/` 里**几乎每样东西都有「另一边」**，只有三样是单源（2026-09-09）

**结论**：`server.config`（§61）不是孤例。服务端包的模板目录里十个文件，
**只有一个**（`server.config`）现在有守卫，其余全靠人记得。逐条清点如下 ——
✅ = 单源或有测试盯着，❌ = 分开维护、没人核对。

| 服务端包里的东西 | 另一边是谁 | 防漏 |
|---|---|---|
| `server.config` | 客户端包那份（= `config.py` 的 `DEFAULT_CONFIG_TEXT`） | ✅ `SERVER_PACKAGE_KEYS` + `test_online.py` 两条（D67） |
| `README.md` | 仓库根 `README.md`（客户端包发的就是它） | ❌ 崩溃上传那一节就只写进了根 README |
| `start.bat` / `start-debug.bat` / `stop.bat` | 仓库根同名三个 | ❌ 开头 11 行 `chcp 65001` 探测块**六个文件逐字节相同**，改一处要改六处 |
| `start.sh` / `start-debug.sh` / `stop.sh` | 同目录的三个 `.bat` | ❌ 同一组入口的两个平台版本 |
| `serverctl.ps1` | `serverctl.sh` | ❌ 同一件事两种语言各写一遍：挑 Python、日志归档、端口占用检查、读注册页端口、拼 `app.py` 参数 |
| 读 `local_register_port` 的那段解析 | `launch.ps1` / `shutdown.ps1` / `serverctl.ps1` / `serverctl.sh` **四份**手写，其中三处注释明写着「解析规则和 `server/config.py` 一致」 | ❌ |
| `app.py --no-tcp-relay` | `launch.ps1` 也必须传（客户端的本机服务端） | ❌ 少传一边就变成「一边有 27798 一边没有」 |
| 端口号 | `server/config.py` 唯一源，`serverctl.*` 走 `python config.py --ports` | 🟡 `test_ports.py` 只盯 `ports.h` / `bshook.c` / CLI；**PowerShell 和 sh 里有没有写死字面量没人查** |
| `server-ClientFilter.config` | 两个包都从仓库根 `config/` 拷同一份 | ✅ 单源 |
| `wincompat.ps1` | 两个包都从仓库 `tools/` 拷同一份（`Move-LogAside` 等就在里面） | ✅ 单源 |
| `server/` 代码 | `Copy-ServerCode` 拷同一份（铁律 8） | ✅ 单源 |

**⚠ 清点时发现两处**已经**漂了的**（都不是这次改动引入的）：

1. **27798 其实早就不监听了，三处文档还让人放行它。**
   `serverctl.ps1` / `serverctl.sh` / `launch.ps1` 现在**都**传 `--no-tcp-relay`
   （`0c0d7bac`「关掉原版 rcp 中继服」），`app.py:495` 走的是 `log("中继服 已关闭")`
   那一支，根本没有 `_start("relay", ...)`。但
   `tools/server-package/README.md` 的 `netsh` 命令行（第 28 行）和端口表
   （第 62 行）、根 `README.md`（第 32 / 468 行）、`build-server-package.ps1`
   写进 `BUILD.ver` 的说明，四处都还写着「要放行 TCP 27798」。
   ⇒ 那次改动只动了脚本，四份文档一份没跟。放行一个没人听的口不会出故障，
   所以**至今没人发现** —— 正是这一类漏子的典型症状。
2. **`tools/shutdown.ps1` 里还有 7 个端口字面量**
   （`@(47611, 27799, 27798, 27800, 47621, 27809, 27808)`）。
   `launch.ps1` 头上明写着「以前这个脚本里有 9 个端口字面量……现在只有一个源」，
   统一那一轮**漏了 `shutdown.ps1`**，`test_ports.py` 又只管 C 那边，于是留到现在。
   值目前和 `port_table()` 一致，但没有任何东西保证它继续一致。

**教训**：判断「这个文件要不要跟着改」的正确问法不是「它在哪个目录」，
而是**「这份包和另一份包，哪些东西必须说同一句话」**。上面那张表就是答案，
加东西时对着它过一遍。处理见 D67。

---

## §63 ★★★★★ 装备**外观**也走 `0x030b`，而且是**按座位**的 —— 服务端只发给本人 ⇒ 别人全看不见（✅静态落锤 + 🔍代码，2026-09-09）

**用户现象**：自己穿的装备，房里 / 局里别人看不见。

**根因一句话**：`0x030b` 全服务端**只有一个发出点**，而它永远是
`座位 = self.my_seat`、`清单 = self.account`、`收件人 = self`
（改前 `gameserver.py:6570` 那一句 `self.send(...)`）。
⇒ 每个客户端手里只有**自己那一格**的清单，别人那五格恒空 = 没穿装备的样子。

### 外观这条路（这一节新查明的部分）

```
0x030b(座位, 掩码×3, itemId[]) → 处理器 0x406ea1
    ├ 写 [LobbyStage + 座位*4 + 0x250]        ← 加成读的也是这一格（§1）
    └ 0x406f42(座位, LobbyStage)
         ├ 0x404ff6 → [LobbyStage + 座位*4 + 0x1d0]   该座位的 3D 角色对象
         ├ 0x506251 清掉它身上已挂的装备（+0x2e4 / +0x348 / +0x3ac / +0x3b0）
         └ 逐件 0x505bb9(itemId)  →  0x5059c0 真的挂上去
```

**`0x505bb9` 查的是 `[0x72e1e0]`，不是服务端喂的 ItemDB `[0x72e1dc]`（§28）。**
`[0x72e1e0]` 由 `0x41652d` 在**启动时**从 pak 里的 `ShopItem-Chn.ini` 读进来
（文件名串 `0x6602a8`，经 `0x55e7f0` 那个 pak 加载器），键是 itemId、
挂着 `Tag`（角色网格路径，§51）。
⇒ **服务端只要给 itemId 就够**，不用也没法下发模型路径；
⇒ 观察者的客户端**不需要**先查过这件物品的定义（`0x0501`），外观照样出得来。

角色限定在 `0x505bb9` 里现算：`key = itemId/1e6 - 1`，`-1` 或等于
`[角色+0x2b0]` 才挂 ⇒ 一次把某人三个角色的装备全发过去是安全的，
客户端自己按那一格当前的角色挑。

**战斗里同一份**：`0x493258` 建完座位角色紧接着就是 `0x406f42`
（`0x49325d` 起，全局 `[0x72e29c]`）⇒ 房间里对了，进图也就对了。

### ⚠ 座位清单是**跨房间活着的全局**

`LobbyStage`（`[0x72e29c]`）不随房间销毁，`[+0x250+座位*4]` 里那份清单
**没人覆盖就一直在**。而 `0x0301` action 0（`0x4066a5` 建 3D 角色 →
`0x4066ae` 调 `0x406f42`）会立刻拿那一格的清单去挂装备。
⇒ **每次某一格换人都必须重发一次，bot 那一格也要发（空清单）**，
否则 bot / 新人会穿着**上一个坐这儿的人**的装备站在那儿。

### 结论：要发的时机（改后全在 `gameserver.py`）

| 时机 | 发给谁 | 函数 |
|---|---|---|
| 进房 | 只给进房的人，**每个有人的座位各一发** | `send_room_equipped_lists()` |
| 有人进房 | 广播给房里其他人（排在 `0x0301` action 0 **之后**）| `announce_join()` |
| 穿 / 脱（`0x0702` / `0x0703`）| 本人 + 广播 | `on_req_equip_item()` |
| bot 坐进某格 | 全房间（含房主），**空清单** | `bot._add_one_bot()` |
| 结算看完回房间（`0x0405`）| 只给自己，**每个有人的座位各一发** | `leave_game_result()` |
| 调试 `equip` / `unequip` / `sync-account` | 本人 + 广播 | `handle_control_command()` |

★ 客户端**不校验**「这个座位号是不是我自己」—— 处理器直接拿包里的座位号
算 `0x250` 的下标，所以六格都能发。

---

## §64 ★★★★★ 岩浆巨龙**永久无敌**是客户端的时序赛跑：放弱点剧情的窗口只有「boss 正在放出生动画」那 3.84 秒（🔍静态落锤 + ✅日志佐证，2026-09-10；会话 27 逐条复核并修正）

**用户现象**（线上，玩家 `Jungle音` / 账号 `d314010739`）：闯关「岩浆巨龙」
（quest id 2）进 boss 房后，**有时**boss 一直无敌、子弹全弹开，
「布洛克分析出弱点」那段剧情不放，关卡再也推不下去。

### 日志把「零伤害」钉死了（`bug调查/16/logs_server/server.out`）

`08:11:56` 换图进 `Quest02_2` → `08:15:09` 放弃，中间 **3 分 12 秒一发
`0x0410` 都没有**。`0x0410` 只在**打到怪**时发、累计值就是伤害（§130）
⇒ 全程零伤害。当天进 `Quest02_2` 的另外四局（`#6` / `#5` / `#18` / `#19`）
都在进图 **17~22 秒**后开始出 `0x0410` ⇒ 不是「打不动」，是「打不了」。
**服务端在 boss 房里既没收也没发别的包 —— 病在客户端。**
★ 服务端日志分不清「Tracing 没放」和「放了但 boss 仍无敌」，这只有客户端的
DRAKA 日志能分（见本节末）。

### 病根（地址是 `re/BigShot_*.img` 的 VA）

boss 的**阶段**在 `[boss+0x2a8]`：构造函数 `0x4b1812` 置 **-1**（石壳护体、
免疫伤害），`0x4b1837` 把状态名 `[boss+0x2b0]` 置 `"birth"`。
把 -1 抬成 0（可以打）的**全客户端只有一处**：任务脚本命令 `$공략포인트`
（Quest02 的处理器 `0x4a76ad`：`[boss+0x2a8]=0` + `vf_124(0)`），
而它**只出现在 `Data/Quest/Quest02/Quest02Tracing`** 这一份剧情里。

放那份剧情的是关卡舞台的**每帧检查** `0x4a7080`，四道门缺一不可：

| # | 判据 | 备注 |
|---|---|---|
| ① | `[stage+0x560] != 0` | boss 已生成 |
| ② | `[0x72e260]->[+0x14] == 0` | **当前没有剧情在播** |
| ③ | `0x4e71c0(剧情播放器) == 3` | 播放器空闲 |
| ④ | `[stage+0x5fc] == 0` 且 `boss->vf_154(4)` | 一次性标志 + boss 说「该放了」 |

**④ 里那个 `vf_154(4)`（`0x4b5a04`）判的是 `[boss+0x2b0] == L"birth"`
—— 一个只活 `BirthTick`(=120) 个游戏刻的瞬时状态。**
刻长 `[0x6dc528]` = **32 ms**（和服务端 32ms 循环同源），120 刻 = **3.84 秒**；
刻数追墙钟（`0x42b4c3`：`tick = (now-t0) / 32`，落后就在同一帧里**无上限**补跑）。
出生一结束，`vf_140("think")` 在 `0x4b1c54` 看见阶段还是 -1，把状态切成
`"idle1"` ⇒ `vf_154(4)` 恒假 ⇒ **Tracing 永远不再放，阶段永远停在 -1 = 永久无敌。**

### ★ 「有时」是谁吃掉了那 3.84 秒 —— **没查清**（会话 27）

会话 26 给的两条解释已被证伪，别再引用：

* **不是 `Quest02Boss` 的尾巴。** `$createboss` 后面 `fadeout 3000` / `clear` /
  `fadein 3000` / `wideoff` 的 `Wait=` 都是空的；脚本引擎里 `fadeout` / `fadein`
  （`0x4e6507` / `0x4e653d`）只把参数交给画面管理器 `[0x72e2d8]` 就返回，**不等**。
  剧情几帧内就结束。真要阻塞 6 秒，窗口会**每次**错过，与四局成功矛盾。
* **不是 `Quest02Lava` 插队。** 代码字符串、地图文件都没有引用它，是死数据。
  Quest02 舞台代码（`0x4a706b`）只放 Tracing / Shell / Change / Phase2 / Phase3 /
  Clear / Intro；`Quest02Boss` 由 `Quest02_2#*.map` 当开场脚本引用。

还没证实的候选：`$createboss` 同步加载 boss 模型 / 特效造成卡帧，追赶循环一帧内
补跑 ≥120 刻，出生在检查跑到之前就过期。要钉死它得看下次出事时的 DRAKA 日志。
（舞台检查是舞台 vf_80 = `0x4a74f5` 在关卡状态 `[+0x3b0]==2` 时调的；它相对
每刻的 boss 更新是先是后，本会话没追到底。）

### ★ 只有 Quest02 会这样，别的关卡不用管

| 关卡 | 为什么不受影响 |
|---|---|
| Quest01 | 判据是 `vf_154(0)` = `[boss+0x2f0] > 5`（挨了几发的**计数**，不过期）；且没有 `$createboss` |
| Quest03 | 检查 `0x4a7ee0` 压根不问 boss 状态，只看 `[stage+0x3b0]==2` |
| Quest04 | boss 预置在图上，没有 `$createboss` |
| Quest05 / Quest06 | `$공략포인트` 在 `Phase2` 里，判 `阶段==1`（持久事实）|
| Quest07 | Map02 / Map03 的 `$createboss` 和 `$공략포인트` 写在**同一份**脚本里挨着执行；**Map01 不是**（`Map01Intro` 末尾 `$createboss`，`$공략포인트` 在 `Map01Phase2`），那只 boss 是否同形**没验** |

### 另外几条顺手查明的硬事实

* **`vf_154(n)` 是 boss 的「进度问答」**（Quest02 那只在 `0x4b59a6`）：
  `4`=该放 Tracing、`5`=某个部件碎了、`6`=`[+0x3fc]`、`8`=`阶段==1`、`9`=`阶段==2`。
  舞台检查 `0x4a706b` 按这五个答案分别放 Tracing / Shell / Change / Phase2 / Phase3，
  各有一次性标志 `[stage+0x5fc..0x5ff]`，放的那一帧就置位。
* **游戏刻 `[GameContext+0xd4]` 追的是墙钟不是帧**（`0x42b4c3` 里
  `tick = (now - [+0xcc]) / [0x6dc528]`，落后就在同一帧里补跑到齐）⇒
  「等 N 刻」= 等 N × 32 ms 的**真实时间**，卡帧不会让它变慢。
  GameContext 指针：`[[0x72e2b4]+8]`（`0x409f0e`）。
* **`vf_124(n)`（`0x4dbdc3`）= 点弱点**：先把所有部件的 `[+0x2a9]` 置 1（弹开），
  再按 n 把对应部件的 `[+0x58]` 清 0（可打）；`n=-1` 谁也不清 ⇒ 石壳免疫就是它。
* **脚本 `Wait=pause` 是限时等待**：`WaitTime` 为空时取默认，再没有就 2000 ms
  （`0x4e4d6c`），不是等玩家点击 ⇒ 剧情长度是确定的，玩家拖不长。
* **DRAKA 诊断日志**（`hook/bshook.c`，四个中途内联 hook，`BSHOOK_DRAKA_DIAG=0` 可关）：
  `boss 创建`（0x4b1812）/ `门开`（0x4a70b4：门①②③过了且 Tracing 未放，按状态名
  翻转去重）/ `Tracing 入队`（0x4a712d）/ `$공략포인트`（0x4a76c9），每行带游戏刻和
  距创建的刻数。**判读**：门开那行 `状态=idle1` = 出生窗口在检查之前已过期；
  有「Tracing 入队」却没有「弱点点出」= 病在别处。

---

## §65 ★★★★★ **崩溃报告器自己会崩** —— 出错地址不在任何已加载模块里就死在 `0x5D76CD`（✅本机 A/B 逐字节实证，2026-09-10）

**先说后果**：这一条不是「又一个闪退」，是**我们为什么看不见闪退**。
bug调查/17 收到的 5 份线上现场里有 **3 份**报告只有开头四行、`.mdmp` 根本没生成 ——
不是上传丢了，是客户端**没写出来**。

**根因**（写报告的 `0x5d7b90` → 找模块的 `0x5d7695`）：

```text
005d76a6  VirtualQuery(出错地址, &mbi, 0x1c)      ; 失败 -> 返回 0
005d76b8  edi = mbi.AllocationBase
005d76bf  GetModuleFileNameA(edi, 名字缓冲区, 0x104)  ; 失败 -> 返回 0
005d76cd  eax = [edi+0x3c]                        ; ★ edi == 0 时读空指针
```

出错地址落在**未提交 / 已释放**的页上（= 通过野函数指针 `call` 跳飞，最常见的崩法）
⇒ `AllocationBase == 0`，而 `GetModuleFileNameA(NULL, …)` 按 Win32 语义返回的是
**主模块路径、非 0**，判断被骗过去，`[0+0x3c]` 当场再崩一次。崩在异常过滤器里
⇒ 报告写到一半停住、`MiniDumpWriteDump` 压根没轮到。

**断点是确定的**：报告的最后一行永远是 `Exception code: …\r\r\n`，
因为那之后到 `Fault address:` 之间**只有 `call 0x5d7695` 这一个调用**。
三份线上现场（238 / 268 / 285 字节）全断在这里，一个字节不多。

**顺带第二个毛病**：三个出参（模块名 / 节号 / 节内偏移）原版**从不初始化**，
两条失败路径都直接 return，调用方照样打 ⇒ `4DB8E501 00:00C5022C \x02` 这种栈上垃圾。

**A/B 实证**（本机 2026-09-10，`CreateRemoteThread(起始地址=0)` 造一次 EIP=0 的崩溃）：

| | `LastCrashReport.txt` | `.mdmp` |
|---|---|---|
| `BSHOOK_KEEP_RPT_CRASHES=1`（原版） | **238 字节，断在 `Exception code` 后** | **没生成** |
| 打了补丁 | 982 字节，寄存器 + 完整调用栈齐全 | 12.6 MB 已生成 |

**修**（`hook/bshook.c` `try_patch_crash17_guards()`，偷 `0x5d76b4` 起 7 字节）：
① 先把三个出参填成「不在模块里」的合法值（名字 = 空串、节号 = 0、偏移 = 地址本身），
之后**任何**一条失败路径打出来的都是 `<地址> 00:<地址>`；
② `AllocationBase == 0` 直接走原版失败出口 `0x5d76c9`。

★★ **推论**：那 3 份断头现场 = **出错地址在未映射页** = 通过野指针 call 过去的，
和 §66 那次 `PRIV_INSTRUCTION` 是同一族（跳进堆里执行垃圾 = 特权指令，
跳进未提交页 = 访问违例）。⇒ 5 次线上闪退里 **4 次是同一类**。

## §66 ★★★★ 崩溃 A：`[Character+0x57C]` 的 `DashDamage` 是野指针（🔍 dump 落锤，2026-09-10）

**现场**（09930195，10:12:48，`C0000096 PRIV_INSTRUCTION @ 4DB8E501`）。栈自内向外：

```text
0x4814f2 (DashDamage 的一个方法)   ← 崩在 00481a51 mov eax,[esi] / 00481a53 call [eax+0x78]
0x5077c6 Character::ProcessDash     ← 00507962 mov ecx,[ebx+0x57c] ; 00507970 call 0x4814f2
0x506fed Character::ProcessMove     ← 里面 0x5074f4 就是 MoveSpd 那个 GetEquipBonus
0x4904cc GameContext::Process        （函数名从 0x5d601f 那套 .cpp/行号 探针串还原）
```

`esi = ecx = 0x3CB75A70`（对象），`eax = [esi] = 0x425DECA0` —— **不是虚表**
（`DashDamage` 的主虚表恒为 `0x66d5dc`，构造函数 `0x481389` / `0x4813e6` 写死）。
⇒ 这一格**非空但已经不是对象了**。exe 是 2007 年的、没开 DEP，
`call` 直接跳进堆里执行垃圾字节 ⇒ `PRIV_INSTRUCTION`。

**这一格的生命周期**：`Character::StartDash`（`0x5020d9`）`new(0x30c)` + `0x481389`
造出来存进 `[Character+0x57c]`；销毁走虚表 `+0x20`（`0x512183`）——
注意它**不一定立刻 free**：对象在句柄管理器 `[0x72e2d4]` 里登记过的话
只是 `SetState(4)` 标成「待删」，**真正释放由管理器做**。
全 exe 只有三处解引用这一格，**三处都不校验**：

| VA | 在哪 | 干什么 |
|---|---|---|
| `0x502182` | `StartDash` | 开新的之前拆旧的（`call [eax+0x20]`）|
| `0x50794a` | `ProcessDash` | 画拖影（`call 0x4814f2`）★ 崩的就是它 |
| `0x5079aa` | `ProcessDash` | 冲刺结束拆掉（`call [eax+0x20]`）|

**谁把对象放掉的还没查到**（dump 里没有堆，两个玩家都复现不了）。
⇒ 现在是**兜底 + 探针**：三处各补一个「虚表必须是 `0x66d5dc`」的校验，对不上就
当这一格没有、跳过；**不写回、不释放**（万一是 `Character` 自己成了野指针，
往 `[ebx+0x57c]` 写等于二次破坏）。触发时按**指针值翻转**去重打一行
`★DASH` 日志 —— 下一次现场靠它就能分辨「真踩到了」还是「另有病灶」。

## §67 ★★★ `0x0411 gspEndGame` 处理器不判空：关卡外收到就闪退（🔍静态 + 本机 `BigShot.rpt` 09-10 00:05:39）

```text
005518de  mov esi,[0x72e2dc]     ; 当前 GameContext —— 不在关卡里就是 NULL
005518e4  call 0x4913fc          ; 弹结算界面，第一句：
0049140c    cmp byte [esi+4], bl ; ★ esi == 0 -> C0000005
```

`[0x72e2dc]` 只在关卡里非空 —— `Character::StartDash`（`0x5020e6`）自己就先判了空，
这条路上原版忘了。**任何一发在关卡外到达的 `0x0411` 都会闪退**：
控制通道的 `clear` / `endgame`（`tools/quest-clear.bat` 调的就是它）人不在关卡里时
发一发就能复现；线上则是「结算包比舞台拆除晚到」。
本机 09-10 00:05:39 那次 `BigShot.rpt`（`0049140C`，`ESI=0`，栈
`0x40ee5d`(WndProc) → `0x5bc176` → 虚表 ×2 → `0x54e39d`(分发树) → `0x551804`）
就是它。**修在客户端**（`0x5518de` 补判空，GameContext 为空就跳过那一发），
不在服务端 —— 见 D71。

## §68 ★★★ 崩溃 B：`nmconew.dll` 的自制互斥体在等待期间被析构（🔍 dump 落锤，2026-09-10）

**现场**（d314010739，10:02:46，`C0000005 写 0x00A2000C`，栈 7 层全在 `nmconew.dll` 里，
最外层是 CRT `_threadstartex` ⇒ 一条后台工作线程）。
`nmconew!0x10098c20` 是它自制的 `Enter()`：

```text
10098c4d  InterlockedIncrement([this+8]+8)   ; 抢不到 ->
10098d53  WaitForSingleObject([this], [this+0xc] ? 10s : 300s)
10098d5c  eax = [this+8]                     ; ★ 醒来重新取，此时已是 0x00A20000
10098d62  mov [eax+0xc], ecx                 ; 写自己的 tid -> 崩
```

`[this+8]` 在函数入口处**还是好的**（`0x10098ddf` 早就读过它，没崩）
⇒ **锁对象是在这条线程阻塞期间被别人析构掉的**，醒来往已释放内存里写。
时间线对得上：10:02:20 客户端在同一个进程里**重新登录**（新开游戏服连接，
旧连接 10:02:25 才断），26 秒后崩 —— 不重启客户端直接重登，NEXON 那套信使/通行证
栈就在拆了重建的缝里翻车。

**没有改它**（D71）：`nmconew.dll` 是动态加载、基址随机（本次 `0x0A170000`，
V0.1 那次 `0x09180000`），而且锁对象已经没了，跳过那两句写只是把崩溃挪到下一句 ——
在能复现之前动它是拿运气换心安。可选的缓解是**回到 D007 的原意、拦掉
`NMService.exe`**（那整套信使连的是停机 15 年的 `platform.tiancity.com` / `ngm.nexon.net`），
但**登录会不会受影响没验过**，属于要单独实机验的改动。

## §69 ★★ 地址空间快照进了 bshook 心跳（2026-09-10）

崩溃 A 那份 dump 里线程栈一路铺到 `0x4F09FDB0`、崩的对象落在 `0x3CB75A70`，
**看着**像 32 位地址空间用到 1.2 GB 了 —— 但 Vista 起线程栈本来就是随机落位的，
**这不是证据**。所以给本来就有的 30 秒心跳加了一行真数（`vm_snapshot()`，
纯 `VirtualQuery` 走一遍，无新依赖）：

```text
--- still alive (30 s)；地址空间 已提交 561 MB / 已保留 111 MB / 最大空闲块 804 MB ---
```

**本机登录界面的基线就是上面这行**（提交 561~570 MB / 最大空闲块 804 MB）。
32 位进程真正先出事的是**最大连续空闲块**而不是总量：块一小 `new` 就开始返回 NULL，
而这个 2007 年的引擎有大把地方不判 NULL。下次现场把这一行和基线一比即可定案。


---

## §70 金币 / 经验原来是怎么算的（搬进配置前的基线，2026-09-10）

搬走之前 `gameserver.py` 里那两个纯函数就是全部：

```text
闯关  经验 = int(基数经验 × 系数) + 本局分数 // 20      基数经验 = 20 + 10×(关卡-1)
      金币 = int(基数金币 × 系数)                       基数金币 = 100 + 50×(关卡-1)
      系数 = 难度加成 {1:1.0, 2:1.6, 3:2.5}，没通关再 ×0.3
对战  经验 = 10 + 杀敌×3 + (赢 ? 15 : 0)
      金币 = 30 + (赢 ? 50 : 0)
```

★ **对战八种组合（生存/夺分 × 道具战 × 个人/组队）给的钱一模一样** ——
`pvp_reward(score, seat_cleared)` 只有这两个入参，模式 / 道具 / 队伍一个都没进去。

★ **金币不吃分数 / 杀敌数**（D152），只有经验吃；**地上捡到的金币另加**
（`quest.coins_of(seat)`，在 `send_end_game` 里加，不走公式）。

★ 三个界面数字的落点（`re/packet_api.md` §3.3 / §3.4，协议这次一个字没动）：
`0x0309` 的值 9 = 经验 +N、值 10 = 金币 +N、值 11 = 竞技场分数 +N（本局增量）；
`0x0411` 的 1/2/3 号位是**经验三件套绝对值**、4 号位是本局金币（客户端 `+=`）。

**怎么保证搬家后一分钱不变**：默认表用**同一个表达式**展开
（`shopdefaults.build_rewards()`），`test_shopcfg.RewardDefaultsTests`
把上面这段公式原样抄成第二个实现逐格比对。⚠ 别手抄成数字表：`int()` 向零取整，
`int(150 × 2.5 × 0.3)` = **112 不是 113**。

## §71 对战房的三个坐标在结算时都拿得到（2026-09-10）

奖励要按「什么模式 / 有没有道具 / 分不分队」发，这三样在 `send_end_game` 里都是现成的：

| 坐标 | 从哪来 | 值 |
|---|---|---|
| 游戏模式 | `Conn.pvp_game_mode()` = 描述符 `arguments[1]` | 0 生存 / 1 计时 / 2 무투전 / 3 夺分 |
| 道具战 | `Conn.item_mode()` = `arguments[2]`（`lobby.item_mode_of`） | bool |
| 组队战 | `Conn.battle_teams()` 的第二个返回值 | `team_layout == TEAM_LAYOUT_TEAMS` |

★ **中国区玩家只选得到生存(0) 和 夺分(3)**：低等级号选生存还会被客户端自己改回夺分
（`0x465338` / `0x465a2c`，hook 的 `try_patch_player_level_gate` 第 3 / 4 处就是解它）。
⇒ 奖励表只放这两行，剩下两个号由 `gameserver.PVP_MODE_ROW` 按**引擎里的胜负条件**
归档（2 和 0 共用 `SurvivalVictoryCondition` ⇒ 归生存；1 和一切没见过的归夺分）。

★ **天梯房 / 练习房（`session_type` 5 / 6）也走对战那条结算路**
（`quest_mode()` 只认 type 2），它们的 `arguments` 不是对战那套含义 ——
落到 `PVP_MODE_ROW` 的兜底，按夺分那一行发钱。要给天梯单独定价，
得先让描述符把「这是天梯」传下来，别在发钱那一步猜。

★ **平局按「输」算**：尾部数组那一格是 `1` 才是赢，`0`（谁都不判）和 `-1` 都走输那一列。

---

## §72 等级曲线是我们自己定的，只有一处定义（2026-09-10）

```text
升 L → L+1 需要        = 100 × L                （`EXPERIENCE_STEP`）
到达 L 级的累计经验    = 100 × L × (L-1) / 2 = 50·L·(L-1)
满级                   = 60（`LEVEL_MAX`）      到 60 级累计 177000
```

★ **不是从客户端逆出来的**：客户端只按 `gspEndGame` 下发的三个绝对值
（总经验 / 下一级所需 / 本级起点）做减法画进度条（§94），它不知道曲线长什么样。
60 这个上限倒是**物理的** —— `Images/General/LevelMark.smf` 只有 60 帧，
客户端 `0x441fc0` 自己也钳 1..60，超了会取到别的图的像素。
（原版实际内容只铺到 21 级，`map.ini` 的 `MinLevel` 最高就是 21。）

★ **满级没有「下一级」**：`level_for_experience()` 钳在 60。
`experience_for_level(61)` 存在，但只给 `experience_bounds()` 在满级时当
**除法分母**用（两个数一样客户端就要除以 0，那是没验过的行为）——
**别把它当成「还能再升一级」显示出来**（D72a）。

★ 管理页那张参照表照 `account_store.level_table()` 画，**前台不自己套公式**：
曲线只有 `account_store` 一处定义，`test_account_store` 把表和
`experience_for_level` / `level_for_experience` 逐级对死。

---

## §73 V0.3.0 的服务端升到「多一份配置」这一版，升级路径是干净的（2026-09-10 实测）

线上已经发布过 V0.3.0，那些服务器的 `server/data/` 里只有**四份**配置。
这一版多了 `rewards.json`。**三条路都实测过**（不是读代码推的）：

| 时刻 | 会发生什么 | 靠谁保证 |
|---|---|---|
| 启动、`ensure_files` 之前就有人打完一局 | 照内置默认值发钱（第 7 关困难 = 经验 200 / 金币 1000），日志里一行警告 | `shopcfg._USE_DEFAULT` → `_load` 的 `fallback()` |
| 启动时 `ensure_files()` | **只新建 `rewards.json`**；老四份逐字节没动，运营手改过的价原样在 | 它逐份看 `os.path.exists`，「已存在一律不覆盖」（D7） |
| 回滚到 V0.3.0 那会儿做的备份 | 回滚框那一格**只列备份里真有的四份**，并写「备份里没有 金币 / 经验获取，它保持现状」；回滚下去不报错，现在这份 `rewards.json` 不动 | `databackup.groups_of()` 取的是 `config_filenames() ∩ 备份里有的`；`check_selection` 也按这个交集判 |

★ **协议一个字节没改** ⇒ **老客户端（V0.3.0）连新服务端照常玩**。
`0x0309` 的值 9 / 10 和 `0x0411` 的经验三件套 / `money_gained` 语义全没动，
改的只是「那几个数从哪儿来」。要不要抬版本号是发版的决定，不是兼容性要求。

★ **写盘失败也不拦着开服**：`app.py._report_shop_config` 把 `ensure_files`
包在 try 里；那种情况下奖励表一直读不到，就一直用内置默认值 ——
最坏是「管理页改不了数」，不会是「打完一局一分钱不给」。

★ 两条回归用例钉着，**都不写死文件名**（拿「`_SPECS` / `config_filenames()`
的最后一份」当新加的那份），所以**下次再加第六份配置时自动跟着走**：
`test_shopcfg.test_an_old_data_dir_only_gets_the_files_it_is_missing`、
`test_backup.test_an_old_backup_without_the_newest_config_still_rolls_back`。

---

## §74 ★★ `$ErrorActionPreference='Stop'` 下，裸奔的 `Stop-Process` 会把「杀不掉一个进程」升格成「整个启动中止」（2026-09-10 玩家实机）

**现场**：玩家跑 `start.bat`，服务端和中继都起来了，最后一句
`[启动失败] Cannot stop process "BigShot (43000)" ... Access is denied.` +
`ProcessCommandException`，游戏没起来。

**这跟执行策略没关系**（第一反应会往那儿想）。`start.bat` 一直带
`-ExecutionPolicy Bypass`；真被策略拦住的话第一行就报「禁止运行脚本」，
屏幕上不会有任何游戏输出。这里的 `Access is denied` 是 Win32 的
`ERROR_ACCESS_DENIED`——拿不到那个进程的 `PROCESS_TERMINATE`。

**放大机制**：`Stop-Process` 的失败本来是**非终止**错误，可以接着往下走；
但 `launch.ps1` 开头是 `$ErrorActionPreference = 'Stop'`，于是它被升格成终止
错误，直接落进 trap → `exit 1`。**全库唯一一处没包 try/catch 的 Stop-Process**，
其余四处（`launch.ps1` 的 `Stop-ListenerOn`、`shutdown.ps1` 两处、
`build-common.ps1`）都包了 ⇒ 是漏网，不是设计。

★ **教训**：本仓库任何 `.ps1` 里的 `Stop-Process` / `Remove-Item` / `Start-Process`
这类「失败很正常」的命令，都必须自己接住错。`$ErrorActionPreference='Stop'`
是个**全局**开关，它把「这一步没成」和「这次跑不下去了」变成了同一件事。

**「拒绝访问」有四种来源，脚本判断不了，只能把事实摆给玩家**：
① 残留进程是提权跑的而当前不是；② 属于另一个用户账户；③ 安全软件拦着；
④ GameGuard 驱动在保护它（玩家点了开始菜单那份**只读原版**的快捷方式——
`Get-Process BigShot` 是按**进程名**匹配的，原版那个 exe 也叫 BigShot，会被
一起捞进来；而铁律 2 的自检只看 `game_patched\GameGuard.des`，管不到玩家
自己那份原版）。分辨这四种的事实只有两个：**进程属主 + exe 完整路径**。
⇒ `wincompat.ps1` 加了 `Format-ProcessIdentityText`。

★ 拿属主/路径**别用 `$proc.Path`**：那个访问器走 `MainModule`，要
`PROCESS_QUERY_INFORMATION | VM_READ`——「进程提权了而我们没提权」时它自己
就抛「拒绝访问」，**而那恰恰是唯一需要它的场景**。`Get-WmiObject Win32_Process`
的 `ExecutablePath` / `GetOwner()` 走 WMI（服务是 SYSTEM 在跑），同样的场景
读得到。（`Get-CimInstance` 是 PowerShell 3.0 才有的，Win7 上没有。）

★ **别把「用管理员身份运行 start.bat」当解法**：情况 ③④ 根本不管用
（GameGuard 是内核层剥权限，SYSTEM 一样被拒）；就算是情况 ①，提权拉起来的
新 BigShot **也是管理员权限的**，玩家下次正常启动照样卡在这儿——
等于把毛病焊死，从此必须一直提权跑。正确的解法是**用管理员的任务管理器
结束那个进程**（不改变新游戏的权限级别），再正常启动；再不行重启电脑。

**判据用「还在不在」，不是「Stop-Process 报没报错」**：报了错却自己退了、
没报错但还在，两种都见得到。杀完等的是 `WaitForExit()` 这个**事件**
（`Stop-Process` 只是发出终止请求就返回，互斥体那会儿可能还没放），
上面那个 10 秒不是判据、是防挂死的兜底，真正的判据是重新 `Get-Process` 复核。

## §75 ★★★★ 原版礼物盒协议全部逆出：`Gift` 线格式、`0x0609` / `0x050a` 三种动作、`0x0507` 到货通知（2026-09-10，🔍静态，**一发都没实机验过**）

**结论**：礼物盒能用来发东西，一份礼物 = 一件物品（含数量）；**经验 / 金币装不进去**
（物品表 1870 条、图标 882 张里没有任何货币类物品）⇒ D76 用凭证 id 绕。

**`Gift`**（vft `0x666140`，内存 0x54，Ser `0x44381a` / Des `0x443868`）：

```text
ShopStock   （Des 0x44360a —— 和 0x0500 货架条目**同一个类**：i32 itemId, wstr 名字, i32 价格,
             i32 划线原价, i32 货币, wstr 说明, i32 ❓, i32 k + k×{i32 id, i32, i32}）
wstr        发送人   +0x30  → 接收弹窗 SenderNameTxt
wstr        留言     +0x34  → MessageTxt
16 B        SYSTEMTIME +0x38 → SendDateTxt = swprintf("%d/%d/%d", 年, 月, 日)（0x461c83，读第 0/1/3 个 u16）
i32         标志     +0x48  bit0 = **未打开**（槽上按钮「받기 领取」；打开过 → 「확인하기 查看」，0x46319c）
i32         礼物 id  +0x4c  → 0x0609 回发的就是它（0x46359d 读 [gift+0x4c]）
```

- ⚠ **赠品清单至少一项**：接收弹窗 `0x461b2e` 直接读 `grant[0]+4` 查 ItemDB，空清单 = 空指针。
- ★ **`Item@ShopStock` 三个 int = 物品 id、数量、期限天数**（2026-09-10 实机 + `0x443444`：
  接收弹窗括号里「N个」读 `+0x08`、「N일」读 `+0x0c`、「N게임」也读 `+0x08`；合并弹窗拿数量算
  「道具数量增加为 N 个」）。货架上客户端只读 id ⇒ 以前填 0 没露馅，礼物里填 0 玩家看到「（0个）」。
  ⇒ 物品礼物的名字**不要再带 ×N**（弹窗会写成「绿色小珠 ×5（5个）」，用户 2026-09-10 第三轮）。
- ★ **图标有两张表**（同一个对象 `[0x72e1e0]`，客户端启动时从 `ShopItem-Chn.ini` 建）：
  `+0x00` 来自 `[Stock-N]` 节（商店在卖的 1817 件），`+0x14` 来自 `[Item-N]` 节（全部 808 件）。
  `0x4169e9` 查第一张（货架 `0x45bd5f`、礼物槽 `0x463289`、接收弹窗 `0x461aeb`），`0x416a1a`
  查第二张（合成 `0x45f941`、仓库、结算 `0x48e966`）。**材料 / 卡片没有 `[Stock-]` 节** ⇒ 礼物槽里
  画成「(FileNotFound)」（2026-09-10 实机）。hook `try_patch_gift_icon_fallback()` 把 `0x4169e9`
  的 miss 分支改成退回第二张（`BSHOOK_KEEP_GIFT_ICON_MISS=1` 保留原版）。
  仓库礼物槽自己的底图是固定的 `Images/Shop/PresentBox.png`。名字 = ShopStock 那个 wstr，服务端随便填。
- **`0x0609 gcpReqGiftAction`** = `i32 礼物id + i32 动作`（Ser `0x54cfa0`）。三处组包点：
  槽上点「받기」先发 **2**（`0x46356a`，随后本地开接收弹窗）；弹窗按「接收礼物」发 **0**（`0x445189`）；
  「丢弃后无法复原」确认框发 **1**（`0x44539d`）。
- **`0x050a gspRepGiftAction`** = `i32 礼物id + i32 动作 + bool(线上 i32) 成功`（Des `0x443b9e`）。
  处理器 `0x448072`：成功 + 动作 0/1 → 弹「已接收礼物[%s]。请在我的仓库中确认。」/「丢弃礼物 [%s]。」
  并**本地删掉这一份**（`0x44bb6a`）；动作 2 → 只 `and ~1` 清未打开位（`0x44baea`）；**失败什么都不显示**。
  ★ 发送人 == `"GM"` 时跳过「向朋友致谢」写纸条流程（`0x4482f0`）。
  ★ 客户端**不会**自己把物品塞进本地仓库 —— 仓库靠 `0x0501`→`0x0601`，经验金币靠 `0x0600`。
- **`0x0507`（服务端 → 客户端，无正文）= 「收到礼物」**（主分发树 → `0x553dc9`，不读正文）：
  弹系统提示「收到礼物。」、置 `[0x72e348]=1`、调当前 Stage `vft+0xb0` —— 四个 Stage 都实现了
  （大厅 / 房间 / 战斗 = `0x42adef` 只让任务栏礼物盒钮变状态；ShopStage = `0x4485ee` **顺带重发 `0x0607`**）；
  当前 Stage / 任务栏为空也判过 ⇒ **任何时候推都安全**。
- 礼物页 `UiGiftStockTab` **8 格一页**、自带翻页（`0x44b3b7: cmp 8`；`[tab+0xf8]` 是页号）。
- `0x0509 gspRepSendGift` = `bool + i32`（玩家互赠的结果，本版用不到，只记格式）。
- 顺带钉了一个老 ❓：`Packet_gspRewardPopup`（`i32 + wstr + wstr + bool`）就是 `0x020d`
  的包类（处理器 `0x553c5f` 构造它，`DlgRewardPopup`）—— 一个通用「奖励弹窗」，本版没用。
- ⚠ 服务端方向的 `0x0609` 仍是耐久更新（§29），**别把这个号往下发**。

**验证状态**：单测两套运行时全绿，`stop.bat` → 真服务端起过。**用户 2026-09-10 实机第一轮**：
礼物盒打得开、8 格 + 翻页、礼物槽 / 接收弹窗 / 合并弹窗都画出来了，发送人 GM、日期 `2026/9/10`、
留言都对 ⇒ `Gift` 线格式和 `0x0508` **✅**；露出来两处：数量字段填 0（上面已改）、材料图标查错表
（hook 补了）。领取 / 丢弃 / `0x0507` 还没看到结果（X-66）。

## §76 ★ 管理页的界面验证：Claude 的浏览器面板**立不起会话**，别在登录上耗时间（2026-09-10 实测）

**结论**：`mcp__Claude_Browser__*` 那个面板把页面套在一个跨站 iframe 里，
**cookie 罐是只读的** —— 登录接口返回 200、`Set-Cookie` 收不下，
`document.cookie = "..."` 写进去也没反应（读得到旧值，写不进新值）。
于是每一发都还带着上一台服务器留下的死令牌，`/admin/api/session` 永远
`logged_in: false`，页面卡在登录框。**和 `SameSite` 无关**：改成 `Lax` 一样不行。

**验管理页界面的正确姿势**（别改 `server/web/admin.py` 去迁就面板）：

1. 用户的正式服务端和游戏客户端可能正开着 —— **不要 `stop.bat`**，
   另起一台**临时**服务端：临时 `data` 目录（`shopcfg.DATA_DIR`）+ 空闲端口，
   照 `test_web_admin._AdminCase` 那套拼（`AccountStore` + `shopcfg.ensure_files`
   + `databackup.BackupService` + `web_server.make_server`）。
2. 在那个**一次性脚本**里 monkeypatch 掉身份：
   `web_admin.AdminRoutes._admin_identity = lambda self: ("admin", "system")`。
   仓库里的代码一个字不改。
3. 数据用 `urllib` 直接打接口造（发几次奖励）。

**另一半坑**：用户在全屏玩游戏时 Claude 的窗口被遮住，
`computer{action:"screenshot"}` 直接超时、`left_click` 报「viewport 0x0」。
这时候**照样能验**，而且比截图更硬：

- `read_page` / `get_page_text` 看结构和文案；
- `javascript_tool` 里 `.click()` 驱动页面、`getBoundingClientRect()` 读**实际布局**
  （「两个钮在不在最右端、顺序对不对」用左右边界的数字判，比看图准）。

⇒ 「看不了截图」不是「验不了」。**别拿「代码写对了」当验收，也别为截图去打断用户。**

## §77 ★★★ 礼物盒的提示框读的是 `Gift` 自带那份 `ShopStock` 的说明，**不是** `ItemInfo+0x18`（2026-09-10 实机 + 🔍静态）

**现象**（用户实机截图）：礼物盒里的装备鼠标指上去，提示框**弹得出来**、
标题 / 图标 / `★Lv.18 以上` / `泰尔专用` / `修理次数无限制` **全对**，
但下半那**两个绿框空的**。

**结论**：那两个框画的是 **`Gift+0x04` 那份 `ShopStock` 的说明字段**
（`ShopStock+0x18`），我们一直发的是空串（`build_gift` 的 `note=""` 默认值）。

**链路**（全静态逆出，`tools/re_bs.py`）：

| 地址 | 干什么 |
|---|---|
| `UiGiftStockTab::Update` `0x44b84f`（vft+0x0c）| 按 `[tab+0xf8]×8` 定位这一页的 `Gift`（步长 **0x54**）；`test byte [gift+0x48],1` —— **未打开的礼物喂空清单**（框直接隐藏），打开过的 `lea edi,[gift+4]` 把那份 `ShopStock` 现造成 `ShopStockGroupItem`（`0x44b11b`）|
| `0x45bc53` | `UiShopToolTip::SetStockList` —— 存进 `+0xcc`（`vector<ShopStockGroupItem>` 的 begin） |
| `UiShopToolTip::Draw` `0x45c254` | `begin == end` 就隐藏；`0x45c302` 取 `ebx = &elem[0]` |
| `0x45c308` | `cmp [ebx+0x14], 0; jle 0x45c485` ⇒ **价格 ≤ 0 整段价格行跳过**（礼物价格是 0，所以框里没有价格 —— 这是对的，不是 bug）|
| `0x45c485` | `lea eax,[ebx+0x20]` 按 `\|`（`0x668274`）切开，**最多画 2 段**（`0x45c4c9` 的 `cmp i,2`）|

`ShopStockGroupItem` 一个元素 **0x34 字节**（`0x448b89` 的 `idiv 0x34` 钉死）：
`+0x00` vft、`+0x04` 档位名 wstr、`+0x08` 起是内嵌的 `ShopStock`
⇒ **itemId `+0x0c` / 名字 `+0x10` / 价格 `+0x14` / 划线原价 `+0x18` / 货币 `+0x1c` / 说明 `+0x20`**。

**★ 为什么这个 bug 特别迷惑**：同一个框里的 **等级 / 角色 / 修理次数**走的是
`ItemInfo`（按 itemId 查 `[0x72e1dc]`，`0x0501` 填的），那份我们一直发得好好的
⇒ 框「弹出来了、有一半是对的、就是没字」，很容易往「客户端没收到定义」上想。

**★ 三个提示框是三个类，读的字段各不相同**（别再混）：

| 类 | 谁在用 | 说明取自 |
|---|---|---|
| `UiShopToolTip` `0x668dc4` | 商店货架 · **礼物盒** | `ShopStock+0x18`（**包里带的**）|
| `UiCabinetToolTip` `0x6681ec` | 仓库格子 | `ItemInfo+0x18`（`0x454b23` 拿 `CabinetItem+4` 查 `[0x72e1dc]`）|
| `UiCompositionToolTip` `0x669154` | 合成 | 同上一路 |

三个都是「按 `\|` 切、最多 2 段」；`ItemInfo2Txt` 那第三个标签不参与切分。

**修法**：`shop.gift_note()` —— 和**货架**同一个来源 `shopcfg.item_desc_zh()`，
不给礼物另写一套文案（铁律 12）；经验 / 金币凭证用 `shop.VOUCHER_DESC`，
和它们 `ItemInfo` 里那句是**同一个常量**（两个字段、一句话）。
`build_gift(note=None)` **默认现算**，不是让调用方记得传 —— 这个 bug 的成因
恰恰是「多一个可选参数、唯一的调用方没传」。显式传 `note=""` 仍旧发空串（探针要用）。

**★ 还空着的（不是这个 bug）**：**材料**（珠子 / 矿料这类）`item_desc_zh` 本来就返回空串
（`KIND_USAGE_ZH` 里没有 `material` 这一档），所以它们在**合成 / 仓库 / 礼物盒三处都是空的**
—— 是「材料从来没有说明」，不是礼物盒漏发。要不要给材料写一句由用户定。

**验证状态**：单测两套运行时全绿（新增 3 项，钉住「礼物带说明」「凭证两处同一句」
「调用方仍能顶掉」）。⏳ **实机还没看**（X-68）—— 服务端改了要 `stop.bat` → `start.bat`。

## §78 ★★★★★ 「进 boss 房黑屏 / 全场一起闪退」= 换图把突击技对象连场景一起放掉了（🔍静态落锤 + ✅云端日志 + ✅ dump ×3，2026-09-10，bug调查/18）

**结论**：关卡内换图（走到地图最右边进下一张图，含各关 boss 房）会**析构掉世界里
所有登记过的对象**，但六个角色是被单独摘出去、原样挂回来的 —— 角色身上
`+0x57C` / `+0x580` 那两格指着的**突击技对象**就这么变成野指针。
下一帧 `Character::ProcessDash` 拿它去用，`call [eax+0x78]` 跳进已释放的堆。
**这就是 §66 那个野指针的来路**（当时记的是「谁把对象放掉的还没查到」）。

### 链路（全部静态坐实）

```text
Character::StartDash 0x5020d9
  0x502182  拆旧的（call [eax+0x20]）+ 0x502191 清零     ← 原版只在这条路上清
  0x5021f0  [Character+0x57C] = new(0x30C)              ← 突击技伤害对象
  0x502229  call 0x473e7c(世界 [0x72e2d4], 它)          ← ★ 登记进世界
 （兄弟函数 0x5024ac / 0x5024e2 对 [Character+0x580] 做同样的事，new(0x310)）

关卡内换图 0x47900a（§111 那条链的第二步）
  0x479058  call 0x474029   把 6 个角色**摘出**世界（只摘角色）
  0x4790be  call GameContext::vft+8 = 0x48d2ad
              └ 0x48d326 call 0x473692 ← ★ 世界的对象表整片析构
                （0x4736a2 / 0x4736b6 / 0x4736ca 三轮 list 析构，末尾 and [ebx],0）
  0x4790d2  call 0x473e7c   把**同一批**角色指针原样挂回去

Character::ProcessDash 0x5077c6
  0x50794a  cmp [ebx+0x57C],0 —— 只判空，不判死活 ⇒ 野指针照用
  0x507970  call 0x4814f2
    0x481a51  mov eax,[esi]      esi = 已经被释放的那块内存
    0x481a53  call [eax+0x78]    ★ 跳进堆里执行垃圾（exe 没开 DEP）
```

`[Character+0x57C]` / `+0x580` 全 exe 只有 `0x502191` / `0x5079d8` / `0x507a8f`
三处写 0，**全在冲刺自己的状态机里，换图一处都不经过**。

### 为什么「一屋子人同时死」

每台客户端都在本地模拟全部 6 个角色 ⇒ 谁在换图那一刻正冲刺，
**每台机器上**那一格都同样悬着，全场在进新图后 1~3 秒一起崩。

### 为什么「有时是黑屏未响应，有时是闪退」

跳飞的落点在**未提交页**时，原版崩溃报告器自己也会崩（§65），进程要挂
40~90 秒才退 —— 窗口就一直停在换图加载那一帧（「正在载入 100% /
正在等待其他玩家」，那句韩文原串 `0x66c13c` 全 exe 只有 `0x4795a1` 引用一处，
就在换图加载循环 `0x47928d..0x479628` 里），显示「未响应」。
落点在已映射页时报告器写得完，进程 2~7 秒就没了 = 普通闪退。
**云端日志逐条对得上**（断头报告 → 沉默 42/63/63/90 秒；完整报告 → 沉默 2~7 秒）。

### 证据（云端 2026-09-10 一整天）

| 证据 | 数 |
|---|---|
| 游戏服连接里「最后一发是 `0x0412 rawMapLoadingDone` 然后彻底沉默」 | **20 / 107** |
| 全房间同时死的簇（2~4 人一起） | 11 次（Quest03_6 ×8、Quest06_boss ×2、Quest02_2 ×1）|
| 23 份崩溃报告里归到这一族的 | **16 份**（其中 3 份是 `d314010739`，那台机器**系统时钟快约 4 分钟**，按收包时刻对齐才能配上）|
| dump 里栈完全一致的 | 3 份（`00481A56` → `00507975` → `00506FF7` → `00490657`）|
| 报告里 `Fault address` 直接写着 `00481A53` 的 | 1 份（abc122112 21:19:37）|

★ **服务端一点问题都没有**：252 次换图**全部**在 1.6 秒内回了 `0x0418`，
`0x0411 → 0x0417 → 0x0412 → 0x0418` 这条闸门一次都没卡过。
玩家看到的「卡在等其他玩家」是**崩完之后的残影**，不是真的在等。

### 修法（D78）

`hook/bshook.c` 的 `try_patch_mapchange_dangling()`：偷 `0x4790cb` 起 6 字节
（`mov ecx,[0x72e2d4]`，正好一条指令，`edi` 就是这一轮的角色），
**卸完场景、重挂角色之前把那两格清零**，再把 `ecx` 装回去回 `0x4790d1`。
清零安全：对象此刻已经被世界析构，我们只丢掉一个悬空指针；而 `NULL`
本来就是这两格的合法取值（`StartDash` 在 `new` 失败时就写 `NULL`，
`ProcessDash` 三处解引用前全都判空）。§66 那三处虚表校验**保留**当最后一道网。

## §79 ★★★ bug调查/18：23 份线上崩溃报告的归类（2026-09-10）

| 归因 | 份数 | 状态 |
|---|---|---|
| §78 换图野指针 | **16** | ✅ 本轮根治（hook） |
| §68 `nmconew.dll` 自制锁在等待期间被析构 | 4 | ✅ 已修（**§82**：放行加载、补它自己那两个失败出口）|
| 胜负条件析构走**已释放的 LobbyStage**（`0x55C811`）| 1 | ✅ 已修（**§81-A**）|
| UTF-16 资源缓冲区 `malloc(len+1)` **少一个终止字节**（`0x4012FE`）| 1 | ✅ 已修（**§81-B**）|
| UI 向量 `_First` 为空还照样取元素（`0x438BD5`，嵌套消息泵重入）| 1 | ✅ 已补判空（**§81-C**）|

★ **`nmconew` 那四次的触发条件这次看出来了**（§68 当时只有一次、说不准）：
**同一个账号在本机重新登录**（新连接把旧连接顶掉，`online.log` 里是同一毫秒的
「✓ 登录 / - 断开」），新登进来的那个客户端 **17~31 秒后**崩在 `nmconew`。
9-10 一天里这个形状出现 5 次（09:57:59 / 12:27:19 / 12:53:33 / 15:52:39 …），
其中 4 次上报到了崩溃日志。★ 拦它加载那条路**行不通**（§80 实机推翻）；改成**放行加载、补它自己那两个失败出口**（**§82**）就干净了 —— 登录路径一个字节不碰。

★ **归因时先对时钟**：崩溃报告里的时刻是**玩家那台机器**的本地时间。
`d314010739` 快约 4 分钟、`concon` / `306052979` 基本准。
判据用 `receipt.json` 的 `received_at`（服务端时刻）去和 `online.log` 对，
别拿 `crash_time_text` 直接配。

## §80 ★★★★★ 【试过，不行】拦掉 `nmconew.dll` 的加载会让**登录直接失败**（✅实机推翻，2026-09-11）

**结论**：为根治 §68 那一族崩溃，试过「让 `nmcogame.dll` 加载 `nmconew.dll` 失败」——
**静态看每一环都干净，实机一登就翻**。**登录路径确实依赖 `NMCO_CallNMFunc` 返回 1。**
已经从 hook 里**整个拆掉**，`nmconew.dll` 照常加载，**§68 仍是未修**。

### 实机现象（本机，2026-09-11 00:12）

```text
00:12:42.064  LOGIN   本轮登录路由锁定 -> 本机服务器      ← 点了「登录」
00:12:42.071  ★NM    挡下 nmcogame 加载信使模块 …（第 1~6 次）
00:12:42.073  ★MSGBOXW cap="登录失败" text="认证服务器失败 (20000)"
```

**9 毫秒**，**一个网络包都没发** —— 本机认证服的 `+++ 连接#N` 计数器全程是 0
（我自己 telnet 一下就变成 1，证明那行日志确实会写）。
而且 nmcogame **把「模块不可用」记住了**：第二次起连 `LoadLibrary` 都不再试，
**后面每一次登录都直接失败**。

### 为什么静态分析没看出来

§80 原来那七条（判空、退路、返回 0、不弹框、不下载、`NMService.exe` 只由 nmconew 起）
**逐条都是对的**，错在最后一跳的推论：我拿 `0x532FC4` 那个初始化「连返回值都没测」
去代表整条登录路。**登录路上另有一发在判**，返回 0 就报「认证服务器失败 (20000)」。

⇒ 真正的机制是：**平时能登录不是因为信使能用**（它连的 `platform.tiancity.com` /
`ngm.nexon.net` 停机 15 年了），**而是因为那一发返回了 1** ——
模块在、调用被派发出去就算成功，后面连不上是异步的事，客户端不管。

### 下次要再试的话

只能在**更外面**下手：BigShot 自己那一格 IAT **`0x63751C`（`NMCO_CallNMFunc`）**
换成「直接返回 1、根本不进 nmcogame」的桩 —— 这样 `nmconew.dll` 不会被加载
（`nmcogame` 是在 `NMCO_CallNMFunc` 里才去 `LoadLibrary` 的，见下表），
客户端拿到的又还是 1 —— 这条现在有了，是 **§83** 那个默认关的开关。

### 顺带查明的硬事实（这些是对的，留着）

| # | 事实 | 证据 |
|---|---|---|
| 1 | BigShot 只用 nmcogame 的 **4 个导入**，IAT 在 `0x637510..0x63751C`，全部调用点挤在 `0x5441DC..0x544260` | 从 dump 里扫「指向 nmcogame 的连续槽群」找出来的 |
| 2 | `nmconew.dll` **全进程只有一个加载点** | `nmcogame!NMCO_CallNMFunc` 的 `0x10002005` / `0x1000201E`；路径 = `GetModuleFileNameA` + 目录 + `0x10006AD0` 返回的 `"nmconew.dll"` |
| 3 | 加载失败时 `NMCO_CallNMFunc` 走 `0x1000204F`：往**日志文件**写一行 `"Fail to load messenger module!"` 然后 `xor eax,eax` 返回 0 | 不弹框（全模块唯一那个 MessageBox 包装 `0x10027CCF` 只被两处调）、不下载 |
| 4 | **`NMService.exe` 只由 `nmconew.dll` 拉起** | exe 名（`nmconew.dll` file `0xD0E60`）+ 命令行模板 `"%s" -domain:%s`（file `0xD70BD`）只在它里面，`nmcogame.dll` 里一个字都没有 |
| 5 | 信使是**点「登录」那一刻**才起来的 | 云端日志：`connect 47611` 那一瞬 → `nmconew.dll` → 790 ms 后 `NMService.exe -domain:<32 hex>` |
| 6 | §68 那族崩溃的**触发条件** | 同一账号在本机重新登录（顶号），新客户端 **17~31 秒后**崩；9-10 一天出现 5 次 |

★ **铁律 5「不启动 `NMService.exe`」现在仍然只是写在纸上** ——
要让它成真只能走 **§83** 那个开关（`BSHOOK_KILL_NM=1`，**还没验**）；
而 §68 那一族崩溃已经用 **§82** 从另一头堵掉了，不必等它。


## §81 ★★★★ bug调查/18 里「各只出现一次」的三种散崩，全部查到根因（🔍 dump 落锤，2026-09-11）

上一轮把这三份记成「无复现」就收工了 —— **不对，它们都带完整 minidump**（各 30 MB）。
逐份挖下去三个根因都清楚了，而且三处都补在**原版自己漏掉的那一句判断**上。

★ 方法上的两个坑（下次照做）：
① **这批 dump 的异常 `CONTEXT` 大半是坏的**（`Eip=0`、`Esp` 指在栈顶）——
别信寄存器，用 `ExceptionInformation[1]`（真正访问的地址）+ 扫栈；
② 扫栈要**按「这个值前面一条确实是 `call`」过滤**，否则一屏都是残留垃圾。

### A. 胜负条件析构时走的是**已经释放掉的** `LobbyStage`（`0x55C811`）

栈（外 → 内）：`WinMain 0x40A60B → 主循环 0x40DE91 → 切 stage 0x40DDF5 → …
→ Stage 析构 0x477D06 → GameContext 析构 0x48CF57 → QuestVictoryCondition 析构
0x55E0D9（尾跳基类 0x55C25A）→ 0x55C284 call 0x55C809 → 崩在 0x55C811 mov edi,[eax]`。

**dump 里的硬事实**：`[0x72e29c]`（LobbyStage 全局）**已经是 0**，
而 `[胜负条件+0x174]` 还是 `0x2023CFE8`，`[那格+0x38]` 读出来是 `0x46EA816B`（堆垃圾）。

那一格就是 LobbyStage：基类构造 `0x55C218 mov [esi+0x174], edi` 存的是入参，
同一个入参在构造里被 `0x4045F9` / `0x40462C`（**已知的 LobbyStage 座位取值器**）用着；
析构里 `0x55C27A call 0x404D42` 算的是 `表基 + 座位*0x3C + 0x40`，逐字节就是座位槽布局。

⇒ **拆除顺序 bug**：`LobbyStage::~LobbyStage`（`0x40545B` 把全局清 0）先跑，
之后 GameContext 才拆，而胜负条件握着一个**裸指针**去遍历六个座位里的链表。

**修**：偷 `0x55C260`（`mov [esi],虚表`，正好一条 6 字节指令），LobbyStage 已经没了、
或和自己记的对不上就**整段逐座位循环跳过**（去 `0x55C294` 收尾）。
不是兜底，是正确语义：**我登记的那个 LobbyStage 都不在了，没有东西需要反注册**。
栈是平的（入口四个 push，`0x55C294` 正好四个 pop）。

### B. UTF-16 资源文件的缓冲区**少一个终止字节**（`0x4012FE`）

`0x5D9DE7` 读文本资源：`malloc(len+1)` / `memset(0, len+1)` / 读进 `len` 字节 /
看到 `FF FE` 就当 UTF-16 → `wcslen(buf+2)`。
**宽字符终止符要 2 个 0 字节，只给了 1 个。** `len` 是偶数时 `buf[len]` 是那唯一的 0，
`buf[len+1]` 已经出界 —— 后面那个字节非 0 就一路扫下去，撞上未提交页才崩。

算得严丝合缝：`ebx=0x1A2C`(6700) ⇒ 分配 6701；`esi=0xD2F`(3375) 个宽字符 ×2 +2 = 6752；
`edi=0x5375B5A0` + 6752 = `0x5375D000` = `ExceptionInformation[1]` 那个地址。
6700 字节的 UTF-16 资源全 `Pack_decrypt` **只有一个**：
`Data/Ui/CompositionToolTipNewUI.ui`（合成界面提示框）。

★ **272 个 UTF-16 `.ui` 文件字节数全是偶数** ⇒ 每一个都踩在这上面，
只是堆里紧跟着的那个字节通常正好是 0，才没天天崩。

**修**：`0x5D9E02` / `0x5D9E0D` 两个 `lea eax,[ebx+1]` 的立即数 `01` → `02`，
**各一个字节**。缓冲区在 `0x5D9E72` `free` 掉、长度不外传，多要 1 字节没有副作用。

### C. UI 向量的 `_First` 为空时照样取元素（`0x438BD5`）

`0x438BC1` 用 `(_Last-_First)>>2` 当元素个数判空，`0x438BD3 mov eax,[eax]` 取 `_First`，
`0x438BD5 mov eax,[eax+ebx*4]` —— `ExceptionInformation` 说**读地址 0**，
即 `_Last != 0` 而 `_First == 0` = **被拆到一半的向量**。

栈上能看到 `0x428122 → 0x40DE91 → DispatchMessage → WndProc(0x40EF4F) → UI 虚表链`
—— 外层代码开了一个**嵌套消息泵**，泵出来的窗口消息又回到这个 UI 对象上，
正撞在它重建 `[esi+0x568+i*12]` 那几个向量的中间。再外面是 `0x544xxx`（NM 信使包装层）。

★ 这一处是**兜底**（只有一份现场，重入的源头没查到）：`_First` 为空就当这张表是空的，
跳到 `0x438BF2`（和原版「个数不够」走的是同一个出口）。地址 0 永远不可能是合法的
`_First`，判空不会误伤。触发时打一行日志，下次现场能对上。

## §82 ★★★★★ 根治 §68：`nmconew.dll` 那两个崩溃点**各自都有原版就存在的失败出口**（🔍静态落锤，2026-09-11）

**结论**：§68 记的「跳过那两句只是把崩溃挪到下一句」**是错的** —— 真去读代码，
两处的原版**自己就有干净的失败出口**，判一下跳过去就完事。
做法是**放行加载、加载完给它打补丁**：`nmconew.dll` 照常加载、
`NMCO_CallNMFunc` 照常返回 ⇒ **登录路径一个字节都不碰**（§80 的教训）。

### 崩溃点 A：自制互斥体 `Enter()`（RVA `0x98C20`）

```text
10098D53  call WaitForSingleObject([this], 10s 或 300s)
10098D59  mov edx,[ebp-0xC]      ; this
10098D5C  mov eax,[edx+8]        ; ★ 共享状态对象 —— 等待期间被别人析构了
10098D62  mov [eax+0xC], ecx     ; ★ 崩（写自己的 tid）
          …后面 +0x10 / [eax] 全部继续用它
10098DBB  mov esp,ebp / pop ebp / ret     ← ★ 函数自己的干净出口
```

`0x10098C35` 那条「我已经持有这把锁了」的路走的就是 `0x98DBB` ⇒
从等醒之后跳过去，**栈是平的、没有要清理的东西、返回值也没人看**。

判据用 `VirtualQuery` 看 `[this+8]` 那一页还提交着且可写吗 ——
不是「跳过一句」，是**发现锁已经没了就当这一轮没抢到，干净地回去**。
★ 只在**真的阻塞过**的那条路上跑（抢到锁的快路径是另一段一模一样的代码），不是热路径。

### 崩溃点 B：魔数校验前不判指针（RVA `0x26570` 那个函数）

```text
10026591  cmp dword [eax], 0x1FCA34   ; ★ eax 是野的 -> 崩
10026597  jne 0x1002666C              ; 魔数不符 -> 收尾 return 0
1002666C  mov ecx,[ebp-4] / call 0x10026700 / xor eax,eax / … ret 8
```

**原版已经在用魔数校验这个对象了**，只是忘了先看指针读不读得到。
⇒ 读不到就走它自己那条「魔数不符」的路，语义完全一致。

### 实现上要知道的两件事

1. `nmconew.dll` **运行时加载、基址随机**（这正是 §68 当时不敢动的理由）。
   解法：hook **`nmcogame.dll` 自己那一格 `LoadLibraryA`**（IAT RVA `0x2C030`，
   §80 查明那是唯一加载点），**放行**之后拿真实基址打补丁。
   detour 的回跳目标只能运行时算，所以放在全局里 `jmp dword ptr [g_...]`。
2. 两处偷的字节里**一个重定位项都没有**（对着 `.reloc` 逐项查过）
   ⇒ **文件字节 == 内存字节**，特征串直接从磁盘那份量出来就行。

逃生门 `BSHOOK_KEEP_NM=1`（要复现 §68 时用）。

## §83 ★★★★★ 【第二次试，还是不行】把 `NMCO_CallNMFunc` 换成「直接回 1」的桩 —— **登录真的需要 nmconew 产出一份应答**（✅实机推翻，2026-09-11）

**结论**：换 BigShot 那一格 IAT `0x63751C`、让 `NMCO_CallNMFunc` 直接返回 1
（`nmcogame` 压根不进去 ⇒ `nmconew.dll` 不加载、`NMService.exe` 不起），
**实机还是「登录失败 / 认证服务器失败 (20000)」，认证服一条连接都没收到**。
代码已整个删掉，默认走 **§82**。

### 实机（本机，2026-09-11 00:36，用户拍板试的）

```text
00:36:40.061  LOGIN   本轮登录路由锁定 -> 本机服务器      ← 点了「登录」
00:36:40.077  ★NM桩   NMCO_CallNMFunc 直接回 1 …
00:36:40.077  ★MSGBOXW cap="登录失败"  caller=00423E1F   ← 和 §80 那次同一个
```

`logs/server.out` 里 `+++ 连接#N 端口47611` **计数器全程 0**。

### 为什么（事前担心的那一点被坐实）

```text
005441CB  lea eax,[ebp-0x10] / push eax   ← ★ 出参
005441DB  call [0x63751C]                 ← NMCO_CallNMFunc
005441E4  cmp eax,1 / jne 失败            ← 返回码只是第一道
005441E8  mov eax,[ebp-0x10]              ← 取出参（应答）
005441EF  call 0x5378B2                   ← 把它塞进 str
00544200  call [eax+0x10]                 ← ★ vf_10 解析 str
00544208  mov ebx,[esi+0x24]              ← ★ 真正返回给登录逻辑的是解析结果
```

桩不填出参 ⇒ `str` 里是**请求**不是应答 ⇒ `vf_10` 解析不出来 ⇒ `[esi+0x24]` = 0
⇒ 包装函数返回 false ⇒ 20000。

### ⇒ 这个 DLL 拿不掉，原因在这

NEXON 的服务器停机 15 年了，可登录**照样成**（每天都在成）——
说明那份应答是 **`nmconew.dll` 自己在本地造出来的**。
要拿掉它就得把那份应答的格式逆出来自己造，**代价远大于收益**：
§82 已经把 §68 那一族崩溃从另一头堵死了。

★ **铁律 5「不启动 `NMService.exe`」到此为止仍然只是写在纸上** ——
两条路（§80 不让加载 / §83 直接回 1）都被实机推翻了。
但它**已经不再是崩溃来源**（§82），所以不必再花力气。

★★ **方法上的教训（第二次了，写死在这）**：这一族改动的判据**只有实机登一次**。
静态能证明「每个可见分支都接得住失败」，**证明不了「整条路都接得住失败」**。
两次都是同一个形状：静态干净 → 实机 9~16 毫秒弹 20000 → 认证服 0 连接。

---

## §84 ★★★★★ §82-A 之后崩点挪到了调用方：`nmconew` 工作线程的**回边跳过了它自己的判空**（🔍静态 + ✅dump 落锤，2026-09-11，bug调查/19）

**结论**：§82 没修错，是**没修完**。§82-A 让 `Mutex::Enter()` 干净返回之后，
调用方拿着「锁到手了」**接着解引用同一批已经被拆掉的字段** ——
崩点从 `Enter()` 里挪到了 `nmconew+0x75CDA`（读 `[NULL+0x100034]`）。

根子是 `nmconew.dll` 自己的回边 bug。工作线程主函数 `+0x754A0`
（线程入口 `+0xB4543` → `+0x75480` → 它）：

```text
000754D1 / 754E5 / 754F9 / 7550D / 75521 / 75535 / 75549
    ★ 序言逐个判 7 个字段（+0x20510 / 2051C / 20518 / 20514 / 20930 /
      20934 互斥体 / 20528 共享内存视图），任何一个是 0 -> jmp 0x75EE2 结束线程
00075557  ┌ 主体
00075CAF  │  ScopedLock lk([this+0x20934])   ← Enter() 里 WaitForSingleObject **10 秒**
00075CD4  │  ecx = [this+0x20528]            ← ★ 这里不判空了
00075CDA  │  edx = [ecx+0x100034]            ← ★★ 崩
00075C86  │  jmp 0x75557 ┐
00075D76  │  jmp 0x75C8B ├ ★★★ 三条回边**全部跳到序言判空之后**
00075D83  └  jmp 0x75557 ┘
```

`+0x20528` 是一块 **1 MB 内存映射视图**；关它的是 `+0x75EF0`
（`UnmapViewOfFile` → 指针置 0），关互斥体的是 `+0x76000`（`CloseHandle` → 置 0）。

**触发**：玩家「返回登录画面 / 重新登录」时主线程把这个对象整个关掉，
而工作线程正卡在 `Enter()` 那 10 秒等待里。
线上现场（`iorikexue` 2026-09-11）：**02:01:46.9 客户端关连接 → 02:01:56.2 崩，9.3 秒**。
这就是 §68「同一个账号在本机重新登录 ⇒ 十几秒后崩」的真正机制。

**dump 铁证**：序言判的那 **7 个字段全是 0**，`[this+0x20530]` 已被写成 `"NULL"`
（`+0x75D9E` 那段收尾写的），而 `[this]` 的虚表还好好的 —— 对象拆干净了，线程还在跑。

### 修法（两处，见 D82）

- **C**：`+0x75CCE`（`mov eax,[ebp-0xB0]`，6 字节）—— 拿到锁之后、解引用之前，
  把序言 `0x75549` 那一判**重做一遍**，没了就 `jmp +0x75EE2`。
  `+0x75EE2` 是整个函数**唯一的 `ret`**、序言里已有 7 处跳它，
  自己摘 SEH 帧、自己按 `ebp` 平栈。三条回边处 `[ebp-4]` 都是 -1（没有活着的局部对象）。
- **D**：`+0x98EE7`（`mov eax,[ebp-4]` + `mov ecx,[eax+8]`，6 字节）——
  `Mutex::Leave` 判活，死了走它自己的收尾出口 `+0x98F90`。

★ **D 不是可选项**：`ScopedLock` 的析构函数**无条件**调 `Leave()`，
而 `Leave` 读改的 `[state+0x10]` 和 `Enter` 崩的是**同一块** ⇒
**§82-A 每命中一次，作用域退出时就一定跟着一发往死内存写的 `Leave`**。
这次只是 `+0x75CDA` 先崩把它挡住了。`ScopedLock` 在这个 DLL 里用了 54 处。

★ **判「那一页读不读得到」，不是判「指针是不是 NULL」**：
`+0x75F0F` 先 `UnmapViewOfFile(p)`、`+0x75F18` 才置 0，中间指针非空但视图已经没了。

★ **基址随机的两个技术点**（同 §82）：回跳目标运行时算好放全局（`jmp dword ptr [g_…]`）；
偷的 6 字节里**一个重定位项都没有**（对着 `.reloc` 的 14960 项逐项查过）
⇒ 文件字节 == 内存字节，特征串直接从磁盘那份量。

### ★ 方法：这一族的判据是「**这个线程该不该继续跑**」，不是「这一句别崩」

§68 → §82 → §84 一路挪了两次崩点，每次都是「把当前这一句堵上」。
真正的判据原版自己写在序言里了：**七个字段任何一个是 0，这个线程就该结束**。
补在「拿到锁之后」而不是回边上，是因为**对象是在那 10 秒等待期间死的**，
回边上判过也没用。

### 顺手记下（没动）：认证服每次登录都挂一条 10 秒的死连接

登录成功约 10 秒后客户端发一发 `0x0015 CUUpdateSessionPacket`（载荷 = 票据），
`authserver.make_reply` 不认它 ⇒ `[hold] 不回包，挂住连接` ⇒ 客户端 10 秒后自己关
（`relay.out` 里「上行 86 / 下行 0」那些）。**和崩溃无关**（时刻对不上），
但白挂 10 秒 + 占一个连接槽。要修就是回 `0x0016 CUUpdateSessionReplyPacket`，
格式没逆过，**风险在登录路径上**（§80 / §83 的教训）。
★ **§85 之后这条自动没了** —— 发 `0x0015` 的是 `nmconew.dll`，它已经不跑了。

---

## §85 ★★★★★ NEXON 信使**整个拆掉了**：BigShot ⇄ 信使那层 RPC 的格式逆出来了（✅实机跑通，2026-09-11）

**结论**：§80 / §83 两次失败的原因 §83 自己已经写对了 ——「**登录真的需要一份应答**」。
这一版把那份应答的格式逆出来自己造，于是拆得掉了：
**`nmconew.dll` 不加载、`NMService.exe` 不起、nmcogame 一次都不被调用。**
登录 / 登录失败文案 / 票据 / 远程模式**全部实机跑通**。

### 一、那层 RPC 长什么样（`BSHOOK_NM_DIAG=1` 实测）

30 个包装函数全走 `BigShot.exe 0x544168`：
序列化 → `NMCO_CallNMFunc(func, 请求, &应答, &长度)` → 反序列化 → 调用方取 `[包对象+0x24]`。

请求和应答**同一个自描述格式**：

```text
34 ca 1f 00      魔数 0x001FCA34（和 §82-B 那个魔数是同一个）
<u32 bodylen>    = 8 + innerlen
01 01 6f 66      类标签：高 16 位(0x666F)必须等于**包对象的 [+4]**
<u32 innerlen>
<u32 字段0>      ★ 这一格就是 `[包对象+0x24]`，也就是包装函数的返回值
…后续字段…       u32 直读；字符串 = `u8(字符数 << 2)` + UTF-16LE
9e 11 8a 00      尾部魔数 0x008A119E
```

- 读取器 `0x5382A5` 只校验「两个 u32 的第一个，高 16 位 == 包对象 [+4]」，
  不等就**整包作废、所有字段读成 0**（`[+0x14]` = 有效位）。
  ⇒ 应答的类标签**照抄请求的**就永远对。
- 读原语 `0x537F30` **带边界检查**（`pos+4 > size+8` 就返回 0 且不推进）
  ⇒ **应答短了只会把后面的字段读成 0，不会越界** ——
  「不认识的 func 回一个最小应答」因此是安全的。

### 二、登录只用到两发

| func | 干什么 | 请求字段 | 应答字段 |
|---|---|---|---|
| `0x110F` | 登录 | u32 0 / u32 2 / 空串 / **用户名** / **密码** | u32 成功?1:0 / u32 **认证服结果码** |
| `0x2105` | 取「域」 | u32 0 | u32 1 / 字符串 = **票据** |

★★ **`0x2105` 返回的 32 位十六进制串就是我们自己认证服发的票据** ——
客户端拿它去 `gcpReqLogin`（游戏服认的就是它），也拿它当 `NMService -domain:`。
实测对得上：认证服 09:00:33.579 发 `f9ce8ec3…`，`0x2105` 09:00:33.819 原样回来。
⇒ **票据不是信使造的**，信使只是个传声筒。顺序是
「连游戏服 → 问 `0x2105` 要票据 → 发 `gcpReqLogin`」。

★ 错误码也是直通的：认证服回 20026 ⇒ 应答字段1 = `3a 4e 00 00`。
失败那一发我们造出来的 28 字节和原版**逐字节相同**。
★ 而且中文文案照样对（实测 `密码错误 (20026)`）——
说明 §7.4 记的「错误码映射表在 nmconew `0x10077000`」**至少不在这条路上**，
拿掉信使之后客户端自己就能把 20026 翻成中文。

### 三、于是 `nmco_call_stub` 干的事

把 BigShot 那 4 格 nmcogame 的 IAT（`0x637510` MemoryFree / `0x637514` SetPatchOption /
`0x637518` SetLocale / `0x63751C` CallNMFunc）全换成自己的桩：

- `0x110F`：从请求里取用户名 / 密码 → **自己连认证服**
  （协议 = `server/protocol.py`，opcode `0x0F` → `0x0C`，**服务端一行没动**）
  → 字段0 = (结果==0)，字段1 = 结果，票据记在 `g_nm_ticket`；
- `0x2105`：回 `1` + 票据；
- 其余全部：回「成功、没数据」（4 字节字段区）。

⇒ nmcogame 一次都不被调 ⇒ `nmconew.dll` 不加载 ⇒ `NMService.exe` 不起。
`nmconew.dll` 的加载钩子留着当**绊线**：真有谁去加载就挡下来 + 打日志。

★ **`nmcogame.dll` 本身还是被 Windows 加载器装进来的**（它在 BigShot 的输入表里，
`NMCO_MemoryFree` 那一格是静态导入）—— 但它的 `DllMain` 什么都不做
（原本也是登录才动），我们只是让它**一次都不被调用**。
要连这个也拿掉就得改 exe 的输入表，没必要。

### 四、副作用（都是好的，但要知道）

- 认证服每次登录从 **3 条连接变成 1 条**：原来是
  `0x0F 登录` + `0x2d CURequestOldFashionInfo` + 10 秒后那发不回的 `0x15`，
  现在只有 `0x0F`。`0x2d` / `0x15` / `0x0d` **都是 nmconew 发的**，它不跑了。
- ⇒ ★ `re/packet_api.md` §7.3 那条「看 `0x0d` 分辨正常退出 / 崩溃遗言」
  （V0.2 §238）**对新客户端不再成立** —— 没有任何 `0x0d` 了。

### 五、实机验过的（本机，2026-09-11）

| 场景 | 结果 |
|---|---|
| 本机服务器 + 正确密码 | ✅ 登录成功、进大厅、游戏服认票据 |
| 本机服务器 + 错密码 | ✅ `密码错误 (20026)`，应答字节和原版**完全一致** |
| **远程服务器**（经本机中继转到 192.168.11.215）| ✅ 登录成功、进大厅；中继日志「上行 62 / 下行 112」|
| `nmconew.dll` / `NMService.exe` | ✅ **一次都没出现**（日志 + `tasklist` 都确认过）|

★ **没验的**：登录之后大厅里那些信使相关的 UI（好友 / 消息）点开是什么样。
按设计是「空列表」（字段读成 0），但没人点过。

### 六、顺手发现的**原版 bug**（和本次改动无关，没动）

**登录失败之后「开始」按钮和两个输入框一直是禁用的**，只能重启游戏再试。
拿 `BSHOOK_KEEP_NM=1`（原版信使）做对照实验，**一模一样** ⇒ 原版行为。
`bshook` 现在只把**分区单选钮**解禁了（那行日志「分区单选钮被客户端禁用了，已解禁」），
`id=1004 / 1005 / 1006` 三个没跟着解。要修就在那儿顺手加三行。

## §86 ★★★★ 登录框顶部那个死掉的公告框**可以直接接管**：URL 是个 4 字节立即数，Navigate 不校验（🔍静态落锤，2026-09-11）

**结论**：登录对话框顶部 533×260 那块（`MPlay.Control.MiniBrowser`，id=0，
矩形 -1,0,533×260）是原版自带的 IE 宿主。**改一个 4 字节立即数就能让它显示我们的页面。**

### 一、导航点和控件创建在同一个函数里

```text
0x424057  push 0x104 / push 0x215    ; 260 / 533 —— 认它就是这个框
0x424067  call 0x442985              ; 内含 call [0x6E6260] = user32!CreateWindowExW
0x424089  68 30 27 66 00             ; push 0x662730 = L"http://gamepopshot.tiancity.com/launcher/notice.html"
0x42408E  call 0x443000              ; MiniBrowser::Navigate
```

`68 30 27 66 00` 这 5 字节**全镜像唯一**；`0x662730` 也**只被这一处引用**
（`re_bs.py xref` 核对过）⇒ 可当唯一特征串，只改 `0x42408A` 那 4 字节。

### 二、`Navigate`(0x443000) 对 URL 一个字节都不校验

```text
0x443010  cmp [esi+0x2c], 0     ; IWebBrowser2* 为空就整段跳过
0x443019  call [0x637334]       ; SysAllocString(arg)  ← 原样转 BSTR
0x443021  VARIANT(vt=VT_I4, lVal=2)          ; Flags
0x44303f  call [ecx+0x2c]       ; IWebBrowser2 vft+0x2C = Navigate
```

不补 `http://` 前缀、不查协议 ⇒ **`file:///` 本地路径它照单全收**。

### 三、`0x6E6260` 确实是 `user32!CreateWindowExW` 的 IAT 槽（双重证据）

1. ASProtect 的导入解析桩 `0x568762` 把「槽地址 / `"CreateWindowExW"` /
   `"user32.dll"`」**成对压栈**，随后 `jmp [0x6E6260]`；
2. 调用点 `0x442A9D` 的 `call [0x6E6260]` 前正好 **12 个 push**，
   其中 `lpClassName` 位上是 `0x6660B4 = L"MPlay.Control.MiniBrowser"`。

这个槽只被 BigShot 自己的三处引用（`call` @ `0x40e2f7` / `0x442a9d`，
`mov edi,[…]` @ `0x4106ff`）⇒ 改 IAT 比内联 hook `CreateWindowExW` 干净得多
（后者会拦下全进程每一次窗口创建）。

### 四、这个框现在是什么状态

域名 `gamepopshot.tiancity.com` 早就注销，框里显示的是
`res://ieframe.dll/navcancl.htm`（「已取消网页导航」）。它走 **wininet**，
不走 ws2_32 ⇒ 现有的 connect 改写拦不到它（V0.2 D065 的「反悔条件」里点过名）。
mshtml / ieframe / urlmon 在**登录框出现那一刻**才加载（实测启动后约 6 秒）。

## §87 ★★★★ BigShot 的 IAT 槽是 ASProtect **惰性解析**的 —— 拿它当钩子挂载点会退化成「比时间」（✅实机落锤，2026-09-11）

**结论**：`0x6E6260`（`user32!CreateWindowExW`）那样的槽，**解壳完成时里面还是
解析桩，不是真地址**。要等 BigShot **第一次真的调用**这个 API，桩跑一遍把真
地址写回槽，之后才是 `real`。

**实测数字**（公告框那次，见 §86 / D84）：

```text
+2.5s   解壳完成（地区锁那五处特征串当场全部对上）
+7.9s   0x6E6260 才变成 user32!CreateWindowExW   ← 槽被解析
+8.2s   mshtml / ieframe 加载 = 公告框控件起来了
```

⇒ 想用「认出 MiniBrowser 控件创建」当事件驱动的触发点，**钩 IAT 槽是做不到的**：
钩子装上的那一刻和它要拦的那件事只差 0.3 秒，纯属撞运气。当时还为此在
`patch_thread` 里死等了 4.4 秒，把后面所有 patch 一起拖慢。

**正解：内联钩 `user32!CreateWindowExW` 导出函数本身**（`install_inline_hook`，
和 `MessageBoxW` 同一套）。导出函数地址在 user32 一加载就有，**和解壳进度无关**，
实测 `+0.4s` 就装上了 —— 比公告框控件创建早了 7 秒以上，而且这个「早」是
结构性的，不依赖机器快慢。

★ **不能拿解析桩当原函数链下去**：桩尾是 `jmp [槽]`，会再读一次槽，
而槽里已经是我们的 detour ⇒ 无限递归。

★ 反过来说，`try_kill_nm` / `try_guard_nexon_messenger` 那两处 IAT 改写没事，
是因为它们**不在乎早晚**（只要在那个 API 被用到之前装上就行，而它们正是在
「槽已经是 real」时才动手）。判据是「你要的是不是**早于某件事**」——
是的话，别用 IAT。

## §88 ★★★★ 公告框（MiniBrowser）的宿主实现：文字选中被关死、链接不开浏览器；**事件接收器压根没被 Advise**（✅实机落锤，2026-09-11）

### 一、`IDocHostUIHandler`（vft `0x665ecc`，18 个槽）—— mshtml 真的在用它

槽 4 `GetHostInfo` = `0x442f2b`：

```text
00442f2f  c7 00 14 00 00 00      mov [eax],   0x14      ; cbSize
00442f35  c7 40 04 8f 00 01 00   mov [eax+4], 0x1008F   ; dwFlags
```

`0x1008F` = `DIALOG(1) | DISABLE_HELP_MENU(2) | NO3DBORDER(4) | SCROLL_NO(8) |
FLAT_SCROLLBAR(0x80) | THEME(0x10000)`。

- **`DOCHOSTUIFLAG_DIALOG` 就是「文字不许选中」**。改成 `0x1008E` 后拖蓝 + Ctrl+C 全好使（实机拖选截图核对过）。
- **`SCROLL_NO` 要留着**：公告框只有 260 像素高，内容超了是**直接裁掉、不出滚动条**。排版必须自己算准。
- 槽 3 `ShowContextMenu` = `0x442f49`（`xor eax,eax; ret 0x14` = S_OK「宿主处理了」）⇒ **右键菜单永远没有**，改不改 DIALOG 都一样。所以只能拖选 + Ctrl+C。
- 槽 12 `TranslateAccelerator` = `0x44327a`（E_NOTIMPL）⇒ 快捷键走 mshtml 默认处理，Ctrl+C 天然可用。

### 二、★ 事件接收器 `MiniBrowserEvent` **没被 Advise**

`Invoke`(`0x443282`) 按 DISPID 分流得好好的：

```text
250 BeforeNavigate2 -> [this+0x24] = 0x44335f   xor al,al; ret 4   （放行，框内导航）
251 NewWindow2      -> [this+0x20] = 0x46f801   mov al,1;  ret     （弹窗一律取消）
```

**但它一次都不会被调到。** 证据：把 `0x44335f` 整个换成我们的 detour 之后，
点公告里的链接照样在框内跳到 GitHub，而我们的日志**一行都没有**；
回读活进程确认 `e9 9c 32 fe 5c 90 90` 在位、vftable `0x66600c` 也指着它。
⇒ 想拦导航，**别指望 `DWebBrowserEvents2`**。

### 三、能拦导航的是 `IDocHostUIHandler::TranslateUrl`（槽 16）

mshtml **直接调宿主接口**，不经过连接点，所以一定会被调到（§88-一 已经证明这张
vftable 是活的）。原值是 `0x44327a` —— 一个 **九个槽共用**的 `E_NOTIMPL` 桩
（`b8 01 40 00 80 / c2 10 00`），**绝不能 patch 那个桩**，只改 vftable 里
`0x665f0c` 那一格指针。

`TranslateUrl` 不能取消导航，但能**换掉目标 URL**，效果一样：
`http(s)://` → `ShellExecuteW` 开系统浏览器，同时把目标换回我们自己那条
`file:///` 公告页 ⇒ 框里看不出跳转过。**实机验证：Edge 起来了，公告框内容没变。**

★ 顺带：原版**从来不开外部浏览器**。玩家印象里「点公告里的新闻会开浏览器」
指的多半是更新器（NGM / `BsPatcherChn`）那个窗口，不是登录框这个。

## §89 ★★★★ 握手那 4 个字节的新布局：版本号 + hook 完整性校验位（✅实机跑通，2026-09-11）

**背景**：旧编码上报的数字**完全来自包根 `BUILD.ver` 这个明文 JSON**
（`bshook.c:read_build_ver`）—— 把版本号改大就绕过了版本门禁，**连重编都不用**。

**新布局**（仍然是那 4 字节，协议一个位都没动）：

```text
bit31      = 1        新编码的标记（原版/旧客户端裸发的是小正数，恒为 0）
bit30..24  = 校验位   sha256( sha256(bshook.dll) ‖ 版本码的 4 字节小端 )[0] & 0x7F
bit23..0   = 版本码   ★ 明着放，合法范围 1000 ~ 15999999（major <= 15）
```

- **版本码必须明着放**：否则服务端的 `manifest-hook.json` 一旦比客户端旧，
  新客户端一条都匹配不上 ⇒ 被判「改过」⇒ 强制更新到它**已经是**的版本 ⇒
  **死循环**。明着放才分得清「这个版本我不认识」和「我认识但校验位不对」。
- **合法版本码只到 15999999**，不是整个 24 位：不然 `0xFFFFFFFF`（-1，典型乱值）
  会解成一个「合法」的 V16.777.215。**既有测试 `test_decode_legacy_and_garbage`
  当场抓到过这个回归。**
- 实测向量（真客户端）：`sha256 = fc75d4…0860` + V0.3.1(3001) -> 校验位 123，
  上报 `0xFB000BB9`。C 侧和 Python 侧逐位对过，`test_hookintegrity.py` 钉着。

**为什么只有 4 字节**：`SimpleCipher` 是有状态流密码，往流里插一个字节后面
全部错位；hook 要注入就得接管整条流的加解密（游戏自己那份 cipher 状态不会
跟着偏移），那是往最热的路径上动刀。⇒ 想上报 32 字节的 hash 本身是做不到的，
只能把它**当密钥用**，折进这 4 字节。

**四条判定路径**（`versioning.verify_client_hook`，四条都实机验过）：

| 情况 | 结论 | 实测 |
|---|---|---|
| 版本在清单里、校验位对得上 | 放行 | ✅「校验通过」→ 登录成功 |
| 版本在清单里、校验位对不上 | **拒 + 强制更新** | ✅「123 != 应有的 33」→ 客户端自己拉起 `BsPatcherChn.exe` |
| 版本**不在**清单里 | **拒 + 强制更新**（2026-09-11 收紧）| 单测钉着；配套安全阀见下 |
| 版本**比清单里最老的还老** | 放行，交给版本门禁（那一版发布时还没有这项功能） | 单测钉着；★ 门禁要抬到清单最老版本才堵得住 |
| 对方**没上报版本号** | 放行 | 交给版本门禁 —— 不然 ClientFilter 填 `0` 就失去意义 |
| 清单缺失 / JSON 坏了 | **放行** + 告警 | ✅（手滑写坏过一次，正好验到） |
| **服务端自己的版本不在清单里** | **整项校验关掉** + 告警 | ✅ 把 BUILD.ver 改成 V9.9.9 实测 |

★★ **最后那条是收紧之后必须配的安全阀**：被拒的客户端会去下载
**服务端自己那个版本**（拒绝文案里写的就是它）。服务端自己都不在清单里的话，
客户端下完还是不在清单里 —— **死循环，而且解不开，全服没人进得来**。

★ **客户端自校验永远能被绕过**（patch 掉算 hash 那段即可），而且期望值就明写在
随包发出去的 `manifest-hook.json` 里。这一步的意义只是把门槛从
「**改个 JSON 文件**」抬到「**真的会用调试器**」，别当成不可破。

## §90 ★★★★★ 更新器有两个洞，「强制更新」一直到不了下载那一步（✅实机落锤，2026-09-11）

做 §89 时用户问「updater 是不是也要改」—— **要，而且发现的第二个是既有的严重 bug。**

### 一、`pick_target`：被服务器拒了，却因为「版本号一样」判定无需更新

```c
if (g_ctx.local_valid && ver_cmp(&g_ctx.local, &m->entries[i].version) >= 0)
    return NULL;               /* 服务器要的版本本地已有 */
```

这条在「拒绝的唯一原因是版本太旧」的年代是对的（版本一样就不可能被拒）。
§89 之后多了第二个拒绝理由（**文件被改过**），于是被篡改的客户端会停在
「已是最新版本，无需更新」，**谁也修不好它**。
⇒ 加 `force` 参数：探针回 `PROBE_REJECTED` 时无视版本比较，
重装同一个版本正好把被改掉的文件覆盖回去（自愈）。
（顺带修掉：`wanted` 那条分支原本忘了置 `*need_update = 1`。）

### 二、★★ `cfg_server_address` 读不到配置，探针一直在连 `192.168.1.100`

```c
n = MultiByteToWideChar(CP_UTF8, 0, raw, got, out, cap - 1);
if (n <= 0) n = -1;     /* ← 缓冲区放不下时它返回 0，这里当成了「读取失败」 */
```

`read_text_file` 读前 8190 字节，而 `cfg_server_address` 的缓冲区只有
**4096 wchar**。`config/server.config` 现在 10515 字节、中文注释密集，
8190 字节解出来约 4960 个宽字符 —— **放不下 ⇒ 返回 0 ⇒ 整个文件读失败 ⇒
悄悄退回默认值 `192.168.1.100`**。

**这是个既有 bug，而且症状完全沉默**：日志里只有一句
`probe unreachable, fall back to newest`，看不出它连的是哪儿。
后果是**探针从来问不到「服务器要哪个版本」**，成对发布（D079）一直没生效 ——
更新器永远装最新版，而不是服务器点名的那个。

修三处：① 放不下时把输入截短到 `cap-1` 字节（退到 UTF-8 起始字节）再解一次，
**不再整个失败**；② `read_text_file` 上限 8190 → 65536；
③ `cfg_server_address` 缓冲区 4096 → 32768 wchar。
④ 顺手加一行 `log_line("probe host %ls:%d (root=%ls)")` —— 这个 bug 就是靠它
两分钟定位的，以后同类问题不用再猜。

### 三、探针自己也要带校验位

探针重演握手时若还发「裸版本号」，服务端会判它「本该带校验位却没带」⇒
**`PROBE_OK` 永远不可能出现**，「服务器已接受当前版本，无需更新」那条路等于废掉。
⇒ `probe_encode_wire()` 和 `bshook.c` 的 `hsver_pick_wire()` 同一套算法，
hash 的是 `<root>\hook\bin\bshook.dll`。

### 四、实机落锤

把清单里的 sha256 改错一位（模拟 DLL 被动过）后：

```text
服务端  ✗ hook 完整性 拒绝 客户端版本=V0.3.1 原因=校验位 122 != 应有的 119
更新器  probe host 127.0.0.1:27799 → target V0.3.1 → 开始下载（测速 37.9 MiB/s）
```

修之前这里是 `probe unreachable` → `已是最新版本，无需更新`。

---

## §91 ★★★★★ 存档转移的三条硬事实：旧服务端会**静默清档**、`popshot_save` 从来没被验过、两个运行时只有标准库（🔍代码 + ✅实测，2026-09-11）

### 一、★★★ 旧服务端拿到 **V0.3.2 的新格式存档** 会把号**清空**，还报「上传成功」

不是推测，是照着旧 `parse_save` 逐行推出来的，机制在 D86 六：

```text
v2 文件里没有 "account" 键
  -> 旧 parse_save 走「退化形状」：把整个 payload 当账号
  -> username 取到了（明文区就有）
  -> fields = payload 里命中 NEW_ACCOUNT_DEFAULTS 的键 = 只剩 {password, display_name}
  -> import_account 是「从默认值起手 + 覆盖」
  => 等级 1 / 金币 0 / 仓库空 / 材料空 / 礼物盒空，**回执写的是「上传成功」**
```

**这个洞在新代码里堵不上** —— 旧服务端已经发出去了。能做的只有三件，都做了：

1. 下载的文件名带上版本：`popshot-save-v2-<用户名>.json`（玩家手里很可能同时
   躺着 v1 和 v2 两个文件，文件名是他唯一能分辨的地方）；
2. 注册页两个 tab + 两份 README 都写死「两台服务器都要 V0.3.2+」；
3. **发版顺序：先把服务端整体覆盖成新版，再发客户端包。**

一个天然的安全网：旧服务端的回执会当场打出「（当前等级 1、经验 0、金币 0）」，
玩家立刻看得出不对 —— 但那时数据已经被覆盖了，这只是「早点知道」，不是「没事」。

### 二、`popshot_save` 这个格式标记，从 V0.2 写到 V0.3.1 **一次都没被读过**

`account_store.py` 里那行注释写着「导入时用它认一眼，避免用户传错文件」，
而老 `parse_save` 通篇没碰它，实际判据只有「有没有一个合法用户名」。
全仓库 grep `popshot_save` 只有三处命中：定义 + 写入，以及两个测试夹具里手写
的常量。**V0.3.2 起它是必验的第一道门**（并且要先挡布尔：`isinstance(True, int)`
是 `True` 且 `True == 1`，一个 `"popshot_save": true` 的文件不特判就会被当成
「旧版明文存档」）。

⇒ 一般化的教训：**「写进去了」不等于「被读了」。** 加一个格式标记的同时
就得加那条读它的用例，否则它只是一个看着让人安心的装饰。

### 三、两个内置运行时**只有标准库**，但密码学够用（✅实测）

`runtime/python`（3.14.3 x64）和 `runtime-win7/python`（3.8.10 win32）的
`Lib/site-packages` **都是空的**，全仓库没有 `cryptography` / `pycryptodome`，
连 `hmac` 在本次之前都没人 import 过。实测可用：

```text
hmac / hmac.compare_digest / hashlib.sha256（_hashlib.pyd 在）/
secrets / base64 / struct / zlib   —— 3.8.10 上全部 OK
```

⇒ **标准库里没有 AES**，所以对称加密只能拿 HMAC 当 PRF 跑计数器模式（D86 三）。

顺带两条实测数字，以后估体积用得上：
- 400 件仓库的账号，明文 JSON 约 15 KB，zlib+base64 之后 **1472 字符**
  —— 比原来的明文存档还小，离 `MAX_BODY_BYTES = 1<<20` 远得很；
- `zlib.compress(b"\0" * 64MB)` 只有 **62 KB** —— 1 MB 的请求体里塞得下能把
  内存吃光的炸弹，所以解压必须封顶（D86 五）。
  `zlib.decompressobj().decompress(data, cap)` 的 `eof` 在「输出长度刚好等于
  cap 而流真的结束」时**仍然是 `True`**，所以判据写 `if not eof: 拒` 就够，
  不需要 ±1。

---

## §92 ★★★ hook 的编译现在是**可复现**的：`/Brepro` 之前每次重编都换 SHA（✅实测，2026-09-11）

**结论（现状）**：`hook\build.bat` 的两条 `cl` 都带 `/Brepro`（编译和链接**各一个**），
连编两次的 `bshook.dll` / `bsloader.exe` **逐字节完全相同**。
⇒ **同源码 = 同字节 = 同 SHA**，打包反复跑也不会弄脏 `hook/bin/` 和 `manifest-hook.json`。
`server/test_hookintegrity.ReproducibleBuildTests` 钉着这两个 `/Brepro`。

### 改之前是什么样（这才是加它的理由）

不带 `/Brepro` 连编两次，源码一个字没改，两次产物**长度一字节不变**，
内容**各只差 4 个字节**，全是 PE 里的 `TimeDateStamp`：

| 产物 | 长度 | 不同的偏移 |
|---|---|---|
| `bshook.dll` | 212480 | `0xF8` `0xF9`、`0x2F504` `0x2F505` |
| `bsloader.exe` | 126464 | `0x108` `0x109`、`0x1BB84` `0x1BB85` |

`bshook.dll` 的 `e_lfanew = 0xF0` ⇒ `0xF8` 正是 COFF 头的 `TimeDateStamp`
（第二处是调试目录里那份副本）。四字节里只有低两字节不同，是因为两次编译隔得近。
实测四次连编的 sha256 前 8 位：`4cfcc7f7` → `6364beca` → `f3365984` → `203392a0`。

这两条后果本来会天天碰到（D87 起**每次打包都重编 hook**）：
① 每打一次包，`hook/bin/*` + `server/manifest-hook.json` 三个文件无缘无故变成改动状态；
② **同一个版本号重打一次包，先前发出去的那批客户端就对不上新清单**了
（D85 只冻结「比最新发布还老」的版本，正在做的那一版随便刷）。

### `/Brepro` 到底改了产物的什么（✅逐字节核对过，别再重新怀疑）

加上 `/Brepro` 之后和加之前那份比，`bshook.dll` 差了 **4720** 字节 —— 数字吓人，
但来路查清了，**一行代码都没变**：

| 区域 | 差异 | 来路 |
|---|---|---|
| PE 头 | 9 字节 | `TimeDateStamp` 从真实时间 `0x6AA405D2` 变成内容哈希 `0xD83770A5`（**不是** `0xFFFFFFFF`） |
| `.rdata` | 4538 | **调试目录多了一条 `IMAGE_DEBUG_TYPE_REPRO`**（1 条 28 字节 → 2 条 56 字节），它后面的东西整体挪了 `0x20`（POGO 数据 rva `0x30ED8` → `0x30EF8`）⇒ 一大片 4 字节指针表只有低位字节变 |
| `.reloc` | 109 | 跟着上面那些 RVA 改 |
| `.text` | 64（52 处单字节） | 引用 `.rdata` 的那些立即数跟着挪 |

⇒ 就是一次**重新排布**，不是代码变了。`bsloader.exe` 同理（3561 字节，同样的四块）。
★ hook 打的是 `BigShot.exe` 里的**硬编码地址**，和它自己的布局无关，所以这次挪动
在原理上碰不到补丁逻辑 —— 但**实机还是要跑一次**（这版还没跑，见 Z-76）。
