# V0.3 后半 PROGRESS —— 现在在哪

**只保留当前状态。** 做完的事从「正在做」挪走，不留历史；流水账进 `sessions/`。

最后更新：2026-09-05（会话 05：**下发真实等级**，原版七道等级门改由 bshook
patch（D22 / §32）；管理页新增**玩家资料**页（D23，含装备只给「有/无」、
列表 10 行一页、物品选择器高度钉死）。服务端 + 客户端已重启，
`bshook_*.log` 里七行 `★玩家等级门槛` 全部命中；界面效果**等实机**）

---

## 现在在哪

| 里程碑 | 状态 |
|---|---|
| M0 进度管理体系 | ✅ **完成** |
| M1 数据基线 | ✅ **完成** |
| M2 存档扩展 | ✅ **完成** |
| M3 ★ spike：装备生效 | ✅ **完成，实机验证过**（§16）|
| M4 ★ spike：商店 UI 协议 | ✅ **完成，实机验证过** —— 货架真的画出来了（§21）|
| M5 商店购买 + 仓库装备 | 🟡 **代码全写完了**；四个实机 bug 的根因已修（§28 / §29），**⏳ 等实机复验** |
| M6 材料掉落 + 结算显示 | ✅ **完成，实机验证过** |
| M7 合成 | ⬜ **线格式全逆通了**（§27），代码没动 |
| M8 管理员网页 | ✅ **完成**；★ 新增「玩家资料」页（找人 / 改等级金币材料 / 非商店物品，D23）|
| M9 测试 · 兼容 · 打包 · 上线 | 🟡 打包 `$must` 已补上商店三件套；还差云上迁移演练的后一半 + 打包实跑 |

**一句话现状**：商店段**所有**上下行现在都有语义，没有 ❓ 剩下。服务端会回：
货架 `0x0500`、**物品定义 `0x0501`**、购买结果 `0x0502`、空礼物 `0x0508`、
**持有物清单 `0x0601`**、穿着的装备 `0x0604`，并处理穿 `0x0702` / 脱 `0x0703`。
★ **等级从此是真实等级**（D22）：商店按真实等级卖、客户端按真实等级显示，
对战 / 生存 / 5-6 人房 / 全地图 / 天梯的解锁全部由 hook 打补丁完成。

## ✅ 已经实机落锤的

**货架整条链路**：图标 + 中文名 + 价格 +「金币」+「购买」按钮全画出来了，
翻页、角色切换、武器 1/2/3 子分类过滤都对（§21），6 个顶级标签的分类 id 全对（§22）。
**排序按钮**：进商店的默认状态是「基本顺序」（= 标志位 0）。

**物品定义 `0x0501` 那一整套（§28 / §29）**：左侧人物模型一进商店就带装备、
仓库列出装备 + 材料（材料在「收集品 → 材料」标签下）、仓库里手动穿脱生效。

**买东西（§30）**：跨角色买（泰尔买布洛克的枪）正常、买完选「直接装备」
真的穿上了、失败框写的是具体原因不再是「未定义的错误」。

**探针轮的负结果只对货架格子成立**：`ShopStock` 那三格里有两格提示框会读
（§31）—— **别再拿「某个面板不读」当「谁都不读」**。

**等级门槛的七处 patch（§32）**：`logs/bshook_*.log` 里七行
`★玩家等级门槛 @ …` 全部出现 ⇒ 特征串在解壳后的真进程里都命中。
**管理页玩家资料页**：找人、材料可改、仓库里商店在卖的 9 件带 🔒 只读、
装备类只给「拥有 / 没有」开关并标出「穿着」、把整份资料原样保存回去
`changes` 为空（存档里残留的 ×2 / ×3 不会被误当成改动）、关掉再打开一件
装备真的来回生效、「往仓库塞一件商店在卖的」被服务端拒绝并写清是哪件
—— 都在本机跑通了。

## ⏳ 待用户验证

★ **服务端和客户端 19:20 都重启过，要重新登录。**

**A. 等级这件事（本次改动，全新）** —— 拿一个**1~3 级**的号试：

1. 右上角数据栏该显示**真实等级**（以前恒显示 ≥4）。
2. 大厅点「**对战**」标签 → 不该弹「不符合等级要求，无法连接」。
3. **建房(对战)** → 「人数」下拉框里该有 **5 人 / 6 人**。
4. 建房时选「**生存**」→ 进房间后**不该**自己变回「夺分」。
5. 建房建出来的**地图不该被强制成「지하유적 입구 / 地下遗迹入口」**。
6. 大厅「**天梯**」标签 → 不该弹「等级 6 以上才能玩」
   （⚠ **能点进去就算这条过了** —— 服务端没有天梯实现，里面大概率是空的）。
7. 关卡 / 全地图 / 难度和以前一样能选（这几条是老 patch，只是确认没被带坏）。
8. ★ **商店**：拿一个等级不够的号去买高等级的枪 → 该弹「**等级太低**」，
   不再是买得动。够等级的照常买得到。

**B. M5 复验（上一轮就欠着的）**：

9. 待机房间下面点仓库 → 装备一件 → **再点一次「卸下」该真的卸下**。
10. 商店提示框的价格该只有一个数（「3000 金币」），不再是「0 → 3000」。
11. 商店 / 仓库提示框下半那块该有内容（武器写伤害/弹匣/换弹/初速）。
    ⚠ 特别看**换行**：说明是用 `\n` 分行的，挤成一行或冒方块字符要说。

**C. 管理页玩家资料（新）**：

12. `http://<服务器>:27810/admin` → 「玩家资料」标签 → 搜自己的号 → 改等级
    和金币 → 保存。**人在线时不用重登**，右上角数据栏该当场跟着变。
13. 材料数量改完 → 进游戏仓库「收集品 → 材料」该对得上。
14. 给自己开一件**商店买不到的**装备（仓库那栏的「拥有」开关）→ 保存 →
    游戏里仓库该看得到、也穿得上（★ 前提是**够等级**，不够会弹
    「等级太低，无法装备道具」，那是对的）。

**结果怎么给我**：一句「行 / 不行」+ 不行的话说清是哪一条、界面上写的什么。

## 当前卡点

没有卡点。

---

## 下一步

1. **实机验证 A / B / C**（见上）。
2. **M7 合成**：线格式已经全逆通（§27），可以直接写。
   ⚠ `0x0606` 认的是**产物 itemId** ⇒ `recipe.json` 里 `result` 不许撞车。
   ★ 合成成功之后要**主动推 `0x0501` + `0x0601`**（和买东西同一条路，D20）。
3. 本版**不做**修理（上行 `0x0604`）、卖出（`0x0603`）、礼物（`0x0609`）、
   用道具（`0x060b`）—— 语义都查明了（§24）。
4. ❓ 还没逆的：`0x0503`（一个 bool + 提示框）、`0x0504`、服务端方向的
   `0x0700`（处理器 `0x552a73`）和 `0x0702`（`0x552dca`）。

## 给接手会话的五句话

0. ★★ **§31 是本版最容易再犯的坑**：「探针在这个面板上没出现」只证明
   **这个面板**不读，别升成「死字段」。§23 和 §26 各栽了一条，三个都是实机才发现的。
1. ★★ **等级只有一个数**（§32 / D22）：显示、原版等级门、**穿装备判定**
   共用 `[0x72e338]`。以后再想「抬一下等级绕过某个门」之前先读这一节 ——
   抬它就等于同时放宽商店的等级门槛。要绕门就去 `hook/bshook.c` 的
   `try_patch_player_level_gate()` 里加一条。
2. **先读 `FINDINGS.md` §28 / §29 / §30 / §31** —— 商店段能不能用全看这几节：
   客户端有**两张**物品表，`ShopItem-Chn.ini` 只填图标和 PartFlag，
   **名字 / 等级 / 角色限定 / 可装备位全靠服务端的 `0x0501`**。
3. 再读 §20 ~ §25（协议）和 §16（装备生效的实机落锤）。§4 的老表已作废。
4. **两处顺序都是硬要求**：`0x0501` 定义排在 `0x0500` / `0x0604` / `0x0601`
   前面（D20）；买完那几发里 `0x0502` **排最后**（§30②）。
   穿脱那条最容易踩的坑在 §24：**每一发都要回一发 `0x0604`**，失败路径也要回。
5. 实机万一界面不对，用控制通道的 `shop-reply off <opcode>` 二分
   （现在管六发：`0500` / `0501` / `0502` / `0508` / `0601` / `0604`），
   **不用改代码也不用重启**。`sync-account` 会把商店三件套一起刷；
   管理页的玩家资料页保存时走的是同一套推送。
