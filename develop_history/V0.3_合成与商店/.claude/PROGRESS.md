# V0.3 后半 PROGRESS —— 现在在哪

**只保留当前状态。** 做完的事从「正在做」挪走，不留历史；流水账进 `sessions/`。

最后更新：2026-09-04（会话 03：M8 管理页改结构化表单 + 原版配色，D16；
M3 / M4 第 1 步 / M6 仍在等实机验证）

---

## 现在在哪

| 里程碑 | 状态 |
|---|---|
| M0 进度管理体系 | ✅ **完成** |
| M1 数据基线 | ✅ **完成** |
| M2 存档扩展 | ✅ **完成** |
| M3 ★ spike：装备生效 | 🟡 代码完成，**⏳ 等实机验证** |
| M4 ★ spike：商店 UI 协议 | 🟡 第 1 步（只装监听）完成，**⏳ 等实机采数据** |
| M5 商店购买 + 仓库装备 | ⬜ 依赖 M3 + M4 的实机结论 |
| M6 材料掉落 + 结算显示 + 调试 bat | 🟡 代码完成，**⏳ 等实机验证** |
| M7 合成 | ⬜ 依赖 M4 + M6 |
| M8 管理员网页 | ✅ **完成**，已改结构化表单（D16）（⏳ 只差你上去点一下看看） |
| M9 测试 · 兼容 · 打包 · 上线 | ⬜ **下一步** |

**一句话现状**：能存、能穿、能掉材料、结算包也按顺序发出去了，商店那四发请求
也有人接了，管理页也能改配置了 —— **但客户端收到之后到底什么样，
一件都还没在真客户端上看过**。三件事凑成**一次进游戏**就能全验完（清单在下面）。

---

## 已经能用的东西

M1 那四件（`tools/shopdata.py` / `shop_items.json` / `shopdata.py` / `shopcfg.py`
+ 三份运营配置）见 git。**M2 / M3 / M4 / M6 / M8 新增：**

| 东西 | 是什么 |
|---|---|
| 存档三字段 + `admin_accounts` | `inventory` / `equipped` / `materials`；管理员默认 `admin`/`Admin123` |
| `AccountStore` | 9 个物品方法 + 5 个 `admin_*` + `ensure_item_fields()`（幂等补齐） |
| `app.py` 启动钩子 | `_report_item_fields()` |
| `0x030b` | 清单 = **商城角色 id + 身上的装备 id** |
| `0x041c` | `build_reward_received()`；结算按 **`0x041c`×N → `0x0309` → `0x0411`** 发 |
| 掉落 | `quest_materials(quest_id, difficulty, cleared, mode, rng)` 纯函数，读 `drops.json` |
| 商店监听 | `on_shop_probe()` 接住 `0x0704/0x0605/0x0607/0x0700/0x0602/0x0603/0x0604/0x0606`，**只打 hexdump 不应答** |
| 调试命令 | `inv` / `give` / `give-material` / `equip` / `unequip` / **`clear`** |
| `tools/quest-clear.bat` | 一键「通关并结算」（调 `gs_ctl clear`） |
| 管理页 | `server/web/admin.{py,html,css,js}`，和注册页共用 27810。**结构化表单**（不再有 JSON 原文，D16）：商店卡片网格 / 配方 4 材料槽 / 掉落规则行，每件东西带图标；改完存盘即生效；管理员增删改密 |
| 物品图标图集 | `server/web/itemicons.png`（664 个图标，1536×1792 PNG8，0.62 MB）+ `itemicons.json`；`tools/shopicons.py` + `update-shopicons.bat` 离线生成 |
| `shopcfg.SCHEMA` | 字段描述表，**贴着 `validate_*` 放**，管理页照它生成输入框；`test_shopcfg.SchemaTests` 卡死两边一致 |
| `shopcfg.QUEST_ZH` | 关卡 id → 中文名（就 7 个，见 FINDINGS §15）。掉落的「关卡」是下拉框，不用背号码 |

**回归**：2433 项绿 × 两个运行时（CPython 3.14.3 + Win7 3.8.10）。

**已实机验证的**（其余一律没验）：
- 真实 `accounts.json`（7 个账号）迁移演练：原有字段一个不少、值一个不变；
  服务端连起两次，第二次一个字都不打（幂等）。
- 控制通道认得全部新命令；`quest-clear.bat` 编码 / 中文 / 错误路径都正常。
- 管理页（改版后）在浏览器里逐个走查过：三个标签页渲染、图标切片、筛选、
  物品选择器、脏标记、保存落盘（`_说明` 确实不再回写）、校验失败时按
  `rules[2]` 这类下标高亮到出错那一条 —— 全部正常，控制台无报错。
  44 条 HTTP 用例覆盖登录 / 限速 / 改配置 / 管理员增删 / 图集 / catalog
  （**未登录访问每个接口都必须 401** 那条最要紧）。
- `tools/update-shopicons.bat` 端到端跑通；`build.bat -Server` 打出来的包里
  `admin.css` / `admin.js` / `itemicons.png` / `itemicons.json` 都在，自检 200。

★ **`logs/server.err` 里的 `ConnectionResetError` 是既有噪音**，不是本版引入的
—— 对**注册页**做同样的连接 reset 会打出一模一样的栈（标准库
`http.server` + `ThreadingMixIn` 的行为，浏览器关标签页就会触发）。
它让「err 该是空的」这条健康检查失效了，已单独开任务收拾。

---

## ⏳ 待用户验证 —— **一次进游戏，三件事一起验**

先 `start.bat`，登录，**建一个房间**。三段可以按顺序做完再退出。

### ⏳ [M4] 第一段：点开「商店」，看服务端收到了什么

★ **这一段只要点开就行**，界面空着 / 转圈 / 报错都是有效结果 ——
服务端**故意一发都不回**，就是要先看客户端自己会怎么样。

**怎么做**：在大厅或房间里点「商店」按钮（合成、仓库界面也顺便点一下）。

**期望看到**：`logs/server.out` 里出现 `[shop-probe]` 开头的行。
**四发**应当依次是 `0x0704` → `0x0605` → `0x0607` → `0x0700`。

**怎么告诉我**：把 `logs/server.out` 里所有 `[shop-probe]` 的行贴给我
（`logs/online.log` 里也有一份，那份不会被下次启动覆盖）。
再加一句「界面长什么样」（空的 / 转圈 / 崩了 / 有商品）。

### ⏳ [M3] 第二段：穿上装备，属性变没变

**① 血条变长**（100% 生效的加法，最好看）

```bash
python tools/gs_ctl.py equip 1010037 && python tools/gs_ctl.py equip 1020062 && python tools/gs_ctl.py equip 1050037 && python tools/gs_ctl.py equip 1040037 && python tools/gs_ctl.py equip 1030058
```

五件铠甲合计 **HP +29**，泰尔基础 100 ⇒ 开一局血条最大值应当是 **129**。

**② ★ 武器 —— 最不确定的一条**：`0x030b` 里放武器 id，客户端会不会真换枪？

```bash
python tools/gs_ctl.py equip 1120012
```

`리볼버 D2` 伤害 5（基础左轮是 4）。打怪看伤害数字变不变。
**没变也是有效结论** —— 说明武器要走 `0x0604`，M5 的换装设计得改。

### ⏳ [M6] 第三段：结算界面「合成材料」那一栏

**开一局闯关**，进到关卡里（不用打完），然后：

```bash
tools\quest-clear.bat
```

**期望看到**：结算界面左下角「合成材料」那一栏画出材料图标。
（默认 `drops.json` 里关卡 3 难度 1 只有一条 25% 的规则 ——
一次没掉就多跑两次 `quest-clear.bat`，或者选难度 3 打，掉率高得多。
`python tools/gs_ctl.py inv` 可以看材料到底进没进存档。）

**怎么告诉我**：「那一栏画出来了 / 没画出来 / 界面崩了」。
`server.out` 里会有「掉落材料 座位N: …」和「已结算本局：每人各收到 N 份
gspRewardReceived(0x041c…)」两行可以对照。

### ⏳ [M8] 第四段（不用进游戏）：管理页

★ 这一段**会话 03 已经改版**：JSON 文本框没了，三份配置都是结构化表单
（D16）。agent 已经在浏览器里逐个走查过渲染 / 筛选 / 选择器 / 保存 / 报错定位，
**但没有替你登录**（不往表单里填口令），所以下面这几步还得你自己点。

服务端跑着的时候浏览器开 <http://127.0.0.1:27810/admin>，用
`admin` / `Admin123` 登录。

**期望看到**（四件事）：

1. 顶上一条红字提醒改默认口令；配色是米白纸面 + 琥珀边框，每件东西**都有图标**。
2. **商店目录**：改一个价格 → 保存 → 提示「即刻生效」，刷新后值还在；
   切一下「上架」开关，格子底色从灰变成橙色星芒。
3. **合成配方**：点空的第 4 个材料槽 → 选择器只列材料 → 选中；
   把某条配方的材料全删光再保存，**应当被拒绝且文件没被改**（`没有材料`）。
4. **材料掉落**：「关卡」是下拉框（`2 · 岩浆巨龙` 这种），选一个看看；
   再把某条的概率填 200 保存 → 被拒绝（`prob 不能大于 100`），
   而且那一行会被红框高亮、自动滚到眼前。

**顺手确认**：页面上**再也看不到 JSON 原文**；原来写在 `_说明` 里的话现在
出现在面板顶上；保存一次之后 `server/data/*.json` 里的 `_说明` 已经没了
（这是 D16 要的，不是 bug）。

**怎么告诉我**：有哪里不好用 / 不好看就说。**顺手把默认口令改掉。**

**当前状态**：四段都在等待中。

**⚠ 止损**：M3 ① 不生效 ⇒ 「往 `0x030b` 里加装备 id」这条路走不通；
M4 四发一发都没收到 ⇒ 商店界面根本没走 `ShopStage`。
两种情况都**停下来重新拍板**，不要硬往 M5 写。

---

## 当前卡点

只有上面那四条 ⏳。M5 / M7 都要等 M3 / M4 的实机结论才能动。

---

## 下一步：M9 的一部分（**不依赖任何实机结论**）

M9 里这几件现在就能做完，不用等：

- 云上迁移演练的**剩下那一半**：确认三份 json 已存在时不被覆盖。
- 打包实跑一次 `tools/build.bat`，确认 `shop_items.json` 和
  `web/admin.*` 都真的在产物里。

（`.gitignore` 的三条会话 01 已经补过了；`Copy-ShopData` 和 web 的验收清单
本会话已经加好。）

M5 / M6 剩下的部分、M7 都要等实机结论。

---

## 给接手会话的三句话

1. **先读 `FINDINGS.md` §1 / §3 / §4** —— 这三条是整个版本的地基。
   §11 / §12 / §14 是同一类坑（**原版 id 里的每一段都是约定不是规则**，
   能直接读到字段就别从 id 推），写任何按 id 分类的代码之前先看。
2. **协议全是 🔍静态结论，一发都没实测过。** 写代码时按它做，
   但**别在文档里把它升级成 ✅实测**，除非真的实机跑过。
3. **线上有真人在玩**（铁律 11）。M2 已经把「加字段」这一步做完并演练过了，
   后面碰 `accounts.json` 的每一步还是要先问「崩在中间老数据还在不在」。
