# V0.3 后半 PROGRESS —— 现在在哪

**只保留当前状态。** 做完的事从「正在做」挪走，不留历史；流水账进 `sessions/`。

最后更新：2026-09-05（会话 04：M3 / M4 第 1 步 / M6 / M8 **全部实机验完**；
难度下拉框改成中文名。下一步 = M5）

---

## 现在在哪

| 里程碑 | 状态 |
|---|---|
| M0 进度管理体系 | ✅ **完成** |
| M1 数据基线 | ✅ **完成** |
| M2 存档扩展 | ✅ **完成** |
| M3 ★ spike：装备生效 | ✅ **完成，实机验证过**（FINDINGS §16）|
| M4 ★ spike：商店 UI 协议 | 🟡 第 1 步（只监听）**实机采到数据了**（§19）；**第 2 步 = 开始回包** |
| M5 商店购买 + 仓库装备 | ⬜ **下一步**（M3 / M4 的止损线都没踩到，可以往下写）|
| M6 材料掉落 + 结算显示 | ✅ **完成，实机验证过**（结算界面画出来了、材料进存档）|
| M7 合成 | ⬜ 依赖 M4 第 2 步 |
| M8 管理员网页 | ✅ **完成，逐条走查过**（会话 04 代用户走完）|
| M9 测试 · 兼容 · 打包 · 上线 | ⬜ 还差云上迁移演练的后一半 + 打包实跑 |

**一句话现状**：**四条 spike 全绿**。装备真的在战斗里生效（内存 + 线上双证），
结算界面的材料栏画得出来，商店界面那四发请求的形状也实测到手了，
而且**一发不回客户端也不崩** —— M5 / M7 可以放心往下写。

---

## 已经能用的东西

见 git 和 `sessions/`。会话 04 新增的只有两件：

| 东西 | 是什么 |
|---|---|
| `tools/probe_equip_bonus.py` | 从跑着的客户端里读**战斗要用的那张装备加成表**。**大厅里就能读**（不用开局），换装后重跑一遍就能看数字跟着变 |
| `shopcfg.DIFFICULTY_ZH` | 难度号 → 中文名（`1 简单 / 2 普通 / 3 困难 / 4 极限`，§18）。管理页的难度下拉框不再显示裸数字 |

**回归**：2434 项绿 × 两个运行时（CPython 3.14.3 + Win7 3.8.10）。

**已实机验证的**（累计）：

- ★ **装备生效**：内存里桶 0 = `Attack=-1 / Defense=3 / Hp=29 / Sp=2`，
  ⇒ 血上限 129；线上 69 发 `rpFire` 的 ammo 全是 D2 的 `1000115`。§16
- ★ **「有时候 5 有时候 4」查清了**：裤子 `1020062` 的 `Attack=-1` 撞上那道
  15% 概率门，`int(5×0.99)=4`。**原版机制，不是 bug。** §16 ③
- ★ **进商店的四发**：顺序对、`0x0605` 是 11 字节 `u8+u16+i32+i32`、
  **一发不回也不崩**。点小标签会重发 `0x0605`。§19
- ★ **结算界面「合成材料」那一栏画出来了**，材料也真的进了存档（用户确认）。
- ★ **管理页**（会话 04 代用户登录走查）：登录 / 三个标签页 / 图标 / 商品筛选 /
  改价保存后刷新还在 / 上架开关变星芒 / 配方材料选择器只列材料 /
  「材料删光」和「概率填 200」都被拒且**文件一个字没动** / 红框高亮 +
  自动滚到眼前 / 未登录访问每个数据接口都 401（静态件照旧 200）/
  错口令连打第 2 次起 429。**改完存盘后热重载即刻生效，不用重启。**
- 真实 `accounts.json` 迁移演练（幂等）；控制通道全部新命令；`quest-clear.bat`。
- `tools/update-shopicons.bat` 端到端；`build.bat -Server` 产物自检。

★ `logs/server.err` 里的 `ConnectionResetError` 是**既有噪音**（浏览器关标签页
就会触发，注册页也一样），不是本版引入的。

---

## ⏳ 待用户验证

**空的。** 会话 04 之后没有卡在用户那边的东西。

★ **一件小事想请你自己拍板**：管理页里 `admin / Admin123` 这个默认口令还在用
（页面顶上那条红字提醒就是因为它）。**改口令这一步 agent 不代做** ——
在「管理员账号」页里改一下，或者干脆把 `admin` 删掉只留你自己那个 `dk`。

---

## 当前卡点

没有卡点。

---

## 下一步：M4 第 2 步 + M5

1. **M4 第 2 步 —— 开始回包**（先回最不可能出事的那两发）：
   `0x0704` → 回 `0x0604 gspRepEquippedList`；`0x0607` → 回空礼物清单。
   每加一发都实机看一眼界面，别一次全放。
2. **货架目录到底走哪个 opcode 仍然没落锤**（`0x0500` 的元素只有 3 个 int，
   装不下名字和价格；`0x0503`/`0x0504` 的处理器还没逆）。这是 M5 的前置。
3. ❓ `0x0605` 那两个非零字段对应界面上的哪个标签 —— 下次实机
   **一次只点一个标签、记下点的是哪个**，一轮就能对上（§19）。

---

## 给接手会话的三句话

1. **先读 `FINDINGS.md` §1 / §2 / §16** —— §16 是 §1 / §2 的实机落锤，
   整个版本的地基现在是实测的了。§11 / §12 / §14 / §18 是同一类坑
   （**原版 id / 号码里的每一段都是约定不是规则**）。
2. **下行协议仍然全是 🔍静态**：上行四发实测过了，`0x05xx` 的线格式、
   `0x0604` 的内容一发都没验过。写代码按它做，**别在文档里升级成 ✅**。
3. **线上有真人在玩**（铁律 11）。碰 `accounts.json` 的每一步都要先问
   「崩在中间老数据还在不在」。
