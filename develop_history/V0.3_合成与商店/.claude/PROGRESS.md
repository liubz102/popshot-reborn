# V0.3 后半 PROGRESS —— 现在在哪

**只保留当前状态。** 做完的事从「正在做」挪走，不留历史；流水账进 `sessions/`。

最后更新：2026-09-05（会话 04 第三轮：跨角色购买 / 直接装备 / 失败原因**实机通过**；
新修三条 —— `Equipment` 头上那 12 字节**不是死字段**（房间里「卸下」按钮靠它，
§31①）、`ShopStock+0x10` 是**划线原价**（「0 → 3000」的成因，§31②）、
`+0x18` / `ItemInfo+0x18` 是**物品说明**（§31③）。**服务端 16:35 重启过**，等实机）

---

## 现在在哪

| 里程碑 | 状态 |
|---|---|
| M0 进度管理体系 | ✅ **完成** |
| M1 数据基线 | ✅ **完成** |
| M2 存档扩展 | ✅ **完成** |
| M3 ★ spike：装备生效 | ✅ **完成，实机验证过**（§16）|
| M4 ★ spike：商店 UI 协议 | ✅ **完成，实机验证过** —— 货架真的画出来了（§21）|
| M5 商店购买 + 仓库装备 | 🟡 **代码全写完了**；四个实机 bug 的根因已修（§28 / §29），**⏳ 等实机复验** |
| M6 材料掉落 + 结算显示 | ✅ **完成，实机验证过** |
| M7 合成 | ⬜ **线格式全逆通了**（§27），代码没动 |
| M8 管理员网页 | ✅ **完成，逐条走查过** |
| M9 测试 · 兼容 · 打包 · 上线 | 🟡 打包 `$must` 已补上商店三件套；还差云上迁移演练的后一半 + 打包实跑 |

**一句话现状**：商店段**所有**上下行现在都有语义，没有 ❓ 剩下。服务端会回：
货架 `0x0500`、**物品定义 `0x0501`**、购买结果 `0x0502`、空礼物 `0x0508`、
**持有物清单 `0x0601`**、穿着的装备 `0x0604`，并处理穿 `0x0702` / 脱 `0x0703`。

## ✅ 已经实机落锤的

**货架整条链路**：图标 + 中文名 + 价格 +「金币」+「购买」按钮全画出来了，
翻页、角色切换、武器 1/2/3 子分类过滤都对（§21），6 个顶级标签的分类 id 全对（§22）。
**排序按钮**：进商店的默认状态是「基本顺序」（= 标志位 0）。

**物品定义 `0x0501` 那一整套（§28 / §29）**：左侧人物模型一进商店就带装备、
仓库列出装备 + 材料（材料在「收集品 → 材料」标签下）、仓库里手动穿脱生效。

**买东西（§30）**：跨角色买（泰尔买布洛克的枪）正常、买完选「直接装备」
真的穿上了、失败框写的是具体原因不再是「未定义的错误」。

**探针轮的负结果只对货架格子成立**：`ShopStock` 那三格里有两格提示框会读
（§31）—— **别再拿「某个面板不读」当「谁都不读」**。

## ⏳ 待用户验证

★ **服务端 16:35 重启过，客户端要重新登录。** 三条：

1. **待机房间**下面点仓库 → 装备一件 → **再点一次「卸下」该真的卸下**
   （以前弹「已卸下。」但实际还穿着 —— 根因是 `0x030b` 那 12 字节槽位掩码
   我们一直发 0，§31①）。
2. **商店提示框的价格** → 该只有一个数（「3000 金币」），不再是「0 → 3000」。
3. **商店 / 仓库提示框下半那块** → 该有内容了（武器写伤害/弹匣/换弹/初速，
   装备写生命/攻击/防御那几行）。
   ⚠ **这一条要特别看一眼换行**：说明是用 `\n` 分行的，如果界面上挤成一行、
   或者冒出方块字符，告诉我 —— 客户端还有个 `|` 分段的机制（分三个标签），
   换过去就是一行代码的事。

★ 已经线上验过字节：划线原价 = 售价、说明发出去了、槽位掩码 = `[4124, 0, 0]`。
**界面长什么样只有实机知道。**

**结果怎么给我**：一句「行 / 不行」+ 不行的话说清是哪一步、界面上写的什么。

## 当前卡点

没有卡点。

---

## 下一步

1. **实机复验 M5**（见上）。
2. **M7 合成**：线格式已经全逆通（§27），可以直接写。
   ⚠ `0x0606` 认的是**产物 itemId** ⇒ `recipe.json` 里 `result` 不许撞车。
   ★ 合成成功之后要**主动推 `0x0501` + `0x0601`**（和买东西同一条路，D20）。
3. 本版**不做**修理（上行 `0x0604`）、卖出（`0x0603`）、礼物（`0x0609`）、
   用道具（`0x060b`）—— 语义都查明了（§24）。
4. ❓ 还没逆的：`0x0503`（一个 bool + 提示框）、`0x0504`、服务端方向的
   `0x0700`（处理器 `0x552a73`）和 `0x0702`（`0x552dca`）。

## 给接手会话的四句话

0. ★★ **§31 是本版最容易再犯的坑**：「探针在这个面板上没出现」只证明
   **这个面板**不读，别升成「死字段」。§23 和 §26 各栽了一条
   （槽位掩码、划线原价 / 商品说明），三个都是实机才发现的。
1. **先读 `FINDINGS.md` §28 / §29 / §30 / §31** —— 商店段能不能用全看这几节：
   客户端有**两张**物品表，`ShopItem-Chn.ini` 只填图标和 PartFlag，
   **名字 / 等级 / 角色限定 / 可装备位全靠服务端的 `0x0501`**。
2. 再读 §20 ~ §25（协议）和 §16（装备生效的实机落锤）。§4 的老表已作废。
3. **两处顺序都是硬要求**：`0x0501` 定义排在 `0x0500` / `0x0604` / `0x0601`
   前面（D20）；买完那几发里 `0x0502` **排最后**（§30②）。
   穿脱那条最容易踩的坑在 §24：**每一发都要回一发 `0x0604`**，失败路径也要回。
4. 实机万一界面不对，用控制通道的 `shop-reply off <opcode>` 二分
   （现在管六发：`0500` / `0501` / `0502` / `0508` / `0601` / `0604`），
   **不用改代码也不用重启**。`sync-account` 现在会把商店三件套一起刷，
   人在商店界面里也能看到效果。
