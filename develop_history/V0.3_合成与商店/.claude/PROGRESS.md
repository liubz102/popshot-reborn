# V0.3 后半 PROGRESS —— 现在在哪

**只保留当前状态。** 做完的事从「正在做」挪走，不留历史；流水账进 `sessions/`。

最后更新：2026-09-17（会话 60）：**管理页「数据管理」→「⇩ 下载日志」做完了**（用户点的题）。
云上的 `logs/` 和玩家传上来的崩溃包不用再远程登录去取：弹窗里看大小、点一下就下 zip。
口径和取舍在 **D131**，硬事实在 **§128**，流水在 `sessions/2026-09-17-02.md`。

* 新模块 `server/logpack.py`（数据层：清单 / 计划 / 流式写 zip）+ `web/admin.py` 两个 GET
  （`/admin/api/logs`、`/admin/api/logs/download`），系统管理员档。
* ★ **流式直发**：zip 直接写进 HTTP/1.1 chunked 响应体，边打包边传，不落临时文件、不轮询、
  不拿数据锁；出错就不发结束块直接断，浏览器报「失败」而不是拿到一个坏 zip。
* `logs/` = 第一层文件（含点文件，不递归）；「最近 12 小时」按 mtime；崩溃包按第一层目录。
* 两套运行时 **3779 项**：3.14 全绿；3.8 两条红在 `test_botsync` 的寻路预热 worker（时序抖动，
  单独复跑全绿，与本次无关）。curl 实测 chunked / zip / HTTP/1.0 / 中途断开都对，stderr 干净。
* **只动服务端 + 管理页静态件** ⇒ 不用重打包；⏳ **真服务端还没重启**（用户正在房间里），
  重启后才有这两个接口 —— 见 **Z-88**。打包脚本必带清单已加 `logpack.py`。
* ⚠ 会话中途 `server/testdata/pkn/Effects0011.*` 被删过（不知道是谁），已从 HEAD 原样恢复。
* 追加修了一个**客户端侧**的老 bug（§129）：崩溃包 `meta.json` 里 `fault` 等字段的中文路径
  乱码（latin-1 读进来的 ANSI 字节被 `json.dumps` 按 UTF-8 写出去）。`crashwatch.ansi_to_text()`。
  ★ 它在客户端包里 ⇒ **要重打客户端包**玩家才能拿到；服务端不用动。

上一轮（2026-09-17，会话 59）：**任务记录框修好了**（用户点的题）。
任务模式待机房间右侧「全体记录 / 个人记录」两个框永远显示「正在查询资料」——
根因是喂它们的包是 **`0x0310` 服务端方向**（`gspRepQuestRecord`），而客户端方向的
`0x0310` 是中继请求，**同号反向**；它的分发不在大厅跳表里而在 `RoomStage` 的虚函数
`0x467a42`，所以历版都没找到（V0.2 照着跳表就结案了）。全部证据在 **§126 / §127**。

* 新模块 `server/questrecord.py`（照 `gifthistory.py`）：`{(关卡,难度): {账号: 最好用时}}`，
  按图、按难度分桶 —— **用户点名要区分的就是这两样**。
* **只有房主会发 `0x0311`**，客人从不问 ⇒ 服务端主动广播给全房间；
  广播**必须排在 `0x0303` 之后**（回显校验比的是 `0x0303` 灌进去的那两格）。
* ★★ **`0` 是禁用值**（客户端把它当「正在查询资料」），「没记录」的哨兵是 **`99999`**。
* 破纪录播报**不用新包**：藏在 `0x0411` 那几个「❓未知，一律填 0」的业务值里
  （索引 9 = 类型、索引 11 = 用时毫秒）。★ 结算表槽号 ≠ 业务值索引，见 §127。
* 计时停在 `0x0417` 而不是结算（差整整 30 秒的金币雨）；**毫秒是权威值、秒由它
  `//` 出来**，播报和榜同源。
* 两套运行时 **3743 项全绿**（+66）。七道拦网逐条**故意打断**验过。
  **只动服务端** ⇒ 不用重打包。服务端**已重启**（11:33）。
* 用户拍板三条在 **D129**：无记录照原版回空列表 / **不做 bot 防刷，只要通关就记** /
  播报一起做。⏳ 实机那一遍在 **Z-87**。
* 追加（**D130**）：回滚对话框上给**运行时记录**（发奖记录 + 任务通关记录）
  单开一格、默认不勾，并补上了回滚前的顶层校验（以前这两份**完全不校验**）。
  前端一个字没改。★ 这推翻了 D129 最后那句「不进回滚那一组」。

上一轮（2026-09-16，会话 58）：**打碎箱子不算击杀**（bug调查/22）。
玩家「随雨」报「零杀人赢了没给蹭分卡」—— 查下来胜负没算错，是**可破坏物
被当成了怪**：打箱子和打怪走同一发 `0x0408`（受害者座位 `0xFF`），
于是「击杀数 == 0」那条判据永远不成立。线上那晚他 **7 胜里 6 胜零杀人，
只发出 1 张卡**。全部证据和账在 **§125**。

* 分箱子和怪的判据是 `.map` 抽出来的**破坏物世界句柄表**
  （`mapdata.MapTerrain.breakable_by_handle()`，§139），不是按句柄大小猜的。
  ★ **对战里没有怪**（用户拍板）⇒ 非座位受害者一律是箱子，连查表都不用；
  闯关照旧「打怪算击杀」，拿不到地形数据时按怪算（保主路）。
* 落点：`Conn.victim_is_scenery()` → `record_kill(scenery=…)`，箱子一个计数器
  都不动。判据算在**连接**那一侧 —— 模式和地图在它手里，`RoomQuest` 手里没有。
* 连带修掉：武器卡片（pvp · 某枪累计击杀 50）以前也被箱子刷进度。
* 两套运行时 **3677 项全绿**（+8 条新回归）。**只动服务端**
  （`gameserver.py` / `cards.py` 的注释 / `test_battle.py`）⇒ 不用重打包，
  `stop.bat` → `start.bat` 就生效。⏳ **还没重启**，要生效得重启一次。
* 两个方向都**故意打断验过**：把判据按回旧样子 ⇒ 3 条「箱子不算」红；
  连怪一起砍 ⇒ 2 条「怪照算」红。
* ⚠ 顺手发现**没修**的口子：`mapdata` 的图名大小写和客户端报上来的对不上
  （`Quest06_stage` / `Quest07_Intro` 查不到），bot 在这两张图上也没有地形。
* ⚠ 老账不回写（铁律 11）：已经落盘的对战累计击杀数和武器卡基准仍是虚高的。

上一轮（2026-09-15，会话 57）：**成就防刷 —— bot 受限的那一局不计成就**
（用户点的题：V0.3.3 发出去之后有人建房加 bot、`/hold` 定住它刷成就）。口径在 **D127**。

两套运行时 **3669 项全绿**（会话 56 是 3611，净增 58 = 新模块
`server/test_cardfair.py`）。**只动服务端**（`bot.py` / `gameserver.py` / 新测试模块），
客户端和资源包一个字节没改 ⇒ 不用重打包，`stop.bat` → `start.bat` 就生效。
⚠ **最后一版改完没重启**（用户当时正开着客户端），要生效得他自己重启一次。

★★ **用户当天实机跑了 4 局，报「解除限制后还是判难度 1」—— 查下来服务端没算错**：
日志里第四下敲成了 **`/b 2`**（想敲 `/d 2`），不认识的命令被当普通聊天广播掉、
不报错，难度从头到尾是 1。⇒ 真正的毛病是**没有回执**，他分不清「解除成功」
和「命令压根没生效」。**已补上「限制已解除」那一句**（D127 ⑥），
并把他那一串按键**逐键复现**成回归用例（`UserReportedSequenceTests`，
敲对 / 敲错两个方向都钉住）。

* **判据是「这一局里出现过限制没有」**，不是「结算那一刻还在不在」（那样的话
  「定住打五十下、结算前解开」照样拿卡），也不是「房间永久拉黑」（用户选的中间那档）。
  闩在新字段 `RoomQuest.bot_limit_reason`，跟着本局生灭。
* 判据读**实况**不读命令：`bot.BOT_FREEDOM_FIELDS` 一张表（`holding` / `weapon_slot` /
  `melee` / `no_explode` / `slow_bullet`）+ 难度 < 2。★ **房里没有 bot 时全部不适用**
  （用户：「下一局 bot 全踢掉也恢复正常」）。
* **三个写入点互为兜底**：建 `RoomQuest` 时扫（管「上一局留下的限制、这一局没人敲命令」）、
  `handle_command()` **前后各扫一次**（管局中开关；★「之前」那一扫专治
  「先定住、打完再解开」）、结算时再扫一次兜底。
* 「不计」= **战绩不并进 `battle_stats` + 不发卡**，经验 / 金币 / 材料**照发**。
  落点是 `apply_battle()` 少传两个参数，`account_store.py` 一个字没改。
* 日志一局一行、**普通日志**、房间级打一次，计入 / 不计入**都写原因**（用户点名）。
* 玩家提示**四种**，都在房间聊天框（按**状态翻转**去重，不存标志位）：
  ① 受限时一行；② **解除时一行** —— 局中解除带「（本局不生效）」、房间里解除不带
  （判据是**本局上没上闩**，不是房间状态）；③ 脏局**每次开局**一行；
  ④ **结算界面**一行「本局开启过 bot 限制，不结算成就」（只在不计时发，
  排在结算三连发之后）。
* ★★ 审查读代码读出来的两个坑（D127 ⑨）：「新一局开张」的发起人**可能已经不在房里**
  （`after_someone_left` 那条路）⇒ 新增模块级 `gameserver.room_system_chat(room, …)`；
  结算路径上**不许 lazy `import bot`**（会中途把 bot 钩子装进 gameserver、污染测试）
  ⇒ 走现成的钩子模式 `BOT_LIMIT_REASON`。
* 七道拦网**逐条故意打断验过真的拦得住**（§109 的规矩）。
* ⏳ 实机那一遍在下面 **Z-86**。
* ⚠ **另一个同源的口子留给用户拍板**（D127 ⑩）：`0x040f → 0x0405 → 0x040f` 能
  **无限重复结算**，一个 bot 都不用。和 bot 刷不是一回事，没动。

上一轮（2026-09-15，会话 56）：**翅膀 = 滑翔，写进说明文**（用户实机发现「穿翅膀套装
感觉和平时不一样」）。结论在 **§122**。

* 机制：`PartFlag & 512` → **连按三下跳跃键**张开翅膀滑翔，重力 ×0.35、下落速度封顶 5.5，
  张开扣 10 点体力、维持每 tick 扣 1 点。五个常量全在 `Data/GameProps.ini`。
  ★ **「三下」是用户试出来的**，静态只读得到「跳跃段数==2 时张开」。
* **唯一性证了**：带部位标志的 `Equipment::HasPart`（`0x558501`）全程序只有一个调用点，
  push 的就是 `0x200` ⇒ 除翅膀外没有第二个「装上就改玩法」的槽位。原版任何界面都不写它。
* ★ `HasPart` 是**按位与**：**羽翼套装 `PartFlag=539`**（一件占五个槽）同样能滑翔。
  我第一版写成 `== 512`，**恰好把套装漏掉**了 —— 就是用户在穿的那件。已改成按位，
  测试里专门要求清单中至少有一件多槽位的。
* 落点 `shopcfg.PART_EFFECT_ZH` + `_effect_lines`，**12 件**（3 套装 + 9 翅膀）的说明文
  从「外观装备。无属性加成。」变成滑翔说明。两套运行时 **3611 项全绿**。
  **只动服务端**，不用重打包；服务端已重启。

上一轮（2026-09-15，会话 55）：**漏译第三轮 —— 这次是全量查法，不再靠想**
（用户点的题：房间提示 / 加载画面还有韩文，「再全体都查一遍，不要再让我一个一个指摘」）。
查法钉在 **§121**，口径在 **D126**。

★ 前两轮之所以反复漏，是**查法不全**，不是某处没注意：
第一轮只查了资源文件里我想到的那几个键，第二轮补了 `MapDisplayName`，
**两轮都没查 exe 自己的字符串表** —— 而带 `%s` / `%d` 的运行时消息全在那儿。

本轮把查法定成「三个来源」：资源文件全键 + `.ui` 的 `<Text>` + **exe 数据节的宽字符串**。
扫 exe 的三道坑（只扫数据节 / 地址必须被代码引用过 / **真换行要转义成字面量 `\n`**）
见 §121，少一道结果就没法用。

* 补 **81 条 exe 运行时消息**（组队匹配、房间「XX 选择了 YY 角色」、修理、战绩初始化、
  好友、登录、爆炸倒计时、GameGuard…）+ 清掉 `Quest05Boss.uni` 里漏进 `TextChn` 的列标题。
* ★ **每一条都有交代**：155 条漏译清单 = 翻 81 + 写明不翻 28 + 3 个整段跳过区间
  （韩文字母表 / 二进制垃圾）。打补丁的脚本强制这一条，漏一条直接报错。
* **不改 exe**（D126），只补 `Chinese.ini`。重打 2 卷，`verify-tree` 16266 文件差异 0，
  update-gamedata 五步 + 240 条测试全过。
* ⏳ 实机看的清单在下面 **Z-84**（已扩充到这一轮）。

上一轮（2026-09-15，会话 54）：**查称号 + 修好红心回血**
（用户点的题：「穿上称号属性面板不动」「各个称号到底能不能在局内生效」
「红心达人和青鸟的回血查到底并修复」）。结论在 **§118 / §119 / §120**。

两套运行时 **3608 项全绿**（新增 11 条）。**只动 `server/gameserver.py` 一个文件。**

* **面板不动不是 bug**（§118）：面板只画 Attack / Defense / MoveSpd / Hp / Sp 五格的
  **静态 INI 值**，称号**算在里面**（用户截图里「攻击 +8%」= 上衣 3 + 称号 5，
  去掉称号就是 +3%）。20 个称号里只有 `560001` / `560010` / `560011` / `560012`
  有静态值，**其余 16 个的效果是 Lua 或 exe 写死的，面板结构上就画不出来**。
* ★★ **修好了 `560006 [红心达人]` + 宠物 `heartboost`（青鸟 `220001`）的回血**（§119）：
  上行 `0x040b` 的 16 字节**就是**下行 `0x040a` 的 16 字节 ⇒
  `Conn.on_heart_effect` 原样换 opcode 广播（**含发包人自己**）。
  三道闸：物件 ∈ {10315, 10316}、目标座位在房里、量 ≤ `heart_effect_amount_max()`
  （★ 上界**从物品表算**，不是拍的常量）；发起者那一格以连接为准。
  ⏳ **实机还没打过**，见「待用户验证」Z-85。
* ⚠ 顺带订正两处旧结论（都写进 §119 了）：上行 `0x040b` 的**线上字段顺序**
  和 §53 ⑤ 记的不一样（是 `目标座位/发起者/物件/量`）；
  `ITEM_EFFECT_ARG2/ARG3` 那句「两个参数没人读」**只对 PvP 道具成立**。
* `560002 [格斗达人]` 确认**永远 +0**（要 `PvpMode==2`，中国区选不到）；
  `560005 [手下留情]` **实机跑通过**（日志里 11 条上行 `0x0415`）。
* §120 订正会话 42 ②：**派克(109)** 的 2/3 号枪有 ROH（`130002`/`130003`）⇒
  用派克能吃到 `[榴弹发射器高手]` `[火箭炮高手]`、也拿得到那两张武器卡。

上一轮（2026-09-14，会话 53）：**挂机状态在「躺着 / 结算界面 / 读图界面」
三处会闪回「游戏中」** —— 用户实机报的，三处同一个根因（**D125**）。

两套运行时 **3597 项全绿**。**只动服务端 + 管理页两处文案。**

* `conn_is_afk()` 里那条「躺着一律不判」原来是 `return False`（= 显示游戏中）,
  改成**返回倒下那一刻定格的结论**（新字段 `Conn.afk_when_down`）。
  `note_seat_died()` **先定格再上闩**，复活 / 新一局一起解冻。
* ★ 为什么一处能连累三处：挂机的人**每局把三条命用光**，最后那次死透之后
  `dead_since` 一直挂着 —— 结算界面 15 秒、回房间、下一局读图 7.5 秒全走那条
  `return False`。一晚上真正显示「挂机中」的只有零星几秒。
* ★★ 补了一条**端到端**用例 `test_battle.AfkAcrossTheWholeRoundTests`：
  进图 → 挂机 → 被打死 → 结算界面 → 回房间 → F5 读图 → 再进图 → 按一下方向键。
  **那三处本来各有各的用例、全是绿的**，漏的是「串成一条链」。
  以后再动挂机判定先看它。

上一轮（2026-09-14，会话 52）：**build-pack 乱码修掉 + 一批锁在包里改不动的翻译还上了**
（用户点的题，**§116 / §117 / D124**，§99 结掉）。

* **乱码**（`[pack]` / `[stamp]` 那几行）：`| Out-Host` 一捕获，CPython 就从
  `WriteConsoleW` 掉到 `GetACP()`=cp936，而 `chcp 65001` 的宿主按 utf-8 解 —— §116。
  两端各钉一次：`pkn.py` 的 `reconfigure(encoding="utf-8")` + `build-common.ps1` 顶部的
  `[Console]::OutputEncoding`。守卫 `test_packdirs.PackToolEncodingTests`。
* **翻译**：§117 查清了中文版的机制（`Chinese.ini` 整串查表，精确匹配不 trim，
  查不到就显示韩文）⇒ 补 key 就等于补翻译，源文件不用动（D124）。这次落地的：
  - `ShopItem-Chn.ini` **20 条称号 `Tag=`**（§99 欠了一版的那件事，头顶那行字）；
  - `Chinese.ini` **改错 7 处 + 补 58 条 key**：商城 / 选角的 6 个角色名、4 个地图主题名
    + 5 条任务剧情简介、晋级任务列表 9 个标签、战斗情境提示 15 条、5 个 buff 名、
    装备加成面板的 `Team/Self %d%% %s` / `Down` / `Spd%+d%%` 错字，等等；
  - `TipMsg-Chn.ini` 3 处、`Promotion-chn.ini` 3 处、`Quest05Tracing.uni` 1 处。
* 实跑过的：`verify-tree` **16266 个文件差异 0**；`git status` 只动白名单里那些；
  `shop_items.json` 只变了 3 个 promotion 名字（改 `Tag` 不影响它，实跑核过）；
  两套运行时全量测试绿。**⏳ 实机那一遍在「待用户验证」里。**

上一轮（2026-09-14，会话 51）：**客户端资源包工作流 —— 能改资源了**
（用户点的题，**§114 / §115 / D123**）。

```text
game_patched\Pack_develop（明文，16266 文件，进 git 不进包）
   → tools\build-pack.bat（tools/pkn.py 增量打包 + 明文树变了就自动跑 update-gamedata）
   → game_patched\Pack_publish（151 卷原版格式加密卷 + pack-index.json，进 git 进包）
   → bshook 在 kernel32!CreateFileW / FindFirstFileW / FindFirstFileExW 上把 Pack\*.pkn 改到 Pack_publish\
```

* **格式全部实测坐实**（§114）：读取器解开原版 82 卷 == Pack_decrypt 逐字节 16266/16266；自己打的
  151 卷回读 == Pack_develop；两次打包字节相同；`--force` 重打哈希不变。金样 `server/testdata/pkn/`。
* **hook 实机冒烟通过**（§115）：钩子装在 DllMain（早于解壳 1 秒），FindFirst 改写 1 次 + CreateFileW
  改写 151 次，登录界面起来、60 秒无崩溃；升级演练里 `launch.ps1` 自动删掉了旧 `Pack`（82 文件 / 324 MB）
  后游戏照常。`hook/bin` 已重编（`bshook.dll` 215552 字节），**版本已抬到 0.3.3**，`manifest-hook.json` 多了
  V0.3.3 一条（V0.3.2 那条没动）。
* **常量单源**：`server/config.py` 三个目录名 → `tools/gen_pack_h.py` → `hook/pack.h`；`--pack-dirs` 给 ps1；
  `test_packdirs.py` 钉三边（连 `.ps1` 里的字面量都拦）。
* **脚本链**：`build-pack.bat/.ps1`（增量 + 戳判据 + 自动 update-gamedata，实跑 83 秒，五份产物**零漂移**，
  二次运行两步都跳过）；`build.bat` 打包前自动 `Invoke-PackBuild`，`build-portable` 排除 Pack_develop / Pack
  并用 `Assert-PackagePackLayout` 逐卷核 sha256；`launch.ps1` 自检 + 更新善后删旧 Pack；`update-gamedata`
  和五个提取器改读 Pack_develop；`.gitattributes` 新建、`.gitignore` 补规则。
* `game_patched\Pack` **已从磁盘删掉**（演练时由 launch.ps1 删的；82 卷在 git 历史和 original_resource 分支里）。

★★ **用户要做的 git 操作**（agent 不碰仓库状态）：

```bash
git rm -r --cached game_patched/Pack
git add .gitattributes .gitignore game_patched/Pack_develop game_patched/Pack_publish   # 约 16400 文件 / 865 MB，要几分钟
git add tools/pkn.py tools/gen_pack_h.py tools/build-pack.bat tools/build-pack.ps1 tools/gamedata-stamp.json hook/pack.h server/test_pkn.py server/test_packdirs.py server/testdata
```

⏳ **待用户实机验证**（登记在下面「⏳ 待用户验证」）：登录 → 大厅 → 商店界面 → 建房进图打一局；
以及改一句 `Pack_develop\Data\Chinese.ini` 的文案 → `tools\build-pack.bat` → `start.bat` 看到新文案。

⏳ **发版顺序**（下一版 V0.3.3）：`tools\build.bat`（自动先 build-pack）→ 提交 `BUILD.ver` +
`server/manifest-hook.json` + `tools/update-manifest.json` → GitHub Release → 最后抬
`config/server-ClientFilter.config`。老玩家整包更新后第一次 `start.bat` 自动删残留 `Pack`。

上一轮（2026-09-14，会话 50）：**`server.out` 按天切分 + 日志行补上完整日期**
（用户点的题，**§113 / D122**）。起因：云主机取回的 `logs/server.out`
**一个文件 940 MB**，而 `log_retention_days = 3` 一直开着 —— 因为清理按 mtime 判老，
**正在写的文件永远是「刚才」**。

* 新模块 `server/daylog.py`：`app.py` / `relay.py` 入口处 `daylog.install()`，
  把 `sys.stdout` / `sys.stderr` 换成自己开文件、跨零点切名的流。
  今天那份仍叫 `server.out`（文档和工具全不用改），昨天的变成 `server-20260913.out`
  —— 没人再写它 ⇒ mtime 冻住 ⇒ 到期被 `logcleanup` 删掉。`relay.out/err` 同治。
* 启动脚本三处（`launch.ps1` / `serverctl.ps1` / `serverctl.sh`）重定向改成
  `*-boot.out/.err`（只兜 `install()` 之前那一小段），并删掉 `Move-LogAside` /
  `rotate_log` —— 自己开文件用 `"a"`，本来就不会覆盖了。
* 四份 `ts()` 一律带完整日期 `2026-09-14 15:14:27.305`；另把 `[web]` / `[bot]`
  那几处**一个时间戳都没有**的行补齐。
* `eventlog._rotate_unlocked` 改成调 `daylog.rotate_to_dated`，两处切名同一套。
* ★ 新开关 `--no-day-log`，**打包自检必须传**（`daylog` 按包根算路径，
  不关就往发出去的包里塞一份启动日志）；`Assert-PackageDataClean` 跟着多验
  一条「包里的 `logs\` 必须是空的」。

* ★ **`logs/updater.log` 一起修了**（用户第二轮点名）：`updater/src/log.c` 加
  `rotate_if_stale()`，跨天切成 `updater-YYYYMMDD.log`，命名和 `daylog` 一致；
  `selftest` 加 5 条闸门（**92 checks, 0 failed**），`BsPatcherChn.exe` **已重编**
  （在工作区，用户说放下一个版本一起发）。
* ★ **`bshook_*.log` 不动**（用户拍板）。顺带查清：它**没有**文件行数上限
  （`BSLOG_RING_BYTES` 是 4 MB 内存环，不是文件上限；磁盘上有 29 MB 的单份），
  但它每次运行一份、退出就不再写，照常被清 —— 本来就不是同一个毛病。

★ **3593 项全绿**（`test_logs.py` 新增 17 条），三条路都真跑过：

| 验的东西 | 怎么验的 | 结果 |
|---|---|---|
| Windows 启动脚本 | `shutdown.ps1` → 把 `server.out` 的 mtime 拨到昨天 → `launch.ps1 -NoGame` | 切出 `server-20260913.out`（**mtime 保住在昨天**）+ 新 `server.out` 带启动分隔线 |
| **一直不重启，过零点切不切** | `LiveMidnightRollTests`：真 `asynclog` 写线程 + 真 `sys.stdout` 替换，只把钟从 23:59:50 拨到 00:00:10 | **会切**，两边一行都不串；空转 3 天再写也对、且不留空文件 |
| `serverctl.sh`（Linux 侧） | 搭「服务端包」形状的沙箱 + 指向真 Python 的 `python3` 垫片，Git Bash 里跑 `start` / `stop` / 两条失败路径 | 全通。失败时三份尾巴各司其职；沙箱没 pyc ⇒ `server-boot.err` 真接住了 2.3 KB SyntaxWarning |
| `updater.log` 切分 | 沙箱里无头（`POPSHOT_UPDATER_NOUI=1`）跑三次，中间把 mtime 拨到昨天 | 切出 `updater-20260913.log`（mtime 保住）；同一天第三次只追加 |

⏳ **仍欠一遍真 Linux**：这台机器没有 WSL 发行版也没 docker。
云主机目前是 Windows（日志路径是 `C:\Users\Administrator\…`），走的是已验的
`serverctl.ps1` 那条。

上一轮（会话 49）：**全量测试提速**（用户点的题，**§112 / D120 / D121**）。
`server/run_tests.py` 加 `-j`、**默认并行**（按 TestCase 类分片）：
3.14 **153.6s → 27~34s**、Win7 3.8 **278.7s → 60~66s**。
半路挖出并修掉**两个生产 bug**（都不是并行引入的，是被并行顶到台面上的）：
① Windows 上 `os.replace` 会随机 `PermissionError`（16 进程压测 0.14%），
撞上就是**存玩家数据那一次写盘丢了** ⇒ 10 处全部改走新的 `server/atomicfile.py`；
② `crashstore` 的清理线程把**正在收**的崩溃包当残骸删掉（线上每天
`DAILY_HOUR` 那一次就会犯）⇒ `Store` 记在途集合，`cleanup()` 跳过在途的。

★ 两套运行时并行各跑多轮 **3572 项全绿**（串行是 3577 —— 差的 5 条是
`test_botbreak` 从 `test_mapdata` import 过来、串行至今每轮跑两遍的那一组）。
会话 48 欠的「Win7 3.8 那一遍」**已经补上**。
★ 验收全量**别再用 `... | tail -N`**：管道要等进程整个跑完才吐第一个字节，
中途零输出 —— 会话 48/49 之前的「卡死 20 分钟」就是这么误判出来的（§112 ①）。

再上一轮（会话 48）：**玩家状态多一档「挂机中」**（用户点的题，
**D118 / D119 / D125 / §110 / §111**）。起因：云上有人开鼠标连点器挂任务模式，用户不禁止挂机，
但**要重启服务端时分不出「真有人在玩」还是「一屋子挂机的」**。

3.14 运行时 **3564 项全绿**（会话 47 是 3521，净增 43）；当时欠的那一遍
Win7 3.8 已由会话 49 补上（见本文件开头）。**只动服务端 + 管理页前端，
客户端一个字节没改。**

* 位置码多两个：`afk_quest` / `afk_battle` —— 中文「挂机中·任务」「挂机中·对战」。
  它是**「游戏中」的细分**，所以管理页三处显示（列表状态列 / 修改仓库弹窗 /
  发奖弹窗）一处都没改。
* 筛选多一档「挂机中」（两个下拉都加了，`test_web_admin` 那条拦网要求一致）。
  账：**挂机中 + 游戏中 = 改之前的游戏中**；待机中不含、在线含、不在线不含。
* ★★ **两条钟，看得见键盘就只看键盘**（D119）：
  - **多人局**看键盘（`AFK_AFTER_S` = 20 秒）：心跳里的方向键掩码 +
    跳/冲刺/蹲/换枪/防御/图腾那几发事件包。鼠标、开火、F5、被打飞的位移
    **一概不算**（§110）。
  - ★★ **单人局**看「打中 / 捡到 / 得分 / 按 Ctrl 用道具」
    （`AFK_SOLO_AFTER_S` = **45 秒**）—— **单人任务房里服务端一发同步包
    都收不到**（§111：4693 个单人局里 4692 局同步包为 0），键盘那条整个
    瞎掉，而挂机的人恰恰全打单人局。45 是真日志上量出来的最小安全值。
* ★★ **跨局继承**（用户第五轮）：结算时记一笔「他这会儿在不在挂机」，
  下一局一上来就标挂机，**看见任何真证据当场撤掉**。只有开始挂机的
  第一局是未知状态。★ 记的是 `conn_afk_clock_expired()` 不是 `conn_is_afk()`
  —— 结算那一刻他一定躺着，用后者的话躺着那条豁免会把结论抹掉。
* ★★ **躺着的人不判**：死了等复活 / 命用完观战的人按不了键。
  `dead_in_match` 由死亡广播上闩、重生广播撤闩（三处：`0x0406` 之后、
  本人 `0x0413` 之后、**看门狗补包之后**）。★★ 躺着那一段**不判也不计时**：
  复活时把表往前拨过那一段，死前欠的秒数**接着数**（用户第四轮：
  挂机的人天然死得勤，每死一次清表的话一轮永远数不满）。
  「躺着」那一格只在新一局清，换图不清。
* 阈值 **20 秒**（`AFK_AFTER_S`，一天里 15 → 30 → 20；回调是因为
  30 秒太长的话「刚判成挂机、这一局就结束了」，状态一闪而过）。
  它是铁律 10 的**那个例外**（没有事件可等，「没有事件」本身就是要等的东西）；
  「他回来了」仍然是事件驱动的。没做成 `server.config` 的键。

★★ **拿云上两天的真日志回放验收过**（`bug调查/挂机识别测试/`，175 份
逐连接抓包，判据跑的是本体）：

| | 判挂机占局内时间 | 一上来就标挂机的局 |
|---|---|---|
| 真玩 8 个号 | **0% ~ 3%** | 0~1 局 |
| 挂机 6 个号 | **51% ~ 78%** | 60/68 ~ 847/950 |

★ 顺带量出挂机的人长什么样（§111）：每局 **77~108 秒**、房间停留 **26~28 秒**
（= 结算界面 15.1 + 按 F5 前 3.2 + 读图 7.5），通关率 0~8%，每局把三条命
全用光。「回房间→按 F5」那 3.2 秒零方差是连点器的指纹（真玩的 p90 是 73~365 秒）。

⏳ **没做 / 欠着**：
* **Win7 3.8 运行时那一遍没跑**（用户中途打断）—— 下次开工第一件事。
* ✅ 实机验过一轮（会话 53）：判得出来，但「躺着 / 结算界面 / 读图界面」
  三处会闪回「游戏中」—— 已修（D125）。**修完那一版还没实机再看**。
* 管理页**不会自动刷新** —— 状态会随时间变，得点「↻ 刷新」才看得到新的。
  （这是原有行为，没动。要自动刷新是另一件事。）
* ★ **只有开始挂机的第一局**有「还看不出来」的窗口（跨局继承之后）。

上一轮（2026-09-14，会话 47）：**结算时把卡片判定的全过程写进调试日志**
（用户点的题，**D117**）。起因是实机那次「火焰弹击杀 2 次、累计只涨 1」——
查清楚花了大半个小时去解原始 UDP 包排时间线，而答案（那一杀被 bot 自己的
火墙收尾、客户端报的凶手是它自己）本来就该在结算日志里。

两套运行时 **3521 项全绿**（会话 46 是 3507，净增 14）。**只动服务端。**

* 每局结算多两样：`累计战绩 座位N:`（按模式分段）+ `── 称号卡片判定 座位N:`
  —— 每张卡片一行「发 / 不发 / 这一局不算（哪一格没对上）」，下面每条条件
  各一行，带**实测值**；累计那一档写「这一轮 X/Y（累计 A − 基准 B）」三个数。
* ★ **只在 `--verbose`（`start-debug.bat`）下打**，而且只给真人打（bot 没账号）。
* ★★ 日志和决策**同一次求值**（`cards.explain()` 复用 `_applies` /
  `eval_conditions`，靠 `seen=[]` 通道带出来）；`_applies()` 本身改成
  `not _why_not_applies(...)`，「算不算」和「为什么不算」再也 drift 不了。
  有用例钉着两边结论恒等。

上一轮（2026-09-14，会话 46）：**把运营调好的卡片条件 / 称号配方回填成出厂值**
（用户点的题，**D116**）。用户在管理页上手改过 `cards.json` 的 17 条获得条件和
`recipe.json` 的 17 条称号配方 ⇒ `shopdefaults.CARD_RULES` / `TITLE` 照盘上
那份**逐条改过来**，我原来那版设计值作废。

两套运行时 **3507 项全绿**（和会话 45 同数）。**只动 `server/shopdefaults.py`
（出厂表）和 `server/test_cards.py`（三条钉着旧值的用例）；盘上的
`server/data/*.json` 一个字节没碰**，客户端和管理页也没动。

① 判据是**模板算出来的 == 盘上那份**，连键序和条件先后都相等
   （`default_cards()["rules"]` / `default_recipes()` 里那 17 条称号，D116②）。
   顺带对了其余 122 条配方：一条没动 ⇒ 用户改的确实只有这两块。
② 形状：一半卡片挪到**累计达标**、或补一条「或者累计够了也给」的兜底；
   武器卡三档**拉平**成累计击杀 50；`60001` / `60006` / `60007` / `60008`
   放开成不限模式；`60003` 改成「命中率 ≥60% 且开枪 ≥10」；
   称号那边金币四档没动，动的全是**张数**。
③ ⚠ **代价记一笔**：`60004` 的条件念出来 56 个字，提示框第 1 段那 6 行里
   要占 3 行（原来 2 行）⇒ `test_cards` 那道软上限 40 → 64。
   **再长就要挤掉「可合成：」那几行 —— 下次该改的是规则，不是那道闸。**

上一轮（2026-09-14，会话 45）：**管理页「改过的那一行亮出来」**
（用户点的题，**D115**）。用户：「修改某一项之后会显示『有未保存的修改』，
但画面上没有标记我到底改了哪里，交互不舒服」⇒ 改过的那**一整行 / 一整张卡**
换一圈**绯红**边框 + 一圈**发光**。

两套运行时 **3507 项全绿**（会话 44 是 3498，净增 8 = 新加的
`test_admindirty.py`）。**只动管理页的 `admin.js` / `admin.css`，
服务端 Python 和客户端一个字节没改。**

① **颜色 = 绯红 + 一圈光**（用户当场推翻我的第一版紫棠）。
   **只换描边 + 那圈光，底色一个字不动** —— 底色那一格是「上架 / 关着 /
   没有」在用的；和 `.bad`（服务端拒收的那一条）靠**粉底**分。
   ★★ 第一版我还**顺手把工具条上那句「有未保存的修改」也改了色**，用户当场
   否掉：「我没让你改那句话」。⇒ **没被要求的东西不要顺手改**，要改先问。
② **判据在 `WeakMap` 里、键是记录对象本身**（不是下标 —— 删一条会让后面
   全体挪位，按下标存就「删一条亮一片」）；比的是 `stableJson`（键序无关）。
   基线在 `adoptConfig` 里和 `snapshot()` **同一时刻**记（`markBase`）。
③ **刷 `.edited` 是画完统一扫一遍**（`paintEdited`），判据是「身上有没有
   `data-index`」—— 那属性本来就是 `markBadCard` 在用，6 个渲染器都挂了，
   以后新加的画面照挂就自动被收进来（同 `lockList()` 那条理由）。
④ **删掉的那几条没有行可标** ⇒ 写在**面板标题**：「51 条规则（删掉 1 条
   还没保存）」。★ 不接在工具条那句话后面 —— 实测掉落页筛选开着时整条
   会折成两行（`main` 那 1320 只剩 70 px 余量）。
⑤ 标的地方：六个配置页、称号卡片两个弹窗、玩家仓库弹窗（等级 / 金币标
   那一格，物品标整格）、卖出价格设置、数据备份设置。
   **「发放奖励」弹窗不标** —— 它那个 dirty 说的是「还有没发出去的选择」，
   没有基线可比。
⑥ 新拦网 `server/test_admindirty.py`（8 条，走 §109 那套 node 跑真 `admin.js`）：
   键序换了不算改过、删一条不会点亮后面、改回原值灭掉、新增算 `added`、
   删掉算 `removed`。★ 按 §109 的规矩**验过它真的拦得住**（把 `stableJson`
   换成 `JSON.stringify`、把基线改成按下标存，两次都当场红）。

**版式在离线渲染台上逐页看过**（这次的渲染台更省事：拿真的 `make_server`
起服务端，只猴补 `AdminRoutes._admin_identity` 永远回系统管理员 ⇒ 免登录，
`shopcfg.DATA_DIR` 指到临时目录，真 `server/data/` 一个字节不碰。
脚本在 scratchpad `lab.py`，没进仓库 —— 30 行，下次照抄）：
物品库 / 商店货架（上架的琥珀底 + 红框并存）/ 合成配方 / 材料掉落 /
称号卡片（含关着的那一档加深一级）/ 金币经验（一条记录两格一起亮）/
卖出价格 / 玩家仓库 / 数据备份，以及保存后标记清掉、放弃修改后清掉、
删一条时标题栏那句话。`touched()` 在 808 件物品那一页量了 2.0ms，够快。

⏳ **没做 / 欠着**：
* **真服务端那一页仍然没点过** —— GM 登录要输密码，agent 不做这件事。
  ★ 这次的改动**只在前端文件里**（`admin.js` / `admin.css`），
  `stop.bat` → `start.bat` 之后刷新一下浏览器就能看见。
* 实机那一遍（打一局看结算界面发没发卡）仍然欠着，见下面的「⏳ 待用户验证」。

上一轮（2026-09-13，会话 44）：**称号卡片页第三次重排 + 达成进度**
（用户点的题，**D111 ~ D114 / §108 / §109**）。用户看了会话 43 那一版说
「改了好几轮界面布局还是不满意」，直接给了新版式 —— **行里不放设置项**。

两套运行时 **3498 项全绿**（会话 43 是 3453）。**只动服务端 + 管理页，
客户端一个字节没改。**

① **规则形状换了**：一条规则 = **对局模式**（模式 / 关卡 / 难度）+
   **一串 and / or 条件**。`count` / `limit` / `note` / `min_shots` /
   `win_only` / 顶层的 `scope`·`metric`·`op`·`threshold` 全删掉；
   达成一次**固定发一张**。「必须胜利」变成新指标 `won`「本局结果」
   （全表唯一的枚举指标，和 `win` 共用一个计数器）。
   ★ 求值**从上往下依次结合，没有括号**；混用连接词时说明文自己把括号写出来。
② ★★ **「玩家累计」改成「攒够就归零、重新一轮」**（用户拍板，**D111**）——
   里程碑那套除法拼不进 and/or。新存档字段 `card_bases`
   `{卡片: {统计键: 上次归零时的累计值}}`，**按卡片存**⇒ 两张卡片共用一个
   指标时各攒各的（用户当天追加的一条，有用例钉着）。
   归零之后进度是 0 ⇒ **一次结算最多发一张**，所以不需要「上限」那一格。
③ **页面重排**：一行只剩 **图标+名称 / 能获得 / 一句现算的说明 / 两颗钮**
   （「对局模式」「达成条件」），17 行**逐像素等高**（66px）。
   条件弹窗里的行也是**列位置写死的 grid**（上一版那条教训）。
   筛选条只剩「全部 / 成就卡片 / 武器卡片」，别的连搜索框一起删了。
④ **说明文两个实现**（**D112**）：服务端那份给游戏提示框，`admin.js` 那份给
   实时预览。**词全部走 catalog 单源**，骨架靠 **`test_cardtext.py` 拿 node
   跑真的 `admin.js`** 逐字比对钉住（§109）。
⑤ **达成进度弹窗**（**D113**）：玩家仓库每行一颗「成就卡片进度」
   （和「修改仓库」左右排，**运营也看得见**）、称号卡片页工具条最右一颗
   「查看本人达成进度」（**只能看自己** —— 它不发 `name`，服务端
   `GET /admin/api/cards/progress` 那一句说了算；看别人要运营以上）。
   一局内的条件写「每局游戏内计算」，只有累计条件画数字 + 进度条。
   ★ 那一行的「筛出 x / y」删掉了 —— 位置让给那颗钮（一行只能有一个 `.grow`）。
⑥ **三条交互返工**（**D114**，用户试出来的）：统计范围**永远改得动**
   （改完指标不合适就清空等重选，不再静默弹回）、「比较」那一格照画
   但只剩一项、**关窗前问「有未保存的修改」**。
⑦ **没写迁移**（用户拍板：那个版本还没发出去过）。本机那份 `cards.json`
   是手工升的，两处运营改过的值（斗士阈值 5、射手限对战）都在。

**版式和交互都在离线渲染台上验过**（真的 `admin.js` + `admin.css`，
`/admin/api/session` 直接回「已登录」，不用输密码）：17 行等高、
条件行六列逐像素对齐、三道护栏的「条件无效」文案、取消/保存/未保存提示、
进度弹窗两组筛选（含累计条件 = 2 张、含局内条件 = 16 张）、
运营身份下两颗钮都锁着。

⏳ **两件没做**：
* **真服务端那一页还没点过** —— GM 登录要输密码，agent 不做这件事。
  ★ 服务端改了要 `stop.bat` → `start.bat` 才生效（铁律 7）。
* **实机那一遍**（打一局看结算界面发没发卡）仍然欠着，见下面的「⏳ 待用户验证」。

上一轮（2026-09-13，会话 43）：**称号卡片掉落页重构**（用户点的题，**D110 / §106**）。
用户看了会话 42 的成果后一句话点破：「**无论怎么设置，总有一个必选的专属指标，
既然这样，还单开一个设置页面干什么**」。根子是 **D100**（当初刻意不做比较符）
—— 于是「为 0」这类条件只能各开一个 0/1 专属旗标。**D100 已作废**（原文留着）。

两套运行时 **3453 项全绿**（会话 42 是 3433，净增 20）。**只动服务端 + 管理页。**

① **指标 22 → 17**：砍掉 `perfect_win` / `carried` / `mob_kills` /
   `weapon_kills` / `weapon_damage`（连带删掉 `CARD_FLAG_METRICS`、
   「适用模式」那一列）。`kills` 现在是**杀人 + 杀怪**，靠「模式」那一格分。
   ★ **「命中率」当天先砍后加**（用户第四轮要回来的）：现在它是全表唯一的
   派生值，**只在「一局内」可用**（比率不能进累计）、**必须配「最少开枪数」**
   （开一枪中一枪也是 100%）、**按不了武器**（`hits@族号` 没人写过，§106）。
   敢加回来是因为有了「用不上的格子不画」——「最少开枪数」只出现在选了
   命中率的那一行，不再是每行都摆着的噪音。
② **加比较符 `op`**（大于等于 / 小于等于 / 等于）。**四条结构性护栏**让「恒成立」
   在配置结构上配不出来，细节和理由全在 D110 —— 其中
   **「小于等于 / 等于」不许带武器维度**那条最不直观（取不到的键是 0 ⇒ 对每一个
   没碰过那把枪的人都成立）。
③ ★ **`cards.due_grants` 里那一行 `max(1, threshold)` 必须跟着改**：不改的话
   「死亡次数 等于 0」被悄悄提成「等于 1」，两边都看不出来。
④ **「武器」升级成和 模式 / 关卡 / 难度 并列的筛选维度**；统计键从
   `weapon_kills@族号` 改名 `kills@族号`。顺带修掉一个潜伏 bug（**§106**）：
   `hits` / `splash_hits` / `accuracy` 以前被标成「能按武器细分」，
   可 `hits@族号` **从来没被写过** ⇒ 那种规则永远取到 0、永远不掉卡、也不报错。
⑤ **管理页三段分组**（算哪一局 / 要达成什么 / 给多少）+ **用不上的格子直接不画**
   （磁盘上真有值的仍然画出来锁住 —— 「手改的值不能从画面上消失」没动）。
   ★ 分段归属写在服务端 `SCHEMA["cards"]` 里，不写在 `admin.js`（D16）。
   ★★ **版式是 grid 不是 flex-wrap**（当天第二轮重做）——
   第一版用自由换行，断点跟着内容宽度走 ⇒ 段边界必然对不齐（用户：
   「一眼看上去还以为排版崩溃了」）。现在列位置写死，
   实测 17 行**逐像素相同**：行宽 1320、每段高 78、段盒子只有
   `when@199+558` / `what@766+558` / `give@199+1125` 三种。
   ★ 比较符改成数学说法（**大于等于 / 小于等于 / 等于**）。
   ★★ **「突击技命中数」做出来了**（用户第三轮：发动次数空房间连点就达标，
   不是成就）。句柄那条路走不通（引信分裂没有包，§107），改用**坐标**：
   `rpDash` 的方向 + 发起点、`rpSplashDamaged` 的受击点、`ChrProps.ini` 的
   `DashNN` 够得着多远。落点 `RoomQuest.note_dash()` / `note_dash_hit()`，
   8 条用例钉着。指标 `dashes` 删掉、换成 `dash_hits`；
   斗士卡从「发动 10 次」改成「**命中 3 人次**」。
   ★ 顺手逆完了 `0x0505`（`packet_api` §2.5a，**订正了原来那句「16 字节一条」**）。
   每条指标那句 `help` 终于画成了下拉的 tooltip（一直在 catalog 里发着，前端从没读过）。
⑥ **不写迁移代码**：本机那份 `cards.json` 实测和默认表逐条相同、云上没这个文件、
   玩家存档里也还没有 `battle_stats` ⇒ 改名成 `.bak-<时刻>`，开服重生。
   已实机跑过：`stop.bat` → 改名 → `launch.ps1 -NoGame` ⇒ 日志「新建了 cards.json」、
   17 条是新形状、`server.err` 空。

**版式验过了**（用真的 `admin.js` + `admin.css` 搭的离线渲染台，数据从 schema 直接导出）：
17 行、每行只剩 10 格左右、三段小标题在位；对战卡不画关卡 / 难度；手改进来的
`stage` / `difficulty` 画出来**并锁着**；累计档「比较」整格不见；指标下拉
一局内 14 条 / 累计 16 条（多的正是「对局数 / 胜利·通关次数」）；
「为 0」没勾胜利时出黄字提醒；退役的 `min_shots` 画成「未登记」不被吃掉。

⏳ **两件没做**：
* **真服务端那一页还没点过** —— GM 登录要输密码，agent 不做这件事，等用户自己登。
* **实机那一遍**（打一局看结算界面发没发卡）仍然欠着，见下面的「⏳ 待用户验证」。

上一轮（2026-09-13，会话 42）：**称号卡片系统**（用户点的题，**D98 ~ D10x / §98**）。
把 D44a 当初砍掉卡片和称号时缺的那个前提 ——「成就统计」—— 补上了。
**本轮只动服务端 + 管理页，客户端一个字节没改**（用户拍板：头顶称号的中文化
等下一版那个「让客户端读我自己改过的资源」的总 hook 一起做）。

已完成（全量 **3433 项两套运行时全绿**，新增 ~83）：

★★ **当天第二轮用户点了五条，其中三条是推翻我自己加的东西**（**D106**）：
称号不可卖出（撤了）、管理页那颗「补齐默认值」按钮（删了，改成开服一次性升级）、
「暴击做不了」（**搞错了对象**：用户说的是攻击加成那 15% 概率，
不是 `EquipBonus` 的死属性 `Critical` —— 服务端**看得到**，`rpExplode` 的
`flags & 0x10`，§102，指标 `crits` 已经加上）。
⇒ 通则记在 D106 里：**发现口子先报告别顺手立规矩**；
**用户说的名词和代码里的同名字段不一定是一回事**。

① **战绩统计底座**：`RoomQuest` 多了 14 个计数器；`record_kill()` 把早就分好的
「杀敌 / 杀怪 / 误伤 / 自杀」四种情况各记一笔（★ 记在 `kills<=0` 那条**早退之前**）；
新的 `Conn.note_battle_stats()` 挂在 `forward_peer_data()`（UDP 旁路和原版中继
**唯一**的合流出口）上，解 `rpFire` / `rpExplode` / `rpSplashDamaged` / `rpDash` /
`rpGuard` 五种内层包。★ **格挡按状态翻转数，不按包数数**（铁律 10）——
`rpGuard` 报的是状态，按包数连点的人一局能刷几百次。
★ 命中只算打到**角色**的（`peer_target_seat`，怪和冰块木箱的句柄也会出现在那一格）。
② **`weapon.ini` 的 `ROH` 进了武器表**（`weapondata.FORMAT` 11→12，和重新生成
`bot_weapons.json` 同一个提交 —— 版本对不上 = **bot 全房间不开枪**且不报错）。
⚠ **只有基础三角色的枪有 ROH**，商城角色自带的枪一个都没有 ⇒ 用商城角色打，
武器卡片拿不到、武器称号的加成也不触发 —— **原版就这样**。
③ **第七份运营配置 `cards.json`** + 管理页新标签页「称号卡片掉落」（排在「材料掉落」
右边）。一张卡片一条规则（自然主键），17 行定死、没有「添加」也没有删除，
「这一版不给」= 关掉「能获得」。
④ **结算发卡**：`0x041c` 的**槽 1** 通电了（协议 §3 早就查明，一直列在「本版不做」）。
⑤ **存档两个新字段** `battle_stats` / `card_grants`，幂等补齐；结算那一发
`apply_battle()` 把经验金币 + 材料 + 战绩 + 卡片**一把锁一次写盘**
（比以前的两发反而少写一次）。
⑥ **17 条称号配方**（`shopdefaults.TITLE` 从 `{}` 填上）：幸运幸存者 = 500 张
幸运卡片 + 100000 金币（用户指定），其余按效果强弱分四档。
⑦ 三张卡片**按韩文原意改名**：厄运→**蹭分**、乌龙→**误伤**、信心→**自爆**。
⑧ **卡片的提示框写获得条件 + 能合成什么 + 那个称号的加成效果**
（**D104 / D106⑤**，用户当天追加）：落点只有一个 `shopcfg.item_desc_zh()`
—— 它既进 `ItemInfo +0x18`（游戏里提示框下半）又进管理页 catalog 的 `desc`
⇒ **改一个函数两边同时亮，前端一行没动**。两段按行数预算分：
第 1 段 = 获得条件 + 每个称号一行「➜ [叫什么]　要什么」，第 2 段 = 每个称号的效果。
★ 配方的**每一种材料都写出来**（本卡片排最前）、**没有配方就不写那一段**、
一张卡片对多个称号 / 一个称号要多种卡片两种形状都说得出来。
⚠ 客户端会**缓存物品定义** ⇒ 改完条件已经在线的人要重登才看得到（写进页面说明了）。
⑨ **开服做一次性升级**（**D106④**，用户拍板）：给老服务器补上 17 条称号配方。
★★ **「一次性」的判据是一个事件**（铁律 10）：`ensure_files()` 这一次
**新建了 `cards.json`** —— 那份配置是这一版才有的，从无到有那一刻正好
就是「第一次跑这一版」。第二次启动整个跳过 ⇒ **运营删掉的配方不会自己回来**
（幂等 ≠ 一次性）。落点 `shopcfg.apply_first_run_upgrades()`。
⑩ 顺手：指标下拉**不按字母序**排（按 key 排在中文下是乱的）；
页面说明里那 7 行残留的 `**星号**` 现在真的画成粗体了（`emphasised()`，**D105**）。
⑪ **武器下拉带上角色**（「左轮手枪（泰尔）」，D106①）—— 武器族本来就绑角色，
说出来省得有人配出「布洛克用左轮」这种永远不成立的规则。
⑫ **「出厂名改过」的一次性升级**（**D107**，用户第三轮）：
`backfill` 只增不改 ⇒ 线上那三个旧卡片名一直没出路。判据是
**盘上那一格还等于我们当初发出去的那个名字**（运营改过的一个都不碰），
自带幂等、不需要标记。两个入口一份实现：**开服**顺手做
（`apply_first_run_upgrades`，云上不用有人记得）、
**`tools/gen_listing.py --refresh-names --apply`** 给已经跑过那一次的服务器补。
★ 本机 dev 服已经用它刷过了：808 件里**只动了那 3 个名字**，其余逐字段不变。

**已实机跑过的**（服务端 + 管理页那一半）：把 `recipe.json` 退回 122 条 +
删掉 `cards.json` ⇒ `launch.ps1 -NoGame` 起真服务端 →
日志打「新建了 cards.json」+「一次性升级: 给合成配方补上了 17 条默认值」→
**122 条原有配方逐字段不变、配方号不撞、旁边留了 `.bak`** →
手工删掉一条配方再重启 ⇒ **一行升级日志都没打、删掉的没回来** →
浏览器里 17 行、6 个筛选、联动锁、「关着的」那一行、提示框的三段内容全走过。
`server.err` 空。

⏳ **只剩实机那一遍**（打一局看结算界面），见下面的「⏳ 待用户验证」。
⚠ **实机要注意**：结算界面那一栏的标签在中文客户端上显示的是**英文 `CARD`**
（`0x48ec0e push L"CARD"`，`Chinese.ini` 里没有这一条，§98），
服务端改不了，**不是 bug**。

上一轮（2026-09-13，会话 41）：**管理页四件事**（**D97 / §97**，用户点的题）。
① ★ **玩家列表新增「状态」列** —— 在线的人现在停在哪：大厅 / 商店界面 /
待机房间·任务|对战 / 游戏中·任务|对战；筛选下拉从三档扩到**五档**
（全部 / 不在线 / 在线 / 在待机 / 在游戏中），仍旧**筛在服务端**（D75）。
★★ 判据是**两组互斥的上行包**，一个计时器都没有（铁律 10）：
§97 实测出**大厅每 3 秒发一发 `0x0200`、人在 ShopStage 期间完全停发、
一退出来当场又来一发** ⇒「回大厅」本身就是事件，不需要「N 秒没动静就算
离开」那种过期口径。房间里那四档由 `LOBBY.room_of()` **现查**（房间状态是
四处都在写的权威），`conn.place` 只管房外那两档。
★ **商店/合成/仓库合并成「在商店界面」一档**（D97a，用户拍板）：三者是同一个
`ShopStage`，进去时五连发**逐字节相同**（52 次实测分类恒为 `0x2`），服务端
分不出；要分开只能改 hook + 重打客户端包，代价不值。
⚠ 两个**不能**当判据的包各有实据：`0x0700`（大厅/房间也发）、
**`0x0601`（★ 结算界面也发）**。
② 「设为管理员（运营）」**从玩家仓库搬回「管理员账号」页**（D97c）——
它是账号管理不是仓库管理；新弹窗「指定玩家为管理员」**复用
`GET /admin/api/players`，一个新接口都没加**，换页栏抽成共用的 `pagerInto()`。
③ 新加管理员 / 改密码**都要输两遍**（D97d），不一致时**一个字节都不发**；
拦在前台（防手滑，不是安全边界，服务端签名没动）。
④ 管理员账号页重排成**左右两栏**（左名单 / 右两张表单卡），新增轻量分区卡
`.sub-panel`；窄屏塌成一栏。⚠ 踩到一个坑：`.field.wide` 的 `flex: 1 1 220px`
在竖排容器里会把 220 当成**高度**，每个字段撑成 220px 高。
⑤ **同一天第二 / 三轮**（用户看了成果之后点的，**D97e ~ D97h**）：
· **「玩家仓库」开给运营** —— 闸必须是 `_require_editor()`，**不能**是
`_require_admin()`（那个只问「登没登录」，只读玩家也过得去）；
这次放开之所以安全，正是因为②已经把提权那颗钮搬走了。标签归档表从三张变
**四张**（新增 `EDITOR_TABS`）。`admin_role` 不再发给运营。
· **管理员账号页两张表单收进弹窗**，页面只剩一张名单（`fit-panel` + 一页 10 行 +
前台分页）；工具条左右分两组、三种配色。★★ 顺带修了 `fit` 那套壳的判据：
改成看「露出来的面板带不带 `fit-panel`」，不再是写死的标签名清单 —— 光给
新页面加那个类**一点效果都没有，也不报错**。
· **金币框 66px → 104px**（实测：66px 正好只装得下 5 位；右对齐的奖励表格子
要 99px 才够 8 位）。★ 哪一格算金币由**字段表自己的单位**说
（`suffix: "金币"`），不是写死清单。顺带修了一条**从来没生效过**的 CSS
（`.reward-bonus input` 权重 (0,1,1) 压不过 (0,2,0)）。
· 筛选文案「在待机 / 在游戏中」→「**待机中 / 游戏中**」。
· 「修改仓库」弹窗 / 「装备卖出」页 / 发奖弹窗左栏那三处也写**在哪** ——
「在线（待机房间·任务）」，出处只有一个 `placeText()`。
⑥ **第五轮（用户当天晚些时候）**：**运营改不了玩家仓库**（**D97i**）—— 他进得了那一页、能查人能发奖，但`GET`/`POST /admin/api/player` 收回了系统管理员（**读也关**）；前台那颗「修改仓库」用 **`aria-disabled`** 锁住（★ **不能用 `disabled`** —— 浏览器不给它派发鼠标事件，那句 `title` 提示根本弹不出来）。另外：弹窗底栏主钮统一金色；★★ **单元测试不再往真实的 `logs/online.log` 里写**（**D97j**，一次排查里它差点把人带偏）。
两套运行时 **3335 项全绿**（新增 58）；浏览器里六档位置、五档筛选、提升管理员
全流程、密码两遍、Esc/遮罩、滚动时表头和按钮不动、运营身份的标签和接口、
六处金币框逐个量过 8 位（验收时建的探针账号都已删回去）。
**服务端改了要 `stop.bat` → `start.bat`**；hook 没动。
**⏳ 真人在游戏里走一圈那一遍还没走（Z-79）。**

上一轮（2026-09-13，会话 40）：**管理页显示「同名玩家账号的昵称」**（**D96**）。
管理员档里本来就没有昵称那一格，用户要的是：**有同名游戏账号的管理员**，
四处都把那个昵称显示出来 —— 顶栏（`已登录：大炮哥（admin，系统管理员）`）、
**操作记录**（`[admin] 'admin'（大炮哥） 保存了 …`，`[sell]` 那行同治）、
**管理员账号页**新增一列「昵称」（没有同名号画 `-`）、**发奖记录**的发送者
（`大炮哥（admin）`）。★ **账号名一律留着，不被昵称顶掉** —— 昵称会改、
也不保证唯一，几个月后能对上号的只有账号名。没有同名号的那一档
**一个字都没变**。出处只有一个：`AccountStore.nickname_of()`，
管理页包一层 `_nickname()` / `_who()`，**每一发现查**（同 `admin_role` 的 D34，
他随时可能在注册页改昵称）。★★ **`gift_history.json` 里不存昵称**（用户当天
第二轮拍板）：记录里只有账号名，昵称是读记录时现填的展示字段（和 `time_text`
同一档），所以**旧记录也显示最新昵称** —— 它回答的是「这是谁」，存快照的话
改过昵称之后整页旧记录写着一个今天谁也不认识的名字。收奖玩家名单和物品名
**仍是快照**（那两栏回答的是「当时发给了谁 / 发的是什么」，用户没点，别顺手改）。
两套运行时全绿（新增 11 项）；浏览器里三档身份 + 管理员账号页 + 发奖记录都点过。
**服务端改了要 `stop.bat` → `start.bat`**；hook 没动。**⏳ 实机那一遍还没走（Z-78）。**

上一轮（2026-09-12，会话 39）：**「装备卖出」返工 —— 版式重排 + 两个后台漏洞**
（**D91 ~ D93 / §95**）。用户看了会话 38 的成果后点了五处版式问题，根子是同一个：
左右分区按 `5fr / 7fr` 分，右栏那排大分类标签**一行放不下**（要 705 px、只有 694），
为了塞下「余量 + 单价 + 按钮」格子又被迫做成一行一件。改法见 D93 ——
左栏钉死宽度、卡片不写单价（浮窗里本来就有）、「将返还的材料」挪到金额栏下面
并画成 chips、价格弹窗删掉顶部说明 + 保存条钉顶不滚 + 两列排 + 「其他」升成
和「装备」平级的一组。**后台两个漏洞**：① §95 —— `sell_items()` 拿
`stackable()` 当「去哪个桶里找」的判据，可**消耗品 / 礼包 / 钥匙那 20 件
`stackable()` 为真却住在 `inventory` 里** ⇒ 页面上摆着、一按确定就说「东西不够」，
一句报错都没有（已换成两个桶相加 + 先材料后仓库地扣）；② D91 —— 卖出回执
只回 `player` / `quotes`，而前台那一发是**整页赋值** ⇒ 管理员卖完一单
「⚙ 卖出价格设置」自己消失（已让回执和 `state` 共用一个构造函数）。
★★ **第三个既有 bug**（§96，用户点的）：卖掉 / 删掉他**正在用**的那张商城
角色卡之后，存档层已经认定他是泰尔了（`player_character()` 会退回 0），
**可大厅座位上那一格是进房时抄的快照，没人改它** ⇒ 人物预览一直停在那个
已经不属于他的角色上，要等重登。`_push_account()` 补第六发
`resync_seat_character()`（状态翻转才发），**卖出 / 修改仓库 / 发送奖励 /
回滚存档四条路一起修好**。
★★ **`sell_price.json` 升成第六份运营配置**（**D95 推翻 D88**，用户拍板
「同等待遇」）：出厂值搬进设计表 `shopdefaults.SELL_PRICE`、开服由
`ensure_files()` 生成、读盘走 `shopcfg._load()`（热重载 + 坏文件保留上一份 +
退回出厂值）、数据备份回滚时并进「运营配置」**那一个勾选项**。
它仍旧**没有配置标签页** —— 标签页那条链是 `SCHEMA` / `CONFIG_FILES` /
`CONFIGS` 三张表，和 `_SPECS` 是两回事（D88 当时把这两件事混成了一件）。
另加 D92（写盘那一侧拦下空 `prices` 表 —— 补默认值是**读盘**的规矩）和
**D94**：卖价百分比上限从 1000 收到 **100**（推翻 D90 那句「100 以上是运营
的自由」—— 那是一条净赚循环，唯一症状是某天有人金币九位数），
价格弹窗改成**管理员可改 / 玩家只读**（`GET /sell/prices` 自己回 `can_edit`），
数量弹窗加拖动条。
两套运行时全绿（新增 6 项）。**服务端改了要 `stop.bat` → `start.bat`**；hook 没动。
**⏳ 实机那一遍还没走（见 Z-77）。**

上一轮（2026-09-12，会话 38）：**管理页新增「装备卖出」**（**D88 ~ D90 / §93 / §94**）。
原版没有卖出功能（客户端那发 `0x0603` 的应答 `0x0503` 还没逆完，本版一直列在
「不做」里），这一版**走管理页**：新标签页「装备卖出」排在「玩家仓库」左边，
**三档身份都进得来** —— 玩家卖自己的，管理员卖他那个**同名游戏账号**的，
找不到同名号就把左右分区盖住（**但工具条照样能点，改卖价和「有没有号可卖」是两回事**）。
定价四条分支：材料按五个小类的单价 / 商店货按买入价 × 百分比 / 合成品按配方金币 ×
百分比**并把配方材料原样退回仓库**（用户拍板，不折价）/ 其余走「其他物品」兜底价；
出厂值 珠子 100 · 通用 200 · 特殊低 300 · 特殊高 600 · 卡片 200 · 装备 95% ·
其他 **500**（会话 38 先按本工程的 100 定，用户当天下午改口 500）。
★ 五个小类**一个硬编码清单都没新建**，全从 `shopdefaults` 那份掉落设计表推
（§93：43 件材料零缺零重；`30016/30017` 是唯二不能按 id 段切的）。
新模块 `server/sellprice.py` + 存档层 `account_store.sell_items()`（一把锁里
扣物品 + 脱装备 + 退材料 + 加金币，照 `compose_item` 的道理，**不**拿
`admin_update_account` 拼）；金币钳到 int32（`MONEY_MAX`）。
**对局中禁止卖出**，判据用现成的 `gameserver.conn_is_playing()`（房间状态翻转，
不是计时器）；提交那一发**再查一次** —— 页面开着的这段时间他完全可能开一局。
越权面只有一个「卖谁的」，由构造堵死：接口连 `name` 字段都不收。
★★ **顺带修了一个既有 bug**（用户点出来的，§94）：`_push_account()` 前四发
**只发给他自己**，而 `0x030b` 是按座位的 ⇒ 管理页给一个正坐在房间里的玩家
开 / 脱装备，**同房另外五个人一点没变**（外观和加成一起过期）。补上第五发
`broadcast_slot_equipped_list()`，**卖出 / 修改仓库 / 发送奖励 / 回滚存档四条路一起修好**。
两套运行时 **3277 项全绿**（新增 67 项）；浏览器里三档身份 + 卖出全流程 +
价格弹窗 + 数量弹窗都走过。

上上轮（2026-09-11，会话 37）：**打包脚本每次都先重编一遍 hook，编不动就一步都不走**
（**D87 / §92**）。D85 之后包里记着 `bshook.dll` 的 SHA-256，而「编 hook」和「打包」
一直是两件分开的事 ⇒ 改完 `hook/*.c` 忘了重编就打包，打出来的是「旧 DLL + 旧 SHA」，
**包自洽、服务端满意、一句报错都没有**。现在 `build-common.ps1` 多了
`Invoke-HookBuild` / `Assert-HookWritable`，两个打包脚本和 `build-menu.ps1` 都在
**删旧成果物之前**调它；hook 被占用（游戏开着 ⇒ DLL 按映像映射，链接器写不进去）
时红框提示「hook 被占用，编译不了 —— 打包中止」并退出，`dist\` 一个字节不动。
判据是真去开一次写句柄，不是猜进程名。**服务端一行没动**；两套运行时
3210 项全绿（新增 6 项）；占用 / 正常两条路都实跑过（服务端包整包 + 自检绿）。
**同一轮第二件事（用户当场拍板）：`hook/build.bat` 两条 `cl` 各加两个 `/Brepro`**
（编译一个、`/link` 后面一个）⇒ 时间戳变成内容哈希，**连编两次逐字节完全相同**，
「每打一次包就弄脏三个文件 / 同版本老客户端对不上新清单」这两条代价一起没有。
★ **代价是这一版 hook 的字节换了**（调试目录多一条 `REPRO` ⇒ `.rdata` 整体挪 `0x20`，
`.text` 64 个引用字节跟着改；逐区域核对表在 §92，**代码没变**）——
所以 `hook/bin/*` + `server/manifest-hook.json` 现在是**改动状态，要一起提交**，
并且**还没实机跑过**（见 Z-76）。V0.3.2 还没发出去，现在换字节不影响在外面的玩家。

上一轮（2026-09-11，会话 35）：**存档转移助手改成「三项明文 + 其余全加密」**
（**D86 / §91**）。导出的存档里只有 `username` / `nickname` / `password` 明文可改，
其余 13 个账号字段封进 `data`（新模块 `server/savecrypt.py`：HKDF 派子密钥 +
HMAC-SHA256 计数器模式 + encrypt-then-MAC，纯标准库）。V0.3.1 及更早的明文存档
**一律拒收**。用户拍板：密钥只用内置常量、不防重放、旧存档一律拒。
⇒ 玩家改数值的唯一出路变成 GM 管理页。
**服务端全量 3198 条（含新增的 `test_savecrypt` 15 条）两套运行时全绿；
⏳ 实机那一遍还没走（见待验证 Z-75）。**

★ 这一版跟着 **V0.3.2** 一起发（用户 2026-09-11：V0.3.2 还没发出去，就用它）。
门禁 / `build-ver` / `manifest-hook` 上一轮就已经是 `0.3.2`，**不用再抬**。

★★ **发版顺序这一次是硬约束**：**先把服务端整体覆盖成 V0.3.2，再发客户端包**。
线上玩家现在跑的是 V0.3.1 —— 新客户端导出的存档传到**还没换的 V0.3.1 服务端**上
会被**静默清档**（§91 一：等级 1 / 金币 0 / 仓库空），提示还写着「上传成功」。

上一轮（2026-09-10，会话 33）：**根治「任务模式进 boss 房黑屏 / 全场一起闪退」**
（**§78 / §79 / D78**，现场在 `bug调查/18/`，报告 `bug调查/18/调查报告.md`）。
用户报的是「进 boss 房有时黑屏无响应」，截图停在换图加载那一帧（100% / 正在等待其他玩家）。
**查下来黑屏和闪退是同一个 bug**：关卡内换图（`0x47900a`）会把世界里登记过的对象
整片析构（`0x48d326 → 0x473692`），可六个角色是单独摘出去、原样挂回来的 ⇒
角色身上 `+0x57C` / `+0x580` 那两格**突击技对象**成了野指针，下一帧
`ProcessDash` 拿去 `call [eax+0x78]` 跳进已释放的堆。
**这就是 §66 那个野指针的来路**（bug调查/17 当时没查到）。每台机器都在本地模拟
全部 6 个角色 ⇒ **一屋子人在进新图后 1~3 秒一起崩**；落点在未提交页时原版报告器
自己也崩（§65），进程要挂 40~90 秒 ⇒ 窗口就停在最后一帧显示「未响应」= 用户看到的黑屏。
数据：9-10 一天 **107 条游戏服连接里 20 条断在 `0x0412` 之后**、11 个「全房间同时死」
的簇、**23 份崩溃报告里 16 份**是它（3 份 dump 栈完全一致）。
★ **服务端一点问题都没有** —— 252 次换图**全部** 1.6 秒内回了 `0x0418`。
修法：`hook/bshook.c` 新增 `try_patch_mapchange_dangling()`，偷 `0x4790cb` 起 6 字节，
**卸完场景、重挂角色之前把那两格清零**（D78 讲了为什么是清零、为什么补在这个位置）。
`hook/bin/` 已重编（bshook.dll 191488 字节）；真客户端起过一次，
`PATCH ★换图野指针根治` 那行在，从**活进程**回读 `0x4790CB` 反汇编核对过
（`E9 rel32 + NOP`，下一条指令 `0x4790D1 push edi` 边界完好）。**服务端一行没动。**
**同一轮的第二件事：把 §79 剩下那三份「各一次」的散崩全部查穿并修掉**
（**§81 / D80**）。上一轮记成「无复现」是**没挖**——三份都带完整 minidump。
① `0x55C811` 胜负条件析构走**已释放的 LobbyStage**（拆除顺序 bug，dump 里
`[0x72e29c]` 已经是 0 而 `[胜负条件+0x174]` 还留着旧指针）；
② `0x4012FE` 读 UTF-16 资源时 `malloc(len+1)` **少一个终止字节** —— 宽字符终止符要 2 个
（`Pack_decrypt` 里 **272 个 UTF-16 `.ui` 字节数全是偶数**，个个都踩着，只是堆里下一个
字节通常正好是 0）；③ `0x438BD5` UI 向量 `_First` 为空还照样取元素（嵌套消息泵重入，
这一处**是兜底**）。
★ 方法上两个坑：这批 dump 的异常 `CONTEXT` 大半是坏的（别信寄存器，用
`ExceptionInformation[1]` + 扫栈），扫栈要按「前一条确实是 call」过滤。

**★ 同一轮还试错了一次，必须记住**：为根治 §68 那 4 份 `nmconew` 崩溃，
试过「不让 `nmconew.dll` 加载」（用户拍板「NMService.exe 没用可以不要」）——
**静态七条逐条坐实，实机第一次登录就翻**（**§80 / D79**）：点登录后 9 毫秒弹
「认证服务器失败 (20000)」，一个网络包都没发。**登录路径确实依赖
`NMCO_CallNMFunc` 返回 1**（平时能登不是因为信使能用，是因为那一发返回了 1）。
**已从 hook 里整个拆掉**（用户实机确认登录恢复：00:24:06 `test1` 认证通过）。
教训写进 D79：「所有可见分支都能接住失败」推不出「整条路都能接住失败」，
拔子系统的判据只有一个 —— 把它拔掉，把那条路真的走一遍。

**第四件事：§68 那一族**换个下手点**根治了**（**§82 / D81**）。
§68 说「跳过崩的那两句只是把崩溃挪一行」—— 真去读代码，**不是**：
两处各自都有**原版就存在的干净失败出口**（`nmconew+0x98DBB` 的
`mov esp,ebp/pop ebp/ret`、`+0x2666C` 的「魔数不符」收尾），判一下跳过去就完事。
做法是**放行加载、加载完再打**：hook `nmcogame` 那一格 `LoadLibraryA`（唯一加载点），
**放行**之后拿真实基址补两处 ⇒ **模块照常加载、`NMCO_CallNMFunc` 照常返回、
登录路径一个字节不碰**，这一处不可能弄坏登录。
（两处偷的字节里一个重定位项都没有，对着 `.reloc` 逐项查过 ⇒ 文件字节 == 内存字节。）
★ 用户随后拍板试了一轮「整个 DLL 都不要、登录那一发直接回 1」（`BSHOOK_KILL_NM`）
—— **实机又翻了**（**§83**，还是 20000、认证服 0 连接），当场退回 §82 并把代码删掉。
⇒ **`nmconew.dll` 拿不掉**：登录真的需要它产出一份**本地造的应答**
（NEXON 服务器停机 15 年，登录却照样成，就是这个道理）。
`NMService.exe` 还是会起，**铁律 5 仍然只是写在纸上**，但它已经不是崩溃来源。
★★ 这一族改动**两次**都栽在同一处：静态能证明「每个可见分支接得住失败」，
**证明不了「整条路接得住失败」** —— 判据只有实机登一次。

⚠ **线上玩家要拿到这两条，必须重打客户端包** —— `dist/PopShot-portable-win64_V0-3-1`
（22:21 打的）里那份 `bshook.dll` 是 `898286D4…`，**不含本轮修复**；
V0.3.0 线上那份 `2290ECAD…` 连 bug调查/17 的五处守护都没有。**等实机（Y-69）**。
上一轮（2026-09-10，会话 32）：**管理页「发送记录」—— 发过的每一次都能翻账**
（**D77 / §76**）。用户点的题三条：① 那个钮挪到工具条**最右端**、改名「批量发送奖励」，
和新加的「发送记录」一起排在「当前在线」左边；② 「发送记录」弹窗左右分屏 —— 左栏列
发送时间 + 发送者，点一条右栏画那一次的时间 / 发送者 / **奖励玩家名单** / **奖励物品**；
③ 左栏列表上方一个「清空记录」；④ 记录落在 `server/data/gift_history.json`，
**不进 git、不进包**。
做法：新模块 `server/gifthistory.py`（`load` / `append` / `clear`，落点每次现取
`shopcfg.DATA_DIR`，写锁借 `shopcfg.write_lock()`，200 条上限，读不动的旧文件挪成
`.bad-<时刻>` 不覆盖）；`admin.py` 加 `_reward_record()` + `GET /admin/api/reward/history`
+ `POST /admin/api/reward/history/clear`（都是系统管理员档，三张门禁清单已补）；
`admin.html/.js/.css` 加 `#historyModal` 和 `.tool-right`。
★ 记录里存的是**发送那一刻**的物品名和昵称（物品改名 / 下架后旧账才不会变成一排 `#id`）；
发失败和被跳过的人**照样留在名单里**；记不下来**不算发送失败**（礼物已经出去了）。
★ 打包排除是 structural 的：包里 `server\data\` 本来就是空目录 + `Assert-PackageDataClean`
守着，跑过一次 `build-server-package.ps1` 验收（`server/data` 空、`gifthistory.py` 进包）。
两套运行时 **3140 项全绿**（新增 11 项）。
**同一轮还修了一个实机 bug**（**§77**）：礼物盒里的装备指上去，提示框弹得出来但下半两块**空的** —— 那两块读的是 `Gift` 自带那份 `ShopStock` 的说明（我们发空串），**不是** `ItemInfo+0x18`；而同一个框里的等级 / 角色 / 修理次数走的正是 `ItemInfo`，所以「有一半是对的」。`shop.gift_note()` 和**货架**同一个来源，`build_gift(note=None)` 默认现算（新增 3 项用例，X-68 等实机）。
★ **服务端改了要 `stop.bat` → `start.bat` 才生效**（铁律 7）—— 这一轮**没重启用户那台**：
他的服务端和客户端当时正开着，验收是另起一台临时服务端做的（怎么做见 §76）。
上一轮（2026-09-10，会话 31）：**管理页「发送奖励」—— 批量发到玩家的原版礼物盒**
（**§75 / D76**）。用户点的题：给多人发奖励只能一个个点开「修改仓库」，太麻烦。
① 先把原版礼物盒协议**整条逆穿**（§75，全静态）：`Gift` = 货架同款 `ShopStock` +
发送人 + 留言 + `SYSTEMTIME` + 未打开位 + 礼物 id；`0x0609` = 礼物 id + 动作（2 打开 /
0 领取 / 1 丢弃）→ `0x050a`；`0x0507` 无正文 = 服务端主动推的「收到礼物」，任何界面都能收。
② 礼物住在 `accounts.json` 每个账号的 `gifts` 里（`add_gifts` / `open` / `claim` / `discard`，
幂等补齐，号永不复用）；`0x0607` 回真清单（先 `0x0501` 定义再 `0x0508`）；领取时先推仓库 /
数据栏、`0x050a` 排最后；发奖后给在线的人推 `0x0507`，登录后第一发 `0x0700` 按 `gift_seq`
翻转补提醒。③ **经验 / 金币装不进原版礼物** ⇒ 凭证 id `9900001 经验` / `9900002 金币`
（物品表里不存在，永远进不了仓库；接收弹窗图标空白是已知代价，D76）。
④ 管理页：「玩家仓库」工具条「当前在线」左边加「发送奖励」钮，弹窗左栏选人（搜索 /
在线筛选 / 全选 / 名单 chips）、右栏经验 / 金币 / **留言（可自由改，默认「管理员发送的
奖励」）** + 物品区（复用「修改仓库」那套分类标签 / 下拉 / 批量选择器），确认框后
`POST /admin/api/reward/send`；装备已拥有的人跳过并在回执里点名。
⑤ 控制通道加 `gift item|exp|money` / `gifts`；顺手修了上一版 `playerOnline` 重复 id
（一开「修改仓库」就把在线筛选下拉抹空），并加了「id 不许重复」的静态检查。
两套运行时 **3126 项全绿**（新增 32 项）；`stop.bat` → `launch.ps1 -NoGame` 起过真服务端，
浏览器里把弹窗走了一遍。
**第二轮（用户实机看了礼物盒）**：礼物盒、8 格、接收 / 合并弹窗都画出来了 ⇒ 线格式对了；
三处修正：① 赠品条目第二个 int 是**数量**（以前填 0 → 弹窗写「（0个）」），现在填真实数量；
② **材料没图标**是客户端查错了表（礼物槽 / 接收弹窗查 `[Stock-]` 表，材料只在 `[Item-]` 表）
—— **hook 新增 `try_patch_gift_icon_fallback()`**，查不到就退回第二张，`hook/bin/` 已重编；
③ 弹窗左栏搜索框改成边打边查（带序号丢弃乱序回包）。
★ 服务端改了要 `stop.bat` → `start.bat`；**hook 改了，客户端要用新的 `hook/bin/bshook.dll`
（本机 `start.bat` 自动带上；线上玩家要重打客户端包 + 抬版本号）**。
上一轮（2026-09-10，会话 30）：**管理页开给普通玩家（只读），玩家仓库加在线筛选**
（**D74 / D75**）。
① 登录框不变，**输什么账号决定你是哪一档**：管理员表里没这个名字就退回游戏账号
验一次，验过 = 第三档 `player` —— 看得到 `CONFIGS` 那几个配置页，**一个字改不了**
（输入框全 `disabled`、按钮整排收走），后面三页连标签都不画。
★ 会话记着「你是拿哪一种口令进来的」，玩家档的令牌**永远**只读（不记的话，
系统管理员建一个同名管理员账号就会让一个正开着页面的玩家当场升权）。
★ 服务端才是门：写配置那一发挂 `_require_editor()`；前台是「画完统一扫一遍」
（`lockList()`），不是每个控件工厂各加一句 —— 漏一个的症状是**改得动、存不上、
不报错**。
② 玩家仓库多一个「全部 / 在线 / 不在线」下拉，**筛在服务端**（`search_accounts(keep=)`，
在数总数的同一遍里）⇒ 命中数和页数都按筛完的算，不会「一页两行、页脚写共 5 页」。
「当前在线：N 人」照旧是全服的，不跟着缩水。
③ 顺带修了个老 bug：退出登录只把 `TAB` 改回起点、没管标签高亮和哪个面板露着
—— 管理员停在「玩家仓库」退出，下一个人登进来看到的还是那一页。
两套运行时 **3094 项全绿**（新增 22 项）。浏览器里三档身份都真登过：只读档
五页逐页点过（1542 个输入框全锁、列表里 0 个按钮）、运营档一切照旧、在线筛选
三档 + 翻页 + 空表文案都对。**服务端改了要 `stop.bat` → `start.bat`**；hook 没动。
上一轮（2026-09-10，会话 29）：**管理页新增「金币 / 经验获取」标签页**（**§70 / §71 / D72**）。
用户点的题：打一局给多少金币和经验，以前 12 个常量写死在 `gameserver.py`，
调一次要改代码 + 重启。现在是**第五份运营配置** `server/data/rewards.json` ——
对战 8 档（生存 / 夺分 × 道具战 × 个人 / 组队）、闯关 21 档（7 关 × 3 难度）、
加成系数 1 条，共 30 档，管理页画成**两张二维表格**，顶上「金币 / 经验」一切换
换填另一对字段。★ **对战八种组合以前给的钱一模一样**，现在各查各的。
★ 默认值**逐格等于老公式算出来的数**（用同一个表达式展开，不手抄；
`test_shopcfg.RewardDefaultsTests` 把老公式抄成第二个实现逐格比对）——
上线那一刻玩家收入一分不变。★ 这一份**读不到 / 缺档位时退回内置默认值而不是空表**
（空表 = 打完一局一分钱不给，那一局补不回来）。
页面仍住 `#cfgPanel`（读 / 存 / 脏标记 / 三方合并全共用），只加了一个渲染器
和四处 `if (which === "rewards")`。
第二轮（用户看完第一版）：**加一张「等级与经验」只读参照表**（**§72 / D72a**）——
60 级分三栏并排，数来自新增的 `account_store.level_table()`，**页面不自己套公式**
（曲线只有 `account_store` 一处定义）；满级那一行写「满级」不写 6000。
第三 ~ 五轮（版式，**D72b**）：「金币 / 经验」切换从独占一行挪进工具条最左边
（复用 `button.cat`，CSS 选择器去掉 `.cat-tabs` 前缀）；等级表改成**弹窗**
（✕ / 点遮罩 / Esc 都能关，只读所以认遮罩）；工具条定为
`[金币|经验] [▤ 等级经验对应表] [↻ 刷新]`；对应表钮的白底太突兀，改穿
**面板标题栏那身衣服**（深棕底 + 琥珀边）；切换改成真正的分段控件
（连在一起、接缝无圆角、一条 2px 竖线）；弹窗里那张表 `width: 100%` 撑满 +
`scrollbar-gutter: stable both-edges` 左右居中。
第七轮（用户问「V0.3.0 已经发了，老服务端没有 rewards.json 怎么办」，**§73**）：
**三条升级路径逐条实测**（启动前就结算 / `ensure_files` 只补新那份 /
回滚到老备份）——**都是干净的，代码不用改**；补了两条回归用例把它钉住
（都不写死文件名，下次加第六份配置自动跟着走），顺手改掉 `app.py`
那段说「三份运营配置」的过时 docstring。
第八轮（用户点出刷新钮还写着「四份配置」，**D72c**）：**份数和页名一律从
`CONFIGS` 现数**，四处用户看得见的文案全改成 `configCount()` / `configTitles()`；
配一条静态检查扫三个前端文件、见到汉字数字的份数就红 —— **它当场就抓到了
第五处**（「管理员账号」页那句「这五页」）。分隔符从「 / 」改成「、」：
新页标题自己带斜杠，五页会读成六页（画到浏览器里才看出来的）。
第九轮（用户追问「运营也要能看到能修改」）：**本来就是对的**（`rewards` 不在
`SYSTEM_ONLY_TABS`、服务端走 `_require_admin()`），实测运营身份下五个配置页全在、
`GET`/`POST /admin/api/config/rewards` 都 200、格子能改能存；补了一条前台护栏
`test_no_config_page_is_hidden_from_operators`（藏错了服务端照样放行、只是标签
不见，收不到任何报错）。两套运行时 **3072 项全绿**（新增 41 项）。
浏览器里用桩数据把两张表、金币 ⇄ 经验切换、脏标记 / 放弃修改都点过；
真机起过服务端，`ensure_files()` 生成的 `rewards.json` 和老公式**逐格核对为 0 处不一致**。
**等实机打一局看结算数（W-65）**；★ 服务端改了要 `stop.bat` → `start.bat`，hook 没动。
上一轮（2026-09-10，会话 28）：**排云端收到的 5 份线上崩溃现场**（**§65 ~ §69 / D71**，
现场在 `bug调查/17/`，报告 `bug调查/17/调查报告.md`）。
最要紧的一条是「**我们为什么看不见闪退**」：5 份里 **3 份是空的** —— 原版写崩溃报告的
`0x5d7695` 在「出错地址不在任何已加载模块里」时 `[0+0x3c]` 读空指针，报告断在
`Exception code` 那一行、`.mdmp` 根本没生成（§65）。本机 A/B 逐字节坐实并修掉了
（原版 238 字节无 dump → 补丁后 982 字节 + 12.6 MB dump）。
⇒ 推论：那 3 次线上闪退 = 出错地址在未映射页 = **通过野指针 call 跳飞**，
和有完整栈的那次 `PRIV_INSTRUCTION`（§66，`[Character+0x57C]` 的 `DashDamage`
虚表已经不是虚表）是同一族 —— **5 次里 4 次同类**。
另外查明：`0x0411 gspEndGame` 处理器关卡外收到就读空指针（§67，本机 09-10 00:05:39
那次 `BigShot.rpt` 就是它）；剩下 1 次是 `nmconew.dll` 自制互斥体在等待期间被析构（§68，
**没修**，理由见 D71 ④）。
`hook/bshook.c` 新增 `try_patch_crash17_guards()` 共 **5 处**补丁
（报告器判空 / 突击技对象三处虚表校验 / `gspEndGame` 判空），外加心跳里的
**地址空间快照**（§69）。`hook/bin/` 已重编（bshook.dll 185344 字节），
真客户端起过一次、5 处签名全对上、从活进程回读反汇编核对过。
**服务端一行没改。** **等实机（V-64）**；★ 线上玩家要拿到得**重打客户端包 + 抬版本号**。
上一轮（2026-09-10，会话 27）：**复核会话 26 的「岩浆巨龙 boss 永久无敌」修复**（**§64 / D70**）。
线上玩家报的，根因**在客户端**：把 boss 从石壳免疫（阶段 -1）解开的全客户端只有
`Quest02Tracing` 里那句 `$공략포인트`，而放这份剧情的舞台检查要求「没有剧情在播」
**且** boss 还在出生状态（`vf_154(4)` = 状态名 == `"birth"`，只活 120 刻 = **3.84 秒**），
错过就永久无敌。会话 26 的补丁（`0x4b5a04` 那 20 字节换成 `cmp [boss+0x2a8], -1`，
判据换成「弱点还没点出来」）**逐字节核对过，正确且是严格超集，保留**。
但它解释「为什么是有时」的两条细节被证伪（`fadeout`/`fadein` 不阻塞脚本；
`Quest02Lava` 无人引用），Quest07 Map01 并非「同脚本挨着写」，**触发条件仍未查清**；
服务端日志也分不清「Tracing 没放」和「放了仍无敌」。
本会话做的：改正 §64 / D70 / hook 注释；`hook/bshook.c` 加 **DRAKA 诊断日志**
（boss 创建 / 门开 / Tracing 入队 / 弱点点出四个中途 hook，一局几行，默认开），
下次出事一份 `logs/bshook_*.log` 就能定案。hook 已重编、真客户端起过一次确认
PATCH / HOOK 行都在。**等实机打一局（U-63）**；★ 线上玩家要拿到得**重打客户端包 +
抬版本号**。
上一轮（2026-09-09，会话 25）：**修「自己装备的装备别人看不见」**（**§63 / D69**）。
根因：`0x030b` 全服务端只有一个发出点，永远是「座位 = 我、清单 = 我的存档、
收件人 = 我」⇒ 每个客户端手里只有自己那一格，别人那五格恒空。
这一节顺带把**外观**那条路查穿了：`0x030b` 的处理器尾巴 `0x406f42` 拿
`[LobbyStage+座位*4+0x1d0]` 那个 3D 角色，`0x506251` 清干净再逐件 `0x505bb9` 挂上去；
itemId→模型那张表是 `[0x72e1e0]`（客户端启动时自己从 pak 的 `ShopItem-Chn.ini` 读），
**不是**服务端喂的 ItemDB ⇒ 服务端只要给 itemId，观察者也不用先查过这件物品。
战斗里同一份（`0x493258` 建完角色紧接着 `0x406f42`）。
改法：组包函数入参从「连接」改成「**存档**」（拿错在类型上就说不通），
新增 `send_room_equipped_lists()`（进房 / 回房间：**每个有人的座位各一发**）和
`broadcast_seat_equipped_list()`（某一格换了内容就广播）。挂了六处：进房、有人进房、
穿脱 `0x0702/0x0703`、bot 占座、结算看完回房间、调试 `equip`/`unequip`/`sync-account`。
★ **bot 那一格发空清单而不是不发** —— 清单存在**跨房间活着**的 LobbyStage 上，
不覆盖的话 bot 会穿着上一个坐这儿的人的装备（改动里最容易漏的一条）。
两套运行时 **3031 项全绿**（新增 5 项）。**等实机验（T-62）**；
★ 服务端改了要 `stop.bat` → `start.bat`，hook 没动。
上一轮（2026-09-09，会话 24）：**管理页的筛选换成「两行分类标签 + 角色 / 上架状态
两个下拉」，换页栏删掉**（**D68**）。用户说原来那几个下拉「不好用、也筛不出想要的东西」，
而「修改仓库」弹窗那两行分类标签「交互很舒服」—— 物品库 / 商店货架 / 合成配方 /
「选择物品」弹窗全换成同一套（游戏仓库那棵树，§41 / D47），四处再各挂一份
角色 + 上架状态下拉（「修改仓库」弹窗也加，在「＋ 添加物品」和「↻ 刷新」之间）；
材料掉落页不动。「类别」下拉和「只看上架」开关删了，换页栏（120 / 10 / 200 一页）
也删了，筛完剩多少画多少、滚动条在列表上；标签上的件数是过了其它筛选之后的数。
只改了 `server/web/admin.{html,js,css}` 三个静态文件，**服务端一行没动、不用重启**
（静态文件每次读盘 + ETag，浏览器刷新即拿到新的）。顺带修了 `matches()` 里上架状态
拿 `entry.id`（合成配方是配方号）的 bug。`test_web_admin` 119 项两套运行时都绿；
浏览器里用桩数据（`fetch` 打桩、不登录）把四页 + 两个弹窗都点过，件数和 §41 对得上。
代价：物品库「全部」一次铺 808 张卡，整份重画约 0.2 秒（筛过之后只有几毫秒）。
第二轮（用户看了截图，**D68a**）：「筛出 x / y」只留掉落页（另外三页标签上有件数）；
掉落页筛选条在 1180 宽里零余量、「筛出」一出现就折行 ⇒ `main` / `.tabs` 加宽到 1320。
第三轮（**D68b**）：商店 / 合成两页的「上架状态」下拉删掉（那两页本来就是上架清单）；
掉落行的材料格定宽 156，行与行的 数量 / 概率 / 备注 上下对齐。
**等用户实机看版式（S-61）**。
上一轮（2026-09-09，会话 23）：**关掉客户端自己画的那行绿色加成文字**（**§59 / D65**）。
用户实机看完说明文后报了五个现象，查下来**四个不是我们改出来的** —— 仓库 / 合成提示框里
那行亮绿字是**客户端自己**按 itemId 查本地 `EquipBonus-Chn.ini` 生成的（`0x414128`），
和服务端下发的说明文重复，还自带三个原版缺陷：`Chinese.ini` 错字「速度+d%」
（`Spd%+d%%=速度+d%%` 漏了个 `%`）、`Team %d%% %s` / `Self %d%% %s` / `Down` 三条漏翻
（称号上冒英文）、`ItemInfo2Txt` 只有 240×14 一行装不下就溢出。
「绿字错位」也不是错位 —— 三个框位置写死在 `.ui`，绿字本来就夹在两段中间（y=256）。
★ 处理：hook 里**两处各一个字节**（`0x41415E` 和 `0x414208` 的 `je` → `jmp`）。
⚠ **第一版只堵了一个生成器，用户实机报「商店浮窗里还有绿字」** —— 商店走的是
**另一个**生成器 `0x4141AC`（按 id 列表求合计加成，给礼包/套装用），
两个都堵上才全灭（五个绘制点：三个提示框 + 修理界面两处）。
`hook/bin/` 重编（178688 字节）。服务端一行没改。
顺带把 §1 留了很久的 ❓ 结掉：**`.ini` 加载器只读 pak，散文件不生效**
（`0x413f13` 不在 pak 就跳函数尾）⇒ 改 `Chinese.ini` 只能重打 `.pkn`。
**等实机验（R-60）**；★ 正式包必须连 hook 一起重打。
上一轮（2026-09-09，会话 22）：**客户端闪退后自动上传诊断日志**（**§54 ~ §58 / D58 ~ D64**）。
新增 `server/crashwatch.py`（客户端侧：认 PID → 等进程退出 → 判崩溃 → 打包 → 上传 → 补传）
和 `server/crashstore.py`（服务端侧：校验 → 流式收包 → 后台落位 → 按天清理），
接口 `POST /api/crash-report` 挂在注册页那个端口上。宿主是**本机中继**不是本机服务端
（D58：模式由构造保证，不推断）。顺带纠正了一条协议结论：**`0x0103` 本身就是崩溃报告包**，
而且**客户端上报完会把 `LastCrashReport.txt` 删掉**（§54，`packet_api.md` 已重写）。
两套运行时全量测试 **3014 项全绿**（新增 73 项）。**等实机验（Q-59）**。
上一轮（2026-09-09，会话 21）：**把 13 种装备加成全写进物品说明文**（**§53 / D57**）。
起因是用户发现「火焰蝙蝠有溅射加成却没说明文，要不是踩到闪退根本不知道」——
根因不在数据（`shop_items.json` 早就把 13 种都提取好了），在 `shopcfg.BONUS_ZH`
**只翻了 5 种**，剩下 8 种被一句注释跳过了。本轮把 `GetEquipBonus` 的 **14 个调用点
逐条对过**，13 种属性的算法 / 概率门 / 单位全部落到 §53；`Critical` 查实是**死属性**。
`item_desc_zh()` 重写成「同类压一行 + 特殊效果用 `|` 另起一段」：
**上架 617 件从「102 件空说明」变成 0 件空**，武器的**溅射伤害 / 范围**第一次画出来，
满 5 属性的 10 件铠甲不再被截掉「体力」。顺带：提示框实际**只有 2 段**不是 3 段
（订正 §31③ 和 `packet_api.md` 两处）；扫出并按 D55 补了 `220004` 迷你机械青蛙的
**同型空指针守护**（`0x481EF3`，hook 重编 178688 字节）。管理页浮窗也跟着认 `|`。
第二轮（用户要求「称号将来要上架，现在就查到底」）：**三个只在 exe 里写死、
任何数据文件都查不到的称号全部查穿**（**§53 ⑤**）——
`560004` 受击 50% 概率完全免疫、`560005` 开局起伤害翻倍（额度 45，
`新伤害 = min(原伤害×2, 剩余额度)`）、`560006` 捡「心」时 15% 概率全队各回 5 点。
⇒ **20 个称号现在一个不落全有说明文**，`title` 也收进了「不许空说明」那条护栏。
顺带查实：`[角色+0x2b4] != 0` = **已死亡**（和当前 HP `[+0x150]` 成对写）；
★ **两条客户端上行包服务端没有处理器** —— `0x040b`（16 字节，青鸟**已上架**会发）
和 `0x0415`（4 字节，560005 用完额度时发），已补进 `re/packet_api.md`。
两套运行时全量测试 **2941 项都绿**（3.14 / 3.8，新增 16 项）。
**等实机验（P 区）**；★ 服务端改了要 `stop.bat` → `start.bat`，正式包连 hook 一起重打。
上一轮（2026-09-09，会话 20，第二轮）：**把 `BigShot.rpt` 里 13 次崩溃全部归了因**
（§48 ~ §50 / D56）：08-06 ~ 08-08 五次是 V0.1 假服务端时期的，早修掉；08-11 一次是
教程弹窗点确认时大厅对象为空；09-05 两次是退出拆到一半又收到 WM_CLOSE（字体管理器
已销毁）；09-06 两次是 D31a 那个「角色限定改宽」—— 机制查清了：`Bone_Spine_01` /
`Bone_Pelvis_01` 只有布洛克骨架有，ch02 网格挂到别的角色身上蒙皮表里骨骼全 NULL。
hook 新增 `try_patch_rpt_crash_guards()` 三处判空（`0x40f4ea` / `0x40ef90` / `0x5d27f1`），
`hook/bin/` 重编（bshook.dll 177664 字节），实机 02:35:25 四条 PATCH 行都在；登录界面
`CloseMainWindow()` 冒烟正常退出。★ 顺带做了上架物品资源审计（**§51**）：478 件角色
网格资源 / 骨骼 / 角色限定全对；羽翼套装 3 件缺图标（`1010022` 系）；330 件中国版没
中文名的实机没穿过 —— 有守护兜底后最坏是缺块模型，不闪退。**等实机验（O-54）。**
第三轮（**§52**）：加成路径逐条排过 —— 14 个 `GetEquipBonus` 调用点、6 枚戒指的
稀有属性，同类漏判空**只有火焰蝙蝠一条**；另外四枚戒指静态干净，实机没戴过，并进 O-54。
本轮第一件事：**修「穿着火焰蝙蝠扔苹果弹打空 → 客户端闪退」**
（**§47 / D55**，用户 01:36 在 `main` 实机踩到，dump 在 `game_patched/Dump/`）。
原版 bug：`rpExplode` 目标句柄查不到时，画「溅射加成」提示的 `0x47e776` 在中国区
分支没判空（`0x47ea6c` 读 NULL）；`flags 0x200` = 射手穿的 **IncSplashRange** 装备
那道 15% 门开了，全表只有 `220003` 火焰蝙蝠带它。自己发的包本地回环先处理，
所以射手自己先崩 ⇒ 只能在客户端修：`hook/bshook.c` 新增
`try_patch_splash_visual_guard()`（`0x47ea21` 补判空，没目标就不画），`hook/bin/`
已重编（bshook.dll 175616 字节），实机 hook 日志 01:59:12 打出
「★溅射加成提示判空 @ 0047EA21」。**「打空不再闪退」等实机复现验（N-53）。**
★ 服务端一行没改；**正式包必须连 hook 一起重打**（09-09 那份 zip 里是旧 DLL）。
上一轮（2026-09-09，会话 19）：**修「一个角色穿铠甲，另两个角色自动脱掉」**
（**§46 / D54**，用户在 `dist/…V0-3-0 - 副本` 实机踩到）。
根因在 `shopdata.resolve_equipped()`：整份 `equipped` 用**一个**总掩码收敛，
同 `part_flag` 就「顶掉」，没看角色；客户端是三个掩码、一个角色一个（§31 ①），
从不认为跨角色冲突（`0x0703` 一发都没发）。武器 / 突击技 / 喷漆 / 戒指全是同一个坑。
改法：`shopdata.slot_owners()` 一处定「哪件点亮哪几个掩码」，收敛 / `conflicts` /
`0x0604` 与 `0x030b` 的掩码三处共用。存档不用迁移（被错顶的只是从 `equipped`
掉出来，仓库里还在，重穿即可）。两套运行时全量测试 **2925 项都绿**（3.14 / 3.8）。
★ 修好的 `server/shopdata.py` / `shop.py` 顺手拷进了那个「副本」目录的 `server/`
（要 `stop.bat` 再 `start.bat` 才生效）；**正式包得重打** —— 09-09 打的 V0.3.0 zip
里还是旧代码。**等实机验（M 区）**。
上一轮（2026-09-09，会话 18）：**管理页三处用户点的题**，没动玩法代码，
`server/test_web_admin.py` 118 项绿。
① 「玩家仓库」页工具条右端那颗「● 当前在线：N 人」的圆点从苔绿改成琥珀、
数字改 `--gold-edge` 金黄（**D40b**，深浅是用户看着五档对比图挑的）；
② 标签页「玩家资料」→**「玩家仓库」**、按钮「修改背包」→**「修改仓库」**，
工程里指这两处的说明文字一并改（**D40c**；★ 协议 / 数据层那个「背包」不动，
`sessions/` 的旧记录不改）；
③ 「选择物品」弹窗加**「上架状态」筛选**（**D42a**：未上架 / 已上架 /
上架商店 / 上架合成，「已上架」= 两边任意一边；和物品库那一页共用同一份
选项和判据）。
上一轮（2026-09-08，会话 16）：**打包不再自动重跑提取，更新数据只剩一个脚本（D53）**。
① `build-portable.ps1` / `build-server-package.ps1` 里的 `Update-MapData` /
`Update-WeaponData` / `Update-ChrProps` 三次调用和三个函数都删了，打包只**拷 + 验收**。
② ★ **`tools\update-gamedata.bat` 是现在唯一一个更新数据的脚本**（2026-09-09 改成
**纯 ASCII 壳 + `update-gamedata.ps1`**，中文全在 ps1 里 —— bat 里放中文会被 cmd
拦腰截断甚至倒退重跑，见 D53 末尾和会话 17）：地形 / 武器 /
角色属性 / 物品表 / 图标图集五份一次全提，每步「提取 + 立刻跑对应测试」；
原来那五个 `update-mapdata / -weapondata / -chrprops / -shopdata / -shopicons.bat`
**已删除**（用户：分得太多太乱，分不清哪个是哪个）。
③ 补的闸：三个 `Copy-*` 各加一道 **format 校验**（新函数 `Get-ServerFormatVersion`
从 `server\<模块>.py` 读 `FORMAT = N` 比对产物），对不上就中止打包并指名跑哪个 bat
—— 因为读取侧 format 不匹配时是**静默返回空表**的。
整脚本实跑过：五步全绿、退出码 0，且**五份产物重新提取后与仓库里逐字节一致**。
上一轮（2026-09-08，会话 15）：**收尾轮，没动玩法代码**。
① 用户回话：**A ~ L 区全部实机验过了，只剩「凌晨 4 点自动备份」那一条**
（H 区 51）—— 待验证区已按此收掉；★ 那一条其实**不用等到 4 点**，
改个时刻保存就能当场验（§45）。
② **拿真的云服存档（71 个号）跑完迁移演练**：一个字段没丢、连开三次不写盘、
四份配置和模板逐字节一致 —— **§44**，上线这一件可以放心了。
③ README 补上「商店 · 合成 · 装备」和「管理员网页」两整节，删掉「商店还没做」。
④ 三处收尾：`.gitignore` 挡住从云上取下来的生产存档（**D52**，71 个真人明文口令
差点能被 `git add -A` 提进开源仓库）、`test_update`（12 项）补进 `run_tests.MODULES`、
`re/packet_api.md` §3.8 的「`0x0505` 一次都没发过」改成实测。
两套运行时全量测试 **2897 项都绿**（3.14 / 3.8）。**没打包**（用户说先不打）。
上一轮（2026-09-07 深夜，会话 14，**D51**）：**商城角色改成商店买**。
服务端「角色全开」开关（`character_unlock_all`，用户以为在 hook 里，其实在存档层，§43）
删了；角色 = 仓库里的角色卡，11 张卡上架「人物 → 佣兵」，统一 1000 金币、不限等级，
模板（`shopdefaults`）和 `server/data/shop.json` 一起改了（484 → 495 件，旧文件留
`.bak-20260907-220642`）。开服时旧存档的两个键被迁移掉，**所有号只剩 3 个基础角色**。
顺带：角色卡（和材料）再也进不了 `equipped`（`resolve_equipped` 丢 `part_flag == 0`）、
管理页给角色卡不画数量框、开服时货架上没角色卡会喊一声。
两套运行时全量测试 **2885 项都绿**（3.14 / 3.8）。服务端已重启（只起了服务端，
客户端用 `start.bat` 拉），**等实机验（L 区）**。
上一轮（会话 13 收尾，**D50**）：四份运营数据的**模板 = 现在的数据**
—— 设计表搬进 `server/shopdefaults.py`，`shopcfg.default_*()` 只是它的切片，
`tools/gen_listing.py` 变成壳；四份 json 和模板逐字节一致（物品库同步了 108 件复制品的等级）。
新下载发布包的人第一次开服拿到的就是这份。两套运行时全量测试 **2869 项都绿**（3.14 / 3.8）。
同一会话前半（**D49**）：掉落和配方按用户的**原版口径**
整体重做了一版（`tools/gen_listing.py` 改表后 `--apply`，只写了 recipe / drops，
旧文件留 `.bak-20260907-175603`）：每关每档正好掉两样、全 100% ——
简单 = 矿料 + 珠子，普通 = 本关低档特殊材料 + 矿料，困难 = 高档 + 矿料，
**困难不掉普通那档**；配方按风格配对、一条只用一个难度档（表在 D49）。
掉落 46 条 / 合成 122 条，物品库和商店没动。服务端热重载了（`shopcfg.drops()` 46 条无警告）。
**等实机验（K 区）**；用户认可数值后再搬进 `shopcfg` 模板（七句话 -3）。
上一轮（会话 12，D48）：管理页五处交互 + 存档转移助手核对，
全是用户点的题：① 玩家仓库页改成「只有列表滚」的壳、换页栏挪到列表上面；
② 换页栏上下间距改成 14 / 12；③ 筛选条上的「第 m / n 页」删掉（弹窗标题也只写件数）；
④ 材料掉落：模式选「对战」时关卡**和难度**自动清成不限并锁住；
⑤ 掉落页多了「关卡 / 难度」两个筛选（各含「不限」一档）。
存档转移助手**本来就能读写新字段**（导出 / 导入都按 `NEW_ACCOUNT_DEFAULTS` 走），
补了 5 条用例钉住，加了导入后向在线玩家推送、注册页说明。两套运行时全量测试
**2867 项都绿**（3.14 / 3.8）。服务端已重启，**页面上的东西等实机验（J 区）**。
上一轮（会话 11，D46 / §40）：管理页新增**「数据备份」**
标签页（★系统管理员，排在「管理员账号」前）：每天到点自动备份 `server/data/*.json`
到 `server/data/backups/<时刻-类型>/`（一份一个目录 + manifest）、手动备份带说明、
列表每页 10 行、「回滚到此版本」/「删除备份」两个红金钮各自弹自绘确认框、
回滚前自动留一份、按保留天数清理；设置三键写回 `server.config` 即刻生效，
老文件启动时自动补默认值（开 / 04:00 / 7 天）。新模块 `server/databackup.py`，
四把配置锁搬进 `shopcfg.write_lock`。两套运行时全量测试 **2856 项都绿**
（3.14 / 3.8）。服务端已重启，**页面上的东西等实机验（H 区）**。
上一轮（会话 10 第三轮，D44b / §39）：套装标签的 id 是 `3`
不是 `0x10006`（实机日志对出来的，商店「装备 → 套装」这下有东西了）；
所有套装进商店；合成「新商品」只列装备（它是装备下的子标签）；戒指归「其他」；
掉落页按 模式 → 关卡 → 难度 → 材料 排；同图同属性的复制品去重 108 件；
金 / 银骑士甲（每角色 3 件）改成合成，银 ← 龙之血、金 ← 龙之精华。
现在：商店 484 件 / 合成 122 条 / 掉落 62 条。服务端已重启。
第二轮：（会话 10 第二轮，用户看完第一稿点的题：
① 管理页配方材料格 / 掉落行的名字读错了来源（改走物品库）；
② **游戏里没有第 4 档「极限」**（中国区版本跳过了它，§38 / D17a）——
下拉、校验器、默认掉落表都只剩三档；③ 掉率改成难度越高只增不减（脚本里
加了断言）；④ **称号和卡片先不上架**（原版按成就给，成就统计还没做，D44a）。
现在：商店 598 件 / 合成 116 条 / 掉落 62 条。服务端已重启。）
第一轮：（会话 10，★ **全量上架**：商店 598 件 / 合成 136 条 /
掉落 97 条按 D44 重新生成进了 `server/data/`（旧文件留了 `.bak-20260906-225552`），
物品名改官方译法（D45），角色卡「真」→「京」**连模板一起改了**；
`shop.category_of` 补了喷漆 / 突击技 / 宠物 / 称号的标签归属，`COMPOSITION_CATEGORIES`
加了称号 `0x70001`（§36）。**全部等实机**，见待验证 G 区。）
以下是会话 09 的记录：（会话 09，全是管理页交互，用户点的题：换页不动滚动条 +
只留上面那条换页栏（D37a）；**整页改成撑满窗口的壳、只有物品列表滚**，
按钮行并进筛选条，保存回执变右上角浮条（D39）；玩家一键设为运营（D40）；
弹窗加角色筛选、合成配方加类别筛选；「口令」全改「密码」；三页加「↻ 刷新」
且一刷四份（D41）；弹窗底下的详情侧栏删掉（D42）；★ **推翻 D23** ——
修改仓库不再挡商店物品（D23a）。见待验证 F 区。
以下是会话 08 的记录：管理页四件事，用户点的题：
① **「天数」整条砍掉**（D35，连磁盘上的残留一起删）；
② **超上限从「不画了」改成翻页**（D37，物品库 + 选择物品弹窗，上下各一条换页栏）；
③ **保存改成三方合并 + 冲突检测**（D36，新模块 `cfgmerge`，含商店 ⇄ 合成互斥
的跨人冲突）；④ **179 件韩文名全翻成中文**（改的是生成模板 `shopcfg.NAME_ZH`，
`items.json` 重新生成过）。附带 D38：确认框自己画，不用 `window.confirm`。
全量测试 2782 项绿，管理页四件事**都在真浏览器上走通了**）

---

## 现在在哪

| 里程碑 | 状态 |
|---|---|
| M0 进度管理体系 | ✅ **完成** |
| M1 数据基线 | ✅ **完成** |
| M2 存档扩展 | ✅ **完成** |
| M3 ★ spike：装备生效 | ✅ **完成，实机验证过**（§16）|
| M4 ★ spike：商店 UI 协议 | ✅ **完成，实机验证过**（§21）|
| M5 商店购买 + 仓库装备 | ✅ **完成，实机验证过** |
| M6 材料掉落 + 结算显示 | ✅ **完成，实机验证过** |
| M7 合成 | ✅ **完成，实机验证过**（合成成功 / 扣材料金币 / 错误文案，§33）|
| M8 管理员网页 | ✅ **完成**（含玩家仓库页，D23）|
| M9 测试 · 兼容 · 打包 · 上线 | 🟡 只差**打包实跑**（用户 2026-09-08 说先不打）。迁移那半**拿真的云服存档跑完了**，§44 |

**一句话现状**：商店段和合成段**所有**上下行现在都有语义，没有 ❓ 剩下。
服务端会回：货架 `0x0500`、物品定义 `0x0501`、购买结果 `0x0502`、
**配方列表 `0x0505`**、**合成结果 `0x0506`**、礼物清单 `0x0508` / 礼物动作结果 `0x050a` /
到货通知 `0x0507`（§75，等实机），持有物清单 `0x0601`、穿着的装备 `0x0604`，
并处理穿 `0x0702` / 脱 `0x0703`、礼物动作 `0x0609`。

## ✅ 已经实机落锤的

**货架整条链路**：图标 + 中文名 + 价格 +「金币」+「购买」按钮全画出来了，
翻页、角色切换、武器 1/2/3 子分类过滤都对（§21 / §22）。

**物品定义 `0x0501` 那一整套（§28 / §29）**：左侧人物模型带装备、
仓库列出装备 + 材料、仓库里手动穿脱生效。

**买东西（§30）**：跨角色买、买完选「直接装备」真的穿上、失败框写具体原因。

**M5 复验（2026-09-05 晚）**：重复卸下、划线原价只有一个数、
提示框下半的说明和换行 —— 三条都对了。

**等级这件事（§32 / D22，2026-09-05 晚全部通过）**：真实等级显示对，
对战频道 / 5-6 人房 / 生存 / 新手图 / 天梯 / 关卡 / 全地图七道门全放行，
商店按真实等级卖。

**管理页玩家仓库页（D23）**：找人、改等级金币材料、开一件商店买不到的装备
—— 在线时不用重登，游戏里当场跟着变。

**M7 合成（2026-09-05 晚）**：合成成功、材料和金币照扣、
**材料槽的「持有量」当场就变**（⇒ D28 那一发 `0x0505` 重放有效）、
「材料不足」/「金币不足」的失败框文案都对。

**★ 2026-09-08 用户一次性确认：A ~ L 区全过。** 全量上架的数值和译名、
D49 的掉落 + 配方、D51 的角色卡、管理页七个标签页的版式与交互、
存档转移助手 —— **都在实机上看过了，没问题**。
⇒ 商店 / 合成 / 掉落 / 装备这条链**整条落锤**，只剩自动备份那一条（见下）。

## ⏳ 待用户验证

**二十二组**（A ~ L 区已全过，见上一节；逐条的验收清单在 `sessions/` 里）。

**Z-88. ★★ 管理页「下载日志」（D131 / §128）—— 重启服务端后点一次**

agent 已验：35 条新测试（数据层 25 + HTTP 层 10，含「中途断开」「HTTP/1.0」），两套运行时
3779 项（3.8 那两条红与本次无关）；独立起的实验服务器上 curl 实测流式下载、zip 校验、
断开收场都对；浏览器面板里看过弹窗。**没验的是真服务端 + 真浏览器下载栏那一遍。**
★ 服务端**还没重启**（用户当时正在房间里）—— `stop.bat` → `start.bat` 之后才有这两个接口。

| 第几步 | 怎么做 | 期望 |
|---|---|---|
| 1 | `stop.bat` → `start.bat`，看 `logs/server.out` | 启动日志多一行 `日志下载 管理页「数据管理」→「下载日志」…` |
| 2 | 浏览器登管理页（系统管理员）→ 标签「数据管理」 | 页名 / 标签都叫「数据管理」；「保存设置」右边多一颗深棕「⇩ 下载日志」 |
| 3 | 点「⇩ 下载日志」 | 弹窗：服务端日志（全部 / 最近 12 小时，各带文件数和大小）+ 客户端崩溃包（本机现在是「还没有收到过崩溃包」，钮是灰的） |
| 4 | 点「下载最近 12 小时」 | 浏览器下载栏立刻出现 `logs_server_12h_<时刻>.zip`，只显示已下载多少（没有总量）；完成后能解开，里面 `logs/server.out` 是当前那份 + `MANIFEST.txt` |
| 5 | 点「下载全量」，下到一半在下载栏里**取消** | 游戏里的对局不卡；`logs/online.log` 多一行 `下载服务端日志（全量）中断（…）`；`logs/server.err` 不长 |
| 6 | （云上）同样下一次全量 | 看一眼下载速率和服务端 CPU：应当是「网速多快就压多快」，CPU 只有几个百分点 |

**Z-87. ★★★ 任务记录框（§126 / §127）—— 打一局就验完大半**

agent 已验：53 条新测试（存储层 27 + 线格式 11 + 广播 17 + 入账 15 里的新增部分），
两套运行时 **3730 项全绿**，七道拦网逐条**故意打断**验过真的拦得住，
服务端重启后正常起来（`logs/server.err` 无新增）。**没验的是实机那一遍** ——
协议全是静态逆出来的，只有真客户端能证伪。
★ 服务端**已经重启过了**（2026-09-17 11:33，`start-debug.bat`）。

| 第几步 | 怎么做 | 期望 |
|---|---|---|
| 1 | 登录 → 建一个**任务**房（默认「神秘岛 / 简单」） | ★★ 右侧「**个人记录**」从「正在查询资料」变成「**无历史战绩**」——<br>**这一条就是「修好了」最硬的判据**。「全体记录」按原版仍显示「正在查询资料」（全服还没人打过，D129 第 1 条，**不是 bug**） |
| 2 | 看 `logs/server.out` | 有 `← 回 0x0310 任务记录（房主来问） 关卡 3 难度 1：座位 [...]，全体榜 0 条，发给 1 人` |
| 3 | 按左右箭头换**难度**、换**任务** | 两个框跟着变（约 2 秒延迟，客户端自己的节流）；日志里每换一次多一发 `0x0310` |
| 4 | 真打通一关 | ★ 结算界面播「**您的新记录为 MM分SS秒**」（第一次通关是破全服记录，播的是「更新为」那一条）；回到房间两个框都显示 `MM:SS` |
| 5 | 同一张图同难度**再打一局、故意打慢** | **不播报**、榜上数字**不变** |
| 6 | 再打一局**更快的** | 播报出现、榜跟着变 |
| 7 | 拉第二个人进房（或开第二个客户端） | ★★ **客人那两个框也要有数据** —— 客人自己从不发请求，全靠服务端推（§126） |
| 8 | 换一张**没打过**的图 | 个人记录回到「无历史战绩」（验按图分桶） |

⚠ 要是第 4 步播报**没出现**但两个框是对的：播报函数 `0x5527a9` 在镜像里没有直接
`call`、也没有 vftable 引用，**它什么时候响没查到底**（§127 末尾）。
那种情况下记录功能本身是好的，播报单独再查。

⚠ 第 1 步要是**仍然**显示「正在查询资料」：先看日志有没有那一行 `← 回 0x0310`。
有 ⇒ 客户端把包丢了（回显校验 `0x46a176` 没过）；没有 ⇒ 没走到广播。

**Z-86. ★★★ 成就防刷（D127）—— 连着打四局就验完了**

agent 已验：58 条单元 / 端到端测试（`server/test_cardfair.py`，十四道拦网逐条打断验过）、
两套运行时全量绿、真服务端起得来。**没验的是实机那一遍。**
⚠ **最后一版改完没重启服务端**（当时用户开着客户端）—— 验之前先
`stop.bat` → `start.bat`。要看逐包日志用 `start-debug.bat`
（`launch.ps1` 的开关叫 **`-DebugLog`**，不是 `-Verbose`）。

| 第几局 | 怎么做 | 期望 |
|---|---|---|
| 1 | 建房 `/a` 加一个 bot，直接打，打死它 | 结算界面**有**卡片（够条件的话）；`logs/server.out` 里 `成就判定: 本局计入成就 —— 1 个 bot 全程自由，难度 3`；★ 结算界面**不该**有任何提示 |
| 2 | 回房间 → `/hold` → 开一局打死它 | 敲 `/hold` **当场**看到 `⚠ N 号位 bot 被 /hold 定住了，本局不计成就。`；★ **开局又看到一行**；结算界面**没有**卡片、★ 而且能看到 `⚠ 本局开启过 bot 限制，不结算成就。`；日志写 `本局不计成就 —— 开局时 …` |
| 3 | 局中敲 `/hold` 解除，接着打完 | ★ **当场**看到 `⚠ bot 限制已解除，下一局起计成就（本局不生效）。` —— **要带括号**；结算界面仍然**没有**卡片、仍然有那行结算提示 |
| 4 | 回房间（**还在房间里**）敲 `/hold` 两下 | ★ 解除那一句**不带括号**：`⚠ bot 限制已解除，下一局起计成就。` |
| 5 | 再开一局（bot 已自由） | 卡片**回来**；日志 `本局计入成就`；★ 结算界面**不该**有提示 |

⏳ ★★ **两处「看不看得见」是本轮仅有的未知**，一并在这几局里确认：
① 第 2 局**开局那一行**（进 stage 7 那一刻发的）；
② 第 2/3 局**结算界面那一行**（排在结算三连发之后发的）。
聊天一行只活 **10 秒**（`0x42afeb`），所以两处都已经尽量贴着「界面弹出来」发了。
**哪一处看不见告诉我**：①的退路是挪到「看完结算回房间」那一刻；
②的退路是跟着 `0x0403`（玩家按下「确认」离开结算界面）之前再补一次。

★ 顺带确认第 2、3 局的**经验 / 金币 / 材料照发**，一分没少（受限只影响成就）。

★★ **顺手验一下「限制已解除」那一句的可观测性**（它是用户第二轮点名加的）：
房间里敲 `/d 1` 再敲 `/d 2`，应该看到「已解除」那一行。
**故意敲错一个**（比如 `/b 2`）⇒ **一个字都不该有** —— 这正是分辨
「命令生效了没有」的办法。⚠ 不认识的 `/xxx` 被当普通聊天原样广播，
这是**故意的**（不然正常聊天里以 `/` 开头的话都会被吞），不是 bug。

⚠ **唯一没底的一条**：第 2 局「开局那一行提示」落在**进 stage 7 那一刻**
（全员加载完、`0x0402` 已经广播出去之后）。战斗中的聊天肯定看得见（`/hold`
的回执就是走这条），但**恰好在切进关卡那一瞬发的那一发**会不会被吞掉没有先例。
★ 而且聊天行只活 **10 秒**（`0x42afeb`：8 秒开始淡出、10 秒不再画）——
所以不能往前挪到读图之前。**看不见的话告诉我**，退路是把这一行挪到
`leave_game_result()`（看完结算切回房间那一刻，一定在房间阶段、一定看得见）。

**Z-85. ★★ 红心回血修好了（§119）—— 打一局捡个心**

agent 已验：11 条单元测试（`test_battle.HeartEffectTests`）、两套运行时全量绿。
**没验的是实机那一遍。** ⚠ **要先 `stop.bat` 再 `start.bat`**（铁律 7）。

| # | 怎么做 | 期望 |
|---|---|---|
| 1 | 合一个 `[红心达人] 560006` 穿上，**对战**里捡「心」（15% 概率触发，多捡几个）| 同队每个活着的人**都掉血条往回涨**；`logs/server.out` 里有一行「★ 座位 x 让座位 y 回 N 点血」|
| 2 | 买青鸟宠物 `220001` 穿上，捡「心」| 同上，物件号是 `10316`、只回自己 |
| 3 | ★ **捡心的人自己也要回** —— 以前连他自己都没有 | 自己的血条也涨 |
| 4 | ★ 队伍**超过 2 人**时看回了多少 | ⚠ 可能**成倍**（原版客户端自己那层循环，§119 怪癖 1）。真的多回了告诉我，要不要压平由你定 |

**Z-84b. ★★★ 第三轮补译（§121 / D126）—— exe 的运行时消息 81 条**

前两轮漏的那一类：带 `%s` / `%d` 的运行时消息在 **exe 字符串表**里，资源文件里一个字都没有。
这次按 §121 的三来源查法扫全了，**155 条漏译清单每条都有交代**（翻 81 / 写明不翻 28 /
3 个整段跳过区间）。要看的：

| # | 怎么做 | 期望 |
|---|---|---|
| 1 | 在房间里换角色 | 聊天框那句是「**XX 选择了 YY 角色。**」，不再是韩文（用户 2026-09-14 报的就是它） |
| 2 | 进图的加载画面 | 那句引导是中文（格斗模式课程 / 网吧特权那两句） |
| 3 | 大厅频道栏 | 公告 / 活动频道 / **竞技场** / 格斗模式 / 广场 / 全部玩家 |
| 4 | 竞技场排队 | 「正在搜索可以匹配的队伍」「组队匹配完成，即将自动开始」「全队都准备完毕才能搜索竞技场对手」 |
| 5 | 仓库里修理一件装备 | 修理 / 可修理 / 无限制 / 「这个道具还能修理 N 次」 |
| 6 | 好友列表 | (在线) (被屏蔽) (等待确认) |
| 7 | 打一局到爆炸倒计时 | 「距离爆炸还有 N 分钟。」 |
| 8 | ⚠ **改表情**（哭/笑/无表情） | 会拼成「X **→** Y」。★ **全表唯一一处我猜了句子结构**：原版是韩语后置助词「A에서 B으로」，中文没有对应语序，我把 `에서` 换成箭头、`으로` 留空。**看着别扭就说，删掉那两行即可** |

★ **明确没翻、也不该翻的**（别再当成漏了）：`Images/…png` / `*.ogg` 等资源路径、
`%s_퀘스트_레벨` 这种 `Promotion.ini` 节名模板（翻了晋级任务全找不到）、
`$불프로그-레이어가리기` 这类关卡脚本命令名、`돋움체`（字体名）、
截图目录 `%s\빅샷\스크린샷`、调试串、以及 36 条韩文字母拼写表。理由逐条写在 §121。

---

**Z-84. ★★★ 补上的中文翻译（§99 / §117 / D124）—— 五个界面各看一眼**

agent 已验：`verify-tree` 16266 文件差异 0；`git status` 只动白名单；`shop_items.json`
只变 3 个 promotion 名字；两套运行时全量测试绿。**没验的全是「眼睛看」那部分。**
`start.bat` 起游戏后逐条看：

| # | 怎么做 | 期望 |
|---|---|---|
| 1 | ★★ 合成一个称号穿上身（`[完美胜利者]` 最好拿） | **头顶那行是中文**、自带方括号、**没被截断**。⚠ 最长的是「华尔兹加农炮高手」8 个字，`[Character+0x3bc]` 那个缓冲多大没人量过（§99），要是被切掉尾巴告诉我，我把译名缩短 |
| 2 | 大厅打开**晋级任务**列表 | 「可进行 / 已完成的晋级任务 / 当前挑战中的晋级任务 / 预定 / 下一个晋级任务 / 再下一个晋级任务 / < 任务信息 > / 任务信息说明」全是中文。★ 「승급」原版没给过中文译法，**「晋级任务」是我起的**，想换叫法直接说 |
| 3 | 商城 / 选角 | 派克、希莉亚 玛斯、拉斯 钛金版、爱琳、暗影泰尔、艾丽亚丝白色版 都是中文 |
| 4 | 任务 / 对战选图 | 主题名「亡者沙漠 / 扎米洛秘密基地 / 天空 / 庆典」是中文；5 张任务图的**剧情简介**是中文（换行位置照原版，别顶出框） |
| 5 | 打一局看屏幕上的**战斗提示** | 「点击鼠标左键开火」「按 W 键跳跃」这一类 15 条是中文 |
| 6 | 频道选择 | 「选择频道」「广场频道」 |
| 7 | 合成面板看 560007 / 560008 的加成 | 是「团队5% 降低」而不是「Team 5% Down」。⚠ 提示框绿字被 D65 关掉了，**要在合成面板看**；或者临时设 `BSHOOK_KEEP_CLIENT_BONUS_TEXT=1` |
| 8 | 随便触发一次「获得金币」提示 | 后面**不再拖着** `/귓 =/to ` 那串尾巴（`Chinese.ini:1078` 的粘行） |

⚠ 只要有一处**反而变差**（乱码 / 方块 / 顶出框 / 闪退），把截图发我，那条单独回退就行 ——
58 条 key 都在 `Chinese.ini` 末尾 `# Popshot Reborn 补译` 那个块里，删哪条都互不影响。

**Z-83. ★★★ 客户端资源包工作流（§114 / §115 / D123）—— 打一局 + 改一句文案**

agent 已验：登录界面起来、151 卷全部经重定向打开、60 秒无崩溃；升级演练里旧 `Pack` 被自动删掉。
**没验的是登录之后的一切**（大厅文字、商店图标、进图的地图 / 模型 / 特效 / 音效都从新卷读）。

1. `start.bat` → 登录 → 大厅 → 打开商店（看图标）→ 建房进图打一局（看地图、模型、特效、开枪音效）→ 结算。
   期望：和以前一模一样。任何一处黑图 / 缺贴图 / 闪退 ⇒ 把 `logs\bshook_*.log` 里带 `PACK` 的行
   和 `game_patched\Dump\LastCrashReport.txt` 发我。
2. 改一句文案：用能保住 UTF-16 的编辑器打开 `game_patched\Pack_develop\Data\Chinese.ini`，
   随便改一条大厅里看得见的翻译 → 双击 `tools\build-pack.bat`（应只重写 `Data~.pkn` 一卷，
   接着自动跑 update-gamedata 五步）→ `start.bat` → 大厅里看到新文案。
   再跑一次 `build-pack.bat` 应打「资源没变 / 都跳过」。
3. （可选）对照：`set BSHOOK_KEEP_PACK_DIR=1` 再起 —— 现在 `Pack\` 已经删了，这条只在你把旧卷
   拷回来时才有意义。

当前状态：⏳ 等实机。

**Z-82. ★ 称号卡片掉落页重构（D110 / §106）—— 管理页点一遍**

服务端已经重启过、`cards.json` 已按新形状重生。浏览器**强刷** `/admin`
→「称号卡片掉落」。⚠ **GM 登录要自己输密码**（agent 不做这件事）。

| # | 操作 | 期望 |
|---|---|---|
| 1 | 看整页 | 17 行，每行三段小标题「算哪一局 / 要达成什么 / 给多少」；**对战卡上没有关卡 / 难度那两格**，非武器指标上**没有武器那一格** |
| 2 | `完美卡片` 那一行 | 读作「对战 · 死亡次数 · 等于 · 0」＋「必须胜利 / 通关」**勾着** |
| 3 | ★ 把「统计范围」切到**玩家累计** | 「比较」那一格**整个消失**；指标下拉里多出「对局数 / 胜利·通关次数」（一局内 14 条 / 累计 16 条）|
| 4 | ★ 比较切成「等于」，数值填 **0**，保存 | **能存**。再切回「大于等于」保存 ⇒ 服务端拒收，错误串能定位到那一行（红框闪一下）|
| 5 | ★ 比较切成「小于等于」，再想选武器 | 武器那一格**不出现**；硬在磁盘上写一个 ⇒ 保存时报「对每一个没用过这把枪的人都成立」|
| 6 | 「为 0」但**不勾**「必须胜利 / 通关」 | 行末出一句**红字提醒**（只提醒，照样存得下去）|
| 7 | 鼠标停在「统计指标」下拉的某一项上 | 出 tooltip（比如「暴击」那句 15% 概率的解释）—— 这是第一次画出来 |
| 8 | 工具条 | 多了一个「全部武器」筛选 |
| 9 | 鼠标停在卡片图标上 | 浮窗第一行「获得条件：对战 · 一局内死亡次数 为 0 · 且获胜 / 通关 → …」|

**★★ 同一轮在游戏里验一条：出厂那两张「为 0」的卡还发不发得出来**

`完美卡片`（零死亡获胜）和 `蹭分卡片`（零击杀获胜）这一版是**拆开重拼**的
（条件 + 「必须胜利」开关）。打一局对战：

| # | 游戏里做什么 | 期望 |
|---|---|---|
| 1 | 对战赢一局，**一次都没死** | 结算界面 `CARD` 那一栏出 `完美卡片` ×1 |
| 2 | ★ 对战**输**一局，也一次没死 | **不给** —— 「赢了」那一半现在靠开关承担，这一条就是验它没漏 |
| 3 | 闯关通一关（会杀怪）| **不给** `蹭分卡片` —— 闯关杀怪现在也算 `kills`，旧口径下这里会误发 |
| 4 | 看 `logs/server.out` 的「本局战绩 座位N」 | 武器那几项写成「左轮手枪（泰尔）击杀数 N」，**不是**两行一样的「击杀数 N」|

**Z-81. ★ 卡片提示框改版（D108 / §103）—— 管理页点两下就知道**

`stop.bat` → `start.bat`（服务端改了，铁律 7），浏览器**强刷**一次 `/admin`：

| # | 操作 | 期望 |
|---|---|---|
| 1 | 「合成配方」页，鼠标停在任意一张卡片上 | 行首是「**可合成：**[称号名]　材料 + 金币」，不再是光秃秃的 `➜` |
| 2 | ★ 停在**两个称号都用得上**的那张（现在是 `完美卡片`）| 「可合成：」**两行**，下面效果也**两行**，每行开头写着是哪个称号的 |
| 3 | ★ 当场把某条配方的材料 / 花费改掉，**按保存**，再停回那张卡上 | 浮窗里那两段**立刻**是新的（以前要按一次「↻ 刷新」才变，而且不报错）|
| 4 | 只有一个称号用得上的卡片（如 `幸运卡片`）| 效果那行**也**写称号名 |
| 5 | ★ 停在**卡片 / 材料 / 消耗品**上 | **没有**「不限角色　不限等级」那一行了（D108④）|
| 6 | ★ 停在**称号 / 铠甲 / 武器**上 | 那一行还在；去「物品库」改一个称号的等级、保存，再停回去 → 当场写「需 N 级」|

⚠ **游戏里要重新登录才看得到新说明** —— 客户端把物品定义缓存住了
（`0x0601` 发过就记成已请求，packet_api §3.9），这是原版行为，不是这次改坏的。
在线玩家要不要也实时刷新，见 §103 末尾那个问号。

**★★ 同一轮还要在游戏里验一条（D109 / §104）：换行改成了字面 `\n`**

用户实机看到卡片提示框第 1 段**第 3 行起字压字**，根因是真 U+000A 排版器不认。
`stop.bat` → `start.bat`，**重新登录**（不重登看到的还是旧说明），然后：

| # | 游戏里做什么 | 期望看到 |
|---|---|---|
| 1 | 仓库 →「收集品 → 卡片」，停在**两个称号都用得上**的那张（`完美卡片`）| 上面一块 4 行、下面一块 2 行，**行行分开，没有叠字** |
| 2 | ★ 行尾行首 | **没有多出来的 `\` 或 `n`**（这一条是这次改动唯一的风险点）|
| 3 | ★★ 每个换行**前一个字还在不在** | 第 1 行末尾要写「→ 获得 1 **张**」——「张」回来了就对了（上一版被排版器吃掉了，§104）|
| 4 | 停在一把**武器**上 | 数值那两行照旧，没被这次改动带坏 |
| 5 | 商店货架、礼物盒里各停一个 | 同上（两个下发点都换了）|

★ 第 2 条要是翻车（真的画出 `\n` 两个字），退路是**整段不发换行**、
让排版器按宽度自己折 —— 说一声我改。

**★ 这两条已经查清是 `.ui` 写死的，服务端改不动（§105），不用再试：**

* 商店「价格」/ 仓库「拥有数量」往下错一点 —— `Price0/1/2Txt` `PosY=134`、
  `LabelItemCount` `PosY=133`，和背景贴图的凹槽对不齐；
* 说明第 2 段前面那道空白 —— 第 1 段的格子到 `y=253` 才完，
  第 2 段固定从 `y=274` 起，中间还夹着已经清空的绿字格子。

  ☐ **要不要把两段并成一段**（缝没了，但 88 px ≈ 7 行封顶，
  一卡两称号会被切）—— **等用户拍板**。

**Z-80. ★★ 称号卡片系统（D98 ~ D106 / §98 ~ §102）—— 打一局，看结算界面那一栏**

服务端和管理页那一半 agent 自己走完了（3433 项两套运行时全绿；真服务端起过、
开服的**一次性升级**把 17 条称号配方补进 `recipe.json` 而原有 122 条逐字段没动、
第二次启动不重跑也不把删掉的补回来；浏览器里 17 行 / 筛选 / 联动锁 /
「关着的」样式 / 提示框三段内容都点过）。
**没走的只有一件：`0x041c` 的槽 1 一次都没实机跑过** ——
`[GameContext + 0xec + 座位*20]` 那一格是静态推出来的（§3 / §98）。

先 `stop.bat` → `start.bat`（服务端改了，铁律 7），然后：

| # | 游戏里做什么 | 期望看到 |
|---|---|---|
| 1 | 打一局**对战**，故意多按几次格挡（默认规则：格挡 ≥ 30 且获胜给 1 张幸运卡片）| 结算界面「合成材料」**下面**那一栏出现卡片图标 + 数量 |
| 2 | ★ 顺便看那一栏的**标题** | 写的是英文 **`CARD`** —— exe 里写死的（§98），服务端改不了，**不是 bug** |
| 3 | ★ 一栏画得下**几个**图标 | 数据结构没有上限（§98），但 UI 的格子位置可能是固定几个 —— 顺手看一眼有几格 |
| 4 | 回大厅开仓库，找「收集品 → 卡片」 | 卡片真的在里面，数量对得上 |
| 5 | ★ 鼠标停在那张卡片上 | 提示框下半两块：上面「获得条件：… / 可合成：[幸运幸存者]　幸运卡片×500 + 100000 金币」，下面「[幸运幸存者]　受到伤害时 50% 概率完全免疫」。**没有溢出、没有裸竖线** |
| 5b | ★ 穿一件加攻击的装备打一局 | 日志里那行战绩有「暴击次数 N」（攻击加成那 15% 掷点，§102）。**身上没有加攻击的装备时它恒为 0，那不是坏了** |
| 6 | 打开合成界面点「**称号**」标签 | 17 条配方（3 页），点开一条看得到材料和金币 |
| 7 | 攒够卡片合一个称号出来、穿上身 | 属性真的变（`[完美胜利者]` 攻 +5 / 生命 −10% 最好看） |
| 8 | ★ **头顶显示的称号** | ~~仍然是韩文~~ ⇒ **2026-09-14 已改成中文**（§99 / D124）。验它请看 **Z-84 第 1 条** |

★★ **最要紧的一条是 `guards`（格挡次数）的语义**：`rpGuard` 报的是
「开 / 关」状态（§101），但**「开」到底对应玩家的哪个操作没实机核过**，
而默认的幸运卡片条件压在它上面。第 1 步打完看服务端日志里那行
`本局战绩 座位N: … 格挡次数 X` —— 跟手感对不上就告诉我，
那个指标要么换判据要么整个摘掉。

★ 出问题随时能停：管理页「称号卡片掉落」把 17 个「能获得」全关掉 =
整个功能停掉，**不用重启服务端、也不用发新客户端**。

**Z-79. ★ 管理页「玩家现在在哪」（D97 / §97）—— 要真的开着游戏走一圈**

浏览器那一层 agent 自己走完了（六档位置的画法、五档筛选的接口、提升管理员
全流程、密码两遍、窄屏塌一栏都点过）。**没走的只有一件：真人真的在游戏里
走动时，那一列跟不跟得上。** 服务端侧有 10 条用例钉着，但判据是从**实测
日志**里推出来的（§97），只有真客户端能证伪。

先 `stop.bat` → `start.bat`（服务端改了，铁律 7），再开客户端登一个号，
**管理页开在旁边，玩家仓库页每走一步点一下「↻ 刷新」**：

| # | 游戏里做什么 | 那一列期望写 |
|---|---|---|
| 1 | 登录进大厅，站着不动 | `● 大厅` |
| 2 | 点开**商店**（或合成、或仓库 —— 三个都试试）| `● 商店界面`（三个都显示这一句，D97a 用户拍板合并）|
| 3 | ★ **关掉商店回大厅**，等一下再刷新 | 回到 `● 大厅`。**最迟 3 秒**（客户端 3 秒发一发 `0x0200`）—— 要是一直卡在「商店界面」，§97 那条判据就错了 |
| 4 | 建一个**闯关**房，坐着不开局 | `● 待机房间·任务` |
| 5 | 建一个**对战**房，坐着不开局 | `● 待机房间·对战` |
| 6 | 在闯关房里**开局进图** | `● 游戏中·任务` |
| 7 | 打完看完结算**回房间** | 变回 `● 待机房间·任务`（★ 结算界面会发 `0x0601`，要是这一步跳成「商店界面」就是那个坑没堵住）|
| 8 | **退出房间**回大厅 | 立刻 `● 大厅`（不等那 3 秒）|
| 9 | 关掉客户端 | `○ 不在线`（灰空心点）|
| 10 | 筛选下拉逐档点：全部 / 不在线 / 在线 / 在待机 / 在游戏中 | 「在待机」= 上面 1/2/4/5 那几种都算；「在游戏中」= 只有 6。翻页时页数跟着筛完的算 |

**Z-78. ★ 管理页显示同名玩家账号的昵称（D96）—— 一个人点两分钟**

浏览器那一层 agent 自己走完了（临时存档 + 临时 data 目录起了一份 `/admin`，
三档身份、管理员账号页、发奖记录都点过）。剩下的只是**在真的 accounts.json
上**看一眼 —— 线上那些管理员到底有没有同名游戏账号，只有真数据说得准。

先 `stop.bat` → `start.bat`（服务端改了，铁律 7），然后：

| # | 操作 | 期望 |
|---|---|---|
| 1 | 用一个**有同名游戏账号**的管理员登 `http://127.0.0.1:27810/admin` | 顶栏写 `已登录：<昵称>（<账号名>，系统管理员）` |
| 2 | 用一个**没有**同名游戏账号的管理员登 | 顶栏还是原来那句 `已登录：<账号名>（…）`，**不多一对空括号** |
| 3 | 进「管理员账号」页 | 多一列「昵称」：有同名号的写昵称，没有的写 `-`；表格不横向滚 |
| 4 | 在注册页把自己的昵称改掉，回管理页**刷新** | 顶栏和那一列当场跟着变（不用重新登录）|
| 5 | 「玩家仓库」→「批量发送奖励」发一次，再点「发送记录」 | 左栏和右边详细里的「发送者」都写 `<昵称>（<账号名>）` |
| 6 | ★ 接着第 4 步改完昵称，再翻**改名之前**发的那几条记录 | 发送者写的是**新**昵称（昵称不存进 `gift_history.json`，每次现查）；顺带看一眼那个文件里 `sender` 只有账号名 |
| 7 | `logs/online.log` 里 grep 一下 `[admin]` | 每行的操作者是 `'账号名'（昵称）`，没有同名号的还是 `'账号名'` |

一句「行 / 不行 + 哪一条」即可。

**Z-77. ★★ 管理页「装备卖出」（D88 ~ D93）—— 要两个人配合走一遍**

单测和浏览器那两层 agent 自己跑完了（全绿；三档身份、卖出全流程、
价格弹窗、数量弹窗、锁定页都在真 HTTP 上点过）。**剩下的全是「游戏里到底
变没变」**，只有实机能判。

先 `stop.bat` → `start.bat`（服务端改了，铁律 7），然后：

| # | 操作 | 期望 |
|---|---|---|
| 1 | 用**游戏账号**登管理页（`http://127.0.0.1:27810/admin`） | 标签行里有「装备卖出」，排在「金币 / 经验」和「玩家仓库」之间；「⚙ 卖出价格设置」点得开，但里面**全是锁住的**、没有保存钮（D94 二） |
| 2 | 进「装备卖出」，鼠标停在一件东西上 | 浮窗最下面多一行「◆ 卖出单价 N 金币」；合成来的那件还列出「退 xxx×N」 |
| 3 | 卖一件**正穿在身上**的装备 | 游戏里金币当场变、仓库里没了、**人物模型上也脱了** |
| 4 | ★★ **两个人坐同一个房间**，其中一个卖掉身上的装备 | **另一个人屏幕上那个角色也当场脱掉了**（§94 那一发广播）。顺带验：管理员在「修改仓库」里给房里的人开一件装备，同房的人也看得见 |
| 5 | 卖一件**合成得来**的装备 | 配方材料**实物**真的回到仓库（不是折成金币） |
| 6 | **进一局游戏中**，点「确定卖出」 | 弹「游戏过程中无法卖出，请完成这一局游戏后再试」，**东西和金币一个都没动** |
| 7 | 用**管理员账号**（没有同名游戏账号）进这一页 | 左右两块被盖住并写明原因，**但「⚙ 卖出价格设置」点得开、改得了、存得上** |
| 8 | 改一个材料单价存下，回卖出页 | 那一类材料的单价当场跟着变 |
| 9 | ★ 卖一件**消耗品 / 礼包 / 钥匙**（§95 那 20 件） | 卖得掉、游戏仓库里数量真的少了 —— 这是 2026-09-12 修掉的那个「页面上摆着、一按确定说东西不够」 |
| 10 | ★★ **坐在房间里、用着一个买来的商城角色**，把那张角色卡卖掉（或让管理员在「修改仓库」里删掉） | 人物预览**当场变回泰尔**，自己和同房的人都看得见（§96 那一发）；卡还在时改别的（比如金币）**不该**播换角色动效 |

一句「行 / 不行 + 哪一条」即可。

★ 版式那几条（D93）agent 在浏览器里量过了，不用实机：1440 / 1200 两档下
大分类标签都是一行、格子三列、价格弹窗不出滚动条、横向不滚。

**Z-76. ★★ `/Brepro` 之后那份 hook（D87 六 / §92）—— 起一次客户端就够**

`hook/bin/bshook.dll` / `bsloader.exe` 的字节换了（**代码没变**，是调试目录多了
一条 `REPRO` 记录导致的整体挪位，§92 有逐区域核对表）。静态该查的都查了，
**能证伪它的只有实机**。

怎么做：`stop.bat` → `start.bat`，登录进大厅一次，然后看：

| # | 看哪儿 | 期望 |
|---|---|---|
| 1 | 游戏起没起来、能不能登进大厅 | 和之前一样 |
| 2 | `logs\` 里 bshook 那份日志的 `PATCH` 行 | **一条不少**（尤其 `★换图野指针根治`、礼物图标兜底那几条） |
| 3 | 登录框顶上的反倒卖公告 | 照常显示 |
| 4 | `logs_client_crash\` 和 `game_patched\BigShot.rpt` | 没有新的 |

一句「行」或者把 `PATCH` 那几行贴回来都行。
★ 顺带：**你那台服务端现在开着**（pid 32756 听着 27810），我没动它 ——
`stop.bat` 会把它一起停掉，你自己挑时间。

**Z-75. ★★ 加密后的存档转移助手（D86 / §91）—— 浏览器走一遍**

单测和真 HTTP 那两层 agent 自己跑完了（3198 + 16 条，两套运行时全绿），
**「用记事本打开长什么样」只有人眼能判**。

怎么做（浏览器打开注册页 `http://127.0.0.1:27810/` → 存档转移助手）：

| # | 操作 | 期望 |
|---|---|---|
| 1 | 「存档下载」导出一份，**用记事本打开看一眼** | 顶上是版本号 + 四行中文说明，然后 `username` / `nickname` / `password` 三项明文，最后一长串 `data`；**搜不到自己的金币数**；文件名是 `popshot-save-v2-<用户名>.json` |
| 2 | 把 `nickname` 和 `password` 改掉，「存档上传」传回去 | 提示成功，回执里的等级 / 经验 / 金币和导出时**一模一样**（本级内攒的经验不许掉） |
| 3 | 用改过的**新密码**登录游戏 | 能进，昵称是新的 |
| 4 | 记事本把 `data` 里随便一个字符改掉再传 | 被拒，提示写着「被改过 …… 只有 username / nickname / password 三项可以自己改」，**号没坏**（等级金币照旧） |
| 5 | 把 `nickname` 改成一串 emoji 再传 | 被拒，提示是昵称规则那一句 |
| 6 | （手头还有的话）拿一份 **V0.3.1 之前导出的明文存档**传上去 | 被拒，提示写着「这是旧版导出的存档，请重新导出一份」 |

把「行 / 不行 + 提示原文」告诉我即可。

**Z-74. ★★ 登录界面的反倒卖公告（§86 / D84）—— 只差实机看一眼**

登录框顶部那块 533×260 的死网页框（原世纪天成公告页）现在改成显示
反倒卖声明。文案混淆后编译进 `bshook.dll`，倒卖者改不掉。

- ✅ **已经实机跑通的**（本机，2026-09-11，PrintWindow 抓图 + 窗口消息驱动登录）：
  1. 公告**真的显示在登录框顶部那块 533×260 里**，中文正常、单行排版、无滚动条、
     **没有 IE 的「已阻止内容」金条**；
  2. `MiniBrowser::Navigate` **原样接受 `file:///` 宽 URL**（§86 静态结论成立）；
  3. 临时路径走 **8.3 短名**（`PSNOTI~1.HTM`），中文/空格用户名那条坑绕过去了；
  4. **登录照常能成** —— 本机服务器 `testuser1` 进大厅，服务端
     `✓ 登录 等级=24`，握手版本号 3001 → V0.3.1，门禁回 0；
  5. **不崩** —— WM_CLOSE 后进程照常响应，`logs_client_crash/` 0 个、
     `BigShot.rpt` 没更新；
  6. `BSHOOK_KEEP_NOTICE=1` **退回原版**（还是那张「已取消网页导航」错误页）；
  7. 混淆验收：`--verify-dll` 11 个特征词 × 3 种编码 **0 命中**（失败路径也确认会红）；
     `server/test_notice.py` 12 条全绿；`hook/build.bat` 编译后自动跑这道闸门。
- ✅ **第二轮追加（用户要求，§88）**：
  8. **公告文字可选中复制** —— 清掉 `DOCHOSTUIFLAG_DIALOG`（`0x442f35` 的
     `0x1008F` -> `0x1008E`）。实机拖选变蓝核对过。右键菜单原版就没有
     （`ShowContextMenu` 回 S_OK），只能拖选 + Ctrl+C；
  9. **GitHub 链接点了开系统浏览器** —— 接管 `IDocHostUIHandler::TranslateUrl`
     （vftable `0x665f0c` 那一格）。实机：Edge 起来了，**公告框内容没被顶掉**。
     ★ 走了弯路：先接管 `BeforeNavigate2`（`0x44335f`），**那个事件接收器
     压根没被 Advise**，patch 在位也不会被调到（§88-二）。
**Z-76. ★★ hook 完整性校验（§89 / D85）—— 已实机跑通，只差发版时的操作**

握手上报的那 4 字节里折进了客户端那份 `bshook.dll` 的 SHA-256；
服务端拿 `server/manifest-hook.json` 比对，**对不上就强制更新**。
堵死了「改包根 `BUILD.ver` 的明文版本号就绕过版本门禁」这个洞。

- ✅ **实机验过的四条路**：校验通过→登录成功 / 校验位对不上→拒绝且客户端
  真的拉起了 `BsPatcherChn.exe` / 清单里没这个版本→放行（一轮时的行为，二轮已改拒）/
  清单 JSON 写坏→放行+告警。C 侧和 Python 侧的校验位**逐位对过**（`0xFB000BB9`，校验位 123）。
- ✅ `server/run_tests.py` **3172 条全绿**（新增 `test_hookintegrity.py` 18 条）。
- ✅ **第二轮收紧（用户 2026-09-11）**：「清单里没这个版本」从放行改成
  **拒 + 强制更新**（理由：服务端由发版人自己管、保证先更新）。
  配套加了安全阀 —— **服务端自己的版本不在清单里时整项校验自动关掉**
  （实测：BUILD.ver 改成 V9.9.9 后日志打「这一版不校验完整性」，登录照常）。
- ✅ `hook/build.bat` 编完**自动刷新清单**（版本号取 `build-ver.config`），
  否则开发机重编 hook 之后会被自己的服务端拒掉。
- ✅ `BUILD.ver` **开始进 git**（`.gitignore` 里改成 `!`），打包收尾把包里那份
  拷回仓库根 —— 和 `server/manifest-hook.json` 成对提交。
- ★ **2026-09-11 review 抓到并已改**（D85 四）：提交里的清单带着一条
  V0.3.1，hash 是开发机新编 hook 的；而**已发布的 V0.3.1 hook 没有校验码**
  → 一部署，所有老玩家被判「该带没带」，旧更新器又不重装同版本 → **全体卡死**。
  已改：`build-ver.config` 抬到 **0.3.2**，清单只留 V0.3.2，根 `BUILD.ver` 生成
  （V0.3.2）；两个打包脚本见 `gen_hook_manifest.py` 非 0 直接中断。
- ⏳ **发版时要做的**（顺序照旧，D85 四·B）：① 版本号已是 0.3.2，下次再抬，
  同时把仓库里 `config/server-ClientFilter.config` 抬到同一版本；② 打包（清单和
  根 `BUILD.ver` 都会自动更新）；③ **把 `BUILD.ver` 和 `server/manifest-hook.json`
  一起提交**；④ 客户端发上 GitHub Release；⑤ 服务端包整体覆盖到云端。
  ★ 门禁低于清单最老版本（0.3.2）时，改个更老的 `BUILD.ver` 就能绕过校验。
- ⚠ **同一个版本号下重编 hook = 作废已经发出去的那一批**（sha256 对不上了，
  会被强制更新）。同版本重发时必须整包重新上传 GitHub 并覆盖服务端，新更新器
  会把老那批重下回来（§90 一）；只覆盖服务端不换 zip 就是死循环。
  闸门：`update-manifest.json` 里最新的版本随便刷（开发机反复打包不受影响），
  比它老的版本 hook 冻结，`gen_hook_manifest.py` 退出码 2、打包中断（D85 三·C）。

**Z-77. ★★★ 更新器的两个洞（§90）—— 已修，实机落锤**

用户问「updater 是不是也要改」，查下来**要，而且挖出一个既有的严重 bug**：

1. `pick_target` 里「服务器要的版本本地已有 → 无需更新」——
   被篡改的客户端版本号没变，会**永远停在「已是最新版本」**。
   加 `force`：探针回 REJECTED 时无视版本比较，重装同版本即自愈。
2. ★★ **`cfg_server_address` 一直读不到配置**（`MultiByteToWideChar` 缓冲区
   放不下时返回 0，被当成读取失败），探针一直去连默认的 `192.168.1.100`。
   ⇒ **探针从来问不到「服务器要哪个版本」，成对发布（D079）一直没生效。**
   症状完全沉默，只有一句 `probe unreachable`。
3. 探针自己也得带完整性校验位，否则 `PROBE_OK` 永远不可能出现。
- ✅ 实机：改坏清单 → 服务端拒 → 更新器 `target V0.3.1` → 真的开始下载
  （修之前是「已是最新版本，无需更新」）。
- ✅ `updater\build.bat` 的自检闸门通过。

- ⏳ **还差用户做的只有一件**：**真点一次退出、走完「确认退出」那个 D3D 自绘框**
  —— 那条路上会析构内嵌 IE 控件（§49-B 的旧账就在那儿），而那个确认框不是
  Win32 窗口，发消息点不到，只能真人点。**期望结果**：正常退到桌面，
  `logs_client_crash/` 仍然是空的。

**Z-75. 许可证换成 PolyForm Noncommercial 1.0.0（D84）—— 等用户过目**

`LICENSE` / `LICENSE.zh.md` / 根 `README.md` / `tools/server-package/README.md`
都改好了，两个打包脚本也开始把 `LICENSE` 带进包。
⏳ 用户过一遍措辞即可，没有需要实机验证的东西。

**Z-70【已验证：不行，已撤销】NEXON 信使不能拦（§80 / D79）**

- 2026-09-11 00:12 用户实机一登就翻：点「登录」后 **9 毫秒**弹
  「登录失败 / 认证服务器失败 (20000)」，**一个网络包都没发**
  （本机认证服的 `+++ 连接#N` 计数器全程 0）。而且 nmcogame 记住了
  「模块不可用」，**之后每一次登录都直接失败**。
- ⇒ **登录路径确实依赖 `NMCO_CallNMFunc` 返回 1**。补丁**已整个拆掉**，
  `nmconew.dll` 照常加载，**§68 那 4 份崩溃仍是未修**。
- 要再试只剩一条路（**没验过**）：换 BigShot 那一格 IAT `0x63751C` 直接返回 1。

**Z-73. ★★ NEXON 信使**整个拆掉**了（§85 / D83）—— 要真人打一局确认**

用户 2026-09-11 拍板：「本来就没用的东西，完全拆掉比较保险」。
现在默认就是拆掉的：`nmconew.dll` 不加载、`NMService.exe` 不起、
nmcogame 一次都不被调用（**铁律 5 到这里才真的落地**）。
登录那一发由 `bshook` 自己连认证服（协议没改，**服务端一行没动**）。

- ✅ **已经实机验过的**（本机，2026-09-11，用 `testuser1/123`）：
  本机服务器正确密码登录进大厅 / 错密码弹「密码错误 (20026)」且应答字节和原版**逐字节相同** /
  **远程服务器模式**（经本机中继）也登得进 / 全程 `nmconew.dll` 和 `NMService.exe`
  **一次都没出现**（日志 + `tasklist` 都确认过）。
- ⏳ **要用户做的**：登录之后**真打一局**，重点看
  ① 大厅里和信使有关的 UI（好友 / 消息）点开会不会出事 —— 设计上是空列表，**没人点过**；
  ② 其余一切照旧。
  日志里该有一行 `PATCH ★NEXON 信使已整个拆掉（§85）…`，
  **不该有** `★信使 !! 信使已拆，但 nmcogame 还是来加载…`（那是绊线，出现就说明拆漏了）。
- **逃生门**：`BSHOOK_KEEP_NM=1` 一键退回 Z-72 那一档（信使照常跑 + 四处崩溃守护，
  代码一行没删）。
- ★ 顺手发现的**原版 bug**（没动）：**登录失败之后「开始」按钮和两个输入框一直禁用，
  只能重启游戏再试**。拿 `BSHOOK_KEEP_NM=1` 做过对照实验，原版一模一样。
  `bshook` 现在只解禁了分区单选钮，要修就在那儿顺手把 `id=1004/1005/1006` 也解了。

**Z-72. `nmconew` 那一族（§82 + §84）—— ★ 已被 Z-73 取代，留作逃生门**

现在默认走 **§82 + §84**：`nmconew.dll` **照常加载**（登录路径一个字节不碰），
在它加载那一刻给它打**四处**崩溃守护。⇒ **这一档不可能弄坏登录。**

- ✅ **2026-09-11 本机自证过了**（`gui_probe.py login <pid> zzz_no_such_account zzz`，
  D81 那条 —— 验它不需要真账号）：

  ```text
  PATCH ★信使崩溃守护 x4（bug调查/18 §82 + bug调查/19）: nmconew.dll base=058E0000
    互斥体判活 @ +98D59 / 魔数校验判空 @ +26591 / 工作线程判空 @ +75CCE / 解锁判活 @ +98EE7
  ```

  从活进程逐条回读核对过（`E9 rel32 + NOP`，下一条指令边界完好，
  四个 detour 落点减各自 RVA 都等于同一个 bshook 基址）。
- ⏳ **还没验的**：**补丁真的挡下一次崩溃**。要复现「打着游戏返回登录画面重新登录」
  并且撞上 `Enter()` 那 **10 秒**窗口（§84：线上那次是「客户端关连接 → 9.3 秒后崩」）。
  真救到时日志里会有 `★信使 nmconew 的工作线程醒来时对象已经被关掉了…`
  或 `★信使锁 …`。**要玩家实际跑出来才算。**
- **逃生门**：`BSHOOK_KEEP_NM=1` = 四处都不打（复现 §68 / §84 用）。
- ★ **前提**：`hook/bin/bshook.dll` = `28124C8E…` 199168 B，**线上 V0.3.1 里是
  `6D6872B5…` 191488 B**（不含 §84 / §85）⇒ **必须起 V0.3.2**，和 Y-69 / Z-73 一个包发。

★ **两条已经关掉的路，别再试**（都实机推翻过，代码都删了）：
`§80` 不让 `nmconew.dll` 加载、`§83` 把 `NMCO_CallNMFunc` 换成直接回 1 ——
两次都是「点登录后十几毫秒弹 20000、认证服 0 连接」。
**登录真的需要 `nmconew` 产出一份本地造的应答**，这个 DLL 拿不掉。
⇒ `NMService.exe` 还是会起，铁律 5 仍然只是写在纸上，但它**已经不是崩溃来源**。

**Z-71. 三处散崩守护（§81 / D80）—— ★ 和 Y-69 同一个包一起验**

- **已经自证过的**：三处根因都从 dump 落锤（`[0x72e29c]` 已为 0 / 缓冲区算术严丝合缝 /
  `ExceptionInformation` 说读地址 0）；补丁点字节逐条核对过。
- **怎么验**：**平时玩就行** —— 这三处都是「原版会崩、现在不崩」，正常情况下
  一行日志都不该出现。真踩到了 `logs/bshook_*.log` 里会有
  `★拆除 …` / `★UI …`（B 那处没有日志，它只是多分配一个字节）。
- **重点看**：起客户端时该有一行
  `PATCH ★散崩守护 x3（bug调查/18 §81）… @ 0055C260 / 005D9E02 005D9E0D / 00438BD3`。
  没有这一行说明特征串没对上（那三处就没装）。
- **顺带**：B 那处修的是**合成界面提示框**那类 UTF-16 `.ui` 的读取 ——
  进一次合成界面、把鼠标在物品上划一圈，没有异常就算过。
- **当前状态**：⏳ 等实机。

**Y-69. 进 boss 房不再全场一起崩（§78 / D78）—— ★ 要重打客户端包才验得了**

- **背景**：用户 2026-09-10 报「V0.3.0 上线后，任务模式进 boss 房有时黑屏无响应」。
  根因是换图把突击技对象连场景一起放掉了（§78），黑屏和闪退是同一个 bug。
- **已经自证过的**：链路全静态坐实（`0x502229` 登记 → `0x473692` 整片析构 →
  `0x50794a` 照用）；云端 23 份崩溃报告里 16 份、3 份 dump 的栈完全一致；
  补丁在真客户端上装上了，从活进程回读反汇编核对过。
- **★ 前提**：`hook/bin/bshook.dll` 现在是 `28124C8E…` 199168 B（含 §78 + §81 + §82 + §84 + §85）。
  **V0.3.1 已经发出去了**（`config/server-ClientFilter.config` = `0.3.1`，
  2026-09-11 那一晚玩家日志里全是 `客户端版本=V0.3.1`，线上那份是 `6D6872B5…`）
  ⇒ **起 V0.3.2**。
  ★ 顺序照「下一步」那五步走（先发 Release，最后才顶 `server-ClientFilter.config`）。
- **怎么验**：任务模式打一关有 boss 房的（Quest03 / Quest05 / Quest06 都行），
  **进 boss 房前先按两下方向键冲刺一下**（野指针只在「换图那一刻手上有突击技对象」
  时才产生），然后走到地图最右边换图。
  **期望**：新图正常开打，没有黑屏、没有闪退，房里其他人也都还在。
  多人房最好两个人一起验（这个 bug 的特征就是**全房间同时死**）。
- **看日志**：`logs/bshook_*.log` 里
  ① 起来时该有一行 `PATCH ★换图野指针根治（bug调查/18）… @ 004790CB`；
  ② 真的救到了的话，换图那一刻会有一行 `★换图 角色 xxxxxxxx 跨图还挂着突击技对象…`。
  ③ 要是还看到 `★DASH …` 那一行，说明**还有别的路子**能放掉这个对象（§66 那三处网兜住了），
     把那一行发我。
- **怎么告诉我**：一句「进 boss 房正常 / 还是崩」+ 上面那两三行日志。
- **当前状态**：⏳ 等重打包 + 实机。

**X-68. 礼物盒里装备的提示框现在有字了（§77）—— 一眼就能验完**

- **背景**：用户 2026-09-10 实机发现「礼物盒里的装备指上去，框弹得出来但里面是空的」。
  原因不是没收到定义 —— 那个框下半两块读的是 **`Gift` 自带那份 `ShopStock` 的说明**
  （我们发的空串），而**同一个框**里的等级 / 角色 / 修理次数读的是 `ItemInfo`（一直是对的）。
- **已经自证过的**：两套运行时 3140 项全绿；`build_gift` 的字节按 `Gift::Des` 解回来，
  说明那一格现在是 `伤害 24（爆头 39）\n换弹 2.20 秒\n初速 225` 这一类
  （和**商店货架**提示框同一个来源），凭证是「管理员发放的奖励，领取后直接到账」。
- **怎么验**（★ 只改了服务端 ⇒ `stop.bat` → `start.bat` 就行，**hook 没动、客户端不用重打**）：
  ① 进游戏 → 礼物盒 → **先点一下**那份装备的「接受」把它打开
     （★ **未打开的礼物本来就不给提示框**，`0x44b84f` 里那句 `test [gift+0x48],1` ——
     这是原版行为，不是 bug），再把鼠标指上去。
     **期望**：下面两个绿框里有字了，武器是「伤害 / 换弹 / 初速」那几行，
     防具是「攻击 +x%」这类；框里**没有价格**是对的（礼物价格 0，客户端自己跳过那一行）。
  ② 经验 / 金币那两份：指上去应该写「管理员发放的奖励，领取后直接到账」。
  ③ **材料**（黑色小珠这类）指上去**仍然是空的** —— 那是「材料从来没有说明」，
     合成界面和仓库里也一样空。要不要给材料写一句（比如「合成材料」）你说了算。
- **怎么告诉我**：一句「①②对不对」+ 想不想要 ③。
- **当前状态**：⏳ 等实机。

**X-67. 管理页「发送记录」（D77 / §76）—— 只剩「在你自己的浏览器里看一眼顺不顺手」**

- **已经自证过的，不用再验**：两套运行时 3137 项全绿（新增 11 项，含「记录落在
  `server/data/gift_history.json`」「清空只删记录、礼物盒不动」「发失败 / 被跳过的人
  照样在名单里」三条）；另起一台临时服务端在浏览器面板里把整套走过了 ——
  三条记录的列表、点第三条切详细、`.bad` 名单标注「跳过 泰尔 大师铠甲·上衣」、
  经验 / 金币两格画成「经」「币」、「清空记录」确认框 →「已清空发送记录（5 条）」→
  文件删掉而五个号的礼物一件没少；工具条的三样按 `getBoundingClientRect` 量过，
  依次贴在最右端（批量发送奖励 893~1011 / 发送记录 1021~1111 / 当前在线 1121~1227，
  条右边界 1241）。★ **面板里看不到截图**（你在全屏玩游戏，Claude 窗口被遮），
  所以「好不好看」这一半没验，见 §76。
- **打包 / git 也验过了**：跑了一次 `build-server-package.ps1`，包里 `server\data` 是空的，
  `gifthistory.py` 进了包；`git check-ignore` 三条规则都命中。
- **怎么验**（★ 先 `stop.bat` → `start.bat`，服务端改了才生效）：
  ① `/admin` → 玩家仓库。**期望**：工具条右端依次是「批量发送奖励」「发送记录」
     「● 当前在线：N 人」，左边那半（搜索 / 下拉 / 查找 / 刷新）不动。
  ② 点「批量发送奖励」发一次（随便给谁）。
  ③ 点「发送记录」。**期望**：左栏最上面是「共 N 次」+「清空记录」，下面是记录列表
     （发送时间 + 发送者 + 「x 名玩家 · y 样奖励」），默认停在最新那条、右栏画着它的
     时间 / 发送者 / 留言 / 结果 / 奖励玩家名单 / 奖励物品（物品有图标，经验金币是「经」「币」）。
  ④ 点别的记录 → 右栏跟着换、左栏那条亮起来。点遮罩或按 Esc 能关。
  ⑤ 「清空记录」→ 确认框 → 列表空了；**再开一次礼物盒确认礼物还在**。
- **怎么告诉我**：一句「①~⑤ 哪几条对」+ 觉得难看 / 不顺手的地方（截图最好）。
- **当前状态**：⏳ 等你在真浏览器里看一眼。

**X-66. 管理页「发送奖励」→ 玩家在游戏里的礼物盒领（§75 / D76）—— 协议全静态，只有实机能证伪**

- **已经自证过的，不用再验**：两套运行时 3126 项全绿；`Gift` / `0x050a` 的字节按客户端
  Des 逐段解回来对过；真服务端起过、管理页弹窗在浏览器里走过（选人 / 全选 / 在线筛选 /
  添加物品 / 经验金币 / 留言 / 确认框 / 回执），发完 `accounts.json` 里那个号的 `gifts` 多了几份。
- **第一轮实机（2026-09-10）已经看到的**：礼物盒能开、8 格一页、材料 / 装备 / 卡片 / 染色剂
  都列出来了，接收弹窗的 礼物名 / 发送人 GM / 日期 / 留言 都对，合并弹窗也会弹 ⇒ ②③ 的「画不画
  得出来」过了。露出来的两处已改：数量（以前「（0个）」）、材料图标（以前「(FileNotFound)」）。
- **怎么验**（服务端 + hook 都改了 ⇒ `stop.bat` → `start.bat`，会带上新的 `bshook.dll`；
  `logs/bshook_*.log` 开头应多一行「★礼物盒图标退回物品表 @ 004169FF」）：
  ① 用测试号登进大厅，管理页 `/admin` → 玩家仓库 → 「发送奖励」：勾上这个号，加一件材料
     ×5、一件装备、经验 500、金币 3000，留言随便写 → 确认发送。
     **期望**：聊天框冒一句「收到礼物。」，任务栏「礼物盒」钮变状态。
  ② 打开礼物盒。**期望**：材料那份**有图标**了（绿色小珠 / 龙之血 / 卡片这类以前是
     (FileNotFound) 的），浮窗里也有。
  ③ 点开材料那份：礼物名只写「绿色小珠」、括号里写「（5个）」（第三轮用户点的：名字里的
     「×5」和括号重复，去掉了；经验 / 金币仍写「经验 ×500」，那一档客户端不显示数量）；
     按「接收礼物」→ 已拥有的会先弹合并框
     「道具数量增加为 <原数+5>个」→ 确认 → 弹「已接收礼物[…]。请在我的仓库中确认。」→
     仓库里材料真的 +5、这一格从礼物盒消失。装备那份同样领一次 → 仓库里有了。
  ④ 领「经验 ×500」「金币 ×3000」：弹窗**图标空白**（已知，D76）但能领，领完大厅的等级 /
     金币当场变。
  ⑤ 再发一份，这次点「丢弃」→ 确认 → 那一格消失、仓库 / 金币不变。
  ⑥ 人**停在商店 / 仓库界面**时管理页再发一份 → 礼物页自己刷出来（不用退出来）。
  ⑦ 有未领礼物时**重登**一次 → 进大厅弹「收到礼物。」；不领、只在大厅和房间之间来回 →
     **不重复弹**。
  ⑧ 全程不闪退，`logs/bshook_*.log` 没新东西。`logs/server.out` 里应能看到
     `← 回 0x0501 …（礼物清单要用）` 紧接着 `← 回 0x0508 礼物清单 N 份`，领取时
     `0x0601`（或 `0x0600`）在 `0x050a` **前面**。
- **怎么告诉我**：一句「①~⑧ 哪几条对、哪几条不对」；不对的那条说清楚现象 + 那段 `server.out`。
  界面不对时 `python tools/gs_ctl.py shop-reply off 050a`（或 `0508`）能不重启二分。
- **当前状态**：⏳ 等实机。

**W-65. 金币 / 经验获取页（§70 / §71 / D72）—— 页面自己验过了，剩下「打一局看数」**

- **已经自证过的，不用再验**：两套运行时 3065 项全绿；`ensure_files()` 真机生成的
  `server/data/rewards.json` 30 档和老公式**逐格比对 0 处不一致**；
  管理页在浏览器里用桩数据点过（两张表的行列、金币 ⇄ 经验切换、改一格亮脏标记、
  放弃修改能退回去、标签行 8 个按钮在 1320 宽里不折行）；
  「存下去不用重启就按新数发钱」有 HTTP 用例钉着
  （`test_web_admin.test_a_reward_edit_lands_on_disk_and_takes_effect_at_once`）。
- **要实机的是这三条**（**只改了 `server/`，hook 没动** ⇒ `stop.bat` → `start.bat` 即可，
  客户端不用重打包）：
  ① **看版式** —— 开 `/admin` → 「金币 / 经验」页，两张表格的行列 / 配色 / 间距顺不顺眼，
     「金币」「经验」那个分段控件切一下，经验那一半下面两个系数框在不在；
     点「▤ 等级经验对应表」弹出那张 60 级的表，✕ / 点旁边空白 / Esc 都试一下。
  ② **打一局闯关** —— 结算界面的「金币 +N / 经验值 +N」和 `logs/server.out` 里那行
     `结算 座位N: … 金币+X（固定 A + 捡到 B）` 对得上，且 A 等于表里那一格。
  ③ **热改生效** —— **不重启服务端**，在管理页把「1 · 机械青蛙 / 简单 / 通关」的金币
     改成 12345 保存，再打一局简单机械青蛙并通关，看那行日志的「固定」是不是 12345。
     ★ 顺带验一局**对战**（生存 或 夺分，个人 / 组队随意），确认它查的是自己那一档。
- **怎么告诉我**：一句「版式行 / 不行（哪里不对）」+ 那一行结算日志。
- **当前状态**：⏳ 等实机。

**V-64. 线上闪退这一批（§65 ~ §69 / D71）—— 部分已自证，剩下的要实机**

- **已经自证过的，不用再验**：崩溃报告器那一处（§65）在本机做过 A/B ——
  `CreateRemoteThread(起始地址=0)` 造一次「出错地址在未映射页」的崩溃，
  原版 `LastCrashReport.txt` 238 字节、断在 `Exception code` 那行、`.mdmp` 没生成；
  打了补丁 982 字节、寄存器 + 完整调用栈齐全、12.6 MB dump 已生成。
  五处补丁点也都从**活进程**里回读反汇编核对过（E9 + NOP 填充 + 下一条指令边界完好）。
- **要实机的是这三条**（都只动了 `hook/`，**服务端不用重启**；客户端要用重编过的
  `hook/bin/bshook.dll`，185344 字节）：
  ① **正常玩一局别出新毛病** —— 突击技那三处校验在冲刺时每帧都要跑，
     随便打一关，看冲刺 / 拖影正不正常、`logs/bshook_*.log` 里**没有** `★DASH` 行。
  ② **`0x0411` 判空（§67）** —— 人**停在大厅 / 房间里、不进关卡**，
     跑一次 `tools/quest-clear.bat`（或 `python tools/gs_ctl.py endgame`）。
     **期望：客户端不闪退**（原版这时候必崩，`BigShot.rpt` 09-10 00:05:39 那次就是）。
     想看对照就先设 `BSHOOK_KEEP_RPT_CRASHES=1` 再跑一遍，那次应该崩。
  ③ **心跳那行数（§69）** —— 打一局下来看 `logs/bshook_*.log` 的
     `--- still alive (…)；地址空间 …` 有没有一路往上爬。
     **本机登录界面基线：已提交 561~570 MB / 最大空闲块 804 MB。**
- **怎么告诉我**：一句「玩了 N 局，正常 / 哪里不对」，加上那份 `logs/bshook_*.log`。
  真再闪退的话，现在崩溃现场是**完整**的了 —— 把 `logs_client_crash` 那一包发过来即可。
- **当前状态**：⏳ 等实机。★ **线上玩家要拿到这一批修复，必须重打客户端包 + 抬版本号**
  （和 U-63 是同一次打包）。
- **明说没做的**：崩溃 B（`nmconew.dll` 那把锁，§68）**没修**，理由见 D71 ④；
  崩溃 A（`DashDamage` 野指针，§66）补的是**兜底 + 探针**，根因（谁放掉的对象）没查到。

**U-63. 岩浆巨龙不再永久无敌（§64 / D70）—— 只能实机打**

- **先问玩家一句**（不用等实机）：卡关那局，boss 出来之后**有没有**出现
  「怎么会……子弹全部都弹开了！/ 交给布洛克扫描吧」这段对话？
  没出现 = 就是 §64 这个病，补丁对症；出现了 = 病在别处，补丁不管用，要换方向查。
- **怎么验**：只动了 `hook/`，**服务端不用重启**；但客户端要用**重编过的
  `hook/bin/bshook.dll`**（会话 27 重编，183296 字节，含 DRAKA 诊断）。
  线上玩家要拿到这个修复，得**重打客户端包 + 抬版本号**走自动更新。
  ① 建闯关房 → 关卡选**岩浆巨龙**（quest 2），难度随便；
  ② 一路打到第二张图 `Quest02_2` 的 boss 房；
  ③ 看「布洛克分析弱点 → 就是那里！」那段剧情**放没放**，
     以及 boss 身上的石壳**打不打得掉**（打得动 = 屏幕右上角分数会涨）。
  ④ 反复进几局 —— 这个 bug 本来就是**有时**才犯的。
- **期望**：每一局最终都能打（剧情可能比原来晚几秒才放，那是正常的：
  修好之后是「门一开就补放」，不再要求 boss 还在放出生动画）。
- **怎么告诉我**：一句「打了 N 局，都能打 / 第几局还是无敌」，再把每局的
  `logs/bshook_*.log` 留下来 —— 里面 `DRAKA` 开头的四行（boss 创建 / 门开 /
  Tracing 入队 / 弱点点出）就是判据：**门开那行 `状态=idle1`** 说明出生窗口真的
  在检查之前过期过（原版会卡关、补丁救回来了）；**有「Tracing 入队」没「弱点点出」**
  说明病在别处。哪怕一局都没卡，这几行也能把 §64 里没查清的触发条件钉死。
- **当前状态**：⏳ 等实机。**已自证的部分**：会话 27 用 `start.bat` 起了一次真客户端，
  `logs/bshook_20260910_112130_pid26384.log` 里有
  `PATCH ★岩浆巨龙弱点剧情 @ 004B5A04` 和 `PATCH ★岩浆巨龙诊断已装 @ 004B1812 /
  004A70B4 / 004A712D / 004A76C9`（四个 hook 各偷 7/6/5/6 字节，安装成功），
  客户端没崩，`stop.bat` 收干净。
  **没自证的**：没有真打一局 —— 「补丁装上了」≠「关卡通了」，DRAKA 那四行也还
  没在真局里出现过。

**T-62. 别人身上的装备（§63 / D69）—— 两个客户端才能验**

- **怎么验**：`stop.bat` → `start.bat`（**服务端改了**，铁律 7；hook 没动）。
  两个号（本机开两份客户端也行）进**同一个房间**：
  ① A 先穿一件**看得出来**的装备（铠甲 / 翅膀这种有模型的，别用戒指宠物），
     B 再进房 ⇒ **B 一进去就该看见 A 穿着它**；
  ② 反过来：B 已经在房里，A 这时候才穿 / 脱 ⇒ **B 那边当场跟着变**；
  ③ 开一局进图 ⇒ 进图之后还是穿着的（战斗里的模型是另一处建的，
     `0x493258`，读的是同一份清单）；
  ④ 打完 → 结算 → 回房间 ⇒ **回到房间还看得见**（回房间那一步会重发全部座位）；
  ⑤ 加成也一起验：A 穿一件带 `Attack` / `Hp` 的，B 看 A 的血条上限 / 伤害数字
     和没穿时不一样（加成和外观读的是客户端同一格，一起对或一起错）。
  ⑥ **座位复用**：A 穿着装备退房，房主 `/a` 加一个 bot 坐进**同一格** ⇒
     bot 应该是**光的**，不能穿着 A 的装备（这一条是改动里最容易漏的，
     清单存在客户端跨房间活着的 LobbyStage 上）。
- **期望**：如上六条。
- **怎么告诉我**：一句「①~⑥ 哪几条对、哪几条不对」就够；不对的那条
  说清楚「谁看谁、什么时候」。
- **当前状态**：⏳ 等实机。单测两套运行时 3031 项全绿（新增 5 项，
  `test_room.EquipmentVisibilityTests` 把六条里的 ①②④⑥ 都钉在协议层了），
  但**协议层对 ≠ 客户端画得出来** —— 外观这条路是静态逆出来的，没实机跑过。

**S-61. 管理页筛选改版（D68）—— 看版式和手感**

- **怎么验**：打开 `/admin`（改的是静态文件，服务端不用重启；浏览器 `Ctrl+F5`
  强刷一次，免得拿到缓存的旧 js）。
  ① 物品库 / 商店货架 / 合成配方：筛选条只剩 搜索 + 角色 + 上架状态 + ↻ 刷新，
  下面两行分类标签（和「修改仓库」弹窗一样），点「装备」出小分类行；换页栏没了，
  列表自己滚。
  ② 每格标签上的数字是**过了角色 / 上架状态 / 搜索之后**的件数 —— 选「泰尔 +
  只看未上架」再看标签，数字应该跟着变；点进去件数和角标一致。
  ③ 「选择物品」弹窗（商店卡片点图标）同样两行标签；合成配方里「＋ 加一种材料」
  弹出的那个**没有**标签行（材料全在一格里）。
  ④ 「修改仓库」弹窗工具条上多了两个下拉，在「＋ 添加物品」和「↻ 刷新」之间。
  ⑤ 材料掉落页和原来一样（模式 / 关卡 / 难度），没有标签行；选一个关卡让
  「筛出 6 / 52」出现，筛选条应该还是**一行**（主区加宽到 1320 了，D68a）。
  ⑥ 另外三页**不再**写「筛出 x / y」，件数看标签上的角标。
  ⑦ 商店货架 / 合成配方的筛选条只剩 搜索 + 角色 + 刷新（D68b）；物品库还有上架状态。
  ⑧ 掉落页每一行的 数量 / 概率 / 开关 / 备注 上下对齐（材料格定宽）。
- **期望**：如上。物品库「全部」一次铺 808 张卡，整份重画约 0.2 秒 ——
  搜索框打第一个字是筛过的结果、几毫秒；清空搜索回到 808 张会顿一下，
  **能不能接受用户说了算**。
- **当前状态**：桩数据下四页 + 两个弹窗都点过，计数对；**真登录的页面没看过**、
  版式观感要用户定。

**Q-59. 客户端闪退后自动上传诊断日志（§54 ~ §58 / D58 ~ D64）**

自己能做的都做完了：3014 项单测两套运行时全绿；拿磁盘上那份**真实的**崩溃现场
（35.2 MB mdmp）离线重放，打包 11.6 MB / 0.5 秒、rpt 只截最后一段、mdmp 按
`Dump File Name:` 定位对；真包端到端 HTTP 上传 0.36 秒、逐字节一致、撞名自动加后缀；
一边灌包一边打注册页，注册响应中位 27 ms vs 基线 30 ms（没被拖慢）。
`stop.bat` → `start.bat` 起过，中继和服务端两条新日志都对、`relay.err` 空。

**只有实机能证伪的三条**：

1. **正向** —— 选**远程服务器**登录，用 §47 那条已知路径（穿火焰蝙蝠 `220003`
   拿溅射武器打空）真崩一次。
   *期望*：云服的 `logs_client_crash/` 里长出 `<账号>_<安装码>_<崩溃时刻>/`，
   里面一个 zip + 一个 `receipt.json`；zip 解开有 `.mdmp`、`LastCrashReport.txt`、
   `BigShot.rpt.last.txt`、`Debug/<当天>.txt`、`logs/`、`meta.json`。
   *怎么告诉我*：把那个目录名和 `meta.json` 的内容贴回来。
2. **反向（更重要）** —— 选**本机服务器**崩一次。
   *期望*：**什么都没上传**，云服那边不长任何东西。这是「只在远程模式上传」的证伪实验。
3. **不误报** —— 正常退出游戏一次。
   *期望*：`logs/relay.out` 里**没有**「捕获到一次崩溃」。

*当前状态*：⏳ 等实机。★ 服务端和中继都改了，要 `stop.bat` → `start.bat` 才生效（铁律 7）。

**已知不覆盖**：崩溃发生时中继进程本身也没在跑（玩家先手工杀了中继）——
此时无人收集，现场只留在本机 `game_patched/Dump/`。见 D58 的「代价」。

**R-60. 绿色加成文字关掉之后（§59 / D65）**

- **怎么验**：`stop.bat` → `start.bat`（**hook 换了**，服务端没动）。
  ① `logs/bshook_*.log` 开头应有**两行**：
  「★客户端加成绿字已关 @ 0041415E（仓库 / 合成提示框 / 修理界面）」和
  「★客户端加成绿字已关 @ 00414208（商店提示框）」——
  ★ **少一行就是只堵了一半**（第一版就栽在这）；
  ② **三个面板都要看**：**商店**（这次新堵的）、合成页停在**防护装置 `220002`**、
  仓库停在**称号 [队内间谍] `560007`** 和 `1010063`（泰尔 最后通牒·上衣）
  —— **中间那行绿字应该整条不见了**，只剩上下两块白字；
  ③ 顺带确认「速度+d%」「Team 5% Down」「绿字换行溢出」三个现象**一起消失**
  （它们都只出现在那行绿字里）。
- **期望**：如上。绿字没了之后，`ItemInfo2Txt`（y=256）那 14 px 会空着 ——
  两块白字中间多一条缝，属正常。
- **当前状态**：`hook/bin/bshook.dll` 已重编（178688 字节），特征串和
  `BSHOOK_KEEP_CLIENT_BONUS_TEXT` 都在二进制里核对过；**实机没跑过**。
  想对照原版就设 `BSHOOK_KEEP_CLIENT_BONUS_TEXT=1`。

**P-55. 物品说明文补全后的四件事（§53 / D57）—— ✅ 用户 2026-09-09 实机看过了**

两段**画得出来**（截图：合成页 `220002` 上下两块、仓库 `560007` 同样），
压行、武器溅射、兜底文案都对。★ 当时看到的问题全部落在那行**绿字**上，
和白字无关 —— 见 §59 / R-60。**这一条可以收掉了**，剩 GM 管理页浮窗没专门看。

**P-56. 迷你机械青蛙（`220004`）的冲刺守护装上了（§53 ⑨）**

- **怎么验**：被动验。`logs/bshook_*.log` 开头应有
  「★突击技加成提示判空 @ 00481EF3」。想主动碰：穿 `220004` 进图**朝空处冲刺**
  十几次（`DashAttack` 没有概率门，每次冲刺都带 flags `0x400`）。
- **期望**：都不闪退。★ **可达性本来就没查实** —— 碰不到才是常态，这条是兜底。
  `BSHOOK_KEEP_DASH_VISUAL_CRASH=1` 可回到原版行为。
- **当前状态**：编译进 `hook/bin/bshook.dll`（178688 字节），四个常量和特征串
  都在二进制里核对过；**实机没跑过**。

**O-54. `BigShot.rpt` 里三种旧闪退的守护（§48 ~ §50 / D56）—— 被动验，出现日志行即可**

- **怎么验**：正常游玩就行，三条都是「原版少一个判空」。能主动碰到的只有两条：
  ① 退出游戏时**连点两下 X**（确认框弹出后再点一次 X）—— 原版 09-05 那两次就是这么崩的；
  ② 管理页「玩家仓库」给某个角色**开一件别的角色的铠甲**（比如给泰尔开 `3010065`）
  再进仓库看模型 —— 原版 09-06 那两次。教程弹窗那条（需要大厅在弹窗期间被拆掉）不用专门验。
  ③ 顺带把另外四枚戒指各戴一次打一局（§52）：青鸟捡心 / 防护装置挨打 /
  迷你机械青蛙冲刺 / 萤火虫和队友一起 —— 静态干净，实机没戴过。
- **期望**：都不闪退。① 正常退出；② 模型缺一块（那件铠甲的顶点缩在原点），退出后
  `logs/bshook_*.log` 末尾有「蒙皮骨骼判空：这次运行跳过了 N 条」。
  每次启动 hook 日志里都应有「★旧闪退守护 x3」那一行。
- **当前状态**：补丁**已装上**（02:35:25 实机日志），登录界面 `CloseMainWindow()`
  冒烟正常退出；三条路径本身没有主动触发过。`BSHOOK_KEEP_RPT_CRASHES=1` 可回到原版。

**N-53. 穿着火焰蝙蝠扔苹果弹打空，不再闪退（§47 / D55 的 hook 补丁）**

- **怎么验**：在 `main` 目录 `stop.bat` → `start-debug.bat`（新 DLL 在 `hook/bin/`，
  客户端一拉起就带上；用 debug 是为了 `logs/server.out` 里能看见每发 `rpExplode`
  的 flags）。`testuser1` 已经穿着 `220003` 火焰蝙蝠、武器槽 3 是苹果弹极速 3。
  建房加 1 个 bot，进图后**朝地面 / 空处扔二三十发苹果弹**（IncSplashRange 那道门
  是 15% 概率，打空才走崩溃路径，多扔几发）。
- **期望**：整局不闪退；`logs/bshook_*.log` 开头有「★溅射加成提示判空 @ 0047EA21」；
  `server.out` 里至少有一发 `rpExplode` 是「目标 0、flags 0x200」（`内层opcode=0x0003`
  那行后面的 hexdump，body `+20` 处 `00 02 00 00`）—— 有它且没崩，这条就落锤。
  打中 bot 的那一发带 0x200 时仍会画「溅射加成」图（没动那一支）。
- **当前状态**：补丁在实机上**已装上**（01:59:12 的 hook 日志），但装上之后
  **没有再扔过弹**，「打空不崩」是推断（detour 只是补了头射那一支本来就有的判空）。
  `BSHOOK_KEEP_SPLASH_VISUAL_CRASH=1` 可回到原版行为复现。

**M-52. 三个角色各穿各的装备，互不影响（§46 / D54 的修复）**

- **怎么验**：先在那个「副本」目录 `stop.bat` → `start.bat`（拷进去的新代码要重启
  才生效）。进仓库，给泰尔 / 卡希尔 / 布洛克各穿一件铠甲（上衣就行）、各换一把
  武器槽 1 的枪、各选一个突击技；切回泰尔再穿一件别的上衣。
- **期望**：切到任一角色，它自己的铠甲 / 枪 / 突击技都还在「使用中」；泰尔换上衣
  只顶掉泰尔自己的旧上衣。`logs/server.out` 里「顶掉 […]」只出现在同角色同槽换装时。
  进房间打一局，卡希尔 / 布洛克的装备加成也在（血量 / 伤害按穿的算）。
- **当前状态**：单测钉了（`test_shopdata` / `test_account_store` / `test_gameserver` /
  `test_shop`），**实机没跑过**。之前被错顶掉的装备要**重新穿一次**。

**H-51. 每天到点的「自动备份」还没见过它真跑一次**

- **怎么验（不用等到凌晨 4 点，§45）**：管理页 →「数据备份」→ 把「备份时刻」
  改成**两分钟后**→「保存设置」（这一发会把调度线程叫醒重排）→ 等到点刷新列表。
- **期望**：列表最上面多一行「**自动** · 每日自动备份」，5 个文件；
  `logs/server.out` 里没有「自动备份失败」。验完把时刻改回 `04:00`。
- **当前状态**：代码路径 `run_due_once()` 单测钉过（`test_backup`），
  **真的由那条守护线程到点触发**这一步没在实机上见过。

## ★★ 上线时的数据迁移 —— 只有 `accounts.json` 这一件

用户 2026-09-06 说清楚了云上的现状：**只有 `accounts.json`**，而且那一份
**没有任何装备相关字段、也没有管理员账号**；`items.json` / `shop.json` /
`recipe.json` / `drops.json` 在云上根本不存在。

**四份运营配置：没有迁移这回事。** 第一次开服 `ensure_files()` 按**当前模板**
生成一份就是最新的（带中文名、没有 `days`）。⇒ 别为「老文件里的字段怎么办」
写代码，也别留兼容层。开发机上那几份是**本机数据**，手工改过就完了。

**`accounts.json`：要迁移，但代码早就写好了**，开服时自动跑，不用人动手：

| 启动步骤（`app.py`） | 干什么 |
|---|---|
| `_report_level_realign` → `realign_levels()` | 等级按当前曲线重算（V0.2 D150）|
| `_report_item_fields` → `ensure_item_fields()` | 补 `inventory`/`equipped`/`materials`；洗脏条目；收敛装备槽不变式；★ D51：旧的 `owned_characters` 转成角色卡、连 `character_unlock_all` 一起删掉；**键不存在时建默认管理员** |
| `_report_shop_config` → `_report_character_shelf()` | ★ D51：`shop.json` 已存在时不动它（D7），但**货架上没有角色卡就喊一声** —— 升级后要跑 `shop-backfill apply`（云上现在没有 `shop.json`，第一次开服生成的模板自带 11 张卡，这条不会响）|

★ 都**幂等**（铁律 11 要的那种「读盘时按默认值补齐」）：没得改就不写盘、
一行日志都不打，重启多少次都一样。

### ★★★ 2026-09-08：拿**真的云服存档**跑完了，干净（§44）

用户把线上那份取下来放在 `server/data-云服/`（71 个号，`schema_version 2`）。
照 `app.py` 的三步跑完：**丢失 0 / 值被改 0 / 等级重算 0**，只多出
`inventory` / `equipped` / `materials` 三个空字段，两个旧键全删干净；
连开三次服 `accounts.json` 的 **sha256 和 mtime 都没动**；四份配置按模板生成，
和开发机上那四份**逐字节相同**；建出 `admin` / `Admin123`。细节和数字在 §44。

⇒ **上线前不用再演练了。** 剩下两件要跟运营 / 玩家交代的事：

⚠ 上线后**第一件事是去 `/admin` 改掉 `Admin123`** —— 这个页面公网可达，
口令还是明文存的（D3）。启动日志里会把这句喊出来。

⚠ **36 个号原来选着付费角色**（109 号 9 个、110 号 5 个……），
上线那一刻他们进游戏会**变回泰尔**，且仓库里没有卡（云上 `owned_characters` 全空）。
这是 D51 要的行为，但**是玩家能直接感觉到的变化**，最好提前说一声。

（以前担心的「等级数字会跳」在这份数据上**不会发生** —— 云上已经在跑当前的
等级曲线了，`realign_levels()` 一个号都没改。）

## 已经答复过、不用再查的


**「合成界面点了不自动加购物车」不是 bug，是原版设计**（§33 ⑥）：
`0x0606` 的载荷是单个 i32，「조합」按钮 push 的是**当前选中那一条**的产物 id
（`0x45d738`）—— 合成一次只处理一件，没有购物车这个概念。
商店那边才有（`0x0602` 是 `i32 n + n×i32`）。

## 当前卡点

没有卡点。

---

## 下一步

**功能已经做完了。** 剩下的全是发版动作，按顺序：

1. ⏳ 验一次**自动备份**（上面 H-51，两分钟就能验完，不用等 4 点）；
   ⏳ 验 **M-52**（三个角色各穿各的）；
   ⏳ 验 **N-53**（穿火焰蝙蝠扔苹果弹打空不闪退）；
   ⏳ 验 **P-55**（说明文两段画不画得出来 —— 唯一有真风险的一条）；
   ⏳ 验 **P-56**（冲刺守护，被动）。
2. ★ **定版本号**：用户已把 `tools/build-ver.config` 改成 `0.3.0`（2026-09-09，
   工作区未提交）。版本号他自己管。
3. **打包实跑**（`tools\build.bat`，或 `build.bat -Client -Server -Zip`）——
   用户 2026-09-09 已打过一次 V0.3.0（`dist/` 里有 zip，manifest 也更新了），
   ★ 但那一份**早于 §46 和 §47 的修复**：装备互斥的 bug 还在里面，
   **`hook/bin/bshook.dll` 也是旧的**（穿火焰蝙蝠打空会闪退），**必须重打**
   （要是那份已经发出去了，就得起 0.3.1）。
4. 发 GitHub Release（`manifest.json` + zip）。
5. ★★ **最后**才把 `config/server-ClientFilter.config` 顶到新版本号
   （用户 2026-09-08 拍板要顶）。**顺序不能反** —— 老客户端被拒之后会拉起更新器，
   更新器是去 `releases/latest/download/manifest.json` 找包的；Release 还没发就顶门禁，
   玩家会卡在「被拒 → 更新失败」的循环里。★ 这个数要和 `build-ver.config` **一致**
   （门禁值 = 本批次版本，不是「历史最低可用版本」，见 README「版本号管理」）。
   ★ 这一版**必须顶**：`hook/bshook.c` 改了（七道等级门挪进 hook），
   老客户端拿不到那些补丁，低等级玩家会被原版等级门挡在房间外。
6. 上线后第一件事：`/admin` 改掉 `Admin123`；提前跟玩家说一声商城角色要买了
   （36 个号会变回泰尔，见上一节）。

**本版不做**（语义都查明了，§24）：修理（上行 `0x0604`）、卖出（`0x0603`）、
玩家互赠礼物（商店里的「赠送」，应答 `0x0509`；**GM 发到礼物盒**已做，§75 / D76）、
用道具（`0x060b`）、期限制物品。
❓ 还没逆的：`0x0503`（一个 bool + 提示框）、`0x0504`、服务端方向的
`0x0700`（处理器 `0x552a73`）和 `0x0702`（`0x552dca`）。

## 给接手会话的七句话

-4. ★★ **数据备份（D46 / §40）**：`server/databackup.py` 是数据层，**不许 import
   `web/` 或 `gameserver`**（在线连接那些事留在 `web/admin.py` 的路由里）。四把配置
   锁在 `shopcfg.write_lock()`，加锁顺序只在 `_SPECS` 一处定；拷贝 / 回滚必须持锁
   （Windows 读着文件会挡别人的 `os.replace`）；回滚后必须 `shopcfg.invalidate()`。
   加接口先问它属于哪一档 —— 这一页整页是系统管理员档。
   ★ 同一页的「下载日志」（D131 / §128）：`server/logpack.py` 同样是数据层；HTTP 那一半在
   `web/admin.py` 的 `_admin_logs_download`，**`end_headers()` 之后的异常必须在那个函数里
   自己兜**（`do_GET` 的兜底会往已发出的流里再写一份 500 JSON），出错**不发结束块**直接断。
   不拿数据锁、不做后台任务 —— 别「顺手」把它改成先打临时文件。

-3. ★★ **设计表在 `server/shopdefaults.py`**（D44 / D49 / D50）：它就是模板 ——
   第一次开服 `ensure_files()` 写出来的、和 `tools/gen_listing.py --apply` 写回
   `server/data/` 的是同一份。改默认数值改那边的表再 `--apply`（写前自动留 `.bak`），
   别手改四份 json 再回头对不上。`--apply` 默认只写 recipe / drops，items / shop
   和磁盘不一样时会跳过并提示（可能被管理页改过），加 `--all` 才覆盖。
   用户在管理页改过配置之后，再跑脚本前先把改动搬回表里。
   `RealDefaultsTests` 钉死了件数（495 / 122 / 52），改表就得改数。
   ★ 商城角色 = 仓库里的角色卡（D51）：`owned_characters()` 只看 `inventory`，
   别再往存档里加「角色开关」；hook 里 110 号的图标修补要留着（§43）。
   染色剂 / 突击技 / 宠物是**第一次**发给客户端，实机没验过。

-2. ★★ **管理页的保存已经不是整份覆盖了**（D36）：前台必须把 `base`
   （打开这一页时服务端给的那份**原文**）带回去，商店 / 合成还要带
   `cross_base`。往这条路上加东西时别把它们丢了 —— 丢了就退回「后保存的
   赢」，而且**没有任何报错**。合并逻辑在 `server/cfgmerge.py`（纯函数，
   `test_cfgmerge` 逐条钉）；「读磁盘→合并→写盘」外面那把
   `_config_locks` 也不能省（服务器是 `ThreadingMixIn`）。

-1. ★★ **中文名 / 等级 / 角色限定只有一个出处：`items.json`（物品库）**（D31）。
   要名字走 `shopcfg.name_of()` / `item_name()`，要门槛走 `rule_of()` /
   `item_rule()`。**永远别再往 `shop.json` / `recipe.json` 里加这三个字段** ——
   这个错已经犯过两遍（第一遍是等级和角色，第二遍是名字）。
   前台同理：JS 里查名字一律 `itemName(id)`，别读 `BYID[id].name`。

0. ★★ **§31 / §34 是本版最容易再犯的两个坑**：「探针在这个面板上没出现」
   只证明**这个面板**不读，别升成「死字段」；「按名字后缀分组」时那张后缀表
   是**观察到的**词汇不是规范，写完必须反查「有多少件一个后缀都没匹配上」。
1. ★★ **`0x0506` 的 `0` 是「合成成功」**（§33 ①）—— 和 `0x0502` 极性相反
   （那边 0 是「未定义的错误」）。写拒绝路径时兜底**绝不能填 0**。
2. ★★ **合成界面只有 8 个标签，没有武器也没有套装**（§33 ②）。
   往 `recipe.json` 加配方前先问「这个产物玩家点得到吗」。
3. ★★ **等级只有一个数**（§32 / D22）：显示、原版等级门、穿装备判定共用
   `[0x72e338]`。要绕某道门就去 `hook/bshook.c` 的
   `try_patch_player_level_gate()` 加一条，别去抬那个数。
4. **两处顺序都是硬要求**：`0x0501` 定义排在 `0x0500` / `0x0505` / `0x0604` /
   `0x0601` 前面（D20 / §33 ④）；一笔交易的**结果**（`0x0502` / `0x0506`）
   **排最后**（§30 ② / D28）。穿脱那条见 §24：**每一发都要回一发 `0x0604`**。
5. 实机万一界面不对，用控制通道的 `shop-reply off <opcode>` 二分
   （现在管八发：`0500` / `0501` / `0502` / **`0505`** / **`0506`** /
   `0508` / `0601` / `0604`），**不用改代码也不用重启**。
   `recipes [分类] [页] [角色] [等级]` 能不进游戏就看「合成这一页会发什么」。
