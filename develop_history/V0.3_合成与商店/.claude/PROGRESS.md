# V0.3 后半 PROGRESS —— 现在在哪

**只保留当前状态。** 做完的事从「正在做」挪走，不留历史；流水账进 `sessions/`。

最后更新：2026-09-05（会话 06：探针轮**负结果**，那三格商店面板不读（§26）；
用户报的「排序按钮点了没反应」= `0x0600` 的标志位没人管，已实装（§25）；
合成段线格式全逆通（§27）。**服务端已重启**，M5 那一轮可以做了）

---

## 现在在哪

| 里程碑 | 状态 |
|---|---|
| M0 进度管理体系 | ✅ **完成** |
| M1 数据基线 | ✅ **完成** |
| M2 存档扩展 | ✅ **完成** |
| M3 ★ spike：装备生效 | ✅ **完成，实机验证过**（§16）|
| M4 ★ spike：商店 UI 协议 | ✅ **完成，实机验证过** —— 货架真的画出来了（§21）|
| M5 商店购买 + 仓库装备 | 🟡 **代码全写完了**（货架 + 排序实机验过；买 `0x0602` / 穿脱 `0x0702`·`0x0703` **⏳ 等实机**）|
| M6 材料掉落 + 结算显示 | ✅ **完成，实机验证过** |
| M7 合成 | ⬜ **线格式全逆通了**（§27：`CompositionRule` 四个 ❓ 只剩一个死字段），代码没动 |
| M8 管理员网页 | ✅ **完成，逐条走查过** |
| M9 测试 · 兼容 · 打包 · 上线 | 🟡 打包 `$must` 已补上商店三件套；还差云上迁移演练的后一半 + 打包实跑 |

**一句话现状**：**D1 认下的那份协议风险已经拆完了**。商店段的**每一发上行**
现在都有语义（§20 / §24），没有 ❓ 剩下；货架已经实机画出来了。
服务端会回：货架 `0x0500`、装备清单 `0x0604`、空礼物清单 `0x0508`、
购买结果 `0x0502`，并处理穿 `0x0702` / 脱 `0x0703`。

---

## ✅ 已经实机落锤的

**货架整条链路**：图标 + 中文名 + 价格 +「金币」+「购买」按钮全画出来了，
翻页（`1 / 3`）、角色切换（`1120xxx → 2120xxx → 3120xxx`）、
武器 1/2/3 子分类过滤都对（§21），6 个顶级标签的分类 id 也全对上了（§22）。

**探针轮是负结果**：`ShopStock` 剩下那三格**商店面板一个都不读**（§26）
—— 探针值确实在包里，界面上就是不出现。别再花时间找它们了。

## ⏳ 待用户验证

★ **服务端 13:19 重启过**，客户端要重新登录。下面四条一次做完最省事：

1. **排序按钮**（§25 新实装）：商店右下角点「上市顺序」↔「基本顺序」。
   期望：货架**真的重排**。基本顺序第一格是「左轮 爆裂1 3000」，
   上市顺序第一格是「苹果雷 复合3 18000」。
   ★ 顺便告诉我**哪个标签给出哪个顺序** —— 「0=基本 / 1=上市」是按
   「进商店的默认值是 0」推的，还差这一句实测。
2. **人物 → 英雄**：现在应该是**空货架**（上一轮那个「列出一堆武器」的 bug）。
3. **仓库界面**：能不能看到自己已有的东西（`0x0604` 只验过「发出去没炸」）。
4. ★ **M5 那一轮**：买一把枪 → 金币有没有扣 → 仓库里有没有 →
   穿上 / 脱下 → **换一件抢同一个槽的**，看界面刷不刷新
   （§24 的计数器，少回一发就「点了没反应」）。

## 当前卡点

没有卡点。

---

## 下一步

1. **实机验 M5**（见上）。
2. **M7 合成**：线格式已经全逆通（§27），可以直接写。
   ⚠ `0x0606` 认的是**产物 itemId** ⇒ `recipe.json` 里 `result` 不许撞车。
3. 本版**不做**修理（上行 `0x0604`）、卖出（`0x0603`）、礼物（`0x0609`）、
   用道具（`0x060b`）—— 语义都查明了（§24），要做的时候不用重新逆。
4. ❓ `0x0504` 没逆（`0x0503` 是「一个 bool + 提示框」，多半是卖出的应答）。

## 给接手会话的三句话

1. **先读 `FINDINGS.md` §20 ~ §23** —— 商店那一整套协议在这四节里，
   §4 的老表已经作废（顶上有横幅）。§16 是装备生效的实机落锤。
2. **§19 记错过一次线格式，§20 把它改回来了。** 教训写在 §20 结尾：
   **「这个数不好看」不是判据** —— `0x50003` 看着不像 id，其实它就是
   `(组<<16)|序号` 编码的分类 id。
3. **`0x0500` 已实机验过；`0x0604` / `0x0508` / `0x0502` 和穿脱两发仍是 🔍静态。**
   实机万一界面不对，用控制通道的 `shop-reply off <opcode>` 二分，
   **不用改代码也不用重启**。
4. ★ 穿脱那条最容易踩的坑写在 §24：**每一发 `0x0702`/`0x0703` 都要回一发
   `0x0604`**（客户端拿它当计数器），失败路径也要回。
