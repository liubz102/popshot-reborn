# V0.3 后半 PROGRESS —— 现在在哪

**只保留当前状态。** 做完的事从「正在做」挪走，不留历史；流水账进 `sessions/`。

最后更新：2026-09-03（会话 01：M0 + M1 完成，下一步 M2）

---

## 现在在哪

| 里程碑 | 状态 |
|---|---|
| M0 进度管理体系 | ✅ **完成** |
| M1 数据基线 | ✅ **完成** |
| M2 存档扩展 | 🟡 **下一步** |
| M3 ★ spike：装备生效 | ⬜ |
| M4 ★ spike：商店 UI 协议 | ⬜ |
| M5 商店购买 + 仓库装备 | ⬜ |
| M6 材料掉落 + 结算显示 + 调试 bat | ⬜ |
| M7 合成 | ⬜ |
| M8 管理员网页 | ⬜ |
| M9 测试 · 兼容 · 打包 · 上线 | ⬜ |

**一句话现状**：物品表 + 三份运营配置都齐了，服务端起来会自动生成配置、
改完不用重启。**但还没碰存档、没发过一个包、没进过一次游戏。**

---

## 已经能用的东西

| 东西 | 是什么 |
|---|---|
| `tools/shopdata.py` + `update-shopdata.bat` | 离线提取器，一键重跑并自动跑测试 |
| `server/shop_items.json` | 产物，351 KB，**1870 件**。进 git、要进服务端包（M9 的 `Copy-ShopData`） |
| `server/shopdata.py` | 原版数据读取器。`get / exists / ownable / part_flag / bonus / conflicts / resolve_equipped / promotions` |
| `server/shopcfg.py` | 运营配置。三份 JSON 的**热重载 + 校验 + 首次生成**，中文名表也在这里 |
| `server/data/{shop,recipe,drops}.json` | 运营配置本体。启动时缺文件才生成，**已存在绝不覆盖**；已进 `.gitignore` |
| `server/test_shopdata.py`(38) + `test_shopcfg.py`(50) | 都已进 `run_tests.py` 的 `MODULES` |

**回归**：2300 项绿 × 两个运行时（CPython 3.14.3 + Win7 3.8.10）。
**已验**：服务端启动路径真的会生成三份配置，且第二次启动一行都不打（幂等）。

### 默认配置的内容（用户随时可改，改完不用重启）

- **`shop.json`** 141 条：上架 **63 件** D/R/F 武器（1/2/3 级 = 3000/8000/18000 金币，
  需 5/10/18 级）；材料和合成产物收在里面但 `listed=false`，只为有个中文名。
- **`recipe.json`** 35 条：四条产线 × 三个角色 ——
  佣兵铠甲（防）/ 大师铠甲（攻）/ 初阶凤凰 / 高阶凤凰。
  ★ 主题是照原版材料名定的：原版有「不死鸟之羽 / 之泪」，装备里正好有一整条
  「피닉스아머（凤凰铠甲）」产线。
- **`drops.json`** 24 条：原版基线 4 条（`Promotion.ini` 那 4 关）+ 按难度掉材料的扩展 + 对战少量。

---

## 下一步：M2 · 存档扩展

`server/account_store.py` 加三个账号字段 + `admin_accounts` + 一批读写方法 +
**幂等补齐**。细节见 `PLAN.md` M2。

★ 动手前先读 **铁律 11**（线上有真人在玩）和 **D5**（为什么不用版本号驱动的 migration）。

之后 **M3 是成败点**，越早让用户实机验越好。

---

## 当前卡点

无。

---

## ⏳ 待用户验证

暂无。**第一批在 M3**（装备生效）。格式：

```
### ⏳ [里程碑] 一句话说明
**怎么做**：……（具体到敲哪条命令、点哪个按钮）
**期望看到**：……
**怎么告诉我**：截图路径 / 日志片段 / 一句「行」「不行」
**当前状态**：等待中 / 已验证通过 / 已验证失败（→ 结论进 FINDINGS §x）
```

M3 / M4 那两批**是 spike，验不过要立刻停下来重新拍板**，不要继续往下写代码。

---

## 给接手会话的三句话

1. **先读 `FINDINGS.md` §1 / §3 / §4** —— 这三条是整个版本的地基。
   §11 / §12 / §13 是 M1 期间新增的坑（只有货架的物品 / 等级标记位 /
   `tools` 与 `server` 同名模块），动数据和写 `tools/` 脚本之前也要看。
2. **协议全是 🔍静态结论，一发都没实测过。** 写代码时按它做，
   但**别在文档里把它升级成 ✅实测**，除非真的实机跑过。
3. **线上有真人在玩**（铁律 11）。碰 `accounts.json` 的每一步都要先问
   「这一步如果崩在中间，老数据还在不在」。
