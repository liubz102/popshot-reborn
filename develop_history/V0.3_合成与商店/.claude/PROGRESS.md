# V0.3 后半 PROGRESS —— 现在在哪

**只保留当前状态。** 做完的事从「正在做」挪走，不留历史；流水账进 `sessions/`。

最后更新：2026-09-05（会话 05：**货架目录那个大未知落锤了** —— 请求是
`0x0600`；M4 第 2 步的三发应答已实装。下一步 = 等实机确认货架画得出来，
然后 M5 的购买 / 装备）

---

## 现在在哪

| 里程碑 | 状态 |
|---|---|
| M0 进度管理体系 | ✅ **完成** |
| M1 数据基线 | ✅ **完成** |
| M2 存档扩展 | ✅ **完成** |
| M3 ★ spike：装备生效 | ✅ **完成，实机验证过**（§16）|
| M4 ★ spike：商店 UI 协议 | ✅ **协议全部落锤**（§20 ~ §23）；三发应答已实装，**⏳ 等实机看界面** |
| M5 商店购买 + 仓库装备 | 🟡 **货架下发做完了**；购买（`0x0602`）/ 装备（上行 `0x0604`）**还没写** |
| M6 材料掉落 + 结算显示 | ✅ **完成，实机验证过** |
| M7 合成 | ⬜ 前置已就绪（`0x0505` 的线格式在 §23），没动 |
| M8 管理员网页 | ✅ **完成，逐条走查过** |
| M9 测试 · 兼容 · 打包 · 上线 | 🟡 打包 `$must` 已补上商店三件套；还差云上迁移演练的后一半 + 打包实跑 |

**一句话现状**：**D1 认下的那份协议风险已经拆完了**。「货架目录走哪个 opcode」
不再是未知 —— 客户端进商店的第一发 `0x0600` 就是它（§20），
`0x0500` 的三层嵌套结构、`0x0604` / `0x0508` / `0x0505` 的线格式全部逆到了字段级。
服务端现在**真的会回**货架 / 装备清单 / 空礼物清单。

---

## ⏳ 待用户验证（★ 这是现在唯一卡在用户那边的事）

**服务端和客户端已经起好了**（`start-debug.bat`，日志在 `logs/server.out`）。

**怎么做**：登录 → 进商店界面 → 看一眼 → 再点几下左边的分类标签。

**期望看到**：

1. 货架格子里**有商品**（图标 + 名字 + 价格），一页 8 个。
   现在上架的是 63 件 D/R/F 武器，价格 3000 / 8000 / 18000 金币；
2. 点「무기 / 武器」那类标签，格子跟着换；
3. 仓库 / 装备栏里能看到自己已有的东西；
4. **不崩、不卡、不断线**。

**怎么告诉我**：一句话说「画出来了 / 没画出来 / 崩了」就行；
如果画出来了，**顺手说一下价格显示在哪一格、有没有哪一格是空的**。

★ **第二件事（同一次进商店就能做完）**：我在服务端挂了个探针开关 ——
`ShopStock` 里还有 3 格没查明语义（§21）。人在商店界面里的时候我跑一句
`shelf-probe on`，客户端 10 秒内会自动重发货架请求，**界面上哪儿冒出
`777` / `888` / 「※探针※」，那一格就是哪个字段**。所以你进了商店之后
**先别退出来**，说一声，我开探针，你再看一眼。

---

## 当前卡点

没有卡点（M5 的购买 / 装备不依赖上面那条验证，可以并行写）。

---

## 下一步

1. **M5 剩下的一半**：`0x0602` 购买（校验上架/等级/角色/未持有/余额 →
   `spend_money` + `add_item` → 回 `0x0502` + 补发 `0x0600 gspRepMoney` +
   重发 `0x0604`）；上行 `0x0604` 装备 / 卸下（→ `set_equipped` →
   回显 + **重发 `0x030b`**，加成的唯一来源）。
2. **M7 合成**：`0x0605` → `0x0505`（线格式在 §23，`CompositionRule` 里
   还有 4 个 ❓ 字段 —— 和货架那三格一样，可以用探针法在界面上找）。
3. ❓ `0x0503` / `0x0504` 到底是什么还没查（`0x0503` 只是「一个 bool +
   提示框」，`0x0504` 没逆）。不挡路，用到再说。

---

## 给接手会话的三句话

1. **先读 `FINDINGS.md` §20 ~ §23** —— 商店那一整套协议在这四节里，
   §4 的老表已经作废（顶上有横幅）。§16 是装备生效的实机落锤。
2. **§19 记错过一次线格式，§20 把它改回来了。** 教训写在 §20 结尾：
   **「这个数不好看」不是判据** —— `0x50003` 看着不像 id，其实它就是
   `(组<<16)|序号` 编码的分类 id。
3. **下行三发仍然全是 🔍静态**（`0x0500` / `0x0604` / `0x0508` 一次都没验过）。
   实机万一界面不对，用控制通道的 `shop-reply off <opcode>` 二分，
   **不用改代码也不用重启**。
