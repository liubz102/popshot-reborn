# V0.3 后半 PROGRESS —— 现在在哪

**只保留当前状态。** 做完的事从「正在做」挪走，不留历史；流水账进 `sessions/`。

最后更新：2026-09-05（会话 05：货架实机通了；**穿装备走的是从没见过的
`0x0702`/`0x0703`**，`0x0604` 上行其实是修理（§24）。M5 代码全写完，等实机）

---

## 现在在哪

| 里程碑 | 状态 |
|---|---|
| M0 进度管理体系 | ✅ **完成** |
| M1 数据基线 | ✅ **完成** |
| M2 存档扩展 | ✅ **完成** |
| M3 ★ spike：装备生效 | ✅ **完成，实机验证过**（§16）|
| M4 ★ spike：商店 UI 协议 | ✅ **完成，实机验证过** —— 货架真的画出来了（§21）|
| M5 商店购买 + 仓库装备 | 🟡 **代码全写完了**（货架实机验过；买 `0x0602` / 穿脱 `0x0702`·`0x0703` **⏳ 等实机**）|
| M6 材料掉落 + 结算显示 | ✅ **完成，实机验证过** |
| M7 合成 | ⬜ 前置已就绪（`0x0505` 的线格式在 §23），没动 |
| M8 管理员网页 | ✅ **完成，逐条走查过** |
| M9 测试 · 兼容 · 打包 · 上线 | 🟡 打包 `$must` 已补上商店三件套；还差云上迁移演练的后一半 + 打包实跑 |

**一句话现状**：**D1 认下的那份协议风险已经拆完了**。商店段的**每一发上行**
现在都有语义（§20 / §24），没有 ❓ 剩下；货架已经实机画出来了。
服务端会回：货架 `0x0500`、装备清单 `0x0604`、空礼物清单 `0x0508`、
购买结果 `0x0502`，并处理穿 `0x0702` / 脱 `0x0703`。

---

## ✅ 2026-09-05 实机结果（M4 收尾）

**货架整条链路通了**：图标 + 中文名 + 价格 +「金币」+「购买」按钮全画出来了，
**翻页**（`1 / 3`）、**角色切换**（`1120xxx → 2120xxx → 3120xxx`）、
**武器 1/2/3 子分类过滤**都对（§21）。`0x0604` / `0x0508` 也发出去了，客户端没炸。

**顺带把 6 个顶级标签的分类 id 全对上了**（§22）。

**修掉一个 bug**：点「人物 → 英雄」列出一堆武器 —— 服务端把 `分类 = 0`
当成了「全部」，其实那是英雄标签的真 id。已改（`shop.CATEGORY_ALL = -1`）。

**「道具」标签空是预期的**：现在 `shop.json` 里上架的只有 63 件武器。

---

## ⏳ 待用户验证

1. **探针那一轮**（服务端已 `shelf-probe on`，不用重启）：人在商店界面里
   点一下标签让客户端重发，看格子上哪儿冒出 `777` / `888` / 「※探针※」
   —— 那一格就是 `ShopStock` 剩下三个 ❓ 字段里的哪一个。
2. 顺手点一下「装备」和「技能」标签（agent 自己看日志对号，用户不用报），
   把 §22 里那两条 🤔 升成 ✅。
3. **仓库界面**里能不能看到自己已有的东西（`0x0604` 只确认了「发出去没炸」，
   没确认「画对了」）。
4. ★ **M5 那一轮**（要重启服务端之后做）：买一把枪 → 看金币有没有扣、
   仓库里有没有 → 在仓库里穿上 / 脱下 → **换一件抢同一个槽的**，
   看界面刷不刷新（§24 的计数器，少回一发就「点了没反应」）。

---

## 当前卡点

没有卡点。★ 分类 0 的修复**要重启服务端才生效**（铁律 7）——
等上面那一轮实机做完再重启，别把用户从商店里踢出来。

---

## 下一步

1. **实机验 M5**：买一把枪、在仓库里穿上脱下。★ 穿脱那条最容易出事的是
   「**一发不落**」（§24 的计数器）—— 换一件抢槽的装备时留意界面刷不刷新。
2. **M7 合成**：`0x0605` → `0x0505`（线格式在 §23，`CompositionRule` 里
   还有 4 个 ❓ 字段 —— 和货架那三格一样，可以用探针法在界面上找）。
3. 本版**不做**修理（上行 `0x0604`）、卖出（`0x0603`）、礼物（`0x0609`）、
   用道具（`0x060b`）—— 语义都查明了（§24），要做的时候不用重新逆。
4. ❓ `0x0504` 没逆（`0x0503` 是「一个 bool + 提示框」，多半是卖出的应答）。

---

## 给接手会话的三句话

1. **先读 `FINDINGS.md` §20 ~ §23** —— 商店那一整套协议在这四节里，
   §4 的老表已经作废（顶上有横幅）。§16 是装备生效的实机落锤。
2. **§19 记错过一次线格式，§20 把它改回来了。** 教训写在 §20 结尾：
   **「这个数不好看」不是判据** —— `0x50003` 看着不像 id，其实它就是
   `(组<<16)|序号` 编码的分类 id。
3. **`0x0500` 已实机验过；`0x0604` / `0x0508` / `0x0502` 和穿脱两发仍是 🔍静态。**
   实机万一界面不对，用控制通道的 `shop-reply off <opcode>` 二分，
   **不用改代码也不用重启**。
4. ★ 穿脱那条最容易踩的坑写在 §24：**每一发 `0x0702`/`0x0703` 都要回一发
   `0x0604`**（客户端拿它当计数器），失败路径也要回。
